<!-- docs/wiki/index.md — auto-generated by build-wiki-index.py. Do not hand-edit. -->

# pocketpaw — Wiki Index

_893 articles · indexed at `69569b5e486e` · generated 2026-04-24T04:26:33+00:00_

Auto-generated from each article's YAML frontmatter. See [README.md](./README.md) for the update workflow.

## By Category (785 categories)

### `A2A protocol` — 7 article(s)

- [A2A Client: HTTP Client for Delegating Tasks to External A2A Agents](./a2a-client-http-client-for-delegating-tasks-to-external-a2a-agents.md) — `A2AClient` is an async HTTP client that allows PocketPaw to communicate with external A2A-compatible agents. It supports blocking task submission, SSE streaming, task polling, cancellation, and Agent Card discovery — with connection sharing, TTL-cached Agent Cards, and transparent wrapping of direct-Message responses into the Task model.
- [A2A Client Tests: HTTP Operations, Context Manager Reuse, Streaming, Delegate Tool, and SSRF Protection](./a2a-client-tests-http-operations-context-manager-reuse-streaming-delegate-tool-a.md) — Tests for the PocketPaw Agent-to-Agent (A2A) client library, covering HTTP task operations (send, get, cancel, stream), response error handling, agent card fetching with caching, the `A2ADelegateTool` multi-turn wrapper, and SSRF protection that blocks requests to private IP ranges and invalid schemes.
- [A2A JSON-RPC Dispatcher: Method Registration and Request Routing](./a2a-json-rpc-dispatcher-method-registration-and-request-routing.md) — `A2ADispatcher` parses incoming JSON-RPC 2.0 requests, validates the envelope, and routes them to registered async handlers. It supports both blocking dispatch and SSE streaming dispatch, batch requests, and consistent error handling — with a deliberate separation between stream-only and non-stream methods.
- [A2A Protocol Error Codes and JSON-RPC 2.0 Error Handling](./a2a-protocol-error-codes-and-json-rpc-20-error-handling.md) — This module defines the full error code registry for the A2A protocol layer — combining standard JSON-RPC 2.0 error codes with A2A-specific extensions — and provides the `JSONRPCError` exception class and helper functions for building well-formed error and success response envelopes.
- [A2A Protocol Pydantic Models: Tasks, Messages, Parts, and Agent Cards](./a2a-protocol-pydantic-models-tasks-messages-parts-and-agent-cards.md) — This module defines all Pydantic models for the A2A protocol (v0.2.5+), including the task lifecycle state machine, typed content parts (text/file/data), message and artifact models, request parameter schemas, the Agent Card capabilities manifest, and JSON-RPC 2.0 envelope models with streaming event types.
- [A2A Server Tests: Models, State Machines, JSON-RPC Dispatch, and Task Lifecycle](./a2a-server-tests-models-state-machines-json-rpc-dispatch-and-task-lifecycle.md) — Extensive tests for the PocketPaw A2A server implementation covering Pydantic model serialisation with type discriminators, TaskState transition validation, JSON-RPC 2.0 dispatcher behaviour (parse errors, batch requests, method routing), and the full task lifecycle (submit, poll, stream, cancel) against a minimal in-memory FastAPI app.
- [Agent-to-Agent (A2A) Protocol Module: Public API and Roadmap](./agent-to-agent-a2a-protocol-module-public-api-and-roadmap.md) — The `pocketpaw.a2a` package implements Google's Agent-to-Agent (A2A) protocol, allowing PocketPaw to act as both an A2A server (accepting tasks from external agents) and an A2A client (delegating tasks to external agents). This `__init__.py` re-exports the full public surface and documents the three-phase implementation roadmap.

### `ABAC` — 4 article(s)

- [ABAC Config Tests: Rule Loading and Allow/Deny Logic](./abac-config-tests-rule-loading-and-allowdeny-logic.md) — This module tests the Attribute-Based Access Control (ABAC) configuration layer for the files subsystem, covering YAML rule loading, the allow logic for tagged vs. untagged entries, attribute matching, and the deny-override behavior when a file carries multiple tags.
- [ABAC Rule Loader and Evaluator for File Access Control](./abac-rule-loader-and-evaluator-for-file-access-control.md) — The `abac_config.py` module implements an Attribute-Based Access Control (ABAC) engine for the files domain, loading restriction rules from a YAML configuration file and evaluating them against per-request user attributes and file entry tags. Rules can only restrict access - they cannot grant permissions beyond the RBAC baseline.
- [Browse Mount - Per-Mount Paginated File Listing with ABAC and Capability Derivation](./browse-mount-per-mount-paginated-file-listing-with-abac-and-capability-derivatio.md) — The `browse_mount` function handles a single paginated directory listing request against one virtual mount, applying ABAC filtering and deriving fine-grained capabilities for each visible entry. It is the core read path for the Files Tab browser.
- [Files Domain Package - Unified File Browser and Provider Registry](./files-domain-package-unified-file-browser-and-provider-registry.md) — The `ee/cloud/files/__init__.py` package entrypoint assembles and re-exports the complete public API for the files domain: the provider registry, ABAC schema types, pagination primitives, and the router factory function. It is the single import surface consumers use to mount the files subsystem.

### `AI capabilities` — 1 article(s)

- [ImageGenerateTool: Text-to-Image Generation via Google Gemini](./imagegeneratetool-text-to-image-generation-via-google-gemini.md) — The `ImageGenerateTool` gives the PocketPaw agent the ability to generate images from text prompts using the Google Gemini image generation API (internally called "Nano Banana"). Generated images are saved to a deterministic directory under the PocketPaw config folder with UUID-based filenames, and the tool returns the local file path so the agent can chain the result with `OpenExplorerTool` or other file-handling tools.

### `AI pipeline` — 2 article(s)

- [Knowledge Compiler: LLM-Powered Raw Text to Structured Wiki Articles](./knowledge-compiler-llm-powered-raw-text-to-structured-wiki-articles.md) — The knowledge compiler is the intelligence layer of PocketPaw's knowledge engine — it sends raw documents to an LLM with a strict prompt that elicits a structured JSON article with title, summary, content, concepts, and categories. Robust JSON parsing handles markdown fences and partial LLM output that would otherwise cause silent data loss.
- [Knowledge Engine: Unified Ingest-Compile-Index-Search Pipeline](./knowledge-engine-unified-ingest-compile-index-search-pipeline.md) — The `KnowledgeEngine` class is the single entry point for all knowledge management operations — ingesting text, URLs, and files, compiling them into structured wiki articles via LLM, indexing concepts and backlinks, and searching with BM25. It is scope-aware, allowing separate knowledge bases per agent, workspace, or pocket.

### `API` — 64 article(s)

- [Agent Status API — SSE Streaming and Debounced Snapshot Polling](./agent-status-api-sse-streaming-and-debounced-snapshot-polling.md) — The agent status router exposes real-time agent state — global status plus per-session breakdowns — through both a polling REST endpoint and a Server-Sent Events stream. It uses fingerprint-based change detection and a 1-second debounce to avoid flooding SSE clients with identical snapshots during high-frequency tool execution.
- [Agent Status API Tests: Auth, Snapshot Shape, Version Tracking, and CLI Formatting](./agent-status-api-tests-auth-snapshot-shape-version-tracking-and-cli-formatting.md) — Tests for PocketPaw's agent status API endpoint, covering authentication via header and query parameter, snapshot structure validation against the API contract, version-based change detection for efficient polling, and CLI duration formatting helpers.
- [Agents Domain FastAPI Router](./agents-domain-fastapi-router.md) — The agents router exposes the full HTTP API surface for managing AI agents within a workspace — CRUD, knowledge ingestion, profile picture upload, backend discovery, and scope assignment — with license gating and permission checks applied at the router and dependency level.
- [Agents Domain Pydantic Schemas](./agents-domain-pydantic-schemas.md) — The agents schemas module defines the Pydantic request and response models for creating, updating, and querying agents, including server-side scope normalisation validators that enforce the same grammar rules as the frontend ScopePicker regardless of how the API is called.
- [API Backward Compatibility and Versioned Endpoint Tests](./api-backward-compatibility-and-versioned-endpoint-tests.md) — This module verifies that both the legacy `/api/` endpoints and the versioned `/api/v1/` endpoints in the PocketPaw dashboard respond with HTTP 200, guarding against breaking changes during the v1 API migration. It also checks that OpenAPI documentation endpoints remain accessible.
- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.
- [API Keys Router — Long-Lived Key CRUD with One-Shot Plaintext Return](./api-keys-router-long-lived-key-crud-with-one-shot-plaintext-return.md) — The API keys router provides create, list, revoke, and rotate operations for long-lived programmatic access keys. A critical security property of the create endpoint is that the plaintext key is returned exactly once — subsequent reads only expose a short prefix — mirroring industry-standard patterns like GitHub personal access tokens.
- [API v1 Router Aggregation and Lazy Module Mounting](./api-v1-router-aggregation-and-lazy-module-mounting.md) — The `__init__.py` for PocketPaw's v1 API layer is responsible for registering every domain router onto the FastAPI application via a single `mount_v1_routers()` function. It uses deferred imports to sidestep circular-dependency problems that arise when a large plugin-style codebase wires itself up at startup.
- [API v1 Schemas Package — Pydantic Schema Namespace](./api-v1-schemas-package-pydantic-schema-namespace.md) — The `schemas/__init__.py` file marks the `pocketpaw.api.v1.schemas` directory as a Python package. It contains no exports, serving purely as a namespace boundary that groups all Pydantic request and response models for the v1 API.
- [Auth API Tests: Session Exchange, Cookie Login, Rate Limiting, and QR Code](./auth-api-tests-session-exchange-cookie-login-rate-limiting-and-qr-code.md) — This test module comprehensively covers PocketPaw's `/api/v1/auth` router, verifying the master-token-to-session exchange, cookie-based login with correct Secure flag handling under reverse proxies, logout, token regeneration, QR code generation, and IP-based rate limiting on all three auth endpoints.
- [Auth Domain FastAPI Router — Profile, Avatar, and Workspace Management](./auth-domain-fastapi-router-profile-avatar-and-workspace-management.md) — The auth router assembles PocketPaw Enterprise's full authentication HTTP surface by composing fastapi-users' built-in auth/register routes with custom profile management, avatar upload, and workspace-switching endpoints.
- [Auth Domain Package Re-Exports](./auth-domain-package-re-exports.md) — The auth domain `__init__.py` is a backward-compatibility shim that re-exports the entire public surface of `auth.core` and `auth.router` from the `ee.cloud.auth` namespace, so existing callers don't need to update their imports if the internal module structure changes.
- [Auth Domain Request and Response Schemas](./auth-domain-request-and-response-schemas.md) — The auth schemas module defines the three Pydantic models used by the auth router: a profile update body, a workspace-switch body, and a user response envelope that shapes how user records are serialised for API consumers.
- [Auth Router — Session Exchange, Cookie Login, QR Code, and Token Regeneration](./auth-router-session-exchange-cookie-login-qr-code-and-token-regeneration.md) — The auth router handles all first-party authentication flows: exchanging a master access token for a time-limited session token, setting and clearing HTTP-only session cookies, generating QR login codes for mobile pairing, and regenerating the master token when credentials need to be rotated. It uses constant-time comparison to close a timing oracle vulnerability in master-token validation.
- [Backends API Tests: Listing, Availability Checks, and Safe Install Subprocess Handling](./backends-api-tests-listing-availability-checks-and-safe-install-subprocess-handl.md) — This test file covers PocketPaw's `/api/v1/backends` router, verifying that the backend registry is exposed correctly, that the `_check_available` helper correctly detects missing imports, and that the async install endpoint handles subprocess timeouts and stderr secret redaction without leaking sensitive data.
- [Backends Router — Agent Backend Discovery and Auto-Install](./backends-router-agent-backend-discovery-and-auto-install.md) — The backends router exposes the registry of agent backends (Claude, Codex CLI, OpenCode, etc.) to the dashboard and API clients, including real-time availability checking that verifies both Python SDK imports and CLI binary presence. An install endpoint allows auto-installing missing backend SDKs via pip without requiring manual server restarts.
- [Browse Mount - Per-Mount Paginated File Listing with ABAC and Capability Derivation](./browse-mount-per-mount-paginated-file-listing-with-abac-and-capability-derivatio.md) — The `browse_mount` function handles a single paginated directory listing request against one virtual mount, applying ABAC filtering and deriving fine-grained capabilities for each visible entry. It is the core read path for the Files Tab browser.
- [Channels API Tests: Status, Config Save, Toggle, and Extras Check](./channels-api-tests-status-config-save-toggle-and-extras-check.md) — This test file covers PocketPaw's `/api/v1/channels` router, which exposes status, configuration, and lifecycle management for messaging channel adapters such as Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It also covers `/extras/check`, the endpoint that detects whether optional channel dependencies are installed.
- [Channels Router — Multi-Platform Adapter Status, Configuration, and Dynamic Toggle](./channels-router-multi-platform-adapter-status-configuration-and-dynamic-toggle.md) — The channels router manages PocketPaw's suite of messaging adapters — Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat — through a unified REST interface. It allows the dashboard to inspect adapter state, save credentials, start or stop adapters at runtime without restarting the server, retrieve WhatsApp QR codes, and install optional channel dependencies on demand.
- [Chat Router — HTTP Send, SSE Streaming, and Session Bridge for Agent Conversations](./chat-router-http-send-sse-streaming-and-session-bridge-for-agent-conversations.md) — The chat router enables external clients to drive PocketPaw agent sessions over plain HTTP, using Server-Sent Events for streaming responses. The `_APISessionBridge` class connects the internal message bus to an SSE stream, ensuring that chat sessions initiated via the HTTP API reuse the same AgentLoop pipeline as native channel adapters.
- [Chat Schemas — Request, Response, and Context Models](./chat-schemas-request-response-and-context-models.md) — The chat schemas define the core request and response contracts for PocketPaw's chat API, including file and pocket context injection, camelCase/snake_case aliasing to prevent session ID loss, and the SSE chunk model for streaming responses.
- [Chat Schemas - Request, Response, and WebSocket Message Models](./chat-schemas-request-response-and-websocket-message-models.md) — This module defines all Pydantic models used by the chat domain: REST request bodies, response shapes, cursor pagination, and WebSocket message envelopes. Centralising schemas here decouples validation logic from both the router and service layers.
- [Cloud Agents Domain Package Entry Point](./cloud-agents-domain-package-entry-point.md) — The `ee/cloud/agents/__init__.py` is a minimal shim that re-exports the agents FastAPI router, making the domain's public surface importable from `ee.cloud.agents` without callers needing to know the internal module structure.
- [Common API Response Schemas — Shared Base Types](./common-api-response-schemas-shared-base-types.md) — The common schemas define PocketPaw's shared response envelope types: a base class with ORM compatibility, a standard error shape, a generic paginated list, and two convenience success models. These types appear across every domain in the v1 API.
- [Connector Status Route Tests: State Transitions, Pocket Isolation, and Secret Non-Leakage](./connector-status-route-tests-state-transitions-pocket-isolation-and-secret-non-l.md) — This test file locks down PocketPaw's connector status route contract — the full state machine from disconnected through connected to expired, strict per-pocket credential isolation so one pocket cannot see another's connection state, and the guarantee that OAuth secrets passed at connect time never appear in status responses.
- [Connectors Router — Data Source CRUD, Credential Management, and Action Execution](./connectors-router-data-source-crud-credential-management-and-action-execution.md) — The connectors router provides a structured REST interface over PocketPaw's `ConnectorRegistry`, allowing the dashboard and enterprise UI to connect agents to external data sources (databases, APIs, cloud services) and execute actions against them. A structured status endpoint was added specifically to feed the `ConnectorCard` UI component with credential state and sync metadata.
- [Deep Work API: FastAPI Endpoints for Project Orchestration](./deep-work-api-fastapi-endpoints-for-project-orchestration.md) — deep_work/api.py defines the FastAPI router mounted at `/api/deep-work/*`, exposing REST endpoints for the full Deep Work project lifecycle — goal parsing, project submission, plan retrieval, approval, pause/resume/cancel, and per-task skip/retry operations.
- [EE Uploads API Router: Workspace-Scoped Upload and Folder Endpoints](./ee-uploads-api-router-workspace-scoped-upload-and-folder-endpoints.md) — This FastAPI router exposes all upload and folder management endpoints for the EE cloud layer, enforcing workspace scope and per-file access control on every operation. It manages the full file lifecycle — upload, download URL generation, folder CRUD, file move, and soft delete — backed by EEUploadService and FolderStore.
- [Enterprise Cloud Application Bootstrap and Router Mounting](./enterprise-cloud-application-bootstrap-and-router-mounting.md) — `ee/cloud/__init__.py` is the application assembly point for the PocketPaw Enterprise REST API. It provides two entry points: `init_realtime()` which wires up the in-process EventBus, and `mount_cloud()` which registers every domain router, the error handler, static file mounts, and lifecycle hooks onto a FastAPI app instance.
- [Events SSE Router — Real-Time System Event Streaming for External Clients](./events-sse-router-real-time-system-event-streaming-for-external-clients.md) — The events router provides a Server-Sent Events endpoint that external clients (such as the Tauri desktop app) can subscribe to for real-time system events without connecting to the WebSocket. It delivers the same event types as the WebSocket — tool_start, tool_result, thinking, error, health_update, inbox_update — via a keepalive-aware HTTP stream.
- [Files API Tests: Browse, Download, Zip, Write, and Path Traversal Defence](./files-api-tests-browse-download-zip-write-and-path-traversal-defence.md) — This test file covers PocketPaw's `/api/v1/files` router, which exposes local filesystem operations (browse, single-file download, directory zip download, and file write) to the dashboard. It places particular emphasis on path traversal prevention, file jail enforcement, and zip bomb mitigations.
- [Files Router — Scoped File Browser, Download, and Write with Path Traversal Guards](./files-router-scoped-file-browser-download-and-write-with-path-traversal-guards.md) — The files router gives the PocketPaw dashboard a full file browser over the agent's accessible filesystem: directory listing, file content serving, single-file download, directory-as-zip download, and file writing. Security hardening in April 2026 added scope guards and symlink filtering to prevent path traversal attacks identified in issues #884 and #886.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [Health Router — System Health, Security Audits, and Self-Audit Reports](./health-router-system-health-security-audits-and-self-audit-reports.md) — The health router aggregates PocketPaw's observability surface into a single API domain: version information, health engine summaries, error log management, on-demand health checks, security audit runs, and a scheduled self-audit report system. It was extracted from the monolithic `dashboard.py` to give each concern a clean, testable home.
- [Identity API Tests: Reading and Partially Updating Agent Identity Files](./identity-api-tests-reading-and-partially-updating-agent-identity-files.md) — This test file covers PocketPaw's `/api/v1/identity` router, which exposes the agent's persistent identity configuration — the markdown files that define personality, soul, style, instructions, and user profile — as a readable and partially updatable API resource.
- [Identity Router — Agent Identity File Management via REST](./identity-router-agent-identity-file-management-via-rest.md) — The identity router exposes PocketPaw's five agent identity files (IDENTITY.md, SOUL.md, STYLE.md, INSTRUCTIONS.md, USER.md) through a simple read/write REST interface, allowing dashboard users to customize the agent's persona, communication style, and behavioral instructions without editing files directly. Changes take effect on the next inbound message without requiring a server restart.
- [Intentions API Tests: CRUD, Toggle, and On-Demand Execution](./intentions-api-tests-crud-toggle-and-on-demand-execution.md) — This test file covers PocketPaw's `/api/v1/intentions` router, which manages proactive agent intentions — scheduled or triggered actions the agent initiates autonomously. It verifies the full CRUD lifecycle, enable/disable toggling, input validation, and on-demand execution.
- [Intentions Router — Proactive Agent Scheduling CRUD and Manual Trigger](./intentions-router-proactive-agent-scheduling-crud-and-manual-trigger.md) — The intentions router manages PocketPaw's proactive behavior system — scheduled or event-triggered agent prompts that run autonomously without user initiation. It exposes full CRUD operations plus a toggle (enable/disable without deleting) and a run-now action that bypasses the scheduler for immediate execution.
- [Kits Router — PawKit Installation, Catalog, and Command Center Data Resolution](./kits-router-pawkit-installation-catalog-and-command-center-data-resolution.md) — The kits router manages PawKits — YAML-defined bundles of tools, skills, and UI panels that extend a PocketPaw agent's capabilities. It provides a catalog browser with installed-status flags, CRUD operations for installed kits, and a data-resolution endpoint that powers command center panels by fetching live data from configured sources.
- [Knowledge Router: Multi-Scope Listing, Filtering, and Cross-Workspace Isolation Tests](./knowledge-router-multi-scope-listing-filtering-and-cross-workspace-isolation-tes.md) — This integration test suite exercises the `/api/v1/knowledge/articles` FastAPI endpoint end-to-end, verifying that it merges workspace and agent scopes, supports `agent_id` filtering, rejects cross-workspace queries with HTTP 403, and returns empty results (not a data leak) for unknown agent IDs.
- [Long-Term Memory API — CRUD, Settings, and Stats](./long-term-memory-api-crud-settings-and-stats.md) — The memory router exposes endpoints for managing PocketPaw's long-term memory store, configuring which backend is active (file vs. mem0), and retrieving usage statistics. It bridges the REST surface to the pluggable MemoryManager, ensuring settings changes take effect immediately by flushing cached singletons.
- [MCP Router — Model Context Protocol Server Management and OAuth Integration](./mcp-router-model-context-protocol-server-management-and-oauth-integration.md) — The MCP router manages PocketPaw's connections to external Model Context Protocol servers — adding, removing, toggling, and testing MCP server configurations at runtime. It also provides a preset catalog for one-click MCP server installation, an OAuth callback endpoint for providers that require browser-based authorization, and auto-installation of Google Workspace skills when the GWS MCP preset is activated.
- [MCP Router Tests: Server Management and OAuth Callback Handling](./mcp-router-tests-server-management-and-oauth-callback-handling.md) — This test file covers PocketPaw's `/api/v1/mcp` router, which manages Model Context Protocol (MCP) server connections — add, remove, toggle, and status — and handles the OAuth callback that completes browser-based MCP server authentication flows.
- [Memory API Tests: Settings, Long-Term Memory CRUD, and Backend Stats](./memory-api-tests-settings-long-term-memory-crud-and-backend-stats.md) — This test file covers PocketPaw's `/api/v1/memory` router, which exposes configuration for the memory backend (file store vs. mem0), CRUD operations over long-term memory entries, and per-backend statistics.
- [Memory Tools: RememberTool, RecallTool, and Session List API](./memory-tools-remembertool-recalltool-and-session-list-api.md) — Tests for the agent-facing memory tools (`remember` and `recall`) and the session list REST endpoint. Covers tool schema validation, content persistence, tag filtering, truncation behavior, and the API's ability to enumerate stored sessions.
- [Mission Control REST API: FastAPI Endpoint Tests](./mission-control-rest-api-fastapi-endpoint-tests.md) — Tests for the FastAPI router that exposes Mission Control functionality over HTTP, covering agent, task, message, document, activity, stats, and notification endpoints. Uses FastAPI's TestClient with monkeypatched singletons for full request/response validation without a live server.
- [MongoMemoryStore Session Index Shape and Filtering Contract Tests](./mongomemorystore-session-index-shape-and-filtering-contract-tests.md) — These tests verify `_load_session_index_async` in MongoMemoryStore, which powers the `GET /sessions/runtime` endpoint. They pin the exact dict shape, filtering of group and soft-deleted sessions, channel inference from session key prefix, and empty title coercion.
- [OAuth Integration Router — Google and Spotify Service Authorization](./oauth-integration-router-google-and-spotify-service-authorization.md) — The OAuth integrations router handles the user-facing authorize and callback flow for connecting PocketPaw to third-party services like Gmail, Google Calendar, Google Drive, Google Docs, and Spotify. It was extracted from the dashboard router so the same flows work in both dashboard and headless serve modes.
- [Plan Mode API — Human Approval Gate for Agent Actions](./plan-mode-api-human-approval-gate-for-agent-actions.md) — The plan mode router provides two endpoints that let users approve or reject an agent's proposed execution plan before any actions are taken. This human-in-the-loop gate prevents agents from running irreversible operations without explicit confirmation.
- [Plan Mode API Tests: Approve and Reject Active Agent Plans](./plan-mode-api-tests-approve-and-reject-active-agent-plans.md) — This test file covers PocketPaw's `/api/v1/plan` router, which lets the dashboard approve or reject an agent's proposed action plan before execution. It verifies the session-keyed lookup, the not-found response when no active plan exists, and input validation.
- [Pocket Chat Router — Context-Aware Workspace Creation and Interaction](./pocket-chat-router-context-aware-workspace-creation-and-interaction.md) — The pockets router is PocketPaw's dedicated endpoint for creating and interacting with pocket workspaces — themed dashboards of widgets, charts, and data panels. It streams responses via SSE, injects dynamic Ripple widget documentation for new pockets, and normalises three distinct AI output formats into render-ready Ripple specs.
- [Pocket Layout Routes: Export, Template Create/List, and Workspace Scoping Tests](./pocket-layout-routes-export-template-createlist-and-workspace-scoping-tests.md) — This integration test suite covers three new routes on the pockets router that implement the layout save/share surface: `POST /pockets/{id}/export-layout` (YAML serialization), `POST /pockets/templates` (template creation), and `GET /pockets/templates` (workspace-scoped listing). It verifies the round-trip fidelity, metadata override support, cross-workspace isolation, and malformed YAML handling.
- [Reminders API — Natural Language Scheduling](./reminders-api-natural-language-scheduling.md) — The reminders router provides list, add, and delete operations for time-based reminders. Reminders are created from natural language strings and processed by the scheduler, which handles parsing relative time expressions like "in 5 minutes" or "tomorrow at 9am".
- [Reminders API Tests: Natural Language Scheduling, Listing, and Deletion](./reminders-api-tests-natural-language-scheduling-listing-and-deletion.md) — This test file covers PocketPaw's `/api/v1/reminders` router, which accepts natural-language reminder messages, parses them into scheduled events via the scheduler, and exposes list and delete operations with human-readable time-remaining formatting.
- [Remote Tunnel API — Cloudflare Tunnel Management](./remote-tunnel-api-cloudflare-tunnel-management.md) — The remote router exposes three endpoints for controlling a Cloudflare tunnel that makes the locally running PocketPaw instance accessible over the public internet. This lets users interact with their companion from mobile devices or share access without configuring port forwarding.
- [Remote Tunnel API Tests: Cloudflare Tunnel Status, Start, and Stop](./remote-tunnel-api-tests-cloudflare-tunnel-status-start-and-stop.md) — This test file covers PocketPaw's `/api/v1/remote` router, which manages a Cloudflare tunnel that exposes the local PocketPaw instance to the internet. It verifies status reporting, tunnel start with error handling for missing binaries, and graceful stop.
- [Runtime Audit API Integration Tests — Workspace Rollup, Search, CSV Export, and Security](./runtime-audit-api-integration-tests-workspace-rollup-search-csv-export-and-secur.md) — This test module validates the canonical `/runtime/audit` endpoint introduced in Cluster C/PR4, covering workspace-scoped filtering, full-text search with length caps, CSV export, SQL injection resistance, and the deprecation forwarding behavior of the legacy `/audit` alias. It exercises both the new runtime router and the legacy router mounted together to prove they share the same underlying AuditStore and scope guard.
- [Session Management API Tests — List, Delete, Title Update, and Search](./session-management-api-tests-list-delete-title-update-and-search.md) — This test module covers all four operations of the `/api/v1/sessions` router: listing sessions with pagination, deleting by ID, updating titles, and full-text searching session history. It uses mock-based isolation of the underlying memory store to prevent filesystem side effects while exercising the router's error-handling and edge-case paths.
- [Settings API Security Tests — Secret Field Filtering and Immutable Field Guards](./settings-api-security-tests-secret-field-filtering-and-immutable-field-guards.md) — This test module validates two critical security properties of the `/api/v1/settings` router: that GET never returns API keys or tokens (regression for a v0.4.16 leak), and that PUT rejects writes to security-critical immutable fields like `file_jail_path` and `bypass_permissions`. It also verifies normal settings read and write paths work correctly for non-secret, non-immutable fields.
- [Skills API Security and Installation Tests — Input Validation, Injection Prevention, and Audit Logging](./skills-api-security-and-installation-tests-input-validation-injection-prevention.md) — This test module covers the full lifecycle of the skills management API: listing, searching, installing from GitHub sources, removing, and reloading. The installation tests are especially dense, exercising a whitelist-based input validator to block path traversal, shell injection, null bytes, and over-segmented source strings before any subprocess is spawned.
- [System and Usage Metrics API](./system-and-usage-metrics-api.md) — The metrics router provides two endpoints: one that reports real-time system resource usage (CPU, RAM, disk, uptime, battery), and one that clears accumulated usage records. Both are protected by an admin scope to prevent unprivileged callers from inspecting host hardware details.
- [Task Status Persistence Tests: Reproducing and Fixing a FastAPI Body vs Query Param Bug](./task-status-persistence-tests-reproducing-and-fixing-a-fastapi-body-vs-query-par.md) — Tests that reproduce and validate the fix for a bug where task status updates via the API did not persist — caused by FastAPI treating a bare `str` parameter as a query param while the frontend sent it as a JSON body. Also validates that `projectTasks` lists are updated alongside the main tasks list.
- [Telegram Channel API Tests — Configuration Status and Pairing State](./telegram-channel-api-tests-configuration-status-and-pairing-state.md) — This test module covers the two read-only Telegram status endpoints: `/api/v1/telegram/status` (reports whether a bot token and allowed user ID are configured) and `/api/v1/telegram/pairing-status` (reports live pairing state from a dashboard-level in-memory dict). Both endpoints inform the dashboard whether the Telegram channel is ready to receive messages.
- [Webhooks API Tests — CRUD, Secret Redaction, Duplicate Prevention, and Secret Rotation](./webhooks-api-tests-crud-secret-redaction-duplicate-prevention-and-secret-rotatio.md) — This test module covers the full lifecycle of PocketPaw's webhook configuration API: listing (with secret redaction), adding (with name validation and duplicate prevention), removing, and regenerating shared secrets. Webhooks enable external services to trigger agent actions, and the tests enforce that secrets are never exposed at rest through the API listing endpoint.

### `API Layer` — 1 article(s)

- [Cloud Files FastAPI Router Integration Tests: Tree and Browse Endpoints](./cloud-files-fastapi-router-integration-tests-tree-and-browse-endpoints.md) — This module runs end-to-end HTTP integration tests against the FastAPI router built by `build_router`, covering tree structure rendering, entry browsing, 404 for unknown mounts, and 403 for cross-workspace access attempts. It uses HTTPX's `ASGITransport` to drive the full request-response cycle in-process without a running server.

### `API Server` — 1 article(s)

- [PocketPaw Dashboard: FastAPI Web Server and API Gateway](./pocketpaw-dashboard-fastapi-web-server-and-api-gateway.md) — The dashboard module is PocketPaw's primary HTTP server, built on FastAPI with an asynccontextmanager lifespan. It mounts all API sub-routers, serves the frontend static files, manages channel adapter auto-start, and coordinates the WebSocket handler — acting as the integration point for every runtime subsystem.

### `API configuration` — 2 article(s)

- [Events SSE Router Tests: Endpoint Registration and OpenAPI Spec Inclusion](./events-sse-router-tests-endpoint-registration-and-openapi-spec-inclusion.md) — Tests that PocketPaw's server-sent events (SSE) endpoint `/api/v1/events/stream` is correctly registered in the events router, present in the v1 router registry, accessible on the dashboard app, and included in the OpenAPI specification. These structural checks prevent the events channel from being silently dropped during refactors.
- [V1 Router Registration and CORS Configuration Tests](./v1-router-registration-and-cors-configuration-tests.md) — Validates that all expected domain routers are registered in PocketPaw's v1 API surface and that CORS origins include the Tauri desktop app origin. These tests act as a registry contract — any new router added to the system must be explicitly listed, preventing silent omissions that would cause 404s from newly added features.

### `API contract` — 1 article(s)

- [Workspace Pydantic Schemas — Requests and Responses](./workspace-pydantic-schemas-requests-and-responses.md) — This module defines the Pydantic request and response schemas for the workspace domain, enforcing input validation at the API boundary before any business logic runs. The slug validator is the key defensive piece: it rejects slugs that would break URL routing or clash with reserved path segments.

### `API design` — 5 article(s)

- [Chat API Test Suite: SSE Streaming, Session Bridge, and Request Aliases](./chat-api-test-suite-sse-streaming-session-bridge-and-request-aliases.md) — Covers the full test surface of PocketPaw's `/api/v1/chat` router, including the `_APISessionBridge` async queue, SSE stream/stop endpoints, non-streaming send, media URL rewriting, and camelCase field alias handling. These tests exist to catch silent regressions in the real-time chat path that would break frontend consumers without an obvious error.
- [Cloud Error Hierarchy: Structured HTTP Error Contract Tests](./cloud-error-hierarchy-structured-http-error-contract-tests.md) — This test file validates the `ee.cloud.shared.errors` module, which provides a typed exception hierarchy that maps Python exceptions to HTTP status codes and machine-readable error codes. It ensures that every error class produces the correct status, code, message, and serialization shape expected by API consumers.
- [Pockets Domain Schemas: Request Validation and Response Model Tests](./pockets-domain-schemas-request-validation-and-response-model-tests.md) — This test file validates all Pydantic request/response schemas in the pockets domain — covering field defaults, enum constraints, length limits, optional-field behavior, and the full `PocketResponse` shape. It ensures the API surface contracts are locked in and that invalid inputs are rejected at the schema layer before reaching business logic.
- [Soul Memories API Tests: Tier Filtering, Limit Clamping, and Entry Serialization](./soul-memories-api-tests-tier-filtering-limit-clamping-and-entry-serialization.md) — Unit tests for the `GET /api/v1/soul/memories` endpoint, focusing on the `_collect_tier_entries` helper that queries individual memory tiers, limit clamping to safe bounds, tier filter validation, and graceful handling of absent souls or missing tier stores. Tests operate at the function level to avoid spinning up a full SSE client.
- [Unified Error Hierarchy for Cloud Domain Modules](./unified-error-hierarchy-for-cloud-domain-modules.md) — This module defines a structured exception hierarchy used across all cloud EE domain packages instead of raw `HTTPException` raises. It standardizes error shape, HTTP status codes, and machine-readable error codes in one place, ensuring consistent API responses and centralized error handling.

### `API endpoints` — 1 article(s)

- [Widget Track Endpoint Tests: Interaction Recording, Validation, Scope Fallback, and Projection Integration](./widget-track-endpoint-tests-interaction-recording-validation-scope-fallback-and-.md) — Tests for `POST /widgets/track`, the writer endpoint that records widget interactions from the SuggestedWidgetsFeed UI. Covers the full happy-path event emission, Pydantic validation of required fields, metadata defaulting, correlation-id propagation, sequential seq increments, the `org:*` scope fallback for anonymous actors, and an end-to-end proof that three POSTs reflect correctly in the `GET /widgets/usage` projection.

### `API key management` — 2 article(s)

- [API Key Management Tests: Creation, Verification, Rotation, and REST Endpoints](./api-key-management-tests-creation-verification-rotation-and-rest-endpoints.md) — Comprehensive tests for PocketPaw's `APIKeyManager` class and its REST endpoints covering the full key lifecycle — creation with scope validation, verification, revocation, rotation, and expiry rejection. The test suite isolates all disk I/O to a temp directory to prevent cross-test contamination and ensure file permission checks work correctly.
- [API Keys Panel Acceptance Tests: UI Lifecycle Flows for Cluster F](./api-keys-panel-acceptance-tests-ui-lifecycle-flows-for-cluster-f.md) — End-to-end acceptance tests that walk the exact flows the `ApiKeysPanel.svelte` UI executes — create, list, rotate (with old key invalidation), revoke, and sort-by-creation. These tests complement unit-level coverage by pinning the API shape the frontend depends on, catching regressions that break the dashboard silently.

### `API routes` — 2 article(s)

- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.

### `API routing` — 4 article(s)

- [Cloud Mount Integration Smoke Tests: Route and Error Handler Registration](./cloud-mount-integration-smoke-tests-route-and-error-handler-registration.md) — This smoke test suite verifies that `mount_cloud()` successfully registers all expected domain route groups (auth, workspaces, agents, chat, pockets, sessions, WebSocket, license) and the `CloudError` exception handler on a fresh FastAPI app. It acts as a fast regression guard ensuring no route group is accidentally omitted from the mount function.
- [Enterprise License Validation and FastAPI Dependency](./enterprise-license-validation-and-fastapi-dependency.md) — A self-contained license system that validates Ed25519-signed license keys for enterprise cloud features. License state is cached process-wide and gates API endpoints via FastAPI `Depends` integration, with a fallback HMAC-SHA256 path for self-hosted deployments that cannot configure asymmetric keys.
- [Knowledge Base REST API Router (Per-Workspace Scope)](./knowledge-base-rest-api-router-per-workspace-scope.md) — The primary FastAPI router for workspace-scoped knowledge base operations — search, ingest, lint, and article browsing — all delegating to the kb Go binary. This router replaced a pure-Python knowledge_base package and defines the REST surface consumed by the wiki pocket template.
- [Workspace-Level Knowledge Browser Router](./workspace-level-knowledge-browser-router.md) — A FastAPI router mounted at `/api/v1/knowledge/*` that fans out KB article listings across every agent in a workspace. It exists as a separate router from `/api/v1/kb/*` because it has different aggregation semantics — workspace-wide rollup rather than single-scope access.

### `API schemas` — 1 article(s)

- [Knowledge Base Request Schemas](./knowledge-base-request-schemas.md) — Pydantic request models for the knowledge base REST API, covering search, text ingestion, URL ingestion, and lint operations. These schemas enforce input validation at the FastAPI boundary before any request reaches the kb Go binary.

### `API security` — 1 article(s)

- [OAuth2 Server — Authorize, Token Exchange, and Revoke](./oauth2-server-authorize-token-exchange-and-revoke.md) — The OAuth2 router implements the server side of the OAuth2 authorization code flow, allowing third-party MCP clients and tools to obtain scoped access tokens to PocketPaw's API. It handles the consent UI, code-to-token exchange, refresh token rotation, and revocation.

### `API server` — 2 article(s)

- [A2A Server Tests: Models, State Machines, JSON-RPC Dispatch, and Task Lifecycle](./a2a-server-tests-models-state-machines-json-rpc-dispatch-and-task-lifecycle.md) — Extensive tests for the PocketPaw A2A server implementation covering Pydantic model serialisation with type discriminators, TaskState transition validation, JSON-RPC 2.0 dispatcher behaviour (parse errors, batch requests, method routing), and the full task lifecycle (submit, poll, stream, cancel) against a minimal in-memory FastAPI app.
- [API-Only Serve Mode Tests: Structure, Auth Middleware, CLI Parsing, and Socket Safety](./api-only-serve-mode-tests-structure-auth-middleware-cli-parsing-and-socket-safet.md) — Tests for PocketPaw's `serve` command, which launches a lightweight API-only server without the dashboard UI. Covers endpoint availability, auth middleware behavior, CLI argument parsing, and a regression suite proving that sockets used for local IP detection are always closed — even when errors occur during detection.

### `API surface` — 1 article(s)

- [LLM Package Public API: LLMClient, LLMRouter, and resolve_llm_client](./llm-package-public-api-llmclient-llmrouter-and-resolvellmclient.md) — The `pocketpaw.llm` package exposes three public symbols — `LLMClient`, `LLMRouter`, and `resolve_llm_client` — forming the canonical entry point for all LLM interactions in PocketPaw. This init file enforces a stable import surface so internal module restructuring does not break callsites.

### `Access Control` — 2 article(s)

- [Cloud Files Browse Mount Tests: ABAC Filtering and Provider Resolution](./cloud-files-browse-mount-tests-abac-filtering-and-provider-resolution.md) — This test module validates the `browse_mount` function in PocketPaw's cloud files subsystem, covering successful entry listing, ABAC tag-based filtering, and graceful errors when a mount references an unregistered provider. Together these tests protect the file-browsing surface against misconfiguration and unauthorized data exposure.
- [Cloud Files Permissions: ABAC Filtering and Capability Derivation Tests](./cloud-files-permissions-abac-filtering-and-capability-derivation-tests.md) — This module validates the layered permissions model in PocketPaw's cloud files system, covering ABAC tag filtering, RBAC-and-mount-writable capability intersection, and the `PermissionsEvaluator` composite. These tests ensure that no combination of user attributes, mount configuration, or entry tags can produce a capability set broader than what all three permission layers jointly allow.

### `Agent Configuration` — 1 article(s)

- [Identity Router — Agent Identity File Management via REST](./identity-router-agent-identity-file-management-via-rest.md) — The identity router exposes PocketPaw's five agent identity files (IDENTITY.md, SOUL.md, STYLE.md, INSTRUCTIONS.md, USER.md) through a simple read/write REST interface, allowing dashboard users to customize the agent's persona, communication style, and behavioral instructions without editing files directly. Changes take effect on the next inbound message without requiring a server restart.

### `Agent Execution` — 1 article(s)

- [MCTaskExecutor — Secure, Streaming AI Agent Task Execution](./mctaskexecutor-secure-streaming-ai-agent-task-execution.md) — `MCTaskExecutor` runs AI agents against Mission Control tasks with real-time WebSocket streaming of output, auto-saves deliverable documents, retries failed tasks up to a configurable limit, enforces per-task timeouts, and limits concurrent executions with UUID validation and error sanitisation as security controls.

### `Agent Infrastructure` — 2 article(s)

- [MCPManager — Server Lifecycle, Tool Discovery, and Tool Execution](./mcpmanager-server-lifecycle-tool-discovery-and-tool-execution.md) — `MCPManager` is the central singleton that owns the full lifecycle of connected MCP servers: it starts and stops subprocess or HTTP connections, discovers tools from each server, and dispatches tool call requests. It supports stdio, SSE, and streamable-HTTP transports as well as OAuth-authenticated remote servers.
- [MemoryManager — High-Level Memory Facade and Singleton](./memorymanager-high-level-memory-facade-and-singleton.md) — `MemoryManager` is the application-facing facade over any `MemoryStoreProtocol` backend, providing agent-friendly methods like `get_context_for_agent`, `auto_learn`, and `get_graph_snapshot`. It is accessed as a singleton via `get_memory_manager()`.

### `Agent Runtime` — 2 article(s)

- [Backends Router — Agent Backend Discovery and Auto-Install](./backends-router-agent-backend-discovery-and-auto-install.md) — The backends router exposes the registry of agent backends (Claude, Codex CLI, OpenCode, etc.) to the dashboard and API clients, including real-time availability checking that verifies both Python SDK imports and CLI binary presence. An install endpoint allows auto-installing missing backend SDKs via pip without requiring manual server restarts.
- [IntentionExecutor: Orchestrating Proactive Agent Invocations](./intentionexecutor-orchestrating-proactive-agent-invocations.md) — IntentionExecutor ties together context gathering, prompt templating, and agent invocation into a single async pipeline that fires when an intention's trigger condition is met. It streams results back to connected clients via a configurable callback, supporting both WebSocket and Telegram delivery.

### `Anthropic` — 1 article(s)

- [AnthropicAdapter: Native Anthropic API Configuration and Client Construction](./anthropicadapter-native-anthropic-api-configuration-and-client-construction.md) — `AnthropicAdapter` implements the `ProviderAdapter` protocol for Anthropic's native API, resolving model and API key from settings and constructing `AsyncAnthropic` clients. It explicitly raises `NotImplementedError` on `build_openai_client()` to enforce that Anthropic-native callers never accidentally get an OpenAI-compatible client.

### `Architecture` — 3 article(s)

- [API v1 Router Aggregation and Lazy Module Mounting](./api-v1-router-aggregation-and-lazy-module-mounting.md) — The `__init__.py` for PocketPaw's v1 API layer is responsible for registering every domain router onto the FastAPI application via a single `mount_v1_routers()` function. It uses deferred imports to sidestep circular-dependency problems that arise when a large plugin-style codebase wires itself up at startup.
- [Connectors Package Init: Public API Surface for the Database Adapter Layer](./connectors-package-init-public-api-surface-for-the-database-adapter-layer.md) — The `connectors/__init__.py` file defines the public import surface for PocketPaw's database connector subsystem, re-exporting the key types and factory functions that other subsystems use to obtain database connections. It acts as a stable interface boundary so that callers do not need to know the internal module structure of the connectors package.
- [Memory Backend Selection Tests: EE MongoDB Default Flip and OSS Import Isolation](./memory-backend-selection-tests-ee-mongodb-default-flip-and-oss-import-isolation.md) — This module tests that the EE edition flips the default memory backend from `file` to `mongodb` at startup without overriding explicit user choices, verifies that `get_memory_manager()` returns a `MongoMemoryStore`-backed manager after the flip, and enforces that OSS package modules (`src/pocketpaw/memory/`) contain no top-level imports from `ee.*` to preserve clean open-source/enterprise boundary separation.

### `Authentication` — 2 article(s)

- [API Keys Router — Long-Lived Key CRUD with One-Shot Plaintext Return](./api-keys-router-long-lived-key-crud-with-one-shot-plaintext-return.md) — The API keys router provides create, list, revoke, and rotate operations for long-lived programmatic access keys. A critical security property of the create endpoint is that the plaintext key is returned exactly once — subsequent reads only expose a short prefix — mirroring industry-standard patterns like GitHub personal access tokens.
- [Auth Router — Session Exchange, Cookie Login, QR Code, and Token Regeneration](./auth-router-session-exchange-cookie-login-qr-code-and-token-regeneration.md) — The auth router handles all first-party authentication flows: exchanging a master access token for a time-limited session token, setting and clearing HTTP-only session cookies, generating QR login codes for mobile pairing, and regenerating the master token when credentials need to be rotated. It uses constant-time comparison to close a timing oracle vulnerability in master-token validation.

### `Automation` — 1 article(s)

- [Intentions Router — Proactive Agent Scheduling CRUD and Manual Trigger](./intentions-router-proactive-agent-scheduling-crud-and-manual-trigger.md) — The intentions router manages PocketPaw's proactive behavior system — scheduled or event-triggered agent prompts that run autonomously without user initiation. It exposes full CRUD operations plus a toggle (enable/disable without deleting) and a run-now action that bypasses the scheduler for immediate execution.

### `Beanie` — 2 article(s)

- [Cloud Document Models Registry — Beanie Initialization Manifest](./cloud-document-models-registry-beanie-initialization-manifest.md) — The central re-export module for all Beanie ODM documents in the enterprise cloud layer, providing a stable import surface and a lazy-loading `ALL_DOCUMENTS` list consumed by Beanie's init sequence. Special handling defers `FileUpload` and `FileFolder` imports to break circular import chains with the uploads subsystem.
- [TimestampedDocument — Base Beanie Document with Automatic Timestamps](./timestampeddocument-base-beanie-document-with-automatic-timestamps.md) — A Beanie `Document` base class that automatically manages `createdAt` and `updatedAt` timestamps using Beanie's event hook system. All cloud ODM documents inherit from this class to get consistent, server-side timestamp management.

### `CI` — 1 article(s)

- [Packaging Sanity Tests: Dependency Conflicts, Version Bounds, and Extra Consistency](./packaging-sanity-tests-dependency-conflicts-version-bounds-and-extra-consistency.md) — Tests that parse `pyproject.toml` directly to catch dependency version conflicts and packaging invariants before they cause runtime failures. Specifically targets the FastAPI/Starlette version conflict introduced by MCP's transitive Starlette dependency, and verifies that dashboard-mode dependencies are included in the core package.

### `CLI` — 19 article(s)

- [API-Only Serve Mode Tests: Structure, Auth Middleware, CLI Parsing, and Socket Safety](./api-only-serve-mode-tests-structure-auth-middleware-cli-parsing-and-socket-safet.md) — Tests for PocketPaw's `serve` command, which launches a lightweight API-only server without the dashboard UI. Covers endpoint availability, auth middleware behavior, CLI argument parsing, and a regression suite proving that sockets used for local IP detection are always closed — even when errors occur during detection.
- [CLI Channel Adapter Management: List, Start, and Stop Channels](./cli-channel-adapter-management-list-start-and-stop-channels.md) — The `channels` CLI command provides operators with a unified interface to inspect and control PocketPaw's eight supported messaging channel adapters — Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It separates read operations (listing configuration status) from write operations (toggling adapters via the dashboard REST API) to avoid side effects during inspection.
- [CLI Config Command: Inspect, Set, Validate, and Locate PocketPaw Settings](./cli-config-command-inspect-set-validate-and-locate-pocketpaw-settings.md) — The `config` CLI command exposes four operations on PocketPaw's `Settings` object — displaying the current config with secrets masked, writing a typed key-value pair, running batch API key validation, and printing the config file path. The file is named `config_cmd.py` to avoid a Python module name collision with the `pocketpaw.config` package.
- [CLI Doctor Command: Full Diagnostic Report with Connectivity Checks](./cli-doctor-command-full-diagnostic-report-with-connectivity-checks.md) — The `doctor` command runs PocketPaw's complete two-phase diagnostic suite — startup checks (environment, config, permissions) followed by async connectivity checks (API reachability, channel credentials) — and reports results in either rich human-readable format or structured JSON. It delegates to the health engine rather than reimplementing checks, keeping the CLI thin.
- [CLI Errors Command: Viewing and Filtering Recent Runtime Errors](./cli-errors-command-viewing-and-filtering-recent-runtime-errors.md) — The `errors` CLI command surfaces recent entries from the health engine's error store, showing timestamp, severity, source, message, and the last line of any traceback. It supports text-based filtering and JSON output to integrate with automated alerting workflows.
- [CLI Health Command: Fast Offline Startup Health Check](./cli-health-command-fast-offline-startup-health-check.md) — The `health` command runs only the startup phase of PocketPaw's health engine — synchronous, network-free checks that verify config file presence, API key format, file permissions, and similar environmental prerequisites. It completes in milliseconds and is designed to be run frequently, for example in shell prompts or startup scripts, unlike the full `doctor` command which also runs slow network connectivity checks.
- [CLI Logs Command: Audit Log Viewer and Tail Mode](./cli-logs-command-audit-log-viewer-and-tail-mode.md) — The `logs` command reads PocketPaw's JSONL audit log at `~/.pocketpaw/audit.jsonl`, rendering recent entries in a color-coded table or streaming new entries in real time via a tail loop. It handles malformed JSON lines gracefully and renders multi-field entries by normalizing common key aliases.
- [CLI Memory Command: Inspecting and Searching Long-Term Agent Memories](./cli-memory-command-inspecting-and-searching-long-term-agent-memories.md) — The `memory` CLI command provides operators with visibility into PocketPaw's persistent memory system — showing statistics about stored memories, daily notes, and sessions, or performing keyword searches across the memory store. It bridges the gap between the agent's internal memory manager and human-readable inspection.
- [CLI Sessions Command: Listing, Searching, and Deleting Chat Sessions](./cli-sessions-command-listing-searching-and-deleting-chat-sessions.md) — The `sessions` command provides operators with management access to PocketPaw's persisted chat sessions — listing recent sessions with metadata, searching session content by keyword, and deleting sessions by key. It wraps the async memory manager's session interface with a synchronous CLI dispatcher.
- [CLI Skills Command: Listing and Searching Available Agent Skills](./cli-skills-command-listing-and-searching-available-agent-skills.md) — The `skills` CLI command discovers and displays all skills available to the PocketPaw agent, optionally filtering by search term. It delegates loading and search to the skill loader, rendering results with invocability status and file paths so operators can quickly find and verify which skills are active.
- [CLI Status Command: Live Agent Status with Watch Mode](./cli-status-command-live-agent-status-with-watch-mode.md) — The `status` command polls the PocketPaw agent's REST status endpoint to display global state (active sessions, uptime, concurrent session capacity) and per-session detail (channel, tool, duration, error). It supports a `--watch` mode that redraws the terminal at a configurable interval, providing a live dashboard view.
- [CLI Update Command: Self-Update via uv with PyPI Version Check](./cli-update-command-self-update-via-uv-with-pypi-version-check.md) — The `update` command checks PyPI for a newer version of PocketPaw and, if one is available, installs it using the `uv` package manager targeting the current Python interpreter. It handles the Windows file-locking edge case where the running executable cannot be replaced while the process is active.
- [CLI Utils: Shared ANSI Colors, Output Helpers, and Secret Masking](./cli-utils-shared-ansi-colors-output-helpers-and-secret-masking.md) — The `cli.utils` module provides the shared visual primitives used by every PocketPaw CLI subcommand — ANSI color constants, formatted print helpers, JSON output, and a `mask_value` function that automatically redacts secrets before display. Centralizing these in one module ensures consistent styling and prevents accidental secret exposure across all CLI commands.
- [Headless Channel Runners: Multi-Adapter CLI Mode for PocketPaw](./headless-channel-runners-multi-adapter-cli-mode-for-pocketpaw.md) — `headless.py` provides the CLI runtime functions that launch PocketPaw in headless server mode — either as a single Telegram bot or as multiple channel adapters (Discord, Slack, WhatsApp, Signal, Matrix, Teams, Google Chat) sharing one `AgentLoop` and message bus. It was extracted from `__main__.py` to keep the entry point thin while keeping channel-setup complexity isolated.
- [Ollama Integration: Provider Detection, Env Var Construction, and CLI Health Check](./ollama-integration-provider-detection-env-var-construction-and-cli-health-check.md) — Tests for PocketPaw's Ollama provider support, verifying that LLMClient correctly detects Ollama from settings, constructs the right subprocess environment variables, and skips smart model routing for local models. Also covers the `--check-ollama` CLI command that validates server reachability and model availability.
- [Paw CLI Tests: init, doctor, channels, and serve Command Coverage](./paw-cli-tests-init-doctor-channels-and-serve-command-coverage.md) — This test file validates PocketPaw's `paw` CLI entry point using Click's `CliRunner`, covering the main command group, `init`, `doctor`, `channels`, and `serve` subcommands. It focuses on exit codes, output presence, option parsing, and graceful degradation when optional dependencies like `soul-protocol` are absent.
- [PocketPaw CLI Entry Point and Server Launcher](./pocketpaw-cli-entry-point-and-server-launcher.md) — The `__main__.py` module is PocketPaw's CLI entry point, handling argument parsing, subcommand dispatch, server startup with automatic port fallback, and channel mode selection. It has evolved significantly from a simple server launcher into a full-featured CLI with subcommands for diagnostics, memory inspection, session management, configuration, and log tailing.
- [Security Audit CLI Tests — Config Permissions, API Key Exposure, and Audit Runner](./security-audit-cli-tests-config-permissions-api-key-exposure-and-audit-runner.md) — This test file validates PocketPaw's security audit command-line tool, which checks for world-readable config files, plaintext API keys, missing audit logs, unreachable guardian agents, invalid file jails, risky tool profiles, and dangerous permission bypasses. Tests cover both detection and auto-fix capabilities.
- [Update Check System: Non-Blocking PyPI Version Checks with Release Notes and Styled CLI Notices](./update-check-system-non-blocking-pypi-version-checks-with-release-notes-and-styl.md) — The update check module queries PyPI once per 24 hours to determine whether a newer version of PocketPaw is available, caches the result to a local file, and prints a styled terminal box on TTY sessions when an update is found. It additionally fetches per-version release notes from GitHub with a separate 1-hour cache and tracks which version the user has last acknowledged.

### `CLI integration` — 2 article(s)

- [Firebase Connector: YAML Definition, CLI Adapter, and Trust Level Tests](./firebase-connector-yaml-definition-cli-adapter-and-trust-level-tests.md) — This test suite validates PocketPaw's Firebase connector from three angles: the declarative YAML definition (action count, trust levels, auth method), the `FirebaseAdapter` class that wraps the Firebase CLI via async subprocess, and the connector registry integration. It uses mocked subprocess calls to test connect, execute, timeout, and error paths without touching a real Firebase project.
- [GCP Connector: YAML Definition, Adapter Authentication, and Execute Path Tests](./gcp-connector-yaml-definition-adapter-authentication-and-execute-path-tests.md) — This test suite validates PocketPaw's Google Cloud Platform connector, covering the `gcp.yaml` declarative definition (20 actions across compute, storage, pub/sub, secrets, Cloud Run, and logging), the `GCPAdapter` lifecycle (connect, execute, disconnect), and registry wiring. All subprocess calls are mocked to avoid requiring live GCP credentials.

### `CQRS` — 2 article(s)

- [FabricJournalStore — Event-Sourced Object Lifecycle for Fabric](./fabricjournalstore-event-sourced-object-lifecycle-for-fabric.md) — `FabricJournalStore` is the Wave 3 replacement for the scope-filtering slice of PR #938, providing event-sourced create, update, archive, and query operations over Fabric objects backed by the org journal and served through an in-memory projection. Every write appends an `EventEntry` to the journal and immediately folds it into the live projection, so reads after a write are always consistent without a rebuild.
- [FabricProjection — In-Memory Journal Projection for Fabric Objects](./fabricprojection-in-memory-journal-projection-for-fabric-objects.md) — `FabricProjection` replays `fabric.object.*` journal events into an in-memory dictionary to maintain a current-state view of all Fabric objects, applying scope filters before computing query totals to permanently fix the pagination leak that blocked PR #938. It is a deliberately minimal CQRS read model: rebuild from the journal on startup, apply events incrementally, serve scoped queries.

### `CRUD` — 1 article(s)

- [Fabric FastAPI Router — Ontology CRUD and Query Endpoints](./fabric-fastapi-router-ontology-crud-and-query-endpoints.md) — The Fabric router exposes HTTP endpoints for managing the ontology layer: defining object types, creating and querying objects, creating and listing links, and retrieving stats. It uses the legacy SQLite `FabricStore` for all persistence and was updated in Cluster C / PR3 to add `GET /fabric/objects` and `GET /fabric/links` so the PocketDataPanel's Objects and Links sub-tabs render real data.

### `Caching` — 1 article(s)

- [Cloud Files Tree Cache Tests: TTL, Invalidation, and Multi-Tenant Key Isolation](./cloud-files-tree-cache-tests-ttl-invalidation-and-multi-tenant-key-isolation.md) — This module tests `CachedTreeBuilder`, the in-process 30-second cache layer over `build_tree` that prevents repeated provider fan-outs on every tree request. Tests validate that cache hits suppress redundant `list_mounts` calls, that explicit invalidation forces a rebuild, that TTL expiry triggers a new fan-out, and that clearing the cache invalidates all per-user keys.

### `Channel Management` — 2 article(s)

- [CLI Channel Adapter Management: List, Start, and Stop Channels](./cli-channel-adapter-management-list-start-and-stop-channels.md) — The `channels` CLI command provides operators with a unified interface to inspect and control PocketPaw's eight supported messaging channel adapters — Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It separates read operations (listing configuration status) from write operations (toggling adapters via the dashboard REST API) to avoid side effects during inspection.
- [Dashboard Channels: Channel Adapter Lifecycle and REST Endpoints](./dashboard-channels-channel-adapter-lifecycle-and-rest-endpoints.md) — dashboard_channels.py manages the lifecycle of channel adapters (Discord, Slack, WhatsApp, custom webhooks) and exposes REST endpoints for querying status, saving config, toggling channels on/off, and handling inbound webhooks. Adapters are started and stopped at runtime without server restarts.

### `Channels` — 1 article(s)

- [Channels Router — Multi-Platform Adapter Status, Configuration, and Dynamic Toggle](./channels-router-multi-platform-adapter-status-configuration-and-dynamic-toggle.md) — The channels router manages PocketPaw's suite of messaging adapters — Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat — through a unified REST interface. It allows the dashboard to inspect adapter state, save credentials, start or stop adapters at runtime without restarting the server, retrieve WhatsApp QR codes, and install optional channel dependencies on demand.

### `Chat` — 1 article(s)

- [Chat Router — HTTP Send, SSE Streaming, and Session Bridge for Agent Conversations](./chat-router-http-send-sse-streaming-and-session-bridge-for-agent-conversations.md) — The chat router enables external clients to drive PocketPaw agent sessions over plain HTTP, using Server-Sent Events for streaming responses. The `_APISessionBridge` class connects the internal message bus to an SSE stream, ensuring that chat sessions initiated via the HTTP API reuse the same AgentLoop pipeline as native channel adapters.

### `Claude SDK` — 1 article(s)

- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.

### `Claude SDK integration` — 2 article(s)

- [ClaudeAgentSDK Dispatch and Persistent Client Tests](./claudeagentsdk-dispatch-and-persistent-client-tests.md) — This module tests the dispatch logic for `ClaudeAgentSDK`, documenting the removal of the `_fast_chat` direct-API bypass in version 0.4.16 and the introduction of a persistent `ClaudeSDKClient` that reuses connections across calls. It verifies that all task complexities (SIMPLE, MODERATE, routing disabled) flow through the CLI path and validates the client lifecycle: creation, reuse, reconnection on model change, fallback on failure, interrupt, and cleanup.
- [StreamEvent Processing Tests: Token-by-Token Streaming and Thinking Integration](./streamevent-processing-tests-token-by-token-streaming-and-thinking-integration.md) — Tests for PocketPaw's handling of `StreamEvent` objects from the Claude Agent SDK, covering text delta emission, thinking block lifecycle, tool-use start events, duplicate suppression, multi-turn state reset, and integration of thinking events into the AgentLoop's system event bus.

### `Cloud Files` — 11 article(s)

- [Cloud Files Browse Mount Tests: ABAC Filtering and Provider Resolution](./cloud-files-browse-mount-tests-abac-filtering-and-provider-resolution.md) — This test module validates the `browse_mount` function in PocketPaw's cloud files subsystem, covering successful entry listing, ABAC tag-based filtering, and graceful errors when a mount references an unregistered provider. Together these tests protect the file-browsing surface against misconfiguration and unauthorized data exposure.
- [Cloud Files Error Codes and Event Importability Contract Tests](./cloud-files-error-codes-and-event-importability-contract-tests.md) — This test module enforces the public contract of PocketPaw's cloud files error hierarchy and event definitions, verifying that each error carries the correct machine-readable code and HTTP status, and that all file lifecycle events are importable. These tests act as a regression guard for the API surface consumed by clients and downstream event handlers.
- [Cloud Files FastAPI Router Integration Tests: Tree and Browse Endpoints](./cloud-files-fastapi-router-integration-tests-tree-and-browse-endpoints.md) — This module runs end-to-end HTTP integration tests against the FastAPI router built by `build_router`, covering tree structure rendering, entry browsing, 404 for unknown mounts, and 403 for cross-workspace access attempts. It uses HTTPX's `ASGITransport` to drive the full request-response cycle in-process without a running server.
- [Cloud Files Permissions: ABAC Filtering and Capability Derivation Tests](./cloud-files-permissions-abac-filtering-and-capability-derivation-tests.md) — This module validates the layered permissions model in PocketPaw's cloud files system, covering ABAC tag filtering, RBAC-and-mount-writable capability intersection, and the `PermissionsEvaluator` composite. These tests ensure that no combination of user attributes, mount configuration, or entry tags can produce a capability set broader than what all three permission layers jointly allow.
- [Cloud Files Schema Validation Tests: FileEntry, MountConfig, Permission, and Page](./cloud-files-schema-validation-tests-fileentry-mountconfig-permission-and-page.md) — This module validates the Pydantic schema constraints for PocketPaw's core cloud files data models, confirming invariants like namespaced entry IDs, enumerated scopes, valid capability sets, absolute mount templates, and permission intersection semantics. These tests prevent malformed data from entering the system by verifying that validation errors are raised at construction time.
- [Cloud Files Tree Builder Tests: Ordering, Nesting, and Resilient Provider Failures](./cloud-files-tree-builder-tests-ordering-nesting-and-resilient-provider-failures.md) — This module tests `build_tree`, which assembles a hierarchical folder tree from all registered provider mounts. Key behaviors tested include mount ordering by the `order` field, recursive path-segment nesting, and graceful warning collection when individual providers fail rather than aborting the entire tree build.
- [Cloud Files Tree Cache Tests: TTL, Invalidation, and Multi-Tenant Key Isolation](./cloud-files-tree-cache-tests-ttl-invalidation-and-multi-tenant-key-isolation.md) — This module tests `CachedTreeBuilder`, the in-process 30-second cache layer over `build_tree` that prevents repeated provider fan-outs on every tree request. Tests validate that cache hits suppress redundant `list_mounts` calls, that explicit invalidation forces a rebuild, that TTL expiry triggers a new fan-out, and that clearing the cache invalidates all per-user keys.
- [FolderProvider Contract Test Base Class: Reusable Compliance Suite](./folderprovider-contract-test-base-class-reusable-compliance-suite.md) — This module defines `ProviderContract`, an abstract base class that encodes the behavioral contract every `FolderProvider` implementation must satisfy, including mount listing, entry listing with pagination, namespaced IDs, and graceful `ProviderUnsupported` errors for write operations. Concrete provider tests subclass it to inherit full compliance coverage automatically.
- [Mount Configuration Loading and Template Resolution Tests](./mount-configuration-loading-and-template-resolution-tests.md) — This module tests the `load_mounts` and `resolve_template` functions that parse and validate `mounts.yaml` configuration files, covering sort order, variable substitution, unknown-variable rejection, and absolute-path enforcement. These tests prevent misconfigured mount definitions from silently producing incorrect or insecure virtual filesystem paths.
- [ProviderRegistry Tests: Registration, Duplicate Detection, and Mount Resolution](./providerregistry-tests-registration-duplicate-detection-and-mount-resolution.md) — This module tests `ProviderRegistry`, the central index that maps provider IDs to `FolderProvider` implementations and resolves virtual mount paths to their owning providers. Key behaviors tested include duplicate registration rejection, longest-prefix mount resolution, and template variable substitution during path matching.
- [Unified Files Service Tests: Workspace Isolation, Dedup, Drive Warnings, and Limit Cap](./unified-files-service-tests-workspace-isolation-dedup-drive-warnings-and-limit-c.md) — This module tests `UnifiedFilesService`, which aggregates files from multiple sources (chat uploads, Drive, local) into a single normalized list, covering workspace scope isolation, soft-delete filtering, cross-source deduplication, source-specific warnings, and the per-request limit cap. It uses `mongomock-motor` to run Beanie document tests in CI without a live MongoDB instance.

### `Cloud Memory` — 3 article(s)

- [Cloud Memory Test Fixtures: mongomock-motor Beanie Isolation and MongoMemoryStore Setup](./cloud-memory-test-fixtures-mongomock-motor-beanie-isolation-and-mongomemorystore.md) — This conftest provides two pytest fixtures for the cloud memory test suite: `beanie_memory_db` initializes an isolated in-memory MongoDB via `mongomock-motor` with a compatibility shim for Beanie's `list_collection_names` signature mismatch, and `store` returns a fresh `MongoMemoryStore` bound to that database. Each test gets its own uniquely-named in-memory database to prevent state leakage.
- [Memory Backend Selection Tests: EE MongoDB Default Flip and OSS Import Isolation](./memory-backend-selection-tests-ee-mongodb-default-flip-and-oss-import-isolation.md) — This module tests that the EE edition flips the default memory backend from `file` to `mongodb` at startup without overriding explicit user choices, verifies that `get_memory_manager()` returns a `MongoMemoryStore`-backed manager after the flip, and enforces that OSS package modules (`src/pocketpaw/memory/`) contain no top-level imports from `ee.*` to preserve clean open-source/enterprise boundary separation.
- [MongoMemoryStore Deduplication Tests: Idempotent Saves Within the 5-Second Window](./mongomemorystore-deduplication-tests-idempotent-saves-within-the-5-second-window.md) — This module tests the deduplication logic in `MongoMemoryStore`, which prevents identical back-to-back writes from creating duplicate memory entries when an agent loop retries the same turn. The dedup key is `(session_key, role, content)` with a 5-second window, and the tests verify that only different content, session, or role values produce distinct database rows.

### `Codex CLI` — 1 article(s)

- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.

### `Configuration` — 3 article(s)

- [CLI Config Command: Inspect, Set, Validate, and Locate PocketPaw Settings](./cli-config-command-inspect-set-validate-and-locate-pocketpaw-settings.md) — The `config` CLI command exposes four operations on PocketPaw's `Settings` object — displaying the current config with secrets masked, writing a typed key-value pair, running batch API key validation, and printing the config file path. The file is named `config_cmd.py` to avoid a Python module name collision with the `pocketpaw.config` package.
- [Mount Configuration Loading and Template Resolution Tests](./mount-configuration-loading-and-template-resolution-tests.md) — This module tests the `load_mounts` and `resolve_template` functions that parse and validate `mounts.yaml` configuration files, covering sort order, variable substitution, unknown-variable rejection, and absolute-path enforcement. These tests prevent misconfigured mount definitions from silently producing incorrect or insecure virtual filesystem paths.
- [PocketPaw Settings: Centralized Configuration with Security Hardening](./pocketpaw-settings-centralized-configuration-with-security-hardening.md) — The `Settings` class is PocketPaw's single source of truth for all runtime configuration — agent backends, channel credentials, memory backends, security policies, soul protocol parameters, and user preferences. Built on `pydantic-settings`, it supports environment variable overrides, encrypted credential storage, SSRF-guarded URL fields, and automatic API key format validation.

### `Configuration Management` — 1 article(s)

- [MCP Server Configuration — Persistence and Schema](./mcp-server-configuration-persistence-and-schema.md) — `pocketpaw.mcp.config` defines the `MCPServerConfig` dataclass and the load/save helpers that persist MCP server definitions to `~/.pocketpaw/mcp_servers.json`. It is the single source of truth for which MCP servers are configured and how to connect to them.

### `Context Management` — 1 article(s)

- [ContextHub: System Context Gatherer for Intention Prompts](./contexthub-system-context-gatherer-for-intention-prompts.md) — ContextHub collects real-time system information — CPU, memory, disk, battery, current time, and health engine status — and injects it into intention prompt templates via `{{variable}}` placeholders. It acts as the sensory layer that allows PocketPaw's proactive intentions to be environment-aware rather than static.

### `Daemon` — 6 article(s)

- [ContextHub: System Context Gatherer for Intention Prompts](./contexthub-system-context-gatherer-for-intention-prompts.md) — ContextHub collects real-time system information — CPU, memory, disk, battery, current time, and health engine status — and injects it into intention prompt templates via `{{variable}}` placeholders. It acts as the sensory layer that allows PocketPaw's proactive intentions to be environment-aware rather than static.
- [IntentionExecutor: Orchestrating Proactive Agent Invocations](./intentionexecutor-orchestrating-proactive-agent-invocations.md) — IntentionExecutor ties together context gathering, prompt templating, and agent invocation into a single async pipeline that fires when an intention's trigger condition is met. It streams results back to connected clients via a configurable callback, supporting both WebSocket and Telegram delivery.
- [IntentionStore: JSON Persistence for Proactive Intentions](./intentionstore-json-persistence-for-proactive-intentions.md) — IntentionStore manages the CRUD lifecycle for user-defined intentions stored as a JSON file in `~/.pocketpaw/intentions.json`. Each intention records what prompt to run, when to run it (trigger), and which context sources to include.
- [ProactiveDaemon: Central Orchestrator for Scheduled Agent Behavior](./proactivedaemon-central-orchestrator-for-scheduled-agent-behavior.md) — ProactiveDaemon is the top-level coordinator that wires together IntentionStore, TriggerEngine, IntentionExecutor, and ContextHub into a single lifecycle-managed service. It starts trigger scheduling on `start()`, fires intentions via `_on_trigger()` when conditions are met, and tears down cleanly on `stop()`.
- [Self-Audit Daemon: Periodic Security and Health Checks](./self-audit-daemon-periodic-security-and-health-checks.md) — The self-audit module runs a suite of automated checks against the PocketPaw runtime environment — stale sessions, config conflicts, disk usage, audit log size, and orphaned OAuth tokens — and saves timestamped JSON reports to `~/.pocketpaw/audit_reports/`. It is designed to run on a schedule and surface problems before they become incidents.
- [TriggerEngine: Cron and Stale-Session Scheduling for Intentions](./triggerengine-cron-and-stale-session-scheduling-for-intentions.md) — TriggerEngine wraps APScheduler to provide two trigger types for proactive intentions: cron-based scheduling (standard 5-field cron expressions plus named presets) and stale-session polling (fires when a WebSocket session has been idle beyond a threshold). It manages job registration, removal, and update for all enabled intentions.

### `Dashboard` — 6 article(s)

- [Dashboard Authentication: Middleware, Session Tokens, and QR Login](./dashboard-authentication-middleware-session-tokens-and-qr-login.md) — dashboard_auth.py centralizes all authentication logic for the PocketPaw dashboard — localhost detection, HMAC token verification, session token exchange, cookie-based login/logout, and QR code generation. It was extracted from dashboard.py to isolate auth concerns and make each piece independently testable.
- [Dashboard Channels: Channel Adapter Lifecycle and REST Endpoints](./dashboard-channels-channel-adapter-lifecycle-and-rest-endpoints.md) — dashboard_channels.py manages the lifecycle of channel adapters (Discord, Slack, WhatsApp, custom webhooks) and exposes REST endpoints for querying status, saving config, toggling channels on/off, and handling inbound webhooks. Adapters are started and stopped at runtime without server restarts.
- [Dashboard Lifecycle: Startup, Shutdown, and Broadcast Orchestration](./dashboard-lifecycle-startup-shutdown-and-broadcast-orchestration.md) — dashboard_lifecycle.py owns the `startup_event()` and `shutdown_event()` functions that initialize and tear down every PocketPaw subsystem in the correct order, plus broadcast helpers that push notifications, audit entries, and health updates to all connected WebSocket clients.
- [Dashboard State: Shared Mutable Singletons and Per-Agent Loop Registry](./dashboard-state-shared-mutable-singletons-and-per-agent-loop-registry.md) — dashboard_state.py is the shared state module for PocketPaw's dashboard, extracted from dashboard.py to break circular imports. It holds the global singletons (WebSocketAdapter, AgentLoop, StatusTracker), the per-agent loop registry with staleness detection, and channel inspection helpers.
- [Dashboard WebSocket Handler: Real-Time Chat, Tools, and File Navigation](./dashboard-websocket-handler-real-time-chat-tools-and-file-navigation.md) — dashboard_ws.py implements the core WebSocket handler that drives PocketPaw's real-time dashboard UI — handling authentication, message dispatch, tool execution, session switching, file browsing, and skill invocation over a persistent WebSocket connection.
- [PocketPaw Dashboard: FastAPI Web Server and API Gateway](./pocketpaw-dashboard-fastapi-web-server-and-api-gateway.md) — The dashboard module is PocketPaw's primary HTTP server, built on FastAPI with an asynccontextmanager lifespan. It mounts all API sub-routers, serves the frontend static files, manages channel adapter auto-start, and coordinates the WebSocket handler — acting as the integration point for every runtime subsystem.

### `Data` — 1 article(s)

- [Connectors Router — Data Source CRUD, Credential Management, and Action Execution](./connectors-router-data-source-crud-credential-management-and-action-execution.md) — The connectors router provides a structured REST interface over PocketPaw's `ConnectorRegistry`, allowing the dashboard and enterprise UI to connect agents to external data sources (databases, APIs, cloud services) and execute actions against them. A structured status endpoint was added specifically to feed the `ConnectorCard` UI component with credential state and sync metadata.

### `Data Models` — 1 article(s)

- [Cloud Files Schema Validation Tests: FileEntry, MountConfig, Permission, and Page](./cloud-files-schema-validation-tests-fileentry-mountconfig-permission-and-page.md) — This module validates the Pydantic schema constraints for PocketPaw's core cloud files data models, confirming invariants like namespaced entry IDs, enumerated scopes, valid capability sets, absolute mount templates, and permission intersection semantics. These tests prevent malformed data from entering the system by verifying that validation errors are raised at construction time.

### `Data Persistence` — 1 article(s)

- [IntentionStore: JSON Persistence for Proactive Intentions](./intentionstore-json-persistence-for-proactive-intentions.md) — IntentionStore manages the CRUD lifecycle for user-defined intentions stored as a JSON file in `~/.pocketpaw/intentions.json`. Each intention records what prompt to run, when to run it (trigger), and which context sources to include.

### `Database` — 2 article(s)

- [Connectors Package Init: Public API Surface for the Database Adapter Layer](./connectors-package-init-public-api-surface-for-the-database-adapter-layer.md) — The `connectors/__init__.py` file defines the public import surface for PocketPaw's database connector subsystem, re-exporting the key types and factory functions that other subsystems use to obtain database connections. It acts as a stable interface boundary so that callers do not need to know the internal module structure of the connectors package.
- [Database Adapter: Unified Async Interface for SQLite-Backed Storage](./database-adapter-unified-async-interface-for-sqlite-backed-storage.md) — The `db_adapter` module provides an async SQLite wrapper that PocketPaw uses for all structured persistence — session records, memory entries, audit events, and credential metadata. It encapsulates connection lifecycle management, schema migrations, and parameterized queries behind a clean async interface to prevent SQL injection and connection leaks.

### `Deep Work` — 3 article(s)

- [Deep Work API: FastAPI Endpoints for Project Orchestration](./deep-work-api-fastapi-endpoints-for-project-orchestration.md) — deep_work/api.py defines the FastAPI router mounted at `/api/deep-work/*`, exposing REST endpoints for the full Deep Work project lifecycle — goal parsing, project submission, plan retrieval, approval, pause/resume/cancel, and per-task skip/retry operations.
- [Deep Work Module: Public API for AI Project Orchestration](./deep-work-module-public-api-for-ai-project-orchestration.md) — The `pocketpaw.deep_work` package exposes a high-level API for AI-driven project orchestration — from natural language goal parsing through multi-agent execution, pause/resume lifecycle, and crash recovery. The singleton `DeepWorkSession` is lazily initialized and wired to the MessageBus for task completion events.
- [SimulationClock: Discrete Tick-Based Time for Multi-Agent Execution](./simulationclock-discrete-tick-based-time-for-multi-agent-execution.md) — SimulationClock provides a tick-based replacement for wall-clock time in multi-agent simulations, allowing all agents to act synchronously on the same tick before the world advances. TickSnapshot captures immutable world state at each tick for replay and analysis.

### `Developer Experience` — 1 article(s)

- [MCP Server Presets — One-Click Integration Catalog](./mcp-server-presets-one-click-integration-catalog.md) — `presets.py` maintains a curated catalog of pre-configured MCP server templates (`MCPPreset`) that users can install from the PocketPaw dashboard by pasting a single API key, eliminating the need to understand MCP server configuration details for common integrations.

### `Diagnostics and Health` — 3 article(s)

- [CLI Doctor Command: Full Diagnostic Report with Connectivity Checks](./cli-doctor-command-full-diagnostic-report-with-connectivity-checks.md) — The `doctor` command runs PocketPaw's complete two-phase diagnostic suite — startup checks (environment, config, permissions) followed by async connectivity checks (API reachability, channel credentials) — and reports results in either rich human-readable format or structured JSON. It delegates to the health engine rather than reimplementing checks, keeping the CLI thin.
- [CLI Errors Command: Viewing and Filtering Recent Runtime Errors](./cli-errors-command-viewing-and-filtering-recent-runtime-errors.md) — The `errors` CLI command surfaces recent entries from the health engine's error store, showing timestamp, severity, source, message, and the last line of any traceback. It supports text-based filtering and JSON output to integrate with automated alerting workflows.
- [CLI Health Command: Fast Offline Startup Health Check](./cli-health-command-fast-offline-startup-health-check.md) — The `health` command runs only the startup phase of PocketPaw's health engine — synchronous, network-free checks that verify config file presence, API key format, file permissions, and similar environmental prerequisites. It completes in milliseconds and is designed to be run frequently, for example in shell prompts or startup scripts, unlike the full `doctor` command which also runs slow network connectivity checks.

### `Discord Integration` — 1 article(s)

- [MCP Discord Server — stdio Bridge to discli](./mcp-discord-server-stdio-bridge-to-discli.md) — `discord_server.py` implements a stdio-based MCP server that wraps the `discli` CLI tool, exposing Discord operations as MCP tools callable by any MCP-capable agent backend. It runs as a subprocess and communicates via JSON-RPC over stdin/stdout.

### `EE cloud` — 15 article(s)

- [AudienceResolver: Event-to-Recipient Mapping with Short-TTL Caching](./audienceresolver-event-to-recipient-mapping-with-short-ttl-caching.md) — AudienceResolver translates a typed event into the list of user IDs that should receive it over WebSocket, applying domain-specific fan-out rules for groups, messages, workspace membership, sessions, agent streams, notifications, and presence. Member lookups are cached with a configurable short TTL to avoid hammering the database on high-frequency events, and the cache can be invalidated selectively when membership changes.
- [EventBus Protocol and In-Process Fan-Out Implementation](./eventbus-protocol-and-in-process-fan-out-implementation.md) — This module defines the EventBus protocol and the InProcessBus implementation that fans out realtime events to WebSocket connections on the same process, routing through AudienceResolver to determine recipients and ConnectionManager to deliver payloads. Module-level singletons with explicit initialisation guards make it safe to wire up during app startup while crashing loudly if accessed before initialisation.
- [Pocket Layout Export and User-Defined Template Registry](./pocket-layout-export-and-user-defined-template-registry.md) — This module provides two distinct but related capabilities: a pure YAML serialisation of a pocket's ripple spec for export or sharing, and an in-process workspace-scoped store for user-defined pocket templates. Both capabilities back new REST endpoints added in Cluster B Sub-PR #3 to close a gap in the UI testing guide around widget layout save and share.
- [Pockets Domain Pydantic Schemas: Request and Response Models](./pockets-domain-pydantic-schemas-request-and-response-models.md) — This file defines all Pydantic request and response models for the pockets domain, covering creation, update, widget management, reordering, share links, collaborators, and the full pocket response shape. A notable recent addition is support for passing agents, rippleSpec, and an initial widgets list on creation so the frontend can fully specify a pocket in a single request.
- [Pockets FastAPI Router: CRUD, Widgets, Sharing, and Layout Export](./pockets-fastapi-router-crud-widgets-sharing-and-layout-export.md) — This is the FastAPI router for the pockets domain, covering the full lifecycle of a pocket from creation through sharing, widget management, agent and team membership, and the newer layout export and user template endpoints. All routes are gated behind a license check dependency, and owner-only operations use explicit access guard dependencies to prevent privilege escalation.
- [PocketService: Business Logic for Pocket Lifecycle, Widgets, Sharing, and Agents](./pocketservice-business-logic-for-pocket-lifecycle-widgets-sharing-and-agents.md) — PocketService is the stateless service class that encapsulates all pocket business logic — from CRUD and widget management through share link generation, collaborator access, and agent-generated pocket creation from ripple specs. Access control is enforced by two helper functions that distinguish owner-only actions from edit-access actions, and every mutation emits events to the realtime bus and shared event bus.
- [Realtime emit() Facade: Fire-and-Forget Event Dispatch After Mutations](./realtime-emit-facade-fire-and-forget-event-dispatch-after-mutations.md) — The `emit` function is a thin, fire-and-forget facade that service methods call after successful database writes to push realtime events to connected clients. It never raises exceptions back to the caller — a delivery failure must not roll back or abort the preceding database mutation — while a missing bus initialisation surfaces immediately as an AssertionError.
- [Realtime Event Type Catalogue: Typed Dataclasses for All Domain Events](./realtime-event-type-catalogue-typed-dataclasses-for-all-domain-events.md) — This module defines the complete catalogue of realtime events as Python dataclasses, covering workspace lifecycle, group and message operations, presence, typing, file uploads, sessions, agent streaming, and notifications. Each concrete event subclass declares a `ClassVar EVENT_TYPE` string that is automatically applied in `__post_init__`, ensuring the `type` field is always set correctly without manual assignment at every call site.
- [Realtime WebSocket Event Package Entry Point](./realtime-websocket-event-package-entry-point.md) — This is the package initialiser for the `ee.cloud.realtime` module, which houses the WebSocket-based realtime event system for the EE cloud tier. The file itself carries only a module docstring, serving as the namespace anchor that makes `ee.cloud.realtime` importable as a package while the substantive components live in sub-modules.
- [Ripple Spec Normalizer: Canonicalising AI-Generated Pocket Layouts Before Persistence](./ripple-spec-normalizer-canonicalising-ai-generated-pocket-layouts-before-persist.md) — The ripple normalizer is a single-function module that accepts the free-form dict output of agent-generated pocket specs and canonicalises it into a consistent envelope shape before it is written to MongoDB. It handles three distinct spec variants (multi-pane, UISpec v1.0, and flat widget lists) with minimal transformation, and generates stable widget IDs when they are missing to ensure idempotent downstream rendering.
- [Sessions Domain Pydantic Schemas: Request and Response Models](./sessions-domain-pydantic-schemas-request-and-response-models.md) — This file defines the three Pydantic models for the sessions domain: CreateSessionRequest, UpdateSessionRequest, and SessionResponse. The schemas are intentionally lean, supporting optional linkage to pockets, groups, agents, and existing runtime sessions at creation time.
- [Sessions FastAPI Router: CRUD, History, Runtime Sessions, and Activity Tracking](./sessions-fastapi-router-crud-history-runtime-sessions-and-activity-tracking.md) — This is the FastAPI router for the sessions domain, providing endpoints to create, list, fetch, update, and delete user chat sessions, retrieve message history, and manage lightweight runtime sessions that are not persisted to MongoDB. A notable design is the runtime session endpoints that bridge the in-memory memory manager with the persistent session store.
- [Sessions Package Entry Point: Router Re-Export](./sessions-package-entry-point-router-re-export.md) — This is the package initialiser for `ee.cloud.sessions`, which re-exports the FastAPI `router` object from `ee.cloud.sessions.router` to make the router accessible via a short import path. The single re-export is the standard pattern used across EE cloud domain packages to allow the top-level application factory to mount domain routers without importing from sub-modules directly.
- [SessionService: Session Lifecycle, History, and Realtime Event Integration](./sessionservice-session-lifecycle-history-and-realtime-event-integration.md) — SessionService is the stateless service class handling the full lifecycle of chat sessions — creating with upsert support for runtime sessions, listing by agent or pocket, retrieving message history from MongoDB, soft-deleting, and emitting realtime events after each mutation. A touch method provides a lightweight activity heartbeat that increments message count and updates lastActivity.
- [Shared Cross-Cutting Concerns Package Entry Point](./shared-cross-cutting-concerns-package-entry-point.md) — This is the package initialiser for `ee.cloud.shared`, the module that hosts cross-cutting utilities shared across all EE cloud domains. The file contains only a module docstring and no code, making it a namespace anchor that allows other modules to import from `ee.cloud.shared.*` sub-modules like `deps`, `errors`, `events`, and `time`.

### `Error Handling` — 1 article(s)

- [Cloud Files Error Codes and Event Importability Contract Tests](./cloud-files-error-codes-and-event-importability-contract-tests.md) — This test module enforces the public contract of PocketPaw's cloud files error hierarchy and event definitions, verifying that each error carries the correct machine-readable code and HTTP status, and that all file lifecycle events are importable. These tests act as a regression guard for the API surface consumed by clients and downstream event handlers.

### `Event System` — 1 article(s)

- [Cloud Files Error Codes and Event Importability Contract Tests](./cloud-files-error-codes-and-event-importability-contract-tests.md) — This test module enforces the public contract of PocketPaw's cloud files error hierarchy and event definitions, verifying that each error carries the correct machine-readable code and HTTP status, and that all file lifecycle events are importable. These tests act as a regression guard for the API surface consumed by clients and downstream event handlers.

### `Events` — 1 article(s)

- [Events SSE Router — Real-Time System Event Streaming for External Clients](./events-sse-router-real-time-system-event-streaming-for-external-clients.md) — The events router provides a Server-Sent Events endpoint that external clients (such as the Tauri desktop app) can subscribe to for real-time system events without connecting to the WebSocket. It delivers the same event types as the WebSocket — tool_start, tool_result, thinking, error, health_update, inbox_update — via a keepalive-aware HTTP stream.

### `Extension System` — 1 article(s)

- [Kits Router — PawKit Installation, Catalog, and Command Center Data Resolution](./kits-router-pawkit-installation-catalog-and-command-center-data-resolution.md) — The kits router manages PawKits — YAML-defined bundles of tools, skills, and UI panels that extend a PocketPaw agent's capabilities. It provides a catalog browser with installed-status flags, CRUD operations for installed kits, and a data-resolution endpoint that powers command center panels by fetching live data from configured sources.

### `FastAPI` — 16 article(s)

- [API Authorization Dependency: Fail-Closed Scope Enforcement](./api-authorization-dependency-fail-closed-scope-enforcement.md) — `require_scope` is a FastAPI dependency that enforces fail-closed authorization across all API endpoints. It accepts requests only when the caller has been explicitly marked as fully trusted, carries an API key with the required scope, or carries an OAuth2 token with the required scope — rejecting all other requests with HTTP 403.
- [API Backward Compatibility and Versioned Endpoint Tests](./api-backward-compatibility-and-versioned-endpoint-tests.md) — This module verifies that both the legacy `/api/` endpoints and the versioned `/api/v1/` endpoints in the PocketPaw dashboard respond with HTTP 200, guarding against breaking changes during the v1 API migration. It also checks that OpenAPI documentation endpoints remain accessible.
- [API Serve: Headless FastAPI Application for External Clients](./api-serve-headless-fastapi-application-for-external-clients.md) — `create_api_app()` builds a standalone FastAPI application with `v1` REST routers, a `/ws` WebSocket endpoint, and full backend infrastructure (message bus, agent loop, scheduler) — but no dashboard frontend assets. This enables the `pocketpaw serve` command to power external clients like the Tauri desktop app or scripts without serving any HTML.
- [API v1 Router Aggregation and Lazy Module Mounting](./api-v1-router-aggregation-and-lazy-module-mounting.md) — The `__init__.py` for PocketPaw's v1 API layer is responsible for registering every domain router onto the FastAPI application via a single `mount_v1_routers()` function. It uses deferred imports to sidestep circular-dependency problems that arise when a large plugin-style codebase wires itself up at startup.
- [Auth Service - User Profile and Workspace Management](./auth-service-user-profile-and-workspace-management.md) — A stateless FastAPI service class handling the business logic for user authentication, profile management, and active workspace switching. It keeps route handlers thin by acting as the bridge between HTTP requests and the User ODM model.
- [Chat Domain Package Entrypoint](./chat-domain-package-entrypoint.md) — The `ee/cloud/chat/__init__.py` file is the public entrypoint for the chat domain, re-exporting the APIRouter so the application can mount the entire chat surface with a single import. It acts as a stable boundary declaration hiding internal module layout.
- [EE Uploads API Router: Workspace-Scoped Upload and Folder Endpoints](./ee-uploads-api-router-workspace-scoped-upload-and-folder-endpoints.md) — This FastAPI router exposes all upload and folder management endpoints for the EE cloud layer, enforcing workspace scope and per-file access control on every operation. It manages the full file lifecycle — upload, download URL generation, folder CRUD, file move, and soft delete — backed by EEUploadService and FolderStore.
- [Fabric FastAPI Router — Ontology CRUD and Query Endpoints](./fabric-fastapi-router-ontology-crud-and-query-endpoints.md) — The Fabric router exposes HTTP endpoints for managing the ontology layer: defining object types, creating and querying objects, creating and listing links, and retrieving stats. It uses the legacy SQLite `FabricStore` for all persistence and was updated in Cluster C / PR3 to add `GET /fabric/objects` and `GET /fabric/links` so the PocketDataPanel's Objects and Links sub-tabs render real data.
- [FastAPI Dependency Injection for Cloud Routers](./fastapi-dependency-injection-for-cloud-routers.md) — This module provides reusable FastAPI dependency functions that extract identity and workspace context from authenticated requests, and composable action-based RBAC guards. It is the single source of truth for authentication wiring across all cloud EE router endpoints.
- [Files Router Bootstrap Integration Test](./files-router-bootstrap-integration-test.md) — This module contains a single integration test that verifies the files router can be mounted into a FastAPI application via build_files_router and that the /files/tree endpoint responds correctly with a 200 status and a JSON body containing the expected tree structure.
- [Guards Dependency Factories: FastAPI-Injectable RBAC and ABAC Guards](./guards-dependency-factories-fastapi-injectable-rbac-and-abac-guards.md) — The `deps.py` module generates FastAPI dependency callables that enforce authorization on route handlers, pulling workspace membership and role from `request.state` (populated by upstream middleware) and raising `HTTPException(403)` on any denial. It also provides non-FastAPI helper functions for cloud routes that authenticate via fastapi-users and carry full user models rather than request state.
- [Org Journal FastAPI Dependency — Shared Audit Log for EE Routes](./org-journal-fastapi-dependency-shared-audit-log-for-ee-routes.md) — Provides a single shared `Journal` instance as a FastAPI dependency, ensuring all enterprise edition routes write their audit events to one canonical org-level log rather than opening separate per-subsystem SQLite files. Supports test isolation via a cache-reset escape hatch.
- [Shared Cross-Cutting Concerns Package Entry Point](./shared-cross-cutting-concerns-package-entry-point.md) — This is the package initialiser for `ee.cloud.shared`, the module that hosts cross-cutting utilities shared across all EE cloud domains. The file contains only a module docstring and no code, making it a namespace anchor that allows other modules to import from `ee.cloud.shared.*` sub-modules like `deps`, `errors`, `events`, and `time`.
- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.
- [Workspace FastAPI Router — CRUD, Members, and Invites](./workspace-fastapi-router-crud-members-and-invites.md) — This module defines all HTTP endpoints for the workspace domain — workspace lifecycle, member management, and invite flows — using FastAPI's dependency injection to enforce authorization at the route layer while keeping service methods auth-agnostic. Authorization gates are declared inline as `Depends(require_action(...))` so the permission model is readable directly from the route signature.
- [Workspace Module Entry Point](./workspace-module-entry-point.md) — The `ee/cloud/workspace/__init__.py` file serves as the public entry point for the workspace domain, re-exporting the FastAPI router so that the top-level application assembler can mount it without knowing internal package structure. This single-line pattern deliberately decouples how the router is imported from where it physically lives.

### `FastAPI router` — 1 article(s)

- [EE Upload Router Integration Tests: Roundtrip, Workspace Isolation, Owner-Only Access, and Bulk Partial Success](./ee-upload-router-integration-tests-roundtrip-workspace-isolation-owner-only-acce.md) — Integration tests for the EE uploads FastAPI router covering the full upload-download-delete cycle, cross-workspace 404 enforcement, owner-only read access within a workspace, and the partial-success bulk upload response that distinguishes accepted files from rejected MIME types.

### `Files` — 1 article(s)

- [Files Router — Scoped File Browser, Download, and Write with Path Traversal Guards](./files-router-scoped-file-browser-download-and-write-with-path-traversal-guards.md) — The files router gives the PocketPaw dashboard a full file browser over the agent's accessible filesystem: directory listing, file content serving, single-file download, directory-as-zip download, and file writing. Security hardening in April 2026 added scope guards and symlink filtering to prevent path traversal attacks identified in issues #884 and #886.

### `Firebase` — 2 article(s)

- [Firebase Connector: YAML Definition, CLI Adapter, and Trust Level Tests](./firebase-connector-yaml-definition-cli-adapter-and-trust-level-tests.md) — This test suite validates PocketPaw's Firebase connector from three angles: the declarative YAML definition (action count, trust levels, auth method), the `FirebaseAdapter` class that wraps the Firebase CLI via async subprocess, and the connector registry integration. It uses mocked subprocess calls to test connect, execute, timeout, and error paths without touching a real Firebase project.
- [FirebaseAdapter: CLI-Backed Connector for Firestore, Auth, Hosting, Functions, and Remote Config](./firebaseadapter-cli-backed-connector-for-firestore-auth-hosting-functions-and-re.md) — `FirebaseAdapter` implements the `ConnectorProtocol` by shelling out to the `firebase` CLI with `--json --non-interactive` flags rather than making REST calls directly, delegating authentication entirely to the Firebase CLI's own session. It supports a wide surface: Firestore CRUD, Auth user management, Hosting deploys, Cloud Functions, Remote Config, and Extensions.

### `Google Cloud` — 1 article(s)

- [FirebaseAdapter: CLI-Backed Connector for Firestore, Auth, Hosting, Functions, and Remote Config](./firebaseadapter-cli-backed-connector-for-firestore-auth-hosting-functions-and-re.md) — `FirebaseAdapter` implements the `ConnectorProtocol` by shelling out to the `firebase` CLI with `--json --non-interactive` flags rather than making REST calls directly, delegating authentication entirely to the Firebase CLI's own session. It supports a wide surface: Firestore CRUD, Auth user management, Hosting deploys, Cloud Functions, Remote Config, and Extensions.

### `Google Cloud Platform` — 2 article(s)

- [GCP Connector: YAML Definition, Adapter Authentication, and Execute Path Tests](./gcp-connector-yaml-definition-adapter-authentication-and-execute-path-tests.md) — This test suite validates PocketPaw's Google Cloud Platform connector, covering the `gcp.yaml` declarative definition (20 actions across compute, storage, pub/sub, secrets, Cloud Run, and logging), the `GCPAdapter` lifecycle (connect, execute, disconnect), and registry wiring. All subprocess calls are mocked to avoid requiring live GCP credentials.
- [GCPAdapter: Google Cloud Platform Connector via gcloud CLI with Multi-Path Binary Discovery](./gcpadapter-google-cloud-platform-connector-via-gcloud-cli-with-multi-path-binary.md) — `GCPAdapter` implements `ConnectorProtocol` by wrapping the `gcloud` CLI, using `--format=json` for structured output and supporting project and region overrides via config. It scans several common install paths to locate the binary, making it resilient to non-standard SDK installations.

### `Google Drive` — 6 article(s)

- [Drive Connector Error Hierarchy: Typed Exceptions for Auth, Rate Limits, and Not-Found Failures](./drive-connector-error-hierarchy-typed-exceptions-for-auth-rate-limits-and-not-fo.md) — `errors.py` defines a minimal, four-class exception hierarchy for the Drive connector so callers can distinguish auth failures, rate-limit exhaustion, and missing-resource errors without string-sniffing. The deliberate smallness is itself a design decision — each exception maps one-to-one to an actionable recovery path.
- [Drive OAuth Bearer Token Resolution: Credential Broker, Env Var, and OAuthManager Fallback Chain](./drive-oauth-bearer-token-resolution-credential-broker-env-var-and-oauthmanager-f.md) — `auth.py` implements a prioritised three-source token-resolution chain so the Drive connector always has a valid OAuth bearer token regardless of environment (production, CI, local dev). It isolates token concerns from `source.py`, keeping the adapter code focused on business logic.
- [DriveClient: Sync HTTP Client for Drive v3 with Exponential-Backoff Retry and Point-in-Time Awareness](./driveclient-sync-http-client-for-drive-v3-with-exponential-backoff-retry-and-poi.md) — `DriveClient` is a purpose-built, sync HTTP client for the Google Drive v3 API that handles rate-limit retries with exponential backoff and jitter, normalises file and revision metadata into typed dataclasses, and stays intentionally separate from the global OAuth token store used by PocketPaw's built-in Drive tools.
- [DriveSourceAdapter: Zero-Copy Live Federation over the Drive v3 API with Point-in-Time Query Support](./drivesourceadapter-zero-copy-live-federation-over-the-drive-v3-api-with-point-in.md) — `DriveSourceAdapter` is the first concrete `SourceAdapter` in PocketPaw, implementing zero-copy live federation against Google Drive: it returns `DataRef` pointers to live files rather than copying bytes into PocketPaw storage. It also pioneered a lightweight `@at=<iso>|query` convention for point-in-time retrieval before soul-protocol adds a native field.
- [Google Drive Connector: DriveClient, SourceAdapter, and RetrievalRouter Integration Tests](./google-drive-connector-driveclient-sourceadapter-and-retrievalrouter-integration.md) — This test suite covers PocketPaw's Google Drive connector end-to-end: HTTP request plumbing with rate-limit backoff, point-in-time revision lookup, `DriveSourceAdapter.query` result shaping, token resolution precedence, and `RetrievalRouter` integration with journal emission. All Drive API traffic is eliminated by a scripted fake `httpx.Client`.
- [Google Drive Connector Module: Public Surface and Package Exports](./google-drive-connector-module-public-surface-and-package-exports.md) — The `pocketpaw.connectors.drive` package is the Google Drive adapter introduced in Workstream C2 of the Org Architecture RFC. Its `__init__.py` defines a minimal public surface, re-exporting the auth helper, HTTP client, error hierarchy, and the `DriveSourceAdapter` that powers zero-copy live federation.

### `Google Gemini` — 1 article(s)

- [GeminiAdapter: Google AI via OpenAI-Compatible Endpoint](./geminiadapter-google-ai-via-openai-compatible-endpoint.md) — `GeminiAdapter` routes Gemini API calls through Google's OpenAI-compatible endpoint, allowing PocketPaw to use the same `AsyncOpenAI` and `AsyncAnthropic` client patterns it uses for other providers. Both client builders point to `GEMINI_BASE_URL` and use `"not-needed"` as a placeholder API key fallback to satisfy SDK validation without passing a real credential.

### `Google Workspace` — 8 article(s)

- [Gmail Client — OAuth-Backed HTTP Client for Gmail API](./gmail-client-oauth-backed-http-client-for-gmail-api.md) — `GmailClient` provides async methods for searching messages, reading full message content, sending emails, and listing labels via the Gmail API, using OAuth bearer tokens from `OAuthManager`. It handles base64url-encoded message bodies and MIME multipart structures for plain text extraction.
- [Gmail Tools: Search, Read, Send, Label, and Batch Modify Emails](./gmail-tools-search-read-send-label-and-batch-modify-emails.md) — The `gmail.py` module provides eight `BaseTool` subclasses covering the full Gmail management surface: `GmailSearchTool`, `GmailReadTool`, `GmailSendTool`, `GmailListLabelsTool`, `GmailCreateLabelTool`, `GmailModifyTool`, `GmailTrashTool`, and `GmailBatchModifyTool`. All tools carry `trust_level = "high"` because email access is sensitive and requires explicit OAuth authorization; the batch modify tool exists specifically to reduce API round-trips when applying label changes to many messages.
- [Google Calendar Client — OAuth-Backed HTTP Client for Calendar API v3](./google-calendar-client-oauth-backed-http-client-for-calendar-api-v3.md) — `CalendarClient` provides async methods for listing upcoming events and creating new calendar events via the Google Calendar API v3, using OAuth bearer tokens managed by `OAuthManager` with automatic token refresh. It is part of PocketPaw's Phase 2 Integration Ecosystem.
- [Google Docs Client — OAuth-Backed HTTP Client for Docs API v1](./google-docs-client-oauth-backed-http-client-for-docs-api-v1.md) — `DocsClient` provides async methods for reading Google Docs as plain text and creating new documents with initial content, using OAuth bearer tokens from `OAuthManager`. It includes a recursive text extraction helper that walks the Docs API's nested paragraph structure.
- [Google Docs Tools: Read, Create, and Search Documents](./google-docs-tools-read-create-and-search-documents.md) — The `gdocs.py` module provides three `BaseTool` subclasses — `DocsReadTool`, `DocsCreateTool`, and `DocsSearchTool` — that allow the PocketPaw agent to read, create, and search Google Docs documents via the Google Docs API. A helper function `_parse_doc_id` normalizes both raw document IDs and full Google Docs URLs so the agent does not need to extract the ID manually.
- [Google Drive Client — OAuth-Backed HTTP Client for Drive API v3](./google-drive-client-oauth-backed-http-client-for-drive-api-v3.md) — `DriveClient` provides async methods for listing, searching, downloading, uploading, and trashing files in Google Drive via the Drive API v3, using OAuth bearer tokens from `OAuthManager`. Downloaded files are saved to `~/.pocketpaw/downloads/` with automatic directory creation.
- [Google Drive Tools: List, Download, Upload, and Share Files](./google-drive-tools-list-download-upload-and-share-files.md) — The `gdrive.py` module exposes four `BaseTool` subclasses — `DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, and `DriveShareTool` — giving the PocketPaw agent full CRUD and sharing access to Google Drive. A `_GDRIVE_ROLES` frozenset enforces valid sharing permissions at the tool layer before the API call is made, preventing invalid-role errors from propagating to the user.
- [GWS CLI System Prompt Injection Tests](./gws-cli-system-prompt-injection-tests.md) — This test suite verifies that the AgentContextBuilder conditionally injects Google Workspace CLI guidance into the system prompt only when the google-workspace MCP server is active and enabled. It also confirms that the gws.md guidance file exists on disk with the required content.

### `Google integration` — 1 article(s)

- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.

### `Google integrations` — 7 article(s)

- [Gmail Integration Body Extraction and Tool Tests](./gmail-integration-body-extraction-and-tool-tests.md) — This test module validates `GmailClient._extract_body`, which parses Gmail's MIME structure to extract plain text from messages, and the three Gmail tools (`GmailSearchTool`, `GmailReadTool`, `GmailSendTool`) — checking their schemas and verifying that each returns a user-readable error when OAuth authentication is absent.
- [Google ADK Backend Tests](./google-adk-backend-tests.md) — This module provides fully mocked tests for `GoogleADKBackend`, PocketPaw's Google Agent Development Kit integration. It covers metadata/capability discovery, initialization with and without the SDK, event stream processing (text, function calls, function responses, errors), session management, MCP toolset policy enforcement, status reporting, and the backward-compatibility alias `GeminiCLIBackend`.
- [Google Calendar Tool Schema and Auth Error Tests](./google-calendar-tool-schema-and-auth-error-tests.md) — This test module validates the three Google Calendar tools (`CalendarListTool`, `CalendarCreateTool`, `CalendarPrepTool`) — checking their names, trust levels, and required parameter schemas — and verifies that each tool returns a user-readable error message rather than propagating an exception when OAuth authentication is unavailable.
- [Google Chat Channel Adapter Tests](./google-chat-channel-adapter-tests.md) — This test module provides full coverage for `GoogleChatAdapter`, which connects PocketPaw to Google Chat via webhook or Pub/Sub mode. It covers initialization, webhook message handling (including slash commands and space filtering), streaming message buffering, error recovery, `MessageBus` integration, and adapter lifecycle management.
- [Google Docs Integration Tool Schema and Execution Tests](./google-docs-integration-tool-schema-and-execution-tests.md) — This test module validates the three Google Docs tools (`DocsReadTool`, `DocsCreateTool`, `DocsSearchTool`), the `_parse_doc_id` URL-parsing utility, and the `DocsClient._extract_text` method that converts the Docs API's nested JSON structure into plain text. It also confirms that each tool returns actionable error messages when OAuth authentication is absent.
- [Google Drive Integration Tool Schema and Execution Tests](./google-drive-integration-tool-schema-and-execution-tests.md) — This test module validates the four Google Drive tools (`DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, `DriveShareTool`), their parameter schemas, and their behavior when authentication is absent or inputs are invalid. It also verifies `DriveListTool`'s happy path with a mocked HTTP response and confirms that `DriveShareTool` rejects invalid sharing roles at the tool layer.
- [ImageGenerateTool: Text-to-Image Generation via Google Gemini](./imagegeneratetool-text-to-image-generation-via-google-gemini.md) — The `ImageGenerateTool` gives the PocketPaw agent the ability to generate images from text prompts using the Google Gemini image generation API (internally called "Nano Banana"). Generated images are saved to a deterministic directory under the PocketPaw config folder with UUID-based filenames, and the tool returns the local file path so the agent can chain the result with `OpenExplorerTool` or other file-handling tools.

### `Guardian` — 1 article(s)

- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.

### `HTTP` — 2 article(s)

- [Critical Gaps Tests: Real HTTP in YAML Connectors, Fabric Queries, and Instinct Tool Operations](./critical-gaps-tests-real-http-in-yaml-connectors-fabric-queries-and-instinct-too.md) — This test module, created in March 2026, addresses three previously untested integration paths: the `DirectRESTAdapter` making authenticated HTTP calls to YAML-defined connectors like Stripe, the Fabric knowledge-graph query tools, and the Instinct action-proposal tools. It uses real YAML connector definitions from the connectors directory and mocks only the network layer.
- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.

### `HTTP API` — 2 article(s)

- [Fabric FastAPI Router — Ontology CRUD and Query Endpoints](./fabric-fastapi-router-ontology-crud-and-query-endpoints.md) — The Fabric router exposes HTTP endpoints for managing the ontology layer: defining object types, creating and querying objects, creating and listing links, and retrieving stats. It uses the legacy SQLite `FabricStore` for all persistence and was updated in Cluster C / PR3 to add `GET /fabric/objects` and `GET /fabric/links` so the PocketDataPanel's Objects and Links sub-tabs render real data.
- [Workspace FastAPI Router — CRUD, Members, and Invites](./workspace-fastapi-router-crud-members-and-invites.md) — This module defines all HTTP endpoints for the workspace domain — workspace lifecycle, member management, and invite flows — using FastAPI's dependency injection to enforce authorization at the route layer while keeping service methods auth-agnostic. Authorization gates are declared inline as `Depends(require_action(...))` so the permission model is readable directly from the route signature.

### `HTTP client` — 2 article(s)

- [A2A Client: HTTP Client for Delegating Tasks to External A2A Agents](./a2a-client-http-client-for-delegating-tasks-to-external-a2a-agents.md) — `A2AClient` is an async HTTP client that allows PocketPaw to communicate with external A2A-compatible agents. It supports blocking task submission, SSE streaming, task polling, cancellation, and Agent Card discovery — with connection sharing, TTL-cached Agent Cards, and transparent wrapping of direct-Message responses into the Task model.
- [DriveClient: Sync HTTP Client for Drive v3 with Exponential-Backoff Retry and Point-in-Time Awareness](./driveclient-sync-http-client-for-drive-v3-with-exponential-backoff-retry-and-poi.md) — `DriveClient` is a purpose-built, sync HTTP client for the Google Drive v3 API that handles rate-limit retries with exponential backoff and jitter, normalises file and revision metadata into typed dataclasses, and stays intentionally separate from the global OAuth token store used by PocketPaw's built-in Drive tools.

### `HTTP clients` — 1 article(s)

- [Reddit Integration: Read-Only Client via Public JSON API](./reddit-integration-read-only-client-via-public-json-api.md) — PocketPaw's Reddit client provides read-only access to Reddit posts and subreddits using Reddit's public JSON API, requiring no API key or authentication. It enforces a one-request-per-second rate limit to comply with Reddit's unauthenticated access policy.

### `HTTP utilities` — 1 article(s)

- [HTTP Utilities — Secure Request Detection via Forwarded Headers](./http-utilities-secure-request-detection-via-forwarded-headers.md) — `http_utils.py` provides `is_request_secure()`, a FastAPI helper that determines whether an incoming request arrived over HTTPS by inspecting both the direct URL scheme and reverse proxy forwarding headers (`X-Forwarded-Proto` and RFC 7239 `Forwarded`). This is essential for correctly setting secure cookies and enforcing HTTPS-only features when PocketPaw runs behind a tunnel or proxy.

### `Idempotency` — 1 article(s)

- [MongoMemoryStore Deduplication Tests: Idempotent Saves Within the 5-Second Window](./mongomemorystore-deduplication-tests-idempotent-saves-within-the-5-second-window.md) — This module tests the deduplication logic in `MongoMemoryStore`, which prevents identical back-to-back writes from creating duplicate memory entries when an agent loop retries the same turn. The dedup key is `(session_key, role, content)` with a 5-second window, and the tests verify that only different content, session, or role values produce distinct database rows.

### `Identity` — 1 article(s)

- [Identity Router — Agent Identity File Management via REST](./identity-router-agent-identity-file-management-via-rest.md) — The identity router exposes PocketPaw's five agent identity files (IDENTITY.md, SOUL.md, STYLE.md, INSTRUCTIONS.md, USER.md) through a simple read/write REST interface, allowing dashboard users to customize the agent's persona, communication style, and behavioral instructions without editing files directly. Changes take effect on the next inbound message without requiring a server restart.

### `Integration` — 3 article(s)

- [Channels Router — Multi-Platform Adapter Status, Configuration, and Dynamic Toggle](./channels-router-multi-platform-adapter-status-configuration-and-dynamic-toggle.md) — The channels router manages PocketPaw's suite of messaging adapters — Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat — through a unified REST interface. It allows the dashboard to inspect adapter state, save credentials, start or stop adapters at runtime without restarting the server, retrieve WhatsApp QR codes, and install optional channel dependencies on demand.
- [Connectors Router — Data Source CRUD, Credential Management, and Action Execution](./connectors-router-data-source-crud-credential-management-and-action-execution.md) — The connectors router provides a structured REST interface over PocketPaw's `ConnectorRegistry`, allowing the dashboard and enterprise UI to connect agents to external data sources (databases, APIs, cloud services) and execute actions against them. A structured status endpoint was added specifically to feed the `ConnectorCard` UI component with credential state and sync metadata.
- [MCP Router — Model Context Protocol Server Management and OAuth Integration](./mcp-router-model-context-protocol-server-management-and-oauth-integration.md) — The MCP router manages PocketPaw's connections to external Model Context Protocol servers — adding, removing, toggling, and testing MCP server configurations at runtime. It also provides a preset catalog for one-click MCP server installation, an OAuth callback endpoint for providers that require browser-based authorization, and auto-installation of Google Workspace skills when the GWS MCP preset is activated.

### `Integration Tests` — 1 article(s)

- [Cloud Files FastAPI Router Integration Tests: Tree and Browse Endpoints](./cloud-files-fastapi-router-integration-tests-tree-and-browse-endpoints.md) — This module runs end-to-end HTTP integration tests against the FastAPI router built by `build_router`, covering tree structure rendering, entry browsing, 404 for unknown mounts, and 403 for cross-workspace access attempts. It uses HTTPX's `ASGITransport` to drive the full request-response cycle in-process without a running server.

### `JSON-RPC` — 2 article(s)

- [A2A JSON-RPC Dispatcher: Method Registration and Request Routing](./a2a-json-rpc-dispatcher-method-registration-and-request-routing.md) — `A2ADispatcher` parses incoming JSON-RPC 2.0 requests, validates the envelope, and routes them to registered async handlers. It supports both blocking dispatch and SSE streaming dispatch, batch requests, and consistent error handling — with a deliberate separation between stream-only and non-stream methods.
- [A2A Protocol Error Codes and JSON-RPC 2.0 Error Handling](./a2a-protocol-error-codes-and-json-rpc-20-error-handling.md) — This module defines the full error code registry for the A2A protocol layer — combining standard JSON-RPC 2.0 error codes with A2A-specific extensions — and provides the `JSONRPCError` exception class and helper functions for building well-formed error and success response envelopes.

### `Kits` — 1 article(s)

- [Kits Router — PawKit Installation, Catalog, and Command Center Data Resolution](./kits-router-pawkit-installation-catalog-and-command-center-data-resolution.md) — The kits router manages PawKits — YAML-defined bundles of tools, skills, and UI panels that extend a PocketPaw agent's capabilities. It provides a catalog browser with installed-status flags, CRUD operations for installed kits, and a data-resolution endpoint that powers command center panels by fetching live data from configured sources.

### `LLM` — 2 article(s)

- [Knowledge Compiler: LLM-Powered Raw Text to Structured Wiki Articles](./knowledge-compiler-llm-powered-raw-text-to-structured-wiki-articles.md) — The knowledge compiler is the intelligence layer of PocketPaw's knowledge engine — it sends raw documents to an LLM with a strict prompt that elicits a structured JSON article with title, summary, content, concepts, and categories. Robust JSON parsing handles markdown fences and partial LLM output that would otherwise cause silent data loss.
- [Knowledge Linter: LLM-Powered Wiki Health Auditor](./knowledge-linter-llm-powered-wiki-health-auditor.md) — The knowledge linter uses an LLM to audit the wiki for inconsistencies, coverage gaps, missing cross-links, stale content, and uncompiled raw documents. It produces structured `LintIssue` objects with severity levels and actionable suggestions, functioning as an automated knowledge quality reviewer.

### `LLM backends` — 1 article(s)

- [LLM Connectivity Health Checks — Provider-Aware Reachability](./llm-connectivity-health-checks-provider-aware-reachability.md) — This module implements async health checks that verify whether the configured LLM API endpoint is reachable, routing to the correct check function based on the active backend and provider. It covers Anthropic, OpenAI, Google AI, Ollama, OpenRouter, LiteLLM, and generic OpenAI-compatible endpoints, each with a 5-second timeout and structured pass/warn/critical results.

### `LLM context` — 1 article(s)

- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.

### `LLM integration` — 17 article(s)

- [AnthropicAdapter: Native Anthropic API Configuration and Client Construction](./anthropicadapter-native-anthropic-api-configuration-and-client-construction.md) — `AnthropicAdapter` implements the `ProviderAdapter` protocol for Anthropic's native API, resolving model and API key from settings and constructing `AsyncAnthropic` clients. It explicitly raises `NotImplementedError` on `build_openai_client()` to enforce that Anthropic-native callers never accidentally get an OpenAI-compatible client.
- [GeminiAdapter: Google AI via OpenAI-Compatible Endpoint](./geminiadapter-google-ai-via-openai-compatible-endpoint.md) — `GeminiAdapter` routes Gemini API calls through Google's OpenAI-compatible endpoint, allowing PocketPaw to use the same `AsyncOpenAI` and `AsyncAnthropic` client patterns it uses for other providers. Both client builders point to `GEMINI_BASE_URL` and use `"not-needed"` as a placeholder API key fallback to satisfy SDK validation without passing a real credential.
- [LiteLLMAdapter: Proxy and Direct SDK Modes for Universal LLM Routing](./litellmadapter-proxy-and-direct-sdk-modes-for-universal-llm-routing.md) — `LiteLLMAdapter` supports two integration modes — a proxy mode that routes through a running LiteLLM server via OpenAI-compatible HTTP, and a direct SDK mode that uses LiteLLM's native Python wrappers when available. This dual-mode design lets PocketPaw work with any model LiteLLM supports without requiring each deployment to run a proxy server.
- [LLM Package Public API: LLMClient, LLMRouter, and resolve_llm_client](./llm-package-public-api-llmclient-llmrouter-and-resolvellmclient.md) — The `pocketpaw.llm` package exposes three public symbols — `LLMClient`, `LLMRouter`, and `resolve_llm_client` — forming the canonical entry point for all LLM interactions in PocketPaw. This init file enforces a stable import surface so internal module restructuring does not break callsites.
- [LLM Provider Adapters Package: Public Interface for get_adapter](./llm-provider-adapters-package-public-interface-for-getadapter.md) — The `pocketpaw.llm.providers` package re-exports `ProviderAdapter`, `ProviderConfig`, `get_adapter`, and `resolve_model` as its public API, giving callers a single import point for the provider adapter system. The init file documents the canonical usage pattern so it doubles as inline API documentation.
- [LLMClient Abstraction: Provider Resolution, Client Construction, and SDK Environment Setup](./llmclient-abstraction-provider-resolution-client-construction-and-sdk-environmen.md) — The `LLMClient` tests validate the centralized abstraction over multiple LLM backends — Anthropic, OpenAI, Ollama, and OpenRouter — ensuring that provider auto-detection, client instantiation, and SDK environment variable injection all behave correctly for each provider and credential combination. The frozen dataclass design prevents accidental mutation after resolution.
- [LLMClient: Centralized Provider Detection, Client Creation, and Environment Setup](./llmclient-centralized-provider-detection-client-creation-and-environment-setup.md) — `LLMClient` is an immutable descriptor that captures a fully resolved LLM provider configuration and exposes methods to create provider-specific async clients. The companion `resolve_backend_env()` function pushes the correct environment variables for the active backend, eliminating manual env-var wrangling when switching providers.
- [LLMRouter: Empty-Response Fallback and Multi-Provider Routing (Issue #664)](./llmrouter-empty-response-fallback-and-multi-provider-routing-issue-664.md) — The LLMRouter test suite focuses on a specific regression (issue #664) where an empty `choices` or `content` list from the upstream LLM API caused an unhandled `IndexError`, crashing the agent loop. Tests also validate backend auto-detection logic for all supported providers — OpenAI-compatible, OpenRouter, Gemini, LiteLLM — and ensure `chat()` routes each provider through the correct internal method.
- [LLMRouter: Lightweight Chat Completion Utility for Non-Agent Callers](./llmrouter-lightweight-chat-completion-utility-for-non-agent-callers.md) — `LLMRouter` is a simple, stateful chat utility for one-off completions — not used by agent backends. It auto-detects the best available backend (Ollama first, then cloud providers), maintains a conversation history list, and provides `clear_history()` for resetting between sessions. Its documented limitations (no streaming, no token counting, unbounded history) make its intended scope explicit.
- [Mem0 Memory Store: Backend Selection, Config Building, and Pluggable Provider Integration](./mem0-memory-store-backend-selection-config-building-and-pluggable-provider-integ.md) — The Mem0 store test suite validates PocketPaw's integration with the `mem0ai` package as an optional semantic memory backend, covering the factory function's backend selection and fallback logic, `_build_mem0_config` helper output for all supported LLM and embedder providers, and the `Mem0MemoryStore` CRUD operations including tag filtering and semantic search.
- [OllamaAdapter: Local LLM Integration via OpenAI-Compatible API](./ollamaadapter-local-llm-integration-via-openai-compatible-api.md) — `OllamaAdapter` connects PocketPaw to locally running Ollama instances, using `"ollama"` as a placeholder API key to satisfy client SDK validation while pointing both OpenAI and Anthropic client builders at Ollama's OpenAI-compatible HTTP endpoint. Error messages distinguish between model-not-found and service-not-running failures to give users actionable guidance.
- [OpenAICompatibleAdapter: Generic OpenAI-Compatible Endpoint Support](./openaicompatibleadapter-generic-openai-compatible-endpoint-support.md) — `OpenAICompatibleAdapter` provides a catch-all provider adapter for any service that exposes an OpenAI-compatible REST API — vLLM, LocalAI, Together AI, Anyscale, and similar endpoints. It reads `openai_compatible_base_url` and `openai_compatible_api_key` from settings and constructs both `AsyncOpenAI` and `AsyncAnthropic` clients pointing at that endpoint.
- [OpenRouterAdapter: Multi-Model Routing with Anthropic-Compat Authentication Quirks](./openrouteradapter-multi-model-routing-with-anthropic-compat-authentication-quirk.md) — `OpenRouterAdapter` integrates OpenRouter's model routing service, which provides access to 100+ models via a single API key. Its most distinctive behavior is an intentional `ANTHROPIC_API_KEY=""` (empty string) workaround: OpenRouter authenticates via `ANTHROPIC_AUTH_TOKEN`, and if `ANTHROPIC_API_KEY` is set to a real value, the Anthropic SDK sends it as `Bearer` and OpenRouter rejects the request.
- [Paw Soul Tools Tests: SoulRemember, SoulRecall, SoulEditCore, and SoulStatus](./paw-soul-tools-tests-soulremember-soulrecall-souleditcore-and-soulstatus.md) — These tests cover the four built-in soul tools that PocketPaw exposes to LLMs: `soul_remember`, `soul_recall`, `soul_edit_core`, and `soul_status`. Each tool is validated for its parameter schema, execution behavior, error handling, and JSON Schema export in both Anthropic and OpenAI formats.
- [Provider Adapter Base Types: ProviderConfig, ProviderAdapter Protocol, and Model Resolution](./provider-adapter-base-types-providerconfig-provideradapter-protocol-and-model-re.md) — `base.py` defines the shared vocabulary for the entire provider adapter system: `ProviderConfig` (the resolved connection config), `ProviderAdapter` (the structural Protocol every adapter implements), and `resolve_model()` (the multi-tier model name fallback chain). These types are the contract between the LLM client layer and individual provider implementations.
- [Provider Adapter Registry: Centralized Adapter Lookup with Descriptive Errors](./provider-adapter-registry-centralized-adapter-lookup-with-descriptive-errors.md) — The provider registry is a module-level dict mapping provider name strings to singleton adapter instances, with a `get_adapter()` function that raises `KeyError` with the list of available providers on unknown names. Pre-instantiating adapters as singletons avoids repeated construction overhead and provides a single point of truth for which providers PocketPaw supports.
- [Tool Protocol and Base Class: The Contract Every PocketPaw Tool Implements](./tool-protocol-and-base-class-the-contract-every-pocketpaw-tool-implements.md) — The `protocol.py` module defines the structural contract that all PocketPaw tools must satisfy -- a `ToolDefinition` dataclass for LLM schema representation, a `ToolProtocol` for structural subtyping, and a `BaseTool` abstract class providing default implementations and helper methods. Cross-backend schema normalization is handled here to prevent silent failures with strict OpenAI-style validators.

### `LLM providers` — 3 article(s)

- [LLM Provider Adapter Tests: Registry, Model Resolution, and Per-Provider Configuration](./llm-provider-adapter-tests-registry-model-resolution-and-per-provider-configurat.md) — PocketPaw supports multiple LLM providers (Anthropic, Ollama, OpenRouter, Gemini, LiteLLM) through a unified adapter pattern. These tests validate the provider registry, model string resolution priority, per-adapter environment variable construction, and the `LLMClient` delegation layer that dispatches to the correct adapter.
- [Ollama Integration: Provider Detection, Env Var Construction, and CLI Health Check](./ollama-integration-provider-detection-env-var-construction-and-cli-health-check.md) — Tests for PocketPaw's Ollama provider support, verifying that LLMClient correctly detects Ollama from settings, constructs the right subprocess environment variables, and skips smart model routing for local models. Also covers the `--check-ollama` CLI command that validates server reachability and model availability.
- [OpenAI-Compatible Provider: Detection, Env Vars, Client Creation, and Error Formatting](./openai-compatible-provider-detection-env-vars-client-creation-and-error-formatti.md) — Tests for PocketPaw's OpenAI-compatible endpoint support in LLMClient, enabling any OpenAI-compatible API (LiteLLM, Groq, local proxies) as the LLM backend. Covers provider detection, model resolution, environment variable construction for subprocess mode, Anthropic client creation with custom base URL, and structured error formatting for connection and authentication failures.

### `LLM-integration` — 2 article(s)

- [Deep Work Goal Parser: GoalAnalysis Validation and Parsing Tests](./deep-work-goal-parser-goalanalysis-validation-and-parsing-tests.md) — This test suite covers the GoalParser and GoalAnalysis components in PocketPaw's Deep Work system, validating JSON parsing, domain/complexity/research-depth validation, field clamping, and serialization round-trips. It ensures robust defensive behavior when LLM-generated goal payloads contain invalid or out-of-range values.
- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.

### `LangChain integration` — 1 article(s)

- [Deep Agents Backend Tests: LangChain Integration, Provider Parsing, Content Extraction, and Registry](./deep-agents-backend-tests-langchain-integration-provider-parsing-content-extract.md) — This test module validates the `DeepAgentsBackend` — PocketPaw's LangChain-based agent backend — through mocks, requiring no real LangChain or LangGraph installation. It covers static metadata, the `provider:model` string parsing convention, LangGraph `Overwrite` object unwrapping, content extraction from mixed message formats, graceful degradation when the SDK is absent, and backend registry integration.

### `Lifecycle Management` — 1 article(s)

- [Dashboard Lifecycle: Startup, Shutdown, and Broadcast Orchestration](./dashboard-lifecycle-startup-shutdown-and-broadcast-orchestration.md) — dashboard_lifecycle.py owns the `startup_event()` and `shutdown_event()` functions that initialize and tear down every PocketPaw subsystem in the correct order, plus broadcast helpers that push notifications, audit entries, and health updates to all connected WebSocket clients.

### `LiteLLM` — 1 article(s)

- [LiteLLMAdapter: Proxy and Direct SDK Modes for Universal LLM Routing](./litellmadapter-proxy-and-direct-sdk-modes-for-universal-llm-routing.md) — `LiteLLMAdapter` supports two integration modes — a proxy mode that routes through a running LiteLLM server via OpenAI-compatible HTTP, and a direct SDK mode that uses LiteLLM's native Python wrappers when available. This dual-mode design lets PocketPaw work with any model LiteLLM supports without requiring each deployment to run a proxy server.

### `MCP` — 3 article(s)

- [In-Process MCP Binding: Delivering Pocket Context to the Claude Agent SDK](./in-process-mcp-binding-delivering-pocket-context-to-the-claude-agent-sdk.md) — This module registers an in-process MCP server named `pocketpaw_pocket` that exposes a `get_pocket` tool, working around a Windows `CreateProcess` command-line length limit of ~32KB that would be exceeded if pocket documents were embedded directly in the system prompt. The actual pocket data is fetched lazily from `ee/cloud/pockets/agent_context.py` only when the agent calls the tool.
- [MCP Router — Model Context Protocol Server Management and OAuth Integration](./mcp-router-model-context-protocol-server-management-and-oauth-integration.md) — The MCP router manages PocketPaw's connections to external Model Context Protocol servers — adding, removing, toggling, and testing MCP server configurations at runtime. It also provides a preset catalog for one-click MCP server installation, an OAuth callback endpoint for providers that require browser-based authorization, and auto-installation of Google Workspace skills when the GWS MCP preset is activated.
- [MCP Router Tests: Server Management and OAuth Callback Handling](./mcp-router-tests-server-management-and-oauth-callback-handling.md) — This test file covers PocketPaw's `/api/v1/mcp` router, which manages Model Context Protocol (MCP) server connections — add, remove, toggle, and status — and handles the OAuth callback that completes browser-based MCP server authentication flows.

### `MCP Integration` — 6 article(s)

- [MCP Client Support Package Entry Point](./mcp-client-support-package-entry-point.md) — The `pocketpaw.mcp` package provides PocketPaw with the ability to connect to any Model Context Protocol (MCP) server and invoke its tools without requiring custom tool implementations for each integration. This module re-exports all public symbols from the config and manager sub-modules, giving consumers a single clean import surface.
- [MCP Discord Server — stdio Bridge to discli](./mcp-discord-server-stdio-bridge-to-discli.md) — `discord_server.py` implements a stdio-based MCP server that wraps the `discli` CLI tool, exposing Discord operations as MCP tools callable by any MCP-capable agent backend. It runs as a subprocess and communicates via JSON-RPC over stdin/stdout.
- [MCP OAuth Token Storage — Secure File-Based Persistence](./mcp-oauth-token-storage-secure-file-based-persistence.md) — `MCPTokenStorage` implements the MCP SDK's `TokenStorage` protocol, persisting OAuth access tokens and dynamic client registration data to per-server JSON files under `~/.pocketpaw/mcp_oauth/`. Files are written with `chmod 0600` to prevent token leakage to other OS users.
- [MCP Server Configuration — Persistence and Schema](./mcp-server-configuration-persistence-and-schema.md) — `pocketpaw.mcp.config` defines the `MCPServerConfig` dataclass and the load/save helpers that persist MCP server definitions to `~/.pocketpaw/mcp_servers.json`. It is the single source of truth for which MCP servers are configured and how to connect to them.
- [MCP Server Presets — One-Click Integration Catalog](./mcp-server-presets-one-click-integration-catalog.md) — `presets.py` maintains a curated catalog of pre-configured MCP server templates (`MCPPreset`) that users can install from the PocketPaw dashboard by pasting a single API key, eliminating the need to understand MCP server configuration details for common integrations.
- [MCPManager — Server Lifecycle, Tool Discovery, and Tool Execution](./mcpmanager-server-lifecycle-tool-discovery-and-tool-execution.md) — `MCPManager` is the central singleton that owns the full lifecycle of connected MCP servers: it starts and stops subprocess or HTTP connections, discovers tools from each server, and dispatches tool call requests. It supports stdio, SSE, and streamable-HTTP transports as well as OAuth-authenticated remote servers.

### `MCP integration` — 7 article(s)

- [GWS CLI System Prompt Injection Tests](./gws-cli-system-prompt-injection-tests.md) — This test suite verifies that the AgentContextBuilder conditionally injects Google Workspace CLI guidance into the system prompt only when the google-workspace MCP server is active and enabled. It also confirms that the gws.md guidance file exists on disk with the required content.
- [GWS Skills Auto-Install and GitHub Skill Installer Tests](./gws-skills-auto-install-and-github-skill-installer-tests.md) — This test suite covers `install_skills_from_github`, a shared async helper that clones a GitHub repository and copies SKILL.md-bearing subdirectories into the local skills install directory, with support for prefix filtering and single-skill targeting. It also tests that the GWS preset installation triggers a non-blocking background task that auto-installs Google Workspace skills.
- [MCP + Claude Agent SDK Integration: Server Filtering, Transport Mapping, and Policy Enforcement](./mcp-claude-agent-sdk-integration-server-filtering-transport-mapping-and-policy-e.md) — The MCP-Claude SDK integration tests (Sprint 17) validate that `ClaudeAgentSDK._get_mcp_servers()` correctly translates PocketPaw's internal `MCPServerConfig` objects into the format expected by the Claude Agent SDK, applying enable/disable filtering, transport type mapping, and security policy checks before handing the server list to the SDK.
- [MCP Manager: Server Lifecycle, Tool Discovery, Config Persistence, and HTTP Transport Auto-Detection](./mcp-manager-server-lifecycle-tool-discovery-config-persistence-and-http-transpor.md) — The MCP Manager test suite (Sprint 16) covers the full lifecycle of MCP server connections — config serialization, parallel server startup, tool discovery, tool invocation, graceful shutdown, and the Streamable-HTTP-to-SSE fallback protocol negotiation. All MCP SDK imports are mocked as the `mcp` package is optional.
- [MCP OAuth Flow: Token Storage, Callback Coordination, Preset Flags, and Dashboard Integration](./mcp-oauth-flow-token-storage-callback-coordination-preset-flags-and-dashboard-in.md) — The MCP OAuth test suite validates PocketPaw's support for OAuth-authenticated MCP servers, covering secure token and client-info persistence, WebSocket-based callback coordination, preset flag propagation, and the unauthenticated OAuth callback dashboard endpoint. Tests also verify that token files are created with restricted permissions and that corrupted token files degrade gracefully.
- [MCP Server Presets: Catalog Registry, Config Conversion, and Dashboard Installation](./mcp-server-presets-catalog-registry-config-conversion-and-dashboard-installation.md) — The MCP presets test suite validates PocketPaw's built-in catalog of popular MCP servers — covering unique ID enforcement, required field validation, category filtering, config generation from presets, and the dashboard API endpoints for browsing and installing presets. The Notion preset's hosted-OAuth path and transform functions receive dedicated coverage.
- [Skills API Tests — SkillLoader Search, REST Endpoints, Install, Remove, and MCP Presets](./skills-api-tests-skillloader-search-rest-endpoints-install-remove-and-mcp-preset.md) — This test file covers `SkillLoader.search()` and the full skills REST API: listing installed skills, searching the skills.sh library, installing skills via git clone, removing skills, force-reloading, and MCP preset configuration with `needs_args` metadata. Tests validate filtering, case-insensitive matching, subprocess handling, and preset response shapes.

### `Memory System` — 8 article(s)

- [Chat Title Generator — Haiku-Powered Session Naming](./chat-title-generator-haiku-powered-session-naming.md) — `titler.py` generates concise (6 words max, Title Case) chat titles from the first user message using a Claude Haiku-class model. Title generation is entirely best-effort: any failure is silently logged and returns `None` rather than disrupting the chat flow.
- [CLI Memory Command: Inspecting and Searching Long-Term Agent Memories](./cli-memory-command-inspecting-and-searching-long-term-agent-memories.md) — The `memory` CLI command provides operators with visibility into PocketPaw's persistent memory system — showing statistics about stored memories, daily notes, and sessions, or performing keyword searches across the memory store. It bridges the gap between the agent's internal memory manager and human-readable inspection.
- [CLI Sessions Command: Listing, Searching, and Deleting Chat Sessions](./cli-sessions-command-listing-searching-and-deleting-chat-sessions.md) — The `sessions` command provides operators with management access to PocketPaw's persisted chat sessions — listing recent sessions with metadata, searching session content by keyword, and deleting sessions by key. It wraps the async memory manager's session interface with a synchronous CLI dispatcher.
- [FileMemoryStore — Markdown, SQLite, and Vector Memory Backend](./filememorystore-markdown-sqlite-and-vector-memory-backend.md) — `FileMemoryStore` is PocketPaw's default memory backend, storing long-term and daily memories as human-readable Markdown files with optional semantic search via SQLite vector columns or ChromaDB and an optional knowledge graph for entity-relationship indexing.
- [Mem0MemoryStore — Semantic Memory with LLM-Powered Fact Extraction](./mem0memorystore-semantic-memory-with-llm-powered-fact-extraction.md) — `Mem0MemoryStore` wraps the `mem0ai` library to provide vector-based semantic search, LLM-powered fact extraction and consolidation, and memory evolution (updating existing memories instead of duplicating them), implementing `MemoryStoreProtocol` as a drop-in replacement for `FileMemoryStore`.
- [Memory Store Protocol — Types and Backend Interface](./memory-store-protocol-types-and-backend-interface.md) — `protocol.py` defines the foundational types for PocketPaw's memory system: the `MemoryType` enum, the `MemoryEntry` dataclass, and the `MemoryStoreProtocol` structural Protocol that any storage backend must implement, keeping `FileMemoryStore` and `Mem0MemoryStore` interchangeable.
- [Memory System Package — Optional Backends and Public API](./memory-system-package-optional-backends-and-public-api.md) — The `pocketpaw.memory` package exposes PocketPaw's memory system, providing two storage backends unified behind a common protocol: `FileMemoryStore` (always available) and `Mem0MemoryStore` (optional, requires `mem0ai`). It supports session history, long-term facts, and daily notes.
- [MemoryManager — High-Level Memory Facade and Singleton](./memorymanager-high-level-memory-facade-and-singleton.md) — `MemoryManager` is the application-facing facade over any `MemoryStoreProtocol` backend, providing agent-friendly methods like `get_context_for_agent`, `auto_learn`, and `get_graph_snapshot`. It is accessed as a singleton via `get_memory_manager()`.

### `Microsoft Teams` — 1 article(s)

- [Microsoft Teams Channel Adapter Tests: Messaging, Tenant Filtering, and Error Recovery](./microsoft-teams-channel-adapter-tests-messaging-tenant-filtering-and-error-recov.md) — Tests for PocketPaw's Microsoft Teams channel adapter, covering initialisation, message processing, stream buffering, tenant-based filtering, bus integration, lifecycle management, and error recovery. The `botbuilder-core` dependency is mocked since it is optional.

### `MongoDB` — 26 article(s)

- [Agent Document — Configuration, Soul Integration, and Scope Assignment](./agent-document-configuration-soul-integration-and-scope-assignment.md) — The Beanie ODM document representing an agent's persistent configuration in MongoDB — covering backend selection, system prompt, tool allowlist, trust level, soul personality settings, and hierarchical scope assignments. This document captures agent identity and configuration, not execution state.
- [Cloud DB Module - Backward-Compatible Database Shim](./cloud-db-module-backward-compatible-database-shim.md) — `ee/cloud/db.py` is a one-line backward-compatibility shim that re-exports the database lifecycle functions from the canonical `ee.cloud.shared.db` module. It exists solely to preserve older import paths after the database logic was consolidated into the shared package.
- [Cloud Document Models Registry — Beanie Initialization Manifest](./cloud-document-models-registry-beanie-initialization-manifest.md) — The central re-export module for all Beanie ODM documents in the enterprise cloud layer, providing a stable import surface and a lazy-loading `ALL_DOCUMENTS` list consumed by Beanie's init sequence. Special handling defers `FileUpload` and `FileFolder` imports to break circular import chains with the uploads subsystem.
- [Cloud Memory Backend Bootstrap — MongoDB Default Registration](./cloud-memory-backend-bootstrap-mongodb-default-registration.md) — Bootstraps the MongoDB memory backend for cloud deployments by directly priming the `pocketpaw.memory.manager` singleton before Beanie is initialized, bypassing the file-based config that would otherwise retain a stale `memory_backend: "file"` setting. Respects the `POCKETPAW_MEMORY_BACKEND` environment variable as an explicit user override.
- [Cloud Memory Package — MongoMemoryStore Public API](./cloud-memory-package-mongomemorystore-public-api.md) — The `ee.cloud.memory` package's public interface, which re-exports `MongoMemoryStore` as the single entry point for MongoDB-backed agent memory in cloud deployments. The module is intentionally minimal, acting as a stable import boundary between the cloud layer and the rest of the application.
- [Cloud Memory Test Fixtures: mongomock-motor Beanie Isolation and MongoMemoryStore Setup](./cloud-memory-test-fixtures-mongomock-motor-beanie-isolation-and-mongomemorystore.md) — This conftest provides two pytest fixtures for the cloud memory test suite: `beanie_memory_db` initializes an isolated in-memory MongoDB via `mongomock-motor` with a compatibility shim for Beanie's `list_collection_names` signature mismatch, and `store` returns a fresh `MongoMemoryStore` bound to that database. Each test gets its own uniquely-named in-memory database to prevent state leakage.
- [Comment Document — Threaded Comments on Pockets and Widgets](./comment-document-threaded-comments-on-pockets-and-widgets.md) — The Beanie ODM document for threaded comments attached to pockets, widgets, or agents. Supports reply threading via a `thread` parent ID, user mentions, and resolution tracking for review-style workflows.
- [Enterprise Edition Test Fixtures: License, MongoDB, FastAPI App, S3 Mock, and User/Workspace Factories](./enterprise-edition-test-fixtures-license-mongodb-fastapi-app-s3-mock-and-userwor.md) — This conftest provides the full test infrastructure for PocketPaw's enterprise edition integration tests: HMAC-signed license key injection, isolated in-memory MongoDB via mongomock-motor, a mounted FastAPI app with mocked agent pool, an async HTTP client, callable factories for users and workspaces, and a session-scoped S3 mock via moto.
- [FileObj Document — File Metadata with Object Storage Backend](./fileobj-document-file-metadata-with-object-storage-backend.md) — A Beanie ODM document that stores file metadata in MongoDB while keeping actual file bytes in S3, GCS, or local storage. Each record captures the owner, storage provider, bucket path, MIME type, and public accessibility flag, acting as the registry layer for a multi-provider object storage abstraction.
- [Group Document — Multi-User Channels with AI Agent Participants](./group-document-multi-user-channels-with-ai-agent-participants.md) — The Beanie ODM document for chat groups and channels — workspace-scoped rooms that mix human members with AI agents. Models Slack-style channel semantics with a role/permission layer, agent respond modes, and pinned message support.
- [MemoryFactDoc — Beanie Document for Agent Long-Term and Daily Memory](./memoryfactdoc-beanie-document-for-agent-long-term-and-daily-memory.md) — The `MemoryFactDoc` Beanie ODM document stores agent long-term and daily memory facts in the `memory_facts` MongoDB collection. It is distinct from the `messages` collection and carries workspace and user ownership fields to support multi-tenant scoping.
- [MongoDB Metadata Store for EE Workspace-Scoped Uploads](./mongodb-metadata-store-for-ee-workspace-scoped-uploads.md) — MongoFileStore is the persistence layer for EE upload metadata, extending the OSS file store interface with workspace isolation, folder path tracking, and bulk operations for path rewriting and soft deletion. It bridges the adapter-agnostic `FileRecord` abstraction with the EE `FileUpload` Beanie document.
- [MongoDBAdapter: Async Document Database Connector via Motor with URI and Field-Based Auth](./mongodbadapter-async-document-database-connector-via-motor-with-uri-and-field-ba.md) — `MongoDBAdapter` implements `ConnectorProtocol` using `motor` (async pymongo) to provide full CRUD access to MongoDB collections. It supports both a connection URI and individual host/port/user/password fields, checks for the optional `motor` dependency at connect time, and exposes collection management, document query/insert/update/delete, and aggregation pipeline actions.
- [MongoFileStore Unit Tests: Scoped Save/Get, Cross-Workspace Isolation, and Soft Delete](./mongofilestore-unit-tests-scoped-saveget-cross-workspace-isolation-and-soft-dele.md) — Unit tests for `MongoFileStore`'s workspace-scoped CRUD operations, validating that files are stored and retrieved correctly within a workspace, are invisible across workspace boundaries, disappear after soft deletion, and return `None` (not raise) for missing IDs.
- [MongoMemoryStore Deduplication Tests: Idempotent Saves Within the 5-Second Window](./mongomemorystore-deduplication-tests-idempotent-saves-within-the-5-second-window.md) — This module tests the deduplication logic in `MongoMemoryStore`, which prevents identical back-to-back writes from creating duplicate memory entries when an agent loop retries the same turn. The dedup key is `(session_key, role, content)` with a 5-second window, and the tests verify that only different content, session, or role values produce distinct database rows.
- [MongoMemoryStore Facts Path: LONG_TERM and DAILY Memory Roundtrip Tests](./mongomemorystore-facts-path-longterm-and-daily-memory-roundtrip-tests.md) — These tests exercise the complete facts path of MongoMemoryStore, covering CRUD, type isolation between LONG_TERM and DAILY entries, cross-type search, regex safety, and protocol conformance. They also verify that user_id is stored as a dedicated column and never duplicated in the metadata dictionary.
- [MongoMemoryStore — Full MemoryStoreProtocol Implementation on MongoDB](./mongomemorystore-full-memorystoreprotocol-implementation-on-mongodb.md) — The production MongoDB implementation of PocketPaw's `MemoryStoreProtocol`, bridging the OSS memory interface to the cloud's Beanie ODM schema. SESSION entries map to the `messages` collection; LONG_TERM and DAILY facts map to `memory_facts`. The store also manages session metadata lifecycle and normalizes session key formats between the message bus and the database.
- [MongoMemoryStore SESSION Path: Protocol Conformance and Adapter-Specific Read Tests](./mongomemorystore-session-path-protocol-conformance-and-adapter-specific-read-tes.md) — These tests cover the full SESSION memory path in MongoMemoryStore — CRUD, chronological message retrieval, session key isolation, combined session-with-messages reads with limit behavior, and cross-collection isolation between pocket and group messages.
- [Session and Message context_type Discriminator Validator Tests](./session-and-message-contexttype-discriminator-validator-tests.md) — These tests verify that Pydantic model validators on Message and Session enforce strict discriminator rules, ensuring group and pocket context types carry exactly the required fields and nothing more. They also confirm backward-compatible inference when context_type is omitted.
- [Smoke Test: MongoDB Memory Backend End-to-End](./smoke-test-mongodb-memory-backend-end-to-end.md) — This script exercises the full MongoDB memory path from `init_cloud_db` through `MemoryManager.add_to_session`, `get_session_history`, raw Mongo inspection, and long-term memory storage in `memory_facts`. It serves as the canonical integration check for the enterprise memory backend.
- [TimestampedDocument — Base Beanie Document with Automatic Timestamps](./timestampeddocument-base-beanie-document-with-automatic-timestamps.md) — A Beanie `Document` base class that automatically manages `createdAt` and `updatedAt` timestamps using Beanie's event hook system. All cloud ODM documents inherit from this class to get consistent, server-side timestamp management.
- [Unified Files Service Tests: Workspace Isolation, Dedup, Drive Warnings, and Limit Cap](./unified-files-service-tests-workspace-isolation-dedup-drive-warnings-and-limit-c.md) — This module tests `UnifiedFilesService`, which aggregates files from multiple sources (chat uploads, Drive, local) into a single normalized list, covering workspace scope isolation, soft-delete filtering, cross-source deduplication, source-specific warnings, and the per-request limit cap. It uses `mongomock-motor` to run Beanie document tests in CI without a live MongoDB instance.
- [Unread Service - Per-User Unread Counts and Mention Badges](./unread-service-per-user-unread-counts-and-mention-badges.md) — `UnreadService` tracks per-user, per-group unread message counts and mention badges by combining a `ReadState` watermark document with a live message count query. It provides the data for inbox badges and the visual unread indicator in the sidebar.
- [Upload and Folder Document Models for the EE Storage Layer](./upload-and-folder-document-models-for-the-ee-storage-layer.md) — This module defines the two Beanie document models underpinning the EE uploads subsystem: `FileUpload` for adapter-backed file metadata and `FileFolder` for the virtual folder tree. Both documents are workspace-scoped with compound indexes designed for the access patterns of the uploads and files listing endpoints.
- [Upload Test Fixtures: Isolated Filesystem Root, In-Memory MongoDB, and MongoFileStore](./upload-test-fixtures-isolated-filesystem-root-in-memory-mongodb-and-mongofilesto.md) — Shared pytest fixtures for the uploads test suite providing a per-test isolated filesystem root, an in-memory MongoDB instance via `mongomock-motor`, and a ready-to-use `MongoFileStore`. The `beanie_upload_db` fixture includes a workaround for a `mongomock-motor` incompatibility with Beanie's `list_collection_names` call.
- [Workspace-Scoped Folder Store for the My Files Mount](./workspace-scoped-folder-store-for-the-my-files-mount.md) — FolderStore is a MongoDB-backed CRUD layer for the virtual folder tree in the uploads provider's My Files feature. It handles path normalization, recursive path creation, bulk path rewrites for renames, and soft delete — all constrained to a single workspace.

### `Monitoring` — 2 article(s)

- [Agent Status API — SSE Streaming and Debounced Snapshot Polling](./agent-status-api-sse-streaming-and-debounced-snapshot-polling.md) — The agent status router exposes real-time agent state — global status plus per-session breakdowns — through both a polling REST endpoint and a Server-Sent Events stream. It uses fingerprint-based change detection and a 1-second debounce to avoid flooding SSE clients with identical snapshots during high-frequency tool execution.
- [Health Router — System Health, Security Audits, and Self-Audit Reports](./health-router-system-health-security-audits-and-self-audit-reports.md) — The health router aggregates PocketPaw's observability surface into a single API domain: version information, health engine summaries, error log management, on-demand health checks, security audit runs, and a scheduled self-audit report system. It was extracted from the monolithic `dashboard.py` to give each concern a clean, testable home.

### `Mount Resolution` — 1 article(s)

- [ProviderRegistry Tests: Registration, Duplicate Detection, and Mount Resolution](./providerregistry-tests-registration-duplicate-detection-and-mount-resolution.md) — This module tests `ProviderRegistry`, the central index that maps provider IDs to `FolderProvider` implementations and resolves virtual mount paths to their owning providers. Key behaviors tested include duplicate registration rejection, longest-prefix mount resolution, and template variable substitution during path matching.

### `Multi-Agent Orchestration` — 3 article(s)

- [MCTaskExecutor — Secure, Streaming AI Agent Task Execution](./mctaskexecutor-secure-streaming-ai-agent-task-execution.md) — `MCTaskExecutor` runs AI agents against Mission Control tasks with real-time WebSocket streaming of output, auto-saves deliverable documents, retries failed tasks up to a configurable limit, enforces per-task timeouts, and limits concurrent executions with UUID validation and error sanitisation as security controls.
- [Mission Control API — FastAPI Router for Agent Orchestration Endpoints](./mission-control-api-fastapi-router-for-agent-orchestration-endpoints.md) — `api.py` defines the FastAPI router for Mission Control, exposing REST endpoints for agents, tasks, messages, documents, activity, notifications, and Deep Work projects, with Pydantic validation on all write requests and business logic delegated to `MissionControlManager`.
- [Mission Control — Multi-Agent Orchestration Package](./mission-control-multi-agent-orchestration-package.md) — The `pocketpaw.mission_control` package provides a shared workspace for multiple AI agents to collaborate on tasks, including agent profiles with status tracking, a task lifecycle engine, threaded messaging with @mentions, document storage, activity feeds, and real-time WebSocket streaming of task execution events.

### `OAuth` — 2 article(s)

- [MCP Router Tests: Server Management and OAuth Callback Handling](./mcp-router-tests-server-management-and-oauth-callback-handling.md) — This test file covers PocketPaw's `/api/v1/mcp` router, which manages Model Context Protocol (MCP) server connections — add, remove, toggle, and status — and handles the OAuth callback that completes browser-based MCP server authentication flows.
- [OAuth Integration: Token Storage, Provider Configuration, and Token Validity](./oauth-integration-token-storage-provider-configuration-and-token-validity.md) — Tests for PocketPaw's OAuth subsystem, covering secure token persistence in TokenStore, provider URL generation via OAuthManager, and token validity checks based on expiry timestamps. Verifies file permissions, non-existent token handling, and graceful degradation for unknown providers.

### `OAuth2` — 7 article(s)

- [OAuth Integration Router — Google and Spotify Service Authorization](./oauth-integration-router-google-and-spotify-service-authorization.md) — The OAuth integrations router handles the user-facing authorize and callback flow for connecting PocketPaw to third-party services like Gmail, Google Calendar, Google Drive, Google Docs, and Spotify. It was extracted from the dashboard router so the same flows work in both dashboard and headless serve modes.
- [OAuth2 Authorization Server: PKCE Flow Implementation](./oauth2-authorization-server-pkce-flow-implementation.md) — `AuthorizationServer` implements the full OAuth2 authorization code + PKCE flow: issuing authorization codes, exchanging them for tokens via S256 challenge verification, refreshing tokens, and revoking tokens. Token lifetimes are 1-hour access / 30-day refresh / 10-minute authorization code.
- [OAuth2 Data Models: Client, Authorization Code, and Token](./oauth2-data-models-client-authorization-code-and-token.md) — Defines the three core data structures for PocketPaw's OAuth2 implementation: `OAuthClient` (registered application with redirect URI validation), `AuthorizationCode` (short-lived PKCE exchange token), and `OAuthToken` (access + refresh token pair). The redirect URI matching logic implements RFC 8252 Section 7.3 for native app loopback flexibility.
- [OAuth2 PKCE Authorization Server Tests: Full Flow, Token Exchange, and Consent](./oauth2-pkce-authorization-server-tests-full-flow-token-exchange-and-consent.md) — Tests PocketPaw's OAuth2 authorization server with PKCE (Proof Key for Code Exchange), covering authorization code issuance, token exchange, token refresh, revocation, and the consent UI endpoints. The tests validate both the server logic class directly and the REST endpoints, ensuring the security guarantees of PKCE are enforced end-to-end.
- [OAuth2 Server — Authorize, Token Exchange, and Revoke](./oauth2-server-authorize-token-exchange-and-revoke.md) — The OAuth2 router implements the server side of the OAuth2 authorization code flow, allowing third-party MCP clients and tools to obtain scoped access tokens to PocketPaw's API. It handles the consent UI, code-to-token exchange, refresh token rotation, and revocation.
- [OAuth2 Storage: In-Memory Auth Codes and File-Persisted Tokens](./oauth2-storage-in-memory-auth-codes-and-file-persisted-tokens.md) — `OAuthStorage` uses a two-tier storage strategy: authorization codes live in memory only (ephemeral, 10-minute TTL), while access and refresh tokens are persisted to `~/.pocketpaw/oauth_tokens.json` so authenticated desktop sessions survive server restarts. A pre-registered `pocketpaw-desktop` client with Tauri-compatible redirect URIs is always available.
- [OAuth2/PKCE Authorization Server Package](./oauth2pkce-authorization-server-package.md) — The `oauth2` package implements a self-contained OAuth2 authorization server with PKCE support, designed primarily for the PocketPaw Tauri desktop application to authenticate against a locally running PocketPaw instance without exposing a client secret. The package is organized into models, server logic, and storage layers.

### `OCR` — 2 article(s)

- [OCR Tool: Schema Validation, File Jail, and Provider Fallback Tests](./ocr-tool-schema-validation-file-jail-and-provider-fallback-tests.md) — Tests for OCRTool, a built-in agent tool that extracts text from images using OpenAI Vision or Tesseract as fallback. Validates the tool schema, enforces a file-system jail to prevent path traversal, and covers error paths for unsupported formats, oversized files, missing providers, and API failures.
- [OCRTool: Multi-Engine Text Extraction from Images and PDFs](./ocrtool-multi-engine-text-extraction-from-images-and-pdfs.md) — The `OCRTool` extracts text from images and PDFs using three interchangeable OCR backends — OpenAI Vision (GPT-4o), Sarvam Vision (specialized for 23 Indian languages), and Tesseract (offline fallback). All path operations go through `is_safe_path` to enforce the file jail, and outputs are saved to a dedicated OCR output directory so extracted text is recoverable after the tool call completes.

### `OSS-EE interop` — 1 article(s)

- [OSS-to-EE Resolver Fallback Tests: Bridging JSONL and Mongo Upload Stores](./oss-to-ee-resolver-fallback-tests-bridging-jsonl-and-mongo-upload-stores.md) — Tests for the `resolve_media_paths_any` OSS→EE fallback, which handles the hybrid scenario where uploads are stored in the EE Mongo store but chat rendering goes through the OSS JSONL lookup path. Without the fallback, EE-uploaded files would appear as broken links in OSS-rendered chat messages.

### `OSS/EE Boundary` — 1 article(s)

- [Memory Backend Selection Tests: EE MongoDB Default Flip and OSS Import Isolation](./memory-backend-selection-tests-ee-mongodb-default-flip-and-oss-import-isolation.md) — This module tests that the EE edition flips the default memory backend from `file` to `mongodb` at startup without overriding explicit user choices, verifies that `get_memory_manager()` returns a `MongoMemoryStore`-backed manager after the flip, and enforces that OSS package modules (`src/pocketpaw/memory/`) contain no top-level imports from `ee.*` to preserve clean open-source/enterprise boundary separation.

### `Observability` — 2 article(s)

- [CLI Logs Command: Audit Log Viewer and Tail Mode](./cli-logs-command-audit-log-viewer-and-tail-mode.md) — The `logs` command reads PocketPaw's JSONL audit log at `~/.pocketpaw/audit.jsonl`, rendering recent entries in a color-coded table or streaming new entries in real time via a tail loop. It handles malformed JSON lines gracefully and renders multi-field entries by normalizing common key aliases.
- [CLI Status Command: Live Agent Status with Watch Mode](./cli-status-command-live-agent-status-with-watch-mode.md) — The `status` command polls the PocketPaw agent's REST status endpoint to display global state (active sessions, uptime, concurrent session capacity) and per-session detail (channel, tool, duration, error). It supports a `--watch` mode that redraws the terminal at a configurable interval, providing a live dashboard view.

### `Ollama` — 2 article(s)

- [Ollama Integration: Provider Detection, Env Var Construction, and CLI Health Check](./ollama-integration-provider-detection-env-var-construction-and-cli-health-check.md) — Tests for PocketPaw's Ollama provider support, verifying that LLMClient correctly detects Ollama from settings, constructs the right subprocess environment variables, and skips smart model routing for local models. Also covers the `--check-ollama` CLI command that validates server reachability and model availability.
- [OllamaAdapter: Local LLM Integration via OpenAI-Compatible API](./ollamaadapter-local-llm-integration-via-openai-compatible-api.md) — `OllamaAdapter` connects PocketPaw to locally running Ollama instances, using `"ollama"` as a placeholder API key to satisfy client SDK validation while pointing both OpenAI and Anthropic client builders at Ollama's OpenAI-compatible HTTP endpoint. Error messages distinguish between model-not-found and service-not-running failures to give users actionable guidance.

### `OpenAI` — 1 article(s)

- [OpenAI Agents SDK Backend: Tool Wiring, Session Management, and Graceful Degradation](./openai-agents-sdk-backend-tool-wiring-session-management-and-graceful-degradatio.md) — Tests for the OpenAI Agents SDK backend adapter, covering custom tool registration, per-backend provider selection, SDK-absent graceful degradation, native SQLite session management, and lazy database path resolution. All tests run without a real OpenAI Agents SDK installation via comprehensive mocking.

### `OpenAI compatibility` — 2 article(s)

- [GeminiAdapter: Google AI via OpenAI-Compatible Endpoint](./geminiadapter-google-ai-via-openai-compatible-endpoint.md) — `GeminiAdapter` routes Gemini API calls through Google's OpenAI-compatible endpoint, allowing PocketPaw to use the same `AsyncOpenAI` and `AsyncAnthropic` client patterns it uses for other providers. Both client builders point to `GEMINI_BASE_URL` and use `"not-needed"` as a placeholder API key fallback to satisfy SDK validation without passing a real credential.
- [OpenAICompatibleAdapter: Generic OpenAI-Compatible Endpoint Support](./openaicompatibleadapter-generic-openai-compatible-endpoint-support.md) — `OpenAICompatibleAdapter` provides a catch-all provider adapter for any service that exposes an OpenAI-compatible REST API — vLLM, LocalAI, Together AI, Anyscale, and similar endpoints. It reads `openai_compatible_base_url` and `openai_compatible_api_key` from settings and constructs both `AsyncOpenAI` and `AsyncAnthropic` clients pointing at that endpoint.

### `OpenAI compatible` — 1 article(s)

- [OpenAI-Compatible Provider: Detection, Env Vars, Client Creation, and Error Formatting](./openai-compatible-provider-detection-env-vars-client-creation-and-error-formatti.md) — Tests for PocketPaw's OpenAI-compatible endpoint support in LLMClient, enabling any OpenAI-compatible API (LiteLLM, Groq, local proxies) as the LLM backend. Covers provider detection, model resolution, environment variable construction for subprocess mode, Anthropic client creation with custom base URL, and structured error formatting for connection and authentication failures.

### `OpenRouter` — 1 article(s)

- [OpenRouterAdapter: Multi-Model Routing with Anthropic-Compat Authentication Quirks](./openrouteradapter-multi-model-routing-with-anthropic-compat-authentication-quirk.md) — `OpenRouterAdapter` integrates OpenRouter's model routing service, which provides access to 100+ models via a single API key. Its most distinctive behavior is an intentional `ANTHROPIC_API_KEY=""` (empty string) workaround: OpenRouter authenticates via `ANTHROPIC_AUTH_TOKEN`, and if `ANTHROPIC_API_KEY` is set to a real value, the Anthropic SDK sends it as `Bearer` and OpenRouter rejects the request.

### `Orchestration` — 2 article(s)

- [Deep Work Module: Public API for AI Project Orchestration](./deep-work-module-public-api-for-ai-project-orchestration.md) — The `pocketpaw.deep_work` package exposes a high-level API for AI-driven project orchestration — from natural language goal parsing through multi-agent execution, pause/resume lifecycle, and crash recovery. The singleton `DeepWorkSession` is lazily initialized and wired to the MessageBus for task completion events.
- [ProactiveDaemon: Central Orchestrator for Scheduled Agent Behavior](./proactivedaemon-central-orchestrator-for-scheduled-agent-behavior.md) — ProactiveDaemon is the top-level coordinator that wires together IntentionStore, TriggerEngine, IntentionExecutor, and ContextHub into a single lifecycle-managed service. It starts trigger scheduling on `start()`, fires intentions via `_on_trigger()` when conditions are met, and tears down cleanly on `stop()`.

### `PII protection` — 1 article(s)

- [PII Scanner Tests: Detection, Actions, False Positives, and Singleton Pattern](./pii-scanner-tests-detection-actions-false-positives-and-singleton-pattern.md) — PocketPaw's PII scanner (`pocketpaw.security.pii`) detects sensitive personal information in text before it reaches logs, LLM context, or external storage. These tests validate detection of SSNs, emails, phones, credit cards, IP addresses, and dates of birth, verify that safe text generates no false positives, and confirm the singleton pattern for the default scanner instance.

### `Package Architecture` — 2 article(s)

- [MCP Client Support Package Entry Point](./mcp-client-support-package-entry-point.md) — The `pocketpaw.mcp` package provides PocketPaw with the ability to connect to any Model Context Protocol (MCP) server and invoke its tools without requiring custom tool implementations for each integration. This module re-exports all public symbols from the config and manager sub-modules, giving consumers a single clean import surface.
- [Memory System Package — Optional Backends and Public API](./memory-system-package-optional-backends-and-public-api.md) — The `pocketpaw.memory` package exposes PocketPaw's memory system, providing two storage backends unified behind a common protocol: `FileMemoryStore` (always available) and `Mem0MemoryStore` (optional, requires `mem0ai`). It supports session history, long-term facts, and daily notes.

### `Package Management` — 1 article(s)

- [CLI Update Command: Self-Update via uv with PyPI Version Check](./cli-update-command-self-update-via-uv-with-pypi-version-check.md) — The `update` command checks PyPI for a newer version of PocketPaw and, if one is available, installs it using the `uv` package manager targeting the current Python interpreter. It handles the Windows file-locking edge case where the running executable cannot be replaced while the process is active.

### `Performance` — 1 article(s)

- [Cloud Files Tree Cache Tests: TTL, Invalidation, and Multi-Tenant Key Isolation](./cloud-files-tree-cache-tests-ttl-invalidation-and-multi-tenant-key-isolation.md) — This module tests `CachedTreeBuilder`, the in-process 30-second cache layer over `build_tree` that prevents repeated provider fan-outs on every tree request. Tests validate that cache hits suppress redundant `list_mounts` calls, that explicit invalidation forces a rebuild, that TTL expiry triggers a new fan-out, and that clearing the cache invalidates all per-user keys.

### `Permissions` — 1 article(s)

- [Cloud Files Permissions: ABAC Filtering and Capability Derivation Tests](./cloud-files-permissions-abac-filtering-and-capability-derivation-tests.md) — This module validates the layered permissions model in PocketPaw's cloud files system, covering ABAC tag filtering, RBAC-and-mount-writable capability intersection, and the `PermissionsEvaluator` composite. These tests ensure that no combination of user attributes, mount configuration, or entry tags can produce a capability set broader than what all three permission layers jointly allow.

### `Playwright` — 2 article(s)

- [BrowserDriver Tests: Playwright Wrapper for Navigation, Interaction, and Lifecycle Management](./browserdriver-tests-playwright-wrapper-for-navigation-interaction-and-lifecycle-.md) — This test suite validates `BrowserDriver`, PocketPaw's thin wrapper around the Playwright async API. Tests cover initialization modes (headless/headful), async context manager lifecycle, browser launch and viewport configuration, all navigation and interaction operations (navigate, click, type, scroll), snapshot generation, screenshot capture, and defensive checks for using the driver before it's launched.
- [E2E Test Configuration: Playwright Browser Setup and Dashboard Server Lifecycle](./e2e-test-configuration-playwright-browser-setup-and-dashboard-server-lifecycle.md) — This conftest manages the full lifecycle of PocketPaw's end-to-end test environment: it checks for Playwright browser installations and skips the entire e2e suite if they are missing, finds a free OS port, starts the dashboard server in a subprocess, waits for it to become ready, and exposes the base URL to all e2e tests through a session-scoped fixture.

### `Plugin Architecture` — 1 article(s)

- [FolderProvider Contract Test Base Class: Reusable Compliance Suite](./folderprovider-contract-test-base-class-reusable-compliance-suite.md) — This module defines `ProviderContract`, an abstract base class that encodes the behavioral contract every `FolderProvider` implementation must satisfy, including mount listing, entry listing with pagination, namespaced IDs, and graceful `ProviderUnsupported` errors for write operations. Concrete provider tests subclass it to inherit full compliance coverage automatically.

### `Project Structure` — 1 article(s)

- [Cloud Memory Test Package Marker](./cloud-memory-test-package-marker.md) — This is an empty `__init__.py` that marks `tests/cloud/memory/` as a Python package, enabling pytest to discover tests within the directory and allowing relative imports from sibling test modules. No functional code is present; its role is purely structural.

### `Protocol Design` — 1 article(s)

- [Memory Store Protocol — Types and Backend Interface](./memory-store-protocol-types-and-backend-interface.md) — `protocol.py` defines the foundational types for PocketPaw's memory system: the `MemoryType` enum, the `MemoryEntry` dataclass, and the `MemoryStoreProtocol` structural Protocol that any storage backend must implement, keeping `FileMemoryStore` and `Mem0MemoryStore` interchangeable.

### `Provider System` — 3 article(s)

- [Cloud Files Browse Mount Tests: ABAC Filtering and Provider Resolution](./cloud-files-browse-mount-tests-abac-filtering-and-provider-resolution.md) — This test module validates the `browse_mount` function in PocketPaw's cloud files subsystem, covering successful entry listing, ABAC tag-based filtering, and graceful errors when a mount references an unregistered provider. Together these tests protect the file-browsing surface against misconfiguration and unauthorized data exposure.
- [FolderProvider Contract Test Base Class: Reusable Compliance Suite](./folderprovider-contract-test-base-class-reusable-compliance-suite.md) — This module defines `ProviderContract`, an abstract base class that encodes the behavioral contract every `FolderProvider` implementation must satisfy, including mount listing, entry listing with pagination, namespaced IDs, and graceful `ProviderUnsupported` errors for write operations. Concrete provider tests subclass it to inherit full compliance coverage automatically.
- [ProviderRegistry Tests: Registration, Duplicate Detection, and Mount Resolution](./providerregistry-tests-registration-duplicate-detection-and-mount-resolution.md) — This module tests `ProviderRegistry`, the central index that maps provider IDs to `FolderProvider` implementations and resolves virtual mount paths to their owning providers. Key behaviors tested include duplicate registration rejection, longest-prefix mount resolution, and template variable substitution during path matching.

### `Pydantic` — 5 article(s)

- [Fabric Pydantic Models — Ontology Layer Data Structures](./fabric-pydantic-models-ontology-layer-data-structures.md) — This module defines the Pydantic models that represent the Fabric ontology layer's core entities: property definitions, object types, object instances, directional links between objects, query parameters, and query results. The `_gen_id` function produces time-ordered, prefix-tagged identifiers that balance human readability with sortability.
- [Fleet Pydantic Models — Manifest and Install Report Types](./fleet-pydantic-models-manifest-and-install-report-types.md) — This module defines the Pydantic models for the Fleet subsystem: the `FleetTemplate` manifest (what to install), `FleetConnector` (a single connector registration), `FleetInstallStep` (one step's outcome), and `FleetInstallReport` (the full install result with convenience methods). These models are thin data containers — no new runtime concepts, just structured representations of the install lifecycle.
- [Pockets Domain Pydantic Schemas: Request and Response Models](./pockets-domain-pydantic-schemas-request-and-response-models.md) — This file defines all Pydantic request and response models for the pockets domain, covering creation, update, widget management, reordering, share links, collaborators, and the full pocket response shape. A notable recent addition is support for passing agents, rippleSpec, and an initial widgets list on creation so the frontend can fully specify a pocket in a single request.
- [Sessions Domain Pydantic Schemas: Request and Response Models](./sessions-domain-pydantic-schemas-request-and-response-models.md) — This file defines the three Pydantic models for the sessions domain: CreateSessionRequest, UpdateSessionRequest, and SessionResponse. The schemas are intentionally lean, supporting optional linkage to pockets, groups, agents, and existing runtime sessions at creation time.
- [Workspace Pydantic Schemas — Requests and Responses](./workspace-pydantic-schemas-requests-and-responses.md) — This module defines the Pydantic request and response schemas for the workspace domain, enforcing input validation at the API boundary before any business logic runs. The slug validator is the key defensive piece: it rejects slugs that would break URL routing or clash with reserved path segments.

### `Pydantic models` — 1 article(s)

- [A2A Protocol Pydantic Models: Tasks, Messages, Parts, and Agent Cards](./a2a-protocol-pydantic-models-tasks-messages-parts-and-agent-cards.md) — This module defines all Pydantic models for the A2A protocol (v0.2.5+), including the task lifecycle state machine, typed content parts (text/file/data), message and artifact models, request parameter schemas, the Agent Card capabilities manifest, and JSON-RPC 2.0 envelope models with streaming event types.

### `Python` — 2 article(s)

- [Files Providers Test Sub-Package Marker](./files-providers-test-sub-package-marker.md) — An empty __init__.py that makes tests/cloud/files/providers/ a Python package, enabling dotted imports from parent test modules such as tests.cloud.files.test_provider_contract. Identical in purpose and content to the sibling files test package marker.
- [Files Test Package Marker](./files-test-package-marker.md) — This is an empty __init__.py that marks tests/cloud/files/ as a Python package so pytest can discover and import tests within it. The SHA-256 hash e3b0c44298fc... is the well-known hash of an empty file.

### `RBAC` — 5 article(s)

- [ACTIONS Registry: Canonical RBAC Action-to-Role Matrix](./actions-registry-canonical-rbac-action-to-role-matrix.md) — The `actions.py` module is the single source of truth for every guarded operation in PocketPaw, mapping action strings (in `"resource.action"` dotted form) to a minimum required role and a stable machine-readable denial code. Unknown actions fail loudly by design, preventing silent authorization bypasses when new routes are added without a corresponding ACTIONS entry.
- [Core RBAC Primitives: Workspace Roles, Pocket Access Levels, and Forbidden Exception](./core-rbac-primitives-workspace-roles-pocket-access-levels-and-forbidden-exceptio.md) — `rbac.py` establishes the foundational enums and exception for PocketPaw's role-based access control: a 3-tier `WorkspaceRole` hierarchy, a 4-tier `PocketAccess` hierarchy, and the `Forbidden` exception that carries machine-readable denial codes. All other guard modules depend on these primitives without circular imports.
- [Group Service - Channel and DM Business Logic](./group-service-channel-and-dm-business-logic.md) — The `GroupService` class encapsulates all group and channel CRUD, membership management, agent attachment, and DM creation logic for the chat domain. It coordinates between the Group ODM model, the realtime event bus, and the RBAC/audit guard layer.
- [KbProvider Tests: Workspace Scoping, RBAC, and Mount Template](./kbprovider-tests-workspace-scoping-rbac-and-mount-template.md) — This module tests the KbProvider file provider, which surfaces the knowledge base as a virtual file tree. Tests verify that entries are scoped to the correct workspace, that baseline RBAC rules grant read access to workspace members and manage access to admins, and that the mount path template correctly resolves the workspace ID.
- [UploadsProvider Tests: Field Mapping, Mounts, RBAC, and Folder Listing](./uploadsprovider-tests-field-mapping-mounts-rbac-and-folder-listing.md) — This module tests the UploadsProvider, which surfaces user file uploads as a virtual file tree with folder hierarchy support. Tests cover field mapping from the raw upload store format to FileEntry, mount resolution, baseline RBAC (owner vs. non-owner vs. admin), and the combined folder+file listing behavior.

### `REST` — 1 article(s)

- [OpenCode Backend: REST API Integration, Session Caching, and Tool Injection](./opencode-backend-rest-api-integration-session-caching-and-tool-injection.md) — Tests for the OpenCodeBackend, which connects PocketPaw to OpenCode's REST API for agentic coding tasks. Covers health checking, session creation and caching, the message endpoint contract, system prompt tool instruction injection, streaming responses, and graceful error handling for unreachable servers.

### `REST API` — 8 article(s)

- [Automations Router: REST API for Rule-Based Pocket Automations](./automations-router-rest-api-for-rule-based-pocket-automations.md) — The automations router exposes a FastAPI-based CRUD API for managing automation rules and controlling the background evaluator that fires them. It acts as the HTTP boundary between the frontend and the lower-level store, bridge, and evaluator subsystems, ensuring that every rule mutation is immediately synchronized to the daemon via the bridge.
- [Fleet Install REST Router](./fleet-install-rest-router.md) — Exposes two HTTP endpoints — listing bundled fleet templates and triggering a fleet install — as part of the enterprise edition's fleet management subsystem. Guards the install route with RBAC and emits correlated audit events into the shared org journal.
- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.
- [Instinct Decision Pipeline — HTTP Router](./instinct-decision-pipeline-http-router.md) — Provides the FastAPI HTTP surface for the Instinct decision pipeline, handling action proposals, approvals with optional human edits, rejections, and corrections listing. It enforces that soul bridge failures never block an approval and that edit diffs are captured and stored before the status transition.
- [Mission Control API — FastAPI Router for Agent Orchestration Endpoints](./mission-control-api-fastapi-router-for-agent-orchestration-endpoints.md) — `api.py` defines the FastAPI router for Mission Control, exposing REST endpoints for agents, tasks, messages, documents, activity, notifications, and Deep Work projects, with Pydantic validation on all write requests and business logic delegated to `MissionControlManager`.
- [Mission Control (Deep Work) API Tests: Project Lifecycle, State Transitions, and REST Endpoints](./mission-control-deep-work-api-tests-project-lifecycle-state-transitions-and-rest.md) — This test module, created in February 2026, covers PocketPaw's Mission Control subsystem — the project and task management layer that tracks long-horizon AI work. It validates the full project lifecycle through both the `MissionControlManager` directly and the FastAPI REST endpoints, including state transitions (draft, approved, paused, resumed) and proper 404 handling for missing resources.
- [Paw Print Router — HTTP Surface for Widget Management and Event Ingest](./paw-print-router-http-surface-for-widget-management-and-event-ingest.md) — Provides the FastAPI routes for the Paw Print widget layer: public CORS-gated spec serving, owner-authenticated widget CRUD, and a multi-layered event ingest endpoint that enforces domain validation, rate limiting, Guardian screening, and Fabric object mapping before persisting customer interactions.
- [Skills API Tests — SkillLoader Search, REST Endpoints, Install, Remove, and MCP Presets](./skills-api-tests-skillloader-search-rest-endpoints-install-remove-and-mcp-preset.md) — This test file covers `SkillLoader.search()` and the full skills REST API: listing installed skills, searching the skills.sh library, installing skills via git clone, removing skills, force-reloading, and MCP preset configuration with `needs_args` metadata. Tests validate filtering, case-insensitive matching, subprocess handling, and preset response shapes.

### `REST integration` — 1 article(s)

- [DirectRESTAdapter and YAML Engine: Declarative REST Connector Definitions via YAML Files](./directrestadapter-and-yaml-engine-declarative-rest-connector-definitions-via-yam.md) — `yaml_engine.py` enables third-party REST API connectors to be defined entirely in YAML without writing Python code. The `DirectRESTAdapter` reads a `ConnectorDef` parsed from a YAML file and executes actions as HTTP requests via `httpx`, templating parameters from the agent's action invocation into the request body, headers, and URL.

### `REST-API` — 1 article(s)

- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.

### `Real-time` — 3 article(s)

- [Agent Status API — SSE Streaming and Debounced Snapshot Polling](./agent-status-api-sse-streaming-and-debounced-snapshot-polling.md) — The agent status router exposes real-time agent state — global status plus per-session breakdowns — through both a polling REST endpoint and a Server-Sent Events stream. It uses fingerprint-based change detection and a 1-second debounce to avoid flooding SSE clients with identical snapshots during high-frequency tool execution.
- [Chat Router — HTTP Send, SSE Streaming, and Session Bridge for Agent Conversations](./chat-router-http-send-sse-streaming-and-session-bridge-for-agent-conversations.md) — The chat router enables external clients to drive PocketPaw agent sessions over plain HTTP, using Server-Sent Events for streaming responses. The `_APISessionBridge` class connects the internal message bus to an SSE stream, ensuring that chat sessions initiated via the HTTP API reuse the same AgentLoop pipeline as native channel adapters.
- [Events SSE Router — Real-Time System Event Streaming for External Clients](./events-sse-router-real-time-system-event-streaming-for-external-clients.md) — The events router provides a Server-Sent Events endpoint that external clients (such as the Tauri desktop app) can subscribe to for real-time system events without connecting to the WebSocket. It delivers the same event types as the WebSocket — tool_start, tool_result, thinking, error, health_update, inbox_update — via a keepalive-aware HTTP stream.

### `Resilience` — 1 article(s)

- [Cloud Files Tree Builder Tests: Ordering, Nesting, and Resilient Provider Failures](./cloud-files-tree-builder-tests-ordering-nesting-and-resilient-provider-failures.md) — This module tests `build_tree`, which assembles a hierarchical folder tree from all registered provider mounts. Key behaviors tested include mount ordering by the `order` field, recursive path-segment nesting, and graceful warning collection when individual providers fail rather than aborting the entire tree build.

### `S3` — 1 article(s)

- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.

### `SQL injection` — 1 article(s)

- [Audit FTS Security Tests: SQL Injection Prevention and LIKE Wildcard Escaping](./audit-fts-security-tests-sql-injection-prevention-and-like-wildcard-escaping.md) — Security regression tests proving that the `q` parameter on PocketPaw's `/runtime/audit` search endpoint is fully parameter-bound and cannot corrupt or drop the `audit_log` table, and that LIKE special characters (`%`, `_`) in the query are correctly escaped to prevent wildcard-widened result sets. Tests correspond to Cluster C PR4.

### `SQLite` — 3 article(s)

- [FabricStore — Async SQLite Backend for the Ontology Layer](./fabricstore-async-sqlite-backend-for-the-ontology-layer.md) — `FabricStore` is the legacy async SQLite persistence layer for Fabric's ontology data, managing object types, object instances, and directional links. It uses lazy schema initialization, parameterized queries throughout, and a manual cascade-delete pattern because SQLite foreign key enforcement is not enabled by default.
- [Instinct Store — Async SQLite Persistence for the Decision Pipeline](./instinct-store-async-sqlite-persistence-for-the-decision-pipeline.md) — Provides all async SQLite read and write operations for the Instinct decision pipeline: action lifecycle management, audit logging, correction recording, reasoning traces, and Fabric object snapshots. It is the single source of truth for the decision state within a PocketPaw enterprise installation.
- [Paw Print Store — Async SQLite Persistence for Widgets and Events](./paw-print-store-async-sqlite-persistence-for-widgets-and-events.md) — Provides all async SQLite read and write operations for the Paw Print widget layer, including widget CRUD with token rotation, append-only event recording, and a dual-level rate-limit enforcement method that checks both overall widget throughput and per-customer throughput within a rolling time window.

### `SSRF prevention` — 1 article(s)

- [SSRF URL Validation Tests: Blocking Internal and Malicious URLs in Settings](./ssrf-url-validation-tests-blocking-internal-and-malicious-urls-in-settings.md) — This test file, added during security sprint cluster E (issue #703), validates the `validate_external_url` function that guards all URL-typed settings fields against Server-Side Request Forgery (SSRF) attacks. It covers rejection of cloud metadata endpoints, RFC 1918 ranges, non-HTTP schemes, and malformed inputs, while confirming a developer-friendly escape hatch for local development.

### `Scheduling` — 2 article(s)

- [Intentions Router — Proactive Agent Scheduling CRUD and Manual Trigger](./intentions-router-proactive-agent-scheduling-crud-and-manual-trigger.md) — The intentions router manages PocketPaw's proactive behavior system — scheduled or event-triggered agent prompts that run autonomously without user initiation. It exposes full CRUD operations plus a toggle (enable/disable without deleting) and a run-now action that bypasses the scheduler for immediate execution.
- [TriggerEngine: Cron and Stale-Session Scheduling for Intentions](./triggerengine-cron-and-stale-session-scheduling-for-intentions.md) — TriggerEngine wraps APScheduler to provide two trigger types for proactive intentions: cron-based scheduling (standard 5-field cron expressions plus named presets) and stale-session polling (fires when a WebSocket session has been idle beyond a threshold). It manages job registration, removal, and update for all enabled intentions.

### `Schema Validation` — 1 article(s)

- [Cloud Files Schema Validation Tests: FileEntry, MountConfig, Permission, and Page](./cloud-files-schema-validation-tests-fileentry-mountconfig-permission-and-page.md) — This module validates the Pydantic schema constraints for PocketPaw's core cloud files data models, confirming invariants like namespaced entry IDs, enumerated scopes, valid capability sets, absolute mount templates, and permission intersection semantics. These tests prevent malformed data from entering the system by verifying that validation errors are raised at construction time.

### `Security` — 9 article(s)

- [API Keys Router — Long-Lived Key CRUD with One-Shot Plaintext Return](./api-keys-router-long-lived-key-crud-with-one-shot-plaintext-return.md) — The API keys router provides create, list, revoke, and rotate operations for long-lived programmatic access keys. A critical security property of the create endpoint is that the plaintext key is returned exactly once — subsequent reads only expose a short prefix — mirroring industry-standard patterns like GitHub personal access tokens.
- [Auth Router — Session Exchange, Cookie Login, QR Code, and Token Regeneration](./auth-router-session-exchange-cookie-login-qr-code-and-token-regeneration.md) — The auth router handles all first-party authentication flows: exchanging a master access token for a time-limited session token, setting and clearing HTTP-only session cookies, generating QR login codes for mobile pairing, and regenerating the master token when credentials need to be rotated. It uses constant-time comparison to close a timing oracle vulnerability in master-token validation.
- [Backends Router — Agent Backend Discovery and Auto-Install](./backends-router-agent-backend-discovery-and-auto-install.md) — The backends router exposes the registry of agent backends (Claude, Codex CLI, OpenCode, etc.) to the dashboard and API clients, including real-time availability checking that verifies both Python SDK imports and CLI binary presence. An install endpoint allows auto-installing missing backend SDKs via pip without requiring manual server restarts.
- [Dashboard Authentication: Middleware, Session Tokens, and QR Login](./dashboard-authentication-middleware-session-tokens-and-qr-login.md) — dashboard_auth.py centralizes all authentication logic for the PocketPaw dashboard — localhost detection, HMAC token verification, session token exchange, cookie-based login/logout, and QR code generation. It was extracted from dashboard.py to isolate auth concerns and make each piece independently testable.
- [Files Router — Scoped File Browser, Download, and Write with Path Traversal Guards](./files-router-scoped-file-browser-download-and-write-with-path-traversal-guards.md) — The files router gives the PocketPaw dashboard a full file browser over the agent's accessible filesystem: directory listing, file content serving, single-file download, directory-as-zip download, and file writing. Security hardening in April 2026 added scope guards and symlink filtering to prevent path traversal attacks identified in issues #884 and #886.
- [Health Router — System Health, Security Audits, and Self-Audit Reports](./health-router-system-health-security-audits-and-self-audit-reports.md) — The health router aggregates PocketPaw's observability surface into a single API domain: version information, health engine summaries, error log management, on-demand health checks, security audit runs, and a scheduled self-audit report system. It was extracted from the monolithic `dashboard.py` to give each concern a clean, testable home.
- [MCP OAuth Token Storage — Secure File-Based Persistence](./mcp-oauth-token-storage-secure-file-based-persistence.md) — `MCPTokenStorage` implements the MCP SDK's `TokenStorage` protocol, persisting OAuth access tokens and dynamic client registration data to per-server JSON files under `~/.pocketpaw/mcp_oauth/`. Files are written with `chmod 0600` to prevent token leakage to other OS users.
- [PocketPaw Settings: Centralized Configuration with Security Hardening](./pocketpaw-settings-centralized-configuration-with-security-hardening.md) — The `Settings` class is PocketPaw's single source of truth for all runtime configuration — agent backends, channel credentials, memory backends, security policies, soul protocol parameters, and user preferences. Built on `pydantic-settings`, it supports environment variable overrides, encrypted credential storage, SSRF-guarded URL fields, and automatic API key format validation.
- [Self-Audit Daemon: Periodic Security and Health Checks](./self-audit-daemon-periodic-security-and-health-checks.md) — The self-audit module runs a suite of automated checks against the PocketPaw runtime environment — stale sessions, config conflicts, disk usage, audit log size, and orphaned OAuth tokens — and saves timestamped JSON reports to `~/.pocketpaw/audit_reports/`. It is designed to run on a schedule and surface problems before they become incidents.

### `Simulation` — 1 article(s)

- [SimulationClock: Discrete Tick-Based Time for Multi-Agent Execution](./simulationclock-discrete-tick-based-time-for-multi-agent-execution.md) — SimulationClock provides a tick-based replacement for wall-clock time in multi-agent simulations, allowing all agents to act synchronously on the same tick before the world advances. TickSnapshot captures immutable world state at each tick for replay and analysis.

### `Skills and Tools` — 1 article(s)

- [CLI Skills Command: Listing and Searching Available Agent Skills](./cli-skills-command-listing-and-searching-available-agent-skills.md) — The `skills` CLI command discovers and displays all skills available to the PocketPaw agent, optionally filtering by search term. It delegates loading and search to the skill loader, rendering results with invocability status and file paths so operators can quickly find and verify which skills are active.

### `State Management` — 1 article(s)

- [Dashboard State: Shared Mutable Singletons and Per-Agent Loop Registry](./dashboard-state-shared-mutable-singletons-and-per-agent-loop-registry.md) — dashboard_state.py is the shared state module for PocketPaw's dashboard, extracted from dashboard.py to break circular imports. It holds the global singletons (WebSocketAdapter, AgentLoop, StatusTracker), the per-agent loop registry with staleness detection, and channel inspection helpers.

### `Storage` — 1 article(s)

- [Database Adapter: Unified Async Interface for SQLite-Backed Storage](./database-adapter-unified-async-interface-for-sqlite-backed-storage.md) — The `db_adapter` module provides an async SQLite wrapper that PocketPaw uses for all structured persistence — session records, memory entries, audit events, and credential metadata. It encapsulates connection lifecycle management, schema migrations, and parameterized queries behind a clean async interface to prevent SQL injection and connection leaks.

### `Storage Backend` — 2 article(s)

- [FileMemoryStore — Markdown, SQLite, and Vector Memory Backend](./filememorystore-markdown-sqlite-and-vector-memory-backend.md) — `FileMemoryStore` is PocketPaw's default memory backend, storing long-term and daily memories as human-readable Markdown files with optional semantic search via SQLite vector columns or ChromaDB and an optional knowledge graph for entity-relationship indexing.
- [Mem0MemoryStore — Semantic Memory with LLM-Powered Fact Extraction](./mem0memorystore-semantic-memory-with-llm-powered-fact-extraction.md) — `Mem0MemoryStore` wraps the `mem0ai` library to provide vector-based semantic search, LLM-powered fact extraction and consolidation, and memory evolution (updating existing memories instead of duplicating them), implementing `MemoryStoreProtocol` as a drop-in replacement for `FileMemoryStore`.

### `Task Management` — 1 article(s)

- [Mission Control — Multi-Agent Orchestration Package](./mission-control-multi-agent-orchestration-package.md) — The `pocketpaw.mission_control` package provides a shared workspace for multiple AI agents to collaborate on tasks, including agent profiles with status tracking, a task lifecycle engine, threaded messaging with @mentions, document storage, activity feeds, and real-time WebSocket streaming of task execution events.

### `Telegram` — 2 article(s)

- [Telegram Channel Adapter Tests: Typing Indicators, Smart Buffering, and Voice Routing](./telegram-channel-adapter-tests-typing-indicators-smart-buffering-and-voice-routi.md) — Tests for PocketPaw's Telegram adapter covering typing indicator lifecycle, smart stream buffering with rate limiting, markdown formatting on flush, voice media routing, and the complete streaming cycle from first chunk to final message. The `python-telegram-bot` library is mocked as an optional dependency.
- [Telegram Forum Topics Support: Chat ID Parsing and Session Key Isolation](./telegram-forum-topics-support-chat-id-parsing-and-session-key-isolation.md) — Tests for Telegram Group Topics (forum supergroups) support in the PocketPaw Telegram adapter, covering the `_parse_chat_id` utility that splits composite chat IDs into chat and topic components, and session key generation that ensures different forum topics produce different session keys.

### `Telegram integration` — 1 article(s)

- [Telegram Channel API Tests — Configuration Status and Pairing State](./telegram-channel-api-tests-configuration-status-and-pairing-state.md) — This test module covers the two read-only Telegram status endpoints: `/api/v1/telegram/status` (reports whether a bot token and allowed user ID are configured) and `/api/v1/telegram/pairing-status` (reports live pairing state from a dashboard-level in-memory dict). Both endpoints inform the dashboard whether the Telegram channel is ready to receive messages.

### `Test Infrastructure` — 1 article(s)

- [Cloud Memory Test Fixtures: mongomock-motor Beanie Isolation and MongoMemoryStore Setup](./cloud-memory-test-fixtures-mongomock-motor-beanie-isolation-and-mongomemorystore.md) — This conftest provides two pytest fixtures for the cloud memory test suite: `beanie_memory_db` initializes an isolated in-memory MongoDB via `mongomock-motor` with a compatibility shim for Beanie's `list_collection_names` signature mismatch, and `store` returns a fresh `MongoMemoryStore` bound to that database. Each test gets its own uniquely-named in-memory database to prevent state leakage.

### `Testing` — 15 article(s)

- [Cloud Files Browse Mount Tests: ABAC Filtering and Provider Resolution](./cloud-files-browse-mount-tests-abac-filtering-and-provider-resolution.md) — This test module validates the `browse_mount` function in PocketPaw's cloud files subsystem, covering successful entry listing, ABAC tag-based filtering, and graceful errors when a mount references an unregistered provider. Together these tests protect the file-browsing surface against misconfiguration and unauthorized data exposure.
- [Cloud Files Error Codes and Event Importability Contract Tests](./cloud-files-error-codes-and-event-importability-contract-tests.md) — This test module enforces the public contract of PocketPaw's cloud files error hierarchy and event definitions, verifying that each error carries the correct machine-readable code and HTTP status, and that all file lifecycle events are importable. These tests act as a regression guard for the API surface consumed by clients and downstream event handlers.
- [Cloud Files FastAPI Router Integration Tests: Tree and Browse Endpoints](./cloud-files-fastapi-router-integration-tests-tree-and-browse-endpoints.md) — This module runs end-to-end HTTP integration tests against the FastAPI router built by `build_router`, covering tree structure rendering, entry browsing, 404 for unknown mounts, and 403 for cross-workspace access attempts. It uses HTTPX's `ASGITransport` to drive the full request-response cycle in-process without a running server.
- [Cloud Files Permissions: ABAC Filtering and Capability Derivation Tests](./cloud-files-permissions-abac-filtering-and-capability-derivation-tests.md) — This module validates the layered permissions model in PocketPaw's cloud files system, covering ABAC tag filtering, RBAC-and-mount-writable capability intersection, and the `PermissionsEvaluator` composite. These tests ensure that no combination of user attributes, mount configuration, or entry tags can produce a capability set broader than what all three permission layers jointly allow.
- [Cloud Files Schema Validation Tests: FileEntry, MountConfig, Permission, and Page](./cloud-files-schema-validation-tests-fileentry-mountconfig-permission-and-page.md) — This module validates the Pydantic schema constraints for PocketPaw's core cloud files data models, confirming invariants like namespaced entry IDs, enumerated scopes, valid capability sets, absolute mount templates, and permission intersection semantics. These tests prevent malformed data from entering the system by verifying that validation errors are raised at construction time.
- [Cloud Files Tree Builder Tests: Ordering, Nesting, and Resilient Provider Failures](./cloud-files-tree-builder-tests-ordering-nesting-and-resilient-provider-failures.md) — This module tests `build_tree`, which assembles a hierarchical folder tree from all registered provider mounts. Key behaviors tested include mount ordering by the `order` field, recursive path-segment nesting, and graceful warning collection when individual providers fail rather than aborting the entire tree build.
- [Cloud Files Tree Cache Tests: TTL, Invalidation, and Multi-Tenant Key Isolation](./cloud-files-tree-cache-tests-ttl-invalidation-and-multi-tenant-key-isolation.md) — This module tests `CachedTreeBuilder`, the in-process 30-second cache layer over `build_tree` that prevents repeated provider fan-outs on every tree request. Tests validate that cache hits suppress redundant `list_mounts` calls, that explicit invalidation forces a rebuild, that TTL expiry triggers a new fan-out, and that clearing the cache invalidates all per-user keys.
- [Cloud Memory Test Fixtures: mongomock-motor Beanie Isolation and MongoMemoryStore Setup](./cloud-memory-test-fixtures-mongomock-motor-beanie-isolation-and-mongomemorystore.md) — This conftest provides two pytest fixtures for the cloud memory test suite: `beanie_memory_db` initializes an isolated in-memory MongoDB via `mongomock-motor` with a compatibility shim for Beanie's `list_collection_names` signature mismatch, and `store` returns a fresh `MongoMemoryStore` bound to that database. Each test gets its own uniquely-named in-memory database to prevent state leakage.
- [Cloud Memory Test Package Marker](./cloud-memory-test-package-marker.md) — This is an empty `__init__.py` that marks `tests/cloud/memory/` as a Python package, enabling pytest to discover tests within the directory and allowing relative imports from sibling test modules. No functional code is present; its role is purely structural.
- [FolderProvider Contract Test Base Class: Reusable Compliance Suite](./folderprovider-contract-test-base-class-reusable-compliance-suite.md) — This module defines `ProviderContract`, an abstract base class that encodes the behavioral contract every `FolderProvider` implementation must satisfy, including mount listing, entry listing with pagination, namespaced IDs, and graceful `ProviderUnsupported` errors for write operations. Concrete provider tests subclass it to inherit full compliance coverage automatically.
- [Memory Backend Selection Tests: EE MongoDB Default Flip and OSS Import Isolation](./memory-backend-selection-tests-ee-mongodb-default-flip-and-oss-import-isolation.md) — This module tests that the EE edition flips the default memory backend from `file` to `mongodb` at startup without overriding explicit user choices, verifies that `get_memory_manager()` returns a `MongoMemoryStore`-backed manager after the flip, and enforces that OSS package modules (`src/pocketpaw/memory/`) contain no top-level imports from `ee.*` to preserve clean open-source/enterprise boundary separation.
- [MongoMemoryStore Deduplication Tests: Idempotent Saves Within the 5-Second Window](./mongomemorystore-deduplication-tests-idempotent-saves-within-the-5-second-window.md) — This module tests the deduplication logic in `MongoMemoryStore`, which prevents identical back-to-back writes from creating duplicate memory entries when an agent loop retries the same turn. The dedup key is `(session_key, role, content)` with a 5-second window, and the tests verify that only different content, session, or role values produce distinct database rows.
- [Mount Configuration Loading and Template Resolution Tests](./mount-configuration-loading-and-template-resolution-tests.md) — This module tests the `load_mounts` and `resolve_template` functions that parse and validate `mounts.yaml` configuration files, covering sort order, variable substitution, unknown-variable rejection, and absolute-path enforcement. These tests prevent misconfigured mount definitions from silently producing incorrect or insecure virtual filesystem paths.
- [ProviderRegistry Tests: Registration, Duplicate Detection, and Mount Resolution](./providerregistry-tests-registration-duplicate-detection-and-mount-resolution.md) — This module tests `ProviderRegistry`, the central index that maps provider IDs to `FolderProvider` implementations and resolves virtual mount paths to their owning providers. Key behaviors tested include duplicate registration rejection, longest-prefix mount resolution, and template variable substitution during path matching.
- [Unified Files Service Tests: Workspace Isolation, Dedup, Drive Warnings, and Limit Cap](./unified-files-service-tests-workspace-isolation-dedup-drive-warnings-and-limit-c.md) — This module tests `UnifiedFilesService`, which aggregates files from multiple sources (chat uploads, Drive, local) into a single normalized list, covering workspace scope isolation, soft-delete filtering, cross-source deduplication, source-specific warnings, and the per-request limit cap. It uses `mongomock-motor` to run Beanie document tests in CI without a live MongoDB instance.

### `Tree Building` — 1 article(s)

- [Cloud Files Tree Builder Tests: Ordering, Nesting, and Resilient Provider Failures](./cloud-files-tree-builder-tests-ordering-nesting-and-resilient-provider-failures.md) — This module tests `build_tree`, which assembles a hierarchical folder tree from all registered provider mounts. Key behaviors tested include mount ordering by the `order` field, recursive path-segment nesting, and graceful warning collection when individual providers fail rather than aborting the entire tree build.

### `UI` — 1 article(s)

- [Dashboard E2E Tests: UI Load, Deep Work View, Agent/Task CRUD, and Navigation](./dashboard-e2e-tests-ui-load-deep-work-view-agenttask-crud-and-navigation.md) — This Playwright-based end-to-end test suite verifies that the PocketPaw dashboard loads correctly in a real browser, that key UI elements (chat view, Deep Work view, agent/task creation modals, sidebar, remote access) are present and functional, and that the dashboard title matches the expected beta branding.

### `UI spec` — 1 article(s)

- [Pocket Tools: Create, Add Widget, and Remove Widget for Ripple UI Specs](./pocket-tools-create-add-widget-and-remove-widget-for-ripple-ui-specs.md) — The `pocket.py` module provides three `BaseTool` subclasses — `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — that allow the agent to generate and modify Ripple UniversalSpec JSON structures for pocket workspaces. The module handles three generations of the spec format (flat widget arrays, single-pane UISpec trees, and multi-pane pane-level UISpec trees) and maintains a display-type-to-Ripple-widget mapping for backward compatibility with legacy spec fields.

### `URL parsing` — 1 article(s)

- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.

### `Unicode` — 1 article(s)

- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.

### `Unified File Listing` — 1 article(s)

- [Unified Files Service Tests: Workspace Isolation, Dedup, Drive Warnings, and Limit Cap](./unified-files-service-tests-workspace-isolation-dedup-drive-warnings-and-limit-c.md) — This module tests `UnifiedFilesService`, which aggregates files from multiple sources (chat uploads, Drive, local) into a single normalized list, covering workspace scope isolation, soft-delete filtering, cross-source deduplication, source-specific warnings, and the per-request limit cap. It uses `mongomock-motor` to run Beanie document tests in CI without a live MongoDB instance.

### `User Experience` — 1 article(s)

- [Chat Title Generator — Haiku-Powered Session Naming](./chat-title-generator-haiku-powered-session-naming.md) — `titler.py` generates concise (6 words max, Title Case) chat titles from the first user message using a Claude Haiku-class model. Title generation is entirely best-effort: any failure is silently logged and returns `None` rather than disrupting the chat flow.

### `Utilities` — 1 article(s)

- [CLI Utils: Shared ANSI Colors, Output Helpers, and Secret Masking](./cli-utils-shared-ansi-colors-output-helpers-and-secret-masking.md) — The `cli.utils` module provides the shared visual primitives used by every PocketPaw CLI subcommand — ANSI color constants, formatted print helpers, JSON output, and a `mask_value` function that automatically redacts secrets before display. Centralizing these in one module ensures consistent styling and prevents accidental secret exposure across all CLI commands.

### `Virtual Filesystem` — 1 article(s)

- [Mount Configuration Loading and Template Resolution Tests](./mount-configuration-loading-and-template-resolution-tests.md) — This module tests the `load_mounts` and `resolve_template` functions that parse and validate `mounts.yaml` configuration files, covering sort order, variable substitution, unknown-variable rejection, and absolute-path enforcement. These tests prevent misconfigured mount definitions from silently producing incorrect or insecure virtual filesystem paths.

### `WebSocket` — 11 article(s)

- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.
- [Chat Domain Schema Validation Tests](./chat-domain-schema-validation-tests.md) — This module comprehensively tests all Pydantic schemas in the chat domain — group management, message sending, editing, reactions, WebSocket inbound/outbound messages, cursor pagination, and attachment handling — covering field defaults, length constraints, enum validation, and discriminated union types for WebSocket message dispatch.
- [Chat Router - REST Endpoints and WebSocket Handler](./chat-router-rest-endpoints-and-websocket-handler.md) — The chat router exposes the full `/chat` REST surface and the `/ws/cloud` WebSocket endpoint, both gated behind an enterprise license check. It manages presence lifecycle with a 30-second grace period and dispatches inbound WebSocket messages to the appropriate service methods.
- [Dashboard WebSocket Handler: Real-Time Chat, Tools, and File Navigation](./dashboard-websocket-handler-real-time-chat-tools-and-file-navigation.md) — dashboard_ws.py implements the core WebSocket handler that drives PocketPaw's real-time dashboard UI — handling authentication, message dispatch, tool execution, session switching, file browsing, and skill invocation over a persistent WebSocket connection.
- [E2E Security Tests: WebSocket Authentication Protocol and HTTP Security Headers](./e2e-security-tests-websocket-authentication-protocol-and-http-security-headers.md) — This Playwright-based suite verifies PocketPaw's WebSocket authentication model — that connections succeed from localhost without explicit auth, that the frontend sends an authenticate action as the first WebSocket message, that tokens never appear in WebSocket URLs, and that API endpoints correctly reject unauthenticated requests while static assets remain accessible.
- [EventBus Protocol and In-Process Fan-Out Implementation](./eventbus-protocol-and-in-process-fan-out-implementation.md) — This module defines the EventBus protocol and the InProcessBus implementation that fans out realtime events to WebSocket connections on the same process, routing through AudienceResolver to determine recipients and ConnectionManager to deliver payloads. Module-level singletons with explicit initialisation guards make it safe to wire up during app startup while crashing loudly if accessed before initialisation.
- [Presence Broadcast and Grace Window Tests](./presence-broadcast-and-grace-window-tests.md) — This test module verifies the WebSocket endpoint's presence online/offline lifecycle, specifically the grace window that delays broadcasting a user offline until reconnection is no longer possible. Tests drive _schedule_presence_offline directly and use time-acceleration to keep the suite fast.
- [Realtime WebSocket Event Package Entry Point](./realtime-websocket-event-package-entry-point.md) — This is the package initialiser for the `ee.cloud.realtime` module, which houses the WebSocket-based realtime event system for the EE cloud tier. The file itself carries only a module docstring, serving as the namespace anchor that makes `ee.cloud.realtime` importable as a package while the substantive components live in sub-modules.
- [Room-Scoped WebSocket Routing Tests](./room-scoped-websocket-routing-tests.md) — This module tests the room-scoped routing layer in ConnectionManager, which limits typing indicators and read receipts to only the sockets currently joined to a specific group. It also verifies that non-members cannot inject typing or read-ack events into rooms they do not belong to.
- [WebSocket Connection Manager - Presence, Rooms, and Typing Indicators](./websocket-connection-manager-presence-rooms-and-typing-indicators.md) — The `ConnectionManager` class maintains the in-memory registry of active WebSocket connections, manages presence state with a 30-second grace window, routes messages to group rooms, and expires typing indicators automatically after 5 seconds. It is the real-time backbone for the chat domain.
- [WebSocket Connection Manager Tests: Multi-Device, Typing Indicators, and Offline Grace Periods](./websocket-connection-manager-tests-multi-device-typing-indicators-and-offline-gr.md) — Tests for `ee.cloud.chat.ws.ConnectionManager`, the in-process registry that tracks active WebSocket connections per user, routes outbound messages, and manages typing indicators with auto-expiry timers. Key behavioral contracts include dead-connection cleanup, multi-device fan-out, idempotent stop-typing, and cancellation of offline grace period tasks on reconnect.

### `WhatsApp` — 2 article(s)

- [Neonize WhatsApp Channel Adapter: Mocking, Streaming, and Bus Integration](./neonize-whatsapp-channel-adapter-mocking-streaming-and-bus-integration.md) — Tests for the NeonizeAdapter, which connects PocketPaw to WhatsApp Personal via the neonize library. Covers auto-installation of the optional dependency, message send/receive lifecycle, stream buffering, QR code exposure, and bus subscription.
- [WhatsApp Adapter Test Suite: Webhook Verification, Lifecycle, and Message Sending](./whatsapp-adapter-test-suite-webhook-verification-lifecycle-and-message-sending.md) — This module tests PocketPaw's `WhatsAppAdapter`, covering the Meta webhook verification handshake, adapter lifecycle (`start`/`stop`), outbound message delivery, allowed-number filtering, inbound message routing, and media handling. It ensures the adapter correctly implements the WhatsApp Cloud API protocol.

### `Windows compatibility` — 1 article(s)

- [In-Process MCP Binding: Delivering Pocket Context to the Claude Agent SDK](./in-process-mcp-binding-delivering-pocket-context-to-the-claude-agent-sdk.md) — This module registers an in-process MCP server named `pocketpaw_pocket` that exposes a `get_pocket` tool, working around a Windows `CreateProcess` command-line length limit of ~32KB that would be exceeded if pocket documents were embedded directly in the system prompt. The actual pocket data is fetched lazily from `ee/cloud/pockets/agent_context.py` only when the agent calls the tool.

### `YAML` — 2 article(s)

- [Connector Protocol: YAML Parsing, Registry, and DirectREST Adapter Lifecycle Tests](./connector-protocol-yaml-parsing-registry-and-directrest-adapter-lifecycle-tests.md) — This module tests the PocketPaw connector infrastructure — YAML definition parsing, the `ConnectorRegistry` for connector discovery and lifecycle management, and the `DirectRESTAdapter` for connecting, executing actions, and disconnecting from external REST APIs like Stripe.
- [Pocket Layout Export and User-Defined Template Registry](./pocket-layout-export-and-user-defined-template-registry.md) — This module provides two distinct but related capabilities: a pure YAML serialisation of a pocket's ripple spec for export or sharing, and an in-process workspace-scoped store for user-defined pocket templates. Both capabilities back new REST endpoints added in Cluster B Sub-PR #3 to close a gap in the UI testing guide around widget layout save and share.

### `YAML parsing` — 1 article(s)

- [Pocket Layout Helpers: Export Determinism, Parse Validation, Round-Trip, and Store Scoping Tests](./pocket-layout-helpers-export-determinism-parse-validation-round-trip-and-store-s.md) — This unit test suite covers the pure Python helpers in `ee.cloud.pockets.layouts` — specifically `export_layout_yaml` (deterministic YAML serialization), `parse_layout_yaml` (safe YAML parsing with input validation), and `UserTemplateStore` (in-process template store with workspace scoping). No FastAPI, Beanie, or MongoDB is involved.

### `a2a` — 1 article(s)

- [Test Suite for A2A Protocol Live Smoke Tests](./test-suite-for-a2a-protocol-live-smoke-tests.md) — This script is a live, end-to-end smoke test suite for PocketPaw's A2A (Agent-to-Agent) protocol endpoints. It requires a running PocketPaw server with `POCKETPAW_A2A_ENABLED=true` and exercises every major A2A feature — agent card discovery, message sending, task management, streaming, cancellation, and error handling — reporting PASS/FAIL for each check.

### `acceptance testing` — 1 article(s)

- [API Keys Panel Acceptance Tests: UI Lifecycle Flows for Cluster F](./api-keys-panel-acceptance-tests-ui-lifecycle-flows-for-cluster-f.md) — End-to-end acceptance tests that walk the exact flows the `ApiKeysPanel.svelte` UI executes — create, list, rotate (with old key invalidation), revoke, and sort-by-creation. These tests complement unit-level coverage by pinning the API shape the frontend depends on, catching regressions that break the dashboard silently.

### `access control` — 11 article(s)

- [ABAC Policy Engine: Plan Gates, Role Checks, and Agent Ceilings](./abac-policy-engine-plan-gates-role-checks-and-agent-ceilings.md) — The `abac.py` module implements PocketPaw's attribute-based access control (ABAC) by combining three declarative tables — plan feature gates, action-to-role mappings, and per-role tool limits — into a single `evaluate_policy` function. It is designed to run after RBAC role resolution, adding plan-tier and agent-ceiling checks that role membership alone cannot express.
- [ConnectorProtocol: The Interface Contract for All PocketPaw Data Source Adapters](./connectorprotocol-the-interface-contract-for-all-pocketpaw-data-source-adapters.md) — `protocol.py` defines the structural typing interface that all connector adapters must satisfy, along with the shared result dataclasses and enums they return. The addition of `IngestACL` and `IngestAdapter` in Move 7 ensures that documents pulled into Single Brain carry their source-side permissions, preventing data from a private Slack channel from becoming accessible to unauthorized users.
- [Fabric Journal Store Tests: Scope Isolation, Pagination, Projection Rebuild, and Policy Correctness](./fabric-journal-store-tests-scope-isolation-pagination-projection-rebuild-and-pol.md) — This suite pins four invariants for PocketPaw's journal-backed Fabric object store: correct lifecycle (create/update/archive), scope-based access control that returns empty results rather than leaking existence, post-filter pagination totals, and deterministic projection rebuild from the event journal. It supersedes a previous implementation that leaked object counts across scope boundaries.
- [Fabric Scope Policy Engine — Visibility Decisions for Ontology Objects](./fabric-scope-policy-engine-visibility-decisions-for-ontology-objects.md) — This module provides the scope-based visibility engine for Fabric objects, implementing bidirectional containment matching so that a caller with scope `org:sales:*` can see objects tagged `org:sales:leads` and vice versa. It is a local copy of the logic from soul-protocol's `scopes_overlap` function, kept independent to avoid pulling the full engine into minimal runtime slices and to preserve the audit trail of which scope granted access.
- [Files Router Security Tests: Scope Enforcement and Symlink Jail Traversal Prevention](./files-router-security-tests-scope-enforcement-and-symlink-jail-traversal-prevent.md) — Security regression tests for issues #884 and #886 in PocketPaw's files router: #884 proved any API key (even one with no scopes) could read arbitrary files; #886 proved `/files/download-zip` followed symlinks outside the configured file jail. These tests lock in the fixes and prevent both vulnerabilities from regressing.
- [Folder CRUD Integration Tests: Auto-Chain Creation, Rename Propagation, and RBAC Enforcement](./folder-crud-integration-tests-auto-chain-creation-rename-propagation-and-rbac-en.md) — Integration tests for the EE uploads folder system covering folder creation with parent requirement, duplicate detection, automatic folder chain creation on upload, cascade rename of descendant paths, safe and cascading deletion, and permission enforcement distinguishing non-admin strangers from workspace admins.
- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.
- [Pockets FastAPI Router: CRUD, Widgets, Sharing, and Layout Export](./pockets-fastapi-router-crud-widgets-sharing-and-layout-export.md) — This is the FastAPI router for the pockets domain, covering the full lifecycle of a pocket from creation through sharing, widget management, agent and team membership, and the newer layout export and user template endpoints. All routes are gated behind a license check dependency, and owner-only operations use explicit access guard dependencies to prevent privilege escalation.
- [PocketService: Business Logic for Pocket Lifecycle, Widgets, Sharing, and Agents](./pocketservice-business-logic-for-pocket-lifecycle-widgets-sharing-and-agents.md) — PocketService is the stateless service class that encapsulates all pocket business logic — from CRUD and widget management through share link generation, collaborator access, and agent-generated pocket creation from ripple specs. Access control is enforced by two helper functions that distinguish owner-only actions from edit-access actions, and every mutation emits events to the realtime bus and shared event bus.
- [RBAC Action Matrix Tests: Parametrized Role Enforcement and Coverage Guard](./rbac-action-matrix-tests-parametrized-role-enforcement-and-coverage-guard.md) — A parametrized test matrix that exercises every entry in the canonical `ACTIONS` table against every role in the matching role family, verifying that `check_action` allows or raises `Forbidden` with the correct deny code. A meta-test enforces that no new action can be added without both an allow-path and a deny-path assertion, preventing silent coverage gaps.
- [Tool Policy System: Profile-Based Access Control for Agent Tools](./tool-policy-system-profile-based-access-control-for-agent-tools.md) — The tool policy module implements a layered access control system that determines which tools an agent backend is allowed to use, using named profiles, logical tool groups, and explicit allow/deny lists with a fixed precedence order. It prevents misconfiguration from silently granting full access and supports fine-grained control over MCP server tools using wildcard patterns.

### `access-control` — 2 article(s)

- [Enterprise Edition Package — Gated Feature Namespace](./enterprise-edition-package-gated-feature-namespace.md) — The `pocketpaw.ee` package is the root namespace for PocketPaw's Enterprise Edition, housing features gated behind team, business, and enterprise plans under the Functional Source License (FSL 1.1). It organizes five EE subsystems: guards (RBAC/ABAC), automations (trigger-action workflows), fabric (workspace mesh), instinct (proactive behaviors), and audit (compliance trail).
- [Workspace Invitation Model](./workspace-invitation-model.md) — The Invite document manages workspace membership invitations sent to email addresses, tracking acceptance state, expiry, and optional group auto-assignment. It enforces a 7-day default TTL with timezone-aware expiry checking to prevent stale invitations from granting unintended access.

### `accessibility` — 2 article(s)

- [Accessibility Tree Snapshot Generator — Semantic Browser State for LLMs](./accessibility-tree-snapshot-generator-semantic-browser-state-for-llms.md) — snapshot.py converts Playwright's raw accessibility tree into a compact, indented text representation with [ref=N] markers that an LLM can read to understand page structure and reference specific elements for interaction. RefMap maintains the mapping from integer references back to element selectors, and SnapshotGenerator handles recursive tree traversal, role filtering, name truncation, and selector generation.
- [Browser Snapshot Generator Tests: Accessibility Tree to Agent-Readable Text Conversion](./browser-snapshot-generator-tests-accessibility-tree-to-agent-readable-text-conve.md) — This test suite validates `SnapshotGenerator`, which converts Playwright's accessibility tree JSON into a compact, ref-tagged text representation that AI agents can read and reason about. Tests cover the full rendering pipeline: interactive element ref assignment, non-interactive element handling, heading/list/image rendering, hidden element filtering, password field masking, and the `RefMap` reference number system.

### `agent autonomy` — 1 article(s)

- [Instinct Decision Pipeline — Package Entry Point](./instinct-decision-pipeline-package-entry-point.md) — The `ee.instinct` package is PocketPaw's human-in-the-loop decision engine: agents propose actions, humans approve or edit them, approved actions execute, and any edits are captured as corrections that feed back into the soul for future improvement. This module re-exports every public symbol from the five sub-modules so consumers import from a single namespace.

### `agent backend` — 2 article(s)

- [ClaudeAgentSDK Dispatch and Persistent Client Tests](./claudeagentsdk-dispatch-and-persistent-client-tests.md) — This module tests the dispatch logic for `ClaudeAgentSDK`, documenting the removal of the `_fast_chat` direct-API bypass in version 0.4.16 and the introduction of a persistent `ClaudeSDKClient` that reuses connections across calls. It verifies that all task complexities (SIMPLE, MODERATE, routing disabled) flow through the CLI path and validates the client lifecycle: creation, reuse, reconnection on model change, fallback on failure, interrupt, and cleanup.
- [Google ADK Backend Tests](./google-adk-backend-tests.md) — This module provides fully mocked tests for `GoogleADKBackend`, PocketPaw's Google Agent Development Kit integration. It covers metadata/capability discovery, initialization with and without the SDK, event stream processing (text, function calls, function responses, errors), session management, MCP toolset policy enforcement, status reporting, and the backward-compatibility alias `GeminiCLIBackend`.

### `agent backends` — 7 article(s)

- [Agent Backend Protocol: Capability Flags, BackendInfo, and Identity Contract Tests](./agent-backend-protocol-capability-flags-backendinfo-and-identity-contract-tests.md) — This test suite validates the core contracts of PocketPaw's agent backend abstraction layer, covering the `_DEFAULT_IDENTITY` fallback string, the `Capability` flag enum, the immutable `BackendInfo` descriptor, and the `AgentBackend.run()` method signature. Together these tests enforce the interface that every backend adapter must satisfy to be a valid plugin in PocketPaw's multi-backend architecture.
- [Agent Backend Registry: Lazy Import, Legacy Name Resolution, and Plugin Registration Tests](./agent-backend-registry-lazy-import-legacy-name-resolution-and-plugin-registratio.md) — This test suite covers PocketPaw's backend registry, which provides lazy-loaded access to agent backends (Claude SDK, OpenAI Agents, Google ADK, OpenCode) while maintaining backward compatibility through a legacy name mapping. Tests verify that deprecated names like `gemini_cli` transparently resolve to `google_adk`, that unknown backends return `None` rather than raising, and that third-party plugins can register themselves at runtime.
- [Agent Backends and Router Tests: ClaudeSDKBackend, Registry Routing, Dangerous Hook, and CLI Auth](./agent-backends-and-router-tests-claudesdkbackend-registry-routing-dangerous-hook.md) — Tests for the multi-SDK agent architecture covering `AgentEvent` protocol, `ClaudeSDKBackend` (formerly ClaudeAgentSDKWrapper), registry-based `AgentRouter` backend selection with legacy fallbacks, the fail-closed dangerous command hook, and a regression test for PocketPaw incorrectly requiring an API key when Claude CLI authentication is active.
- [Backends API Tests: Listing, Availability Checks, and Safe Install Subprocess Handling](./backends-api-tests-listing-availability-checks-and-safe-install-subprocess-handl.md) — This test file covers PocketPaw's `/api/v1/backends` router, verifying that the backend registry is exposed correctly, that the `_check_available` helper correctly detects missing imports, and that the async install endpoint handles subprocess timeouts and stderr secret redaction without leaking sensitive data.
- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.
- [OpenAI Agents SDK Backend: Tool Wiring, Session Management, and Graceful Degradation](./openai-agents-sdk-backend-tool-wiring-session-management-and-graceful-degradatio.md) — Tests for the OpenAI Agents SDK backend adapter, covering custom tool registration, per-backend provider selection, SDK-absent graceful degradation, native SQLite session management, and lazy database path resolution. All tests run without a real OpenAI Agents SDK installation via comprehensive mocking.
- [OpenCode Backend: REST API Integration, Session Caching, and Tool Injection](./opencode-backend-rest-api-integration-session-caching-and-tool-injection.md) — Tests for the OpenCodeBackend, which connects PocketPaw to OpenCode's REST API for agentic coding tasks. Covers health checking, session creation and caching, the message endpoint contract, system prompt tool instruction injection, streaming responses, and graceful error handling for unreachable servers.

### `agent behavior` — 1 article(s)

- [Proactive Daemon Module: Transforming PocketPaw from Reactive Chatbot to Intention-Driven Agent](./proactive-daemon-module-transforming-pocketpaw-from-reactive-chatbot-to-intentio.md) — The `pocketpaw.daemon` package turns PocketPaw from a request/response chatbot into a proactive agent that initiates actions based on user-defined intentions and event triggers. Its `__init__.py` assembles the five subsystems — `IntentionStore`, `TriggerEngine`, `ContextHub`, `IntentionExecutor`, and `ProactiveDaemon` — into a single importable surface.

### `agent bridge` — 2 article(s)

- [Agent Bridge Attachment Forwarding Regression Test](./agent-bridge-attachment-forwarding-regression-test.md) — This regression test guards against the silent attachment-drop bug in `_run_agent_response`, where file attachments sent by users in channel messages were never forwarded into the agent's prompt. The test confirms that the channel code path formats attachment metadata — filename, MIME type, and size — into the prompt in the same shape used by the DM path.
- [Agent Bridge Event Routing and Wire Type Tests](./agent-bridge-event-routing-and-wire-type-tests.md) — This module verifies that `agent_bridge` routes all group broadcasts through `emit()` rather than directly calling `ws_manager.broadcast_to_group`, and that the four agent stream event types (`AgentStreamStart`, `AgentStreamChunk`, `AgentToolUse`, `AgentStreamEnd`) are correctly defined and importable from the bridge.

### `agent capabilities` — 4 article(s)

- [Filesystem Tools: Read, Write, List, and Edit Files](./filesystem-tools-read-write-list-and-edit-files.md) — The `filesystem.py` module provides four foundational `BaseTool` subclasses — `ReadFileTool`, `WriteFileTool`, `ListDirTool`, and `EditFileTool` — that give the PocketPaw agent controlled access to the local filesystem. All path operations go through an `is_safe_path` guard that enforces the configured `file_jail_path` boundary, preventing agents from reading or writing outside the permitted sandbox.
- [InstallPackageTool: Guardian-Reviewed pip Package Installation](./installpackagetool-guardian-reviewed-pip-package-installation.md) — The `InstallPackageTool` allows the PocketPaw agent to install Python packages via pip at runtime, subject to two security layers: a strict regex that validates the package spec format before any subprocess is spawned, and a Guardian AI review that checks the package name for typosquatting and malicious intent before installation proceeds. It runs with `trust_level = "elevated"` because it can permanently modify the Python environment.
- [Memory Tools: Remember, Recall, and Forget Long-Term Agent Memory](./memory-tools-remember-recall-and-forget-long-term-agent-memory.md) — The `memory.py` module provides three `BaseTool` subclasses — `RememberTool`, `RecallTool`, and `ForgetTool` — that expose PocketPaw's long-term memory manager to the agent, enabling it to save facts about the user, retrieve them in future sessions, and delete outdated or incorrect memories. These tools are the agent-facing surface of the memory manager, which persists memories across session boundaries independently of the LLM's context window.
- [RunPythonTool: Guardian-Reviewed Sandboxed Python Execution](./runpythontool-guardian-reviewed-sandboxed-python-execution.md) — The `RunPythonTool` lets the PocketPaw agent execute arbitrary Python code in a sandboxed subprocess, subject to a Guardian AI safety review before execution. The sandbox uses the configured `file_jail_path` as its working directory, and UUID-based script filenames prevent filename collisions when multiple executions overlap. Trust level is `elevated` because arbitrary code execution is the most powerful and dangerous capability an agent can have.

### `agent communication` — 2 article(s)

- [A2A Client Tests: HTTP Operations, Context Manager Reuse, Streaming, Delegate Tool, and SSRF Protection](./a2a-client-tests-http-operations-context-manager-reuse-streaming-delegate-tool-a.md) — Tests for the PocketPaw Agent-to-Agent (A2A) client library, covering HTTP task operations (send, get, cancel, stream), response error handling, agent card fetching with caching, the `A2ADelegateTool` multi-turn wrapper, and SSRF protection that blocks requests to private IP ranges and invalid schemes.
- [A2A Server Tests: Models, State Machines, JSON-RPC Dispatch, and Task Lifecycle](./a2a-server-tests-models-state-machines-json-rpc-dispatch-and-task-lifecycle.md) — Extensive tests for the PocketPaw A2A server implementation covering Pydantic model serialisation with type discriminators, TaskState transition validation, JSON-RPC 2.0 dispatcher behaviour (parse errors, batch requests, method routing), and the full task lifecycle (submit, poll, stream, cancel) against a minimal in-memory FastAPI app.

### `agent configuration` — 2 article(s)

- [Identity API Tests: Reading and Partially Updating Agent Identity Files](./identity-api-tests-reading-and-partially-updating-agent-identity-files.md) — This test file covers PocketPaw's `/api/v1/identity` router, which exposes the agent's persistent identity configuration — the markdown files that define personality, soul, style, instructions, and user profile — as a readable and partially updatable API resource.
- [Memory API Tests: Settings, Long-Term Memory CRUD, and Backend Stats](./memory-api-tests-settings-long-term-memory-crud-and-backend-stats.md) — This test file covers PocketPaw's `/api/v1/memory` router, which exposes configuration for the memory backend (file store vs. mem0), CRUD operations over long-term memory entries, and per-backend statistics.

### `agent context` — 1 article(s)

- [Recent Files Tracker Tests: Bash Path Heuristic and Tool Path Extraction](./recent-files-tracker-tests-bash-path-heuristic-and-tool-path-extraction.md) — `RecentFilesTracker` maintains a most-recently-used list of files accessed by the agent, enabling context-aware suggestions and memory seeding. These tests cover the Bash command path heuristic (which was missing and is now implemented), tool-specific path extraction for Read/Write/Edit/Bash tools, and tracker deduplication and ordering behavior.

### `agent extensibility` — 2 article(s)

- [CreateSkillTool Tests — Skill Generation, Naming Validation, and YAML Frontmatter](./createskilltool-tests-skill-generation-naming-validation-and-yaml-frontmatter.md) — This test file validates `CreateSkillTool`, which allows agents to generate new PocketPaw skill files (SKILL.md) at runtime. Tests cover tool metadata, skill name regex validation, YAML frontmatter format, overwrite protection, skill loader reload triggering, and the allowed-tools and user-invocable flags.
- [SkillLoader and SKILL.md Parsing Tests](./skillloader-and-skillmd-parsing-tests.md) — This test file validates `SkillLoader` and `parse_skill_md` from `pocketpaw.skills.loader`, covering SKILL.md frontmatter parsing, optional field handling, no-frontmatter rejection, directory-name fallback for skill naming, prompt argument substitution, loader lifecycle, skill retrieval, reload, multi-path override resolution, and integration with real skill paths.

### `agent integration` — 1 article(s)

- [UploadResolver: Bridging Upload URLs to Local Agent Paths](./uploadresolver-bridging-upload-urls-to-local-agent-paths.md) — UploadResolver translates frontend upload URLs (e.g., `/api/v1/uploads/{id}`) into local filesystem paths that the agent loop can inject into Read tool calls or image blocks. It is the OSS-only half of a two-track design, with EE supplying a workspace-scoped variant backed by MongoDB.

### `agent interface` — 1 article(s)

- [BrowserTool Integration Tests: Agent-Facing Browser Interface, Actions, Error Handling, and Session Management](./browsertool-integration-tests-agent-facing-browser-interface-actions-error-handl.md) — This test suite validates `BrowserTool`, the agent-facing wrapper that exposes browser automation as a single tool with an action-dispatch interface. Tests cover the tool's metadata contract (name, trust level, JSON schema), all seven supported actions (navigate, click, type, scroll, snapshot, screenshot, close), input validation, error propagation, and multi-session isolation.

### `agent loop` — 4 article(s)

- [Agent Loop Media Helpers: _format_bytes Unit Tests](./agent-loop-media-helpers-formatbytes-unit-tests.md) — Focused unit tests for the `_format_bytes` helper in `pocketpaw.agents.loop`, verifying that file sizes are formatted correctly across the B, KB, MB, and GB ranges. This utility generates human-readable size strings for media attachment prompts injected into the agent's context.
- [Agent Loop Pocket Threading Tests: Cloud User and Workspace Context Resolution](./agent-loop-pocket-threading-tests-cloud-user-and-workspace-context-resolution.md) — Tests for the `_create_pocket_and_session` and `_publish_pocket_event` functions in `pocketpaw.agents.loop`, covering all resolution paths for threading cloud user identity and workspace context through pocket creation: explicit ids, active-workspace fallback, first-user fallback for self-hosted deployments, graceful handling of invalid ids, and metadata propagation in pocket events.
- [Agent Loop Tests: Message Processing, AgentEvent Architecture, Error Handling, and GC Lock Management](./agent-loop-tests-message-processing-agentevent-architecture-error-handling-and-g.md) — Tests for the unified `AgentLoop` that orchestrates message consumption, agent router invocation, memory management, and outbound publishing. Updated for the `AgentEvent`-based streaming architecture (replacing raw dict chunks), these tests verify end-to-end message processing, graceful error handling, stale-lock GC, and loop teardown.
- [AGENTS.md Support Tests: Loader, Caching, Directory Walk, Context Injection, and SystemEvent Emission](./agentsmd-support-tests-loader-caching-directory-walk-context-injection-and-syste.md) — Tests for the AGENTS.md feature (issue #456) that allows project-specific constraints to be injected into the agent's system prompt. Covers the `AgentsMdLoader` find-and-cache logic (including `.git` boundary stopping, nearest-file preference, and mtime-based cache invalidation), `AgentsMd` formatting, `AgentContextBuilder` injection, and `AgentLoop` `SystemEvent` emission when the file is found.

### `agent persona` — 1 article(s)

- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.

### `agent pipelines` — 1 article(s)

- [ResearchTool Test Suite — Web Search, URL Extraction, and Happy Path](./researchtool-test-suite-web-search-url-extraction-and-happy-path.md) — This test file validates `ResearchTool`, PocketPaw's built-in agentic research capability that chains web search, URL content extraction, and LLM summarization. Tests cover tool metadata, URL parsing edge cases, search failure handling, and the full happy-path pipeline using mocked external dependencies.

### `agent routing` — 1 article(s)

- [AgentRouter Fallback and Streaming Tests](./agentrouter-fallback-and-streaming-tests.md) — This test file validates `AgentRouter`'s fault-tolerant backend selection logic, covering hard exception failures, error-event failures, full backend chain exhaustion, streaming event pass-through, and no-fallback configurations. The tests use in-process stub backends injected via `monkeypatch` to exercise the router without real LLM backends.

### `agent runtime` — 31 article(s)

- [A2A Client: HTTP Client for Delegating Tasks to External A2A Agents](./a2a-client-http-client-for-delegating-tasks-to-external-a2a-agents.md) — `A2AClient` is an async HTTP client that allows PocketPaw to communicate with external A2A-compatible agents. It supports blocking task submission, SSE streaming, task polling, cancellation, and Agent Card discovery — with connection sharing, TTL-cached Agent Cards, and transparent wrapping of direct-Message responses into the Task model.
- [Agent Bridge: Routing Chat Messages to the Agent Pool](./agent-bridge-routing-chat-messages-to-the-agent-pool.md) — The agent bridge connects incoming group-chat `message.sent` events to the PocketPaw agent pool, evaluating each agent's respond mode and streaming responses back over WebSocket. It decouples the chat send path from agent processing by dispatching all work to background tasks, preventing smart-mode LLM calls from blocking message delivery.
- [Agent-to-Agent (A2A) Protocol Module: Public API and Roadmap](./agent-to-agent-a2a-protocol-module-public-api-and-roadmap.md) — The `pocketpaw.a2a` package implements Google's Agent-to-Agent (A2A) protocol, allowing PocketPaw to act as both an A2A server (accepting tasks from external agents) and an A2A client (delegating tasks to external agents). This `__init__.py` re-exports the full public surface and documents the three-phase implementation roadmap.
- [AgentLoop Concurrency Controls: Session Locks, Global Semaphore, and File Memory Safety](./agentloop-concurrency-controls-session-locks-global-semaphore-and-file-memory-sa.md) — This test module validates the three-layer concurrency model in PocketPaw's AgentLoop: per-session serialization via asyncio locks, cross-session parallelism as the happy path, and a global semaphore that caps total simultaneous conversations. It also covers the FileMemoryStore's write lock and transient PermissionError retry logic that prevent JSON corruption under concurrent writes.
- [Audit Logging System: Append-Only Agent Action Trail](./audit-logging-system-append-only-agent-action-trail.md) — This module implements a secure, append-only audit log that records every critical agent action to a JSONL file at `~/.pocketpaw/audit.jsonl`. It provides structured severity tiers, optional PII filtering, a callback fan-out mechanism, and a singleton accessor — forming the forensic backbone of PocketPaw's security stack.
- [Cloudflare Tunnel Manager: Exposing Local PocketPaw Servers to the Public Internet](./cloudflare-tunnel-manager-exposing-local-pocketpaw-servers-to-the-public-interne.md) — The `TunnelManager` class manages a `cloudflared` subprocess to create a Cloudflare Tunnel that exposes a locally running PocketPaw server on a public HTTPS URL, enabling webhook delivery, mobile access, and external agent connectivity without firewall configuration. It supports auto-installation via Homebrew, URL extraction from stderr with a regex, graceful shutdown, and singleton lifecycle registration.
- [Context Window Budget Tests: Priority-Based Block Assembly and KB Integration](./context-window-budget-tests-priority-based-block-assembly-and-kb-integration.md) — This test module, added in April 2026, validates `AgentContextBuilder._assemble_with_budget`, which assembles the agent's system prompt from priority-ranked blocks while respecting both a global character budget and per-block caps. It also tests the knowledge-base context injection path that calls the `kb-go` CLI subprocess to fetch relevant articles.
- [Copilot SDK Backend Tests: Capabilities, Provider Config, Session Management, and Tool Injection](./copilot-sdk-backend-tests-capabilities-provider-config-session-management-and-to.md) — This test module validates the `CopilotSDKBackend` — PocketPaw's adapter for GitHub Copilot's agent SDK — entirely through mocks, requiring no real CLI binary or SDK installation. It covers static capability metadata, provider-specific configuration dispatch, multi-turn session reuse, event stream mapping, and the injection of PocketPaw tool instructions into the Copilot prompt.
- [Credential Scrubber: Keeping Secrets Out of Audit Logs and Error Records](./credential-scrubber-keeping-secrets-out-of-audit-logs-and-error-records.md) — This module provides `scrub_params()` and `scrub_command()` — two functions that mask credential-like data before it enters audit logs, system logger fallbacks, and dangerous-command records. It combines an explicit secret field list from `credentials.py` with heuristic field-name patterns to catch secrets regardless of how they are named.
- [Deep Agents Backend Tests: LangChain Integration, Provider Parsing, Content Extraction, and Registry](./deep-agents-backend-tests-langchain-integration-provider-parsing-content-extract.md) — This test module validates the `DeepAgentsBackend` — PocketPaw's LangChain-based agent backend — through mocks, requiring no real LangChain or LangGraph installation. It covers static metadata, the `provider:model` string parsing convention, LangGraph `Overwrite` object unwrapping, content extraction from mixed message formats, graceful degradation when the SDK is absent, and backend registry integration.
- [Enterprise Automations Module (Trigger Engine Placeholder)](./enterprise-automations-module-trigger-engine-placeholder.md) — The `ee/automations/` module is a reserved namespace for a rule-based trigger engine that will let users define time-based and data-condition-based automations — turning PocketPaw agents from reactive responders into proactive actors.
- [Guardian Agent: Secondary LLM-Based Security Filter for Dangerous Commands](./guardian-agent-secondary-llm-based-security-filter-for-dangerous-commands.md) — The Guardian Agent provides a second-opinion AI security layer that evaluates commands and actions against a SAFE/DANGEROUS classification before execution. It sits above the deterministic regex rails, handling ambiguous cases where pattern matching alone cannot determine intent.
- [MCP + Claude Agent SDK Integration: Server Filtering, Transport Mapping, and Policy Enforcement](./mcp-claude-agent-sdk-integration-server-filtering-transport-mapping-and-policy-e.md) — The MCP-Claude SDK integration tests (Sprint 17) validate that `ClaudeAgentSDK._get_mcp_servers()` correctly translates PocketPaw's internal `MCPServerConfig` objects into the format expected by the Claude Agent SDK, applying enable/disable filtering, transport type mapping, and security policy checks before handing the server list to the SDK.
- [Mission Control (Deep Work) API Tests: Project Lifecycle, State Transitions, and REST Endpoints](./mission-control-deep-work-api-tests-project-lifecycle-state-transitions-and-rest.md) — This test module, created in February 2026, covers PocketPaw's Mission Control subsystem — the project and task management layer that tracks long-horizon AI work. It validates the full project lifecycle through both the `MissionControlManager` directly and the FastAPI REST endpoints, including state transitions (draft, approved, paused, resumed) and proper 404 handling for missing resources.
- [Model Router: Task Complexity Classification and Tiered Model Selection](./model-router-task-complexity-classification-and-tiered-model-selection.md) — Tests for ModelRouter, which classifies incoming messages by complexity and routes them to the appropriate LLM tier (Haiku for simple, Sonnet for moderate, Opus for complex). Validates classification boundaries, edge cases like empty messages, and that the model names match the configured tiers.
- [Output Redaction: Preventing Secret Leakage in Agent Responses](./output-redaction-preventing-secret-leakage-in-agent-responses.md) — This module provides output-level redaction that strips API keys, tokens, and other credentials from agent response text before it reaches clients. Operating at the message bus level makes it backend-agnostic — the same redaction applies regardless of whether the agent backend is Claude Code, Open Interpreter, or any future addition.
- [PawConfig Tests: YAML Loading, Defaults, and Environment Variable Overrides](./pawconfig-tests-yaml-loading-defaults-and-environment-variable-overrides.md) — `PawConfig` is PocketPaw's project-level configuration object, loaded from `paw.yaml` in the project root. These tests cover the full priority chain: defaults when no file exists, YAML values when the file is present, and environment variable overrides that take precedence over both.
- [PII Detection and Masking: Regex-Based Personal Data Protection Layer](./pii-detection-and-masking-regex-based-personal-data-protection-layer.md) — This module provides a configurable PII detection and masking system using pre-compiled regex patterns for common personal data types (emails, phone numbers, SSNs, etc.). It supports per-type action overrides — mask, redact, hash, or passthrough — and integrates as both a standalone scanner and a subsystem of the audit logger's PII filter.
- [PocketPaw Skills Module: Entry Point for the AgentSkills Ecosystem Integration](./pocketpaw-skills-module-entry-point-for-the-agentskills-ecosystem-integration.md) — The `skills` package `__init__.py` assembles and re-exports the three subsystems that make up PocketPaw's skill integration layer: loading, installation, and execution. By surfacing a clean public API here, consumers throughout the codebase can import skill primitives from a single location without knowing the internal module structure.
- [Proactive Daemon Tests: Cron Scheduling, Intention CRUD, Context Hub, and Stale Session Triggers](./proactive-daemon-tests-cron-scheduling-intention-crud-context-hub-and-stale-sess.md) — This test module covers PocketPaw's proactive daemon — the subsystem that fires agent prompts on a schedule rather than waiting for user messages. It validates cron expression parsing (including named presets), the `IntentionStore` CRUD lifecycle, `ContextHub` template variable injection, the `TriggerEngine` scheduling layer, and the stale-session trigger that nudges idle users.
- [Prompt Injection Scanner: Two-Tier Heuristic and LLM Detection](./prompt-injection-scanner-two-tier-heuristic-and-llm-detection.md) — This module implements a two-tier prompt injection detection system: fast regex heuristics (~20 patterns) as Tier 1 for known attack signatures, and an optional LLM-based deep scan as Tier 2 for novel or obfuscated injections. It normalizes Unicode before scanning to defeat homoglyph substitution attacks.
- [Security Rails: Single Source of Truth for Dangerous Command Patterns](./security-rails-single-source-of-truth-for-dangerous-command-patterns.md) — This module defines the canonical set of dangerous command patterns used across every security layer in PocketPaw — Guardian, ShellTool, native agent, and Claude SDK hooks all import from here. Centralizing patterns in one place eliminates the drift and inconsistency that would arise from each subsystem maintaining its own ad-hoc block list.
- [SkillExecutor: Streaming Skill Execution Through the Agent Backend](./skillexecutor-streaming-skill-execution-through-the-agent-backend.md) — The `SkillExecutor` bridges skill definitions (Markdown files) and the agent backend (Open Interpreter or Claude Code) by building a prompt from skill content and user arguments, then streaming the agent's response back as an async iterator of result dicts. It is the runtime engine that transforms a loaded skill into an executed task.
- [SkillLoader: Discovery, Parsing, and Hot-Reload of AgentSkills from Disk](./skillloader-discovery-parsing-and-hot-reload-of-agentskills-from-disk.md) — The `SkillLoader` discovers and parses skills from two filesystem paths — the central AgentSkills directory and a PocketPaw-specific directory — by reading YAML frontmatter and Markdown content from `SKILL.md` files. It supports hot-reloading, invocability filtering, search, and a singleton accessor for shared use across the application.
- [Smoke Test: Agent Dispatch Respond-Mode Matrix in Group Chat](./smoke-test-agent-dispatch-respond-mode-matrix-in-group-chat.md) — This script verifies that `_should_agent_respond` correctly routes messages in group chat based on each agent's respond mode (`auto`, `mention_only`, `silent`) and the set of @mentions present. It was written to lock down a real bug where two `auto` agents both fired when only one was mentioned.
- [Smoke Test: MemoryManager Singleton Stays Live After Backend Swap](./smoke-test-memorymanager-singleton-stays-live-after-backend-swap.md) — This test reproduces a real bug where `AgentLoop` captured `get_memory_manager()` at construction time — before cloud database initialization — and then held a stale reference to the file-based store even after the MongoDB backend was registered. It verifies that the singleton's internal store is swapped in-place so pre-captured references automatically see the new backend.
- [Soul Bridge and Bootstrap Provider Tests: SoulBridge and SoulBootstrapProvider](./soul-bridge-and-bootstrap-provider-tests-soulbridge-and-soulbootstrapprovider.md) — These tests cover the `SoulBootstrapProvider` — which translates a Soul Protocol soul object into a `BootstrapContext` for PocketPaw's agent runtime — and `SoulBridge`, the async shim that wraps soul `observe()` and `recall()` calls with error-swallowing guarantees. Together they ensure the paw module can safely integrate with soul-protocol without crashing when the soul is unavailable or misbehaves.
- [Tool Policy System: Profile-Based Access Control for Agent Tools](./tool-policy-system-profile-based-access-control-for-agent-tools.md) — The tool policy module implements a layered access control system that determines which tools an agent backend is allowed to use, using named profiles, logical tool groups, and explicit allow/deny lists with a fixed precedence order. It prevents misconfiguration from silently granting full access and supports fine-grained control over MCP server tools using wildcard patterns.
- [Tool Pre-Filtering: Relevance Scoring to Reduce Prompt Bloat](./tool-pre-filtering-relevance-scoring-to-reduce-prompt-bloat.md) — The tool filter module scores each registered tool against the current user message using token overlap, returning only the top-N most relevant tools to avoid overloading the LLM context window. A curated set of essential tools -- memory, search, and pocket management -- are always included regardless of relevance score.
- [Tool Protocol and Base Class: The Contract Every PocketPaw Tool Implements](./tool-protocol-and-base-class-the-contract-every-pocketpaw-tool-implements.md) — The `protocol.py` module defines the structural contract that all PocketPaw tools must satisfy -- a `ToolDefinition` dataclass for LLM schema representation, a `ToolProtocol` for structural subtyping, and a `BaseTool` abstract class providing default implementations and helper methods. Cross-backend schema normalization is handled here to prevent silent failures with strict OpenAI-style validators.
- [Tool Registry: Centralized Execution Hub with Policy Enforcement and Audit Logging](./tool-registry-centralized-execution-hub-with-policy-enforcement-and-audit-loggin.md) — The `ToolRegistry` is PocketPaw's central runtime for tool management -- it stores registered tools, enforces `ToolPolicy` access control, validates parameters, executes tools under a configurable timeout, scrubs sensitive data from logs, and writes to the audit trail. Multiple iterative hardening passes reflect its role as the primary security enforcement point at execution time.

### `agent safety` — 3 article(s)

- [EE Instinct: Human-in-the-Loop Action Approval System Tests](./ee-instinct-human-in-the-loop-action-approval-system-tests.md) — This test suite validates the full lifecycle of PocketPaw's Instinct subsystem, which gates AI-proposed actions behind human approval before execution. It covers store-layer CRUD, audit trail integrity, REST endpoint contracts, lifecycle state transitions, and edge cases such as approving non-existent actions.
- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.
- [Plan Mode Tests: PlanStep, ExecutionPlan, PlanManager, and Approval Flow](./plan-mode-tests-planstep-executionplan-planmanager-and-approval-flow.md) — PocketPaw's plan mode allows agents to propose multi-step execution plans and wait for human approval before running potentially destructive operations. These tests cover `PlanStep` previews, `ExecutionPlan` assembly, `PlanManager` lifecycle (create, approve, reject, expire), and the async approval-wait mechanism.

### `agent tools` — 4 article(s)

- [Critical Gaps Tests: Real HTTP in YAML Connectors, Fabric Queries, and Instinct Tool Operations](./critical-gaps-tests-real-http-in-yaml-connectors-fabric-queries-and-instinct-too.md) — This test module, created in March 2026, addresses three previously untested integration paths: the `DirectRESTAdapter` making authenticated HTTP calls to YAML-defined connectors like Stripe, the Fabric knowledge-graph query tools, and the Instinct action-proposal tools. It uses real YAML connector definitions from the connectors directory and mocks only the network layer.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [Memory Tools: RememberTool, RecallTool, and Session List API](./memory-tools-remembertool-recalltool-and-session-list-api.md) — Tests for the agent-facing memory tools (`remember` and `recall`) and the session list REST endpoint. Covers tool schema validation, content persistence, tag filtering, truncation behavior, and the API's ability to enumerate stored sessions.
- [OCR Tool: Schema Validation, File Jail, and Provider Fallback Tests](./ocr-tool-schema-validation-file-jail-and-provider-fallback-tests.md) — Tests for OCRTool, a built-in agent tool that extracts text from images using OpenAI Vision or Tesseract as fallback. Validates the tool schema, enforces a file-system jail to prevent path traversal, and covers error paths for unsupported formats, oversized files, missing providers, and API failures.

### `agent-backends` — 3 article(s)

- [Deep Agents Tool Bridge: LangChain Integration and Graceful Degradation](./deep-agents-tool-bridge-langchain-integration-and-graceful-degradation.md) — These tests cover the `build_deep_agents_tools` bridge function which adapts PocketPaw tools for LangChain-based Deep Agents backends. The key behavior tested is that the function returns an empty list gracefully when `langchain-core` is not installed, and that the Deep Agents backend is not subject to the same tool exclusions as the Claude Agent SDK.
- [PocketPawCognitiveEngine: Bridging Soul Protocol Cognition to Agent Backends](./pocketpawcognitiveengine-bridging-soul-protocol-cognition-to-agent-backends.md) — `PocketPawCognitiveEngine` implements the Soul Protocol's `CognitiveEngine` interface, routing fact-extraction and reflection calls to PocketPaw's active agent backend or to a cheaper dedicated Anthropic model when configured. It enables the soul's cognitive layer to reuse the existing LLM infrastructure rather than requiring a separate AI provider setup.
- [Tool Bridge: Multi-Backend Tool Adaptation and Policy Enforcement](./tool-bridge-multi-backend-tool-adaptation-and-policy-enforcement.md) — The tool bridge (`agents/tool_bridge.py`) adapts PocketPaw's internal tool registry for multiple AI backend SDKs — OpenAI Agents, Google ADK, Claude Agent SDK, and others — while enforcing tool policies and generating compact markdown instructions. Tests cover backend-specific tool exclusions, graceful import failure handling, OpenAI schema normalization, invoke callback error handling, and policy-filtered instruction generation.

### `agent-capabilities` — 4 article(s)

- [Agent Skill Management Schemas](./agent-skill-management-schemas.md) — Defines the Pydantic models for PocketPaw's skill management API — listing installed skills, installing new ones from a GitHub source, and removing existing ones. The install schema enforces a minimum source format to prevent obviously malformed installation requests.
- [BrowserTool: Playwright-Powered Web Automation with Accessibility Snapshots](./browsertool-playwright-powered-web-automation-with-accessibility-snapshots.md) — `BrowserTool` exposes browser automation to PocketPaw's agent via a Playwright-backed session manager, using accessibility tree snapshots (rather than raw HTML or pixel screenshots) for element identification — a technique that dramatically reduces token usage while maintaining LLM-parseable page structure.
- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.

### `agent-delegation` — 2 article(s)

- [Agent Delegation: ExternalAgentDelegate and DelegateTool Tests](./agent-delegation-externalagentdelegate-and-delegatetool-tests.md) — This suite tests PocketPaw's agent delegation system, which allows a running agent to hand off subtasks to external CLI-based agents (initially Claude Code). It covers the DelegationResult dataclass, availability detection via PATH lookup, unknown agent rejection, and the DelegateTool's trust level, parameter schema, and execution paths including error handling for missing installations.
- [DelegateToClaudeCodeTool: Critical-Trust Agent-to-Agent Delegation via CLI](./delegatetoclaudecodetool-critical-trust-agent-to-agent-delegation-via-cli.md) — `DelegateToClaudeCodeTool` gives PocketPaw's agent the ability to hand off complex coding tasks to Claude Code CLI, the Anthropic autonomous coding agent. It is marked `critical` trust — the highest level in PocketPaw's trust hierarchy — because Claude Code has unrestricted filesystem, shell, and web access.

### `agent-events` — 1 article(s)

- [SSE System Event Schema](./sse-system-event-schema.md) — Defines the Pydantic model for system events delivered to clients over Server-Sent Events (SSE). This thin schema ensures every event has a typed identity and an optional payload, keeping the SSE stream contract explicit and validated.

### `agent-factory` — 1 article(s)

- [Paw Agent Factory: Wiring Soul, Bootstrap, and Tools Together](./paw-agent-factory-wiring-soul-bootstrap-and-tools-together.md) — get_paw_agent() is the single composition root for a fully configured paw agent: it loads PawConfig, awakens or births a Soul, wires SoulBridge and SoulBootstrapProvider, registers the four core soul tools, and returns a PawAgent dataclass ready for CLI commands. A startup guard raises a clear error if soul-protocol is not installed.

### `agent-identity` — 5 article(s)

- [Agent Identity File Schemas](./agent-identity-file-schemas.md) — Defines Pydantic models for reading and updating the five identity files that shape an agent's persona: identity, soul, style, instructions, and user context. The partial-update pattern on the save request ensures callers only need to supply the files they want to change.
- [Bootstrap Package Public API](./bootstrap-package-public-api.md) — The bootstrap package exposes the four core primitives needed to assemble an agent's initial context: the protocol contracts, the default file-based provider, the cloud-agent provider, and the context builder. Centralising these re-exports here keeps consumer imports clean and makes it easy to see what the subsystem offers at a glance.
- [Bootstrap Protocol — BootstrapContext and BootstrapProviderProtocol Contracts](./bootstrap-protocol-bootstrapcontext-and-bootstrapproviderprotocol-contracts.md) — This module defines the two core contracts of the bootstrap subsystem: BootstrapContext, a dataclass holding all fields that compose an agent's system prompt, and BootstrapProviderProtocol, a structural Protocol that any identity source must implement. The system prompt layout — instructions first, identity block last — is a deliberate positioning strategy to keep the model anchored on persona during long conversations.
- [Cloud Agent Bootstrap Provider](./cloud-agent-bootstrap-provider.md) — CloudAgentBootstrapProvider builds a BootstrapContext from a MongoDB agent config dict, enabling cloud-hosted agents to carry their own custom identity, persona, archetype, and values rather than inheriting the generic OSS defaults. It satisfies the same BootstrapProviderProtocol as the default file-based provider, so the rest of the runtime is unaware of which source is in use.
- [Default Bootstrap Provider — File-Based Agent Identity](./default-bootstrap-provider-file-based-agent-identity.md) — DefaultBootstrapProvider reads the agent's identity, soul, style, instructions, and user profile from markdown files in the PocketPaw config directory, caching each file's content keyed by modification time to avoid redundant disk reads. It is the standard provider for self-hosted and developer deployments where identity is managed as local configuration rather than a database record.

### `agent-integration` — 3 article(s)

- [Agent-Facing Pocket Context Fetcher](./agent-facing-pocket-context-fetcher.md) — `fetch_pocket_for_agent` retrieves a pocket document and strips sensitive fields before returning it as a compact JSON-safe dict for injection into AI agent tool responses. The function follows a defensive result-envelope pattern, always returning `{"ok": bool, ...}` rather than raising exceptions, so agent tool handlers can branch on success without try/except.
- [Pocket Journal SSE Stream for RippleGraphWidget](./pocket-journal-sse-stream-for-ripplegraphwidget.md) — The journal stream router serves a live Server-Sent Events feed of soul-protocol journal entries filtered to a single pocket, enabling the frontend RippleGraphWidget to render agent decisions, retrievals, and tool traces as a live causation graph. It supports resumable cursors, cold-start backlog replay, and keepalive heartbeats to survive proxy timeouts.
- [Tools CLI Dispatcher: Bash-Accessible Tool Invocation for Agent Loops](./tools-cli-dispatcher-bash-accessible-tool-invocation-for-agent-loops.md) — The `pocketpaw.tools.cli` module is a CLI dispatcher that lets any builtin tool be invoked from a Bash shell via `python -m pocketpaw.tools.cli <tool_name> '<json_args>'`. It serves as the bridge between shell-based agent loops (like Claude Code) and PocketPaw's async Python tool ecosystem.

### `agent-memory` — 2 article(s)

- [Chat Session Document for Pocket and Group Contexts](./chat-session-document-for-pocket-and-group-contexts.md) — The `Session` document stores chat session metadata shared between one-on-one pocket conversations and multi-user group chats, using a `context_type` discriminator validated on construction. It tracks message counts, last activity, and soft deletion, with four compound indexes covering the dominant query patterns.
- [Unified Message Document for Group Chat and Agent Memory](./unified-message-document-for-group-chat-and-agent-memory.md) — The `Message` model stores two structurally different record types — multi-user group chat rows and single-agent LLM memory rows — in one MongoDB collection, discriminated by a `context_type` field. A model validator enforces strict field invariants per context type, preventing cross-contamination of group chat metadata into agent history and vice versa.

### `agent-orchestration` — 3 article(s)

- [Mission Control Data Models](./mission-control-data-models.md) — This module defines all core dataclasses and enums for Mission Control covering agent profiles, tasks, messages, activities, documents, and notifications. The models use Python dataclasses with ISO 8601 timestamps and StrEnum statuses, and have grown through two major revisions to support Deep Work v2 autonomous execution fields.
- [Mission Control Heartbeat Daemon](./mission-control-heartbeat-daemon.md) — The HeartbeatDaemon is a background APScheduler-driven daemon that periodically wakes every registered agent to check for pending work, update status, and fire optional event callbacks. It uses staggered agent wakeups and a singleton pattern to ensure only one daemon runs per process.
- [Mission Control Manager: High-Level Orchestration Layer](./mission-control-manager-high-level-orchestration-layer.md) — MissionControlManager sits above the raw file store and combines storage operations with business logic: creating tasks with automatic activity logging, resolving @mention notifications, managing agent heartbeats, and orchestrating project lifecycle including on-disk directory creation.

### `agent-protocol` — 1 article(s)

- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.

### `agent-runtime` — 17 article(s)

- [A2A Protocol Server — Agent-to-Agent HTTP Endpoint for PocketPaw](./a2a-protocol-server-agent-to-agent-http-endpoint-for-pocketpaw.md) — Implements the Agent-to-Agent (A2A) protocol server that exposes PocketPaw as an interoperable AI agent over HTTP. It provides a JSON-RPC 2.0 dispatcher, an Agent Card capability manifest, task submission endpoints, SSE streaming, and an in-memory task store with TTL-based expiry.
- [Agent Pool — On-Demand Agent Instance Management with Idle Eviction](./agent-pool-on-demand-agent-instance-management-with-idle-eviction.md) — Implements `AgentPool`, a lifecycle manager for cloud-deployed agent instances. Each agent gets its own `AgentBackend`, `SoulManager`, and isolated memory namespace. Instances are cached for reuse and evicted after configurable idle timeout (default 5 minutes) to bound memory usage.
- [Agent Runtime Status Schemas](./agent-runtime-status-schemas.md) — Defines the Pydantic models for PocketPaw's agent status API, covering per-session execution states and global runtime health. The session model captures granular execution phase (thinking, tool running, streaming) alongside token usage and duration, giving operators deep visibility into what the agent is doing at any moment.
- [AgentBackend Protocol — Unified Interface for All Agent SDK Adapters](./agentbackend-protocol-unified-interface-for-all-agent-sdk-adapters.md) — Defines the structural `Protocol` and supporting types that every agent backend (Claude SDK, OpenAI Agents, Codex CLI, Google ADK, etc.) must satisfy. The contract enforces a consistent streaming `run()` generator and a static `info()` method so the router and loop can treat all backends polymorphically.
- [Agents Package Initializer — AgentRouter Public Surface](./agents-package-initializer-agentrouter-public-surface.md) — The `pocketpaw.agents` package init re-exports `AgentRouter` as the sole public symbol, establishing a clean import boundary for the agents subsystem. Consumers import the router without knowing its internal module location.
- [Claude Agent SDK Backend — Recommended Default Agent Runtime](./claude-agent-sdk-backend-recommended-default-agent-runtime.md) — Implements `ClaudeSDKBackend`, PocketPaw's primary agent backend that wraps Anthropic's `claude-agent-sdk`. It provides built-in tools (Bash, Read, Write, Edit, Glob, Grep, WebSearch, WebFetch), streaming token delivery, a `PreToolUse` security hook, MCP server support, and headless permission bypass for messaging channels.
- [Codex CLI Backend — Subprocess Wrapper for OpenAI's Terminal Agent](./codex-cli-backend-subprocess-wrapper-for-openais-terminal-agent.md) — Implements `CodexCLIBackend`, which spawns OpenAI's `codex` CLI as an async subprocess and parses its NDJSON event stream. Prompts are passed via stdin rather than as command-line arguments to avoid OS argument-length limits, and a 10 MiB subprocess buffer accommodates large MCP tool results.
- [Deep Agents Backend — LangChain/LangGraph Agent Framework Integration](./deep-agents-backend-langchainlanggraph-agent-framework-integration.md) — Implements `DeepAgentsBackend`, which runs agents via the `deepagents` SDK built on LangChain and LangGraph. It provides durable execution, built-in planning and filesystem tools, multi-provider LLM routing, MCP tool integration, and streaming via LangGraph's event stream.
- [External Agent Delegation — Subprocess Execution of CLI Agents](./external-agent-delegation-subprocess-execution-of-cli-agents.md) — Provides `ExternalAgentDelegate`, a utility for dispatching tasks to external CLI agents (currently only Claude Code CLI) via `asyncio` subprocesses. It captures structured JSON output and surfaces it as a `DelegationResult`, enabling PocketPaw to use external agents as heavyweight tools.
- [GitHub Copilot SDK Backend — Copilot CLI Agent Integration](./github-copilot-sdk-backend-copilot-cli-agent-integration.md) — Implements `CopilotSDKBackend`, which wraps the `github-copilot-sdk` Python package to run GitHub Copilot as a PocketPaw agent. The SDK communicates with the `copilot` CLI via JSON-RPC internally, providing streaming, tool use, and multi-provider support (OpenAI, Azure, Anthropic, LiteLLM).
- [Google ADK Backend — Gemini Agent Integration via Google Agent Development Kit](./google-adk-backend-gemini-agent-integration-via-google-agent-development-kit.md) — Implements `GoogleADKBackend`, which runs Gemini-powered agents using the official `google-adk` Python SDK. It supports built-in tools (Google Search, code execution), MCP toolset integration via both stdio and SSE transports, custom PocketPaw function tools, and streaming via the ADK's `InMemoryRunner`.
- [OpenAI Agents SDK Backend — GPT and Ollama Agent Support with SQLite Session Persistence](./openai-agents-sdk-backend-gpt-and-ollama-agent-support-with-sqlite-session-persi.md) — Implements `OpenAIAgentsBackend` using OpenAI's `openai-agents` SDK, supporting GPT models and any OpenAI-compatible endpoint (Ollama, OpenRouter, LiteLLM). Sessions are persisted to a SQLite database so multi-turn conversations survive process restarts, unlike most other backends.
- [OpenCode Backend — REST API Client for a Running OpenCode Server](./opencode-backend-rest-api-client-for-a-running-opencode-server.md) — Implements `OpenCodeBackend`, which communicates with a locally running OpenCode server (`opencode --server`) via its REST API using `httpx`. Sessions are mapped from PocketPaw session keys to OpenCode session IDs, and streaming responses are parsed from NDJSON parts.
- [Plan Mode — Human-in-the-Loop Approval Flow for Agent Tool Execution](./plan-mode-human-in-the-loop-approval-flow-for-agent-tool-execution.md) — Implements `PlanManager` and `ExecutionPlan`, which intercept agent tool calls and hold them pending user approval before execution. Plans have a per-session lifecycle with timeout-based auto-rejection, providing an auditable gate between agent intent and system-modifying actions.
- [Smart Model Router — Heuristic Classifier for Automatic Model Tier Selection](./smart-model-router-heuristic-classifier-for-automatic-model-tier-selection.md) — Implements `ModelRouter`, a zero-API-call heuristic classifier that maps each incoming message to a `TaskComplexity` tier (SIMPLE, MODERATE, COMPLEX) and selects the appropriate model (Haiku, Sonnet, Opus). Pattern matching prevents false-positive simple classifications for messages that require tools or deep reasoning.
- [Telegram Bot Gateway — Agent Runtime Entry Point](./telegram-bot-gateway-agent-runtime-entry-point.md) — bot_gateway.py wires the three core runtime components — MessageBus, TelegramAdapter, and AgentLoop — into a running process for Telegram-based deployments. It handles clean shutdown on cancellation and is the primary entry point when running PocketPaw as a Telegram bot.
- [Unified Agent Loop — Core Orchestrator for Message Processing and Agent Execution](./unified-agent-loop-core-orchestrator-for-message-processing-and-agent-execution.md) — Implements `AgentLoop`, the central orchestrator that consumes messages from the bus, routes them through `AgentRouter` to the configured backend, applies security scanning and PII redaction, stores memories, and streams responses back to channels. It also manages the soul's cognitive pipeline and per-session concurrency locks.

### `agent-safety` — 1 article(s)

- [ShellTool: Sandboxed Shell Command Execution with Guardian Approval](./shelltool-sandboxed-shell-command-execution-with-guardian-approval.md) — `ShellTool` wraps `asyncio.subprocess` with two security layers — a precompiled dangerous-pattern regex rail and a Guardian Agent check — before executing any shell command. It operates at `critical` trust level, the highest in PocketPaw's trust hierarchy, and defaults to a jailed working directory.

### `agent-tools` — 4 article(s)

- [Accessibility Tree Snapshot Generator — Semantic Browser State for LLMs](./accessibility-tree-snapshot-generator-semantic-browser-state-for-llms.md) — snapshot.py converts Playwright's raw accessibility tree into a compact, indented text representation with [ref=N] markers that an LLM can read to understand page structure and reference specific elements for interaction. RefMap maintains the mapping from integer references back to element selectors, and SnapshotGenerator handles recursive tree traversal, role filtering, name truncation, and selector generation.
- [Browser Automation Module Public API](./browser-automation-module-public-api.md) — The browser package re-exports the public surface of PocketPaw's Playwright-based browser automation subsystem: snapshot types (RefMap, AccessibilityNode, SnapshotGenerator), the low-level driver (BrowserDriver, NavigationResult), and the session management layer (BrowserSession, BrowserSessionManager, get_browser_session_manager). These exports give agent tools a single import point for all browser interaction primitives.
- [Critical Gap Coverage: Real HTTP in DirectRESTAdapter and Agent Tools for Fabric and Instinct](./critical-gap-coverage-real-http-in-directrestadapter-and-agent-tools-for-fabric-.md) — This test file was written specifically to close gaps in test coverage identified during a gap analysis — areas where code existed but no test exercised it end-to-end. It covers real HTTP dispatch through `DirectRESTAdapter`, the Fabric agent tools (`FabricQueryTool`, `FabricCreateTool`), and the Instinct agent tools (`InstinctProposeTool`, `InstinctPendingTool`, `InstinctAuditTool`).
- [Playwright Browser Driver — LLM-Oriented Browser Control](./playwright-browser-driver-llm-oriented-browser-control.md) — BrowserDriver wraps Playwright's async API into a simplified interface designed for LLM-driven browser automation, where the agent reads accessibility tree snapshots instead of raw HTML and interacts via stable integer reference numbers. It handles Playwright import validation at construction time, auto-installs Chromium if no browser binary is found, and prefers the system's installed Chrome to avoid downloading a separate Chromium build.

### `agents` — 21 article(s)

- [Agent Document — Configuration, Soul Integration, and Scope Assignment](./agent-document-configuration-soul-integration-and-scope-assignment.md) — The Beanie ODM document representing an agent's persistent configuration in MongoDB — covering backend selection, system prompt, tool allowlist, trust level, soul personality settings, and hierarchical scope assignments. This document captures agent identity and configuration, not execution state.
- [Agent Domain Schema Validation Tests](./agent-domain-schema-validation-tests.md) — This module comprehensively tests the Pydantic schemas for the agents domain — `CreateAgentRequest`, `UpdateAgentRequest`, `DiscoverRequest`, `AgentResponse`, `ScopeAssignmentRequest`, and `ScopeAssignmentResponse` — covering required fields, defaults, length constraints, visibility validation, and scope normalization. Scope-related tests were added in the `feat/cluster-d-agent-scope-picker` feature.
- [Agent Knowledge Service — kb-go Binary Wrapper](./agent-knowledge-service-kb-go-binary-wrapper.md) — `knowledge.py` is a Python service that gives each agent a private knowledge base by delegating to the `kb` Go binary for compilation, indexing, and search, while handling heavyweight extraction (PDF, DOCX, URL, images) in Python before piping the plain text to `kb` via stdin.
- [Agent Protocol: Core Event Types and Legacy Interface](./agent-protocol-core-event-types-and-legacy-interface.md) — Defines the standardized `AgentEvent` dataclass that all agent backends emit, and the `AgentProtocol` structural typing interface that existed before the more capable `AgentBackend` ABC replaced it. Together they form the lingua franca for streaming agent output across PocketPaw's multi-backend architecture.
- [Agent Router: Registry-Based Backend Selection with Fallback](./agent-router-registry-based-backend-selection-with-fallback.md) — `AgentRouter` is the runtime dispatcher that resolves a configured backend name through the registry, instantiates it, and routes all agent calls through it. It supports a user-configurable ordered list of fallback backends so that a misconfigured or unavailable primary backend degrades gracefully rather than crashing.
- [Agent Scope Rules: Normalization and Assignment Authorization Tests](./agent-scope-rules-normalization-and-assignment-authorization-tests.md) — This module tests `ee.cloud.agents.scope_rules`, covering the `normalise_and_validate_scopes` function (grammar enforcement, lowercasing, deduplication, forbidden-wildcard rejection) and the `admin_can_assign_scopes` function (containment check that prevents scope-narrowed admins from assigning scopes outside their own grant).
- [Agent Scope Validation and Assignment Rules](./agent-scope-validation-and-assignment-rules.md) — `scope_rules.py` implements the server-side grammar and authorisation rules for hierarchical scope tags — normalising, validating, and deduplicating scope strings, and checking whether an admin's own scope grant covers the scopes they attempt to assign to an agent.
- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.
- [Agent Soul Configuration and OCEAN Personality Schema Tests](./agent-soul-configuration-and-ocean-personality-schema-tests.md) — This module tests the `AgentConfig` model's soul-related fields — default values, persona assignment, custom OCEAN personality vectors, and absence of a `soul_path` attribute — and verifies that `CreateAgentRequest` correctly propagates `persona` and soul OCEAN customization fields.
- [Agents Domain Business Logic Service](./agents-domain-business-logic-service.md) — The `AgentService` is the stateless business logic layer for the agents domain, handling agent creation (with slug uniqueness enforcement and eager soul materialisation), updates, discovery, and the new scope get/set operations introduced for the ScopePicker feature.
- [Agents Domain FastAPI Router](./agents-domain-fastapi-router.md) — The agents router exposes the full HTTP API surface for managing AI agents within a workspace — CRUD, knowledge ingestion, profile picture upload, backend discovery, and scope assignment — with license gating and permission checks applied at the router and dependency level.
- [Agents Domain Pydantic Schemas](./agents-domain-pydantic-schemas.md) — The agents schemas module defines the Pydantic request and response models for creating, updating, and querying agents, including server-side scope normalisation validators that enforce the same grammar rules as the frontend ScopePicker regardless of how the API is called.
- [AGENTS.md Loader: Git-Aware File Discovery with Mtime Caching](./agentsmd-loader-git-aware-file-discovery-with-mtime-caching.md) — `AgentsMdLoader` walks the directory tree upward from a starting path — stopping at git repository roots — to find the nearest AGENTS.md file, parses it into a structured `AgentsMd` object, and caches results by `(path, mtime)` so repeated calls within a session cost nothing. A 32 KiB file size cap prevents oversized AGENTS.md files from bloating the agent's system prompt.
- [AGENTS.md Support Package: Standardized Agent Capability Declaration](./agentsmd-support-package-standardized-agent-capability-declaration.md) — The `agents_md` package implements support for the Anthropic AGENTS.md specification — a convention where repositories publish a markdown file describing constraints, capabilities, and instructions for AI agents operating on that codebase. PocketPaw both reads AGENTS.md from target repositories (to inject constraints into the agent system prompt) and publishes its own AGENTS.md for other agents to consume.
- [Backend Registry: Lazy Discovery and Plugin Support for Agent Backends](./backend-registry-lazy-discovery-and-plugin-support-for-agent-backends.md) — The backend registry maps short backend names to `(module_path, class_name)` pairs and imports them on demand, so PocketPaw starts cleanly even when optional SDKs like `openai-agents` or `google-adk` are not installed. It also handles legacy backend name migration and exposes a plugin registration API.
- [Cloud Agents Domain Package Entry Point](./cloud-agents-domain-package-entry-point.md) — The `ee/cloud/agents/__init__.py` is a minimal shim that re-exports the agents FastAPI router, making the domain's public surface importable from `ee.cloud.agents` without callers needing to know the internal module structure.
- [Group Document — Multi-User Channels with AI Agent Participants](./group-document-multi-user-channels-with-ai-agent-participants.md) — The Beanie ODM document for chat groups and channels — workspace-scoped rooms that mix human members with AI agents. Models Slack-style channel semantics with a role/permission layer, agent respond modes, and pinned message support.
- [In-Process MCP Binding: Delivering Pocket Context to the Claude Agent SDK](./in-process-mcp-binding-delivering-pocket-context-to-the-claude-agent-sdk.md) — This module registers an in-process MCP server named `pocketpaw_pocket` that exposes a `get_pocket` tool, working around a Windows `CreateProcess` command-line length limit of ~32KB that would be exceeded if pocket documents were embedded directly in the system prompt. The actual pocket data is fetched lazily from `ee/cloud/pockets/agent_context.py` only when the agent calls the tool.
- [Plan Mode API — Human Approval Gate for Agent Actions](./plan-mode-api-human-approval-gate-for-agent-actions.md) — The plan mode router provides two endpoints that let users approve or reject an agent's proposed execution plan before any actions are taken. This human-in-the-loop gate prevents agents from running irreversible operations without explicit confirmation.
- [Pocket Chat Router — Context-Aware Workspace Creation and Interaction](./pocket-chat-router-context-aware-workspace-creation-and-interaction.md) — The pockets router is PocketPaw's dedicated endpoint for creating and interacting with pocket workspaces — themed dashboards of widgets, charts, and data panels. It streams responses via SSE, injects dynamic Ripple widget documentation for new pockets, and normalises three distinct AI output formats into render-ready Ripple specs.
- [Tool Bridge: Adapting PocketPaw Tools for Multiple Agent SDK Formats](./tool-bridge-adapting-pocketpaw-tools-for-multiple-agent-sdk-formats.md) — The tool bridge discovers all registered PocketPaw tools and wraps them in the native function-tool format expected by each agent backend (OpenAI Agents SDK `FunctionTool`, Google ADK `FunctionTool`, LangChain `StructuredTool`). It enforces backend-aware exclusion rules — the Claude Agent SDK gets shell/FS/edit tools excluded because the CLI provides them natively — and scans tool outputs for injection attacks before returning results to the agent.

### `aggregation` — 1 article(s)

- [Workspace Knowledge Aggregator — Cross-Agent KB Merge](./workspace-knowledge-aggregator-cross-agent-kb-merge.md) — A pure-Python aggregation layer that merges the workspace-scoped KB with every per-agent KB inside a workspace into a single sorted, deduplicated article stream. Designed to be thin, injectable, and unit-testable without a live database or kb binary.

### `ai safety` — 2 article(s)

- [Guardian Agent: Secondary LLM-Based Security Filter for Dangerous Commands](./guardian-agent-secondary-llm-based-security-filter-for-dangerous-commands.md) — The Guardian Agent provides a second-opinion AI security layer that evaluates commands and actions against a SAFE/DANGEROUS classification before execution. It sits above the deterministic regex rails, handling ambiguous cases where pattern matching alone cannot determine intent.
- [Prompt Injection Scanner: Two-Tier Heuristic and LLM Detection](./prompt-injection-scanner-two-tier-heuristic-and-llm-detection.md) — This module implements a two-tier prompt injection detection system: fast regex heuristics (~20 patterns) as Tier 1 for known attack signatures, and an optional LLM-based deep scan as Tier 2 for novel or obfuscated injections. It normalizes Unicode before scanning to defeat homoglyph substitution attacks.

### `ai-backend` — 1 article(s)

- [PocketPaw Compiler Backend Adapter for Knowledge Base](./pocketpaw-compiler-backend-adapter-for-knowledge-base.md) — Implements `PocketPawCompilerBackend`, a `CompilerBackend` Protocol adapter that makes PocketPaw's active agent LLM backend available to the `knowledge_base` package's compiler pipeline. This enables KB article compilation to use whatever AI backend is configured in the agent registry -- Claude, OpenAI, or any other -- without hardcoding a specific model.

### `ai-backends` — 1 article(s)

- [Memory Entry and Settings Schemas](./memory-entry-and-settings-schemas.md) — Defines the Pydantic models for PocketPaw's memory system — a single memory entry with tagging support, and a configuration response that exposes the pluggable memory backend settings including optional mem0 AI-inference configuration. The settings model reflects the system's multi-backend memory architecture.

### `api` — 34 article(s)

- [A2A Protocol Server — Agent-to-Agent HTTP Endpoint for PocketPaw](./a2a-protocol-server-agent-to-agent-http-endpoint-for-pocketpaw.md) — Implements the Agent-to-Agent (A2A) protocol server that exposes PocketPaw as an interoperable AI agent over HTTP. It provides a JSON-RPC 2.0 dispatcher, an Agent Card capability manifest, task submission endpoints, SSE streaming, and an in-memory task store with TTL-based expiry.
- [API Authorization Dependency: Fail-Closed Scope Enforcement](./api-authorization-dependency-fail-closed-scope-enforcement.md) — `require_scope` is a FastAPI dependency that enforces fail-closed authorization across all API endpoints. It accepts requests only when the caller has been explicitly marked as fully trusted, carries an API key with the required scope, or carries an OAuth2 token with the required scope — rejecting all other requests with HTTP 403.
- [API Key Manager: Secure Key Lifecycle with Hash-Only Storage](./api-key-manager-secure-key-lifecycle-with-hash-only-storage.md) — `APIKeyManager` provides create, verify, revoke, rotate, and list operations for API keys using a GitHub PAT-style design: keys are shown once at creation and only their SHA-256 hash is persisted. Keys use the `pp_` prefix for log identification, support fine-grained scopes, and are stored in `~/.pocketpaw/api_keys.json`.
- [API Layer Package: Versioned REST Endpoints for External Clients](./api-layer-package-versioned-rest-endpoints-for-external-clients.md) — The `api` package provides a versioned REST API layer (`/api/v1/`) alongside backward-compatible aliases at `/api/`, designed for external clients such as the Tauri desktop application and automation scripts. It mounts alongside the dashboard routes rather than replacing them, enabling both UI and programmatic access to the same PocketPaw instance.
- [API Serve: Headless FastAPI Application for External Clients](./api-serve-headless-fastapi-application-for-external-clients.md) — `create_api_app()` builds a standalone FastAPI application with `v1` REST routers, a `/ws` WebSocket endpoint, and full backend infrastructure (message bus, agent loop, scheduler) — but no dashboard frontend assets. This enables the `pocketpaw serve` command to power external clients like the Tauri desktop app or scripts without serving any HTML.
- [Canonical Runtime Audit Router with Full-Text Search](./canonical-runtime-audit-router-with-full-text-search.md) — The authoritative audit query endpoint at `/api/v1/runtime/audit`, consolidating two legacy paths behind a single surface that adds workspace-level rollup filtering and bound-parameter full-text search. SQL injection is explicitly prevented by using parameterized LIKE queries, verified by a dedicated regression test.
- [Decision Trace Wiring: Store-Level and Router-Level Integration Tests for Trace Persistence and Hydration](./decision-trace-wiring-store-level-and-router-level-integration-tests-for-trace-p.md) — Integration tests that verify `ReasoningTrace` objects are persisted into audit context when `propose()` is called, that `FabricObjectSnapshot` records are keyed to the correct audit row, and that the `/instinct/audit/{id}` hydration endpoint expands trace IDs into full records at the correct depth level.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Enterprise Automations Rule CRUD: Store and FastAPI Router Tests](./enterprise-automations-rule-crud-store-and-fastapi-router-tests.md) — Full test coverage for the enterprise `AutomationStore` and its FastAPI router, covering three rule types (threshold, schedule, data change), all CRUD operations, toggle/enable behavior, fire tracking, file persistence, and a complete lifecycle end-to-end test. Every test uses `tmp_path` isolation to avoid touching the user's real `~/.pocketpaw` configuration.
- [Fabric List Endpoints: Object and Link Filtering, Envelope Shape, and SQL Injection Safety Tests](./fabric-list-endpoints-object-and-link-filtering-envelope-shape-and-sql-injection.md) — Integration tests for the `/fabric/objects` and `/fabric/links` list endpoints added as part of the PocketDataPanel's Objects/Links sub-tab wiring. Covers store-level link filtering by type and source, HTTP envelope shape, type-based filtering, pagination parameter validation, and an explicit SQL injection safety test for the `link_type` parameter.
- [FastAPI Router for Retrieval Logs and Graduation Endpoints](./fastapi-router-for-retrieval-logs-and-graduation-endpoints.md) — Exposes the retrieval projection and graduation policy through a FastAPI router, providing read endpoints for recent retrievals and graduation state, plus an action endpoint that triggers a graduation scan and applies decisions. The store is cached per journal instance so the projection is not rebuilt on every request.
- [FastAPI Router for Widget Interactions, Graduation, and Co-occurrence](./fastapi-router-for-widget-interactions-graduation-and-co-occurrence.md) — Exposes the widget journal projection through REST endpoints for reading widget usage stats, graduation state, and co-occurrence suggestions, plus write endpoints for tracking interactions and recording accept/dismiss decisions on suggested widget pairings. The POST /widgets/track endpoint closes a silent data-loss bug where every dashboard interaction 404'd before this change.
- [File Uploads Router with Signed URL Access](./file-uploads-router-with-signed-url-access.md) — Implements the OSS single-user file upload system with POST (single and bulk), streaming GET with signed URL authorization, and DELETE endpoints. Uploaded files are stored under `~/.pocketpaw/uploads/` with a JSONL metadata index, and downloads are gated by short-lived HMAC-signed tokens to prevent unauthorized access.
- [Files API Router: Unified Listing and v2 Tree/Browse Endpoints](./files-api-router-unified-listing-and-v2-treebrowse-endpoints.md) — Defines the FastAPI router for the EE `/files` endpoints, including the legacy `GET /files` unified listing endpoint and the v2 tree/browse API built on `ProviderRegistry`. The router factory `build_router` composes the registry, ABAC rules, and request-context factory into a single mountable router.
- [Files Module Public Pydantic Schemas](./files-module-public-pydantic-schemas.md) — Defines the canonical Pydantic data models shared across the entire files subsystem: `Permission`, `RequestContext`, `FileEntry`, `FolderNode`, `MountConfig`, `ResolvedMount`, `Page`, and `SearchQuery`. Validators on these models enforce invariants (absolute paths, namespaced IDs) that providers cannot violate, ensuring the API response shape is consistent regardless of which provider produced it.
- [Files Module Typed Exception Hierarchy](./files-module-typed-exception-hierarchy.md) — Defines the complete typed error hierarchy for PocketPaw's cloud files subsystem. Every exception carries a machine-readable `code` string and a matching HTTP status code so the FastAPI router can translate domain failures into consistent API responses without ad-hoc status mapping.
- [Legacy Audit REST Router (Deprecated)](./legacy-audit-rest-router-deprecated.md) — The original `/api/v1/audit` endpoint is kept as a backwards-compatibility alias that forwards to the canonical `runtime_router` implementation. All responses include `Deprecation: true` and a `Link` header pointing callers to the successor path, enabling a graceful migration without breaking existing integrations.
- [Live Cloud API Smoke Tests with Backend Reachability Guard](./live-cloud-api-smoke-tests-with-backend-reachability-guard.md) — Smoke tests that run real HTTP calls against a locally running `paw-cloud` backend. The entire module skips automatically when the backend is unreachable, making these safe to include in CI without a live server requirement. Tests cover login, profile retrieval, agent listing, room lifecycle, DMs, and unauthenticated rejection.
- [Notifications REST API Router](./notifications-rest-api-router.md) — The notifications router exposes three endpoints for listing, marking read, and bulk-clearing a user's in-app notifications, with authentication enforced via `current_active_user` dependency injection. All mutation operations are scoped strictly to the authenticated user to prevent cross-user data access.
- [OAuth2 Authorization Server: PKCE Flow Implementation](./oauth2-authorization-server-pkce-flow-implementation.md) — `AuthorizationServer` implements the full OAuth2 authorization code + PKCE flow: issuing authorization codes, exchanging them for tokens via S256 challenge verification, refreshing tokens, and revoking tokens. Token lifetimes are 1-hour access / 30-day refresh / 10-minute authorization code.
- [OAuth2 Data Models: Client, Authorization Code, and Token](./oauth2-data-models-client-authorization-code-and-token.md) — Defines the three core data structures for PocketPaw's OAuth2 implementation: `OAuthClient` (registered application with redirect URI validation), `AuthorizationCode` (short-lived PKCE exchange token), and `OAuthToken` (access + refresh token pair). The redirect URI matching logic implements RFC 8252 Section 7.3 for native app loopback flexibility.
- [OAuth2 Storage: In-Memory Auth Codes and File-Persisted Tokens](./oauth2-storage-in-memory-auth-codes-and-file-persisted-tokens.md) — `OAuthStorage` uses a two-tier storage strategy: authorization codes live in memory only (ephemeral, 10-minute TTL), while access and refresh tokens are persisted to `~/.pocketpaw/oauth_tokens.json` so authenticated desktop sessions survive server restarts. A pre-registered `pocketpaw-desktop` client with Tauri-compatible redirect URIs is always available.
- [OAuth2/PKCE Authorization Server Package](./oauth2pkce-authorization-server-package.md) — The `oauth2` package implements a self-contained OAuth2 authorization server with PKCE support, designed primarily for the PocketPaw Tauri desktop application to authenticate against a locally running PocketPaw instance without exposing a client secret. The package is organized into models, server logic, and storage layers.
- [Output Redaction: Preventing Secret Leakage in Agent Responses](./output-redaction-preventing-secret-leakage-in-agent-responses.md) — This module provides output-level redaction that strips API keys, tokens, and other credentials from agent response text before it reaches clients. Operating at the message bus level makes it backend-agnostic — the same redaction applies regardless of whether the agent backend is Claude Code, Open Interpreter, or any future addition.
- [Session Management REST Router](./session-management-rest-router.md) — Provides the REST endpoints for creating, listing, deleting, renaming, searching, and exporting conversation sessions. Session IDs are UUID-based slugs prefixed with `websocket_` to maintain compatibility with the WebSocket session key format used by the memory subsystem.
- [Settings REST Router with Security Field Filtering](./settings-rest-router-with-security-field-filtering.md) — Exposes GET and PUT REST endpoints for reading and updating PocketPaw's runtime configuration, with two hard-coded protection layers: secrets are never returned over the wire, and a set of security-critical fields cannot be modified via the API at all. An asyncio lock prevents concurrent write races.
- [SkillExecutor: Streaming Skill Execution Through the Agent Backend](./skillexecutor-streaming-skill-execution-through-the-agent-backend.md) — The `SkillExecutor` bridges skill definitions (Markdown files) and the agent backend (Open Interpreter or Claude Code) by building a prompt from skill content and user arguments, then streaming the agent's response back as an async iterator of result dicts. It is the runtime engine that transforms a loaded skill into an executed task.
- [Skills Management REST Router](./skills-management-rest-router.md) — Provides REST endpoints for listing installed skills, searching the skills.sh marketplace, installing skills from GitHub sources, removing skills, and force-reloading the skill index. Skills are user-invocable slash commands that extend PocketPaw's capabilities without core code changes.
- [Soul Protocol REST API Endpoints](./soul-protocol-rest-api-endpoints.md) — Exposes the full Soul Protocol interface over REST, covering identity, OCEAN personality state, core memory editing, memory storage and recall, soul import/export, and rubric-based self-evaluation. All endpoints require the `settings:read` scope and safely handle the case where no soul is loaded.
- [Telegram Channel Router: Setup and Pairing Flow](./telegram-channel-router-setup-and-pairing-flow.md) — Implements the three REST endpoints that drive Telegram bot pairing: checking current configuration status, initiating the pairing flow with a bot token, and polling for pairing completion. The router wraps the shared pairing state dict from the dashboard module, adding token validation and graceful handling of missing optional dependencies.
- [Token-Bucket Rate Limiter: Stdlib-Only DoS Protection for the Web Dashboard](./token-bucket-rate-limiter-stdlib-only-dos-protection-for-the-web-dashboard.md) — This module implements an in-memory token-bucket rate limiter using only Python's standard library, providing pre-configured tiers for API endpoints, authentication, WebSocket connections, and per-API-key limiting. The zero-dependency design makes it suitable for embedding directly in the dashboard without adding external Redis or other state store requirements.
- [Tools Registry REST Endpoint](./tools-registry-rest-endpoint.md) — Provides a single GET endpoint that aggregates builtin tools, MCP tools, and tool groups into a unified discovery response. Lazy imports and broad exception handling ensure the endpoint returns partial results gracefully when the MCP manager or OAuth token store is not yet initialized.
- [URL Validators: SSRF Protection for Settings Fields](./url-validators-ssrf-protection-for-settings-fields.md) — This module provides a Pydantic validator (`validate_external_url`) that guards Settings URL fields against Server-Side Request Forgery by blocking internal, loopback, and private-range hostnames. An environment variable escape hatch allows operators to opt in to internal URLs for legitimate local deployments.
- [Webhook Slot Management Router](./webhook-slot-management-router.md) — Manages the lifecycle of PocketPaw's inbound webhook slots — creating named endpoints, listing them with dynamically constructed URLs, removing them, and rotating their HMAC secrets. All operations require the `admin` scope, reflecting that webhook configuration controls which external systems can trigger agent workflows.

### `api-models` — 1 article(s)

- [Automations Models — Rule, RuleType, ExecutionMode, and Request Models](./automations-models-rule-ruletype-executionmode-and-request-models.md) — This module defines the Pydantic data models for the EE automations engine: the `Rule` entity (the core automation record), `RuleType` and `ExecutionMode` enums, and `CreateRuleRequest` / `UpdateRuleRequest` for the API layer. The models cover three rule types (threshold, schedule, data_change) with three execution modes (require_approval, auto_execute, notify_only).

### `api-schemas` — 18 article(s)

- [Agent Identity File Schemas](./agent-identity-file-schemas.md) — Defines Pydantic models for reading and updating the five identity files that shape an agent's persona: identity, soul, style, instructions, and user context. The partial-update pattern on the save request ensures callers only need to supply the files they want to change.
- [Agent Runtime Status Schemas](./agent-runtime-status-schemas.md) — Defines the Pydantic models for PocketPaw's agent status API, covering per-session execution states and global runtime health. The session model captures granular execution phase (thinking, tool running, streaming) alongside token usage and duration, giving operators deep visibility into what the agent is doing at any moment.
- [Agent Skill Management Schemas](./agent-skill-management-schemas.md) — Defines the Pydantic models for PocketPaw's skill management API — listing installed skills, installing new ones from a GitHub source, and removing existing ones. The install schema enforces a minimum source format to prevent obviously malformed installation requests.
- [Conversation Session Schemas](./conversation-session-schemas.md) — Defines the Pydantic models for PocketPaw's conversation session API — listing, creating, renaming, and searching sessions. The search model captures both the matched content and the message role, enabling contextually rich search result previews in the dashboard.
- [File Browser and File Operation Schemas](./file-browser-and-file-operation-schemas.md) — Defines the Pydantic models for PocketPaw's file-browser API, covering directory listings, path navigation, recent-file tracking from agent tool use, and file-write requests. These schemas form the contract between the FastAPI backend and the dashboard's file explorer UI.
- [Generic Settings Read and Update Schemas](./generic-settings-read-and-update-schemas.md) — Defines the Pydantic models for PocketPaw's settings API, using a deliberately generic dict-based approach to handle the wide variety of configurable settings without requiring a separate schema for each. The design prioritises flexibility over strict typing, with secrets excluded from the read response.
- [Health and Security Audit Schemas](./health-and-security-audit-schemas.md) — Defines Pydantic models for PocketPaw's health-monitoring and security-audit APIs, covering runtime health summaries, error log entries, per-check security results, and self-audit report summaries. These schemas give operators a structured view into the agent's operational integrity.
- [Intention Scheduling Schemas](./intention-scheduling-schemas.md) — Defines the Pydantic models for PocketPaw's intention system — scheduled, trigger-driven agent tasks that run prompts automatically. The schema covers the full CRUD lifecycle plus manual trigger acknowledgement, with field-level validation to prevent empty or oversized intentions.
- [MCP Server Management Schemas](./mcp-server-management-schemas.md) — Defines the Pydantic request models for managing Model Context Protocol (MCP) servers in PocketPaw — adding servers by configuration, referencing them by name, test-connecting before saving, and installing from a curated preset library. These schemas expose the full MCP lifecycle through the REST API.
- [Memory Entry and Settings Schemas](./memory-entry-and-settings-schemas.md) — Defines the Pydantic models for PocketPaw's memory system — a single memory entry with tagging support, and a configuration response that exposes the pluggable memory backend settings including optional mem0 AI-inference configuration. The settings model reflects the system's multi-backend memory architecture.
- [Notification Response Schema](./notification-response-schema.md) — `NotificationResponse` is the Pydantic response model for the notifications API, defining the wire format returned to frontend clients. It normalizes MongoDB document fields into a consistent camelCase-compatible shape with string-typed timestamps for JSON serialization safety.
- [OAuth2 Token Exchange and Revocation Schemas](./oauth2-token-exchange-and-revocation-schemas.md) — Defines the Pydantic models for PocketPaw's OAuth2 token lifecycle — code exchange, token refresh, standard token responses, and revocation. Field-level validation enforces RFC 6749 grant type restrictions at the schema boundary, catching invalid requests before they reach the OAuth2 implementation.
- [Plan Mode Approval Schemas](./plan-mode-approval-schemas.md) — Defines the minimal Pydantic models for PocketPaw's plan-mode API — the human-in-the-loop gate where a user approves or rejects an agent's proposed plan before execution begins. The session key binds the approval action to a specific pending plan.
- [Reminder Management Schemas](./reminder-management-schemas.md) — Defines Pydantic models for PocketPaw's reminder system, which accepts natural-language reminder requests, parses them into scheduled entries, and delivers notifications when the trigger time arrives. The schema design keeps the creation API intentionally simple to support conversational input.
- [Remote Tunnel Status and Control Schemas](./remote-tunnel-status-and-control-schemas.md) — Defines the minimal Pydantic models for PocketPaw's remote tunnel API, which exposes the agent's local HTTP server to the internet via a secure tunnel. The two schemas cover tunnel status querying and tunnel start results, including error reporting for failed tunnel launches.
- [SSE System Event Schema](./sse-system-event-schema.md) — Defines the Pydantic model for system events delivered to clients over Server-Sent Events (SSE). This thin schema ensures every event has a typed identity and an optional payload, keeping the SSE stream contract explicit and validated.
- [Telegram Pairing Schema Contracts](./telegram-pairing-schema-contracts.md) — Defines the Pydantic request and response models used by the Telegram channel pairing flow. These schemas enforce a clean contract between the REST API layer and the bot token validation and QR code generation logic.
- [Webhook Slot Schema Contracts](./webhook-slot-schema-contracts.md) — Defines the Pydantic models that represent a webhook slot's configuration and the request payloads for creating or identifying webhooks. The name pattern constraint prevents injection-prone characters from reaching URL-based routing.

### `application lifecycle` — 1 article(s)

- [Singleton Lifecycle Management: Coordinated Shutdown and Test Reset](./singleton-lifecycle-management-coordinated-shutdown-and-test-reset.md) — The lifecycle module provides a central registry for singletons that need graceful shutdown on app teardown and deterministic reset between test runs. It decouples teardown orchestration from individual module implementations by letting each module register its own callbacks.

### `approval-flow` — 1 article(s)

- [Plan Mode — Human-in-the-Loop Approval Flow for Agent Tool Execution](./plan-mode-human-in-the-loop-approval-flow-for-agent-tool-execution.md) — Implements `PlanManager` and `ExecutionPlan`, which intercept agent tool calls and hold them pending user approval before execution. Plans have a per-session lifecycle with timeout-based auto-rejection, providing an auditable gate between agent intent and system-modifying actions.

### `architecture` — 44 article(s)

- [AgentBackend Protocol — Unified Interface for All Agent SDK Adapters](./agentbackend-protocol-unified-interface-for-all-agent-sdk-adapters.md) — Defines the structural `Protocol` and supporting types that every agent backend (Claude SDK, OpenAI Agents, Codex CLI, Google ADK, etc.) must satisfy. The contract enforces a consistent streaming `run()` generator and a static `info()` method so the router and loop can treat all backends polymorphically.
- [API Layer Package: Versioned REST Endpoints for External Clients](./api-layer-package-versioned-rest-endpoints-for-external-clients.md) — The `api` package provides a versioned REST API layer (`/api/v1/`) alongside backward-compatible aliases at `/api/`, designed for external clients such as the Tauri desktop application and automation scripts. It mounts alongside the dashboard routes rather than replacing them, enabling both UI and programmatic access to the same PocketPaw instance.
- [API v1 Schemas Package — Pydantic Schema Namespace](./api-v1-schemas-package-pydantic-schema-namespace.md) — The `schemas/__init__.py` file marks the `pocketpaw.api.v1.schemas` directory as a Python package. It contains no exports, serving purely as a namespace boundary that groups all Pydantic request and response models for the v1 API.
- [Auth Domain Package Re-Exports](./auth-domain-package-re-exports.md) — The auth domain `__init__.py` is a backward-compatibility shim that re-exports the entire public surface of `auth.core` and `auth.router` from the `ee.cloud.auth` namespace, so existing callers don't need to update their imports if the internal module structure changes.
- [BaseFolderProvider: Default Provider Stub Implementation](./basefolderprovider-default-provider-stub-implementation.md) — Defines `BaseFolderProvider`, the default base class for all file providers, where every operation raises `ProviderUnsupported` unless overridden. This design enforces capability-based provider contracts: concrete providers only implement the operations they actually support, and the error type communicates unsupported operations cleanly to the API layer.
- [Built-in File Providers Package Initialiser](./built-in-file-providers-package-initialiser.md) — The package initialiser for the built-in `FolderProvider` implementations under `ee.cloud.files.providers`. This file acts as a namespace anchor, grouping the concrete provider modules (`base`, `kb`, `uploads`) under a single importable package without adding its own logic.
- [Chat Domain Package Entrypoint](./chat-domain-package-entrypoint.md) — The `ee/cloud/chat/__init__.py` file is the public entrypoint for the chat domain, re-exporting the APIRouter so the application can mount the entire chat surface with a single import. It acts as a stable boundary declaration hiding internal module layout.
- [Chat Service - Backward-Compatibility Re-Export Facade](./chat-service-backward-compatibility-re-export-facade.md) — `service.py` is a thin facade that re-exports `GroupService`, `MessageService`, and their helper functions from their new dedicated modules, preserving import compatibility for all code written before the refactor. It carries no logic of its own.
- [Cloud Agents Domain Package Entry Point](./cloud-agents-domain-package-entry-point.md) — The `ee/cloud/agents/__init__.py` is a minimal shim that re-exports the agents FastAPI router, making the domain's public surface importable from `ee.cloud.agents` without callers needing to know the internal module structure.
- [Cloud DB Module - Backward-Compatible Database Shim](./cloud-db-module-backward-compatible-database-shim.md) — `ee/cloud/db.py` is a one-line backward-compatibility shim that re-exports the database lifecycle functions from the canonical `ee.cloud.shared.db` module. It exists solely to preserve older import paths after the database logic was consolidated into the shared package.
- [Cloud Document Models Registry — Beanie Initialization Manifest](./cloud-document-models-registry-beanie-initialization-manifest.md) — The central re-export module for all Beanie ODM documents in the enterprise cloud layer, providing a stable import surface and a lazy-loading `ALL_DOCUMENTS` list consumed by Beanie's init sequence. Special handling defers `FileUpload` and `FileFolder` imports to break circular import chains with the uploads subsystem.
- [Cloud Feature Flags - Always-On Overrides for the EE Product](./cloud-feature-flags-always-on-overrides-for-the-ee-product.md) — The `ee/cloud/features.py` module force-enables features that are optional in the OSS build but required for the cloud EE product to function correctly. Currently it overrides `chat_titles_enabled` to always return `True`, ensuring sidebar titles and realtime UI always work in cloud mode.
- [Cloud Memory Package — MongoMemoryStore Public API](./cloud-memory-package-mongomemorystore-public-api.md) — The `ee.cloud.memory` package's public interface, which re-exports `MongoMemoryStore` as the single entry point for MongoDB-backed agent memory in cloud deployments. The module is intentionally minimal, acting as a stable import boundary between the cloud layer and the rest of the application.
- [Common API Response Schemas — Shared Base Types](./common-api-response-schemas-shared-base-types.md) — The common schemas define PocketPaw's shared response envelope types: a base class with ORM compatibility, a standard error shape, a generic paginated list, and two convenience success models. These types appear across every domain in the v1 API.
- [ConnectorProtocol: The Interface Contract for All PocketPaw Data Source Adapters](./connectorprotocol-the-interface-contract-for-all-pocketpaw-data-source-adapters.md) — `protocol.py` defines the structural typing interface that all connector adapters must satisfy, along with the shared result dataclasses and enums they return. The addition of `IngestACL` and `IngestAdapter` in Move 7 ensures that documents pulled into Single Brain carry their source-side permissions, preventing data from a private Slack channel from becoming accessible to unauthorized users.
- [ConnectorRegistry: Dynamic Connector Discovery for YAML, SQL, MongoDB, Firebase, and GCP Adapters](./connectorregistry-dynamic-connector-discovery-for-yaml-sql-mongodb-firebase-and-.md) — `ConnectorRegistry` scans a directory for YAML connector definitions and also hard-registers native Python adapters (SQL databases, MongoDB, Firebase, GCP) that cannot be expressed as simple REST YAML. It manages per-pocket adapter instances and provides a `reload()` method for hot configuration changes.
- [DirectRESTAdapter and YAML Engine: Declarative REST Connector Definitions via YAML Files](./directrestadapter-and-yaml-engine-declarative-rest-connector-definitions-via-yam.md) — `yaml_engine.py` enables third-party REST API connectors to be defined entirely in YAML without writing Python code. The `DirectRESTAdapter` reads a `ConnectorDef` parsed from a YAML file and executes actions as HTTP requests via `httpx`, templating parameters from the agent's action invocation into the request body, headers, and URL.
- [Enterprise API Singleton Accessors](./enterprise-api-singleton-accessors.md) — `ee/api.py` provides module-level singleton factory functions for the two main enterprise data stores — `InstinctStore` and `PawPrintStore` — so that both agent tool code and domain routers can reach a shared, lazily-initialised database instance without dependency injection.
- [Enterprise Cloud Application Bootstrap and Router Mounting](./enterprise-cloud-application-bootstrap-and-router-mounting.md) — `ee/cloud/__init__.py` is the application assembly point for the PocketPaw Enterprise REST API. It provides two entry points: `init_realtime()` which wires up the in-process EventBus, and `mount_cloud()` which registers every domain router, the error handler, static file mounts, and lifecycle hooks onto a FastAPI app instance.
- [Enterprise Extensions (ee/) Package Overview](./enterprise-extensions-ee-package-overview.md) — The `ee/` package is the boundary layer separating PocketPaw's open-source core from its commercially-licensed Enterprise features. It groups capabilities like the instinct decision pipeline, audit/compliance logging, automations engine, and singleton API accessors under a single FSL 1.1 license gate.
- [Fabric Event Payload Types and Action Constants](./fabric-event-payload-types-and-action-constants.md) — This module pins the three canonical event shapes that drive Fabric's journal projection — object created, updated, and archived — as stable action name constants and payload builder functions. It was created specifically to replace PR #938's scope-filtering approach in the legacy SQLite store, which had two unfixable structural bugs: a schema migration gap and a pagination leak.
- [Fabric Module Entry Point — Ontology Layer Public API](./fabric-module-entry-point-ontology-layer-public-api.md) — The `ee/fabric/__init__.py` consolidates the public API of the Fabric ontology layer into a single import surface, exposing both the legacy SQLite store and the newer journal-backed path introduced in Wave 3. The `__all__` list is the canonical documentation of what is stable and intended for external use.
- [Files Bootstrap - Composing Providers, Mounts, and Rules into a Router](./files-bootstrap-composing-providers-mounts-and-rules-into-a-router.md) — `bootstrap.py` is the composition root for the files domain: it reads mount configuration, registers the built-in providers (uploads and knowledge base), loads ABAC rules, and returns a fully-configured `APIRouter` ready to be mounted by the application. It is the only place where concrete dependencies are wired together.
- [Files Domain Package - Unified File Browser and Provider Registry](./files-domain-package-unified-file-browser-and-provider-registry.md) — The `ee/cloud/files/__init__.py` package entrypoint assembles and re-exports the complete public API for the files domain: the provider registry, ABAC schema types, pagination primitives, and the router factory function. It is the single import surface consumers use to mount the files subsystem.
- [HealthEngine — Health Check Orchestrator and Error Store Integration](./healthengine-health-check-orchestrator-and-error-store-integration.md) — `HealthEngine` is PocketPaw's central health orchestration class, running startup config/storage checks synchronously and connectivity checks asynchronously, storing results in memory and routing check failures to the persistent `ErrorStore`. It is deliberately LLM-independent so the health system remains operational even when the AI backend is down.
- [In-Process Async Event Bus for Cloud Domain Communication](./in-process-async-event-bus-for-cloud-domain-communication.md) — This module implements a lightweight in-process pub/sub event bus that lets cloud domains react to events from other domains without direct imports. It is the backbone for cross-domain side effects in the PocketPaw EE layer, used by the agent bridge, event handlers, and any future domain that needs to observe another.
- [Knowledge Base Domain Package Initialiser](./knowledge-base-domain-package-initialiser.md) — The package initialiser for the `ee.cloud.kb` Knowledge Base domain, which exposes workspace-scoped KB endpoints including search, ingest, browse, lint, and stats. The comment header documents the package's scope and public surface, serving as the entry point for the KB subsystem.
- [Message Bus: Central Routing Hub for All Channel Communication](./message-bus-central-routing-hub-for-all-channel-communication.md) — MessageBus is the central publish-subscribe hub in PocketPaw that decouples channel adapters from the agent loop. Adapters publish inbound messages to a bounded asyncio queue; the agent loop consumes them. Outbound messages are fanned out to registered adapter callbacks. System events flow separately to dashboard and monitoring subscribers.
- [Mission Control Storage Protocol Interface](./mission-control-storage-protocol-interface.md) — MissionControlStoreProtocol defines the abstract interface all Mission Control storage backends must implement using Python's structural Protocol pattern with runtime_checkable, enabling dependency injection and future backend swappability across all six entity types.
- [MongoMemoryStore — Full MemoryStoreProtocol Implementation on MongoDB](./mongomemorystore-full-memorystoreprotocol-implementation-on-mongodb.md) — The production MongoDB implementation of PocketPaw's `MemoryStoreProtocol`, bridging the OSS memory interface to the cloud's Beanie ODM schema. SESSION entries map to the `messages` collection; LONG_TERM and DAILY facts map to `memory_facts`. The store also manages session metadata lifecycle and normalizes session key formats between the message bus and the database.
- [Notifications Domain Package](./notifications-domain-package.md) — The notifications `__init__.py` serves as the package boundary marker for PocketPaw's in-app notifications domain, grouping the service, router, and response schemas into a cohesive module. No symbols are re-exported; the package is imported by the application factory to register the router.
- [Paw Module Entry Point](./paw-module-entry-point.md) — The paw package's __init__.py exposes only PawConfig from its config submodule, deliberately keeping the import surface minimal to avoid pulling in soul-protocol as a hard dependency for users who install the base pocketpaw package without the soul extra.
- [PocketPaw Skills Module: Entry Point for the AgentSkills Ecosystem Integration](./pocketpaw-skills-module-entry-point-for-the-agentskills-ecosystem-integration.md) — The `skills` package `__init__.py` assembles and re-exports the three subsystems that make up PocketPaw's skill integration layer: loading, installation, and execution. By surfacing a clean public API here, consumers throughout the codebase can import skill primitives from a single location without knowing the internal module structure.
- [Pockets Domain Package](./pockets-domain-package.md) — The pockets `__init__.py` re-exports the `router` symbol from `ee.cloud.pockets.router`, making the router available as `ee.cloud.pockets.router` for registration by the application factory. This single-line re-export pattern keeps the application factory import simple while preserving module boundaries within the domain.
- [Proactive Daemon Module: Transforming PocketPaw from Reactive Chatbot to Intention-Driven Agent](./proactive-daemon-module-transforming-pocketpaw-from-reactive-chatbot-to-intentio.md) — The `pocketpaw.daemon` package turns PocketPaw from a request/response chatbot into a proactive agent that initiates actions based on user-defined intentions and event triggers. Its `__init__.py` assembles the five subsystems — `IntentionStore`, `TriggerEngine`, `ContextHub`, `IntentionExecutor`, and `ProactiveDaemon` — into a single importable surface.
- [Provider Adapter Registry: Centralized Adapter Lookup with Descriptive Errors](./provider-adapter-registry-centralized-adapter-lookup-with-descriptive-errors.md) — The provider registry is a module-level dict mapping provider name strings to singleton adapter instances, with a `get_adapter()` function that raises `KeyError` with the list of available providers on unknown names. Pre-instantiating adapters as singletons avoids repeated construction overhead and provides a single point of truth for which providers PocketPaw supports.
- [Security Rails: Single Source of Truth for Dangerous Command Patterns](./security-rails-single-source-of-truth-for-dangerous-command-patterns.md) — This module defines the canonical set of dangerous command patterns used across every security layer in PocketPaw — Guardian, ShellTool, native agent, and Claude SDK hooks all import from here. Centralizing patterns in one place eliminates the drift and inconsistency that would arise from each subsystem maintaining its own ad-hoc block list.
- [StorageAdapter Protocol: The Backend-Agnostic Interface for File Persistence](./storageadapter-protocol-the-backend-agnostic-interface-for-file-persistence.md) — The `StorageAdapter` protocol defines the minimal async interface that all storage backends (local disk, S3, GCS) must satisfy, keeping the upload pipeline decoupled from any specific storage technology. The companion `StoredObject` dataclass represents the canonical result of a successful write operation.
- [TimestampedDocument — Base Beanie Document with Automatic Timestamps](./timestampeddocument-base-beanie-document-with-automatic-timestamps.md) — A Beanie `Document` base class that automatically manages `createdAt` and `updatedAt` timestamps using Beanie's event hook system. All cloud ODM documents inherit from this class to get consistent, server-side timestamp management.
- [Tool Protocol and Base Class: The Contract Every PocketPaw Tool Implements](./tool-protocol-and-base-class-the-contract-every-pocketpaw-tool-implements.md) — The `protocol.py` module defines the structural contract that all PocketPaw tools must satisfy -- a `ToolDefinition` dataclass for LLM schema representation, a `ToolProtocol` for structural subtyping, and a `BaseTool` abstract class providing default implementations and helper methods. Cross-backend schema normalization is handled here to prevent silent failures with strict OpenAI-style validators.
- [Upload Error Hierarchy: Typed Exceptions for the Upload Pipeline](./upload-error-hierarchy-typed-exceptions-for-the-upload-pipeline.md) — The `uploads/errors.py` module defines a typed exception hierarchy rooted at `UploadError`, with specific subclasses for each failure mode (file too large, unsupported MIME, empty file, not found, access denied, storage failure). Machine-readable `code` class attributes on each exception type enable API layers to translate exceptions into structured error responses without string matching.
- [Uploads Package Init: Namespace Marker for the Upload Subsystem](./uploads-package-init-namespace-marker-for-the-upload-subsystem.md) — The `uploads/__init__.py` is an intentionally empty package initializer that establishes `pocketpaw.uploads` as a Python namespace without importing any submodules at package load time. This lazy-loading design means the upload subsystem's dependencies are only loaded when explicitly imported, keeping base startup time low.
- [Workspace Knowledge Aggregator — Cross-Agent KB Merge](./workspace-knowledge-aggregator-cross-agent-kb-merge.md) — A pure-Python aggregation layer that merges the workspace-scoped KB with every per-agent KB inside a workspace into a single sorted, deduplicated article stream. Designed to be thin, injectable, and unit-testable without a live database or kb binary.
- [Workspace Module Entry Point](./workspace-module-entry-point.md) — The `ee/cloud/workspace/__init__.py` file serves as the public entry point for the workspace domain, re-exporting the FastAPI router so that the top-level application assembler can mount it without knowing internal package structure. This single-line pattern deliberately decouples how the router is imported from where it physically lives.

### `assets` — 1 article(s)

- [Platform Icon Generator: .ico and .icns from a Single PNG Source](./platform-icon-generator-ico-and-icns-from-a-single-png-source.md) — The `make_icons.py` script generates platform-specific application icon files for Windows (`.ico`) and macOS (`.icns`) from a single source `icon.png`. It uses the native macOS `iconutil` tool when available and falls back to a Pillow-based implementation for cross-platform compatibility.

### `async` — 2 article(s)

- [FabricStore — Async SQLite Backend for the Ontology Layer](./fabricstore-async-sqlite-backend-for-the-ontology-layer.md) — `FabricStore` is the legacy async SQLite persistence layer for Fabric's ontology data, managing object types, object instances, and directional links. It uses lazy schema initialization, parameterized queries throughout, and a manual cascade-delete pattern because SQLite foreign key enforcement is not enabled by default.
- [Internal Async Event Bus: Subscribe, Emit, and Fault Isolation Tests](./internal-async-event-bus-subscribe-emit-and-fault-isolation-tests.md) — This test file validates the `ee.cloud.shared.events` module's async event bus, which enables decoupled communication between cloud subsystems without direct imports. The tests confirm subscription, emission, unsubscription, multi-handler ordering, handler fault isolation, and the existence of a module-level singleton.

### `async patterns` — 1 article(s)

- [Singleton Lifecycle Management: Coordinated Shutdown and Test Reset](./singleton-lifecycle-management-coordinated-shutdown-and-test-reset.md) — The lifecycle module provides a central registry for singletons that need graceful shutdown on app teardown and deterministic reset between test runs. It decouples teardown orchestration from individual module implementations by letting each module register its own callbacks.

### `async-runtime` — 4 article(s)

- [Slack Bot Gateway: Bootstrapping the PocketPaw Slack Integration](./slack-bot-gateway-bootstrapping-the-pocketpaw-slack-integration.md) — The `slack_gateway.py` module is the top-level entry point for running PocketPaw as a Slack bot, wiring together the message bus, Slack adapter, and agent loop into a single coroutine. It handles clean startup and shutdown so the bot can run reliably as a long-lived async process.
- [SoulManager: Lifecycle Management for the PocketPaw Soul Instance](./soulmanager-lifecycle-management-for-the-pocketpaw-soul-instance.md) — `SoulManager` is PocketPaw's singleton that owns the entire lifecycle of a `.soul` file — initialization, concurrency-safe observation, periodic auto-save, external change detection, and graceful shutdown. It is the integration seam between PocketPaw's real-time agent sessions and the persistent soul-protocol memory system.
- [StatusTracker: Real-Time Agent State Monitoring via Message Bus Events](./statustracker-real-time-agent-state-monitoring-via-message-bus-events.md) — `StatusTracker` subscribes to the PocketPaw message bus and maintains a live snapshot of every active agent session's current state — thinking, running a tool, streaming a response, or in an error condition. It powers the status indicator in the dashboard and supports long-poll queries so the UI can update without polling.
- [WhatsApp Personal Mode Adapter via Neonize](./whatsapp-personal-mode-adapter-via-neonize.md) — NeonizeAdapter connects PocketPaw to WhatsApp using the neonize Python library, which wraps the open-source whatsmeow Go client. It requires only a QR-code scan to pair — no Meta Developer account or public webhook URL is needed.

### `attachments` — 2 article(s)

- [Agent Bridge Attachment Forwarding Regression Test](./agent-bridge-attachment-forwarding-regression-test.md) — This regression test guards against the silent attachment-drop bug in `_run_agent_response`, where file attachments sent by users in channel messages were never forwarded into the agent's prompt. The test confirms that the channel code path formats attachment metadata — filename, MIME type, and size — into the prompt in the same shape used by the DM path.
- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.

### `attack detection` — 1 article(s)

- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.

### `audience resolution` — 1 article(s)

- [AudienceResolver: Event-to-Recipient Mapping with Short-TTL Caching](./audienceresolver-event-to-recipient-mapping-with-short-ttl-caching.md) — AudienceResolver translates a typed event into the list of user IDs that should receive it over WebSocket, applying domain-specific fan-out rules for groups, messages, workspace membership, sessions, agent streams, notifications, and presence. Member lookups are cached with a configurable short TTL to avoid hammering the database on high-frequency events, and the cache can be invalidated selectively when membership changes.

### `audio` — 2 article(s)

- [TextToSpeechTool: Multi-Provider TTS with Auto-Loop Integration and Media Tag Protocol](./texttospeechtool-multi-provider-tts-with-auto-loop-integration-and-media-tag-pro.md) — `TextToSpeechTool` converts text to audio via OpenAI TTS, ElevenLabs, or Sarvam AI Bulbul, saving output to `~/.pocketpaw/generated/audio/` with UUID filenames. A standalone `synthesize_speech()` helper and a `<!-- media:path -->` HTML comment protocol enable the agent loop to auto-play TTS responses without parsing tool output.
- [TextToSpeechTool: Multi-Provider TTS with Error Handling and Audio File Management](./texttospeechtool-multi-provider-tts-with-error-handling-and-audio-file-managemen.md) — The `TextToSpeechTool` converts text to speech using configurable providers (OpenAI TTS, ElevenLabs) and saves audio files to a managed directory. Tests cover the tool's definition (name, trust level, parameters), provider-specific error paths for missing API keys, unknown provider rejection, successful ElevenLabs synthesis, API error handling, the `synthesize_speech` helper, and the audio directory creation utility.

### `audit` — 18 article(s)

- [Audit Entry Model for Enterprise Compliance Logging](./audit-entry-model-for-enterprise-compliance-logging.md) — Defines the `AuditEntry` Pydantic model that captures the complete decision context for every auditable action in PocketPaw: who acted, on what data, what the AI recommended, and what actually happened. Field validators enforce closed enum sets for category and status, and serialization helpers bridge between the Pydantic model and SQLite storage.
- [Audit FTS Security Tests: SQL Injection Prevention and LIKE Wildcard Escaping](./audit-fts-security-tests-sql-injection-prevention-and-like-wildcard-escaping.md) — Security regression tests proving that the `q` parameter on PocketPaw's `/runtime/audit` search endpoint is fully parameter-bound and cannot corrupt or drop the `audit_log` table, and that LIKE special characters (`%`, `_`) in the query are correctly escaped to prevent wildcard-widened result sets. Tests correspond to Cluster C PR4.
- [Audit Logging Completeness Tests: Append-Only Guarantee, Rotation, Failure Resilience, and PII Masking](./audit-logging-completeness-tests-append-only-guarantee-rotation-failure-resilien.md) — Layer 7 security tests verifying that PocketPaw's audit logger captures all security-relevant events, maintains an append-only JSONL log that cannot be deleted through the logger interface, archives rather than deletes on rotation, survives write failures without crashing, and masks PII (e.g., SSNs) before writing to disk.
- [Audit Module Package Initialization](./audit-module-package-initialization.md) — The `pocketpaw.audit` package init re-exports the three public symbols consumers need: `AuditEntry` (the Pydantic model), `AuditStore` (the SQLite backend), and `get_audit_store` (the singleton accessor). This keeps the import path flat for callers while the implementations stay in separate sub-modules.
- [Audit Store Tests: Model Validation, Filtering, Export, and API Endpoints](./audit-store-tests-model-validation-filtering-export-and-api-endpoints.md) — Comprehensive tests for PocketPaw's `AuditStore` — the SQLite-backed audit trail that records all security-relevant operations. Tests cover the `AuditEntry` model, log persistence, multi-field query filtering, date range filtering, CSV and JSON export, REST API endpoints, and integration helpers for tool execution and connector sync logging.
- [Canonical Runtime Audit Router with Full-Text Search](./canonical-runtime-audit-router-with-full-text-search.md) — The authoritative audit query endpoint at `/api/v1/runtime/audit`, consolidating two legacy paths behind a single surface that adds workspace-level rollup filtering and bound-parameter full-text search. SQL injection is explicitly prevented by using parameterized LIKE queries, verified by a dedicated regression test.
- [Decision Trace Collector: Lifecycle, Event Aggregation, Deduplication, and Snapshot Persistence Tests](./decision-trace-collector-lifecycle-event-aggregation-deduplication-and-snapshot-.md) — This suite tests the `TraceCollector` context manager that records which Fabric objects, soul memories, KB articles, and tool calls an agent consulted while generating a decision. It also verifies the `FabricObjectSnapshot` persistence layer that captures point-in-time object state alongside each audit entry.
- [Decision Trace Wiring: Store-Level and Router-Level Integration Tests for Trace Persistence and Hydration](./decision-trace-wiring-store-level-and-router-level-integration-tests-for-trace-p.md) — Integration tests that verify `ReasoningTrace` objects are persisted into audit context when `propose()` is called, that `FabricObjectSnapshot` records are keyed to the correct audit row, and that the `/instinct/audit/{id}` hydration endpoint expands trace IDs into full records at the correct depth level.
- [EE Instinct: Human-in-the-Loop Action Approval System Tests](./ee-instinct-human-in-the-loop-action-approval-system-tests.md) — This test suite validates the full lifecycle of PocketPaw's Instinct subsystem, which gates AI-proposed actions behind human approval before execution. It covers store-layer CRUD, audit trail integrity, REST endpoint contracts, lifecycle state transitions, and edge cases such as approving non-existent actions.
- [Enterprise Audit Module (Compliance Logging Placeholder)](./enterprise-audit-module-compliance-logging-placeholder.md) — The `ee/audit/` module is a reserved namespace for enhanced compliance-grade audit logging, intended to extend the instinct pipeline's built-in event log with exportable formats, configurable retention policies, and structured compliance reports for SOC2 and GDPR.
- [Fabric Scope Policy Engine — Visibility Decisions for Ontology Objects](./fabric-scope-policy-engine-visibility-decisions-for-ontology-objects.md) — This module provides the scope-based visibility engine for Fabric objects, implementing bidirectional containment matching so that a caller with scope `org:sales:*` can see objects tagged `org:sales:leads` and vice versa. It is a local copy of the logic from soul-protocol's `scopes_overlap` function, kept independent to avoid pulling the full engine into minimal runtime slices and to preserve the audit trail of which scope granted access.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [Legacy Audit REST Router (Deprecated)](./legacy-audit-rest-router-deprecated.md) — The original `/api/v1/audit` endpoint is kept as a backwards-compatibility alias that forwards to the canonical `runtime_router` implementation. All responses include `Deprecation: true` and a `Link` header pointing callers to the successor path, enabling a graceful migration without breaking existing integrations.
- [Rate Limiter, Audit API Events, and OpenAPI Config Tests](./rate-limiter-audit-api-events-and-openapi-config-tests.md) — Tests PocketPaw's token-bucket rate limiter, the `RateLimitInfo` response model and its header formatting, audit logging for API events (OAuth and key revocation), and the OpenAPI endpoint configuration. Together these tests ensure the API protects against request floods, produces correct rate limit headers, and maintains a searchable audit trail.
- [RBAC Audit Helpers: Structured Denial and Privileged Action Logging](./rbac-audit-helpers-structured-denial-and-privileged-action-logging.md) — The `audit.py` module provides two thin wrapper functions — `log_denial` and `log_privileged_action` — that emit structured `AuditEvent` records whenever authorization decisions block or allow a sensitive operation. These wrappers ensure that RBAC events are logged at the correct severity level with machine-readable codes that can be queried, alerted on, or exported to a SIEM.
- [Security Package Entry Point: Audit, Guardian, and PII Surface](./security-package-entry-point-audit-guardian-and-pii-surface.md) — The pocketpaw.security package re-exports three security subsystems through its __init__.py: AuditLogger for structured event logging with severity levels, GuardianAgent for content policy enforcement, and PIIScanner for detecting and acting on personally identifiable information in agent inputs and outputs.
- [SQLite-Backed Audit Log Store](./sqlite-backed-audit-log-store.md) — Implements the `AuditStore` class that persists audit entries to a SQLite database using the stdlib `sqlite3` module with async-friendly execution via `run_in_executor`. Provides filtered querying, full-text search with injection-safe LIKE escaping, CSV/JSON export, and convenience methods for common audit event types.
- [Tool Registry: Centralized Execution Hub with Policy Enforcement and Audit Logging](./tool-registry-centralized-execution-hub-with-policy-enforcement-and-audit-loggin.md) — The `ToolRegistry` is PocketPaw's central runtime for tool management -- it stores registered tools, enforces `ToolPolicy` access control, validates parameters, executes tools under a configurable timeout, scrubs sensitive data from logs, and writes to the audit trail. Multiple iterative hardening passes reflect its role as the primary security enforcement point at execution time.

### `audit and compliance` — 6 article(s)

- [Audit Logging Verification Script — Manual Smoke Test for Tool Execution Audit Trail](./audit-logging-verification-script-manual-smoke-test-for-tool-execution-audit-tra.md) — This standalone script provides a manual, human-readable verification that the audit logging pipeline records both `attempt` and `success` entries when a tool is executed through the ToolRegistry. It runs outside the pytest harness and is designed to be invoked directly to confirm audit infrastructure is wired correctly in a live environment.
- [Fleet Install REST Router](./fleet-install-rest-router.md) — Exposes two HTTP endpoints — listing bundled fleet templates and triggering a fleet install — as part of the enterprise edition's fleet management subsystem. Guards the install route with RBAC and emits correlated audit events into the shared org journal.
- [Instinct Decision Pipeline — Core Data Models](./instinct-decision-pipeline-core-data-models.md) — Defines the Pydantic data models and enumerations that underpin the Instinct decision pipeline: the proposed action, its lifecycle states, audit entries, and the contextual metadata that connects an action to its triggering source. These types are the stable contract shared by the store, router, correction system, and any agent that proposes actions.
- [Instinct Reasoning Trace Types — Decision Audit Context](./instinct-reasoning-trace-types-decision-audit-context.md) — Defines the immutable data types that record the full reasoning context behind each proposed action: the queries made, soul memories consulted, KB articles injected, and tool calls executed. These types power the "Why?" audit drawer and enable humans to verify exactly what evidence the agent used.
- [Org Journal FastAPI Dependency — Shared Audit Log for EE Routes](./org-journal-fastapi-dependency-shared-audit-log-for-ee-routes.md) — Provides a single shared `Journal` instance as a FastAPI dependency, ensuring all enterprise edition routes write their audit events to one canonical org-level log rather than opening separate per-subsystem SQLite files. Supports test isolation via a cache-reset escape hatch.
- [Runtime Audit API Integration Tests — Workspace Rollup, Search, CSV Export, and Security](./runtime-audit-api-integration-tests-workspace-rollup-search-csv-export-and-secur.md) — This test module validates the canonical `/runtime/audit` endpoint introduced in Cluster C/PR4, covering workspace-scoped filtering, full-text search with length caps, CSV export, SQL injection resistance, and the deprecation forwarding behavior of the legacy `/audit` alias. It exercises both the new runtime router and the legacy router mounted together to prove they share the same underlying AuditStore and scope guard.

### `auth` — 8 article(s)

- [Auth Domain FastAPI Router — Profile, Avatar, and Workspace Management](./auth-domain-fastapi-router-profile-avatar-and-workspace-management.md) — The auth router assembles PocketPaw Enterprise's full authentication HTTP surface by composing fastapi-users' built-in auth/register routes with custom profile management, avatar upload, and workspace-switching endpoints.
- [Auth Domain Package Re-Exports](./auth-domain-package-re-exports.md) — The auth domain `__init__.py` is a backward-compatibility shim that re-exports the entire public surface of `auth.core` and `auth.router` from the `ee.cloud.auth` namespace, so existing callers don't need to update their imports if the internal module structure changes.
- [Auth Domain Request and Response Schemas](./auth-domain-request-and-response-schemas.md) — The auth schemas module defines the three Pydantic models used by the auth router: a profile update body, a workspace-switch body, and a user response envelope that shapes how user records are serialised for API consumers.
- [Auth Domain Schema Tests](./auth-domain-schema-tests.md) — This module tests the Pydantic schemas for the auth domain — `ProfileUpdateRequest`, `SetWorkspaceRequest`, and `UserResponse` — verifying optional field defaults, value round-trips, and the structure of the user identity response.
- [Auth Service - User Profile and Workspace Management](./auth-service-user-profile-and-workspace-management.md) — A stateless FastAPI service class handling the business logic for user authentication, profile management, and active workspace switching. It keeps route handlers thin by acting as the bridge between HTTP requests and the User ODM model.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Enterprise Authentication Core — JWT, User Management, and Startup Seeding](./enterprise-authentication-core-jwt-user-management-and-startup-seeding.md) — `auth/core.py` implements PocketPaw Enterprise's authentication system using `fastapi-users` with dual JWT transports (cookie for browser, bearer for API/Tauri), and provides idempotent startup seeding functions that ensure a default admin, workspace, chat group, and default agent exist before the first user logs in.
- [Live Cloud API Smoke Tests with Backend Reachability Guard](./live-cloud-api-smoke-tests-with-backend-reachability-guard.md) — Smoke tests that run real HTTP calls against a locally running `paw-cloud` backend. The entire module skips automatically when the backend is unreachable, making these safe to include in CI without a live server requirement. Tests cover login, profile retrieval, agent listing, room lifecycle, DMs, and unauthenticated rejection.

### `authentication` — 27 article(s)

- [API Key Management Tests: Creation, Verification, Rotation, and REST Endpoints](./api-key-management-tests-creation-verification-rotation-and-rest-endpoints.md) — Comprehensive tests for PocketPaw's `APIKeyManager` class and its REST endpoints covering the full key lifecycle — creation with scope validation, verification, revocation, rotation, and expiry rejection. The test suite isolates all disk I/O to a temp directory to prevent cross-test contamination and ensure file permission checks work correctly.
- [API Key Manager: Secure Key Lifecycle with Hash-Only Storage](./api-key-manager-secure-key-lifecycle-with-hash-only-storage.md) — `APIKeyManager` provides create, verify, revoke, rotate, and list operations for API keys using a GitHub PAT-style design: keys are shown once at creation and only their SHA-256 hash is persisted. Keys use the `pp_` prefix for log identification, support fine-grained scopes, and are stored in `~/.pocketpaw/api_keys.json`.
- [API Key Schemas — Create, Info, and One-Time Creation Response](./api-key-schemas-create-info-and-one-time-creation-response.md) — The API key schemas define the request and response shapes for managing PocketPaw API keys. The design enforces a security invariant: the full plaintext key is returned only once at creation time, after which only a short prefix is exposed for identification.
- [Auth API Tests: Session Exchange, Cookie Login, Rate Limiting, and QR Code](./auth-api-tests-session-exchange-cookie-login-rate-limiting-and-qr-code.md) — This test module comprehensively covers PocketPaw's `/api/v1/auth` router, verifying the master-token-to-session exchange, cookie-based login with correct Secure flag handling under reverse proxies, logout, token regeneration, QR code generation, and IP-based rate limiting on all three auth endpoints.
- [Auth Exception Logging Tests: Silent Swallow Prevention in Auth Dispatch and OAuth2](./auth-exception-logging-tests-silent-swallow-prevention-in-auth-dispatch-and-oaut.md) — Regression tests for issue #627 — verifies that bare `except Exception: pass` blocks in authentication dispatch paths have been replaced with `logger.warning(..., exc_info=True)` calls. Tests cover API key validation, OAuth2 token validation, cookie login branches, and OAuth2 server audit log failures, ensuring auth failures are always logged and always return 401.
- [Authentication Schemas — Login, Session Token, and Token Regeneration](./authentication-schemas-login-session-token-and-token-regeneration.md) — The auth schemas define the request and response shapes for PocketPaw's cookie-based authentication flow, including master token login, session token exchange, and token regeneration. These three models represent the full lifecycle of an authenticated session.
- [Dashboard Auth Cookie Tests: Secure Flag Behavior Under HTTP and HTTPS Proxies](./dashboard-auth-cookie-tests-secure-flag-behavior-under-http-and-https-proxies.md) — This test file validates that the dashboard's session cookie is set without the `Secure` flag in plain HTTP environments (to avoid breaking self-hosted HTTP deployments) and with the `Secure` flag when the request arrives via an HTTPS reverse proxy detected by the `X-Forwarded-Proto: https` header.
- [Dashboard Security Tests: Rate Limiting, Session Tokens, Tunnel Auth Bypass, Security Headers, and WebSocket Auth](./dashboard-security-tests-rate-limiting-session-tokens-tunnel-auth-bypass-securit.md) — This comprehensive test module validates PocketPaw's dashboard security hardening across six dimensions: token-bucket rate limiting, HMAC-signed session tokens with TTL, the localhost tunnel auth bypass fix, security headers, CORS rejection, and WebSocket authentication before the upgrade handshake completes.
- [Drive OAuth Bearer Token Resolution: Credential Broker, Env Var, and OAuthManager Fallback Chain](./drive-oauth-bearer-token-resolution-credential-broker-env-var-and-oauthmanager-f.md) — `auth.py` implements a prioritised three-source token-resolution chain so the Drive connector always has a valid OAuth bearer token regardless of environment (production, CI, local dev). It isolates token concerns from `source.py`, keeping the adapter code focused on business logic.
- [FastAPI Dependency Injection for Cloud Routers](./fastapi-dependency-injection-for-cloud-routers.md) — This module provides reusable FastAPI dependency functions that extract identity and workspace context from authenticated requests, and composable action-based RBAC guards. It is the single source of truth for authentication wiring across all cloud EE router endpoints.
- [Fleet Router Test Suite: Template Listing, Install Auth, and Journal Integration](./fleet-router-test-suite-template-listing-install-auth-and-journal-integration.md) — Covers the FastAPI fleet REST router that powers the paw-enterprise InstallFleetPanel, verifying template enumeration, end-to-end install flows, journal event emission, and a layered auth guard (401/403/200) added when the P0 auth requirement landed. Uses FastAPI dependency_overrides throughout so tests remain hermetic — no real Mongo, no real soul-protocol runtime, no writes to ~/.soul/.
- [HMAC Session Tokens: Stateless Dashboard Authentication with Instant Invalidation](./hmac-session-tokens-stateless-dashboard-authentication-with-instant-invalidation.md) — This module implements stateless session tokens using HMAC-SHA256 with a `{expires_unix}:{hex_hmac}` format, keyed against the master access token. Regenerating the master token instantly invalidates all outstanding sessions without requiring a server-side token store or database.
- [MCP OAuth Flow: Token Storage, Callback Coordination, Preset Flags, and Dashboard Integration](./mcp-oauth-flow-token-storage-callback-coordination-preset-flags-and-dashboard-in.md) — The MCP OAuth test suite validates PocketPaw's support for OAuth-authenticated MCP servers, covering secure token and client-info persistence, WebSocket-based callback coordination, preset flag propagation, and the unauthenticated OAuth callback dashboard endpoint. Tests also verify that token files are created with restricted permissions and that corrupted token files degrade gracefully.
- [OAuth Manager — Google OAuth 2.0 Authorization Code Flow and Token Lifecycle](./oauth-manager-google-oauth-20-authorization-code-flow-and-token-lifecycle.md) — `OAuthManager` implements the full OAuth 2.0 authorization code flow for Google (and optionally Spotify), handling authorization URL generation, code-for-token exchange, access token refresh, and token revocation, backed by `TokenStore` for persistent credential storage. It provides a `get_valid_token()` convenience method that transparently refreshes expired tokens.
- [OAuth Token Store: Secure File-Based Credential Persistence](./oauth-token-store-secure-file-based-credential-persistence.md) — The `TokenStore` persists OAuth 2.0 tokens to `~/.pocketpaw/oauth/{service}.json` with strict `0600` file permissions to prevent other OS users from reading credentials. It provides load, save, delete, and list operations over the `OAuthTokens` dataclass that covers the full OAuth 2.0 token surface.
- [OAuth2 Authorization Server: PKCE Flow Implementation](./oauth2-authorization-server-pkce-flow-implementation.md) — `AuthorizationServer` implements the full OAuth2 authorization code + PKCE flow: issuing authorization codes, exchanging them for tokens via S256 challenge verification, refreshing tokens, and revoking tokens. Token lifetimes are 1-hour access / 30-day refresh / 10-minute authorization code.
- [OAuth2 PKCE Authorization Server Tests: Full Flow, Token Exchange, and Consent](./oauth2-pkce-authorization-server-tests-full-flow-token-exchange-and-consent.md) — Tests PocketPaw's OAuth2 authorization server with PKCE (Proof Key for Code Exchange), covering authorization code issuance, token exchange, token refresh, revocation, and the consent UI endpoints. The tests validate both the server logic class directly and the REST endpoints, ensuring the security guarantees of PKCE are enforced end-to-end.
- [OAuth2 Server — Authorize, Token Exchange, and Revoke](./oauth2-server-authorize-token-exchange-and-revoke.md) — The OAuth2 router implements the server side of the OAuth2 authorization code flow, allowing third-party MCP clients and tools to obtain scoped access tokens to PocketPaw's API. It handles the consent UI, code-to-token exchange, refresh token rotation, and revocation.
- [OAuth2 Token Exchange and Revocation Schemas](./oauth2-token-exchange-and-revocation-schemas.md) — Defines the Pydantic models for PocketPaw's OAuth2 token lifecycle — code exchange, token refresh, standard token responses, and revocation. Field-level validation enforces RFC 6749 grant type restrictions at the schema boundary, catching invalid requests before they reach the OAuth2 implementation.
- [OAuth2/PKCE Authorization Server Package](./oauth2pkce-authorization-server-package.md) — The `oauth2` package implements a self-contained OAuth2 authorization server with PKCE support, designed primarily for the PocketPaw Tauri desktop application to authenticate against a locally running PocketPaw instance without exposing a client secret. The package is organized into models, server logic, and storage layers.
- [OpenRouterAdapter: Multi-Model Routing with Anthropic-Compat Authentication Quirks](./openrouteradapter-multi-model-routing-with-anthropic-compat-authentication-quirk.md) — `OpenRouterAdapter` integrates OpenRouter's model routing service, which provides access to 100+ models via a single API key. Its most distinctive behavior is an intentional `ANTHROPIC_API_KEY=""` (empty string) workaround: OpenRouter authenticates via `ANTHROPIC_AUTH_TOKEN`, and if `ANTHROPIC_API_KEY` is set to a real value, the Anthropic SDK sends it as `Bearer` and OpenRouter rejects the request.
- [Spotify Integration: OAuth-Authenticated Web API Client](./spotify-integration-oauth-authenticated-web-api-client.md) — The Spotify client provides PocketPaw agents with authenticated access to the Spotify Web API for playback control, track search, and playlist management. It delegates token lifecycle management to the shared `OAuthManager` and `TokenStore` infrastructure, raising a clear error if the user has not completed the OAuth flow.
- [Telegram Channel Router: Setup and Pairing Flow](./telegram-channel-router-setup-and-pairing-flow.md) — Implements the three REST endpoints that drive Telegram bot pairing: checking current configuration status, initiating the pairing flow with a bot token, and polling for pairing completion. The router wraps the shared pairing state dict from the dashboard module, adding token validation and graceful handling of missing optional dependencies.
- [Telegram Pairing Schema Contracts](./telegram-pairing-schema-contracts.md) — Defines the Pydantic request and response models used by the Telegram channel pairing flow. These schemas enforce a clean contract between the REST API layer and the bot token validation and QR code generation logic.
- [Upload Grant Tokens: HMAC-Signed Short-Lived URL Access](./upload-grant-tokens-hmac-signed-short-lived-url-access.md) — The signing module issues and verifies HMAC-SHA256 grant tokens that allow unauthenticated HTTP access to a specific upload for a limited time window. Grants are designed for use in raw `<img src>` and `<a href download>` attributes where an HTTP `Authorization` header cannot be sent.
- [User and OAuth Account Models](./user-and-oauth-account-models.md) — The `User` document extends `fastapi-users`' Beanie base to add enterprise-specific fields: multi-workspace membership with per-workspace roles, presence status, OAuth provider linkage, and an active workspace pointer. The `WorkspaceMembership` embedded model tracks role and join timestamp per workspace.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.

### `authorization` — 15 article(s)

- [ABAC Policy Engine: Plan Gates, Role Checks, and Agent Ceilings](./abac-policy-engine-plan-gates-role-checks-and-agent-ceilings.md) — The `abac.py` module implements PocketPaw's attribute-based access control (ABAC) by combining three declarative tables — plan feature gates, action-to-role mappings, and per-role tool limits — into a single `evaluate_policy` function. It is designed to run after RBAC role resolution, adding plan-tier and agent-ceiling checks that role membership alone cannot express.
- [ACTIONS Registry: Canonical RBAC Action-to-Role Matrix](./actions-registry-canonical-rbac-action-to-role-matrix.md) — The `actions.py` module is the single source of truth for every guarded operation in PocketPaw, mapping action strings (in `"resource.action"` dotted form) to a minimum required role and a stable machine-readable denial code. Unknown actions fail loudly by design, preventing silent authorization bypasses when new routes are added without a corresponding ACTIONS entry.
- [Agent Scope Rules: Normalization and Assignment Authorization Tests](./agent-scope-rules-normalization-and-assignment-authorization-tests.md) — This module tests `ee.cloud.agents.scope_rules`, covering the `normalise_and_validate_scopes` function (grammar enforcement, lowercasing, deduplication, forbidden-wildcard rejection) and the `admin_can_assign_scopes` function (containment check that prevents scope-narrowed admins from assigning scopes outside their own grant).
- [API Authorization Dependency: Fail-Closed Scope Enforcement](./api-authorization-dependency-fail-closed-scope-enforcement.md) — `require_scope` is a FastAPI dependency that enforces fail-closed authorization across all API endpoints. It accepts requests only when the caller has been explicitly marked as fully trusted, carries an API key with the required scope, or carries an OAuth2 token with the required scope — rejecting all other requests with HTTP 403.
- [Core RBAC Primitives: Workspace Roles, Pocket Access Levels, and Forbidden Exception](./core-rbac-primitives-workspace-roles-pocket-access-levels-and-forbidden-exceptio.md) — `rbac.py` establishes the foundational enums and exception for PocketPaw's role-based access control: a 3-tier `WorkspaceRole` hierarchy, a 4-tier `PocketAccess` hierarchy, and the `Forbidden` exception that carries machine-readable denial codes. All other guard modules depend on these primitives without circular imports.
- [Enterprise Guard Layer: RBAC, ABAC, Plan Feature Gates, and FastAPI Dependency Tests](./enterprise-guard-layer-rbac-abac-plan-feature-gates-and-fastapi-dependency-tests.md) — Comprehensive tests for PocketPaw's enterprise authorization layer, covering role-based access control (RBAC) enums and guard functions, attribute-based access control (ABAC) policy evaluation, plan feature tables, action-to-role mapping, tool whitelists, and three FastAPI dependency guards (`require_role`, `require_plan_feature`, `require_policy`).
- [FastAPI Dependency Injection for Cloud Routers](./fastapi-dependency-injection-for-cloud-routers.md) — This module provides reusable FastAPI dependency functions that extract identity and workspace context from authenticated requests, and composable action-based RBAC guards. It is the single source of truth for authentication wiring across all cloud EE router endpoints.
- [Guards Dependency Factories: FastAPI-Injectable RBAC and ABAC Guards](./guards-dependency-factories-fastapi-injectable-rbac-and-abac-guards.md) — The `deps.py` module generates FastAPI dependency callables that enforce authorization on route handlers, pulling workspace membership and role from `request.state` (populated by upstream middleware) and raising `HTTPException(403)` on any denial. It also provides non-FastAPI helper functions for cloud routes that authenticate via fastapi-users and carry full user models rather than request state.
- [Guards Package: Unified Re-Export for RBAC and ABAC Authorization](./guards-package-unified-re-export-for-rbac-and-abac-authorization.md) — The `guards` package `__init__.py` is a single-point re-export that collects all authorization primitives — roles, access levels, policy evaluation, action rules, FastAPI dependency factories, and audit helpers — under one importable namespace. This prevents callers from needing to know which sub-module owns each symbol, and makes future refactoring transparent to the rest of the codebase.
- [PolicyContext and PolicyResult: Authorization Decision Data Types](./policycontext-and-policyresult-authorization-decision-data-types.md) — `policy.py` defines the two frozen dataclasses that form the data contract for the ABAC evaluation pipeline: `PolicyContext` bundles everything a guard needs to make a decision, and `PolicyResult` carries the outcome with a machine-readable code. Their immutability and slot optimization make them safe to pass across async boundaries without defensive copying.
- [RBAC Audit Helpers: Structured Denial and Privileged Action Logging](./rbac-audit-helpers-structured-denial-and-privileged-action-logging.md) — The `audit.py` module provides two thin wrapper functions — `log_denial` and `log_privileged_action` — that emit structured `AuditEvent` records whenever authorization decisions block or allow a sensitive operation. These wrappers ensure that RBAC events are logged at the correct severity level with machine-readable codes that can be queried, alerted on, or exported to a SIEM.
- [Scope Enforcement and Tool Policy Fail-Closed Tests](./scope-enforcement-and-tool-policy-fail-closed-tests.md) — This test module validates that PocketPaw's `require_scope` FastAPI dependency rejects requests that lack explicit authorization markers, preventing silent scope bypass via master, session, or cookie auth. It also verifies that `ToolPolicy` raises on unknown profile names rather than defaulting to an open or undefined state.
- [Smoke Test: DM and Group Chat Persistence for Human Users](./smoke-test-dm-and-group-chat-persistence-for-human-users.md) — This end-to-end test verifies that DM creation between two workspace members, message exchange, and group chat all persist correctly in MongoDB — without any agent involvement. It validates the full social messaging layer: workspace invites, DM groups, populated member objects, message context types, and cross-member read access.
- [Upload Read Authorization Tests: Owner, Chat Member, Workspace Admin, and Existence-Hiding](./upload-read-authorization-tests-owner-chat-member-workspace-admin-and-existence-.md) — Tests for `EEUploadService._assert_can_read`, the read gate used by `stream`, `presigned_get`, and the `/download-url` and `/grant` endpoints. The parametrized suite verifies that the owner, chat members, and workspace admins can read while strangers receive `NotFound` (not `Forbidden`) — an existence-hiding design that prevents file-ID probing.
- [Workspace FastAPI Router — CRUD, Members, and Invites](./workspace-fastapi-router-crud-members-and-invites.md) — This module defines all HTTP endpoints for the workspace domain — workspace lifecycle, member management, and invite flows — using FastAPI's dependency injection to enforce authorization at the route layer while keeping service methods auth-agnostic. Authorization gates are declared inline as `Depends(require_action(...))` so the permission model is readable directly from the route signature.

### `automation` — 2 article(s)

- [Enterprise Automations Module (Trigger Engine Placeholder)](./enterprise-automations-module-trigger-engine-placeholder.md) — The `ee/automations/` module is a reserved namespace for a rule-based trigger engine that will let users define time-based and data-condition-based automations — turning PocketPaw agents from reactive responders into proactive actors.
- [Intention Scheduling Schemas](./intention-scheduling-schemas.md) — Defines the Pydantic models for PocketPaw's intention system — scheduled, trigger-driven agent tasks that run prompts automatically. The schema covers the full CRUD lifecycle plus manual trigger acknowledgement, with field-level validation to prevent empty or oversized intentions.

### `automations` — 8 article(s)

- [Automation Evaluator — Background Condition Polling Engine](./automation-evaluator-background-condition-polling-engine.md) — AutomationEvaluator is a singleton background loop that periodically checks threshold and data_change automation rules against live Fabric data, then fires matched rules through the Instinct pipeline (for approval-required rules) or directly via the daemon (for auto-execute rules). Schedule rules are handled by the bridge/daemon and do not go through the evaluator.
- [Automation Evaluator: Rule-to-Daemon Sync, Lifecycle, Cooldown, Fire Modes, and Router Integration Tests](./automation-evaluator-rule-to-daemon-sync-lifecycle-cooldown-fire-modes-and-route.md) — Tests for the `AutomationEvaluator` — the runtime that translates `AutomationStore` rules into daemon intentions, evaluates which rules should fire, and dispatches actions. Covers the full evaluator lifecycle, cooldown enforcement, three execution modes (require approval, auto-execute, notify-only), daemon sync/unsync, and the router bridge integration.
- [Automations Bridge — Syncing EE Rules to Core Daemon Intentions](./automations-bridge-syncing-ee-rules-to-core-daemon-intentions.md) — bridge.py translates enterprise automation rules into core daemon Intention specs, and removes them when rules are deleted. It maps schedule presets and rule types (schedule, threshold, data_change) to cron, interval, and event trigger types respectively, ensuring automation rules run through the same scheduling infrastructure as built-in daemon behaviors.
- [Automations Models — Rule, RuleType, ExecutionMode, and Request Models](./automations-models-rule-ruletype-executionmode-and-request-models.md) — This module defines the Pydantic data models for the EE automations engine: the `Rule` entity (the core automation record), `RuleType` and `ExecutionMode` enums, and `CreateRuleRequest` / `UpdateRuleRequest` for the API layer. The models cover three rule types (threshold, schedule, data_change) with three execution modes (require_approval, auto_execute, notify_only).
- [Automations Router: REST API for Rule-Based Pocket Automations](./automations-router-rest-api-for-rule-based-pocket-automations.md) — The automations router exposes a FastAPI-based CRUD API for managing automation rules and controlling the background evaluator that fires them. It acts as the HTTP boundary between the frontend and the lower-level store, bridge, and evaluator subsystems, ensuring that every rule mutation is immediately synchronized to the daemon via the bridge.
- [Automations Subpackage — Rule-Based Automation Engine Namespace](./automations-subpackage-rule-based-automation-engine-namespace.md) — The `pocketpaw.ee.automations` package is the namespace for PocketPaw's rule-based automation engine, grouping the models, bridge, evaluator, and store modules that together implement trigger-action workflows for enterprise pockets.
- [AutomationStore: JSON File-Backed Persistence for Automation Rules](./automationstore-json-file-backed-persistence-for-automation-rules.md) — The `AutomationStore` class manages CRUD operations and fire-count tracking for automation rules, persisting state to a JSON file at `~/.pocketpaw/automations/rules.json`. It is accessed as a module-level singleton to ensure all router and evaluator calls operate on the same in-memory rule set.
- [Enterprise Automations Rule CRUD: Store and FastAPI Router Tests](./enterprise-automations-rule-crud-store-and-fastapi-router-tests.md) — Full test coverage for the enterprise `AutomationStore` and its FastAPI router, covering three rule types (threshold, schedule, data change), all CRUD operations, toggle/enable behavior, fire tracking, file persistence, and a complete lifecycle end-to-end test. Every test uses `tmp_path` isolation to avoid touching the user's real `~/.pocketpaw` configuration.

### `autostart` — 1 article(s)

- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.

### `backend adapters` — 2 article(s)

- [Copilot SDK Backend Tests: Capabilities, Provider Config, Session Management, and Tool Injection](./copilot-sdk-backend-tests-capabilities-provider-config-session-management-and-to.md) — This test module validates the `CopilotSDKBackend` — PocketPaw's adapter for GitHub Copilot's agent SDK — entirely through mocks, requiring no real CLI binary or SDK installation. It covers static capability metadata, provider-specific configuration dispatch, multi-turn session reuse, event stream mapping, and the injection of PocketPaw tool instructions into the Copilot prompt.
- [Deep Agents Backend Tests: LangChain Integration, Provider Parsing, Content Extraction, and Registry](./deep-agents-backend-tests-langchain-integration-provider-parsing-content-extract.md) — This test module validates the `DeepAgentsBackend` — PocketPaw's LangChain-based agent backend — through mocks, requiring no real LangChain or LangGraph installation. It covers static metadata, the `provider:model` string parsing convention, LangGraph `Overwrite` object unwrapping, content extraction from mixed message formats, graceful degradation when the SDK is absent, and backend registry integration.

### `backend initialization` — 2 article(s)

- [Smoke Test: MemoryManager Singleton Stays Live After Backend Swap](./smoke-test-memorymanager-singleton-stays-live-after-backend-swap.md) — This test reproduces a real bug where `AgentLoop` captured `get_memory_manager()` at construction time — before cloud database initialization — and then held a stale reference to the file-based store even after the MongoDB backend was registered. It verifies that the singleton's internal store is swapped in-place so pre-captured references automatically see the new backend.
- [Smoke Test: MongoDB Memory Backend End-to-End](./smoke-test-mongodb-memory-backend-end-to-end.md) — This script exercises the full MongoDB memory path from `init_cloud_db` through `MemoryManager.add_to_session`, `get_session_history`, raw Mongo inspection, and long-term memory storage in `memory_facts`. It serves as the canonical integration check for the enterprise memory backend.

### `backends` — 1 article(s)

- [LLM Backend Schemas — Capability and Installation Contracts](./llm-backend-schemas-capability-and-installation-contracts.md) — The backend schemas define how PocketPaw describes available LLM backends to clients. `BackendInfo` captures a backend's capabilities and installation requirements, while `BackendInstallRequest` provides the payload for triggering a backend installation.

### `background processing` — 1 article(s)

- [Automations Router: REST API for Rule-Based Pocket Automations](./automations-router-rest-api-for-rule-based-pocket-automations.md) — The automations router exposes a FastAPI-based CRUD API for managing automation rules and controlling the background evaluator that fires them. It acts as the HTTP boundary between the frontend and the lower-level store, bridge, and evaluator subsystems, ensuring that every rule mutation is immediately synchronized to the daemon via the bridge.

### `background tasks` — 1 article(s)

- [Mission Control Heartbeat Daemon: Agent Health and Work Polling](./mission-control-heartbeat-daemon-agent-health-and-work-polling.md) — Tests for HeartbeatDaemon, the background process that periodically wakes registered agents, checks for pending tasks and notifications, and records liveness signals. Covers daemon lifecycle, configurable intervals, work discovery, error resilience, and manual triggers.

### `background-tasks` — 1 article(s)

- [Automation Evaluator — Background Condition Polling Engine](./automation-evaluator-background-condition-polling-engine.md) — AutomationEvaluator is a singleton background loop that periodically checks threshold and data_change automation rules against live Fabric data, then fires matched rules through the Instinct pipeline (for approval-required rules) or directly via the daemon (for auto-execute rules). Schedule rules are handled by the bridge/daemon and do not go through the evaluator.

### `backward compatibility` — 1 article(s)

- [API Backward Compatibility and Versioned Endpoint Tests](./api-backward-compatibility-and-versioned-endpoint-tests.md) — This module verifies that both the legacy `/api/` endpoints and the versioned `/api/v1/` endpoints in the PocketPaw dashboard respond with HTTP 200, guarding against breaking changes during the v1 API migration. It also checks that OpenAPI documentation endpoints remain accessible.

### `bootstrap` — 11 article(s)

- [Agent Context Builder — System Prompt Assembly with Budget Tracking](./agent-context-builder-system-prompt-assembly-with-budget-tracking.md) — AgentContextBuilder is the central orchestrator that assembles the full system prompt for each agent turn, weaving together identity, instructions, memory recalls, knowledge-base results, channel hints, health state, and injected files while staying within a configurable character budget. Its priority-based injection model ensures critical blocks (identity, instructions) are never dropped, while optional blocks are trimmed or skipped as budget tightens.
- [Bootstrap Package Public API](./bootstrap-package-public-api.md) — The bootstrap package exposes the four core primitives needed to assemble an agent's initial context: the protocol contracts, the default file-based provider, the cloud-agent provider, and the context builder. Centralising these re-exports here keeps consumer imports clean and makes it easy to see what the subsystem offers at a glance.
- [Bootstrap Protocol — BootstrapContext and BootstrapProviderProtocol Contracts](./bootstrap-protocol-bootstrapcontext-and-bootstrapproviderprotocol-contracts.md) — This module defines the two core contracts of the bootstrap subsystem: BootstrapContext, a dataclass holding all fields that compose an agent's system prompt, and BootstrapProviderProtocol, a structural Protocol that any identity source must implement. The system prompt layout — instructions first, identity block last — is a deliberate positioning strategy to keep the model anchored on persona during long conversations.
- [Bootstrap System Tests: System Prompt Assembly, Identity File Caching, and Channel Hints](./bootstrap-system-tests-system-prompt-assembly-identity-file-caching-and-channel-.md) — This test suite validates PocketPaw's bootstrap pipeline, which reads identity configuration files (IDENTITY.md, SOUL.md, STYLE.md, USER.md) and assembles them into a structured system prompt for the AI agent. Tests cover the XML-tag layout contract of `BootstrapContext`, the mtime-based file cache in `DefaultBootstrapProvider`, and the channel-specific formatting hints added by `AgentContextBuilder`.
- [Cloud Agent Bootstrap Provider](./cloud-agent-bootstrap-provider.md) — CloudAgentBootstrapProvider builds a BootstrapContext from a MongoDB agent config dict, enabling cloud-hosted agents to carry their own custom identity, persona, archetype, and values rather than inheriting the generic OSS defaults. It satisfies the same BootstrapProviderProtocol as the default file-based provider, so the rest of the runtime is unaware of which source is in use.
- [Default Bootstrap Provider — File-Based Agent Identity](./default-bootstrap-provider-file-based-agent-identity.md) — DefaultBootstrapProvider reads the agent's identity, soul, style, instructions, and user profile from markdown files in the PocketPaw config directory, caching each file's content keyed by modification time to avoid redundant disk reads. It is the standard provider for self-hosted and developer deployments where identity is managed as local configuration rather than a database record.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.
- [PocketPaw Interactive Installer](./pocketpaw-interactive-installer.md) — A self-contained, single-file installer that guides users through PocketPaw setup with optional rich prompts, falling back to plain text input when the terminal environment cannot support InquirerPy. Supports both interactive and headless (non-interactive) installation profiles, with a robust dependency bootstrap cascade that verifies each step before proceeding.
- [Soul Bridge: Connecting soul-protocol to PocketPaw's Agent Loop](./soul-bridge-connecting-soul-protocol-to-pocketpaws-agent-loop.md) — soul_bridge.py provides two classes: SoulBootstrapProvider maps soul state and memories into the BootstrapContext consumed by AgentContextBuilder while preserving tool documentation from the default provider, and SoulBridge exposes a simple observe/recall interface with transparent fallback to older soul-protocol API versions.
- [User Profile Bootstrap: USER.md Integration with BootstrapContext and System Prompt Injection](./user-profile-bootstrap-usermd-integration-with-bootstrapcontext-and-system-promp.md) — The bootstrap system supports a `USER.md` file that stores human-readable user profile information (name, timezone, preferences) which is loaded into `BootstrapContext` and injected into the agent's system prompt under a `# User Profile` section. Tests verify the default empty state, template creation, idempotent non-overwrite, loading, system prompt injection, and graceful handling of a missing file.

### `bot-infrastructure` — 1 article(s)

- [Discord Gateway — Bot Initialization and MessageBus Integration](./discord-gateway-bot-initialization-and-messagebus-integration.md) — discord_gateway.py provides the `run_discord_bot` entry point that wires the Discord adapter to the MessageBus and starts the AgentLoop for the Discord channel. It translates all Discord-specific configuration from Settings into the adapter, then hands off to the agent infrastructure.

### `boto3` — 1 article(s)

- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.

### `browser` — 4 article(s)

- [Accessibility Tree Snapshot Generator — Semantic Browser State for LLMs](./accessibility-tree-snapshot-generator-semantic-browser-state-for-llms.md) — snapshot.py converts Playwright's raw accessibility tree into a compact, indented text representation with [ref=N] markers that an LLM can read to understand page structure and reference specific elements for interaction. RefMap maintains the mapping from integer references back to element selectors, and SnapshotGenerator handles recursive tree traversal, role filtering, name truncation, and selector generation.
- [Browser Automation Module Public API](./browser-automation-module-public-api.md) — The browser package re-exports the public surface of PocketPaw's Playwright-based browser automation subsystem: snapshot types (RefMap, AccessibilityNode, SnapshotGenerator), the low-level driver (BrowserDriver, NavigationResult), and the session management layer (BrowserSession, BrowserSessionManager, get_browser_session_manager). These exports give agent tools a single import point for all browser interaction primitives.
- [Browser Session Manager — Named Session Pool with Idle Cleanup](./browser-session-manager-named-session-pool-with-idle-cleanup.md) — BrowserSession and BrowserSessionManager provide a named pool of persistent browser sessions so that agent tools can reuse an open browser across multiple tool calls without launching and closing a new browser for every interaction. Per-session asyncio locks prevent race conditions when concurrent tool calls reference the same session, and idle cleanup reclaims resources from sessions that have not been used within a configurable timeout.
- [Playwright Browser Driver — LLM-Oriented Browser Control](./playwright-browser-driver-llm-oriented-browser-control.md) — BrowserDriver wraps Playwright's async API into a simplified interface designed for LLM-driven browser automation, where the agent reads accessibility tree snapshots instead of raw HTML and interacts via stable integer reference numbers. It handles Playwright import validation at construction time, auto-installs Chromium if no browser binary is found, and prefers the system's installed Chrome to avoid downloading a separate Chromium build.

### `browser automation` — 5 article(s)

- [Browser Automation Test Package Initializer](./browser-automation-test-package-initializer.md) — This is the `__init__.py` for the `tests/test_browser/` package, marking the browser automation tests as a proper Python package so pytest can discover and import them as a cohesive module group. The file contains no code or fixtures — its presence enables relative imports between the four browser test modules.
- [Browser Session Management Tests: Lifecycle, Idle Cleanup, Singleton, and Concurrency](./browser-session-management-tests-lifecycle-idle-cleanup-singleton-and-concurrenc.md) — This test suite validates `BrowserSessionManager`, which maintains a pool of named `BrowserSession` objects backed by individual `BrowserDriver` instances. Tests cover session creation, reuse, idle timeout cleanup, graceful close, the global singleton accessor, and thread-safety under concurrent `get_or_create` calls.
- [Browser Snapshot Generator Tests: Accessibility Tree to Agent-Readable Text Conversion](./browser-snapshot-generator-tests-accessibility-tree-to-agent-readable-text-conve.md) — This test suite validates `SnapshotGenerator`, which converts Playwright's accessibility tree JSON into a compact, ref-tagged text representation that AI agents can read and reason about. Tests cover the full rendering pipeline: interactive element ref assignment, non-interactive element handling, heading/list/image rendering, hidden element filtering, password field masking, and the `RefMap` reference number system.
- [BrowserDriver Tests: Playwright Wrapper for Navigation, Interaction, and Lifecycle Management](./browserdriver-tests-playwright-wrapper-for-navigation-interaction-and-lifecycle-.md) — This test suite validates `BrowserDriver`, PocketPaw's thin wrapper around the Playwright async API. Tests cover initialization modes (headless/headful), async context manager lifecycle, browser launch and viewport configuration, all navigation and interaction operations (navigate, click, type, scroll), snapshot generation, screenshot capture, and defensive checks for using the driver before it's launched.
- [BrowserTool Integration Tests: Agent-Facing Browser Interface, Actions, Error Handling, and Session Management](./browsertool-integration-tests-agent-facing-browser-interface-actions-error-handl.md) — This test suite validates `BrowserTool`, the agent-facing wrapper that exposes browser automation as a single tool with an action-dispatch interface. Tests cover the tool's metadata contract (name, trust level, JSON schema), all seven supported actions (navigate, click, type, scroll, snapshot, screenshot, close), input validation, error propagation, and multi-session isolation.

### `browser-automation` — 1 article(s)

- [BrowserTool: Playwright-Powered Web Automation with Accessibility Snapshots](./browsertool-playwright-powered-web-automation-with-accessibility-snapshots.md) — `BrowserTool` exposes browser automation to PocketPaw's agent via a Playwright-backed session manager, using accessibility tree snapshots (rather than raw HTML or pixel screenshots) for element identification — a technique that dramatically reduces token usage while maintaining LLM-parseable page structure.

### `bug fixes` — 1 article(s)

- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.

### `bug regression` — 3 article(s)

- [File Browser Hidden File Ordering Bug Regression Tests](./file-browser-hidden-file-ordering-bug-regression-tests.md) — This test file reproduces and documents a bug in `handle_file_browse` where applying the `items[:50]` slice before the hidden-file filter caused all visible files to disappear from results when a directory contained 50 or more dot-prefixed items. The tests were created on 2026-02-12 to lock in the correct behavior — filter first, then slice.
- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.

### `bug reproduction` — 1 article(s)

- [Task Status Persistence Tests: Reproducing and Fixing a FastAPI Body vs Query Param Bug](./task-status-persistence-tests-reproducing-and-fixing-a-fastapi-body-vs-query-par.md) — Tests that reproduce and validate the fix for a bug where task status updates via the API did not persist — caused by FastAPI treating a bare `str` parameter as a query param while the frontend sent it as a JSON body. Also validates that `projectTasks` lists are updated alongside the main tasks list.

### `bug-fix` — 2 article(s)

- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.
- [Widget Event Payload Definitions and Co-occurrence Signature Utilities](./widget-event-payload-definitions-and-co-occurrence-signature-utilities.md) — Defines the five canonical action names for the widget journal projection domain, their payload builders, and the stable co-occurrence signature algorithm that fixes a token-sort ordering bug shipped in PR #942. All constants are pinned at the module level so the projection, policy, store, and tests share a single import path.

### `build-tooling` — 2 article(s)

- [Desktop Launcher Build Pipeline: PyInstaller, Code Signing, DMG, and Inno Setup](./desktop-launcher-build-pipeline-pyinstaller-code-signing-dmg-and-inno-setup.md) — The build script packages the PocketPaw desktop launcher into a distributable native application for macOS and Windows. It chains icon generation, PyInstaller bundling, platform-specific code signing, DMG creation for macOS, and Inno Setup invocation for Windows into a single `build()` function.
- [Platform Icon Generator: .ico and .icns from a Single PNG Source](./platform-icon-generator-ico-and-icns-from-a-single-png-source.md) — The `make_icons.py` script generates platform-specific application icon files for Windows (`.ico`) and macOS (`.icns`) from a single source `icon.png`. It uses the native macOS `iconutil` tool when available and falls back to a Pillow-based implementation for cross-platform compatibility.

### `built-in tools` — 6 article(s)

- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.
- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.
- [Pocket Tool Test Suite: Widget Lifecycle, Legacy Conversion, and UISpec Contracts](./pocket-tool-test-suite-widget-lifecycle-legacy-conversion-and-uispec-contracts.md) — Comprehensive tests for `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — the three built-in tools that let AI agents build and mutate dashboard "pockets" in PocketPaw. The suite enforces spec versioning (v1.0 UISpec vs v2.0 widget array), backward compatibility of the `name→title` alias, and structural contracts on every tool's JSON output.
- [Speech-to-Text Tool Tests: Schema, File Jail, Multi-Provider, and Error Handling](./speech-to-text-tool-tests-schema-file-jail-multi-provider-and-error-handling.md) — Tests for PocketPaw's `SpeechToTextTool`, covering schema validation, file path jailing for security, size limits, successful transcription with both OpenAI Whisper and ElevenLabs providers, language parameter forwarding, and graceful handling of missing API keys and provider errors.
- [Spotify Tool Tests: Schema Validation, Auth Guards, and Playback Control](./spotify-tool-tests-schema-validation-auth-guards-and-playback-control.md) — Tests for PocketPaw's Spotify integration tools covering schema correctness, authentication failure handling, playback action validation, and successful API interactions. Ensures the tools surface clear errors when credentials are absent and route API calls correctly when authenticated.
- [WebSearchTool Test Suite: Multi-Provider Search Validation](./websearchtool-test-suite-multi-provider-search-validation.md) — This test module validates the `WebSearchTool` built-in tool in PocketPaw, covering provider dispatch, API key guards, HTTP error handling, result clamping, and the parallel-search variant. It ensures the tool behaves safely and predictably across Tavily, Brave, and Parallel AI backends.

### `builtin tools` — 15 article(s)

- [Fabric Tools: Ontology Query, Creation, and Statistics](./fabric-tools-ontology-query-creation-and-statistics.md) — The Fabric tools module exposes three agent-facing tools — `FabricQueryTool`, `FabricCreateTool`, and `FabricStatsTool` — that allow the agent to interact with PocketPaw's Fabric ontology: a graph-based knowledge store for objects and their relationships. All three tools use lazy imports and silent telemetry to avoid circular dependencies and ensure tool calls never fail due to missing enterprise modules or sick tracing infrastructure.
- [Filesystem Tools: Read, Write, List, and Edit Files](./filesystem-tools-read-write-list-and-edit-files.md) — The `filesystem.py` module provides four foundational `BaseTool` subclasses — `ReadFileTool`, `WriteFileTool`, `ListDirTool`, and `EditFileTool` — that give the PocketPaw agent controlled access to the local filesystem. All path operations go through an `is_safe_path` guard that enforces the configured `file_jail_path` boundary, preventing agents from reading or writing outside the permitted sandbox.
- [Gmail Tools: Search, Read, Send, Label, and Batch Modify Emails](./gmail-tools-search-read-send-label-and-batch-modify-emails.md) — The `gmail.py` module provides eight `BaseTool` subclasses covering the full Gmail management surface: `GmailSearchTool`, `GmailReadTool`, `GmailSendTool`, `GmailListLabelsTool`, `GmailCreateLabelTool`, `GmailModifyTool`, `GmailTrashTool`, and `GmailBatchModifyTool`. All tools carry `trust_level = "high"` because email access is sensitive and requires explicit OAuth authorization; the batch modify tool exists specifically to reduce API round-trips when applying label changes to many messages.
- [Google Docs Tools: Read, Create, and Search Documents](./google-docs-tools-read-create-and-search-documents.md) — The `gdocs.py` module provides three `BaseTool` subclasses — `DocsReadTool`, `DocsCreateTool`, and `DocsSearchTool` — that allow the PocketPaw agent to read, create, and search Google Docs documents via the Google Docs API. A helper function `_parse_doc_id` normalizes both raw document IDs and full Google Docs URLs so the agent does not need to extract the ID manually.
- [Google Drive Tools: List, Download, Upload, and Share Files](./google-drive-tools-list-download-upload-and-share-files.md) — The `gdrive.py` module exposes four `BaseTool` subclasses — `DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, and `DriveShareTool` — giving the PocketPaw agent full CRUD and sharing access to Google Drive. A `_GDRIVE_ROLES` frozenset enforces valid sharing permissions at the tool layer before the API call is made, preventing invalid-role errors from propagating to the user.
- [Health Diagnostic Tools: System Checks, Error Logs, and Config Doctor](./health-diagnostic-tools-system-checks-error-logs-and-config-doctor.md) — The `health.py` module provides three diagnostic `BaseTool` subclasses — `HealthCheckTool`, `ErrorLogTool`, and `ConfigDoctorTool` — that give the PocketPaw agent access to the runtime health engine, persistent error log, and configuration validator. These tools exist so the agent can self-diagnose problems when a user reports something is broken, rather than returning a generic error and asking the user to check logs manually.
- [ImageGenerateTool: Text-to-Image Generation via Google Gemini](./imagegeneratetool-text-to-image-generation-via-google-gemini.md) — The `ImageGenerateTool` gives the PocketPaw agent the ability to generate images from text prompts using the Google Gemini image generation API (internally called "Nano Banana"). Generated images are saved to a deterministic directory under the PocketPaw config folder with UUID-based filenames, and the tool returns the local file path so the agent can chain the result with `OpenExplorerTool` or other file-handling tools.
- [InstallPackageTool: Guardian-Reviewed pip Package Installation](./installpackagetool-guardian-reviewed-pip-package-installation.md) — The `InstallPackageTool` allows the PocketPaw agent to install Python packages via pip at runtime, subject to two security layers: a strict regex that validates the package spec format before any subprocess is spawned, and a Guardian AI review that checks the package name for typosquatting and malicious intent before installation proceeds. It runs with `trust_level = "elevated"` because it can permanently modify the Python environment.
- [Instinct Tools: Action Proposal, Pending Queue, and Decision Audit](./instinct-tools-action-proposal-pending-queue-and-decision-audit.md) — The `instinct_tools.py` module provides three `BaseTool` subclasses — `InstinctProposeTool`, `InstinctPendingTool`, and `InstinctAuditTool` — that form the agent-facing surface of PocketPaw's human-in-the-loop decision pipeline. The agent proposes actions rather than executing them autonomously; humans approve or reject from a queue; all decisions are recorded in an audit log the agent can query.
- [InstinctCorrectionsTool: Agent-Facing Surface for Human Edit History](./instinctcorrectionstool-agent-facing-surface-for-human-edit-history.md) — The `InstinctCorrectionsTool` (tool name: `instinct_corrections`) lets an agent fetch the history of human corrections applied to previously proposed actions within a pocket before generating its next proposal. By loading past edits into context, the agent can match the user's preferred tone, thresholds, and style without requiring repeated instruction — effectively turning human corrections into implicit few-shot examples for future drafts.
- [Memory Tools: Remember, Recall, and Forget Long-Term Agent Memory](./memory-tools-remember-recall-and-forget-long-term-agent-memory.md) — The `memory.py` module provides three `BaseTool` subclasses — `RememberTool`, `RecallTool`, and `ForgetTool` — that expose PocketPaw's long-term memory manager to the agent, enabling it to save facts about the user, retrieve them in future sessions, and delete outdated or incorrect memories. These tools are the agent-facing surface of the memory manager, which persists memories across session boundaries independently of the LLM's context window.
- [OCRTool: Multi-Engine Text Extraction from Images and PDFs](./ocrtool-multi-engine-text-extraction-from-images-and-pdfs.md) — The `OCRTool` extracts text from images and PDFs using three interchangeable OCR backends — OpenAI Vision (GPT-4o), Sarvam Vision (specialized for 23 Indian languages), and Tesseract (offline fallback). All path operations go through `is_safe_path` to enforce the file jail, and outputs are saved to a dedicated OCR output directory so extracted text is recoverable after the tool call completes.
- [OpenExplorerTool: Desktop File Explorer Integration](./openexplorertool-desktop-file-explorer-integration.md) — The `OpenExplorerTool` lets a PocketPaw agent open any file or folder in the user's native desktop file manager after completing a task. It delegates the actual OS interaction to the dashboard REST API rather than calling OS APIs directly, which makes it work identically whether the tool is invoked in-process via the tool bridge or from a CLI subprocess spawned by the Claude SDK.
- [Pocket Tools: Create, Add Widget, and Remove Widget for Ripple UI Specs](./pocket-tools-create-add-widget-and-remove-widget-for-ripple-ui-specs.md) — The `pocket.py` module provides three `BaseTool` subclasses — `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — that allow the agent to generate and modify Ripple UniversalSpec JSON structures for pocket workspaces. The module handles three generations of the spec format (flat widget arrays, single-pane UISpec trees, and multi-pane pane-level UISpec trees) and maintains a display-type-to-Ripple-widget mapping for backward compatibility with legacy spec fields.
- [RunPythonTool: Guardian-Reviewed Sandboxed Python Execution](./runpythontool-guardian-reviewed-sandboxed-python-execution.md) — The `RunPythonTool` lets the PocketPaw agent execute arbitrary Python code in a sandboxed subprocess, subject to a Guardian AI safety review before execution. The sandbox uses the configured `file_jail_path` as its working directory, and UUID-based script filenames prevent filename collisions when multiple executions overlap. Trust level is `elevated` because arbitrary code execution is the most powerful and dangerous capability an agent can have.

### `bus` — 6 article(s)

- [Channel-Aware Markdown Formatting and LLM Prompt Hints](./channel-aware-markdown-formatting-and-llm-prompt-hints.md) — format.py provides two capabilities: a `convert_markdown()` function that translates standard Markdown output from the LLM into each channel's native formatting syntax, and a `CHANNEL_FORMAT_HINTS` dictionary that injects per-channel formatting instructions into the LLM system prompt so the model generates natively formatted text in the first place.
- [Channel Notifier for Autonomous Outbound Alerts](./channel-notifier-for-autonomous-outbound-alerts.md) — notifier.py provides a `notify()` function that lets any part of PocketPaw push a message to configured notification channels without knowing which adapters are active. It parses `channel:chat_id` target strings, resolves them to `Channel` enum values, and publishes `OutboundMessage` events to the bus.
- [Cross-Channel Slash Command Handler](./cross-channel-slash-command-handler.md) — CommandHandler intercepts slash commands from any channel (Telegram, Slack, WhatsApp, CLI, etc.) and processes them directly without invoking the AI agent backend. It manages session lifecycle operations — creating, listing, resuming, renaming, and deleting conversation sessions — as well as runtime configuration of backend, model, and tool settings.
- [Media Download Utility for Channel Adapters](./media-download-utility-for-channel-adapters.md) — media.py provides a shared `MediaDownloader` singleton and related utilities that all channel adapters use to save incoming media attachments to local disk. It handles collision-free filename generation, file size enforcement, authenticated downloads, and MIME type guessing.
- [Message Bus: Central Routing Hub for All Channel Communication](./message-bus-central-routing-hub-for-all-channel-communication.md) — MessageBus is the central publish-subscribe hub in PocketPaw that decouples channel adapters from the agent loop. Adapters publish inbound messages to a bounded asyncio queue; the agent loop consumes them. Outbound messages are fanned out to registered adapter callbacks. System events flow separately to dashboard and monitoring subscribers.
- [Message Bus Event Types: Channel, InboundMessage, OutboundMessage, SystemEvent](./message-bus-event-types-channel-inboundmessage-outboundmessage-systemevent.md) — events.py defines the four core data types that flow through PocketPaw's message bus: the Channel enum identifying communication platforms, InboundMessage for received messages, OutboundMessage for agent replies, and SystemEvent for internal runtime signals.

### `business logic` — 2 article(s)

- [Agents Domain Business Logic Service](./agents-domain-business-logic-service.md) — The `AgentService` is the stateless business logic layer for the agents domain, handling agent creation (with slug uniqueness enforcement and eager soul materialisation), updates, discovery, and the new scope get/set operations introduced for the ScopePicker feature.
- [Workspace Service — Business Logic for Workspaces, Members, and Invites](./workspace-service-business-logic-for-workspaces-members-and-invites.md) — The `WorkspaceService` class is a stateless async service that encapsulates all business logic for the workspace domain, including workspace lifecycle, member management, invite flow, and realtime event emission. It enforces several data-level invariants (slug uniqueness, seat limits, owner demotion guard) that cannot be expressed as route-level permission checks.

### `business-api` — 1 article(s)

- [WhatsApp Business Cloud API Adapter](./whatsapp-business-cloud-api-adapter.md) — WhatsAppAdapter connects PocketPaw to the official Meta WhatsApp Business Cloud API, receiving messages via webhook callbacks verified with HMAC and sending replies via the Graph API. It handles the two-step media download flow and sends automatic read receipts.

### `bypass resistance` — 1 article(s)

- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.

### `caching` — 4 article(s)

- [AGENTS.md Loader: Git-Aware File Discovery with Mtime Caching](./agentsmd-loader-git-aware-file-discovery-with-mtime-caching.md) — `AgentsMdLoader` walks the directory tree upward from a starting path — stopping at git repository roots — to find the nearest AGENTS.md file, parses it into a structured `AgentsMd` object, and caches results by `(path, mtime)` so repeated calls within a session cost nothing. A 32 KiB file size cap prevents oversized AGENTS.md files from bloating the agent's system prompt.
- [AudienceResolver Behavioral Tests: Fan-Out Logic, Caching, and Edge Cases](./audienceresolver-behavioral-tests-fan-out-logic-caching-and-edge-cases.md) — These tests verify the complete behavioral contract of AudienceResolver — determining which user IDs receive each event type, including removed-member inclusion, invite routing to admins plus invitee, TTL caching with manual invalidation, session deduplication, and empty-list fallback for unknown event types.
- [AudienceResolver: Event-to-Recipient Mapping with Short-TTL Caching](./audienceresolver-event-to-recipient-mapping-with-short-ttl-caching.md) — AudienceResolver translates a typed event into the list of user IDs that should receive it over WebSocket, applying domain-specific fan-out rules for groups, messages, workspace membership, sessions, agent streams, notifications, and presence. Member lookups are cached with a configurable short TTL to avoid hammering the database on high-frequency events, and the cache can be invalidated selectively when membership changes.
- [Parallel File Tree Builder with TTL Cache](./parallel-file-tree-builder-with-ttl-cache.md) — Implements the parallel fan-out logic that queries all registered providers' mount lists simultaneously, merges them into a hierarchical `FolderNode` tree, and applies ABAC mount-level filtering. `CachedTreeBuilder` wraps this with a per-user, per-workspace TTL cache to avoid hammering providers on repeated tree requests.

### `cancellation` — 1 article(s)

- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.

### `capability flags` — 1 article(s)

- [Agent Backend Protocol: Capability Flags, BackendInfo, and Identity Contract Tests](./agent-backend-protocol-capability-flags-backendinfo-and-identity-contract-tests.md) — This test suite validates the core contracts of PocketPaw's agent backend abstraction layer, covering the `_DEFAULT_IDENTITY` fallback string, the `Capability` flag enum, the immutable `BackendInfo` descriptor, and the `AgentBackend.run()` method signature. Together these tests enforce the interface that every backend adapter must satisfy to be a valid plugin in PocketPaw's multi-backend architecture.

### `catalog` — 1 article(s)

- [PawKit Catalog: Registry of Installable Dashboard Presets](./pawkit-catalog-registry-of-installable-dashboard-presets.md) — The PawKit Catalog is a curated in-memory registry of pre-configured PawKit types that users can browse and install from the Pocket Store. Each entry describes a dashboard preset with ID, display name, category, Lucide icon, and preview text — without bundling the full YAML configuration.

### `channel adapter` — 1 article(s)

- [WhatsApp Gateway: Standalone Webhook Server for WhatsApp Bot](./whatsapp-gateway-standalone-webhook-server-for-whatsapp-bot.md) — The WhatsApp gateway module creates a minimal FastAPI application with Meta webhook verification and message-delivery endpoints, then runs it under uvicorn as a standalone service. It delegates all WhatsApp message handling to `WhatsAppAdapter`, keeping the gateway thin and the adapter reusable across deployment modes.

### `channel adapters` — 15 article(s)

- [Channel-Aware Markdown Formatting Tests](./channel-aware-markdown-formatting-tests.md) — This test module validates `convert_markdown`, PocketPaw's channel-aware formatter that translates LLM-generated markdown into the native formatting of each messaging platform. Each channel has its own rendering rules — WhatsApp uses `*bold*`, Slack uses `<url|label>` hyperlinks, Signal strips all formatting — and the tests prevent these channel-specific transformations from regressing.
- [Channels API Tests: Status, Config Save, Toggle, and Extras Check](./channels-api-tests-status-config-save-toggle-and-extras-check.md) — This test file covers PocketPaw's `/api/v1/channels` router, which exposes status, configuration, and lifecycle management for messaging channel adapters such as Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It also covers `/extras/check`, the endpoint that detects whether optional channel dependencies are installed.
- [Google Chat Channel Adapter Tests](./google-chat-channel-adapter-tests.md) — This test module provides full coverage for `GoogleChatAdapter`, which connects PocketPaw to Google Chat via webhook or Pub/Sub mode. It covers initialization, webhook message handling (including slash commands and space filtering), streaming message buffering, error recovery, `MessageBus` integration, and adapter lifecycle management.
- [Headless Channel Runners: Multi-Adapter CLI Mode for PocketPaw](./headless-channel-runners-multi-adapter-cli-mode-for-pocketpaw.md) — `headless.py` provides the CLI runtime functions that launch PocketPaw in headless server mode — either as a single Telegram bot or as multiple channel adapters (Discord, Slack, WhatsApp, Signal, Matrix, Teams, Google Chat) sharing one `AgentLoop` and message bus. It was extracted from `__main__.py` to keep the entry point thin while keeping channel-setup complexity isolated.
- [Matrix Channel Adapter: Message Routing, Streaming, Error Recovery, and Bus Integration](./matrix-channel-adapter-message-routing-streaming-error-recovery-and-bus-integrat.md) — The Matrix adapter test suite (Sprint 21) validates PocketPaw's Matrix channel integration, covering initial-sync message filtering, room authorization, streaming with edit-in-place, MessageBus subscription lifecycle, and error isolation. The `matrix-nio` library is fully mocked as an optional dependency, allowing tests to run without a live Matrix homeserver.
- [Message Bus Tests: Inbound/Outbound Pub-Sub, Multi-Subscriber Fan-out, and Channel Routing](./message-bus-tests-inboundoutbound-pub-sub-multi-subscriber-fan-out-and-channel-r.md) — This test suite validates PocketPaw's internal message bus, which decouples channel adapters (Telegram, Discord, WhatsApp, CLI) from the core agent logic using an async publish-subscribe queue. Tests cover inbound message queueing, outbound pub-sub subscription and delivery, multi-subscriber fan-out, unsubscribe, adapter integration, broadcast to all channels, and per-channel filtering.
- [Microsoft Teams Channel Adapter Tests: Messaging, Tenant Filtering, and Error Recovery](./microsoft-teams-channel-adapter-tests-messaging-tenant-filtering-and-error-recov.md) — Tests for PocketPaw's Microsoft Teams channel adapter, covering initialisation, message processing, stream buffering, tenant-based filtering, bus integration, lifecycle management, and error recovery. The `botbuilder-core` dependency is mocked since it is optional.
- [Neonize WhatsApp Channel Adapter: Mocking, Streaming, and Bus Integration](./neonize-whatsapp-channel-adapter-mocking-streaming-and-bus-integration.md) — Tests for the NeonizeAdapter, which connects PocketPaw to WhatsApp Personal via the neonize library. Covers auto-installation of the optional dependency, message send/receive lifecycle, stream buffering, QR code exposure, and bus subscription.
- [Signal Channel Adapter Tests — Init, Lifecycle, Message Handling, and Bus Integration](./signal-channel-adapter-tests-init-lifecycle-message-handling-and-bus-integration.md) — This Sprint 20 test file validates `SignalAdapter`, PocketPaw's channel adapter for Signal messenger. Tests cover adapter configuration defaults, start/stop lifecycle, inbound message parsing with phone number authorization, outbound message sending with streaming support, error recovery, and MessageBus integration.
- [Slack Channel Adapter: Integration Tests for Messaging, Streaming, and Auth](./slack-channel-adapter-integration-tests-for-messaging-streaming-and-auth.md) — Tests for PocketPaw's Slack channel adapter, covering message send/receive, threaded replies, stream buffering, channel filtering, and token validation. Uses lightweight mock classes to avoid a real Slack dependency while exercising the full adapter surface.
- [Telegram Channel Adapter Tests: Typing Indicators, Smart Buffering, and Voice Routing](./telegram-channel-adapter-tests-typing-indicators-smart-buffering-and-voice-routi.md) — Tests for PocketPaw's Telegram adapter covering typing indicator lifecycle, smart stream buffering with rate limiting, markdown formatting on flush, voice media routing, and the complete streaming cycle from first chunk to final message. The `python-telegram-bot` library is mocked as an optional dependency.
- [Telegram Channel API Tests — Configuration Status and Pairing State](./telegram-channel-api-tests-configuration-status-and-pairing-state.md) — This test module covers the two read-only Telegram status endpoints: `/api/v1/telegram/status` (reports whether a bot token and allowed user ID are configured) and `/api/v1/telegram/pairing-status` (reports live pairing state from a dashboard-level in-memory dict). Both endpoints inform the dashboard whether the Telegram channel is ready to receive messages.
- [Telegram Forum Topics Support: Chat ID Parsing and Session Key Isolation](./telegram-forum-topics-support-chat-id-parsing-and-session-key-isolation.md) — Tests for Telegram Group Topics (forum supergroups) support in the PocketPaw Telegram adapter, covering the `_parse_chat_id` utility that splits composite chat IDs into chat and topic components, and session key generation that ensures different forum topics produce different session keys.
- [Webhook Adapter Test Suite: Inbound Payload Handling, Sync Mode, and Send Routing](./webhook-adapter-test-suite-inbound-payload-handling-sync-mode-and-send-routing.md) — This module tests the `WebhookAdapter`, PocketPaw's generic inbound HTTP webhook channel. It covers slot configuration, payload normalization, sync vs. async dispatch modes, future-based response resolution, and streaming accumulation.
- [WhatsApp Adapter Test Suite: Webhook Verification, Lifecycle, and Message Sending](./whatsapp-adapter-test-suite-webhook-verification-lifecycle-and-message-sending.md) — This module tests PocketPaw's `WhatsAppAdapter`, covering the Meta webhook verification handshake, adapter lifecycle (`start`/`stop`), outbound message delivery, allowed-number filtering, inbound message routing, and media handling. It ensures the adapter correctly implements the WhatsApp Cloud API protocol.

### `channel management` — 2 article(s)

- [Channel Autostart Tests: Per-Channel Boot Control, Dashboard Startup, API Integration, and Config Persistence](./channel-autostart-tests-per-channel-boot-control-dashboard-startup-api-integrati.md) — This test suite validates PocketPaw's per-channel autostart feature, which lets operators control which channel adapters automatically start when the dashboard boots. Tests cover the `_channel_autostart_enabled()` helper, startup behavior with mixed enabled/disabled channels, the REST API endpoints for reading and saving autostart config, and round-trip persistence to disk.
- [Extras Install and Channel Dependency Endpoint Tests](./extras-install-and-channel-dependency-endpoint-tests.md) — This test module covers the `GET /api/extras/check` and `POST /api/extras/install` dashboard endpoints that manage optional channel adapter dependencies. It validates the allowlist-based security model that prevents arbitrary package installation and tests the `missing_dep` fallback path when a channel adapter fails to start due to an ImportError.

### `channel notifier` — 1 article(s)

- [Channel Notifier Tests: Multi-Target Message Dispatch, Validation, and Settings-Driven Defaults](./channel-notifier-tests-multi-target-message-dispatch-validation-and-settings-dri.md) — This test suite validates `notify()`, PocketPaw's high-level function for pushing outbound messages to one or more channels. Tests cover multi-target fan-out, graceful handling of malformed target strings, unknown channel names, empty target lists, settings-driven default targets, chat IDs containing colons, and delivery across all supported channels.

### `channel-adapters` — 19 article(s)

- [Channel Adapter Base Classes and Auto-Install Utility](./channel-adapter-base-classes-and-auto-install-utility.md) — This module defines ChannelAdapter (a structural Protocol) and BaseChannelAdapter (an abstract base class with lifecycle management), providing the contracts that all channel adapters must satisfy. It also contains the auto_install() utility that lets adapters install their optional Python dependencies at runtime using uv or pip, with a restart-required path for packages that cannot be reloaded in-process.
- [Channel-Aware Markdown Formatting and LLM Prompt Hints](./channel-aware-markdown-formatting-and-llm-prompt-hints.md) — format.py provides two capabilities: a `convert_markdown()` function that translates standard Markdown output from the LLM into each channel's native formatting syntax, and a `CHANNEL_FORMAT_HINTS` dictionary that injects per-channel formatting instructions into the LLM system prompt so the model generates natively formatted text in the first place.
- [Deep Work Human Task Router — Multi-Channel Notification Fanout](./deep-work-human-task-router-multi-channel-notification-fanout.md) — HumanTaskRouter bridges the Deep Work execution layer and the user by pushing human-required tasks and project lifecycle events to all configured messaging channels via MessageBus broadcast. It covers four notification types: human tasks needing action, agent-completed tasks awaiting review, plans ready for approval, and project completion summaries.
- [DeliverArtifactTool: Secure File Delivery from Agent Workspace to User Channel](./deliverartifacttool-secure-file-delivery-from-agent-workspace-to-user-channel.md) — `DeliverArtifactTool` lets PocketPaw's agent send generated or downloaded files (images, PDFs, audio) to the user through their current channel — Slack, Discord, or the web dashboard — after verifying the file is within the agent's sandboxed working directory.
- [Discord Channel Adapter — discli Subprocess Bridge](./discord-channel-adapter-discli-subprocess-bridge.md) — DiscliAdapter connects PocketPaw to Discord by spawning a discli serve subprocess and communicating with it over stdin/stdout JSONL, rather than using discord.py directly. This architecture isolates the Discord event loop in a separate process, preventing it from interfering with PocketPaw's asyncio loop, and lets discli handle rate limiting, reconnection, and slash command registration independently.
- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.
- [Discord Gateway — Bot Initialization and MessageBus Integration](./discord-gateway-bot-initialization-and-messagebus-integration.md) — discord_gateway.py provides the `run_discord_bot` entry point that wires the Discord adapter to the MessageBus and starts the AgentLoop for the Discord channel. It translates all Discord-specific configuration from Settings into the adapter, then hands off to the agent infrastructure.
- [DiscordCLITool: Full Discord Management via the discli CLI Subprocess](./discordclitool-full-discord-management-via-the-discli-cli-subprocess.md) — `DiscordCLITool` wraps the `discli` command-line tool to give PocketPaw's agent comprehensive Discord server management capabilities — messaging, channel management, moderation, polls, webhooks, and more — all through a single tool that constructs safe subprocess invocations with shell-split argument parsing.
- [Generic Inbound Webhook Adapter with Sync and Async Modes](./generic-inbound-webhook-adapter-with-sync-and-async-modes.md) — WebhookAdapter allows any external service — GitHub, Zapier, n8n, Home Assistant, cron scripts — to push events into PocketPaw via HTTP POST. It supports both fire-and-forget (async) mode and a synchronous request-response mode where the HTTP call blocks until the agent produces a reply.
- [Google Chat Channel Adapter — Webhook and Pub/Sub Modes](./google-chat-channel-adapter-webhook-and-pubsub-modes.md) — GoogleChatAdapter connects PocketPaw to Google Workspace Chat via two modes: webhook (for setups where the PocketPaw dashboard receives HTTP POST events from Google) and pubsub (for server-side polling via Google Cloud Pub/Sub). Both modes normalise incoming events to InboundMessage on the bus and use the Google Chat REST API to send replies, with markdown converted to Google Chat's Card format.
- [Matrix Channel Adapter — Long-Polling with Streaming via Message Edits](./matrix-channel-adapter-long-polling-with-streaming-via-message-edits.md) — MatrixAdapter connects PocketPaw to the Matrix protocol using matrix-nio's AsyncClient, supporting both access token and password authentication. Streaming agent responses are delivered by editing the initial message in place using Matrix's m.replace relation, rate-limited to one edit per 1.5 seconds to avoid homeserver rate limiting.
- [Microsoft Teams Channel Adapter via Bot Framework SDK](./microsoft-teams-channel-adapter-via-bot-framework-sdk.md) — TeamsAdapter integrates PocketPaw with Microsoft Teams using the Bot Framework SDK, receiving messages via an internal aiohttp webhook server and sending replies through the Bot Framework's `send_activity` API. Streaming is emulated by editing a placeholder message with `update_activity()`.
- [Signal Channel Adapter via signal-cli REST API](./signal-channel-adapter-via-signal-cli-rest-api.md) — SignalAdapter integrates PocketPaw with Signal by polling a self-hosted signal-cli REST API instance every 2 seconds for inbound messages and sending replies via its HTTP endpoint. No streaming is supported — responses are buffered and sent as a single message when the agent finishes.
- [Slack Bot Gateway: Bootstrapping the PocketPaw Slack Integration](./slack-bot-gateway-bootstrapping-the-pocketpaw-slack-integration.md) — The `slack_gateway.py` module is the top-level entry point for running PocketPaw as a Slack bot, wiring together the message bus, Slack adapter, and agent loop into a single coroutine. It handles clean startup and shutdown so the bot can run reliably as a long-lived async process.
- [Slack Channel Adapter with Socket Mode and Live Streaming](./slack-channel-adapter-with-socket-mode-and-live-streaming.md) — SlackAdapter connects PocketPaw to Slack using Socket Mode, which requires no public webhook URL — the bot maintains a persistent WebSocket connection to Slack's servers. It supports slash commands, DM messages, app mentions, threaded replies, file uploads, and live-edited streaming responses.
- [Telegram Channel Adapter with Streaming, Typing Indicators, and Forum Topic Support](./telegram-channel-adapter-with-streaming-typing-indicators-and-forum-topic-suppor.md) — TelegramAdapter connects PocketPaw to the Telegram Bot API using python-telegram-bot, supporting streaming responses via live message edits, animated typing indicators, media file sending, voice note detection, and Telegram forum topic (Supergroup) thread routing.
- [WebSocket Channel Adapter for the PocketPaw Dashboard](./websocket-channel-adapter-for-the-pocketpaw-dashboard.md) — WebSocketAdapter manages real-time bidirectional communication between PocketPaw's FastAPI backend and its web dashboard frontend, routing messages, streaming chunks, and system events (tool execution, thinking indicators) to the correct connected browser session.
- [WhatsApp Business Cloud API Adapter](./whatsapp-business-cloud-api-adapter.md) — WhatsAppAdapter connects PocketPaw to the official Meta WhatsApp Business Cloud API, receiving messages via webhook callbacks verified with HMAC and sending replies via the Graph API. It handles the two-step media download flow and sends automatic read receipts.
- [WhatsApp Personal Mode Adapter via Neonize](./whatsapp-personal-mode-adapter-via-neonize.md) — NeonizeAdapter connects PocketPaw to WhatsApp using the neonize Python library, which wraps the open-source whatsmeow Go client. It requires only a QR-code scan to pair — no Meta Developer account or public webhook URL is needed.

### `channel-integration` — 2 article(s)

- [Telegram Channel Router: Setup and Pairing Flow](./telegram-channel-router-setup-and-pairing-flow.md) — Implements the three REST endpoints that drive Telegram bot pairing: checking current configuration status, initiating the pairing flow with a bot token, and polling for pairing completion. The router wraps the shared pairing state dict from the dashboard module, adding token validation and graceful handling of missing optional dependencies.
- [Telegram Pairing Schema Contracts](./telegram-pairing-schema-contracts.md) — Defines the Pydantic request and response models used by the Telegram channel pairing flow. These schemas enforce a clean contract between the REST API layer and the bot token validation and QR code generation logic.

### `channels` — 1 article(s)

- [Channel Adapter Schemas — Status, Configuration, and Toggle](./channel-adapter-schemas-status-configuration-and-toggle.md) — The channel schemas define the state and control contracts for PocketPaw's messaging platform adapters (Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, Google Chat). They separate read (status queries) from write (save config, start/stop) operations.

### `chat` — 21 article(s)

- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.
- [Chat Domain Package Entrypoint](./chat-domain-package-entrypoint.md) — The `ee/cloud/chat/__init__.py` file is the public entrypoint for the chat domain, re-exporting the APIRouter so the application can mount the entire chat surface with a single import. It acts as a stable boundary declaration hiding internal module layout.
- [Chat Domain Schema Validation Tests](./chat-domain-schema-validation-tests.md) — This module comprehensively tests all Pydantic schemas in the chat domain — group management, message sending, editing, reactions, WebSocket inbound/outbound messages, cursor pagination, and attachment handling — covering field defaults, length constraints, enum validation, and discriminated union types for WebSocket message dispatch.
- [Chat Router - REST Endpoints and WebSocket Handler](./chat-router-rest-endpoints-and-websocket-handler.md) — The chat router exposes the full `/chat` REST surface and the `/ws/cloud` WebSocket endpoint, both gated behind an enterprise license check. It manages presence lifecycle with a 30-second grace period and dispatches inbound WebSocket messages to the appropriate service methods.
- [Chat Schemas — Request, Response, and Context Models](./chat-schemas-request-response-and-context-models.md) — The chat schemas define the core request and response contracts for PocketPaw's chat API, including file and pocket context injection, camelCase/snake_case aliasing to prevent session ID loss, and the SSE chunk model for streaming responses.
- [Chat Schemas - Request, Response, and WebSocket Message Models](./chat-schemas-request-response-and-websocket-message-models.md) — This module defines all Pydantic models used by the chat domain: REST request bodies, response shapes, cursor pagination, and WebSocket message envelopes. Centralising schemas here decouples validation logic from both the router and service layers.
- [Chat Service - Backward-Compatibility Re-Export Facade](./chat-service-backward-compatibility-re-export-facade.md) — `service.py` is a thin facade that re-exports `GroupService`, `MessageService`, and their helper functions from their new dedicated modules, preserving import compatibility for all code written before the refactor. It carries no logic of its own.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Group Service - Channel and DM Business Logic](./group-service-channel-and-dm-business-logic.md) — The `GroupService` class encapsulates all group and channel CRUD, membership management, agent attachment, and DM creation logic for the chat domain. It coordinates between the Group ODM model, the realtime event bus, and the RBAC/audit guard layer.
- [GroupService Realtime Emit Tests](./groupservice-realtime-emit-tests.md) — This test module verifies that every public mutation in GroupService fires the correct realtime event via the bus emit() call. It also confirms that membership-changing operations correctly invalidate the AudienceResolver group cache so downstream subscribers see fresh data.
- [Message Service - Chat Message CRUD, Reactions, Threads, and Search](./message-service-chat-message-crud-reactions-threads-and-search.md) — The `MessageService` class handles all message lifecycle operations in the chat domain, including sending, editing, deletion, reactions, thread replies, pin management, mention fan-out, and full-text search. It was split from the original `service.py` monolith and extended with agent message creation and workspace-scoped search.
- [MessageService Realtime Emit Tests](./messageservice-realtime-emit-tests.md) — This test module enforces that every public mutation in MessageService fires the correct typed realtime event through emit(). It also includes regression guards ensuring the WebSocket router itself does not perform direct broadcasts that would bypass the event bus.
- [ReadState Model and UnreadService Upsert Tests](./readstate-model-and-unreadservice-upsert-tests.md) — This module tests the ReadState model's default values and the atomic upsert behavior of UnreadService.mark_read and bump_mention against a real in-memory MongoDB. The tests enforce the unique (user, group) index constraint and verify that concurrent operations remain idempotent.
- [Room-Scoped WebSocket Routing Tests](./room-scoped-websocket-routing-tests.md) — This module tests the room-scoped routing layer in ConnectionManager, which limits typing indicators and read receipts to only the sockets currently joined to a specific group. It also verifies that non-members cannot inject typing or read-ack events into rooms they do not belong to.
- [Smoke Test: Pocket Sidebar Session Creation and Chat Persistence](./smoke-test-pocket-sidebar-session-creation-and-chat-persistence.md) — This test mirrors the full flow of PocketPaw's enterprise sidebar: a user creates a Pocket, opens a new session under that Pocket, sends a message, and verifies the message appears in the session history API. It validates that `Session` documents are correctly linked to their parent Pocket in MongoDB.
- [Smoke Test: Session Creation Then Chat Persistence Round-Trip](./smoke-test-session-creation-then-chat-persistence-round-trip.md) — This test validates the core "desired flow": create a session via the API, send a user message, confirm it is stored in MongoDB keyed by the session ID, and retrieve it through the history endpoint. It was written to capture the exact behavior contract for session-scoped message storage.
- [Smoke Test: Session Key Normalization Between Agent Bus and UI Chat History](./smoke-test-session-key-normalization-between-agent-bus-and-ui-chat-history.md) — This test verifies that the colon-delimited session key written by the agent loop (`websocket:X`) is normalized to the underscore form used by the UI (`websocket_X`), so that chat history returned by `GET /api/v1/sessions/{sessionId}/history` actually contains the messages the agent wrote. Without this normalization, the session history API returns empty results despite messages being stored.
- [Unread Service - Per-User Unread Counts and Mention Badges](./unread-service-per-user-unread-counts-and-mention-badges.md) — `UnreadService` tracks per-user, per-group unread message counts and mention badges by combining a `ReadState` watermark document with a live message count query. It provides the data for inbox badges and the visual unread indicator in the sidebar.
- [UnreadService List Unreads Calculation Tests](./unreadservice-list-unreads-calculation-tests.md) — This module tests UnreadService.list_unreads, which computes per-group unread message and mention counts for a user. Tests cover the caught-up state, partial reads, the fresh-user fallback, and the edge case where a ReadState row exists but has no recorded read position.
- [WebSocket Connection Manager - Presence, Rooms, and Typing Indicators](./websocket-connection-manager-presence-rooms-and-typing-indicators.md) — The `ConnectionManager` class maintains the in-memory registry of active WebSocket connections, manages presence state with a 30-second grace window, routes messages to group rooms, and expires typing indicators automatically after 5 seconds. It is the real-time backbone for the chat domain.
- [Workspace Message Search Tests](./workspace-message-search-tests.md) — This test module covers MessageService's workspace-wide message search, verifying that results respect group visibility (channels vs. private rooms), workspace scope isolation, regex-safety of user queries, and server-side result limits. Tests run against an in-memory MongoDB via the shared beanie_memory_db fixture to exercise real Mongo query behavior.

### `chat API` — 2 article(s)

- [Chat API Test Suite: SSE Streaming, Session Bridge, and Request Aliases](./chat-api-test-suite-sse-streaming-session-bridge-and-request-aliases.md) — Covers the full test surface of PocketPaw's `/api/v1/chat` router, including the `_APISessionBridge` async queue, SSE stream/stop endpoints, non-streaming send, media URL rewriting, and camelCase field alias handling. These tests exist to catch silent regressions in the real-time chat path that would break frontend consumers without an obvious error.
- [Cloud Context Threading in Chat API: User ID and Workspace Propagation Tests](./cloud-context-threading-in-chat-api-user-id-and-workspace-propagation-tests.md) — Tests that `resolve_cloud_context` correctly injects `cloud_user_id` and `cloud_workspace_id` into `InboundMessage.metadata` when an authenticated cloud user is present, and leaves those keys absent for anonymous or unauthenticated requests. The test file operates at the unit level — directly calling `_build_inbound_message` and `_send_message` — to avoid the overhead of standing up a full SSE streaming client.

### `chat completion` — 1 article(s)

- [LLMRouter: Lightweight Chat Completion Utility for Non-Agent Callers](./llmrouter-lightweight-chat-completion-utility-for-non-agent-callers.md) — `LLMRouter` is a simple, stateful chat utility for one-off completions — not used by agent backends. It auto-detects the best available backend (Ollama first, then cloud providers), maintains a conversation history list, and provides `clear_history()` for resetting between sessions. Its documented limitations (no streaming, no token counting, unbounded history) make its intended scope explicit.

### `claude` — 1 article(s)

- [Claude Agent SDK Backend — Recommended Default Agent Runtime](./claude-agent-sdk-backend-recommended-default-agent-runtime.md) — Implements `ClaudeSDKBackend`, PocketPaw's primary agent backend that wraps Anthropic's `claude-agent-sdk`. It provides built-in tools (Bash, Read, Write, Edit, Glob, Grep, WebSearch, WebFetch), streaming token delivery, a `PreToolUse` security hook, MCP server support, and headless permission bypass for messaging channels.

### `cli` — 6 article(s)

- [CLI Package Namespace for PocketPaw Subcommands](./cli-package-namespace-for-pocketpaw-subcommands.md) — The `pocketpaw.cli` package namespace serves as the entry point for all PocketPaw CLI subcommands. The `__init__.py` file contains only a module docstring, acting as a namespace marker that groups CLI subcommand modules under a shared package.
- [Paw CLI: Click-Based Command Interface for Project-Level AI](./paw-cli-click-based-command-interface-for-project-level-ai.md) — paw/cli.py implements the full paw CLI using Click with eight commands — init, ask, chat, serve, status, doctor, os, channels — and an async bridge that handles both normal invocation and pytest-asyncio environments. The serve command is a documented placeholder; all other commands are functional.
- [PocketPaw CLI Diagnostics — Doctor, Ollama, and OpenAI-Compatible Health Checks](./pocketpaw-cli-diagnostics-doctor-ollama-and-openai-compatible-health-checks.md) — diagnostics.py contains the three CLI health check implementations extracted from `__main__.py`: `run_doctor` for a full system health report, `check_ollama` for Ollama connectivity and model tool-calling support, and `check_openai_compatible` for OpenAI-compatible endpoint validation. All three produce ANSI-colored terminal output with structured pass/warn/fail indicators.
- [PocketPaw Interactive Installer](./pocketpaw-interactive-installer.md) — A self-contained, single-file installer that guides users through PocketPaw setup with optional rich prompts, falling back to plain text input when the terminal environment cannot support InquirerPy. Supports both interactive and headless (non-interactive) installation profiles, with a robust dependency bootstrap cascade that verifies each step before proceeding.
- [Security Audit CLI: Runtime Health Checks and Permission Repair](./security-audit-cli-runtime-health-checks-and-permission-repair.md) — This module provides an async CLI-callable security auditor that checks file permissions, validates the audit log setup, and optionally scans memory files for PII. It was built as a "Phase 1 Quick Wins" hardening measure to catch misconfigurations before they become vulnerabilities.
- [Tools CLI Dispatcher: Bash-Accessible Tool Invocation for Agent Loops](./tools-cli-dispatcher-bash-accessible-tool-invocation-for-agent-loops.md) — The `pocketpaw.tools.cli` module is a CLI dispatcher that lets any builtin tool be invoked from a Bash shell via `python -m pocketpaw.tools.cli <tool_name> '<json_args>'`. It serves as the bridge between shell-based agent loops (like Claude Code) and PocketPaw's async Python tool ecosystem.

### `client construction` — 1 article(s)

- [AnthropicAdapter: Native Anthropic API Configuration and Client Construction](./anthropicadapter-native-anthropic-api-configuration-and-client-construction.md) — `AnthropicAdapter` implements the `ProviderAdapter` protocol for Anthropic's native API, resolving model and API key from settings and constructing `AsyncAnthropic` clients. It explicitly raises `NotImplementedError` on `build_openai_client()` to enforce that Anthropic-native callers never accidentally get an OpenAI-compatible client.

### `cloud` — 16 article(s)

- [BaseFolderProvider: Default Provider Stub Implementation](./basefolderprovider-default-provider-stub-implementation.md) — Defines `BaseFolderProvider`, the default base class for all file providers, where every operation raises `ProviderUnsupported` unless overridden. This design enforces capability-based provider contracts: concrete providers only implement the operations they actually support, and the error type communicates unsupported operations cleanly to the API layer.
- [Built-in File Providers Package Initialiser](./built-in-file-providers-package-initialiser.md) — The package initialiser for the built-in `FolderProvider` implementations under `ee.cloud.files.providers`. This file acts as a namespace anchor, grouping the concrete provider modules (`base`, `kb`, `uploads`) under a single importable package without adding its own logic.
- [File Mutation Domain Events](./file-mutation-domain-events.md) — Declares the Pydantic event shapes published to the `ee.cloud.realtime` bus whenever a file is added, updated, removed, or moved. These schemas form the Phase 1-2 contract between the files subsystem and future realtime subscribers, ensuring the event bus wire format is stable before the subscriber (Phase 4) is built.
- [Files API Router: Unified Listing and v2 Tree/Browse Endpoints](./files-api-router-unified-listing-and-v2-treebrowse-endpoints.md) — Defines the FastAPI router for the EE `/files` endpoints, including the legacy `GET /files` unified listing endpoint and the v2 tree/browse API built on `ProviderRegistry`. The router factory `build_router` composes the registry, ABAC rules, and request-context factory into a single mountable router.
- [Files Module Public Pydantic Schemas](./files-module-public-pydantic-schemas.md) — Defines the canonical Pydantic data models shared across the entire files subsystem: `Permission`, `RequestContext`, `FileEntry`, `FolderNode`, `MountConfig`, `ResolvedMount`, `Page`, and `SearchQuery`. Validators on these models enforce invariants (absolute paths, namespaced IDs) that providers cannot violate, ensuring the API response shape is consistent regardless of which provider produced it.
- [Files Module Typed Exception Hierarchy](./files-module-typed-exception-hierarchy.md) — Defines the complete typed error hierarchy for PocketPaw's cloud files subsystem. Every exception carries a machine-readable `code` string and a matching HTTP status code so the FastAPI router can translate domain failures into consistent API responses without ad-hoc status mapping.
- [Files RBAC and ABAC Permission Evaluator](./files-rbac-and-abac-permission-evaluator.md) — Implements the two-layer permission model for the files module: RBAC permissions come from providers on a per-entry basis, while ABAC rules apply as a post-filter that can only further restrict visibility. The `PermissionsEvaluator` combines both layers to produce the final UI-facing capability list for each file entry.
- [KbProvider: Knowledge Base File Provider](./kbprovider-knowledge-base-file-provider.md) — Exposes workspace Knowledge Base documents as a browseable file mount in the unified files tree. `KbProvider` delegates reads to a `_KbService` Protocol adapter rather than directly calling the KB FastAPI routes, decoupling the files subsystem from the KB module's current route-and-Go-binary architecture.
- [Knowledge Base Domain Package Initialiser](./knowledge-base-domain-package-initialiser.md) — The package initialiser for the `ee.cloud.kb` Knowledge Base domain, which exposes workspace-scoped KB endpoints including search, ingest, browse, lint, and stats. The comment header documents the package's scope and public surface, serving as the entry point for the KB subsystem.
- [Mount Configuration YAML Loader](./mount-configuration-yaml-loader.md) — Provides `load_mounts`, which reads a `mounts.yaml` file, validates each row into a typed `MountConfig` object, and returns them sorted by `order`. Also exposes `resolve_template` for expanding path templates containing workspace or user variables at request time.
- [Parallel File Tree Builder with TTL Cache](./parallel-file-tree-builder-with-ttl-cache.md) — Implements the parallel fan-out logic that queries all registered providers' mount lists simultaneously, merges them into a hierarchical `FolderNode` tree, and applies ABAC mount-level filtering. `CachedTreeBuilder` wraps this with a per-user, per-workspace TTL cache to avoid hammering providers on repeated tree requests.
- [PocketPaw Compiler Backend Adapter for Knowledge Base](./pocketpaw-compiler-backend-adapter-for-knowledge-base.md) — Implements `PocketPawCompilerBackend`, a `CompilerBackend` Protocol adapter that makes PocketPaw's active agent LLM backend available to the `knowledge_base` package's compiler pipeline. This enables KB article compilation to use whatever AI backend is configured in the agent registry -- Claude, OpenAI, or any other -- without hardcoding a specific model.
- [Provider Registry and Mount Path Router](./provider-registry-and-mount-path-router.md) — Implements `ProviderRegistry`, which maps mount path prefixes to registered `FolderProvider` instances and resolves incoming file paths to the correct provider via longest-prefix matching. The `FolderProvider` Protocol definition here is the canonical duck-typed interface contract all providers must satisfy.
- [S3StorageAdapter: S3-Compatible Cloud Blob Storage](./s3storageadapter-s3-compatible-cloud-blob-storage.md) — S3StorageAdapter implements the StorageAdapter interface against any S3-compatible object store—AWS S3, MinIO, Cloudflare R2, or Wasabi—using the synchronous boto3 library pushed off the event loop via `asyncio.to_thread`. Credentials use the same environment variable names as the companion interacly-backend service, enabling shared-bucket deployments.
- [Unified Files Service: Merging Upload and Drive Sources](./unified-files-service-merging-upload-and-drive-sources.md) — Implements `UnifiedFilesService`, the Cluster E sub-PR 4 facade that merges chat S3 uploads and (stub) Google Drive files into a single flat list for the legacy `GET /files` endpoint. A `_dedupe` function drops duplicates by content identity to prevent the same file appearing twice when sources overlap.
- [UploadsProvider: Personal Upload File Provider with Folder Support](./uploadsprovider-personal-upload-file-provider-with-folder-support.md) — Wraps `MongoFileStore` to expose user-uploaded files and folders as a personal 'My Files' mount in the unified files tree. As the only provider with folder support, it uses a dedicated `folder_store` and renders directory entries with `mime = application/x-directory`.

### `cloud API` — 5 article(s)

- [Download URL Endpoint Tests: TTL Responses, Cross-Workspace Isolation, and Path Traversal Defense](./download-url-endpoint-tests-ttl-responses-cross-workspace-isolation-and-path-tra.md) — Integration tests for the `/uploads/{file_id}/download-url` endpoint added in the EE upload router, verifying that the endpoint returns a time-bounded URL with the original filename, blocks cross-workspace access with 404, rejects path-traversal IDs, and hides soft-deleted files. Uses the same FastAPI test-client pattern as `test_router.py` with header-driven identity injection.
- [EE Upload Router Integration Tests: Roundtrip, Workspace Isolation, Owner-Only Access, and Bulk Partial Success](./ee-upload-router-integration-tests-roundtrip-workspace-isolation-owner-only-acce.md) — Integration tests for the EE uploads FastAPI router covering the full upload-download-delete cycle, cross-workspace 404 enforcement, owner-only read access within a workspace, and the partial-success bulk upload response that distinguishes accepted files from rejected MIME types.
- [Session Schema Tests: Create, Update, and Response Contract Validation](./session-schema-tests-create-update-and-response-contract-validation.md) — Unit tests for the three Pydantic schemas governing session lifecycle in PocketPaw's EE cloud layer — `CreateSessionRequest`, `UpdateSessionRequest`, and `SessionResponse`. The tests pin default values, optional-field behavior, and the nullable `deleted_at` field used for soft-deletion semantics.
- [WebSocket Connection Manager Tests: Multi-Device, Typing Indicators, and Offline Grace Periods](./websocket-connection-manager-tests-multi-device-typing-indicators-and-offline-gr.md) — Tests for `ee.cloud.chat.ws.ConnectionManager`, the in-process registry that tracks active WebSocket connections per user, routes outbound messages, and manages typing indicators with auto-expiry timers. Key behavioral contracts include dead-connection cleanup, multi-device fan-out, idempotent stop-typing, and cancellation of offline grace period tasks on reconnect.
- [Workspace Schema Tests: Slug Validation, Invite Rules, and Member Role Contracts](./workspace-schema-tests-slug-validation-invite-rules-and-member-role-contracts.md) — Tests for the workspace domain Pydantic schemas in PocketPaw's EE layer, covering slug format enforcement, invite role validation, and member role update rules. The slug tests are particularly important because workspace slugs appear in URLs and must be lowercase, hyphen-separated, and free of leading/trailing hyphens.

### `cloud EE` — 30 article(s)

- [ABAC Rule Loader and Evaluator for File Access Control](./abac-rule-loader-and-evaluator-for-file-access-control.md) — The `abac_config.py` module implements an Attribute-Based Access Control (ABAC) engine for the files domain, loading restriction rules from a YAML configuration file and evaluating them against per-request user attributes and file entry tags. Rules can only restrict access - they cannot grant permissions beyond the RBAC baseline.
- [Agent Bridge: Routing Chat Messages to the Agent Pool](./agent-bridge-routing-chat-messages-to-the-agent-pool.md) — The agent bridge connects incoming group-chat `message.sent` events to the PocketPaw agent pool, evaluating each agent's respond mode and streaming responses back over WebSocket. It decouples the chat send path from agent processing by dispatching all work to background tasks, preventing smart-mode LLM calls from blocking message delivery.
- [Auth Service - User Profile and Workspace Management](./auth-service-user-profile-and-workspace-management.md) — A stateless FastAPI service class handling the business logic for user authentication, profile management, and active workspace switching. It keeps route handlers thin by acting as the bridge between HTTP requests and the User ODM model.
- [Browse Mount - Per-Mount Paginated File Listing with ABAC and Capability Derivation](./browse-mount-per-mount-paginated-file-listing-with-abac-and-capability-derivatio.md) — The `browse_mount` function handles a single paginated directory listing request against one virtual mount, applying ABAC filtering and deriving fine-grained capabilities for each visible entry. It is the core read path for the Files Tab browser.
- [Chat Domain Package Entrypoint](./chat-domain-package-entrypoint.md) — The `ee/cloud/chat/__init__.py` file is the public entrypoint for the chat domain, re-exporting the APIRouter so the application can mount the entire chat surface with a single import. It acts as a stable boundary declaration hiding internal module layout.
- [Chat Router - REST Endpoints and WebSocket Handler](./chat-router-rest-endpoints-and-websocket-handler.md) — The chat router exposes the full `/chat` REST surface and the `/ws/cloud` WebSocket endpoint, both gated behind an enterprise license check. It manages presence lifecycle with a 30-second grace period and dispatches inbound WebSocket messages to the appropriate service methods.
- [Chat Schemas - Request, Response, and WebSocket Message Models](./chat-schemas-request-response-and-websocket-message-models.md) — This module defines all Pydantic models used by the chat domain: REST request bodies, response shapes, cursor pagination, and WebSocket message envelopes. Centralising schemas here decouples validation logic from both the router and service layers.
- [Chat Service - Backward-Compatibility Re-Export Facade](./chat-service-backward-compatibility-re-export-facade.md) — `service.py` is a thin facade that re-exports `GroupService`, `MessageService`, and their helper functions from their new dedicated modules, preserving import compatibility for all code written before the refactor. It carries no logic of its own.
- [Cloud DB Module - Backward-Compatible Database Shim](./cloud-db-module-backward-compatible-database-shim.md) — `ee/cloud/db.py` is a one-line backward-compatibility shim that re-exports the database lifecycle functions from the canonical `ee.cloud.shared.db` module. It exists solely to preserve older import paths after the database logic was consolidated into the shared package.
- [Cloud Feature Flags - Always-On Overrides for the EE Product](./cloud-feature-flags-always-on-overrides-for-the-ee-product.md) — The `ee/cloud/features.py` module force-enables features that are optional in the OSS build but required for the cloud EE product to function correctly. Currently it overrides `chat_titles_enabled` to always return `True`, ensuring sidebar titles and realtime UI always work in cloud mode.
- [Cross-Domain Event Handlers: Side Effects Without Direct Coupling](./cross-domain-event-handlers-side-effects-without-direct-coupling.md) — This module wires up application-level event handlers that react to events from other domains — such as invite acceptance, message delivery, pocket sharing, and member removal — without importing those domains directly. It is registered once at app startup and acts as the glue layer for cross-cutting side effects.
- [EE Upload Service: Workspace-Scoped Pipeline over the OSS Upload Engine](./ee-upload-service-workspace-scoped-pipeline-over-the-oss-upload-engine.md) — EEUploadService wraps the OSS UploadService to add workspace scoping, per-file access control, Mongo metadata persistence, and real-time WebSocket events for file lifecycle. It substitutes a null metadata store at the OSS layer so that all persistence flows through MongoDB rather than the JSONL flat file.
- [EE Uploads API Router: Workspace-Scoped Upload and Folder Endpoints](./ee-uploads-api-router-workspace-scoped-upload-and-folder-endpoints.md) — This FastAPI router exposes all upload and folder management endpoints for the EE cloud layer, enforcing workspace scope and per-file access control on every operation. It manages the full file lifecycle — upload, download URL generation, folder CRUD, file move, and soft delete — backed by EEUploadService and FolderStore.
- [FastAPI Dependency Injection for Cloud Routers](./fastapi-dependency-injection-for-cloud-routers.md) — This module provides reusable FastAPI dependency functions that extract identity and workspace context from authenticated requests, and composable action-based RBAC guards. It is the single source of truth for authentication wiring across all cloud EE router endpoints.
- [Files Bootstrap - Composing Providers, Mounts, and Rules into a Router](./files-bootstrap-composing-providers-mounts-and-rules-into-a-router.md) — `bootstrap.py` is the composition root for the files domain: it reads mount configuration, registers the built-in providers (uploads and knowledge base), loads ABAC rules, and returns a fully-configured `APIRouter` ready to be mounted by the application. It is the only place where concrete dependencies are wired together.
- [Files Domain Package - Unified File Browser and Provider Registry](./files-domain-package-unified-file-browser-and-provider-registry.md) — The `ee/cloud/files/__init__.py` package entrypoint assembles and re-exports the complete public API for the files domain: the provider registry, ABAC schema types, pagination primitives, and the router factory function. It is the single import surface consumers use to mount the files subsystem.
- [Group Service - Channel and DM Business Logic](./group-service-channel-and-dm-business-logic.md) — The `GroupService` class encapsulates all group and channel CRUD, membership management, agent attachment, and DM creation logic for the chat domain. It coordinates between the Group ODM model, the realtime event bus, and the RBAC/audit guard layer.
- [In-Process Async Event Bus for Cloud Domain Communication](./in-process-async-event-bus-for-cloud-domain-communication.md) — This module implements a lightweight in-process pub/sub event bus that lets cloud domains react to events from other domains without direct imports. It is the backbone for cross-domain side effects in the PocketPaw EE layer, used by the agent bridge, event handlers, and any future domain that needs to observe another.
- [Message Service - Chat Message CRUD, Reactions, Threads, and Search](./message-service-chat-message-crud-reactions-threads-and-search.md) — The `MessageService` class handles all message lifecycle operations in the chat domain, including sending, editing, deletion, reactions, thread replies, pin management, mention fan-out, and full-text search. It was split from the original `service.py` monolith and extended with agent message creation and workspace-scoped search.
- [MongoDB Connection and Beanie ODM Initialization](./mongodb-connection-and-beanie-odm-initialization.md) — This module manages the lifecycle of the MongoDB connection and initializes the Beanie ODM with all document models required by the cloud EE layer. It also bootstraps the memory backend after Beanie is ready to prevent race conditions on first database access.
- [MongoDB Metadata Store for EE Workspace-Scoped Uploads](./mongodb-metadata-store-for-ee-workspace-scoped-uploads.md) — MongoFileStore is the persistence layer for EE upload metadata, extending the OSS file store interface with workspace isolation, folder path tracking, and bulk operations for path rewriting and soft deletion. It bridges the adapter-agnostic `FileRecord` abstraction with the EE `FileUpload` Beanie document.
- [Path Normalization and Validation for the My Files Folder Tree](./path-normalization-and-validation-for-the-my-files-folder-tree.md) — This module provides a small set of path utilities that enforce a strict, well-defined path format for the uploads folder tree. All paths must be absolute, forward-slash separated, with no trailing slash, no traversal sequences, and no control characters — preventing path injection and ensuring consistent storage key formatting across the uploads subsystem.
- [Unified Error Hierarchy for Cloud Domain Modules](./unified-error-hierarchy-for-cloud-domain-modules.md) — This module defines a structured exception hierarchy used across all cloud EE domain packages instead of raw `HTTPException` raises. It standardizes error shape, HTTP status codes, and machine-readable error codes in one place, ensuring consistent API responses and centralized error handling.
- [Unread Service - Per-User Unread Counts and Mention Badges](./unread-service-per-user-unread-counts-and-mention-badges.md) — `UnreadService` tracks per-user, per-group unread message counts and mention badges by combining a `ReadState` watermark document with a live message count query. It provides the data for inbox badges and the visual unread indicator in the sidebar.
- [Upload and Folder Document Models for the EE Storage Layer](./upload-and-folder-document-models-for-the-ee-storage-layer.md) — This module defines the two Beanie document models underpinning the EE uploads subsystem: `FileUpload` for adapter-backed file metadata and `FileFolder` for the virtual folder tree. Both documents are workspace-scoped with compound indexes designed for the access patterns of the uploads and files listing endpoints.
- [Uploads Package Namespace Marker](./uploads-package-namespace-marker.md) — This is an empty `__init__.py` that marks `ee/cloud/uploads/` as a Python package. It contains no code or exports; all uploads functionality is provided by the submodules within this package.
- [UTC Datetime Serialization Helper for MongoDB Timestamps](./utc-datetime-serialization-helper-for-mongodb-timestamps.md) — This module provides `iso_utc`, a one-function utility that fixes a silent timestamp corruption bug caused by MongoDB and Beanie returning naive datetime objects that JavaScript then misinterprets as local time. By re-anchoring naive datetimes to UTC before formatting, it ensures all serialized timestamps carry an unambiguous UTC offset.
- [WebSocket Connection Manager - Presence, Rooms, and Typing Indicators](./websocket-connection-manager-presence-rooms-and-typing-indicators.md) — The `ConnectionManager` class maintains the in-memory registry of active WebSocket connections, manages presence state with a 30-second grace window, routes messages to group rooms, and expires typing indicators automatically after 5 seconds. It is the real-time backbone for the chat domain.
- [Workspace-Scoped Folder Store for the My Files Mount](./workspace-scoped-folder-store-for-the-my-files-mount.md) — FolderStore is a MongoDB-backed CRUD layer for the virtual folder tree in the uploads provider's My Files feature. It handles path normalization, recursive path creation, bulk path rewrites for renames, and soft delete — all constrained to a single workspace.
- [Workspace-Scoped Upload URL Resolver with Cross-Tenant Isolation](./workspace-scoped-upload-url-resolver-with-cross-tenant-isolation.md) — EEUploadResolver translates opaque upload URLs into local filesystem paths while enforcing workspace isolation — a lookup for a file that exists in a different workspace returns `None`, preventing existence leaks across tenants. It is used by agents and media processors that need to access uploaded file content.

### `cloud integration` — 2 article(s)

- [Agent Loop Pocket Threading Tests: Cloud User and Workspace Context Resolution](./agent-loop-pocket-threading-tests-cloud-user-and-workspace-context-resolution.md) — Tests for the `_create_pocket_and_session` and `_publish_pocket_event` functions in `pocketpaw.agents.loop`, covering all resolution paths for threading cloud user identity and workspace context through pocket creation: explicit ids, active-workspace fallback, first-user fallback for self-hosted deployments, graceful handling of invalid ids, and metadata propagation in pocket events.
- [Cloud Context Threading in Chat API: User ID and Workspace Propagation Tests](./cloud-context-threading-in-chat-api-user-id-and-workspace-propagation-tests.md) — Tests that `resolve_cloud_context` correctly injects `cloud_user_id` and `cloud_workspace_id` into `InboundMessage.metadata` when an authenticated cloud user is present, and leaves those keys absent for anonymous or unauthenticated requests. The test file operates at the unit level — directly calling `_build_inbound_message` and `_send_message` — to avoid the overhead of standing up a full SSE streaming client.

### `cloud storage` — 1 article(s)

- [Google Drive Tools: List, Download, Upload, and Share Files](./google-drive-tools-list-download-upload-and-share-files.md) — The `gdrive.py` module exposes four `BaseTool` subclasses — `DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, and `DriveShareTool` — giving the PocketPaw agent full CRUD and sharing access to Google Drive. A `_GDRIVE_ROLES` frozenset enforces valid sharing permissions at the tool layer before the API call is made, preventing invalid-role errors from propagating to the user.

### `cloud-agents` — 1 article(s)

- [Cloud Agent Bootstrap Provider](./cloud-agent-bootstrap-provider.md) — CloudAgentBootstrapProvider builds a BootstrapContext from a MongoDB agent config dict, enabling cloud-hosted agents to carry their own custom identity, persona, archetype, and values rather than inheriting the generic OSS defaults. It satisfies the same BootstrapProviderProtocol as the default file-based provider, so the rest of the runtime is unaware of which source is in use.

### `co-occurrence` — 5 article(s)

- [FastAPI Router for Widget Interactions, Graduation, and Co-occurrence](./fastapi-router-for-widget-interactions-graduation-and-co-occurrence.md) — Exposes the widget journal projection through REST endpoints for reading widget usage stats, graduation state, and co-occurrence suggestions, plus write endpoints for tracking interactions and recording accept/dismiss decisions on suggested widget pairings. The POST /widgets/track endpoint closes a silent data-loss bug where every dashboard interaction 404'd before this change.
- [In-Memory Projection for Widget Interactions, Usage, and Co-occurrence](./in-memory-projection-for-widget-interactions-usage-and-co-occurrence.md) — Provides three unified read views over the widget event stream — per-interaction history, per-widget usage roll-up for graduation decisions, and per-pair co-occurrence counts for suggestion candidates — all rebuilt from a single org journal replay pass. Supersedes the JSONL-based scans from PRs #941 and #942.
- [Widget Event Payload Definitions and Co-occurrence Signature Utilities](./widget-event-payload-definitions-and-co-occurrence-signature-utilities.md) — Defines the five canonical action names for the widget journal projection domain, their payload builders, and the stable co-occurrence signature algorithm that fixes a token-sort ordering bug shipped in PR #942. All constants are pinned at the module level so the projection, policy, store, and tests share a single import path.
- [Widget Graduation and Co-occurrence Policy Engine](./widget-graduation-and-co-occurrence-policy-engine.md) — Implements the pin/fade/archive graduation logic for individual widgets and the threshold-based co-occurrence candidate scanner, both ported verbatim from PRs #941 and #942 onto the journal-backed projection. Decision objects and tuning knobs are preserved under their original names so existing tests and configuration carry over without renaming.
- [Widget Journal Projection Package — Public API](./widget-journal-projection-package-public-api.md) — Re-exports the complete public surface of the widget journal projection domain, including the store, projection classes, policy functions, and all canonical event names and payload builders. This module also documents why the package supersedes held PRs #941 and #942 and ships the fix for a token-sort bug that broke co-occurrence deduplication.

### `code execution` — 2 article(s)

- [RunPythonTool: Guardian-Reviewed Sandboxed Python Execution](./runpythontool-guardian-reviewed-sandboxed-python-execution.md) — The `RunPythonTool` lets the PocketPaw agent execute arbitrary Python code in a sandboxed subprocess, subject to a Guardian AI safety review before execution. The sandbox uses the configured `file_jail_path` as its working directory, and UUID-based script filenames prevent filename collisions when multiple executions overlap. Trust level is `elevated` because arbitrary code execution is the most powerful and dangerous capability an agent can have.
- [RunPythonTool Test Suite — Sandboxed Code Execution with Guardian and File Jail](./runpythontool-test-suite-sandboxed-code-execution-with-guardian-and-file-jail.md) — This test file covers `RunPythonTool`, which executes arbitrary Python code in a sandboxed subprocess controlled by a file jail and a guardian policy engine. Tests validate stdout capture, stderr surfacing, exit code reporting, timeout enforcement, guardian blocking, filesystem isolation, temp file cleanup, and tool metadata.

### `code quality` — 1 article(s)

- [AudienceResolver Exhaustive Coverage: All Event Subclasses Must Resolve and Have Unique Wire Types](./audienceresolver-exhaustive-coverage-all-event-subclasses-must-resolve-and-have-.md) — These tests use Python's inspect module to enumerate every Event subclass at runtime and verify AudienceResolver handles each without raising. A companion test enforces that no two event classes share the same EVENT_TYPE wire string, preventing silent routing collisions when new event types are added.

### `coding agents` — 1 article(s)

- [OpenCode Backend: REST API Integration, Session Caching, and Tool Injection](./opencode-backend-rest-api-integration-session-caching-and-tool-injection.md) — Tests for the OpenCodeBackend, which connects PocketPaw to OpenCode's REST API for agentic coding tasks. Covers health checking, session creation and caching, the message endpoint contract, system prompt tool instruction injection, streaming responses, and graceful error handling for unreachable servers.

### `cognitive engine` — 1 article(s)

- [PocketPaw Cognitive Engine: Wiring Tests for Soul-Aware Thinking](./pocketpaw-cognitive-engine-wiring-tests-for-soul-aware-thinking.md) — Tests for the `PocketPawCognitiveEngine`, which bridges the soul-protocol's identity layer with PocketPaw's LLM backend to produce soul-contextualised responses. Covers correct parameter forwarding, graceful error handling, stream termination logic, and end-to-end wiring through `SoulManager` and `AgentLoop`.

### `collaboration` — 2 article(s)

- [Comment Document — Threaded Comments on Pockets and Widgets](./comment-document-threaded-comments-on-pockets-and-widgets.md) — The Beanie ODM document for threaded comments attached to pockets, widgets, or agents. Supports reply threading via a `thread` parent ID, user mentions, and resolution tracking for review-style workflows.
- [Group Document — Multi-User Channels with AI Agent Participants](./group-document-multi-user-channels-with-ai-agent-participants.md) — The Beanie ODM document for chat groups and channels — workspace-scoped rooms that mix human members with AI agents. Models Slack-style channel semantics with a role/permission layer, agent respond modes, and pinned message support.

### `command execution` — 1 article(s)

- [Security Rails Tests: Dangerous Command Blocking, Bypass Detection, and Safe Allowlist](./security-rails-tests-dangerous-command-blocking-bypass-detection-and-safe-allowl.md) — Layer 5 of PocketPaw's security stack (`pocketpaw.security.rails`) blocks dangerous shell commands before they reach the execution layer. These tests validate the blocking of destructive file operations, remote code execution patterns, obfuscation bypasses, privilege escalation, data exfiltration, and system damage commands, while confirming that safe commands are not falsely blocked.

### `commands` — 2 article(s)

- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.
- [Cross-Channel Slash Command Handler](./cross-channel-slash-command-handler.md) — CommandHandler intercepts slash commands from any channel (Telegram, Slack, WhatsApp, CLI, etc.) and processes them directly without invoking the AI agent backend. It manages session lifecycle operations — creating, listing, resuming, renaming, and deleting conversation sessions — as well as runtime configuration of backend, model, and tool settings.

### `compaction` — 1 article(s)

- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.

### `compatibility` — 1 article(s)

- [Dashboard Starlette Compatibility Regression Test: Index Route on Starlette 1.0.0](./dashboard-starlette-compatibility-regression-test-index-route-on-starlette-100.md) — This short regression test ensures the dashboard's root route returns HTTP 200 with `text/html` content, specifically guarding against a breaking change introduced in Starlette 1.0.0 that changed the `TemplateResponse` constructor signature. The Starlette version is logged in assertion failure messages to make version-related CI failures immediately diagnosable.

### `compiler` — 1 article(s)

- [PocketPaw Compiler Backend Adapter for Knowledge Base](./pocketpaw-compiler-backend-adapter-for-knowledge-base.md) — Implements `PocketPawCompilerBackend`, a `CompilerBackend` Protocol adapter that makes PocketPaw's active agent LLM backend available to the `knowledge_base` package's compiler pipeline. This enables KB article compilation to use whatever AI backend is configured in the agent registry -- Claude, OpenAI, or any other -- without hardcoding a specific model.

### `compliance` — 8 article(s)

- [Audit Entry Model for Enterprise Compliance Logging](./audit-entry-model-for-enterprise-compliance-logging.md) — Defines the `AuditEntry` Pydantic model that captures the complete decision context for every auditable action in PocketPaw: who acted, on what data, what the AI recommended, and what actually happened. Field validators enforce closed enum sets for category and status, and serialization helpers bridge between the Pydantic model and SQLite storage.
- [Audit Logging Completeness Tests: Append-Only Guarantee, Rotation, Failure Resilience, and PII Masking](./audit-logging-completeness-tests-append-only-guarantee-rotation-failure-resilien.md) — Layer 7 security tests verifying that PocketPaw's audit logger captures all security-relevant events, maintains an append-only JSONL log that cannot be deleted through the logger interface, archives rather than deletes on rotation, survives write failures without crashing, and masks PII (e.g., SSNs) before writing to disk.
- [Audit Module Package Initialization](./audit-module-package-initialization.md) — The `pocketpaw.audit` package init re-exports the three public symbols consumers need: `AuditEntry` (the Pydantic model), `AuditStore` (the SQLite backend), and `get_audit_store` (the singleton accessor). This keeps the import path flat for callers while the implementations stay in separate sub-modules.
- [Audit Store Tests: Model Validation, Filtering, Export, and API Endpoints](./audit-store-tests-model-validation-filtering-export-and-api-endpoints.md) — Comprehensive tests for PocketPaw's `AuditStore` — the SQLite-backed audit trail that records all security-relevant operations. Tests cover the `AuditEntry` model, log persistence, multi-field query filtering, date range filtering, CSV and JSON export, REST API endpoints, and integration helpers for tool execution and connector sync logging.
- [Canonical Runtime Audit Router with Full-Text Search](./canonical-runtime-audit-router-with-full-text-search.md) — The authoritative audit query endpoint at `/api/v1/runtime/audit`, consolidating two legacy paths behind a single surface that adds workspace-level rollup filtering and bound-parameter full-text search. SQL injection is explicitly prevented by using parameterized LIKE queries, verified by a dedicated regression test.
- [Enterprise Audit Module (Compliance Logging Placeholder)](./enterprise-audit-module-compliance-logging-placeholder.md) — The `ee/audit/` module is a reserved namespace for enhanced compliance-grade audit logging, intended to extend the instinct pipeline's built-in event log with exportable formats, configurable retention policies, and structured compliance reports for SOC2 and GDPR.
- [Legacy Audit REST Router (Deprecated)](./legacy-audit-rest-router-deprecated.md) — The original `/api/v1/audit` endpoint is kept as a backwards-compatibility alias that forwards to the canonical `runtime_router` implementation. All responses include `Deprecation: true` and a `Link` header pointing callers to the successor path, enabling a graceful migration without breaking existing integrations.
- [SQLite-Backed Audit Log Store](./sqlite-backed-audit-log-store.md) — Implements the `AuditStore` class that persists audit entries to a SQLite database using the stdlib `sqlite3` module with async-friendly execution via `run_in_executor`. Provides filtered querying, full-text search with injection-safe LIKE escaping, CSV/JSON export, and convenience methods for common audit event types.

### `concurrency` — 8 article(s)

- [Agent Loop Tests: Message Processing, AgentEvent Architecture, Error Handling, and GC Lock Management](./agent-loop-tests-message-processing-agentevent-architecture-error-handling-and-g.md) — Tests for the unified `AgentLoop` that orchestrates message consumption, agent router invocation, memory management, and outbound publishing. Updated for the `AgentEvent`-based streaming architecture (replacing raw dict chunks), these tests verify end-to-end message processing, graceful error handling, stale-lock GC, and loop teardown.
- [AgentLoop Concurrency Controls: Session Locks, Global Semaphore, and File Memory Safety](./agentloop-concurrency-controls-session-locks-global-semaphore-and-file-memory-sa.md) — This test module validates the three-layer concurrency model in PocketPaw's AgentLoop: per-session serialization via asyncio locks, cross-session parallelism as the happy path, and a global semaphore that caps total simultaneous conversations. It also covers the FileMemoryStore's write lock and transient PermissionError retry logic that prevent JSON corruption under concurrent writes.
- [Browser Session Management Tests: Lifecycle, Idle Cleanup, Singleton, and Concurrency](./browser-session-management-tests-lifecycle-idle-cleanup-singleton-and-concurrenc.md) — This test suite validates `BrowserSessionManager`, which maintains a pool of named `BrowserSession` objects backed by individual `BrowserDriver` instances. Tests cover session creation, reuse, idle timeout cleanup, graceful close, the global singleton accessor, and thread-safety under concurrent `get_or_create` calls.
- [Browser Session Manager — Named Session Pool with Idle Cleanup](./browser-session-manager-named-session-pool-with-idle-cleanup.md) — BrowserSession and BrowserSessionManager provide a named pool of persistent browser sessions so that agent tools can reuse an open browser across multiple tool calls without launching and closing a new browser for every interaction. Per-session asyncio locks prevent race conditions when concurrent tool calls reference the same session, and idle cleanup reclaims resources from sessions that have not been used within a configurable timeout.
- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.
- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.
- [Memory Edge Cases: Concurrency, Compaction, Unicode, Auto-Learn, and Session Lifecycle](./memory-edge-cases-concurrency-compaction-unicode-auto-learn-and-session-lifecycl.md) — The memory edge case test suite (addressing issue #36) stress-tests PocketPaw's memory subsystem under conditions that routine tests miss — concurrent writes to the same session, session compaction when history exceeds the threshold, Unicode and emoji content, empty session handling, auto-learn trigger conditions, and session listing and deletion operations.
- [Telegram Bot Gateway — Agent Runtime Entry Point](./telegram-bot-gateway-agent-runtime-entry-point.md) — bot_gateway.py wires the three core runtime components — MessageBus, TelegramAdapter, and AgentLoop — into a running process for Telegram-based deployments. It handles clean shutdown on cancellation and is the primary entry point when running PocketPaw as a Telegram bot.

### `condition-evaluation` — 1 article(s)

- [Automation Evaluator — Background Condition Polling Engine](./automation-evaluator-background-condition-polling-engine.md) — AutomationEvaluator is a singleton background loop that periodically checks threshold and data_change automation rules against live Fabric data, then fires matched rules through the Instinct pipeline (for approval-required rules) or directly via the daemon (for auto-execute rules). Schedule rules are handled by the bridge/daemon and do not go through the evaluator.

### `configuration` — 39 article(s)

- [Agent Identity File Schemas](./agent-identity-file-schemas.md) — Defines Pydantic models for reading and updating the five identity files that shape an agent's persona: identity, soul, style, instructions, and user context. The partial-update pattern on the save request ensures callers only need to supply the files they want to change.
- [Agent Router: Registry-Based Backend Selection with Fallback](./agent-router-registry-based-backend-selection-with-fallback.md) — `AgentRouter` is the runtime dispatcher that resolves a configured backend name through the registry, instantiates it, and routes all agent calls through it. It supports a user-configurable ordered list of fallback backends so that a misconfigured or unavailable primary backend degrades gracefully rather than crashing.
- [AGENTS.md Loader: Git-Aware File Discovery with Mtime Caching](./agentsmd-loader-git-aware-file-discovery-with-mtime-caching.md) — `AgentsMdLoader` walks the directory tree upward from a starting path — stopping at git repository roots — to find the nearest AGENTS.md file, parses it into a structured `AgentsMd` object, and caches results by `(path, mtime)` so repeated calls within a session cost nothing. A 32 KiB file size cap prevents oversized AGENTS.md files from bloating the agent's system prompt.
- [AGENTS.md Support Package: Standardized Agent Capability Declaration](./agentsmd-support-package-standardized-agent-capability-declaration.md) — The `agents_md` package implements support for the Anthropic AGENTS.md specification — a convention where repositories publish a markdown file describing constraints, capabilities, and instructions for AI agents operating on that codebase. PocketPaw both reads AGENTS.md from target repositories (to inject constraints into the agent system prompt) and publishes its own AGENTS.md for other agents to consume.
- [API Key Health Checks: Presence, Format Validation, and Encryption Verification](./api-key-health-checks-presence-format-validation-and-encryption-verification.md) — The `api_keys.py` module implements four health checks covering the full lifecycle of API credentials: whether a key exists for the selected backend, whether it matches the expected format prefix, whether required backend packages are installed, and whether the secrets file is Fernet-encrypted rather than plaintext. Together these checks prevent the most common misconfiguration failures that cause silent agent startup errors.
- [Channel Autostart Tests: Per-Channel Boot Control, Dashboard Startup, API Integration, and Config Persistence](./channel-autostart-tests-per-channel-boot-control-dashboard-startup-api-integrati.md) — This test suite validates PocketPaw's per-channel autostart feature, which lets operators control which channel adapters automatically start when the dashboard boots. Tests cover the `_channel_autostart_enabled()` helper, startup behavior with mixed enabled/disabled channels, the REST API endpoints for reading and saving autostart config, and round-trip persistence to disk.
- [Cloud Feature Flags - Always-On Overrides for the EE Product](./cloud-feature-flags-always-on-overrides-for-the-ee-product.md) — The `ee/cloud/features.py` module force-enables features that are optional in the OSS build but required for the cloud EE product to function correctly. Currently it overrides `chat_titles_enabled` to always return `True`, ensuring sidebar titles and realtime UI always work in cloud mode.
- [Cloud Memory Backend Bootstrap — MongoDB Default Registration](./cloud-memory-backend-bootstrap-mongodb-default-registration.md) — Bootstraps the MongoDB memory backend for cloud deployments by directly priming the `pocketpaw.memory.manager` singleton before Beanie is initialized, bypassing the file-based config that would otherwise retain a stale `memory_backend: "file"` setting. Respects the `POCKETPAW_MEMORY_BACKEND` environment variable as an explicit user override.
- [Cloud Tests Shared Fixture: No-Op Realtime Bus](./cloud-tests-shared-fixture-no-op-realtime-bus.md) — This conftest.py installs an autouse fixture that replaces the realtime event bus with a no-op AsyncMock for the duration of every cloud test. This prevents AssertionError crashes when services call emit() in tests that do not start the full application lifecycle.
- [Config File Health Checks: Existence, JSON Validity, and Permission Auditing](./config-file-health-checks-existence-json-validity-and-permission-auditing.md) — The `config.py` health check module verifies three properties of PocketPaw's configuration file: that it exists, that it contains valid JSON, and that its filesystem permissions are restricted to 600. These checks run at startup to catch configuration problems before they produce cryptic runtime failures.
- [Config Validation Tests: API Key Formats, Numeric Constraints, and Enum Fields](./config-validation-tests-api-key-formats-numeric-constraints-and-enum-fields.md) — This test module covers three tiers of Settings validation: the `validate_api_key()` function that checks provider-specific key prefixes and warns on mismatches, Pydantic `gt`/`ge` constraints on numeric fields that prevent nonsensical configurations, and `Literal`-typed enum fields for provider selection. Together they ensure that misconfigured credentials and out-of-range settings fail loudly at startup rather than silently at runtime.
- [Credential Store Tests: Fernet Encryption, Config Separation, Migration, and Log Scrubbing](./credential-store-tests-fernet-encryption-config-separation-migration-and-log-scr.md) — This test module validates PocketPaw's `CredentialStore` — the Fernet-encrypted store that keeps API keys off disk in plaintext — along with the separation of secrets from non-secret config, a one-time migration from legacy plaintext `config.json`, strict Unix file permissions, and a `SecretFilter` that scrubs API keys from log output.
- [Cross-Channel Slash Command Handler](./cross-channel-slash-command-handler.md) — CommandHandler intercepts slash commands from any channel (Telegram, Slack, WhatsApp, CLI, etc.) and processes them directly without invoking the AI agent backend. It manages session lifecycle operations — creating, listing, resuming, renaming, and deleting conversation sessions — as well as runtime configuration of backend, model, and tool settings.
- [Default Bootstrap Provider — File-Based Agent Identity](./default-bootstrap-provider-file-based-agent-identity.md) — DefaultBootstrapProvider reads the agent's identity, soul, style, instructions, and user profile from markdown files in the PocketPaw config directory, caching each file's content keyed by modification time to avoid redundant disk reads. It is the standard provider for self-hosted and developer deployments where identity is managed as local configuration rather than a database record.
- [Feature Flag Resolution: OSS Settings with Cloud Override Layer](./feature-flag-resolution-oss-settings-with-cloud-override-layer.md) — `features.py` provides a thin feature flag resolution layer that reads from OSS `Settings` but allows the `ee.cloud.features` module to force-override specific capabilities when running in cloud mode. The `ee.cloud` import is lazy, ensuring that OSS builds without the enterprise package continue to work without modification.
- [Generic Settings Read and Update Schemas](./generic-settings-read-and-update-schemas.md) — Defines the Pydantic models for PocketPaw's settings API, using a deliberately generic dict-based approach to handle the wide variety of configurable settings without requiring a separate schema for each. The design prioritises flexibility over strict typing, with secrets excluded from the read response.
- [get_journal FastAPI Dependency: Caching, SOUL_DATA_DIR Override, and Cache Reset Tests](./getjournal-fastapi-dependency-caching-souldatadir-override-and-cache-reset-tests.md) — Tests the `get_journal` FastAPI dependency from `ee.journal_dep`, pinning three contracts that the rest of the `ee/` subsystem depends on: the dep returns a live Journal instance, repeated calls within one process return the same cached instance, and the `SOUL_DATA_DIR` environment variable is honoured at call time — not frozen at import. Also verifies `reset_journal_cache()` as the test-isolation escape hatch.
- [Health Check Provider Constants — NON_ANTHROPIC and NON_OPENAI Provider Sets](./health-check-provider-constants-nonanthropic-and-nonopenai-provider-sets.md) — This module defines two tuple constants used across the health check subsystem to identify which LLM providers do not require Anthropic or OpenAI API keys, enabling the connectivity router to correctly dispatch provider-specific reachability checks. Centralising these sets prevents scattered string literals and makes adding a new provider a single-file change.
- [Launcher Shared Constants and Helpers: Paths, Package Metadata, and Python Discovery](./launcher-shared-constants-and-helpers-paths-package-metadata-and-python-discover.md) — The `common.py` module is the single source of truth for paths, package names, and utility functions shared across all launcher components. Centralizing these definitions prevents the bootstrap, server manager, updater, and uninstaller from each hard-coding `~/.pocketpaw` and drifting out of sync.
- [LLM Backend Schemas — Capability and Installation Contracts](./llm-backend-schemas-capability-and-installation-contracts.md) — The backend schemas define how PocketPaw describes available LLM backends to clients. `BackendInfo` captures a backend's capabilities and installation requirements, while `BackendInstallRequest` provides the payload for triggering a backend installation.
- [LLM Provider Adapter Tests: Registry, Model Resolution, and Per-Provider Configuration](./llm-provider-adapter-tests-registry-model-resolution-and-per-provider-configurat.md) — PocketPaw supports multiple LLM providers (Anthropic, Ollama, OpenRouter, Gemini, LiteLLM) through a unified adapter pattern. These tests validate the provider registry, model string resolution priority, per-adapter environment variable construction, and the `LLMClient` delegation layer that dispatches to the correct adapter.
- [LLMClient Abstraction: Provider Resolution, Client Construction, and SDK Environment Setup](./llmclient-abstraction-provider-resolution-client-construction-and-sdk-environmen.md) — The `LLMClient` tests validate the centralized abstraction over multiple LLM backends — Anthropic, OpenAI, Ollama, and OpenRouter — ensuring that provider auto-detection, client instantiation, and SDK environment variable injection all behave correctly for each provider and credential combination. The frozen dataclass design prevents accidental mutation after resolution.
- [LLMClient: Centralized Provider Detection, Client Creation, and Environment Setup](./llmclient-centralized-provider-detection-client-creation-and-environment-setup.md) — `LLMClient` is an immutable descriptor that captures a fully resolved LLM provider configuration and exposes methods to create provider-specific async clients. The companion `resolve_backend_env()` function pushes the correct environment variables for the active backend, eliminating manual env-var wrangling when switching providers.
- [Long-Term Memory API — CRUD, Settings, and Stats](./long-term-memory-api-crud-settings-and-stats.md) — The memory router exposes endpoints for managing PocketPaw's long-term memory store, configuring which backend is active (file vs. mem0), and retrieving usage statistics. It bridges the REST surface to the pluggable MemoryManager, ensuring settings changes take effect immediately by flushing cached singletons.
- [MCP Server Management Schemas](./mcp-server-management-schemas.md) — Defines the Pydantic request models for managing Model Context Protocol (MCP) servers in PocketPaw — adding servers by configuration, referencing them by name, test-connecting before saving, and installing from a curated preset library. These schemas expose the full MCP lifecycle through the REST API.
- [MCP Server Presets: Catalog Registry, Config Conversion, and Dashboard Installation](./mcp-server-presets-catalog-registry-config-conversion-and-dashboard-installation.md) — The MCP presets test suite validates PocketPaw's built-in catalog of popular MCP servers — covering unique ID enforcement, required field validation, category filtering, config generation from presets, and the dashboard API endpoints for browsing and installing presets. The Notion preset's hosted-OAuth path and transform functions receive dedicated coverage.
- [Memory Entry and Settings Schemas](./memory-entry-and-settings-schemas.md) — Defines the Pydantic models for PocketPaw's memory system — a single memory entry with tagging support, and a configuration response that exposes the pluggable memory backend settings including optional mem0 AI-inference configuration. The settings model reflects the system's multi-backend memory architecture.
- [Mount Configuration YAML Loader](./mount-configuration-yaml-loader.md) — Provides `load_mounts`, which reads a `mounts.yaml` file, validates each row into a typed `MountConfig` object, and returns them sorted by `order`. Also exposes `resolve_template` for expanding path templates containing workspace or user variables at request time.
- [PawConfig: Project-Level Configuration with YAML Fallback Parser](./pawconfig-project-level-configuration-with-yaml-fallback-parser.md) — PawConfig is a dataclass that loads paw.yaml from the project root, applies PAW_PROVIDER and PAW_SOUL_PATH environment variable overrides, and provides computed properties for the .paw directory and default soul file path. A minimal built-in YAML parser handles environments where PyYAML is not installed.
- [PawConfig Tests: YAML Loading, Defaults, and Environment Variable Overrides](./pawconfig-tests-yaml-loading-defaults-and-environment-variable-overrides.md) — `PawConfig` is PocketPaw's project-level configuration object, loaded from `paw.yaml` in the project root. These tests cover the full priority chain: defaults when no file exists, YAML values when the file is present, and environment variable overrides that take precedence over both.
- [PawKit Configuration Schema — Command Center Template Format](./pawkit-configuration-schema-command-center-template-format.md) — PawKit is a publishable Command Center template defined as a Pydantic v2 YAML configuration. It specifies dashboard layout (panels, sections, grid columns), automated workflows (scheduled or trigger-based), user-configurable install fields, bundled skills, and external integration requirements — everything needed to ship a reusable agent workspace.
- [Provider Adapter Base Types: ProviderConfig, ProviderAdapter Protocol, and Model Resolution](./provider-adapter-base-types-providerconfig-provideradapter-protocol-and-model-re.md) — `base.py` defines the shared vocabulary for the entire provider adapter system: `ProviderConfig` (the resolved connection config), `ProviderAdapter` (the structural Protocol every adapter implements), and `resolve_model()` (the multi-tier model name fallback chain). These types are the contract between the LLM client layer and individual provider implementations.
- [Remote Tunnel Status and Control Schemas](./remote-tunnel-status-and-control-schemas.md) — Defines the minimal Pydantic models for PocketPaw's remote tunnel API, which exposes the agent's local HTTP server to the internet via a secure tunnel. The two schemas cover tunnel status querying and tunnel start results, including error reporting for failed tunnel launches.
- [Settings REST Router with Security Field Filtering](./settings-rest-router-with-security-field-filtering.md) — Exposes GET and PUT REST endpoints for reading and updating PocketPaw's runtime configuration, with two hard-coded protection layers: secrets are never returned over the wire, and a set of security-critical fields cannot be modified via the API at all. An asyncio lock prevents concurrent write races.
- [SSRF URL Validation Tests: Blocking Internal and Malicious URLs in Settings](./ssrf-url-validation-tests-blocking-internal-and-malicious-urls-in-settings.md) — This test file, added during security sprint cluster E (issue #703), validates the `validate_external_url` function that guards all URL-typed settings fields against Server-Side Request Forgery (SSRF) attacks. It covers rejection of cloud metadata endpoints, RFC 1918 ranges, non-HTTP schemes, and malformed inputs, while confirming a developer-friendly escape hatch for local development.
- [Storage Adapter Factory: Environment-Driven Backend Selection for the Upload Pipeline](./storage-adapter-factory-environment-driven-backend-selection-for-the-upload-pipe.md) — The `factory.py` module provides a single `build_adapter()` function that reads the `POCKETPAW_UPLOAD_ADAPTER` environment variable and returns either a local disk adapter or an S3-compatible adapter, enabling backend swaps without config file changes. A defensive `load_dotenv()` call at build time ensures environment variables are visible even when the factory runs before the application lifecycle loads `.env`.
- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.
- [Upload Configuration: Size Limits, MIME Allowlist, and Inline Rendering Rules](./upload-configuration-size-limits-mime-allowlist-and-inline-rendering-rules.md) — The `uploads/config.py` module centralizes all static configuration for the upload pipeline -- maximum file size, per-batch file count, allowed MIME types, and local storage root -- while also defining which MIME types are safe for inline browser rendering versus forcing an attachment download. The `extension_for()` helper maps canonical MIME types to file extensions for consistent key generation.
- [URL Validators: SSRF Protection for Settings Fields](./url-validators-ssrf-protection-for-settings-fields.md) — This module provides a Pydantic validator (`validate_external_url`) that guards Settings URL fields against Server-Side Request Forgery by blocking internal, loopback, and private-range hostnames. An environment variable escape hatch allows operators to opt in to internal URLs for legitimate local deployments.

### `configuration management` — 1 article(s)

- [Settings API Security Tests — Secret Field Filtering and Immutable Field Guards](./settings-api-security-tests-secret-field-filtering-and-immutable-field-guards.md) — This test module validates two critical security properties of the `/api/v1/settings` router: that GET never returns API keys or tokens (regression for a v0.4.16 leak), and that PUT rejects writes to security-critical immutable fields like `file_jail_path` and `bypass_permissions`. It also verifies normal settings read and write paths work correctly for non-secret, non-immutable fields.

### `connectors` — 22 article(s)

- [Connector Protocol: YAML Parsing, Registry, and DirectREST Adapter Lifecycle Tests](./connector-protocol-yaml-parsing-registry-and-directrest-adapter-lifecycle-tests.md) — This module tests the PocketPaw connector infrastructure — YAML definition parsing, the `ConnectorRegistry` for connector discovery and lifecycle management, and the `DirectRESTAdapter` for connecting, executing actions, and disconnecting from external REST APIs like Stripe.
- [Connector Status Route Tests: State Transitions, Pocket Isolation, and Secret Non-Leakage](./connector-status-route-tests-state-transitions-pocket-isolation-and-secret-non-l.md) — This test file locks down PocketPaw's connector status route contract — the full state machine from disconnected through connected to expired, strict per-pocket credential isolation so one pocket cannot see another's connection state, and the guarantee that OAuth secrets passed at connect time never appear in status responses.
- [Connector-to-Fabric Pipeline: Stripe Integration, Threshold Rules, and Instinct Proposal Chain Tests](./connector-to-fabric-pipeline-stripe-integration-threshold-rules-and-instinct-pro.md) — End-to-end tests for the data ingestion pipeline that runs from a YAML-defined connector (Stripe) through HTTP execution, Fabric object creation, threshold evaluation, and Instinct action proposal — all without real network calls via patched `httpx`. Covers the full chain including error paths and source deduplication behavior.
- [Connector Tools: Agent Interface for the Dynamic Connector Registry](./connector-tools-agent-interface-for-the-dynamic-connector-registry.md) — The connector tools module wires PocketPaw's `ConnectorRegistry` into the agent's tool interface, giving the LLM the ability to discover available data connectors, establish connections, execute connector actions, and inspect what actions a connector supports — all through natural language tool calls.
- [ConnectorProtocol: The Interface Contract for All PocketPaw Data Source Adapters](./connectorprotocol-the-interface-contract-for-all-pocketpaw-data-source-adapters.md) — `protocol.py` defines the structural typing interface that all connector adapters must satisfy, along with the shared result dataclasses and enums they return. The addition of `IngestACL` and `IngestAdapter` in Move 7 ensures that documents pulled into Single Brain carry their source-side permissions, preventing data from a private Slack channel from becoming accessible to unauthorized users.
- [ConnectorRegistry: Dynamic Connector Discovery for YAML, SQL, MongoDB, Firebase, and GCP Adapters](./connectorregistry-dynamic-connector-discovery-for-yaml-sql-mongodb-firebase-and-.md) — `ConnectorRegistry` scans a directory for YAML connector definitions and also hard-registers native Python adapters (SQL databases, MongoDB, Firebase, GCP) that cannot be expressed as simple REST YAML. It manages per-pocket adapter instances and provides a `reload()` method for hot configuration changes.
- [Connectors Test Package Initializer](./connectors-test-package-initializer.md) — This `__init__.py` marks `tests/connectors/` as a Python package and documents its scope as the test home for `pocketpaw.connectors.*` adapter implementations. Its single comment line serves as a breadcrumb for developers adding new connector tests.
- [Critical Gap Coverage: Real HTTP in DirectRESTAdapter and Agent Tools for Fabric and Instinct](./critical-gap-coverage-real-http-in-directrestadapter-and-agent-tools-for-fabric-.md) — This test file was written specifically to close gaps in test coverage identified during a gap analysis — areas where code existed but no test exercised it end-to-end. It covers real HTTP dispatch through `DirectRESTAdapter`, the Fabric agent tools (`FabricQueryTool`, `FabricCreateTool`), and the Instinct agent tools (`InstinctProposeTool`, `InstinctPendingTool`, `InstinctAuditTool`).
- [Critical Gaps Tests: Real HTTP in YAML Connectors, Fabric Queries, and Instinct Tool Operations](./critical-gaps-tests-real-http-in-yaml-connectors-fabric-queries-and-instinct-too.md) — This test module, created in March 2026, addresses three previously untested integration paths: the `DirectRESTAdapter` making authenticated HTTP calls to YAML-defined connectors like Stripe, the Fabric knowledge-graph query tools, and the Instinct action-proposal tools. It uses real YAML connector definitions from the connectors directory and mocks only the network layer.
- [DirectRESTAdapter and YAML Engine: Declarative REST Connector Definitions via YAML Files](./directrestadapter-and-yaml-engine-declarative-rest-connector-definitions-via-yam.md) — `yaml_engine.py` enables third-party REST API connectors to be defined entirely in YAML without writing Python code. The `DirectRESTAdapter` reads a `ConnectorDef` parsed from a YAML file and executes actions as HTTP requests via `httpx`, templating parameters from the agent's action invocation into the request body, headers, and URL.
- [Drive Connector Error Hierarchy: Typed Exceptions for Auth, Rate Limits, and Not-Found Failures](./drive-connector-error-hierarchy-typed-exceptions-for-auth-rate-limits-and-not-fo.md) — `errors.py` defines a minimal, four-class exception hierarchy for the Drive connector so callers can distinguish auth failures, rate-limit exhaustion, and missing-resource errors without string-sniffing. The deliberate smallness is itself a design decision — each exception maps one-to-one to an actionable recovery path.
- [Drive OAuth Bearer Token Resolution: Credential Broker, Env Var, and OAuthManager Fallback Chain](./drive-oauth-bearer-token-resolution-credential-broker-env-var-and-oauthmanager-f.md) — `auth.py` implements a prioritised three-source token-resolution chain so the Drive connector always has a valid OAuth bearer token regardless of environment (production, CI, local dev). It isolates token concerns from `source.py`, keeping the adapter code focused on business logic.
- [DriveClient: Sync HTTP Client for Drive v3 with Exponential-Backoff Retry and Point-in-Time Awareness](./driveclient-sync-http-client-for-drive-v3-with-exponential-backoff-retry-and-poi.md) — `DriveClient` is a purpose-built, sync HTTP client for the Google Drive v3 API that handles rate-limit retries with exponential backoff and jitter, normalises file and revision metadata into typed dataclasses, and stays intentionally separate from the global OAuth token store used by PocketPaw's built-in Drive tools.
- [DriveSourceAdapter: Zero-Copy Live Federation over the Drive v3 API with Point-in-Time Query Support](./drivesourceadapter-zero-copy-live-federation-over-the-drive-v3-api-with-point-in.md) — `DriveSourceAdapter` is the first concrete `SourceAdapter` in PocketPaw, implementing zero-copy live federation against Google Drive: it returns `DataRef` pointers to live files rather than copying bytes into PocketPaw storage. It also pioneered a lightweight `@at=<iso>|query` convention for point-in-time retrieval before soul-protocol adds a native field.
- [Firebase Connector: YAML Definition, CLI Adapter, and Trust Level Tests](./firebase-connector-yaml-definition-cli-adapter-and-trust-level-tests.md) — This test suite validates PocketPaw's Firebase connector from three angles: the declarative YAML definition (action count, trust levels, auth method), the `FirebaseAdapter` class that wraps the Firebase CLI via async subprocess, and the connector registry integration. It uses mocked subprocess calls to test connect, execute, timeout, and error paths without touching a real Firebase project.
- [FirebaseAdapter: CLI-Backed Connector for Firestore, Auth, Hosting, Functions, and Remote Config](./firebaseadapter-cli-backed-connector-for-firestore-auth-hosting-functions-and-re.md) — `FirebaseAdapter` implements the `ConnectorProtocol` by shelling out to the `firebase` CLI with `--json --non-interactive` flags rather than making REST calls directly, delegating authentication entirely to the Firebase CLI's own session. It supports a wide surface: Firestore CRUD, Auth user management, Hosting deploys, Cloud Functions, Remote Config, and Extensions.
- [GCP Connector: YAML Definition, Adapter Authentication, and Execute Path Tests](./gcp-connector-yaml-definition-adapter-authentication-and-execute-path-tests.md) — This test suite validates PocketPaw's Google Cloud Platform connector, covering the `gcp.yaml` declarative definition (20 actions across compute, storage, pub/sub, secrets, Cloud Run, and logging), the `GCPAdapter` lifecycle (connect, execute, disconnect), and registry wiring. All subprocess calls are mocked to avoid requiring live GCP credentials.
- [GCPAdapter: Google Cloud Platform Connector via gcloud CLI with Multi-Path Binary Discovery](./gcpadapter-google-cloud-platform-connector-via-gcloud-cli-with-multi-path-binary.md) — `GCPAdapter` implements `ConnectorProtocol` by wrapping the `gcloud` CLI, using `--format=json` for structured output and supporting project and region overrides via config. It scans several common install paths to locate the binary, making it resilient to non-standard SDK installations.
- [Google Drive Connector: DriveClient, SourceAdapter, and RetrievalRouter Integration Tests](./google-drive-connector-driveclient-sourceadapter-and-retrievalrouter-integration.md) — This test suite covers PocketPaw's Google Drive connector end-to-end: HTTP request plumbing with rate-limit backoff, point-in-time revision lookup, `DriveSourceAdapter.query` result shaping, token resolution precedence, and `RetrievalRouter` integration with journal emission. All Drive API traffic is eliminated by a scripted fake `httpx.Client`.
- [Google Drive Connector Module: Public Surface and Package Exports](./google-drive-connector-module-public-surface-and-package-exports.md) — The `pocketpaw.connectors.drive` package is the Google Drive adapter introduced in Workstream C2 of the Org Architecture RFC. Its `__init__.py` defines a minimal public surface, re-exporting the auth helper, HTTP client, error hierarchy, and the `DriveSourceAdapter` that powers zero-copy live federation.
- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.
- [MongoDBAdapter: Async Document Database Connector via Motor with URI and Field-Based Auth](./mongodbadapter-async-document-database-connector-via-motor-with-uri-and-field-ba.md) — `MongoDBAdapter` implements `ConnectorProtocol` using `motor` (async pymongo) to provide full CRUD access to MongoDB collections. It supports both a connection URI and individual host/port/user/password fields, checks for the optional `motor` dependency at connect time, and exposes collection management, document query/insert/update/delete, and aggregation pipeline actions.

### `constants` — 2 article(s)

- [Constants Package Init: Empty Namespace Module for pocketpaw.constants](./constants-package-init-empty-namespace-module-for-pocketpawconstants.md) — The `pocketpaw/constants/__init__.py` is an intentionally empty module that exists solely to make `pocketpaw.constants` a Python package. Sub-modules like `tool_categories.py` are imported directly by consumers; no symbols are re-exported at the package level.
- [Tool Category Mapping: Frontend Category IDs to Backend Tool Groups for Permission and Selection](./tool-category-mapping-frontend-category-ids-to-backend-tool-groups-for-permissio.md) — `tool_categories.py` provides two lookup tables that translate the frontend's UI category IDs (e.g., `"google_workspace"`) to the backend's tool group names (e.g., `["group:gmail", "group:calendar"]`), enabling the dashboard's category-level permission checkboxes to correctly gate individual tools without hard-coding the mapping in the UI layer.

### `content-extraction` — 1 article(s)

- [UrlExtractTool: SSRF-Safe URL Content Extraction with IP Pinning and Parallel AI Fallback](./urlextracttool-ssrf-safe-url-content-extraction-with-ip-pinning-and-parallel-ai-.md) — `UrlExtractTool` extracts clean text from URLs using Parallel AI's extract API as the primary path and a secure local HTTP fetch as fallback. The local path resolves DNS before connecting and validates the resolved IP against a public-address allowlist, preventing SSRF attacks against private infrastructure.

### `content-retrieval` — 1 article(s)

- [Reddit Tools: Search, Read Posts, and Surface Trending Content](./reddit-tools-search-read-posts-and-surface-trending-content.md) — PocketPaw ships three Reddit tools — `reddit_search`, `reddit_read`, and `reddit_trending` — that give agents access to Reddit content without requiring an API key. They delegate to a shared `RedditClient` integration and return richly formatted markdown text suitable for direct LLM consumption.

### `context management` — 2 article(s)

- [AGENTS.md Support Tests: Loader, Caching, Directory Walk, Context Injection, and SystemEvent Emission](./agentsmd-support-tests-loader-caching-directory-walk-context-injection-and-syste.md) — Tests for the AGENTS.md feature (issue #456) that allows project-specific constraints to be injected into the agent's system prompt. Covers the `AgentsMdLoader` find-and-cache logic (including `.git` boundary stopping, nearest-file preference, and mtime-based cache invalidation), `AgentsMd` formatting, `AgentContextBuilder` injection, and `AgentLoop` `SystemEvent` emission when the file is found.
- [Context Window Budget Tests: Priority-Based Block Assembly and KB Integration](./context-window-budget-tests-priority-based-block-assembly-and-kb-integration.md) — This test module, added in April 2026, validates `AgentContextBuilder._assemble_with_budget`, which assembles the agent's system prompt from priority-ranked blocks while respecting both a global character budget and per-block caps. It also tests the knowledge-base context injection path that calls the `kb-go` CLI subprocess to fetch relevant articles.

### `context-management` — 2 article(s)

- [Agent Context Builder — System Prompt Assembly with Budget Tracking](./agent-context-builder-system-prompt-assembly-with-budget-tracking.md) — AgentContextBuilder is the central orchestrator that assembles the full system prompt for each agent turn, weaving together identity, instructions, memory recalls, knowledge-base results, channel hints, health state, and injected files while staying within a configurable character budget. Its priority-based injection model ensures critical blocks (identity, instructions) are never dropped, while optional blocks are trimmed or skipped as budget tightens.
- [Tool Filter: Relevance-Based Tool Pre-Selection for Context Window Management](./tool-filter-relevance-based-tool-pre-selection-for-context-window-management.md) — The tool filter (`tools/filter.py`) limits the number of tools injected into an LLM context window by scoring tools against the current user message and always pinning a core set. Tests cover passthrough below the cap, max-tools truncation, always-include pinning semantics, relevance scoring, empty message handling, pinned slot accounting, and the tokenizer helper.

### `contracts` — 1 article(s)

- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.

### `conversation` — 1 article(s)

- [Session Management Tools: LLM-Driven Conversation Branching](./session-management-tools-llm-driven-conversation-branching.md) — Six `BaseTool` subclasses expose session lifecycle operations — create, list, switch, clear, rename, delete — as agent-callable tools, replacing an earlier regex-based NL detection approach. The LLM decides when to invoke them based on conversational context, making session control robust to any phrasing.

### `conversation-management` — 1 article(s)

- [Conversation Session Schemas](./conversation-session-schemas.md) — Defines the Pydantic models for PocketPaw's conversation session API — listing, creating, renaming, and searching sessions. The search model captures both the matched content and the message role, enabling contextually rich search result previews in the dashboard.

### `core protocol` — 1 article(s)

- [Agent Backend Protocol: Capability Flags, BackendInfo, and Identity Contract Tests](./agent-backend-protocol-capability-flags-backendinfo-and-identity-contract-tests.md) — This test suite validates the core contracts of PocketPaw's agent backend abstraction layer, covering the `_DEFAULT_IDENTITY` fallback string, the `Capability` flag enum, the immutable `BackendInfo` descriptor, and the `AgentBackend.run()` method signature. Together these tests enforce the interface that every backend adapter must satisfy to be a valid plugin in PocketPaw's multi-backend architecture.

### `core runtime` — 2 article(s)

- [Agent Backends and Router Tests: ClaudeSDKBackend, Registry Routing, Dangerous Hook, and CLI Auth](./agent-backends-and-router-tests-claudesdkbackend-registry-routing-dangerous-hook.md) — Tests for the multi-SDK agent architecture covering `AgentEvent` protocol, `ClaudeSDKBackend` (formerly ClaudeAgentSDKWrapper), registry-based `AgentRouter` backend selection with legacy fallbacks, the fail-closed dangerous command hook, and a regression test for PocketPaw incorrectly requiring an API key when Claude CLI authentication is active.
- [Agent Loop Tests: Message Processing, AgentEvent Architecture, Error Handling, and GC Lock Management](./agent-loop-tests-message-processing-agentevent-architecture-error-handling-and-g.md) — Tests for the unified `AgentLoop` that orchestrates message consumption, agent router invocation, memory management, and outbound publishing. Updated for the `AgentEvent`-based streaming architecture (replacing raw dict chunks), these tests verify end-to-end message processing, graceful error handling, stale-lock GC, and loop teardown.

### `correction` — 1 article(s)

- [Instinct Correction Loop: Patch Computation, Summarization, Store Persistence, and Approve Endpoint Tests](./instinct-correction-loop-patch-computation-summarization-store-persistence-and-a.md) — Covers the "Correction Loop" — the subsystem that detects when a user edits an AI-proposed action during approval, records what changed as structured patches, and surfaces those corrections for soul memory promotion. Tests span the pure diff functions, the `InstinctStore` correction persistence layer, and the `/approve` HTTP endpoint behavior.

### `correction loop` — 2 article(s)

- [Instinct Correction Loop — Diff Capture and Soul Learning Signal](./instinct-correction-loop-diff-capture-and-soul-learning-signal.md) — Defines the data types and diff logic that record what a human changed when editing an agent's proposed action before approving it. These structured diffs are consumed by soul-protocol to bias future proposals toward the human's preferences.
- [Instinct Correction Soul Bridge — Wiring Human Edits to Soul Memory](./instinct-correction-soul-bridge-wiring-human-edits-to-soul-memory.md) — Acts as the connector between the Instinct correction system and soul-protocol's memory tiers, turning captured human edits into episodic observations and promoting repeated field-level corrections into procedural rules. It degrades silently when the soul is unavailable so the approval flow is never blocked.

### `cost optimization` — 1 article(s)

- [Model Router: Task Complexity Classification and Tiered Model Selection](./model-router-task-complexity-classification-and-tiered-model-selection.md) — Tests for ModelRouter, which classifies incoming messages by complexity and routes them to the appropriate LLM tier (Haiku for simple, Sonnet for moderate, Opus for complex). Validates classification boundaries, edge cases like empty messages, and that the model names match the configured tiers.

### `cost-management` — 1 article(s)

- [UsageTracker: Cached Token Counting Fix and Unbounded Summary Aggregation](./usagetracker-cached-token-counting-fix-and-unbounded-summary-aggregation.md) — These tests document and verify two bug fixes in `UsageTracker`: cached input tokens were silently excluded from `total_tokens`, and `get_summary()` was capped at 10,000 records, causing understated lifetime aggregation totals for high-volume installations. Both fixes ensure accurate cost accounting across all Anthropic and OpenAI backends.

### `credential protection` — 1 article(s)

- [Web Server Security Tests: Bot Token Redaction in /setup Errors](./web-server-security-tests-bot-token-redaction-in-setup-errors.md) — This module tests that the `/setup` endpoint in PocketPaw's web server never leaks the Telegram bot token in HTTP error responses (F-07, issue #445). It verifies that when the upstream Telegram API returns an error whose message embeds the token URL, the response is redacted before reaching the client.

### `credentials` — 3 article(s)

- [Credential Store Tests: Fernet Encryption, Config Separation, Migration, and Log Scrubbing](./credential-store-tests-fernet-encryption-config-separation-migration-and-log-scr.md) — This test module validates PocketPaw's `CredentialStore` — the Fernet-encrypted store that keeps API keys off disk in plaintext — along with the separation of secrets from non-secret config, a one-time migration from legacy plaintext `config.json`, strict Unix file permissions, and a `SecretFilter` that scrubs API keys from log output.
- [Credential Store v2 Tests: AES-256-GCM, Argon2id, AEAD, Migration, and Platform Fallbacks](./credential-store-v2-tests-aes-256-gcm-argon2id-aead-migration-and-platform-fallb.md) — This test module validates the v2 upgrade of PocketPaw's `CredentialStore`, which replaces Fernet (PBKDF2-based) encryption with AES-256-GCM authenticated encryption using Argon2id key derivation. It covers the full v2 round-trip, authenticated data enforcement, automatic v1-to-v2 migration with backup, corruption recovery, and cross-platform hardware UUID fallbacks used to derive the encryption key.
- [CredentialStore: Machine-Locked Encrypted Secret Storage with Argon2id + AES-256-GCM](./credentialstore-machine-locked-encrypted-secret-storage-with-argon2id-aes-256-gc.md) — `CredentialStore` encrypts API keys and OAuth tokens to `~/.pocketpaw/secrets.enc` using a key derived from machine identity via Argon2id, ensuring the encrypted file is only usable on the same machine and user account. Version 2 upgraded from Fernet + PBKDF2 to AES-256-GCM + Argon2id with automatic migration and AEAD authentication.

### `cross-platform` — 6 article(s)

- [Cross-Platform Auto-Start Manager for the Desktop Launcher](./cross-platform-auto-start-manager-for-the-desktop-launcher.md) — Implements start-on-login for the PocketPaw desktop launcher across macOS (launchd plist), Windows (registry Run key), and Linux (XDG .desktop file), handling both frozen PyInstaller executables and source-run scenarios through a unified `AutoStartManager` interface.
- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.
- [PocketPaw CLI Entry Point and Server Launcher](./pocketpaw-cli-entry-point-and-server-launcher.md) — The `__main__.py` module is PocketPaw's CLI entry point, handling argument parsing, subcommand dispatch, server startup with automatic port fallback, and channel mode selection. It has evolved significantly from a simple server launcher into a full-featured CLI with subcommands for diagnostics, memory inspection, session management, configuration, and log tailing.
- [PocketPaw Desktop Launcher Entry Point](./pocketpaw-desktop-launcher-entry-point.md) — The main entry point for the PocketPaw desktop launcher, handling first-run bootstrap (Python venv setup, pip install), subsequent-run startup (server launch, browser open, tray icon), and headless mode for server environments. Includes a PyInstaller package fixup that repairs relative imports when the frozen executable runs `__main__.py` as a top-level script.
- [PocketPaw Desktop Launcher Package Version Bootstrap](./pocketpaw-desktop-launcher-package-version-bootstrap.md) — Defines the `pocketpaw-launcher` package version using `importlib.metadata` with a fallback to the `POCKETPAW_VERSION` environment variable, supporting both installed-package and frozen-executable (PyInstaller) deployment scenarios. This thin init file establishes the package namespace for the launcher subsystem.
- [PocketPaw Interactive Installer](./pocketpaw-interactive-installer.md) — A self-contained, single-file installer that guides users through PocketPaw setup with optional rich prompts, falling back to plain text input when the terminal environment cannot support InquirerPy. Supports both interactive and headless (non-interactive) installation profiles, with a robust dependency bootstrap cascade that verifies each step before proceeding.

### `cryptography` — 1 article(s)

- [Upload Grant Signing Tests: HMAC Token Integrity and Expiry](./upload-grant-signing-tests-hmac-token-integrity-and-expiry.md) — This small but security-critical test module verifies the `sign_grant` / `verify_grant` pair in PocketPaw's upload subsystem, ensuring that download grants are cryptographically bound to a specific file ID, expire correctly, and cannot be forged or transplanted across files.

### `customer engagement` — 1 article(s)

- [Paw Print Widget Layer — Package Entry Point](./paw-print-widget-layer-package-entry-point.md) — The `ee.paw_print` package provides the backend for customer-facing embedded widgets that close the decision loop: customer interactions on a rendered widget flow into a Pocket in real time, Instinct nudges the owner, and approved actions feed back to the widget. This module re-exports every public symbol from the two sub-modules.

### `daemon` — 2 article(s)

- [Proactive Daemon Module: Transforming PocketPaw from Reactive Chatbot to Intention-Driven Agent](./proactive-daemon-module-transforming-pocketpaw-from-reactive-chatbot-to-intentio.md) — The `pocketpaw.daemon` package turns PocketPaw from a request/response chatbot into a proactive agent that initiates actions based on user-defined intentions and event triggers. Its `__init__.py` assembles the five subsystems — `IntentionStore`, `TriggerEngine`, `ContextHub`, `IntentionExecutor`, and `ProactiveDaemon` — into a single importable surface.
- [Self-Audit Daemon Tests — Stale Sessions, Config Conflicts, Disk Usage, and OAuth Tokens](./self-audit-daemon-tests-stale-sessions-config-conflicts-disk-usage-and-oauth-tok.md) — This test file covers `pocketpaw.daemon.self_audit`, a background health-check daemon that monitors for stale sessions, dangerous config combinations, excessive disk usage, oversized audit logs, and orphaned OAuth tokens. Tests validate each individual check and the full `run_self_audit` orchestrator.

### `daemon-integration` — 1 article(s)

- [Automations Bridge — Syncing EE Rules to Core Daemon Intentions](./automations-bridge-syncing-ee-rules-to-core-daemon-intentions.md) — bridge.py translates enterprise automation rules into core daemon Intention specs, and removes them when rules are deleted. It maps schedule presets and rule types (schedule, threshold, data_change) to cron, interval, and event trigger types respectively, ensuring automation rules run through the same scheduling infrastructure as built-in daemon behaviors.

### `dashboard` — 21 article(s)

- [Channel Autostart Tests: Per-Channel Boot Control, Dashboard Startup, API Integration, and Config Persistence](./channel-autostart-tests-per-channel-boot-control-dashboard-startup-api-integrati.md) — This test suite validates PocketPaw's per-channel autostart feature, which lets operators control which channel adapters automatically start when the dashboard boots. Tests cover the `_channel_autostart_enabled()` helper, startup behavior with mixed enabled/disabled channels, the REST API endpoints for reading and saving autostart config, and round-trip persistence to disk.
- [Channels API Tests: Status, Config Save, Toggle, and Extras Check](./channels-api-tests-status-config-save-toggle-and-extras-check.md) — This test file covers PocketPaw's `/api/v1/channels` router, which exposes status, configuration, and lifecycle management for messaging channel adapters such as Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It also covers `/extras/check`, the endpoint that detects whether optional channel dependencies are installed.
- [Dashboard Auth Cookie Tests: Secure Flag Behavior Under HTTP and HTTPS Proxies](./dashboard-auth-cookie-tests-secure-flag-behavior-under-http-and-https-proxies.md) — This test file validates that the dashboard's session cookie is set without the `Secure` flag in plain HTTP environments (to avoid breaking self-hosted HTTP deployments) and with the `Secure` flag when the request arrives via an HTTPS reverse proxy detected by the `X-Forwarded-Proto: https` header.
- [Dashboard E2E Tests: UI Load, Deep Work View, Agent/Task CRUD, and Navigation](./dashboard-e2e-tests-ui-load-deep-work-view-agenttask-crud-and-navigation.md) — This Playwright-based end-to-end test suite verifies that the PocketPaw dashboard loads correctly in a real browser, that key UI elements (chat view, Deep Work view, agent/task creation modals, sidebar, remote access) are present and functional, and that the dashboard title matches the expected beta branding.
- [Dashboard Security Tests: Rate Limiting, Session Tokens, Tunnel Auth Bypass, Security Headers, and WebSocket Auth](./dashboard-security-tests-rate-limiting-session-tokens-tunnel-auth-bypass-securit.md) — This comprehensive test module validates PocketPaw's dashboard security hardening across six dimensions: token-bucket rate limiting, HMAC-signed session tokens with TTL, the localhost tunnel auth bypass fix, security headers, CORS rejection, and WebSocket authentication before the upgrade handshake completes.
- [Dashboard Starlette Compatibility Regression Test: Index Route on Starlette 1.0.0](./dashboard-starlette-compatibility-regression-test-index-route-on-starlette-100.md) — This short regression test ensures the dashboard's root route returns HTTP 200 with `text/html` content, specifically guarding against a breaking change introduced in Starlette 1.0.0 that changed the `TemplateResponse` constructor signature. The Starlette version is logged in assertion failure messages to make version-related CI failures immediately diagnosable.
- [E2E Test Configuration: Playwright Browser Setup and Dashboard Server Lifecycle](./e2e-test-configuration-playwright-browser-setup-and-dashboard-server-lifecycle.md) — This conftest manages the full lifecycle of PocketPaw's end-to-end test environment: it checks for Playwright browser installations and skips the entire e2e suite if they are missing, finds a free OS port, starts the dashboard server in a subprocess, waits for it to become ready, and exposes the base URL to all e2e tests through a session-scoped fixture.
- [Extras Install and Channel Dependency Endpoint Tests](./extras-install-and-channel-dependency-endpoint-tests.md) — This test module covers the `GET /api/extras/check` and `POST /api/extras/install` dashboard endpoints that manage optional channel adapter dependencies. It validates the allowlist-based security model that prevents arbitrary package installation and tests the `missing_dep` fallback path when a channel adapter fails to start due to an ImportError.
- [File Browser and File Operation Schemas](./file-browser-and-file-operation-schemas.md) — Defines the Pydantic models for PocketPaw's file-browser API, covering directory listings, path navigation, recent-file tracking from agent tool use, and file-write requests. These schemas form the contract between the FastAPI backend and the dashboard's file explorer UI.
- [File Browser Hidden File Ordering Bug Regression Tests](./file-browser-hidden-file-ordering-bug-regression-tests.md) — This test file reproduces and documents a bug in `handle_file_browse` where applying the `items[:50]` slice before the hidden-file filter caused all visible files to disappear from results when a directory contained 50 or more dot-prefixed items. The tests were created on 2026-02-12 to lock in the correct behavior — filter first, then slice.
- [Frontend JavaScript Syntax Validation Tests](./frontend-javascript-syntax-validation-tests.md) — This module validates that all JavaScript files in PocketPaw's frontend directory have syntactically correct code by running each file through `node --check`. It also verifies structural contracts — that `app.js` defines the Alpine.js `app()` function and that `websocket.js` references `WebSocket` — catching integration breakage before it reaches users.
- [PawKit Catalog: Registry of Installable Dashboard Presets](./pawkit-catalog-registry-of-installable-dashboard-presets.md) — The PawKit Catalog is a curated in-memory registry of pre-configured PawKit types that users can browse and install from the Pocket Store. Each entry describes a dashboard preset with ID, display name, category, Lucide icon, and preview text — without bundling the full YAML configuration.
- [PawKit Configuration Schema — Command Center Template Format](./pawkit-configuration-schema-command-center-template-format.md) — PawKit is a publishable Command Center template defined as a Pydantic v2 YAML configuration. It specifies dashboard layout (panels, sections, grid columns), automated workflows (scheduled or trigger-based), user-configurable install fields, bundled skills, and external integration requirements — everything needed to ship a reusable agent workspace.
- [PawKit Pydantic Models: Dashboard Configuration Schema](./pawkit-pydantic-models-dashboard-configuration-schema.md) — The `kits.models` module defines the full Pydantic schema hierarchy for PawKit configurations — from individual metric cards up to the root `PawKitConfig` that a YAML file is parsed into. It also defines `InstalledKit`, which tracks activation status and user-provided configuration values at runtime.
- [PawKits Package: Dashboard System Public API](./pawkits-package-dashboard-system-public-api.md) — The `pocketpaw.kits` package exposes the complete public surface for PawKits — PocketPaw's configurable command-center dashboard system. It re-exports models, catalog entries, and store operations as a single flat namespace so consumers never need to import from internal submodules.
- [Pocket Tool Tests: Dashboard Widget Creation, Legacy Conversion, and UI Spec Generation](./pocket-tool-tests-dashboard-widget-creation-legacy-conversion-and-ui-spec-genera.md) — The pocket tool system lets AI agents create and modify PocketPaw dashboard "pockets" — widget-based layouts containing metrics, charts, tables, and feeds. These tests validate the `create_pocket`, `add_widget`, and `remove_widget` tools, including backward-compatible legacy widget conversion, the `ui` parameter for pre-built specs, and the JSON Schema definitions used by LLM providers.
- [Pocket Tools: Create, Add Widget, and Remove Widget for Ripple UI Specs](./pocket-tools-create-add-widget-and-remove-widget-for-ripple-ui-specs.md) — The `pocket.py` module provides three `BaseTool` subclasses — `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — that allow the agent to generate and modify Ripple UniversalSpec JSON structures for pocket workspaces. The module handles three generations of the spec format (flat widget arrays, single-pane UISpec trees, and multi-pane pane-level UISpec trees) and maintains a display-type-to-Ripple-widget mapping for backward compatibility with legacy spec fields.
- [Recent Files Tracker: Agent Tool Usage History](./recent-files-tracker-agent-tool-usage-history.md) — RecentFilesTracker intercepts agent tool calls to extract file paths and maintains a capped LRU list of the 50 most recently accessed files with metadata. It uses lazy loading, a heuristic Bash path extractor via regex, and JSON persistence to power the dashboard's recent files widget.
- [StatusTracker: Real-Time Agent State Monitoring via Message Bus Events](./statustracker-real-time-agent-state-monitoring-via-message-bus-events.md) — `StatusTracker` subscribes to the PocketPaw message bus and maintains a live snapshot of every active agent session's current state — thinking, running a tool, streaming a response, or in an error condition. It powers the status indicator in the dashboard and supports long-poll queries so the UI can update without polling.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.
- [WebSocket Channel Adapter for the PocketPaw Dashboard](./websocket-channel-adapter-for-the-pocketpaw-dashboard.md) — WebSocketAdapter manages real-time bidirectional communication between PocketPaw's FastAPI backend and its web dashboard frontend, routing messages, streaming chunks, and system events (tool execution, thinking indicators) to the correct connected browser session.

### `dashboard UI` — 1 article(s)

- [API Keys Panel Acceptance Tests: UI Lifecycle Flows for Cluster F](./api-keys-panel-acceptance-tests-ui-lifecycle-flows-for-cluster-f.md) — End-to-end acceptance tests that walk the exact flows the `ApiKeysPanel.svelte` UI executes — create, list, rotate (with old key invalidation), revoke, and sort-by-creation. These tests complement unit-level coverage by pinning the API shape the frontend depends on, catching regressions that break the dashboard silently.

### `dashboard authentication` — 1 article(s)

- [Remote Access Auth Tests: Token Generation, Middleware, and QR Endpoint Security](./remote-access-auth-tests-token-generation-middleware-and-qr-endpoint-security.md) — PocketPaw's dashboard uses a file-backed token for remote access authentication. These tests validate token generation and persistence, the auth middleware's enforcement of Bearer token and query parameter authentication, and a security fix (issue #854) that closed an unauthenticated `/api/qr` endpoint.

### `dashboard tools` — 1 article(s)

- [Pocket Tool Test Suite: Widget Lifecycle, Legacy Conversion, and UISpec Contracts](./pocket-tool-test-suite-widget-lifecycle-legacy-conversion-and-uispec-contracts.md) — Comprehensive tests for `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — the three built-in tools that let AI agents build and mutate dashboard "pockets" in PocketPaw. The suite enforces spec versioning (v1.0 UISpec vs v2.0 widget array), backward compatibility of the `name→title` alias, and structural contracts on every tool's JSON output.

### `dashboard-config` — 1 article(s)

- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.

### `data access` — 1 article(s)

- [Enterprise API Singleton Accessors](./enterprise-api-singleton-accessors.md) — `ee/api.py` provides module-level singleton factory functions for the two main enterprise data stores — `InstinctStore` and `PawPrintStore` — so that both agent tool code and domain routers can reach a shared, lazily-initialised database instance without dependency injection.

### `data aggregation` — 1 article(s)

- [Workspace KB Aggregator: Merge, Filter, Dedup, Sort, and Resilience Tests](./workspace-kb-aggregator-merge-filter-dedup-sort-and-resilience-tests.md) — This test suite validates the `ee.cloud.kb.workspace_aggregator` module, which merges knowledge base articles from workspace and agent scopes into a single sorted, deduplicated list. It tests merge logic, agent filtering, deduplication by (scope, id), newest-first ordering, resilience when a scope returns malformed data, and the `AggregatedArticle.to_dict()` shape.

### `data consistency` — 1 article(s)

- [MongoMemoryStore Session Upkeep: messageCount and lastActivity Auto-Maintenance Tests](./mongomemorystore-session-upkeep-messagecount-and-lastactivity-auto-maintenance-t.md) — These tests verify that MongoMemoryStore automatically updates Session metadata (messageCount, lastActivity) on every save and auto-creates missing Session rows when a message arrives before the session is explicitly created. They pin behaviors introduced after a historical refactor that removed a broken parallel bus subscriber.

### `data ingestion` — 1 article(s)

- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.

### `data model` — 2 article(s)

- [ReadState Model and UnreadService Upsert Tests](./readstate-model-and-unreadservice-upsert-tests.md) — This module tests the ReadState model's default values and the atomic upsert behavior of UnreadService.mark_read and bump_mention against a real in-memory MongoDB. The tests enforce the unique (user, group) index constraint and verify that concurrent operations remain idempotent.
- [UnreadService List Unreads Calculation Tests](./unreadservice-list-unreads-calculation-tests.md) — This module tests UnreadService.list_unreads, which computes per-group unread message and mention counts for a user. Tests cover the caught-up state, partial reads, the fresh-user fallback, and the edge case where a ReadState row exists but has no recorded read position.

### `data modeling` — 8 article(s)

- [Agent Document — Configuration, Soul Integration, and Scope Assignment](./agent-document-configuration-soul-integration-and-scope-assignment.md) — The Beanie ODM document representing an agent's persistent configuration in MongoDB — covering backend selection, system prompt, tool allowlist, trust level, soul personality settings, and hierarchical scope assignments. This document captures agent identity and configuration, not execution state.
- [Cloud Document Models Registry — Beanie Initialization Manifest](./cloud-document-models-registry-beanie-initialization-manifest.md) — The central re-export module for all Beanie ODM documents in the enterprise cloud layer, providing a stable import surface and a lazy-loading `ALL_DOCUMENTS` list consumed by Beanie's init sequence. Special handling defers `FileUpload` and `FileFolder` imports to break circular import chains with the uploads subsystem.
- [Comment Document — Threaded Comments on Pockets and Widgets](./comment-document-threaded-comments-on-pockets-and-widgets.md) — The Beanie ODM document for threaded comments attached to pockets, widgets, or agents. Supports reply threading via a `thread` parent ID, user mentions, and resolution tracking for review-style workflows.
- [FileObj Document — File Metadata with Object Storage Backend](./fileobj-document-file-metadata-with-object-storage-backend.md) — A Beanie ODM document that stores file metadata in MongoDB while keeping actual file bytes in S3, GCS, or local storage. Each record captures the owner, storage provider, bucket path, MIME type, and public accessibility flag, acting as the registry layer for a multi-provider object storage abstraction.
- [Group Document — Multi-User Channels with AI Agent Participants](./group-document-multi-user-channels-with-ai-agent-participants.md) — The Beanie ODM document for chat groups and channels — workspace-scoped rooms that mix human members with AI agents. Models Slack-style channel semantics with a role/permission layer, agent respond modes, and pinned message support.
- [MemoryFactDoc — Beanie Document for Agent Long-Term and Daily Memory](./memoryfactdoc-beanie-document-for-agent-long-term-and-daily-memory.md) — The `MemoryFactDoc` Beanie ODM document stores agent long-term and daily memory facts in the `memory_facts` MongoDB collection. It is distinct from the `messages` collection and carries workspace and user ownership fields to support multi-tenant scoping.
- [PolicyContext and PolicyResult: Authorization Decision Data Types](./policycontext-and-policyresult-authorization-decision-data-types.md) — `policy.py` defines the two frozen dataclasses that form the data contract for the ABAC evaluation pipeline: `PolicyContext` bundles everything a guard needs to make a decision, and `PolicyResult` carries the outcome with a machine-readable code. Their immutability and slot optimization make them safe to pass across async boundaries without defensive copying.
- [TimestampedDocument — Base Beanie Document with Automatic Timestamps](./timestampeddocument-base-beanie-document-with-automatic-timestamps.md) — A Beanie `Document` base class that automatically manages `createdAt` and `updatedAt` timestamps using Beanie's event hook system. All cloud ODM documents inherit from this class to get consistent, server-side timestamp management.

### `data models` — 9 article(s)

- [Cloud Pydantic Model Tests: Field Defaults and Schema Validation Without a Database](./cloud-pydantic-model-tests-field-defaults-and-schema-validation-without-a-databa.md) — This test file verifies field-level Pydantic model behavior for core cloud entities (Group, Message, Pocket, Invite, Workspace, Session, Notification) using `model_construct()` to bypass Beanie's MongoDB-dependent `__init__`. It pins default values, optional field nullability, and enum acceptance for fields added in recent schema migrations.
- [Fabric Pydantic Models — Ontology Layer Data Structures](./fabric-pydantic-models-ontology-layer-data-structures.md) — This module defines the Pydantic models that represent the Fabric ontology layer's core entities: property definitions, object types, object instances, directional links between objects, query parameters, and query results. The `_gen_id` function produces time-ordered, prefix-tagged identifiers that balance human readability with sortability.
- [Fleet Pydantic Models — Manifest and Install Report Types](./fleet-pydantic-models-manifest-and-install-report-types.md) — This module defines the Pydantic models for the Fleet subsystem: the `FleetTemplate` manifest (what to install), `FleetConnector` (a single connector registration), `FleetInstallStep` (one step's outcome), and `FleetInstallReport` (the full install result with convenience methods). These models are thin data containers — no new runtime concepts, just structured representations of the install lifecycle.
- [HealthCheckResult — Shared Data Contract for All Health Checks](./healthcheckresult-shared-data-contract-for-all-health-checks.md) — `HealthCheckResult` is the dataclass that all PocketPaw health check functions return, providing a consistent schema with check identity, status severity, human-readable message, and an actionable fix hint. It auto-stamps a UTC ISO-8601 timestamp on creation and includes a `to_dict()` serializer for API and storage use.
- [Instinct Decision Pipeline — Core Data Models](./instinct-decision-pipeline-core-data-models.md) — Defines the Pydantic data models and enumerations that underpin the Instinct decision pipeline: the proposed action, its lifecycle states, audit entries, and the contextual metadata that connects an action to its triggering source. These types are the stable contract shared by the store, router, correction system, and any agent that proposes actions.
- [Instinct Reasoning Trace Types — Decision Audit Context](./instinct-reasoning-trace-types-decision-audit-context.md) — Defines the immutable data types that record the full reasoning context behind each proposed action: the queries made, soul memories consulted, KB articles injected, and tool calls executed. These types power the "Why?" audit drawer and enable humans to verify exactly what evidence the agent used.
- [OAuth2 Data Models: Client, Authorization Code, and Token](./oauth2-data-models-client-authorization-code-and-token.md) — Defines the three core data structures for PocketPaw's OAuth2 implementation: `OAuthClient` (registered application with redirect URI validation), `AuthorizationCode` (short-lived PKCE exchange token), and `OAuthToken` (access + refresh token pair). The redirect URI matching logic implements RFC 8252 Section 7.3 for native app loopback flexibility.
- [Paw Print Models — Secure Widget Render Vocabulary and Event Types](./paw-print-models-secure-widget-render-vocabulary-and-event-types.md) — Defines the Pydantic models for the Paw Print widget layer with a security-first design: no raw HTML, capped collection sizes, a per-widget rotatable access token, and a 4KB payload cap on inbound events. The models form the contract between the backend and the widget.js bundle.
- [Upload and Folder Document Models for the EE Storage Layer](./upload-and-folder-document-models-for-the-ee-storage-layer.md) — This module defines the two Beanie document models underpinning the EE uploads subsystem: `FileUpload` for adapter-backed file metadata and `FileFolder` for the virtual folder tree. Both documents are workspace-scoped with compound indexes designed for the access patterns of the uploads and files listing endpoints.

### `data persistence` — 6 article(s)

- [Audit Store Tests: Model Validation, Filtering, Export, and API Endpoints](./audit-store-tests-model-validation-filtering-export-and-api-endpoints.md) — Comprehensive tests for PocketPaw's `AuditStore` — the SQLite-backed audit trail that records all security-relevant operations. Tests cover the `AuditEntry` model, log persistence, multi-field query filtering, date range filtering, CSV and JSON export, REST API endpoints, and integration helpers for tool execution and connector sync logging.
- [Instinct Store — Async SQLite Persistence for the Decision Pipeline](./instinct-store-async-sqlite-persistence-for-the-decision-pipeline.md) — Provides all async SQLite read and write operations for the Instinct decision pipeline: action lifecycle management, audit logging, correction recording, reasoning traces, and Fabric object snapshots. It is the single source of truth for the decision state within a PocketPaw enterprise installation.
- [JSONL File Store: Append-Only Metadata Persistence for Uploaded Files](./jsonl-file-store-append-only-metadata-persistence-for-uploaded-files.md) — The `JSONLFileStore` provides a lightweight, append-only metadata store for file upload records using newline-delimited JSON, with an in-memory cache for fast lookups and thread-safe writes via a `Lock`. Soft deletes are implemented by appending a delete operation record rather than removing lines, preserving the full audit history of every file lifecycle event.
- [MongoMemoryStore Facts Path: LONG_TERM and DAILY Memory Roundtrip Tests](./mongomemorystore-facts-path-longterm-and-daily-memory-roundtrip-tests.md) — These tests exercise the complete facts path of MongoMemoryStore, covering CRUD, type isolation between LONG_TERM and DAILY entries, cross-type search, regex safety, and protocol conformance. They also verify that user_id is stored as a dedicated column and never duplicated in the metadata dictionary.
- [Org Journal FastAPI Dependency — Shared Audit Log for EE Routes](./org-journal-fastapi-dependency-shared-audit-log-for-ee-routes.md) — Provides a single shared `Journal` instance as a FastAPI dependency, ensuring all enterprise edition routes write their audit events to one canonical org-level log rather than opening separate per-subsystem SQLite files. Supports test isolation via a cache-reset escape hatch.
- [Paw Print Store — Async SQLite Persistence for Widgets and Events](./paw-print-store-async-sqlite-persistence-for-widgets-and-events.md) — Provides all async SQLite read and write operations for the Paw Print widget layer, including widget CRUD with token rotation, append-only event recording, and a dual-level rate-limit enforcement method that checks both overall widget throughput and per-customer throughput within a rolling time window.

### `data serialization` — 1 article(s)

- [Pocket Layout Helpers: Export Determinism, Parse Validation, Round-Trip, and Store Scoping Tests](./pocket-layout-helpers-export-determinism-parse-validation-round-trip-and-store-s.md) — This unit test suite covers the pure Python helpers in `ee.cloud.pockets.layouts` — specifically `export_layout_yaml` (deterministic YAML serialization), `parse_layout_yaml` (safe YAML parsing with input validation), and `UserTemplateStore` (in-process template store with workspace scoping). No FastAPI, Beanie, or MongoDB is involved.

### `data storage` — 1 article(s)

- [AutomationStore: JSON File-Backed Persistence for Automation Rules](./automationstore-json-file-backed-persistence-for-automation-rules.md) — The `AutomationStore` class manages CRUD operations and fire-count tracking for automation rules, persisting state to a JSON file at `~/.pocketpaw/automations/rules.json`. It is accessed as a module-level singleton to ensure all router and evaluator calls operate on the same in-memory rule set.

### `data store` — 1 article(s)

- [MongoFileStore Unit Tests: Scoped Save/Get, Cross-Workspace Isolation, and Soft Delete](./mongofilestore-unit-tests-scoped-saveget-cross-workspace-isolation-and-soft-dele.md) — Unit tests for `MongoFileStore`'s workspace-scoped CRUD operations, validating that files are stored and retrieved correctly within a workspace, are invisible across workspace boundaries, disappear after soft deletion, and return `None` (not raise) for missing IDs.

### `data structures` — 1 article(s)

- [Knowledge Engine Data Models: RawDoc, WikiArticle, Concept, LintIssue, KnowledgeIndex](./knowledge-engine-data-models-rawdoc-wikiarticle-concept-lintissue-knowledgeindex.md) — The `knowledge.models` module defines the five core dataclasses of PocketPaw's knowledge engine — `RawDoc` (ingested source), `WikiArticle` (compiled artifact), `Concept` (cross-article entity), `LintIssue` (audit finding), and `KnowledgeIndex` (master index). Each includes a `to_dict()` method for JSON serialization to disk.

### `data validation` — 3 article(s)

- [Event Model Structural Tests: Base Event Fields and Typed Subclass Wire Type Assignment](./event-model-structural-tests-base-event-fields-and-typed-subclass-wire-type-assi.md) — Two minimal tests verify the structural contract of the Event base model: that every Event has type, data, and ts fields; and that typed subclasses like GroupCreated automatically set the correct wire type string from EVENT_TYPE without the caller needing to pass it.
- [Paw Print Widget Backend: Model Validation, CRUD, and Rate Limiting Tests](./paw-print-widget-backend-model-validation-crud-and-rate-limiting-tests.md) — This test suite covers the backend layer of PocketPaw's Paw Print embeddable widget system — validating Pydantic model constraints (block caps, domain normalization, rate limits, token format), SQLite-backed CRUD operations, event persistence, and the dual rate-limit primitives (overall and per-customer) that protect the public ingest endpoint.
- [Session and Message context_type Discriminator Validator Tests](./session-and-message-contexttype-discriminator-validator-tests.md) — These tests verify that Pydantic model validators on Message and Session enforce strict discriminator rules, ensuring group and pocket context types carry exactly the required fields and nothing more. They also confirm backward-compatible inference when context_type is omitted.

### `data-integration` — 1 article(s)

- [Connector Tools: Agent Interface for the Dynamic Connector Registry](./connector-tools-agent-interface-for-the-dynamic-connector-registry.md) — The connector tools module wires PocketPaw's `ConnectorRegistry` into the agent's tool interface, giving the LLM the ability to discover available data connectors, establish connections, execute connector actions, and inspect what actions a connector supports — all through natural language tool calls.

### `data-model` — 1 article(s)

- [Message Bus Event Types: Channel, InboundMessage, OutboundMessage, SystemEvent](./message-bus-event-types-channel-inboundmessage-outboundmessage-systemevent.md) — events.py defines the four core data types that flow through PocketPaw's message bus: the Channel enum identifying communication platforms, InboundMessage for received messages, OutboundMessage for agent replies, and SystemEvent for internal runtime signals.

### `data-models` — 15 article(s)

- [Automations Models — Rule, RuleType, ExecutionMode, and Request Models](./automations-models-rule-ruletype-executionmode-and-request-models.md) — This module defines the Pydantic data models for the EE automations engine: the `Rule` entity (the core automation record), `RuleType` and `ExecutionMode` enums, and `CreateRuleRequest` / `UpdateRuleRequest` for the API layer. The models cover three rule types (threshold, schedule, data_change) with three execution modes (require_approval, auto_execute, notify_only).
- [Chat Session Document for Pocket and Group Contexts](./chat-session-document-for-pocket-and-group-contexts.md) — The `Session` document stores chat session metadata shared between one-on-one pocket conversations and multi-user group chats, using a `context_type` discriminator validated on construction. It tracks message counts, last activity, and soft deletion, with four compound indexes covering the dominant query patterns.
- [Deep Work Data Models — Project, TaskSpec, AgentSpec, and PlannerResult](./deep-work-data-models-project-taskspec-agentspec-and-plannerresult.md) — This module defines the core data structures for the Deep Work orchestration layer: Project (top-level orchestration unit), TaskSpec and AgentSpec (planning-phase blueprints), and PlannerResult (complete planner output). These models separate planning artifacts from execution artifacts, allowing the planner to produce a full plan before any Mission Control objects are created.
- [Deep Work Goal Parser — Structured Goal Analysis via LLM](./deep-work-goal-parser-structured-goal-analysis-via-llm.md) — GoalParser is the first primitive in the Deep Work pipeline, taking raw user input and producing a structured GoalAnalysis that captures domain, complexity, AI/human role split, constraints, and clarification questions. It routes the LLM prompt through AgentRouter, strips code fences from the response, and defensively validates every field before constructing the dataclass.
- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.
- [Deep Work Task Model Extensions: New Field Tests](./deep-work-task-model-extensions-new-field-tests.md) — This suite tests the Deep Work extensions added to the core Task model in PocketPaw's mission control system, specifically the five new fields—project_id, task_type, blocks, active_description, and estimated_minutes—that enable multi-task project management. It verifies defaults, settability, serialization, backward compatibility, and round-trip fidelity.
- [In-App Notification Document](./in-app-notification-document.md) — The `Notification` model is the persistence layer for PocketPaw's in-app notification system, storing per-user alerts with typed sources, read state, and optional expiry. It pairs with the `NotificationService` for fan-out and the realtime layer for push delivery.
- [Mission Control Data Models](./mission-control-data-models.md) — This module defines all core dataclasses and enums for Mission Control covering agent profiles, tasks, messages, activities, documents, and notifications. The models use Python dataclasses with ISO 8601 timestamps and StrEnum statuses, and have grown through two major revisions to support Deep Work v2 autonomous execution fields.
- [PawKit Configuration Schema — Command Center Template Format](./pawkit-configuration-schema-command-center-template-format.md) — PawKit is a publishable Command Center template defined as a Pydantic v2 YAML configuration. It specifies dashboard layout (panels, sections, grid columns), automated workflows (scheduled or trigger-based), user-configurable install fields, bundled skills, and external integration requirements — everything needed to ship a reusable agent workspace.
- [Pocket and Widget Documents](./pocket-and-widget-documents.md) — The `Pocket` document is the primary workspace unit in PocketPaw — a named canvas that holds an ordered grid of `Widget` sub-documents, a team roster, assigned agents, and a ripple spec for graph-based automation. Widgets are embedded directly in the Pocket document and carry their own ObjectId so the frontend can address them by stable ID rather than array index.
- [ReadState: Per-User Unread Message Tracking](./readstate-per-user-unread-message-tracking.md) — The `ReadState` document records the last message a user has read in each group, enabling O(1) unread count computation by comparing a single pointer against the group's `message_count` counter. It is updated on `read.ack` WebSocket events and tracks unread mention counts separately.
- [Unified Message Document for Group Chat and Agent Memory](./unified-message-document-for-group-chat-and-agent-memory.md) — The `Message` model stores two structurally different record types — multi-user group chat rows and single-agent LLM memory rows — in one MongoDB collection, discriminated by a `context_type` field. A model validator enforces strict field invariants per context type, preventing cross-contamination of group chat metadata into agent history and vice versa.
- [User and OAuth Account Models](./user-and-oauth-account-models.md) — The `User` document extends `fastapi-users`' Beanie base to add enterprise-specific fields: multi-workspace membership with per-workspace roles, presence status, OAuth provider linkage, and an active workspace pointer. The `WorkspaceMembership` embedded model tracks role and join timestamp per workspace.
- [Workspace Document: Organization Tenancy Model](./workspace-document-organization-tenancy-model.md) — The `Workspace` document is the top-level tenancy boundary in PocketPaw EE, representing one organization or deployment. It records billing plan, seat count, configurable settings, and supports soft deletion, with a unique `slug` index for human-readable URL routing.
- [Workspace Invitation Model](./workspace-invitation-model.md) — The Invite document manages workspace membership invitations sent to email addresses, tracking acceptance state, expiry, and optional group auto-assignment. It enforces a 7-day default TTL with timezone-aware expiry checking to prevent stale invitations from granting unintended access.

### `database` — 8 article(s)

- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.
- [Cloud Chat Test Fixtures: In-Memory MongoDB via mongomock-motor](./cloud-chat-test-fixtures-in-memory-mongodb-via-mongomock-motor.md) — The `beanie_memory_db` fixture initialises Beanie against an isolated in-memory MongoDB database for each test using `mongomock-motor`, enabling the full cloud chat test suite to run in CI without a real MongoDB service. It includes a compatibility shim that silences unsupported keyword arguments in mongomock-motor's `list_collection_names` stub.
- [Cloud DB Module - Backward-Compatible Database Shim](./cloud-db-module-backward-compatible-database-shim.md) — `ee/cloud/db.py` is a one-line backward-compatibility shim that re-exports the database lifecycle functions from the canonical `ee.cloud.shared.db` module. It exists solely to preserve older import paths after the database logic was consolidated into the shared package.
- [MongoDB Cloud Database Diagnostic: Collection Counts and Sample Document Inspection](./mongodb-cloud-database-diagnostic-collection-counts-and-sample-document-inspecti.md) — A targeted diagnostic script for the PocketPaw enterprise cloud MongoDB database that lists collection document counts and prints sample documents from key collections. Unlike the broader `diag_mongo_all.py`, this script focuses on the `paw-cloud` database and respects the `POCKETPAW_CLOUD_MONGO_URI` environment variable for flexible targeting.
- [MongoDB Connection and Beanie ODM Initialization](./mongodb-connection-and-beanie-odm-initialization.md) — This module manages the lifecycle of the MongoDB connection and initializes the Beanie ODM with all document models required by the cloud EE layer. It also bootstraps the memory backend after Beanie is ready to prevent race conditions on first database access.
- [MongoDB Diagnostic: List All Databases and Collection Document Counts](./mongodb-diagnostic-list-all-databases-and-collection-document-counts.md) — A diagnostic script that connects to a local MongoDB instance and prints every non-system database alongside its collections and document counts. It exists to quickly answer the question "where did my data actually land?" when debugging unexpected data routing between the local dev database, the enterprise cloud database, and any other MongoDB instance on the machine.
- [MongoDBAdapter: Async Document Database Connector via Motor with URI and Field-Based Auth](./mongodbadapter-async-document-database-connector-via-motor-with-uri-and-field-ba.md) — `MongoDBAdapter` implements `ConnectorProtocol` using `motor` (async pymongo) to provide full CRUD access to MongoDB collections. It supports both a connection URI and individual host/port/user/password fields, checks for the optional `motor` dependency at connect time, and exposes collection management, document query/insert/update/delete, and aggregation pipeline actions.
- [Role Roundtrip Diagnostic: Verifying User and Assistant Message Persistence](./role-roundtrip-diagnostic-verifying-user-and-assistant-message-persistence.md) — A targeted diagnostic script that writes four messages with alternating `user` and `assistant` roles to MongoDB via the memory manager, then reads them back to verify that roles are persisted and retrieved correctly. It was created to debug a specific class of bug where the agent loop was saving messages but role information was being lost or corrupted during the roundtrip.

### `dataclasses` — 1 article(s)

- [Realtime Event Type Catalogue: Typed Dataclasses for All Domain Events](./realtime-event-type-catalogue-typed-dataclasses-for-all-domain-events.md) — This module defines the complete catalogue of realtime events as Python dataclasses, covering workspace lifecycle, group and message operations, presence, typing, file uploads, sessions, agent streaming, and notifications. Each concrete event subclass declares a `ClassVar EVENT_TYPE` string that is automatically applied in `__post_init__`, ensuring the `type` field is always set correctly without manual assignment at every call site.

### `datetime` — 1 article(s)

- [UTC Datetime Serialization Helper for MongoDB Timestamps](./utc-datetime-serialization-helper-for-mongodb-timestamps.md) — This module provides `iso_utc`, a one-function utility that fixes a silent timestamp corruption bug caused by MongoDB and Beanie returning naive datetime objects that JavaScript then misinterprets as local time. By re-anchoring naive datetimes to UTC before formatting, it ensures all serialized timestamps carry an unambiguous UTC offset.

### `decision pipeline` — 2 article(s)

- [Instinct Decision Pipeline — HTTP Router](./instinct-decision-pipeline-http-router.md) — Provides the FastAPI HTTP surface for the Instinct decision pipeline, handling action proposals, approvals with optional human edits, rejections, and corrections listing. It enforces that soul bridge failures never block an approval and that edit diffs are captured and stored before the status transition.
- [Instinct Decision Pipeline — Package Entry Point](./instinct-decision-pipeline-package-entry-point.md) — The `ee.instinct` package is PocketPaw's human-in-the-loop decision engine: agents propose actions, humans approve or edit them, approved actions execute, and any edits are captured as corrections that feed back into the soul for future improvement. This module re-exports every public symbol from the five sub-modules so consumers import from a single namespace.

### `deep work` — 1 article(s)

- [Deep Work E2E: Project Lifecycle, Output Chaining, Auto-Retry, Timeout, and Cancellation](./deep-work-e2e-project-lifecycle-output-chaining-auto-retry-timeout-and-cancellat.md) — This test suite exercises PocketPaw's Deep Work (multi-agent task orchestration) system end-to-end through the FastAPI test client, covering project approval, task dependency cascades, output persistence, auto-retry on failure, timeout triggers, project cancellation in all lifecycle states, and the manual retry and skip APIs.

### `deep-work` — 18 article(s)

- [Deep Work Data Models — Project, TaskSpec, AgentSpec, and PlannerResult](./deep-work-data-models-project-taskspec-agentspec-and-plannerresult.md) — This module defines the core data structures for the Deep Work orchestration layer: Project (top-level orchestration unit), TaskSpec and AgentSpec (planning-phase blueprints), and PlannerResult (complete planner output). These models separate planning artifacts from execution artifacts, allowing the planner to produce a full plan before any Mission Control objects are created.
- [Deep Work Dependency Scheduler — DAG-Based Task Dispatch with Cycle Detection](./deep-work-dependency-scheduler-dag-based-task-dispatch-with-cycle-detection.md) — DependencyScheduler manages task dispatch order in a Deep Work project by treating tasks as a directed acyclic graph (DAG). It finds ready tasks (all blockers resolved), dispatches them concurrently, handles human task routing, validates for cycles via Kahn's algorithm, and optionally runs in a tick-synchronized mode driven by a SimulationClock.
- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.
- [Deep Work Goal Parser: GoalAnalysis Validation and Parsing Tests](./deep-work-goal-parser-goalanalysis-validation-and-parsing-tests.md) — This test suite covers the GoalParser and GoalAnalysis components in PocketPaw's Deep Work system, validating JSON parsing, domain/complexity/research-depth validation, field clamping, and serialization round-trips. It ensures robust defensive behavior when LLM-generated goal payloads contain invalid or out-of-range values.
- [Deep Work Goal Parser — Structured Goal Analysis via LLM](./deep-work-goal-parser-structured-goal-analysis-via-llm.md) — GoalParser is the first primitive in the Deep Work pipeline, taking raw user input and producing a structured GoalAnalysis that captures domain, complexity, AI/human role split, constraints, and clarification questions. It routes the LLM prompt through AgentRouter, strips code fences from the response, and defensively validates every field before constructing the dataclass.
- [Deep Work Human Task Router — Multi-Channel Notification Fanout](./deep-work-human-task-router-multi-channel-notification-fanout.md) — HumanTaskRouter bridges the Deep Work execution layer and the user by pushing human-required tasks and project lifecycle events to all configured messaging channels via MessageBus broadcast. It covers four notification types: human tasks needing action, agent-completed tasks awaiting review, plans ready for approval, and project completion summaries.
- [Deep Work Human Task Router: Notification Broadcast Tests](./deep-work-human-task-router-notification-broadcast-tests.md) — This test suite validates the HumanTaskRouter, which bridges the automated Deep Work engine and human operators by publishing structured notifications over the MessageBus. It covers task notifications, review routing, plan-ready announcements, and project completion summaries, including edge cases like missing project IDs and empty task lists.
- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.
- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.
- [Deep Work Planner Agent — Four-Phase LLM Project Planning](./deep-work-planner-agent-four-phase-llm-project-planning.md) — PlannerAgent orchestrates the four-phase Deep Work planning pipeline — research, PRD generation, task breakdown, and team assembly — through sequential LLM calls via AgentRouter. It produces a PlannerResult containing the PRD markdown, a list of TaskSpec objects with dependency keys, and recommended AgentSpec team members.
- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.
- [Deep Work Prompt Templates — LLM Instruction Set for Planning Pipeline](./deep-work-prompt-templates-llm-instruction-set-for-planning-pipeline.md) — This module centralizes all LLM prompt templates used by the Deep Work planning pipeline as module-level string constants. Separating prompts from logic enables iteration on prompt text without touching orchestration code, and makes the planning pipeline's LLM behavior fully visible in one file.
- [Deep Work Session: End-to-End Orchestration and Lifecycle Tests](./deep-work-session-end-to-end-orchestration-and-lifecycle-tests.md) — This integration test suite covers the DeepWorkSession, the top-level orchestrator that coordinates planning, task materialization, agent assignment, approval gating, pause/resume, and cancellation for multi-task AI projects. It verifies the full session lifecycle from DRAFT through PLANNING to AWAITING_APPROVAL and beyond, including edge cases like cyclic dependencies, empty plans, and planner exceptions.
- [Deep Work Session — Full Project Lifecycle Orchestrator](./deep-work-session-full-project-lifecycle-orchestrator.md) — DeepWorkSession is the top-level coordinator for a Deep Work project, tying together GoalParser, PlannerAgent, DependencyScheduler, MCTaskExecutor, and HumanTaskRouter into a single class that manages a project from user input through goal analysis, LLM planning, approval, execution, pause, resume, and cancellation. It also handles crash recovery for interrupted projects on startup.
- [Deep Work Task Model Extensions: New Field Tests](./deep-work-task-model-extensions-new-field-tests.md) — This suite tests the Deep Work extensions added to the core Task model in PocketPaw's mission control system, specifically the five new fields—project_id, task_type, blocks, active_description, and estimated_minutes—that enable multi-task project management. It verifies defaults, settability, serialization, backward compatibility, and round-trip fidelity.
- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.
- [Task Skip Semantics: Dependency Unblocking and Project Completion](./task-skip-semantics-dependency-unblocking-and-project-completion.md) — Covers the test suite that validates how SKIPPED tasks interact with the dependency scheduler and project completion logic in PocketPaw's deep-work system. Ensures skipped tasks satisfy blocked_by conditions, contribute to project completion, and appear correctly in progress reporting.
- [Task Status Persistence Tests: Reproducing and Fixing a FastAPI Body vs Query Param Bug](./task-status-persistence-tests-reproducing-and-fixing-a-fastapi-body-vs-query-par.md) — Tests that reproduce and validate the fix for a bug where task status updates via the API did not persist — caused by FastAPI treating a bare `str` parameter as a query param while the frontend sent it as a JSON body. Also validates that `projectTasks` lists are updated alongside the main tasks list.

### `delegation` — 1 article(s)

- [External Agent Delegation — Subprocess Execution of CLI Agents](./external-agent-delegation-subprocess-execution-of-cli-agents.md) — Provides `ExternalAgentDelegate`, a utility for dispatching tasks to external CLI agents (currently only Claude Code CLI) via `asyncio` subprocesses. It captures structured JSON output and surfaces it as a `DelegationResult`, enabling PocketPaw to use external agents as heavyweight tools.

### `dependency injection` — 3 article(s)

- [Files Bootstrap - Composing Providers, Mounts, and Rules into a Router](./files-bootstrap-composing-providers-mounts-and-rules-into-a-router.md) — `bootstrap.py` is the composition root for the files domain: it reads mount configuration, registers the built-in providers (uploads and knowledge base), loads ABAC rules, and returns a fully-configured `APIRouter` ready to be mounted by the application. It is the only place where concrete dependencies are wired together.
- [get_journal FastAPI Dependency: Caching, SOUL_DATA_DIR Override, and Cache Reset Tests](./getjournal-fastapi-dependency-caching-souldatadir-override-and-cache-reset-tests.md) — Tests the `get_journal` FastAPI dependency from `ee.journal_dep`, pinning three contracts that the rest of the `ee/` subsystem depends on: the dep returns a live Journal instance, repeated calls within one process return the same cached instance, and the `SOUL_DATA_DIR` environment variable is honoured at call time — not frozen at import. Also verifies `reset_journal_cache()` as the test-isolation escape hatch.
- [VectorStoreProtocol: Runtime-Checkable Vector DB Interface](./vectorstoreprotocol-runtime-checkable-vector-db-interface.md) — VectorStoreProtocol defines the minimal async interface every vector store adapter must satisfy—`add`, `search`, `delete`, and `get_by_id`—using Python's `@runtime_checkable Protocol` so that implementations can be verified via `isinstance` without requiring inheritance. This creates a clean dependency boundary between the agent loop and any specific vector database backend.

### `dependency management` — 4 article(s)

- [Backend Registry: Lazy Discovery and Plugin Support for Agent Backends](./backend-registry-lazy-discovery-and-plugin-support-for-agent-backends.md) — The backend registry maps short backend names to `(module_path, class_name)` pairs and imports them on demand, so PocketPaw starts cleanly even when optional SDKs like `openai-agents` or `google-adk` are not installed. It also handles legacy backend name migration and exposes a plugin registration API.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.
- [Packaging Sanity Tests: Dependency Conflicts, Version Bounds, and Extra Consistency](./packaging-sanity-tests-dependency-conflicts-version-bounds-and-extra-consistency.md) — Tests that parse `pyproject.toml` directly to catch dependency version conflicts and packaging invariants before they cause runtime failures. Specifically targets the FastAPI/Starlette version conflict introduced by MCP's transitive Starlette dependency, and verifies that dashboard-mode dependencies are included in the core package.

### `dependency-graph` — 2 article(s)

- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.
- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.

### `dependency-management` — 2 article(s)

- [Channel Adapter Base Classes and Auto-Install Utility](./channel-adapter-base-classes-and-auto-install-utility.md) — This module defines ChannelAdapter (a structural Protocol) and BaseChannelAdapter (an abstract base class with lifecycle management), providing the contracts that all channel adapters must satisfy. It also contains the auto_install() utility that lets adapters install their optional Python dependencies at runtime using uv or pip, with a restart-required path for packages that cannot be reloaded in-process.
- [Paw Module Entry Point](./paw-module-entry-point.md) — The paw package's __init__.py exposes only PawConfig from its config submodule, deliberately keeping the import surface minimal to avoid pulling in soul-protocol as a hard dependency for users who install the base pocketpaw package without the soul extra.

### `deployment` — 6 article(s)

- [API Serve: Headless FastAPI Application for External Clients](./api-serve-headless-fastapi-application-for-external-clients.md) — `create_api_app()` builds a standalone FastAPI application with `v1` REST routers, a `/ws` WebSocket endpoint, and full backend infrastructure (message bus, agent loop, scheduler) — but no dashboard frontend assets. This enables the `pocketpaw serve` command to power external clients like the Tauri desktop app or scripts without serving any HTML.
- [Cloudflare Tunnel Manager: Exposing Local PocketPaw Servers to the Public Internet](./cloudflare-tunnel-manager-exposing-local-pocketpaw-servers-to-the-public-interne.md) — The `TunnelManager` class manages a `cloudflared` subprocess to create a Cloudflare Tunnel that exposes a locally running PocketPaw server on a public HTTPS URL, enabling webhook delivery, mobile access, and external agent connectivity without firewall configuration. It supports auto-installation via Homebrew, URL extraction from stderr with a regex, graceful shutdown, and singleton lifecycle registration.
- [Feature Flag Resolution: OSS Settings with Cloud Override Layer](./feature-flag-resolution-oss-settings-with-cloud-override-layer.md) — `features.py` provides a thin feature flag resolution layer that reads from OSS `Settings` but allows the `ee.cloud.features` module to force-override specific capabilities when running in cloud mode. The `ee.cloud` import is lazy, ensuring that OSS builds without the enterprise package continue to work without modification.
- [Headless Channel Runners: Multi-Adapter CLI Mode for PocketPaw](./headless-channel-runners-multi-adapter-cli-mode-for-pocketpaw.md) — `headless.py` provides the CLI runtime functions that launch PocketPaw in headless server mode — either as a single Telegram bot or as multiple channel adapters (Discord, Slack, WhatsApp, Signal, Matrix, Teams, Google Chat) sharing one `AgentLoop` and message bus. It was extracted from `__main__.py` to keep the entry point thin while keeping channel-setup complexity isolated.
- [PocketPaw CLI Entry Point and Server Launcher](./pocketpaw-cli-entry-point-and-server-launcher.md) — The `__main__.py` module is PocketPaw's CLI entry point, handling argument parsing, subcommand dispatch, server startup with automatic port fallback, and channel mode selection. It has evolved significantly from a simple server launcher into a full-featured CLI with subcommands for diagnostics, memory inspection, session management, configuration, and log tailing.
- [Storage Adapter Factory: Environment-Driven Backend Selection for the Upload Pipeline](./storage-adapter-factory-environment-driven-backend-selection-for-the-upload-pipe.md) — The `factory.py` module provides a single `build_adapter()` function that reads the `POCKETPAW_UPLOAD_ADAPTER` environment variable and returns either a local disk adapter or an S3-compatible adapter, enabling backend swaps without config file changes. A defensive `load_dotenv()` call at build time ensures environment variables are visible even when the factory runs before the application lifecycle loads `.env`.

### `deprecation` — 1 article(s)

- [Legacy Audit REST Router (Deprecated)](./legacy-audit-rest-router-deprecated.md) — The original `/api/v1/audit` endpoint is kept as a backwards-compatibility alias that forwards to the canonical `runtime_router` implementation. All responses include `Deprecation: true` and a `Link` header pointing callers to the successor path, enabling a graceful migration without breaking existing integrations.

### `desktop` — 7 article(s)

- [Desktop Launcher Bootstrap: Python Detection, venv Creation, and PocketPaw Installation](./desktop-launcher-bootstrap-python-detection-venv-creation-and-pocketpaw-installa.md) — The Bootstrap module is the entry point for the PocketPaw desktop launcher's first-run experience. It detects a suitable Python interpreter, creates an isolated virtual environment, and installs the PocketPaw package — handling Windows edge cases by downloading an embeddable Python build when none is found.
- [Desktop Launcher Build Pipeline: PyInstaller, Code Signing, DMG, and Inno Setup](./desktop-launcher-build-pipeline-pyinstaller-code-signing-dmg-and-inno-setup.md) — The build script packages the PocketPaw desktop launcher into a distributable native application for macOS and Windows. It chains icon generation, PyInstaller bundling, platform-specific code signing, DMG creation for macOS, and Inno Setup invocation for Windows into a single `build()` function.
- [Desktop Launcher Splash Window: First-Run Progress UI with Threaded Installation](./desktop-launcher-splash-window-first-run-progress-ui-with-threaded-installation.md) — The `SplashWindow` class provides a lightweight tkinter progress window shown during first-run installation. It runs the bootstrap install function in a background thread so the UI remains responsive, and uses `after()` polling to safely update the progress bar from the main thread.
- [Desktop Launcher System Tray Icon: Controls, Auto-Start, Updates, and Notifications](./desktop-launcher-system-tray-icon-controls-auto-start-updates-and-notifications.md) — The `TrayIcon` class provides the persistent system tray presence for the PocketPaw desktop launcher using `pystray`. It exposes a context menu for all runtime operations — starting, stopping, restarting the server, opening the dashboard, managing auto-start, checking for updates, viewing logs, uninstalling, and quitting.
- [Desktop Launcher Uninstaller: Selective Component Removal with Interactive Confirmation](./desktop-launcher-uninstaller-selective-component-removal-with-interactive-confir.md) — The `Uninstaller` class manages the removal of PocketPaw's desktop installation components — venv, uv binary, embedded Python, logs, config, memory, and audit data — with per-component granularity. Each component can be individually included or excluded, and the interactive mode prompts the user before removing sensitive data like memory and audit logs.
- [Desktop Launcher Updater: PyPI Version Checks and In-Place venv Upgrades](./desktop-launcher-updater-pypi-version-checks-and-in-place-venv-upgrades.md) — The `Updater` class polls PyPI for newer PocketPaw releases and applies updates by running `uv pip install --upgrade` (or falling back to pip) inside the existing venv. Dev mode is detected via a marker file and switches the update path from PyPI to a git branch install.
- [Platform Icon Generator: .ico and .icns from a Single PNG Source](./platform-icon-generator-ico-and-icns-from-a-single-png-source.md) — The `make_icons.py` script generates platform-specific application icon files for Windows (`.ico`) and macOS (`.icns`) from a single source `icon.png`. It uses the native macOS `iconutil` tool when available and falls back to a Pillow-based implementation for cross-platform compatibility.

### `desktop integration` — 4 article(s)

- [OpenExplorerTool: Desktop File Explorer Integration](./openexplorertool-desktop-file-explorer-integration.md) — The `OpenExplorerTool` lets a PocketPaw agent open any file or folder in the user's native desktop file manager after completing a task. It delegates the actual OS interaction to the dashboard REST API rather than calling OS APIs directly, which makes it work identically whether the tool is invoked in-process via the tool bridge or from a CLI subprocess spawned by the Claude SDK.
- [Screenshot Tool: Optional Desktop Capture with Graceful Headless Fallback](./screenshot-tool-optional-desktop-capture-with-graceful-headless-fallback.md) — The screenshot module provides a single function for capturing the current display as PNG bytes, using pyautogui when available and returning an actionable error string when it is not. The optional-import pattern ensures PocketPaw's core runtime does not require a GUI dependency, keeping server and headless deployments working without modification.
- [System Status Tool: Real-Time Hardware Metrics with psutil Fallback](./system-status-tool-real-time-hardware-metrics-with-psutil-fallback.md) — The system status tool collects CPU, memory, disk, battery, and uptime metrics using psutil and formats them as a human-readable markdown string for display in the agent UI. When psutil is not installed, it falls back to a limited report using the standard library `platform` module, preserving basic utility without requiring the desktop extras.
- [V1 Router Registration and CORS Configuration Tests](./v1-router-registration-and-cors-configuration-tests.md) — Validates that all expected domain routers are registered in PocketPaw's v1 API surface and that CORS origins include the Tauri desktop app origin. These tests act as a registry contract — any new router added to the system must be explicitly listed, preventing silent omissions that would cause 404s from newly added features.

### `desktop-app` — 2 article(s)

- [Cross-Platform Auto-Start Manager for the Desktop Launcher](./cross-platform-auto-start-manager-for-the-desktop-launcher.md) — Implements start-on-login for the PocketPaw desktop launcher across macOS (launchd plist), Windows (registry Run key), and Linux (XDG .desktop file), handling both frozen PyInstaller executables and source-run scenarios through a unified `AutoStartManager` interface.
- [PocketPaw Desktop Launcher Entry Point](./pocketpaw-desktop-launcher-entry-point.md) — The main entry point for the PocketPaw desktop launcher, handling first-run bootstrap (Python venv setup, pip install), subsequent-run startup (server launch, browser open, tray icon), and headless mode for server environments. Includes a PyInstaller package fixup that repairs relative imports when the frozen executable runs `__main__.py` as a top-level script.

### `desktop-integration` — 1 article(s)

- [Desktop Tools: Screenshot Capture and System Status for Local Agent Sessions](./desktop-tools-screenshot-capture-and-system-status-for-local-agent-sessions.md) — `ScreenshotTool` and `StatusTool` are lightweight desktop interaction tools that give PocketPaw's agent visibility into the local machine — capturing the screen for visual context and querying system health metrics. Both tools are designed for local deployments where the agent runs on the same machine as the user.

### `developer experience` — 2 article(s)

- [Optional Dependency Helper: `require_extra`](./optional-dependency-helper-requireextra.md) — `_compat.py` provides a single utility function that raises a clean `ImportError` with pip install instructions when an optional PocketPaw feature's dependency is missing. This replaces scattered `try/except ImportError` blocks across channel adapters and integrations with a single, consistent error message pattern.
- [Update Check System: Non-Blocking PyPI Version Checks with Release Notes and Styled CLI Notices](./update-check-system-non-blocking-pypi-version-checks-with-release-notes-and-styl.md) — The update check module queries PyPI once per 24 hours to determine whether a newer version of PocketPaw is available, caches the result to a local file, and prints a styled terminal box on TTY sessions when an update is found. It additionally fetches per-version release notes from GitHub with a separate 1-hour cache and tracks which version the user has last acknowledged.

### `developer tooling` — 2 article(s)

- [Logging Setup: Rich Console Output with Secret and PII Scrubbing](./logging-setup-rich-console-output-with-secret-and-pii-scrubbing.md) — `logging_setup.py` configures PocketPaw's logging infrastructure with Rich-formatted console output and two security filters: `SecretFilter` (scrubs API key patterns from all log messages) and `PIILogFilter` (opt-in scrubbing of personally identifiable information). These filters prevent credential and user data leakage through log output regardless of log level.
- [Paw CLI Tests: init, doctor, channels, and serve Command Coverage](./paw-cli-tests-init-doctor-channels-and-serve-command-coverage.md) — This test file validates PocketPaw's `paw` CLI entry point using Click's `CliRunner`, covering the main command group, `init`, `doctor`, `channels`, and `serve` subcommands. It focuses on exit codes, output presence, option parsing, and graceful degradation when optional dependencies like `soul-protocol` are absent.

### `developer utilities` — 2 article(s)

- [Audit Logging Verification Script — Manual Smoke Test for Tool Execution Audit Trail](./audit-logging-verification-script-manual-smoke-test-for-tool-execution-audit-tra.md) — This standalone script provides a manual, human-readable verification that the audit logging pipeline records both `attempt` and `success` entries when a tool is executed through the ToolRegistry. It runs outside the pytest harness and is designed to be invoked directly to confirm audit infrastructure is wired correctly in a live environment.
- [Guardian Agent Verification Script — Manual Safety Layer Smoke Test for ShellTool](./guardian-agent-verification-script-manual-safety-layer-smoke-test-for-shelltool.md) — This standalone script manually validates that the Guardian safety layer correctly permits safe shell commands while blocking dangerous ones, providing a human-readable pass/fail report for the tool permission model. It distinguishes between two blocking mechanisms — regex-based pre-screening in ShellTool and the Guardian agent's semantic analysis — to help developers diagnose which layer is active.

### `developer-experience` — 1 article(s)

- [Update Check System: PyPI Version Polling, Caching, Release Notes, and Styled Notices](./update-check-system-pypi-version-polling-caching-release-notes-and-styled-notice.md) — The update check module polls PyPI for new PocketPaw versions, caches results to avoid repeated network calls, fetches release notes from GitHub, tracks which versions the user has already seen, and displays styled terminal notices. Tests cover version parsing, cache freshness/staleness, network error handling, corrupted cache recovery, async wrapping, TTY detection for notice suppression, and the version-seen tracking mechanism.

### `developer-tools` — 1 article(s)

- [MongoDB Diagnostic: List All Databases and Collection Document Counts](./mongodb-diagnostic-list-all-databases-and-collection-document-counts.md) — A diagnostic script that connects to a local MongoDB instance and prints every non-system database alongside its collections and document counts. It exists to quickly answer the question "where did my data actually land?" when debugging unexpected data routing between the local dev database, the enterprise cloud database, and any other MongoDB instance on the machine.

### `devops` — 1 article(s)

- [Security Audit CLI: Runtime Health Checks and Permission Repair](./security-audit-cli-runtime-health-checks-and-permission-repair.md) — This module provides an async CLI-callable security auditor that checks file permissions, validates the audit log setup, and optionally scans memory files for PII. It was built as a "Phase 1 Quick Wins" hardening measure to catch misconfigurations before they become vulnerabilities.

### `diagnostics` — 13 article(s)

- [API Key Health Checks: Presence, Format Validation, and Encryption Verification](./api-key-health-checks-presence-format-validation-and-encryption-verification.md) — The `api_keys.py` module implements four health checks covering the full lifecycle of API credentials: whether a key exists for the selected backend, whether it matches the expected format prefix, whether required backend packages are installed, and whether the secrets file is Fernet-encrypted rather than plaintext. Together these checks prevent the most common misconfiguration failures that cause silent agent startup errors.
- [Config File Health Checks: Existence, JSON Validity, and Permission Auditing](./config-file-health-checks-existence-json-validity-and-permission-auditing.md) — The `config.py` health check module verifies three properties of PocketPaw's configuration file: that it exists, that it contains valid JSON, and that its filesystem permissions are restricted to 600. These checks run at startup to catch configuration problems before they produce cryptic runtime failures.
- [Health Checks Registry: Modular Check Catalogue with Tiered Execution Lists](./health-checks-registry-modular-check-catalogue-with-tiered-execution-lists.md) — The health checks `__init__.py` is both a re-export facade and a check registry, defining three typed lists — `STARTUP_CHECKS`, `INTEGRATION_CHECKS`, and `CONNECTIVITY_CHECKS` — that the `HealthEngine` iterates to run diagnostics at the right time and in the right order. All individual check functions are re-exported so that existing imports remain stable across refactors.
- [Health Diagnostic Tools: System Checks, Error Logs, and Config Doctor](./health-diagnostic-tools-system-checks-error-logs-and-config-doctor.md) — The `health.py` module provides three diagnostic `BaseTool` subclasses — `HealthCheckTool`, `ErrorLogTool`, and `ConfigDoctorTool` — that give the PocketPaw agent access to the runtime health engine, persistent error log, and configuration validator. These tools exist so the agent can self-diagnose problems when a user reports something is broken, rather than returning a generic error and asking the user to check logs manually.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [Health Engine Package: Singleton Factory for Self-Healing Diagnostics](./health-engine-package-singleton-factory-for-self-healing-diagnostics.md) — The health package `__init__.py` provides a lazy singleton factory `get_health_engine()` that creates and caches a `HealthEngine` instance on first access. The deferred import pattern means the `HealthEngine` class and all its check dependencies are only loaded when health checking is actually needed, keeping startup time minimal.
- [Health Repair Playbooks — Diagnostic Cause-and-Fix Mapping for Check IDs](./health-repair-playbooks-diagnostic-cause-and-fix-mapping-for-check-ids.md) — `playbooks.py` maps each health check ID to a structured diagnostic record describing the symptom, likely causes, and step-by-step fix instructions, providing the data layer for the dashboard's repair guidance UI and the `config_doctor` tool. It also includes a `build_health_summary()` helper that formats check results and playbook context into a human-readable or prompt-injectable text block.
- [LLMRouter: Lightweight Chat Completion Utility for Non-Agent Callers](./llmrouter-lightweight-chat-completion-utility-for-non-agent-callers.md) — `LLMRouter` is a simple, stateful chat utility for one-off completions — not used by agent backends. It auto-detects the best available backend (Ollama first, then cloud providers), maintains a conversation history list, and provides `clear_history()` for resetting between sessions. Its documented limitations (no streaming, no token counting, unbounded history) make its intended scope explicit.
- [MongoDB Cloud Database Diagnostic: Collection Counts and Sample Document Inspection](./mongodb-cloud-database-diagnostic-collection-counts-and-sample-document-inspecti.md) — A targeted diagnostic script for the PocketPaw enterprise cloud MongoDB database that lists collection document counts and prints sample documents from key collections. Unlike the broader `diag_mongo_all.py`, this script focuses on the `paw-cloud` database and respects the `POCKETPAW_CLOUD_MONGO_URI` environment variable for flexible targeting.
- [MongoDB Diagnostic: List All Databases and Collection Document Counts](./mongodb-diagnostic-list-all-databases-and-collection-document-counts.md) — A diagnostic script that connects to a local MongoDB instance and prints every non-system database alongside its collections and document counts. It exists to quickly answer the question "where did my data actually land?" when debugging unexpected data routing between the local dev database, the enterprise cloud database, and any other MongoDB instance on the machine.
- [PocketPaw CLI Diagnostics — Doctor, Ollama, and OpenAI-Compatible Health Checks](./pocketpaw-cli-diagnostics-doctor-ollama-and-openai-compatible-health-checks.md) — diagnostics.py contains the three CLI health check implementations extracted from `__main__.py`: `run_doctor` for a full system health report, `check_ollama` for Ollama connectivity and model tool-calling support, and `check_openai_compatible` for OpenAI-compatible endpoint validation. All three produce ANSI-colored terminal output with structured pass/warn/fail indicators.
- [Role Roundtrip Diagnostic: Verifying User and Assistant Message Persistence](./role-roundtrip-diagnostic-verifying-user-and-assistant-message-persistence.md) — A targeted diagnostic script that writes four messages with alternating `user` and `assistant` roles to MongoDB via the memory manager, then reads them back to verify that roles are persisted and retrieved correctly. It was created to debug a specific class of bug where the agent loop was saving messages but role information was being lost or corrupted during the roundtrip.
- [WebSocket Persistence Diagnostic: Simulating the Chat Message Save Path with Full Debug Logging](./websocket-persistence-diagnostic-simulating-the-chat-message-save-path-with-full.md) — This script simulates the exact code path the WebSocket adapter executes when saving a chat message, but with DEBUG-level logging and no exception swallowing. It was created to surface silent persistence failures that were invisible in normal operation because the WebSocket handler caught and discarded exceptions.

### `directory-navigation` — 1 article(s)

- [DirectoryTreeTool: Jail-Bounded Recursive Directory Listing with Entry Cap](./directorytreetool-jail-bounded-recursive-directory-listing-with-entry-cap.md) — `DirectoryTreeTool` generates a `tree`-style directory listing with configurable depth, hidden-file filtering, and optional file sizes — all bounded by the `file_jail_path` and a hard `MAX_ENTRIES = 500` cap. Path resolution uses `is_relative_to()` to prevent traversal outside the allowed root.

### `discord` — 3 article(s)

- [Discord Channel Adapter — discli Subprocess Bridge](./discord-channel-adapter-discli-subprocess-bridge.md) — DiscliAdapter connects PocketPaw to Discord by spawning a discli serve subprocess and communicating with it over stdin/stdout JSONL, rather than using discord.py directly. This architecture isolates the Discord event loop in a separate process, preventing it from interfering with PocketPaw's asyncio loop, and lets discli handle rate limiting, reconnection, and slash command registration independently.
- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.
- [Discord Gateway — Bot Initialization and MessageBus Integration](./discord-gateway-bot-initialization-and-messagebus-integration.md) — discord_gateway.py provides the `run_discord_bot` entry point that wires the Discord adapter to the MessageBus and starts the AgentLoop for the Discord channel. It translates all Discord-specific configuration from Settings into the adapter, then hands off to the agent infrastructure.

### `discord-integration` — 1 article(s)

- [DiscordCLITool: Full Discord Management via the discli CLI Subprocess](./discordclitool-full-discord-management-via-the-discli-cli-subprocess.md) — `DiscordCLITool` wraps the `discli` command-line tool to give PocketPaw's agent comprehensive Discord server management capabilities — messaging, channel management, moderation, polls, webhooks, and more — all through a single tool that constructs safe subprocess invocations with shell-split argument parsing.

### `document management` — 1 article(s)

- [Google Docs Tools: Read, Create, and Search Documents](./google-docs-tools-read-create-and-search-documents.md) — The `gdocs.py` module provides three `BaseTool` subclasses — `DocsReadTool`, `DocsCreateTool`, and `DocsSearchTool` — that allow the PocketPaw agent to read, create, and search Google Docs documents via the Google Docs API. A helper function `_parse_doc_id` normalizes both raw document IDs and full Google Docs URLs so the agent does not need to extract the ID manually.

### `document processing` — 1 article(s)

- [Knowledge Ingestion: Multi-Source Text Extraction to RawDocs](./knowledge-ingestion-multi-source-text-extraction-to-rawdocs.md) — The ingestion layer extracts clean text from plain text, URLs, PDFs, images (OCR), and DOCX files, producing `RawDoc` objects identified by a content hash. URL ingestion uses `trafilatura` as the primary extractor for its best-in-class boilerplate removal (F1=0.958), with a regex-based HTML strip as a fallback.

### `domain architecture` — 1 article(s)

- [Cross-Domain Event Handlers: Side Effects Without Direct Coupling](./cross-domain-event-handlers-side-effects-without-direct-coupling.md) — This module wires up application-level event handlers that react to events from other domains — such as invite acceptance, message delivery, pocket sharing, and member removal — without importing those domains directly. It is registered once at app startup and acts as the glue layer for cross-cutting side effects.

### `dos-hardening` — 1 article(s)

- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.

### `driver` — 1 article(s)

- [BrowserDriver Tests: Playwright Wrapper for Navigation, Interaction, and Lifecycle Management](./browserdriver-tests-playwright-wrapper-for-navigation-interaction-and-lifecycle-.md) — This test suite validates `BrowserDriver`, PocketPaw's thin wrapper around the Playwright async API. Tests cover initialization modes (headless/headful), async context manager lifecycle, browser launch and viewport configuration, all navigation and interaction operations (navigate, click, type, scroll), snapshot generation, screenshot capture, and defensive checks for using the driver before it's launched.

### `e2e` — 3 article(s)

- [Brew & Co. Business Scenario: End-to-End Fabric and Instinct Pipeline Walkthrough](./brew-co-business-scenario-end-to-end-fabric-and-instinct-pipeline-walkthrough.md) — A narrative-driven end-to-end test that simulates a real small business — a coffee shop on Monday morning — using PocketPaw's `FabricStore` for business objects and `InstinctStore` for the AI decision pipeline. It demonstrates the full operational flow: data modeling, connector sync simulation, threshold-triggered automation, human approval, execution, and audit export.
- [Full Decision Loop: Fabric Threshold Detection to Instinct Audit Export Tests](./full-decision-loop-fabric-threshold-detection-to-instinct-audit-export-tests.md) — End-to-end tests for the complete decision loop from Fabric object creation through threshold rule evaluation to Instinct action proposal, approval, and JSON audit export — all using real store implementations with `tmp_path` SQLite databases. Three scenarios cover single trigger, multiple triggers, and audit trail identity.
- [Live Cloud API Smoke Tests with Backend Reachability Guard](./live-cloud-api-smoke-tests-with-backend-reachability-guard.md) — Smoke tests that run real HTTP calls against a locally running `paw-cloud` backend. The entire module skips automatically when the backend is unreachable, making these safe to include in CI without a live server requirement. Tests cover login, profile retrieval, agent listing, room lifecycle, DMs, and unauthenticated rejection.

### `email` — 1 article(s)

- [Gmail Tools: Search, Read, Send, Label, and Batch Modify Emails](./gmail-tools-search-read-send-label-and-batch-modify-emails.md) — The `gmail.py` module provides eight `BaseTool` subclasses covering the full Gmail management surface: `GmailSearchTool`, `GmailReadTool`, `GmailSendTool`, `GmailListLabelsTool`, `GmailCreateLabelTool`, `GmailModifyTool`, `GmailTrashTool`, and `GmailBatchModifyTool`. All tools carry `trust_level = "high"` because email access is sensitive and requires explicit OAuth authorization; the batch modify tool exists specifically to reduce API round-trips when applying label changes to many messages.

### `embeddable widgets` — 2 article(s)

- [Paw Print Ingest Router: HTTP Surface, CORS, Rate Limiting, and Fabric Mapping Tests](./paw-print-ingest-router-http-surface-cors-rate-limiting-and-fabric-mapping-tests.md) — This test suite covers the HTTP layer of the Paw Print widget system — including owner-authenticated CRUD endpoints, CORS enforcement for the public spec and event ingest routes, payload size limits, per-customer rate limiting, and event-to-Fabric-object mapping via Jinja-style template interpolation.
- [Paw Print Widget Backend: Model Validation, CRUD, and Rate Limiting Tests](./paw-print-widget-backend-model-validation-crud-and-rate-limiting-tests.md) — This test suite covers the backend layer of PocketPaw's Paw Print embeddable widget system — validating Pydantic model constraints (block caps, domain normalization, rate limits, token format), SQLite-backed CRUD operations, event persistence, and the dual rate-limit primitives (overall and per-customer) that protect the public ingest endpoint.

### `embeddings` — 1 article(s)

- [ChromaAdapter: Pluggable Chroma Vector Store with Multi-Provider Embeddings](./chromaadapter-pluggable-chroma-vector-store-with-multi-provider-embeddings.md) — ChromaAdapter wraps ChromaDB's persistent local vector store behind the `VectorStoreProtocol` interface and supports five embedding providers—Chroma's default SentenceTransformer, HuggingFace, OpenAI, Google Gemini, and Voyage AI. Blocking Chroma calls are pushed off the asyncio event loop via `asyncio.to_thread`, keeping the agent loop non-blocking.

### `encryption` — 2 article(s)

- [Credential Store v2 Tests: AES-256-GCM, Argon2id, AEAD, Migration, and Platform Fallbacks](./credential-store-v2-tests-aes-256-gcm-argon2id-aead-migration-and-platform-fallb.md) — This test module validates the v2 upgrade of PocketPaw's `CredentialStore`, which replaces Fernet (PBKDF2-based) encryption with AES-256-GCM authenticated encryption using Argon2id key derivation. It covers the full v2 round-trip, authenticated data enforcement, automatic v1-to-v2 migration with backup, corruption recovery, and cross-platform hardware UUID fallbacks used to derive the encryption key.
- [CredentialStore: Machine-Locked Encrypted Secret Storage with Argon2id + AES-256-GCM](./credentialstore-machine-locked-encrypted-secret-storage-with-argon2id-aes-256-gc.md) — `CredentialStore` encrypts API keys and OAuth tokens to `~/.pocketpaw/secrets.enc` using a key derived from machine identity via Argon2id, ensuring the encrypted file is only usable on the same machine and user account. Version 2 upgraded from Fernet + PBKDF2 to AES-256-GCM + Argon2id with automatic migration and AEAD authentication.

### `end-to-end` — 4 article(s)

- [Dashboard E2E Tests: UI Load, Deep Work View, Agent/Task CRUD, and Navigation](./dashboard-e2e-tests-ui-load-deep-work-view-agenttask-crud-and-navigation.md) — This Playwright-based end-to-end test suite verifies that the PocketPaw dashboard loads correctly in a real browser, that key UI elements (chat view, Deep Work view, agent/task creation modals, sidebar, remote access) are present and functional, and that the dashboard title matches the expected beta branding.
- [Deep Work E2E: Project Lifecycle, Output Chaining, Auto-Retry, Timeout, and Cancellation](./deep-work-e2e-project-lifecycle-output-chaining-auto-retry-timeout-and-cancellat.md) — This test suite exercises PocketPaw's Deep Work (multi-agent task orchestration) system end-to-end through the FastAPI test client, covering project approval, task dependency cascades, output persistence, auto-retry on failure, timeout triggers, project cancellation in all lifecycle states, and the manual retry and skip APIs.
- [E2E Security Tests: WebSocket Authentication Protocol and HTTP Security Headers](./e2e-security-tests-websocket-authentication-protocol-and-http-security-headers.md) — This Playwright-based suite verifies PocketPaw's WebSocket authentication model — that connections succeed from localhost without explicit auth, that the frontend sends an authenticate action as the first WebSocket message, that tokens never appear in WebSocket URLs, and that API endpoints correctly reject unauthenticated requests while static assets remain accessible.
- [E2E Test Configuration: Playwright Browser Setup and Dashboard Server Lifecycle](./e2e-test-configuration-playwright-browser-setup-and-dashboard-server-lifecycle.md) — This conftest manages the full lifecycle of PocketPaw's end-to-end test environment: it checks for Playwright browser installations and skips the entire e2e suite if they are missing, finds a free OS port, starts the dashboard server in a subprocess, waits for it to become ready, and exposes the base URL to all e2e tests through a session-scoped fixture.

### `enterprise` — 23 article(s)

- [Agents Domain Business Logic Service](./agents-domain-business-logic-service.md) — The `AgentService` is the stateless business logic layer for the agents domain, handling agent creation (with slug uniqueness enforcement and eager soul materialisation), updates, discovery, and the new scope get/set operations introduced for the ScopePicker feature.
- [Agents Domain FastAPI Router](./agents-domain-fastapi-router.md) — The agents router exposes the full HTTP API surface for managing AI agents within a workspace — CRUD, knowledge ingestion, profile picture upload, backend discovery, and scope assignment — with license gating and permission checks applied at the router and dependency level.
- [Audit Entry Model for Enterprise Compliance Logging](./audit-entry-model-for-enterprise-compliance-logging.md) — Defines the `AuditEntry` Pydantic model that captures the complete decision context for every auditable action in PocketPaw: who acted, on what data, what the AI recommended, and what actually happened. Field validators enforce closed enum sets for category and status, and serialization helpers bridge between the Pydantic model and SQLite storage.
- [Audit Module Package Initialization](./audit-module-package-initialization.md) — The `pocketpaw.audit` package init re-exports the three public symbols consumers need: `AuditEntry` (the Pydantic model), `AuditStore` (the SQLite backend), and `get_audit_store` (the singleton accessor). This keeps the import path flat for callers while the implementations stay in separate sub-modules.
- [Automation Evaluator: Rule-to-Daemon Sync, Lifecycle, Cooldown, Fire Modes, and Router Integration Tests](./automation-evaluator-rule-to-daemon-sync-lifecycle-cooldown-fire-modes-and-route.md) — Tests for the `AutomationEvaluator` — the runtime that translates `AutomationStore` rules into daemon intentions, evaluates which rules should fire, and dispatches actions. Covers the full evaluator lifecycle, cooldown enforcement, three execution modes (require approval, auto-execute, notify-only), daemon sync/unsync, and the router bridge integration.
- [Correction Soul Bridge: Observe Callbacks and Procedural Memory Promotion Tests](./correction-soul-bridge-observe-callbacks-and-procedural-memory-promotion-tests.md) — This test suite covers the `CorrectionSoulBridge` — the enterprise component that writes user corrections into the Soul Protocol's memory system and promotes recurring correction patterns into procedural memory. It also validates the `InstinctCorrectionsTool`, the agent-facing interface that exposes captured corrections as readable context.
- [Enterprise API Singleton Accessors](./enterprise-api-singleton-accessors.md) — `ee/api.py` provides module-level singleton factory functions for the two main enterprise data stores — `InstinctStore` and `PawPrintStore` — so that both agent tool code and domain routers can reach a shared, lazily-initialised database instance without dependency injection.
- [Enterprise Audit Module (Compliance Logging Placeholder)](./enterprise-audit-module-compliance-logging-placeholder.md) — The `ee/audit/` module is a reserved namespace for enhanced compliance-grade audit logging, intended to extend the instinct pipeline's built-in event log with exportable formats, configurable retention policies, and structured compliance reports for SOC2 and GDPR.
- [Enterprise Authentication Core — JWT, User Management, and Startup Seeding](./enterprise-authentication-core-jwt-user-management-and-startup-seeding.md) — `auth/core.py` implements PocketPaw Enterprise's authentication system using `fastapi-users` with dual JWT transports (cookie for browser, bearer for API/Tauri), and provides idempotent startup seeding functions that ensure a default admin, workspace, chat group, and default agent exist before the first user logs in.
- [Enterprise Automations Module (Trigger Engine Placeholder)](./enterprise-automations-module-trigger-engine-placeholder.md) — The `ee/automations/` module is a reserved namespace for a rule-based trigger engine that will let users define time-based and data-condition-based automations — turning PocketPaw agents from reactive responders into proactive actors.
- [Enterprise Automations Rule CRUD: Store and FastAPI Router Tests](./enterprise-automations-rule-crud-store-and-fastapi-router-tests.md) — Full test coverage for the enterprise `AutomationStore` and its FastAPI router, covering three rule types (threshold, schedule, data change), all CRUD operations, toggle/enable behavior, fire tracking, file persistence, and a complete lifecycle end-to-end test. Every test uses `tmp_path` isolation to avoid touching the user's real `~/.pocketpaw` configuration.
- [Enterprise Cloud Application Bootstrap and Router Mounting](./enterprise-cloud-application-bootstrap-and-router-mounting.md) — `ee/cloud/__init__.py` is the application assembly point for the PocketPaw Enterprise REST API. It provides two entry points: `init_realtime()` which wires up the in-process EventBus, and `mount_cloud()` which registers every domain router, the error handler, static file mounts, and lifecycle hooks onto a FastAPI app instance.
- [Enterprise Extensions (ee/) Package Overview](./enterprise-extensions-ee-package-overview.md) — The `ee/` package is the boundary layer separating PocketPaw's open-source core from its commercially-licensed Enterprise features. It groups capabilities like the instinct decision pipeline, audit/compliance logging, automations engine, and singleton API accessors under a single FSL 1.1 license gate.
- [Enterprise Guard Layer: RBAC, ABAC, Plan Feature Gates, and FastAPI Dependency Tests](./enterprise-guard-layer-rbac-abac-plan-feature-gates-and-fastapi-dependency-tests.md) — Comprehensive tests for PocketPaw's enterprise authorization layer, covering role-based access control (RBAC) enums and guard functions, attribute-based access control (ABAC) policy evaluation, plan feature tables, action-to-role mapping, tool whitelists, and three FastAPI dependency guards (`require_role`, `require_plan_feature`, `require_policy`).
- [Enterprise License Validation and FastAPI Dependency](./enterprise-license-validation-and-fastapi-dependency.md) — A self-contained license system that validates Ed25519-signed license keys for enterprise cloud features. License state is cached process-wide and gates API endpoints via FastAPI `Depends` integration, with a fallback HMAC-SHA256 path for self-hosted deployments that cannot configure asymmetric keys.
- [Fabric Store: Object Type CRUD, Object Lifecycle, Link Traversal, and Query Tests](./fabric-store-object-type-crud-object-lifecycle-link-traversal-and-query-tests.md) — Unit tests for the enterprise `FabricStore` — PocketPaw's ontology-based business object store backed by SQLite. Covers defining and querying object types, creating and updating objects with source tracking, linking objects and traversing relationships, filtering queries, pagination, and cascade deletion.
- [In-Process MCP Binding: Delivering Pocket Context to the Claude Agent SDK](./in-process-mcp-binding-delivering-pocket-context-to-the-claude-agent-sdk.md) — This module registers an in-process MCP server named `pocketpaw_pocket` that exposes a `get_pocket` tool, working around a Windows `CreateProcess` command-line length limit of ~32KB that would be exceeded if pocket documents were embedded directly in the system prompt. The actual pocket data is fetched lazily from `ee/cloud/pockets/agent_context.py` only when the agent calls the tool.
- [Instinct Correction Loop: Patch Computation, Summarization, Store Persistence, and Approve Endpoint Tests](./instinct-correction-loop-patch-computation-summarization-store-persistence-and-a.md) — Covers the "Correction Loop" — the subsystem that detects when a user edits an AI-proposed action during approval, records what changed as structured patches, and surfaces those corrections for soul memory promotion. Tests span the pure diff functions, the `InstinctStore` correction persistence layer, and the `/approve` HTTP endpoint behavior.
- [Knowledge Base Domain Package Initialiser](./knowledge-base-domain-package-initialiser.md) — The package initialiser for the `ee.cloud.kb` Knowledge Base domain, which exposes workspace-scoped KB endpoints including search, ingest, browse, lint, and stats. The comment header documents the package's scope and public surface, serving as the entry point for the KB subsystem.
- [Knowledge Base REST API Router (Per-Workspace Scope)](./knowledge-base-rest-api-router-per-workspace-scope.md) — The primary FastAPI router for workspace-scoped knowledge base operations — search, ingest, lint, and article browsing — all delegating to the kb Go binary. This router replaced a pure-Python knowledge_base package and defines the REST surface consumed by the wiki pocket template.
- [Microsoft Teams Channel Adapter via Bot Framework SDK](./microsoft-teams-channel-adapter-via-bot-framework-sdk.md) — TeamsAdapter integrates PocketPaw with Microsoft Teams using the Bot Framework SDK, receiving messages via an internal aiohttp webhook server and sending replies through the Bot Framework's `send_activity` API. Streaming is emulated by editing a placeholder message with `update_activity()`.
- [MongoDB Cloud Database Diagnostic: Collection Counts and Sample Document Inspection](./mongodb-cloud-database-diagnostic-collection-counts-and-sample-document-inspecti.md) — A targeted diagnostic script for the PocketPaw enterprise cloud MongoDB database that lists collection document counts and prints sample documents from key collections. Unlike the broader `diag_mongo_all.py`, this script focuses on the `paw-cloud` database and respects the `POCKETPAW_CLOUD_MONGO_URI` environment variable for flexible targeting.
- [Workspace-Level Knowledge Browser Router](./workspace-level-knowledge-browser-router.md) — A FastAPI router mounted at `/api/v1/knowledge/*` that fans out KB article listings across every agent in a workspace. It exists as a separate router from `/api/v1/kb/*` because it has different aggregation semantics — workspace-wide rollup rather than single-scope access.

### `enterprise edition` — 33 article(s)

- [ABAC Policy Engine: Plan Gates, Role Checks, and Agent Ceilings](./abac-policy-engine-plan-gates-role-checks-and-agent-ceilings.md) — The `abac.py` module implements PocketPaw's attribute-based access control (ABAC) by combining three declarative tables — plan feature gates, action-to-role mappings, and per-role tool limits — into a single `evaluate_policy` function. It is designed to run after RBAC role resolution, adding plan-tier and agent-ceiling checks that role membership alone cannot express.
- [ACTIONS Registry: Canonical RBAC Action-to-Role Matrix](./actions-registry-canonical-rbac-action-to-role-matrix.md) — The `actions.py` module is the single source of truth for every guarded operation in PocketPaw, mapping action strings (in `"resource.action"` dotted form) to a minimum required role and a stable machine-readable denial code. Unknown actions fail loudly by design, preventing silent authorization bypasses when new routes are added without a corresponding ACTIONS entry.
- [Automations Router: REST API for Rule-Based Pocket Automations](./automations-router-rest-api-for-rule-based-pocket-automations.md) — The automations router exposes a FastAPI-based CRUD API for managing automation rules and controlling the background evaluator that fires them. It acts as the HTTP boundary between the frontend and the lower-level store, bridge, and evaluator subsystems, ensuring that every rule mutation is immediately synchronized to the daemon via the bridge.
- [AutomationStore: JSON File-Backed Persistence for Automation Rules](./automationstore-json-file-backed-persistence-for-automation-rules.md) — The `AutomationStore` class manages CRUD operations and fire-count tracking for automation rules, persisting state to a JSON file at `~/.pocketpaw/automations/rules.json`. It is accessed as a module-level singleton to ensure all router and evaluator calls operate on the same in-memory rule set.
- [Core RBAC Primitives: Workspace Roles, Pocket Access Levels, and Forbidden Exception](./core-rbac-primitives-workspace-roles-pocket-access-levels-and-forbidden-exceptio.md) — `rbac.py` establishes the foundational enums and exception for PocketPaw's role-based access control: a 3-tier `WorkspaceRole` hierarchy, a 4-tier `PocketAccess` hierarchy, and the `Forbidden` exception that carries machine-readable denial codes. All other guard modules depend on these primitives without circular imports.
- [EEUploadService Core Behavior: Workspace-Scoped Storage and Streaming Tests](./eeuploadservice-core-behavior-workspace-scoped-storage-and-streaming-tests.md) — This test suite verifies that EEUploadService correctly stores uploaded files under a workspace scope, enforces cross-workspace access boundaries, streams content back to authorized requesters, and handles bulk uploads atomically. The in-memory `_MemAdapter` stands in for real cloud storage so tests run without external dependencies.
- [EEUploadService Realtime Event Emission: FileReady and FileDeleted Tests](./eeuploadservice-realtime-event-emission-fileready-and-filedeleted-tests.md) — This suite verifies that `EEUploadService` fires `FileReady` events over the realtime bus when files are uploaded to a chat-scoped context, and `FileDeleted` events when those files are removed. It also verifies that neither event fires when the upload or file lacks a `chat_id`, preventing noise from workspace-level background operations.
- [Enterprise Edition Test Fixtures: License, MongoDB, FastAPI App, S3 Mock, and User/Workspace Factories](./enterprise-edition-test-fixtures-license-mongodb-fastapi-app-s3-mock-and-userwor.md) — This conftest provides the full test infrastructure for PocketPaw's enterprise edition integration tests: HMAC-signed license key injection, isolated in-memory MongoDB via mongomock-motor, a mounted FastAPI app with mocked agent pool, an async HTTP client, callable factories for users and workspaces, and a session-scoped S3 mock via moto.
- [Enterprise Edition Test Package Initializer](./enterprise-edition-test-package-initializer.md) — This `__init__.py` marks `tests/ee/` as a Python package for pytest import resolution and documents in its header comment that it was introduced with the `feat/fleet-journal-emission` feature branch to enable fixture sharing across enterprise edition tests.
- [Fabric Journal Store Tests: Scope Isolation, Pagination, Projection Rebuild, and Policy Correctness](./fabric-journal-store-tests-scope-isolation-pagination-projection-rebuild-and-pol.md) — This suite pins four invariants for PocketPaw's journal-backed Fabric object store: correct lifecycle (create/update/archive), scope-based access control that returns empty results rather than leaking existence, post-filter pagination totals, and deterministic projection rebuild from the event journal. It supersedes a previous implementation that leaked object counts across scope boundaries.
- [Feature Flag Resolution: OSS Settings with Cloud Override Layer](./feature-flag-resolution-oss-settings-with-cloud-override-layer.md) — `features.py` provides a thin feature flag resolution layer that reads from OSS `Settings` but allows the `ee.cloud.features` module to force-override specific capabilities when running in cloud mode. The `ee.cloud` import is lazy, ensuring that OSS builds without the enterprise package continue to work without modification.
- [Fleet Install REST Router](./fleet-install-rest-router.md) — Exposes two HTTP endpoints — listing bundled fleet templates and triggering a fleet install — as part of the enterprise edition's fleet management subsystem. Guards the install route with RBAC and emits correlated audit events into the shared org journal.
- [Fleet Installer Journal Emission Tests: Correlated Events, Backward Compatibility, and Partial Install](./fleet-installer-journal-emission-tests-correlated-events-backward-compatibility-.md) — This suite verifies that the fleet installer emits a correlated sequence of journal events (`fleet.install.started`, `agent.spawned` per soul, `fleet.installed` summary) when given a journal, remains silent when the journal is omitted, suppresses the terminal event on soul-creation failure, and falls back to a fleet-scoped tag when the fleet declares no scopes.
- [Guards Dependency Factories: FastAPI-Injectable RBAC and ABAC Guards](./guards-dependency-factories-fastapi-injectable-rbac-and-abac-guards.md) — The `deps.py` module generates FastAPI dependency callables that enforce authorization on route handlers, pulling workspace membership and role from `request.state` (populated by upstream middleware) and raising `HTTPException(403)` on any denial. It also provides non-FastAPI helper functions for cloud routes that authenticate via fastapi-users and carry full user models rather than request state.
- [Guards Package: Unified Re-Export for RBAC and ABAC Authorization](./guards-package-unified-re-export-for-rbac-and-abac-authorization.md) — The `guards` package `__init__.py` is a single-point re-export that collects all authorization primitives — roles, access levels, policy evaluation, action rules, FastAPI dependency factories, and audit helpers — under one importable namespace. This prevents callers from needing to know which sub-module owns each symbol, and makes future refactoring transparent to the rest of the codebase.
- [Instinct Correction Loop — Diff Capture and Soul Learning Signal](./instinct-correction-loop-diff-capture-and-soul-learning-signal.md) — Defines the data types and diff logic that record what a human changed when editing an agent's proposed action before approving it. These structured diffs are consumed by soul-protocol to bias future proposals toward the human's preferences.
- [Instinct Correction Soul Bridge — Wiring Human Edits to Soul Memory](./instinct-correction-soul-bridge-wiring-human-edits-to-soul-memory.md) — Acts as the connector between the Instinct correction system and soul-protocol's memory tiers, turning captured human edits into episodic observations and promoting repeated field-level corrections into procedural rules. It degrades silently when the soul is unavailable so the approval flow is never blocked.
- [Instinct Decision Pipeline — Core Data Models](./instinct-decision-pipeline-core-data-models.md) — Defines the Pydantic data models and enumerations that underpin the Instinct decision pipeline: the proposed action, its lifecycle states, audit entries, and the contextual metadata that connects an action to its triggering source. These types are the stable contract shared by the store, router, correction system, and any agent that proposes actions.
- [Instinct Decision Pipeline — HTTP Router](./instinct-decision-pipeline-http-router.md) — Provides the FastAPI HTTP surface for the Instinct decision pipeline, handling action proposals, approvals with optional human edits, rejections, and corrections listing. It enforces that soul bridge failures never block an approval and that edit diffs are captured and stored before the status transition.
- [Instinct Decision Pipeline — Package Entry Point](./instinct-decision-pipeline-package-entry-point.md) — The `ee.instinct` package is PocketPaw's human-in-the-loop decision engine: agents propose actions, humans approve or edit them, approved actions execute, and any edits are captured as corrections that feed back into the soul for future improvement. This module re-exports every public symbol from the five sub-modules so consumers import from a single namespace.
- [Instinct Reasoning Trace Types — Decision Audit Context](./instinct-reasoning-trace-types-decision-audit-context.md) — Defines the immutable data types that record the full reasoning context behind each proposed action: the queries made, soul memories consulted, KB articles injected, and tool calls executed. These types power the "Why?" audit drawer and enable humans to verify exactly what evidence the agent used.
- [Instinct Store — Async SQLite Persistence for the Decision Pipeline](./instinct-store-async-sqlite-persistence-for-the-decision-pipeline.md) — Provides all async SQLite read and write operations for the Instinct decision pipeline: action lifecycle management, audit logging, correction recording, reasoning traces, and Fabric object snapshots. It is the single source of truth for the decision state within a PocketPaw enterprise installation.
- [Instinct Trace Collector — Bus-Subscriber Context Manager for Reasoning Capture](./instinct-trace-collector-bus-subscriber-context-manager-for-reasoning-capture.md) — An async context manager that subscribes to the message bus for the duration of a single agent proposal, collecting fabric queries, soul recalls, KB injections, and tool calls into a structured `ReasoningTrace`. Each proposal gets its own isolated collector instance, preventing cross-contamination between concurrent proposals.
- [Org Journal FastAPI Dependency — Shared Audit Log for EE Routes](./org-journal-fastapi-dependency-shared-audit-log-for-ee-routes.md) — Provides a single shared `Journal` instance as a FastAPI dependency, ensuring all enterprise edition routes write their audit events to one canonical org-level log rather than opening separate per-subsystem SQLite files. Supports test isolation via a cache-reset escape hatch.
- [Paw Print Models — Secure Widget Render Vocabulary and Event Types](./paw-print-models-secure-widget-render-vocabulary-and-event-types.md) — Defines the Pydantic models for the Paw Print widget layer with a security-first design: no raw HTML, capped collection sizes, a per-widget rotatable access token, and a 4KB payload cap on inbound events. The models form the contract between the backend and the widget.js bundle.
- [Paw Print Router — HTTP Surface for Widget Management and Event Ingest](./paw-print-router-http-surface-for-widget-management-and-event-ingest.md) — Provides the FastAPI routes for the Paw Print widget layer: public CORS-gated spec serving, owner-authenticated widget CRUD, and a multi-layered event ingest endpoint that enforces domain validation, rate limiting, Guardian screening, and Fabric object mapping before persisting customer interactions.
- [Paw Print Store — Async SQLite Persistence for Widgets and Events](./paw-print-store-async-sqlite-persistence-for-widgets-and-events.md) — Provides all async SQLite read and write operations for the Paw Print widget layer, including widget CRUD with token rotation, append-only event recording, and a dual-level rate-limit enforcement method that checks both overall widget throughput and per-customer throughput within a rolling time window.
- [Paw Print Widget Layer — Package Entry Point](./paw-print-widget-layer-package-entry-point.md) — The `ee.paw_print` package provides the backend for customer-facing embedded widgets that close the decision loop: customer interactions on a rendered widget flow into a Pocket in real time, Instinct nudges the owner, and approved actions feed back to the widget. This module re-exports every public symbol from the two sub-modules.
- [PolicyContext and PolicyResult: Authorization Decision Data Types](./policycontext-and-policyresult-authorization-decision-data-types.md) — `policy.py` defines the two frozen dataclasses that form the data contract for the ABAC evaluation pipeline: `PolicyContext` bundles everything a guard needs to make a decision, and `PolicyResult` carries the outcome with a machine-readable code. Their immutability and slot optimization make them safe to pass across async boundaries without defensive copying.
- [RBAC Action Matrix Tests: Parametrized Role Enforcement and Coverage Guard](./rbac-action-matrix-tests-parametrized-role-enforcement-and-coverage-guard.md) — A parametrized test matrix that exercises every entry in the canonical `ACTIONS` table against every role in the matching role family, verifying that `check_action` allows or raises `Forbidden` with the correct deny code. A meta-test enforces that no new action can be added without both an allow-path and a deny-path assertion, preventing silent coverage gaps.
- [RBAC Audit Helpers: Structured Denial and Privileged Action Logging](./rbac-audit-helpers-structured-denial-and-privileged-action-logging.md) — The `audit.py` module provides two thin wrapper functions — `log_denial` and `log_privileged_action` — that emit structured `AuditEvent` records whenever authorization decisions block or allow a sensitive operation. These wrappers ensure that RBAC events are logged at the correct severity level with machine-readable codes that can be queried, alerted on, or exported to a SIEM.
- [Retrieval Journal Projection — Package Entry Point and Design History](./retrieval-journal-projection-package-entry-point-and-design-history.md) — The `ee.retrieval` package implements an observable retrieval trail and access-count graduation policy as a projection over the org journal, superseding two earlier JSONL-based designs. This module re-exports every public symbol and documents the architectural decision to consolidate onto the journal rather than maintain a parallel JSONL sink.
- [WorkspaceService Realtime Event Emission and Cache Invalidation Tests](./workspaceservice-realtime-event-emission-and-cache-invalidation-tests.md) — This suite verifies that every mutating `WorkspaceService` method fires the correct typed event through `emit()`, and that membership or admin list changes also invalidate the `AudienceResolver` workspace cache. All database and permission primitives are patched at their seams, isolating the emit behavior from persistence concerns.

### `enterprise features` — 10 article(s)

- [Fabric Tools: Ontology Query, Creation, and Statistics](./fabric-tools-ontology-query-creation-and-statistics.md) — The Fabric tools module exposes three agent-facing tools — `FabricQueryTool`, `FabricCreateTool`, and `FabricStatsTool` — that allow the agent to interact with PocketPaw's Fabric ontology: a graph-based knowledge store for objects and their relationships. All three tools use lazy imports and silent telemetry to avoid circular dependencies and ensure tool calls never fail due to missing enterprise modules or sick tracing infrastructure.
- [Fleet Router Test Suite: Template Listing, Install Auth, and Journal Integration](./fleet-router-test-suite-template-listing-install-auth-and-journal-integration.md) — Covers the FastAPI fleet REST router that powers the paw-enterprise InstallFleetPanel, verifying template enumeration, end-to-end install flows, journal event emission, and a layered auth guard (401/403/200) added when the P0 auth requirement landed. Uses FastAPI dependency_overrides throughout so tests remain hermetic — no real Mongo, no real soul-protocol runtime, no writes to ~/.soul/.
- [Instinct Tools: Action Proposal, Pending Queue, and Decision Audit](./instinct-tools-action-proposal-pending-queue-and-decision-audit.md) — The `instinct_tools.py` module provides three `BaseTool` subclasses — `InstinctProposeTool`, `InstinctPendingTool`, and `InstinctAuditTool` — that form the agent-facing surface of PocketPaw's human-in-the-loop decision pipeline. The agent proposes actions rather than executing them autonomously; humans approve or reject from a queue; all decisions are recorded in an audit log the agent can query.
- [InstinctCorrectionsTool: Agent-Facing Surface for Human Edit History](./instinctcorrectionstool-agent-facing-surface-for-human-edit-history.md) — The `InstinctCorrectionsTool` (tool name: `instinct_corrections`) lets an agent fetch the history of human corrections applied to previously proposed actions within a pocket before generating its next proposal. By loading past edits into context, the agent can match the user's preferred tone, thresholds, and style without requiring repeated instruction — effectively turning human corrections into implicit few-shot examples for future drafts.
- [Pocket Journal SSE Stream Router Tests: Filtering, Cursors, and Frame Contract](./pocket-journal-sse-stream-router-tests-filtering-cursors-and-frame-contract.md) — Tests the Server-Sent Events (SSE) endpoint that streams journal events for a specific pocket to the RippleGraphWidget frontend. Covers the event-matching predicate, backlog replay with `since_seq` cursor support for reconnects, the `connected` handshake frame, idle-poll timeout, and strict isolation so events from other pockets never appear in a pocket's stream.
- [Retrieval Journal Projection Tests: Write Path, Scope Filtering, Graduation Policy, and REST Router](./retrieval-journal-projection-tests-write-path-scope-filtering-graduation-policy-.md) — Comprehensive tests for the `ee.retrieval` subsystem that journals every memory-retrieval query and drives a graduation policy promoting frequently-retrieved memories to higher tiers. Covers event write-path fidelity, scope containment, correlation grouping, threshold-based graduation decisions, projection rebuild equivalence, and the REST router's GET endpoints.
- [Wave 2 Shared Test Fixtures: Workspace Factory, User Token, Seeded Channel, and Mock S3 Smoke Tests](./wave-2-shared-test-fixtures-workspace-factory-user-token-seeded-channel-and-mock.md) — Smoke tests that validate the four shared pytest fixtures defined in `tests/ee/conftest.py` for the Wave 2 ee/cloud test infrastructure. Each test exercises exactly one factory (workspace creation, user authentication, channel seeding, and in-memory S3), proving the conftest wiring works end-to-end before the feature-level tests that depend on it are run.
- [Widget Co-occurrence Accept/Dismiss Endpoint Tests: Route Separation, Validation, and Signature Echo](./widget-co-occurrence-acceptdismiss-endpoint-tests-route-separation-validation-an.md) — Tests the two writer endpoints (`POST /widgets/cooccurrence/accept` and `POST /widgets/cooccurrence/dismiss`) that record user decisions on co-occurring widget suggestions from the SuggestedWidgetsFeed. Pins happy-path response shape, Pydantic validation gates, scope fallback to `["org:*"]`, strict route separation between the two event types, and a signature-echo contract that prevents feed state corruption if the tokenisation rule changes.
- [Widget Journal Projection Tests: Interactions, Graduation, Co-occurrence Signature Fix, and Router](./widget-journal-projection-tests-interactions-graduation-co-occurrence-signature-.md) — Comprehensive tests for the widget journal projection subsystem covering the write path for interactions, graduations, and co-occurrence events; scope containment in usage roll-ups; the pin-threshold graduation policy; a regression guard for the `sorted(tokens[:6])` co-occurrence signature bug from PR #942; archive rules for inactive widgets; and the three GET router endpoints.
- [Widget Track Endpoint Tests: Interaction Recording, Validation, Scope Fallback, and Projection Integration](./widget-track-endpoint-tests-interaction-recording-validation-scope-fallback-and-.md) — Tests for `POST /widgets/track`, the writer endpoint that records widget interactions from the SuggestedWidgetsFeed UI. Covers the full happy-path event emission, Pydantic validation of required fields, metadata defaulting, correlation-id propagation, sequential seq increments, the `org:*` scope fallback for anonymous actors, and an end-to-end proof that three POSTs reflect correctly in the `GET /widgets/usage` projection.

### `enterprise-cloud` — 15 article(s)

- [Agent-Facing Pocket Context Fetcher](./agent-facing-pocket-context-fetcher.md) — `fetch_pocket_for_agent` retrieves a pocket document and strips sensitive fields before returning it as a compact JSON-safe dict for injection into AI agent tool responses. The function follows a defensive result-envelope pattern, always returning `{"ok": bool, ...}` rather than raising exceptions, so agent tool handlers can branch on success without try/except.
- [Chat Session Document for Pocket and Group Contexts](./chat-session-document-for-pocket-and-group-contexts.md) — The `Session` document stores chat session metadata shared between one-on-one pocket conversations and multi-user group chats, using a `context_type` discriminator validated on construction. It tracks message counts, last activity, and soft deletion, with four compound indexes covering the dominant query patterns.
- [In-App Notification Document](./in-app-notification-document.md) — The `Notification` model is the persistence layer for PocketPaw's in-app notification system, storing per-user alerts with typed sources, read state, and optional expiry. It pairs with the `NotificationService` for fan-out and the realtime layer for push delivery.
- [Notification Response Schema](./notification-response-schema.md) — `NotificationResponse` is the Pydantic response model for the notifications API, defining the wire format returned to frontend clients. It normalizes MongoDB document fields into a consistent camelCase-compatible shape with string-typed timestamps for JSON serialization safety.
- [Notification Service: CRUD and Realtime Fan-Out](./notification-service-crud-and-realtime-fan-out.md) — The `NotificationService` is a stateless class providing four async operations — create, list, mark-read, and clear-all — each of which writes to MongoDB via Beanie and immediately emits a corresponding realtime event for connected WebSocket clients. The service is the single point of truth for notification lifecycle, keeping routers and other domain services free of MongoDB and emit logic.
- [Notifications Domain Package](./notifications-domain-package.md) — The notifications `__init__.py` serves as the package boundary marker for PocketPaw's in-app notifications domain, grouping the service, router, and response schemas into a cohesive module. No symbols are re-exported; the package is imported by the application factory to register the router.
- [Notifications REST API Router](./notifications-rest-api-router.md) — The notifications router exposes three endpoints for listing, marking read, and bulk-clearing a user's in-app notifications, with authentication enforced via `current_active_user` dependency injection. All mutation operations are scoped strictly to the authenticated user to prevent cross-user data access.
- [Pocket and Widget Documents](./pocket-and-widget-documents.md) — The `Pocket` document is the primary workspace unit in PocketPaw — a named canvas that holds an ordered grid of `Widget` sub-documents, a team roster, assigned agents, and a ripple spec for graph-based automation. Widgets are embedded directly in the Pocket document and carry their own ObjectId so the frontend can address them by stable ID rather than array index.
- [Pocket Journal SSE Stream for RippleGraphWidget](./pocket-journal-sse-stream-for-ripplegraphwidget.md) — The journal stream router serves a live Server-Sent Events feed of soul-protocol journal entries filtered to a single pocket, enabling the frontend RippleGraphWidget to render agent decisions, retrievals, and tool traces as a live causation graph. It supports resumable cursors, cold-start backlog replay, and keepalive heartbeats to survive proxy timeouts.
- [Pockets Domain Package](./pockets-domain-package.md) — The pockets `__init__.py` re-exports the `router` symbol from `ee.cloud.pockets.router`, making the router available as `ee.cloud.pockets.router` for registration by the application factory. This single-line re-export pattern keeps the application factory import simple while preserving module boundaries within the domain.
- [ReadState: Per-User Unread Message Tracking](./readstate-per-user-unread-message-tracking.md) — The `ReadState` document records the last message a user has read in each group, enabling O(1) unread count computation by comparing a single pointer against the group's `message_count` counter. It is updated on `read.ack` WebSocket events and tracks unread mention counts separately.
- [Unified Message Document for Group Chat and Agent Memory](./unified-message-document-for-group-chat-and-agent-memory.md) — The `Message` model stores two structurally different record types — multi-user group chat rows and single-agent LLM memory rows — in one MongoDB collection, discriminated by a `context_type` field. A model validator enforces strict field invariants per context type, preventing cross-contamination of group chat metadata into agent history and vice versa.
- [User and OAuth Account Models](./user-and-oauth-account-models.md) — The `User` document extends `fastapi-users`' Beanie base to add enterprise-specific fields: multi-workspace membership with per-workspace roles, presence status, OAuth provider linkage, and an active workspace pointer. The `WorkspaceMembership` embedded model tracks role and join timestamp per workspace.
- [Workspace Document: Organization Tenancy Model](./workspace-document-organization-tenancy-model.md) — The `Workspace` document is the top-level tenancy boundary in PocketPaw EE, representing one organization or deployment. It records billing plan, seat count, configurable settings, and supports soft deletion, with a unique `slug` index for human-readable URL routing.
- [Workspace Invitation Model](./workspace-invitation-model.md) — The Invite document manages workspace membership invitations sent to email addresses, tracking acceptance state, expiry, and optional group auto-assignment. It enforces a 7-day default TTL with timezone-aware expiry checking to prevent stale invitations from granting unintended access.

### `enterprise-edition` — 5 article(s)

- [Automation Evaluator — Background Condition Polling Engine](./automation-evaluator-background-condition-polling-engine.md) — AutomationEvaluator is a singleton background loop that periodically checks threshold and data_change automation rules against live Fabric data, then fires matched rules through the Instinct pipeline (for approval-required rules) or directly via the daemon (for auto-execute rules). Schedule rules are handled by the bridge/daemon and do not go through the evaluator.
- [Automations Bridge — Syncing EE Rules to Core Daemon Intentions](./automations-bridge-syncing-ee-rules-to-core-daemon-intentions.md) — bridge.py translates enterprise automation rules into core daemon Intention specs, and removes them when rules are deleted. It maps schedule presets and rule types (schedule, threshold, data_change) to cron, interval, and event trigger types respectively, ensuring automation rules run through the same scheduling infrastructure as built-in daemon behaviors.
- [Automations Models — Rule, RuleType, ExecutionMode, and Request Models](./automations-models-rule-ruletype-executionmode-and-request-models.md) — This module defines the Pydantic data models for the EE automations engine: the `Rule` entity (the core automation record), `RuleType` and `ExecutionMode` enums, and `CreateRuleRequest` / `UpdateRuleRequest` for the API layer. The models cover three rule types (threshold, schedule, data_change) with three execution modes (require_approval, auto_execute, notify_only).
- [Automations Subpackage — Rule-Based Automation Engine Namespace](./automations-subpackage-rule-based-automation-engine-namespace.md) — The `pocketpaw.ee.automations` package is the namespace for PocketPaw's rule-based automation engine, grouping the models, bridge, evaluator, and store modules that together implement trigger-action workflows for enterprise pockets.
- [Enterprise Edition Package — Gated Feature Namespace](./enterprise-edition-package-gated-feature-namespace.md) — The `pocketpaw.ee` package is the root namespace for PocketPaw's Enterprise Edition, housing features gated behind team, business, and enterprise plans under the Functional Source License (FSL 1.1). It organizes five EE subsystems: guards (RBAC/ABAC), automations (trigger-action workflows), fabric (workspace mesh), instinct (proactive behaviors), and audit (compliance trail).

### `environment-setup` — 1 article(s)

- [Desktop Launcher Bootstrap: Python Detection, venv Creation, and PocketPaw Installation](./desktop-launcher-bootstrap-python-detection-venv-creation-and-pocketpaw-installa.md) — The Bootstrap module is the entry point for the PocketPaw desktop launcher's first-run experience. It detects a suitable Python interpreter, creates an isolated virtual environment, and installs the PocketPaw package — handling Windows edge cases by downloading an embeddable Python build when none is found.

### `error handling` — 15 article(s)

- [A2A Protocol Error Codes and JSON-RPC 2.0 Error Handling](./a2a-protocol-error-codes-and-json-rpc-20-error-handling.md) — This module defines the full error code registry for the A2A protocol layer — combining standard JSON-RPC 2.0 error codes with A2A-specific extensions — and provides the `JSONRPCError` exception class and helper functions for building well-formed error and success response envelopes.
- [Auth Exception Logging Tests: Silent Swallow Prevention in Auth Dispatch and OAuth2](./auth-exception-logging-tests-silent-swallow-prevention-in-auth-dispatch-and-oaut.md) — Regression tests for issue #627 — verifies that bare `except Exception: pass` blocks in authentication dispatch paths have been replaced with `logger.warning(..., exc_info=True)` calls. Tests cover API key validation, OAuth2 token validation, cookie login branches, and OAuth2 server audit log failures, ensuring auth failures are always logged and always return 401.
- [Cloud Error Hierarchy: Structured HTTP Error Contract Tests](./cloud-error-hierarchy-structured-http-error-contract-tests.md) — This test file validates the `ee.cloud.shared.errors` module, which provides a typed exception hierarchy that maps Python exceptions to HTTP status codes and machine-readable error codes. It ensures that every error class produces the correct status, code, message, and serialization shape expected by API consumers.
- [Drive Connector Error Hierarchy: Typed Exceptions for Auth, Rate Limits, and Not-Found Failures](./drive-connector-error-hierarchy-typed-exceptions-for-auth-rate-limits-and-not-fo.md) — `errors.py` defines a minimal, four-class exception hierarchy for the Drive connector so callers can distinguish auth failures, rate-limit exhaustion, and missing-resource errors without string-sniffing. The deliberate smallness is itself a design decision — each exception maps one-to-one to an actionable recovery path.
- [GWS Skills Auto-Install and GitHub Skill Installer Tests](./gws-skills-auto-install-and-github-skill-installer-tests.md) — This test suite covers `install_skills_from_github`, a shared async helper that clones a GitHub repository and copies SKILL.md-bearing subdirectories into the local skills install directory, with support for prefix filtering and single-skill targeting. It also tests that the GWS preset installation triggers a non-blocking background task that auto-installs Google Workspace skills.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.
- [LLMClient: Centralized Provider Detection, Client Creation, and Environment Setup](./llmclient-centralized-provider-detection-client-creation-and-environment-setup.md) — `LLMClient` is an immutable descriptor that captures a fully resolved LLM provider configuration and exposes methods to create provider-specific async clients. The companion `resolve_backend_env()` function pushes the correct environment variables for the active backend, eliminating manual env-var wrangling when switching providers.
- [LLMRouter: Empty-Response Fallback and Multi-Provider Routing (Issue #664)](./llmrouter-empty-response-fallback-and-multi-provider-routing-issue-664.md) — The LLMRouter test suite focuses on a specific regression (issue #664) where an empty `choices` or `content` list from the upstream LLM API caused an unhandled `IndexError`, crashing the agent loop. Tests also validate backend auto-detection logic for all supported providers — OpenAI-compatible, OpenRouter, Gemini, LiteLLM — and ensure `chat()` routes each provider through the correct internal method.
- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.
- [OpenAI-Compatible Provider: Detection, Env Vars, Client Creation, and Error Formatting](./openai-compatible-provider-detection-env-vars-client-creation-and-error-formatti.md) — Tests for PocketPaw's OpenAI-compatible endpoint support in LLMClient, enabling any OpenAI-compatible API (LiteLLM, Groq, local proxies) as the LLM backend. Covers provider detection, model resolution, environment variable construction for subprocess mode, Anthropic client creation with custom base URL, and structured error formatting for connection and authentication failures.
- [Optional Dependency Helper: `require_extra`](./optional-dependency-helper-requireextra.md) — `_compat.py` provides a single utility function that raises a clean `ImportError` with pip install instructions when an optional PocketPaw feature's dependency is missing. This replaces scattered `try/except ImportError` blocks across channel adapters and integrations with a single, consistent error message pattern.
- [Unified Error Hierarchy for Cloud Domain Modules](./unified-error-hierarchy-for-cloud-domain-modules.md) — This module defines a structured exception hierarchy used across all cloud EE domain packages instead of raw `HTTPException` raises. It standardizes error shape, HTTP status codes, and machine-readable error codes in one place, ensuring consistent API responses and centralized error handling.
- [Upload Error Hierarchy Tests: Typed Exception Codes and Inheritance](./upload-error-hierarchy-tests-typed-exception-codes-and-inheritance.md) — This module validates the upload error hierarchy in `pocketpaw.uploads.errors`, ensuring all domain-specific exceptions inherit from `UploadError`, carry typed `code` attributes, and preserve error messages. These contracts allow API routes and clients to handle upload failures programmatically without string matching.
- [Upload Error Hierarchy: Typed Exceptions for the Upload Pipeline](./upload-error-hierarchy-typed-exceptions-for-the-upload-pipeline.md) — The `uploads/errors.py` module defines a typed exception hierarchy rooted at `UploadError`, with specific subclasses for each failure mode (file too large, unsupported MIME, empty file, not found, access denied, storage failure). Machine-readable `code` class attributes on each exception type enable API layers to translate exceptions into structured error responses without string matching.

### `error-handling` — 2 article(s)

- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.
- [Files Module Typed Exception Hierarchy](./files-module-typed-exception-hierarchy.md) — Defines the complete typed error hierarchy for PocketPaw's cloud files subsystem. Every exception carries a machine-readable `code` string and a matching HTTP status code so the FastAPI router can translate domain failures into consistent API responses without ad-hoc status mapping.

### `evaluator` — 1 article(s)

- [Automation Evaluator: Rule-to-Daemon Sync, Lifecycle, Cooldown, Fire Modes, and Router Integration Tests](./automation-evaluator-rule-to-daemon-sync-lifecycle-cooldown-fire-modes-and-route.md) — Tests for the `AutomationEvaluator` — the runtime that translates `AutomationStore` rules into daemon intentions, evaluates which rules should fire, and dispatches actions. Covers the full evaluator lifecycle, cooldown enforcement, three execution modes (require approval, auto-execute, notify-only), daemon sync/unsync, and the router bridge integration.

### `event bus` — 4 article(s)

- [emit() Facade Tests: Bus Delegation, Error Swallowing, and Initialization Guard](./emit-facade-tests-bus-delegation-error-swallowing-and-initialization-guard.md) — These tests verify the `emit()` top-level facade that service code calls to publish real-time events. The suite ensures emit delegates unchanged event objects to the active bus, swallows bus errors to protect callers from infrastructure failures, and raises AssertionError when called before the bus singleton is initialized.
- [EventBus Protocol and In-Process Fan-Out Implementation](./eventbus-protocol-and-in-process-fan-out-implementation.md) — This module defines the EventBus protocol and the InProcessBus implementation that fans out realtime events to WebSocket connections on the same process, routing through AudienceResolver to determine recipients and ConnectionManager to deliver payloads. Module-level singletons with explicit initialisation guards make it safe to wire up during app startup while crashing loudly if accessed before initialisation.
- [InProcessBus Tests: Fan-Out Correctness, Error Isolation, and Module Singleton](./inprocessbus-tests-fan-out-correctness-error-isolation-and-module-singleton.md) — These tests verify InProcessBus — the in-process real-time event bus — covering correct fan-out to all resolved recipients, the WsOutbound payload shape, per-recipient exception isolation so one dead socket does not stop others, graceful audience resolution error swallowing, and the module-level singleton get/set contract.
- [Realtime Bus Initialization and Group Member Resolution Tests](./realtime-bus-initialization-and-group-member-resolution-tests.md) — This test module verifies the wiring of `init_realtime`, ensuring the correct bus backend is selected by default and that the `AudienceResolver` is properly exposed. It also covers `GroupService.list_member_ids` behavior for found and missing groups.

### `event derivation` — 1 article(s)

- [Notification Derivation Tests: Mention, Reaction, and Invite Trigger Verification](./notification-derivation-tests-mention-reaction-and-invite-trigger-verification.md) — These tests verify the three primary notification derivation paths — @mention in a group message, emoji reaction, and workspace invite — correctly call NotificationService.create with the right arguments. They also verify that self-targeted events and reaction removals never produce notifications.

### `event dispatch` — 1 article(s)

- [Realtime emit() Facade: Fire-and-Forget Event Dispatch After Mutations](./realtime-emit-facade-fire-and-forget-event-dispatch-after-mutations.md) — The `emit` function is a thin, fire-and-forget facade that service methods call after successful database writes to push realtime events to connected clients. It never raises exceptions back to the caller — a delivery failure must not roll back or abort the preceding database mutation — while a missing bus initialisation surfaces immediately as an AssertionError.

### `event emission` — 1 article(s)

- [SessionService Realtime Event Emission Tests](./sessionservice-realtime-event-emission-tests.md) — This module tests that every mutating `SessionService` method — create, create_for_pocket, update, delete, and touch — fires the correct realtime event through `emit()` after the database commit. Tests are unit-scoped, patching DB and bus primitives at seam boundaries to isolate emit behavior from persistence.

### `event handling` — 3 article(s)

- [Agent Bridge: Routing Chat Messages to the Agent Pool](./agent-bridge-routing-chat-messages-to-the-agent-pool.md) — The agent bridge connects incoming group-chat `message.sent` events to the PocketPaw agent pool, evaluating each agent's respond mode and streaming responses back over WebSocket. It decouples the chat send path from agent processing by dispatching all work to background tasks, preventing smart-mode LLM calls from blocking message delivery.
- [Cross-Domain Event Handlers: Side Effects Without Direct Coupling](./cross-domain-event-handlers-side-effects-without-direct-coupling.md) — This module wires up application-level event handlers that react to events from other domains — such as invite acceptance, message delivery, pocket sharing, and member removal — without importing those domains directly. It is registered once at app startup and acts as the glue layer for cross-cutting side effects.
- [In-Process Async Event Bus for Cloud Domain Communication](./in-process-async-event-bus-for-cloud-domain-communication.md) — This module implements a lightweight in-process pub/sub event bus that lets cloud domains react to events from other domains without direct imports. It is the backbone for cross-domain side effects in the PocketPaw EE layer, used by the agent bridge, event handlers, and any future domain that needs to observe another.

### `event model` — 1 article(s)

- [Event Model Structural Tests: Base Event Fields and Typed Subclass Wire Type Assignment](./event-model-structural-tests-base-event-fields-and-typed-subclass-wire-type-assi.md) — Two minimal tests verify the structural contract of the Event base model: that every Event has type, data, and ts fields; and that typed subclasses like GroupCreated automatically set the correct wire type string from EVENT_TYPE without the caller needing to pass it.

### `event processing` — 1 article(s)

- [Paw Print Ingest Router: HTTP Surface, CORS, Rate Limiting, and Fabric Mapping Tests](./paw-print-ingest-router-http-surface-cors-rate-limiting-and-fabric-mapping-tests.md) — This test suite covers the HTTP layer of the Paw Print widget system — including owner-authenticated CRUD endpoints, CORS enforcement for the public spec and event ingest routes, payload size limits, per-customer rate limiting, and event-to-Fabric-object mapping via Jinja-style template interpolation.

### `event routing` — 3 article(s)

- [Agent Bridge Event Routing and Wire Type Tests](./agent-bridge-event-routing-and-wire-type-tests.md) — This module verifies that `agent_bridge` routes all group broadcasts through `emit()` rather than directly calling `ws_manager.broadcast_to_group`, and that the four agent stream event types (`AgentStreamStart`, `AgentStreamChunk`, `AgentToolUse`, `AgentStreamEnd`) are correctly defined and importable from the bridge.
- [AudienceResolver Behavioral Tests: Fan-Out Logic, Caching, and Edge Cases](./audienceresolver-behavioral-tests-fan-out-logic-caching-and-edge-cases.md) — These tests verify the complete behavioral contract of AudienceResolver — determining which user IDs receive each event type, including removed-member inclusion, invite routing to admins plus invitee, TTL caching with manual invalidation, session deduplication, and empty-list fallback for unknown event types.
- [AudienceResolver Exhaustive Coverage: All Event Subclasses Must Resolve and Have Unique Wire Types](./audienceresolver-exhaustive-coverage-all-event-subclasses-must-resolve-and-have-.md) — These tests use Python's inspect module to enumerate every Event subclass at runtime and verify AudienceResolver handles each without raising. A companion test enforces that no two event classes share the same EVENT_TYPE wire string, preventing silent routing collisions when new event types are added.

### `event sourcing` — 3 article(s)

- [Fabric Event Payload Types and Action Constants](./fabric-event-payload-types-and-action-constants.md) — This module pins the three canonical event shapes that drive Fabric's journal projection — object created, updated, and archived — as stable action name constants and payload builder functions. It was created specifically to replace PR #938's scope-filtering approach in the legacy SQLite store, which had two unfixable structural bugs: a schema migration gap and a pagination leak.
- [FabricJournalStore — Event-Sourced Object Lifecycle for Fabric](./fabricjournalstore-event-sourced-object-lifecycle-for-fabric.md) — `FabricJournalStore` is the Wave 3 replacement for the scope-filtering slice of PR #938, providing event-sourced create, update, archive, and query operations over Fabric objects backed by the org journal and served through an in-memory projection. Every write appends an `EventEntry` to the journal and immediately folds it into the live projection, so reads after a write are always consistent without a rebuild.
- [FabricProjection — In-Memory Journal Projection for Fabric Objects](./fabricprojection-in-memory-journal-projection-for-fabric-objects.md) — `FabricProjection` replays `fabric.object.*` journal events into an in-memory dictionary to maintain a current-state view of all Fabric objects, applying scope filters before computing query totals to permanently fix the pagination leak that blocked PR #938. It is a deliberately minimal CQRS read model: rebuild from the journal on startup, apply events incrementally, serve scoped queries.

### `event system` — 2 article(s)

- [Events SSE Router Tests: Endpoint Registration and OpenAPI Spec Inclusion](./events-sse-router-tests-endpoint-registration-and-openapi-spec-inclusion.md) — Tests that PocketPaw's server-sent events (SSE) endpoint `/api/v1/events/stream` is correctly registered in the events router, present in the v1 router registry, accessible on the dashboard app, and included in the OpenAPI specification. These structural checks prevent the events channel from being silently dropped during refactors.
- [StatusTracker Tests: Event-Driven Agent State Machine](./statustracker-tests-event-driven-agent-state-machine.md) — Tests for `StatusTracker`, which maintains real-time agent state by consuming `SystemEvent` objects from the internal event bus. Covers state transitions from idle through thinking, tool execution, and error states, plus multi-session concurrency and token usage accumulation.

### `event-driven architecture` — 1 article(s)

- [Internal Async Event Bus: Subscribe, Emit, and Fault Isolation Tests](./internal-async-event-bus-subscribe-emit-and-fault-isolation-tests.md) — This test file validates the `ee.cloud.shared.events` module's async event bus, which enables decoupled communication between cloud subsystems without direct imports. The tests confirm subscription, emission, unsubscription, multi-handler ordering, handler fault isolation, and the existence of a module-level singleton.

### `event-integration` — 2 article(s)

- [Webhook Slot Management Router](./webhook-slot-management-router.md) — Manages the lifecycle of PocketPaw's inbound webhook slots — creating named endpoints, listing them with dynamically constructed URLs, removing them, and rotating their HMAC secrets. All operations require the `admin` scope, reflecting that webhook configuration controls which external systems can trigger agent workflows.
- [Webhook Slot Schema Contracts](./webhook-slot-schema-contracts.md) — Defines the Pydantic models that represent a webhook slot's configuration and the request payloads for creating or identifying webhooks. The name pattern constraint prevents injection-prone characters from reaching URL-based routing.

### `event-sourcing` — 7 article(s)

- [In-Memory Projection for Retrieval and Graduation Events](./in-memory-projection-for-retrieval-and-graduation-events.md) — Provides the read-side of the retrieval domain by replaying the org journal into two in-memory views: a recent-retrievals list (filterable by scope, actor, and pocket) and a per-memory graduation state table. A single replay pass builds both views because they share the same event stream.
- [In-Memory Projection for Widget Interactions, Usage, and Co-occurrence](./in-memory-projection-for-widget-interactions-usage-and-co-occurrence.md) — Provides three unified read views over the widget event stream — per-interaction history, per-widget usage roll-up for graduation decisions, and per-pair co-occurrence counts for suggestion candidates — all rebuilt from a single org journal replay pass. Supersedes the JSONL-based scans from PRs #941 and #942.
- [Journal-Backed Write Store for Retrieval and Graduation Events](./journal-backed-write-store-for-retrieval-and-graduation-events.md) — Provides the write path for the retrieval domain by appending `retrieval.query` and `graduation.applied` events to the org journal and immediately folding them into the shared in-memory projection. Replaces the JSONL-plus-asyncio-lock pattern from PR #936 with SQLite WAL semantics that are multi-process safe by design.
- [Journal-Backed Write Store for Widget Events](./journal-backed-write-store-for-widget-events.md) — Provides the write facade for the widget domain, emitting all five widget action types onto the org journal and folding each emission into the shared in-memory projection for immediate read consistency. Adds a seq-returning variant of the interaction writer to support the POST /widgets/track endpoint's cursor ack requirement.
- [Retrieval and Graduation Event Payload Definitions](./retrieval-and-graduation-event-payload-definitions.md) — Defines the canonical action names and payload builders for the two journal event types that power PocketPaw's retrieval tracing and memory graduation system. These constants anchor the projection, policy, and store layers to a shared, stable vocabulary sourced from soul-protocol's v0.3.1 action catalog.
- [Widget Event Payload Definitions and Co-occurrence Signature Utilities](./widget-event-payload-definitions-and-co-occurrence-signature-utilities.md) — Defines the five canonical action names for the widget journal projection domain, their payload builders, and the stable co-occurrence signature algorithm that fixes a token-sort ordering bug shipped in PR #942. All constants are pinned at the module level so the projection, policy, store, and tests share a single import path.
- [Widget Journal Projection Package — Public API](./widget-journal-projection-package-public-api.md) — Re-exports the complete public surface of the widget journal projection domain, including the store, projection classes, policy functions, and all canonical event names and payload builders. This module also documents why the package supersedes held PRs #941 and #942 and ships the fix for a token-sort bug that broke co-occurrence deduplication.

### `event-system` — 1 article(s)

- [Message Bus Package Public API](./message-bus-package-public-api.md) — The pocketpaw.bus package re-exports the full public surface of the messaging subsystem: event types (InboundMessage, OutboundMessage, SystemEvent, Channel), the MessageBus and its singleton accessor, and the channel adapter contracts (ChannelAdapter protocol and BaseChannelAdapter base class). All runtime code that sends or receives messages imports from here rather than from sub-modules.

### `events` — 3 article(s)

- [File Mutation Domain Events](./file-mutation-domain-events.md) — Declares the Pydantic event shapes published to the `ee.cloud.realtime` bus whenever a file is added, updated, removed, or moved. These schemas form the Phase 1-2 contract between the files subsystem and future realtime subscribers, ensuring the event bus wire format is stable before the subscriber (Phase 4) is built.
- [Message Bus Event Types: Channel, InboundMessage, OutboundMessage, SystemEvent](./message-bus-event-types-channel-inboundmessage-outboundmessage-systemevent.md) — events.py defines the four core data types that flow through PocketPaw's message bus: the Channel enum identifying communication platforms, InboundMessage for received messages, OutboundMessage for agent replies, and SystemEvent for internal runtime signals.
- [Realtime Event Type Catalogue: Typed Dataclasses for All Domain Events](./realtime-event-type-catalogue-typed-dataclasses-for-all-domain-events.md) — This module defines the complete catalogue of realtime events as Python dataclasses, covering workspace lifecycle, group and message operations, presence, typing, file uploads, sessions, agent streaming, and notifications. Each concrete event subclass declares a `ClassVar EVENT_TYPE` string that is automatically applied in `__post_init__`, ensuring the `type` field is always set correctly without manual assignment at every call site.

### `exceptions` — 1 article(s)

- [Upload Error Hierarchy Tests: Typed Exception Codes and Inheritance](./upload-error-hierarchy-tests-typed-exception-codes-and-inheritance.md) — This module validates the upload error hierarchy in `pocketpaw.uploads.errors`, ensuring all domain-specific exceptions inherit from `UploadError`, carry typed `code` attributes, and preserve error messages. These contracts allow API routes and clients to handle upload failures programmatically without string matching.

### `execution` — 1 article(s)

- [Mission Control Task Executor: Security, Concurrency, and Streaming](./mission-control-task-executor-security-concurrency-and-streaming.md) — Tests for MCTaskExecutor, the component that runs agent tasks in the background, streams results, and broadcasts events. Covers singleton lifecycle, UUID validation, error sanitization, duplicate-execution prevention, and background task completion.

### `executor` — 1 article(s)

- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.

### `extensibility` — 3 article(s)

- [Agent Skill Management Schemas](./agent-skill-management-schemas.md) — Defines the Pydantic models for PocketPaw's skill management API — listing installed skills, installing new ones from a GitHub source, and removing existing ones. The install schema enforces a minimum source format to prevent obviously malformed installation requests.
- [CreateSkillTool: Runtime Skill Generation with SKILL.md and Memory Persistence](./createskilltool-runtime-skill-generation-with-skillmd-and-memory-persistence.md) — `CreateSkillTool` lets agents create new reusable skills at runtime by writing SKILL.md files with YAML frontmatter to `~/.claude/skills/`. After creation, it reloads the skill loader and persists the skill's description to long-term memory so the agent remembers it across sessions.
- [Skills Management REST Router](./skills-management-rest-router.md) — Provides REST endpoints for listing installed skills, searching the skills.sh marketplace, installing skills from GitHub sources, removing skills, and force-reloading the skill index. Skills are user-invocable slash commands that extend PocketPaw's capabilities without core code changes.

### `external clients` — 2 article(s)

- [API Layer Package: Versioned REST Endpoints for External Clients](./api-layer-package-versioned-rest-endpoints-for-external-clients.md) — The `api` package provides a versioned REST API layer (`/api/v1/`) alongside backward-compatible aliases at `/api/`, designed for external clients such as the Tauri desktop application and automation scripts. It mounts alongside the dashboard routes rather than replacing them, enabling both UI and programmatic access to the same PocketPaw instance.
- [API Serve: Headless FastAPI Application for External Clients](./api-serve-headless-fastapi-application-for-external-clients.md) — `create_api_app()` builds a standalone FastAPI application with `v1` REST routers, a `/ws` WebSocket endpoint, and full backend infrastructure (message bus, agent loop, scheduler) — but no dashboard frontend assets. This enables the `pocketpaw serve` command to power external clients like the Tauri desktop app or scripts without serving any HTML.

### `external-agents` — 1 article(s)

- [Agent Delegation: ExternalAgentDelegate and DelegateTool Tests](./agent-delegation-externalagentdelegate-and-delegatetool-tests.md) — This suite tests PocketPaw's agent delegation system, which allows a running agent to hand off subtasks to external CLI-based agents (initially Claude Code). It covers the DelegationResult dataclass, availability detection via PATH lookup, unknown agent rejection, and the DelegateTool's trust level, parameter schema, and execution paths including error handling for missing installations.

### `fabric` — 13 article(s)

- [Brew & Co. Business Scenario: End-to-End Fabric and Instinct Pipeline Walkthrough](./brew-co-business-scenario-end-to-end-fabric-and-instinct-pipeline-walkthrough.md) — A narrative-driven end-to-end test that simulates a real small business — a coffee shop on Monday morning — using PocketPaw's `FabricStore` for business objects and `InstinctStore` for the AI decision pipeline. It demonstrates the full operational flow: data modeling, connector sync simulation, threshold-triggered automation, human approval, execution, and audit export.
- [Connector-to-Fabric Pipeline: Stripe Integration, Threshold Rules, and Instinct Proposal Chain Tests](./connector-to-fabric-pipeline-stripe-integration-threshold-rules-and-instinct-pro.md) — End-to-end tests for the data ingestion pipeline that runs from a YAML-defined connector (Stripe) through HTTP execution, Fabric object creation, threshold evaluation, and Instinct action proposal — all without real network calls via patched `httpx`. Covers the full chain including error paths and source deduplication behavior.
- [Fabric Event Payload Types and Action Constants](./fabric-event-payload-types-and-action-constants.md) — This module pins the three canonical event shapes that drive Fabric's journal projection — object created, updated, and archived — as stable action name constants and payload builder functions. It was created specifically to replace PR #938's scope-filtering approach in the legacy SQLite store, which had two unfixable structural bugs: a schema migration gap and a pagination leak.
- [Fabric FastAPI Router — Ontology CRUD and Query Endpoints](./fabric-fastapi-router-ontology-crud-and-query-endpoints.md) — The Fabric router exposes HTTP endpoints for managing the ontology layer: defining object types, creating and querying objects, creating and listing links, and retrieving stats. It uses the legacy SQLite `FabricStore` for all persistence and was updated in Cluster C / PR3 to add `GET /fabric/objects` and `GET /fabric/links` so the PocketDataPanel's Objects and Links sub-tabs render real data.
- [Fabric List Endpoints: Object and Link Filtering, Envelope Shape, and SQL Injection Safety Tests](./fabric-list-endpoints-object-and-link-filtering-envelope-shape-and-sql-injection.md) — Integration tests for the `/fabric/objects` and `/fabric/links` list endpoints added as part of the PocketDataPanel's Objects/Links sub-tab wiring. Covers store-level link filtering by type and source, HTTP envelope shape, type-based filtering, pagination parameter validation, and an explicit SQL injection safety test for the `link_type` parameter.
- [Fabric Module Entry Point — Ontology Layer Public API](./fabric-module-entry-point-ontology-layer-public-api.md) — The `ee/fabric/__init__.py` consolidates the public API of the Fabric ontology layer into a single import surface, exposing both the legacy SQLite store and the newer journal-backed path introduced in Wave 3. The `__all__` list is the canonical documentation of what is stable and intended for external use.
- [Fabric Pydantic Models — Ontology Layer Data Structures](./fabric-pydantic-models-ontology-layer-data-structures.md) — This module defines the Pydantic models that represent the Fabric ontology layer's core entities: property definitions, object types, object instances, directional links between objects, query parameters, and query results. The `_gen_id` function produces time-ordered, prefix-tagged identifiers that balance human readability with sortability.
- [Fabric Scope Policy Engine — Visibility Decisions for Ontology Objects](./fabric-scope-policy-engine-visibility-decisions-for-ontology-objects.md) — This module provides the scope-based visibility engine for Fabric objects, implementing bidirectional containment matching so that a caller with scope `org:sales:*` can see objects tagged `org:sales:leads` and vice versa. It is a local copy of the logic from soul-protocol's `scopes_overlap` function, kept independent to avoid pulling the full engine into minimal runtime slices and to preserve the audit trail of which scope granted access.
- [Fabric Store: Object Type CRUD, Object Lifecycle, Link Traversal, and Query Tests](./fabric-store-object-type-crud-object-lifecycle-link-traversal-and-query-tests.md) — Unit tests for the enterprise `FabricStore` — PocketPaw's ontology-based business object store backed by SQLite. Covers defining and querying object types, creating and updating objects with source tracking, linking objects and traversing relationships, filtering queries, pagination, and cascade deletion.
- [FabricJournalStore — Event-Sourced Object Lifecycle for Fabric](./fabricjournalstore-event-sourced-object-lifecycle-for-fabric.md) — `FabricJournalStore` is the Wave 3 replacement for the scope-filtering slice of PR #938, providing event-sourced create, update, archive, and query operations over Fabric objects backed by the org journal and served through an in-memory projection. Every write appends an `EventEntry` to the journal and immediately folds it into the live projection, so reads after a write are always consistent without a rebuild.
- [FabricProjection — In-Memory Journal Projection for Fabric Objects](./fabricprojection-in-memory-journal-projection-for-fabric-objects.md) — `FabricProjection` replays `fabric.object.*` journal events into an in-memory dictionary to maintain a current-state view of all Fabric objects, applying scope filters before computing query totals to permanently fix the pagination leak that blocked PR #938. It is a deliberately minimal CQRS read model: rebuild from the journal on startup, apply events incrementally, serve scoped queries.
- [FabricStore — Async SQLite Backend for the Ontology Layer](./fabricstore-async-sqlite-backend-for-the-ontology-layer.md) — `FabricStore` is the legacy async SQLite persistence layer for Fabric's ontology data, managing object types, object instances, and directional links. It uses lazy schema initialization, parameterized queries throughout, and a manual cascade-delete pattern because SQLite foreign key enforcement is not enabled by default.
- [Full Decision Loop: Fabric Threshold Detection to Instinct Audit Export Tests](./full-decision-loop-fabric-threshold-detection-to-instinct-audit-export-tests.md) — End-to-end tests for the complete decision loop from Fabric object creation through threshold rule evaluation to Instinct action proposal, approval, and JSON audit export — all using real store implementations with `tmp_path` SQLite databases. Three scenarios cover single trigger, multiple triggers, and audit trail identity.

### `facade pattern` — 1 article(s)

- [emit() Facade Tests: Bus Delegation, Error Swallowing, and Initialization Guard](./emit-facade-tests-bus-delegation-error-swallowing-and-initialization-guard.md) — These tests verify the `emit()` top-level facade that service code calls to publish real-time events. The suite ensures emit delegates unchanged event objects to the active bus, swallows bus errors to protect callers from infrastructure failures, and raises AssertionError when called before the bus singleton is initialized.

### `factory pattern` — 1 article(s)

- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.

### `fastapi` — 2 article(s)

- [FastAPI Router for Retrieval Logs and Graduation Endpoints](./fastapi-router-for-retrieval-logs-and-graduation-endpoints.md) — Exposes the retrieval projection and graduation policy through a FastAPI router, providing read endpoints for recent retrievals and graduation state, plus an action endpoint that triggers a graduation scan and applies decisions. The store is cached per journal instance so the projection is not rebuilt on every request.
- [FastAPI Router for Widget Interactions, Graduation, and Co-occurrence](./fastapi-router-for-widget-interactions-graduation-and-co-occurrence.md) — Exposes the widget journal projection through REST endpoints for reading widget usage stats, graduation state, and co-occurrence suggestions, plus write endpoints for tracking interactions and recording accept/dismiss decisions on suggested widget pairings. The POST /widgets/track endpoint closes a silent data-loss bug where every dashboard interaction 404'd before this change.

### `fault tolerance` — 3 article(s)

- [Agent Router: Registry-Based Backend Selection with Fallback](./agent-router-registry-based-backend-selection-with-fallback.md) — `AgentRouter` is the runtime dispatcher that resolves a configured backend name through the registry, instantiates it, and routes all agent calls through it. It supports a user-configurable ordered list of fallback backends so that a misconfigured or unavailable primary backend degrades gracefully rather than crashing.
- [AgentRouter Fallback and Streaming Tests](./agentrouter-fallback-and-streaming-tests.md) — This test file validates `AgentRouter`'s fault-tolerant backend selection logic, covering hard exception failures, error-event failures, full backend chain exhaustion, streaming event pass-through, and no-fallback configurations. The tests use in-process stub backends injected via `monkeypatch` to exercise the router without real LLM backends.
- [InProcessBus Tests: Fan-Out Correctness, Error Isolation, and Module Singleton](./inprocessbus-tests-fan-out-correctness-error-isolation-and-module-singleton.md) — These tests verify InProcessBus — the in-process real-time event bus — covering correct fan-out to all resolved recipients, the WsOutbound payload shape, per-recipient exception isolation so one dead socket does not stop others, graceful audience resolution error swallowing, and the module-level singleton get/set contract.

### `fault-tolerance` — 1 article(s)

- [PocketPaw Scheduler: Reminders and Self-Audit Scheduling](./pocketpaw-scheduler-reminders-and-self-audit-scheduling.md) — ReminderScheduler wraps APScheduler to provide one-shot and recurring reminders with natural language time parsing, per-entry schema validation, and a corrupt-file quarantine mechanism. It also manages the daily self-audit job as a built-in named APScheduler task.

### `feature flags` — 2 article(s)

- [Cloud Feature Flags - Always-On Overrides for the EE Product](./cloud-feature-flags-always-on-overrides-for-the-ee-product.md) — The `ee/cloud/features.py` module force-enables features that are optional in the OSS build but required for the cloud EE product to function correctly. Currently it overrides `chat_titles_enabled` to always return `True`, ensuring sidebar titles and realtime UI always work in cloud mode.
- [Feature Flag Resolution: OSS Settings with Cloud Override Layer](./feature-flag-resolution-oss-settings-with-cloud-override-layer.md) — `features.py` provides a thin feature flag resolution layer that reads from OSS `Settings` but allows the `ee.cloud.features` module to force-override specific capabilities when running in cloud mode. The `ee.cloud` import is lazy, ensuring that OSS builds without the enterprise package continue to work without modification.

### `file I/O` — 1 article(s)

- [WikiStore: File-Based CRUD for Knowledge Articles](./wikistore-file-based-crud-for-knowledge-articles.md) — WikiStore is the persistence layer for PocketPaw's knowledge subsystem, managing file-based CRUD for raw documents and compiled wiki articles organized by scope. It maintains a predictable three-directory layout per scope and handles index serialization to ensure fast listing without scanning the filesystem on every read.

### `file management` — 3 article(s)

- [MongoDB Metadata Store for EE Workspace-Scoped Uploads](./mongodb-metadata-store-for-ee-workspace-scoped-uploads.md) — MongoFileStore is the persistence layer for EE upload metadata, extending the OSS file store interface with workspace isolation, folder path tracking, and bulk operations for path rewriting and soft deletion. It bridges the adapter-agnostic `FileRecord` abstraction with the EE `FileUpload` Beanie document.
- [Workspace-Scoped Folder Store for the My Files Mount](./workspace-scoped-folder-store-for-the-my-files-mount.md) — FolderStore is a MongoDB-backed CRUD layer for the virtual folder tree in the uploads provider's My Files feature. It handles path normalization, recursive path creation, bulk path rewrites for renames, and soft delete — all constrained to a single workspace.
- [Workspace-Scoped Upload URL Resolver with Cross-Tenant Isolation](./workspace-scoped-upload-url-resolver-with-cross-tenant-isolation.md) — EEUploadResolver translates opaque upload URLs into local filesystem paths while enforcing workspace isolation — a lookup for a file that exists in a different workspace returns `None`, preventing existence leaks across tenants. It is used by agents and media processors that need to access uploaded file content.

### `file storage` — 1 article(s)

- [FileObj Document — File Metadata with Object Storage Backend](./fileobj-document-file-metadata-with-object-storage-backend.md) — A Beanie ODM document that stores file metadata in MongoDB while keeping actual file bytes in S3, GCS, or local storage. Each record captures the owner, storage provider, bucket path, MIME type, and public accessibility flag, acting as the registry layer for a multi-provider object storage abstraction.

### `file system` — 6 article(s)

- [AGENTS.md Loader: Git-Aware File Discovery with Mtime Caching](./agentsmd-loader-git-aware-file-discovery-with-mtime-caching.md) — `AgentsMdLoader` walks the directory tree upward from a starting path — stopping at git repository roots — to find the nearest AGENTS.md file, parses it into a structured `AgentsMd` object, and caches results by `(path, mtime)` so repeated calls within a session cost nothing. A 32 KiB file size cap prevents oversized AGENTS.md files from bloating the agent's system prompt.
- [Files API Tests: Browse, Download, Zip, Write, and Path Traversal Defence](./files-api-tests-browse-download-zip-write-and-path-traversal-defence.md) — This test file covers PocketPaw's `/api/v1/files` router, which exposes local filesystem operations (browse, single-file download, directory zip download, and file write) to the dashboard. It places particular emphasis on path traversal prevention, file jail enforcement, and zip bomb mitigations.
- [Files Router Security Tests: Scope Enforcement and Symlink Jail Traversal Prevention](./files-router-security-tests-scope-enforcement-and-symlink-jail-traversal-prevent.md) — Security regression tests for issues #884 and #886 in PocketPaw's files router: #884 proved any API key (even one with no scopes) could read arbitrary files; #886 proved `/files/download-zip` followed symlinks outside the configured file jail. These tests lock in the fixes and prevent both vulnerabilities from regressing.
- [MediaDownloader: Filename Sanitization, Unique Naming, HTTP Download, and Size Limiting](./mediadownloader-filename-sanitization-unique-naming-http-download-and-size-limit.md) — The `MediaDownloader` test suite validates PocketPaw's utility for safely downloading and storing media files from URLs, covering filename sanitization (path traversal prevention), collision-free unique filename generation, configurable size limits, HTTP error handling, and authenticated downloads. The singleton pattern ensures a single downloader instance manages all media across the application.
- [OpenExplorerTool: Desktop File Explorer Integration](./openexplorertool-desktop-file-explorer-integration.md) — The `OpenExplorerTool` lets a PocketPaw agent open any file or folder in the user's native desktop file manager after completing a task. It delegates the actual OS interaction to the dashboard REST API rather than calling OS APIs directly, which makes it work identically whether the tool is invoked in-process via the tool bridge or from a CLI subprocess spawned by the Claude SDK.
- [SkillLoader: Discovery, Parsing, and Hot-Reload of AgentSkills from Disk](./skillloader-discovery-parsing-and-hot-reload-of-agentskills-from-disk.md) — The `SkillLoader` discovers and parses skills from two filesystem paths — the central AgentSkills directory and a PocketPaw-specific directory — by reading YAML frontmatter and Markdown content from `SKILL.md` files. It supports hot-reloading, invocability filtering, search, and a singleton accessor for shared use across the application.

### `file tools` — 1 article(s)

- [Fetch Tool Path Security and Jail Boundary Tests](./fetch-tool-path-security-and-jail-boundary-tests.md) — This test module validates the security model of PocketPaw's file fetch tool, specifically the `is_safe_path` predicate and `handle_path`/`list_directory` handlers that enforce a configurable jail directory. The tests were created as regression guards for issue #619, where an empty path string could bypass the jail check by resolving to the current working directory.

### `file tracking` — 1 article(s)

- [Recent Files Tracker Tests: Bash Path Heuristic and Tool Path Extraction](./recent-files-tracker-tests-bash-path-heuristic-and-tool-path-extraction.md) — `RecentFilesTracker` maintains a most-recently-used list of files accessed by the agent, enabling context-aware suggestions and memory seeding. These tests cover the Bash command path heuristic (which was missing and is now implemented), tool-specific path extraction for Read/Write/Edit/Bash tools, and tracker deduplication and ordering behavior.

### `file uploads` — 4 article(s)

- [EEUploadService Core Behavior: Workspace-Scoped Storage and Streaming Tests](./eeuploadservice-core-behavior-workspace-scoped-storage-and-streaming-tests.md) — This test suite verifies that EEUploadService correctly stores uploaded files under a workspace scope, enforces cross-workspace access boundaries, streams content back to authorized requesters, and handles bulk uploads atomically. The in-memory `_MemAdapter` stands in for real cloud storage so tests run without external dependencies.
- [EEUploadService Realtime Event Emission: FileReady and FileDeleted Tests](./eeuploadservice-realtime-event-emission-fileready-and-filedeleted-tests.md) — This suite verifies that `EEUploadService` fires `FileReady` events over the realtime bus when files are uploaded to a chat-scoped context, and `FileDeleted` events when those files are removed. It also verifies that neither event fires when the upload or file lacks a `chat_id`, preventing noise from workspace-level background operations.
- [Upload Grant Signing Tests: HMAC Token Integrity and Expiry](./upload-grant-signing-tests-hmac-token-integrity-and-expiry.md) — This small but security-critical test module verifies the `sign_grant` / `verify_grant` pair in PocketPaw's upload subsystem, ensuring that download grants are cryptographically bound to a specific file ID, expire correctly, and cannot be forged or transplanted across files.
- [Upload Service Tests: Validation, Bulk Handling, and Streaming](./upload-service-tests-validation-bulk-handling-and-streaming.md) — This test suite exercises the `UploadService` layer in PocketPaw's file upload subsystem, covering single and bulk file ingestion, magic-byte MIME sniffing, path sanitisation, and ownership-gated streaming and deletion. It uses an in-memory `_FakeAdapter` to isolate tests from real storage backends while still exercising the full service contract.

### `file-delivery` — 1 article(s)

- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.

### `file-handling` — 1 article(s)

- [Media Download Utility for Channel Adapters](./media-download-utility-for-channel-adapters.md) — media.py provides a shared `MediaDownloader` singleton and related utilities that all channel adapters use to save incoming media attachments to local disk. It handles collision-free filename generation, file size enforcement, authenticated downloads, and MIME type guessing.

### `file-management` — 2 article(s)

- [DeliverArtifactTool: Secure File Delivery from Agent Workspace to User Channel](./deliverartifacttool-secure-file-delivery-from-agent-workspace-to-user-channel.md) — `DeliverArtifactTool` lets PocketPaw's agent send generated or downloaded files (images, PDFs, audio) to the user through their current channel — Slack, Discord, or the web dashboard — after verifying the file is within the agent's sandboxed working directory.
- [File Browser and File Operation Schemas](./file-browser-and-file-operation-schemas.md) — Defines the Pydantic models for PocketPaw's file-browser API, covering directory listings, path navigation, recent-file tracking from agent tool use, and file-write requests. These schemas form the contract between the FastAPI backend and the dashboard's file explorer UI.

### `file-store` — 1 article(s)

- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.

### `file-tracking` — 1 article(s)

- [Recent Files Tracker: Agent Tool Usage History](./recent-files-tracker-agent-tool-usage-history.md) — RecentFilesTracker intercepts agent tool calls to extract file paths and maintains a capped LRU list of the 50 most recently accessed files with metadata. It uses lazy loading, a heuristic Bash path extractor via regex, and JSON persistence to power the dashboard's recent files widget.

### `files` — 22 article(s)

- [ABAC Config Tests: Rule Loading and Allow/Deny Logic](./abac-config-tests-rule-loading-and-allowdeny-logic.md) — This module tests the Attribute-Based Access Control (ABAC) configuration layer for the files subsystem, covering YAML rule loading, the allow logic for tagged vs. untagged entries, attribute matching, and the deny-override behavior when a file carries multiple tags.
- [ABAC Rule Loader and Evaluator for File Access Control](./abac-rule-loader-and-evaluator-for-file-access-control.md) — The `abac_config.py` module implements an Attribute-Based Access Control (ABAC) engine for the files domain, loading restriction rules from a YAML configuration file and evaluating them against per-request user attributes and file entry tags. Rules can only restrict access - they cannot grant permissions beyond the RBAC baseline.
- [BaseFolderProvider: Default Provider Stub Implementation](./basefolderprovider-default-provider-stub-implementation.md) — Defines `BaseFolderProvider`, the default base class for all file providers, where every operation raises `ProviderUnsupported` unless overridden. This design enforces capability-based provider contracts: concrete providers only implement the operations they actually support, and the error type communicates unsupported operations cleanly to the API layer.
- [Browse Mount - Per-Mount Paginated File Listing with ABAC and Capability Derivation](./browse-mount-per-mount-paginated-file-listing-with-abac-and-capability-derivatio.md) — The `browse_mount` function handles a single paginated directory listing request against one virtual mount, applying ABAC filtering and deriving fine-grained capabilities for each visible entry. It is the core read path for the Files Tab browser.
- [Built-in File Providers Package Initialiser](./built-in-file-providers-package-initialiser.md) — The package initialiser for the built-in `FolderProvider` implementations under `ee.cloud.files.providers`. This file acts as a namespace anchor, grouping the concrete provider modules (`base`, `kb`, `uploads`) under a single importable package without adding its own logic.
- [File Mutation Domain Events](./file-mutation-domain-events.md) — Declares the Pydantic event shapes published to the `ee.cloud.realtime` bus whenever a file is added, updated, removed, or moved. These schemas form the Phase 1-2 contract between the files subsystem and future realtime subscribers, ensuring the event bus wire format is stable before the subscriber (Phase 4) is built.
- [Files API Router: Unified Listing and v2 Tree/Browse Endpoints](./files-api-router-unified-listing-and-v2-treebrowse-endpoints.md) — Defines the FastAPI router for the EE `/files` endpoints, including the legacy `GET /files` unified listing endpoint and the v2 tree/browse API built on `ProviderRegistry`. The router factory `build_router` composes the registry, ABAC rules, and request-context factory into a single mountable router.
- [Files Bootstrap - Composing Providers, Mounts, and Rules into a Router](./files-bootstrap-composing-providers-mounts-and-rules-into-a-router.md) — `bootstrap.py` is the composition root for the files domain: it reads mount configuration, registers the built-in providers (uploads and knowledge base), loads ABAC rules, and returns a fully-configured `APIRouter` ready to be mounted by the application. It is the only place where concrete dependencies are wired together.
- [Files Domain Package - Unified File Browser and Provider Registry](./files-domain-package-unified-file-browser-and-provider-registry.md) — The `ee/cloud/files/__init__.py` package entrypoint assembles and re-exports the complete public API for the files domain: the provider registry, ABAC schema types, pagination primitives, and the router factory function. It is the single import surface consumers use to mount the files subsystem.
- [Files Module Public Pydantic Schemas](./files-module-public-pydantic-schemas.md) — Defines the canonical Pydantic data models shared across the entire files subsystem: `Permission`, `RequestContext`, `FileEntry`, `FolderNode`, `MountConfig`, `ResolvedMount`, `Page`, and `SearchQuery`. Validators on these models enforce invariants (absolute paths, namespaced IDs) that providers cannot violate, ensuring the API response shape is consistent regardless of which provider produced it.
- [Files Module Typed Exception Hierarchy](./files-module-typed-exception-hierarchy.md) — Defines the complete typed error hierarchy for PocketPaw's cloud files subsystem. Every exception carries a machine-readable `code` string and a matching HTTP status code so the FastAPI router can translate domain failures into consistent API responses without ad-hoc status mapping.
- [Files RBAC and ABAC Permission Evaluator](./files-rbac-and-abac-permission-evaluator.md) — Implements the two-layer permission model for the files module: RBAC permissions come from providers on a per-entry basis, while ABAC rules apply as a post-filter that can only further restrict visibility. The `PermissionsEvaluator` combines both layers to produce the final UI-facing capability list for each file entry.
- [Files Router Bootstrap Integration Test](./files-router-bootstrap-integration-test.md) — This module contains a single integration test that verifies the files router can be mounted into a FastAPI application via build_files_router and that the /files/tree endpoint responds correctly with a 200 status and a JSON body containing the expected tree structure.
- [Files Test Fixtures: FakeProvider, ctx, make_entry, make_mount](./files-test-fixtures-fakeprovider-ctx-makeentry-makemount.md) — This conftest.py provides the shared test infrastructure for the files subsystem: a FakeProvider that implements the full provider protocol in memory, plus factory fixtures for RequestContext, FileEntry, and ResolvedMount. All file provider tests build on these primitives.
- [KbProvider: Knowledge Base File Provider](./kbprovider-knowledge-base-file-provider.md) — Exposes workspace Knowledge Base documents as a browseable file mount in the unified files tree. `KbProvider` delegates reads to a `_KbService` Protocol adapter rather than directly calling the KB FastAPI routes, decoupling the files subsystem from the KB module's current route-and-Go-binary architecture.
- [KbProvider Tests: Workspace Scoping, RBAC, and Mount Template](./kbprovider-tests-workspace-scoping-rbac-and-mount-template.md) — This module tests the KbProvider file provider, which surfaces the knowledge base as a virtual file tree. Tests verify that entries are scoped to the correct workspace, that baseline RBAC rules grant read access to workspace members and manage access to admins, and that the mount path template correctly resolves the workspace ID.
- [Mount Configuration YAML Loader](./mount-configuration-yaml-loader.md) — Provides `load_mounts`, which reads a `mounts.yaml` file, validates each row into a typed `MountConfig` object, and returns them sorted by `order`. Also exposes `resolve_template` for expanding path templates containing workspace or user variables at request time.
- [Parallel File Tree Builder with TTL Cache](./parallel-file-tree-builder-with-ttl-cache.md) — Implements the parallel fan-out logic that queries all registered providers' mount lists simultaneously, merges them into a hierarchical `FolderNode` tree, and applies ABAC mount-level filtering. `CachedTreeBuilder` wraps this with a per-user, per-workspace TTL cache to avoid hammering providers on repeated tree requests.
- [Provider Registry and Mount Path Router](./provider-registry-and-mount-path-router.md) — Implements `ProviderRegistry`, which maps mount path prefixes to registered `FolderProvider` instances and resolves incoming file paths to the correct provider via longest-prefix matching. The `FolderProvider` Protocol definition here is the canonical duck-typed interface contract all providers must satisfy.
- [Unified Files Service: Merging Upload and Drive Sources](./unified-files-service-merging-upload-and-drive-sources.md) — Implements `UnifiedFilesService`, the Cluster E sub-PR 4 facade that merges chat S3 uploads and (stub) Google Drive files into a single flat list for the legacy `GET /files` endpoint. A `_dedupe` function drops duplicates by content identity to prevent the same file appearing twice when sources overlap.
- [UploadsProvider: Personal Upload File Provider with Folder Support](./uploadsprovider-personal-upload-file-provider-with-folder-support.md) — Wraps `MongoFileStore` to expose user-uploaded files and folders as a personal 'My Files' mount in the unified files tree. As the only provider with folder support, it uses a dedicated `folder_store` and renders directory entries with `mime = application/x-directory`.
- [UploadsProvider Tests: Field Mapping, Mounts, RBAC, and Folder Listing](./uploadsprovider-tests-field-mapping-mounts-rbac-and-folder-listing.md) — This module tests the UploadsProvider, which surfaces user file uploads as a virtual file tree with folder hierarchy support. Tests cover field mapping from the raw upload store format to FileEntry, mount resolution, baseline RBAC (owner vs. non-owner vs. admin), and the combined folder+file listing behavior.

### `filesystem` — 4 article(s)

- [DirectoryTreeTool: Jail-Bounded Recursive Directory Listing with Entry Cap](./directorytreetool-jail-bounded-recursive-directory-listing-with-entry-cap.md) — `DirectoryTreeTool` generates a `tree`-style directory listing with configurable depth, hidden-file filtering, and optional file sizes — all bounded by the `file_jail_path` and a hard `MAX_ENTRIES = 500` cap. Path resolution uses `is_relative_to()` to prevent traversal outside the allowed root.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.
- [fetch.py: Jail-Safe File Browser with Pydantic Validation and Telegram Keyboard Support](./fetchpy-jail-safe-file-browser-with-pydantic-validation-and-telegram-keyboard-su.md) — `fetch.py` provides path-traversal-safe file browsing via `is_safe_path()`, a `FetchRequest` Pydantic model, and `handle_path()` / `list_directory()` helpers. The Telegram `InlineKeyboardMarkup` import is optional, enabling interactive directory navigation buttons in Telegram without breaking non-Telegram deployments.
- [Filesystem Tools: Read, Write, List, and Edit Files](./filesystem-tools-read-write-list-and-edit-files.md) — The `filesystem.py` module provides four foundational `BaseTool` subclasses — `ReadFileTool`, `WriteFileTool`, `ListDirTool`, and `EditFileTool` — that give the PocketPaw agent controlled access to the local filesystem. All path operations go through an `is_safe_path` guard that enforces the configured `file_jail_path` boundary, preventing agents from reading or writing outside the permitted sandbox.

### `fixtures` — 6 article(s)

- [Cloud Chat Test Fixtures: In-Memory MongoDB via mongomock-motor](./cloud-chat-test-fixtures-in-memory-mongodb-via-mongomock-motor.md) — The `beanie_memory_db` fixture initialises Beanie against an isolated in-memory MongoDB database for each test using `mongomock-motor`, enabling the full cloud chat test suite to run in CI without a real MongoDB service. It includes a compatibility shim that silences unsupported keyword arguments in mongomock-motor's `list_collection_names` stub.
- [Cloud Tests Shared Fixture: No-Op Realtime Bus](./cloud-tests-shared-fixture-no-op-realtime-bus.md) — This conftest.py installs an autouse fixture that replaces the realtime event bus with a no-op AsyncMock for the duration of every cloud test. This prevents AssertionError crashes when services call emit() in tests that do not start the full application lifecycle.
- [Enterprise Edition Test Fixtures: License, MongoDB, FastAPI App, S3 Mock, and User/Workspace Factories](./enterprise-edition-test-fixtures-license-mongodb-fastapi-app-s3-mock-and-userwor.md) — This conftest provides the full test infrastructure for PocketPaw's enterprise edition integration tests: HMAC-signed license key injection, isolated in-memory MongoDB via mongomock-motor, a mounted FastAPI app with mocked agent pool, an async HTTP client, callable factories for users and workspaces, and a session-scoped S3 mock via moto.
- [Files Test Fixtures: FakeProvider, ctx, make_entry, make_mount](./files-test-fixtures-fakeprovider-ctx-makeentry-makemount.md) — This conftest.py provides the shared test infrastructure for the files subsystem: a FakeProvider that implements the full provider protocol in memory, plus factory fixtures for RequestContext, FileEntry, and ResolvedMount. All file provider tests build on these primitives.
- [Uploads Test Conftest: Isolated Temporary Upload Root Fixture](./uploads-test-conftest-isolated-temporary-upload-root-fixture.md) — This conftest defines `tmp_upload_root`, a pytest fixture that provides an isolated temporary directory for each test in the uploads test suite. It prevents test pollution between file system tests by ensuring every test starts with a clean, empty upload root.
- [Wave 2 Shared Test Fixtures: Workspace Factory, User Token, Seeded Channel, and Mock S3 Smoke Tests](./wave-2-shared-test-fixtures-workspace-factory-user-token-seeded-channel-and-mock.md) — Smoke tests that validate the four shared pytest fixtures defined in `tests/ee/conftest.py` for the Wave 2 ee/cloud test infrastructure. Each test exercises exactly one factory (workspace creation, user authentication, channel seeding, and in-memory S3), proving the conftest wiring works end-to-end before the feature-level tests that depend on it are run.

### `fleet` — 4 article(s)

- [Fleet Installer Journal Emission Tests: Correlated Events, Backward Compatibility, and Partial Install](./fleet-installer-journal-emission-tests-correlated-events-backward-compatibility-.md) — This suite verifies that the fleet installer emits a correlated sequence of journal events (`fleet.install.started`, `agent.spawned` per soul, `fleet.installed` summary) when given a journal, remains silent when the journal is omitted, suppresses the terminal event on soul-creation failure, and falls back to a fleet-scoped tag when the fleet declares no scopes.
- [Fleet Installer — Template Loading, Path Traversal Guard, and Journal Emission](./fleet-installer-template-loading-path-traversal-guard-and-journal-emission.md) — The Fleet installer loads YAML or JSON fleet templates, installs the bundled soul/pocket/connector/scope configuration, and emits correlated journal events for observability. A critical path-traversal security fix was applied in `fix/fleet-install-auth-guard`: string-path inputs from the REST router are now clamped to the bundled templates directory, preventing a workspace admin from coercing the server into reading arbitrary files.
- [Fleet Module Entry Point — One-Command Bundle Installation](./fleet-module-entry-point-one-command-bundle-installation.md) — The `ee/fleet/__init__.py` is the public entry point for PocketPaw's Fleet subsystem, re-exporting the models, installer functions, and report types so callers can manage installable bundles — FleetTemplates — with a single import. Fleet was introduced in Move 7 PR-B to let non-technical operators install a complete agent configuration (soul, pocket, connectors, scopes) in one command.
- [Fleet Pydantic Models — Manifest and Install Report Types](./fleet-pydantic-models-manifest-and-install-report-types.md) — This module defines the Pydantic models for the Fleet subsystem: the `FleetTemplate` manifest (what to install), `FleetConnector` (a single connector registration), `FleetInstallStep` (one step's outcome), and `FleetInstallReport` (the full install result with convenience methods). These models are thin data containers — no new runtime concepts, just structured representations of the install lifecycle.

### `fleet management` — 3 article(s)

- [Fleet Install REST Router](./fleet-install-rest-router.md) — Exposes two HTTP endpoints — listing bundled fleet templates and triggering a fleet install — as part of the enterprise edition's fleet management subsystem. Guards the install route with RBAC and emits correlated audit events into the shared org journal.
- [Fleet Installer: Manifest Loading, Path Traversal Guard, and Install Orchestration Tests](./fleet-installer-manifest-loading-path-traversal-guard-and-install-orchestration-.md) — This test suite covers PocketPaw's fleet installer, which provisions a full AI agent environment (soul, pocket, connectors) from a declarative manifest. It validates YAML and JSON manifest loading, a P0 path-traversal security fix that clamps string inputs to the bundled fleet directory, install orchestration with partial failure handling, and the InstallReport shape.
- [Fleet Router Test Suite: Template Listing, Install Auth, and Journal Integration](./fleet-router-test-suite-template-listing-install-auth-and-journal-integration.md) — Covers the FastAPI fleet REST router that powers the paw-enterprise InstallFleetPanel, verifying template enumeration, end-to-end install flows, journal event emission, and a layered auth guard (401/403/200) added when the P0 auth requirement landed. Uses FastAPI dependency_overrides throughout so tests remain hermetic — no real Mongo, no real soul-protocol runtime, no writes to ~/.soul/.

### `folder management` — 1 article(s)

- [Folder CRUD Integration Tests: Auto-Chain Creation, Rename Propagation, and RBAC Enforcement](./folder-crud-integration-tests-auto-chain-creation-rename-propagation-and-rbac-en.md) — Integration tests for the EE uploads folder system covering folder creation with parent requirement, duplicate detection, automatic folder chain creation on upload, cascade rename of descendant paths, safe and cascading deletion, and permission enforcement distinguishing non-admin strangers from workspace admins.

### `formatting` — 1 article(s)

- [Channel-Aware Markdown Formatting and LLM Prompt Hints](./channel-aware-markdown-formatting-and-llm-prompt-hints.md) — format.py provides two capabilities: a `convert_markdown()` function that translates standard Markdown output from the LLM into each channel's native formatting syntax, and a `CHANNEL_FORMAT_HINTS` dictionary that injects per-channel formatting instructions into the LLM system prompt so the model generates natively formatted text in the first place.

### `frontend` — 1 article(s)

- [Frontend JavaScript Syntax Validation Tests](./frontend-javascript-syntax-validation-tests.md) — This module validates that all JavaScript files in PocketPaw's frontend directory have syntactically correct code by running each file through `node --check`. It also verifies structural contracts — that `app.js` defines the Alpine.js `app()` function and that `websocket.js` references `WebSocket` — catching integration breakage before it reaches users.

### `gateway` — 1 article(s)

- [Telegram Bot Gateway — Agent Runtime Entry Point](./telegram-bot-gateway-agent-runtime-entry-point.md) — bot_gateway.py wires the three core runtime components — MessageBus, TelegramAdapter, and AgentLoop — into a running process for Telegram-based deployments. It handles clean shutdown on cancellation and is the primary entry point when running PocketPaw as a Telegram bot.

### `gemini` — 1 article(s)

- [Google ADK Backend — Gemini Agent Integration via Google Agent Development Kit](./google-adk-backend-gemini-agent-integration-via-google-agent-development-kit.md) — Implements `GoogleADKBackend`, which runs Gemini-powered agents using the official `google-adk` Python SDK. It supports built-in tools (Google Search, code execution), MCP toolset integration via both stdio and SSE transports, custom PocketPaw function tools, and streaming via the ADK's `InMemoryRunner`.

### `github-copilot` — 1 article(s)

- [GitHub Copilot SDK Backend — Copilot CLI Agent Integration](./github-copilot-sdk-backend-copilot-cli-agent-integration.md) — Implements `CopilotSDKBackend`, which wraps the `github-copilot-sdk` Python package to run GitHub Copilot as a PocketPaw agent. The SDK communicates with the `copilot` CLI via JSON-RPC internally, providing streaming, tool use, and multi-provider support (OpenAI, Azure, Anthropic, LiteLLM).

### `goal-parsing` — 1 article(s)

- [Deep Work Goal Parser: GoalAnalysis Validation and Parsing Tests](./deep-work-goal-parser-goalanalysis-validation-and-parsing-tests.md) — This test suite covers the GoalParser and GoalAnalysis components in PocketPaw's Deep Work system, validating JSON parsing, domain/complexity/research-depth validation, field clamping, and serialization round-trips. It ensures robust defensive behavior when LLM-generated goal payloads contain invalid or out-of-range values.

### `google` — 1 article(s)

- [Google ADK Backend — Gemini Agent Integration via Google Agent Development Kit](./google-adk-backend-gemini-agent-integration-via-google-agent-development-kit.md) — Implements `GoogleADKBackend`, which runs Gemini-powered agents using the official `google-adk` Python SDK. It supports built-in tools (Google Search, code execution), MCP toolset integration via both stdio and SSE transports, custom PocketPaw function tools, and streaming via the ADK's `InMemoryRunner`.

### `google-chat` — 1 article(s)

- [Google Chat Channel Adapter — Webhook and Pub/Sub Modes](./google-chat-channel-adapter-webhook-and-pubsub-modes.md) — GoogleChatAdapter connects PocketPaw to Google Workspace Chat via two modes: webhook (for setups where the PocketPaw dashboard receives HTTP POST events from Google) and pubsub (for server-side polling via Google Cloud Pub/Sub). Both modes normalise incoming events to InboundMessage on the bus and use the Google Chat REST API to send replies, with markdown converted to Google Chat's Card format.

### `google-integration` — 1 article(s)

- [Google Calendar Tools: Event Listing, Creation, and Meeting Preparation](./google-calendar-tools-event-listing-creation-and-meeting-preparation.md) — The `calendar.py` module provides three `BaseTool` implementations — `CalendarListTool`, `CalendarCreateTool`, and `CalendarPrepTool` — that give PocketPaw's agent read/write access to Google Calendar and the ability to generate pre-meeting briefings. These tools were part of the Phase 2 Integration Ecosystem and require an authenticated Google OAuth session.

### `google-workspace` — 1 article(s)

- [Google Chat Channel Adapter — Webhook and Pub/Sub Modes](./google-chat-channel-adapter-webhook-and-pubsub-modes.md) — GoogleChatAdapter connects PocketPaw to Google Workspace Chat via two modes: webhook (for setups where the PocketPaw dashboard receives HTTP POST events from Google) and pubsub (for server-side polling via Google Cloud Pub/Sub). Both modes normalise incoming events to InboundMessage on the bus and use the Google Chat REST API to send replies, with markdown converted to Google Chat's Card format.

### `graph` — 1 article(s)

- [Knowledge Indexer: Concept Graph and Backlink Builder](./knowledge-indexer-concept-graph-and-backlink-builder.md) — The knowledge indexer maintains a `KnowledgeIndex` that maps concepts to the articles mentioning them and builds bidirectional backlinks between articles that reference each other's titles. It supports both full rebuilds from scratch and incremental updates when a single article is added or removed.

### `graph database` — 1 article(s)

- [Fabric Tools: Ontology Query, Creation, and Statistics](./fabric-tools-ontology-query-creation-and-statistics.md) — The Fabric tools module exposes three agent-facing tools — `FabricQueryTool`, `FabricCreateTool`, and `FabricStatsTool` — that allow the agent to interact with PocketPaw's Fabric ontology: a graph-based knowledge store for objects and their relationships. All three tools use lazy imports and silent telemetry to avoid circular dependencies and ensure tool calls never fail due to missing enterprise modules or sick tracing infrastructure.

### `graph-algorithms` — 1 article(s)

- [Deep Work Dependency Scheduler — DAG-Based Task Dispatch with Cycle Detection](./deep-work-dependency-scheduler-dag-based-task-dispatch-with-cycle-detection.md) — DependencyScheduler manages task dispatch order in a Deep Work project by treating tasks as a directed acyclic graph (DAG). It finds ready tasks (all blockers resolved), dispatches them concurrently, handles human task routing, validates for cycles via Kahn's algorithm, and optionally runs in a tick-synchronized mode driven by a SimulationClock.

### `group chat` — 1 article(s)

- [Smoke Test: Agent Dispatch Respond-Mode Matrix in Group Chat](./smoke-test-agent-dispatch-respond-mode-matrix-in-group-chat.md) — This script verifies that `_should_agent_respond` correctly routes messages in group chat based on each agent's respond mode (`auto`, `mention_only`, `silent`) and the set of @mentions present. It was written to lock down a real bug where two `auto` agents both fired when only one was mentioned.

### `group management` — 1 article(s)

- [GroupService Realtime Emit Tests](./groupservice-realtime-emit-tests.md) — This test module verifies that every public mutation in GroupService fires the correct realtime event via the bus emit() call. It also confirms that membership-changing operations correctly invalidate the AudienceResolver group cache so downstream subscribers see fresh data.

### `group service` — 1 article(s)

- [Realtime Bus Initialization and Group Member Resolution Tests](./realtime-bus-initialization-and-group-member-resolution-tests.md) — This test module verifies the wiring of `init_realtime`, ensuring the correct bus backend is selected by default and that the `AudienceResolver` is properly exposed. It also covers `GroupService.list_member_ids` behavior for found and missing groups.

### `guardian` — 2 article(s)

- [Comprehensive GuardianAgent Security Filter Tests](./comprehensive-guardianagent-security-filter-tests.md) — This module provides exhaustive coverage of `GuardianAgent` — PocketPaw's Layer 6 AI safety filter — including LLM-based classification, fail-closed behavior on all error conditions, local regex fallback when no API key is present, audit logging for every code path, concurrent safety, singleton correctness, and prompt-injection hardening (issue #873).
- [GuardianAgent Empty Response Regression Tests (Issue #636)](./guardianagent-empty-response-regression-tests-issue-636.md) — This test module documents the fix for issue #636, where an empty `response.content` list from the Anthropic API caused an `IndexError` inside `GuardianAgent.check_command`. The fix implements fail-closed behavior: any empty, malformed, or missing API response defaults to `DANGEROUS` rather than crashing or defaulting to safe.

### `headless channels` — 2 article(s)

- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.
- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.

### `health monitoring` — 15 article(s)

- [API Key Health Checks: Presence, Format Validation, and Encryption Verification](./api-key-health-checks-presence-format-validation-and-encryption-verification.md) — The `api_keys.py` module implements four health checks covering the full lifecycle of API credentials: whether a key exists for the selected backend, whether it matches the expected format prefix, whether required backend packages are installed, and whether the secrets file is Fernet-encrypted rather than plaintext. Together these checks prevent the most common misconfiguration failures that cause silent agent startup errors.
- [Config File Health Checks: Existence, JSON Validity, and Permission Auditing](./config-file-health-checks-existence-json-validity-and-permission-auditing.md) — The `config.py` health check module verifies three properties of PocketPaw's configuration file: that it exists, that it contains valid JSON, and that its filesystem permissions are restricted to 600. These checks run at startup to catch configuration problems before they produce cryptic runtime failures.
- [ErrorStore — Persistent Append-Only JSONL Error Log for the Health Engine](./errorstore-persistent-append-only-jsonl-error-log-for-the-health-engine.md) — `ErrorStore` is PocketPaw's persistent error logging backend, storing health engine errors as newline-delimited JSON in `~/.pocketpaw/health/errors.jsonl` with log rotation, tail-read retrieval, and optional search filtering. It survives server restarts and page refreshes, ensuring operators can inspect errors from previous sessions.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [Health Check Provider Constants — NON_ANTHROPIC and NON_OPENAI Provider Sets](./health-check-provider-constants-nonanthropic-and-nonopenai-provider-sets.md) — This module defines two tuple constants used across the health check subsystem to identify which LLM providers do not require Anthropic or OpenAI API keys, enabling the connectivity router to correctly dispatch provider-specific reachability checks. Centralising these sets prevents scattered string literals and makes adding a new provider a single-file change.
- [Health Checks Registry: Modular Check Catalogue with Tiered Execution Lists](./health-checks-registry-modular-check-catalogue-with-tiered-execution-lists.md) — The health checks `__init__.py` is both a re-export facade and a check registry, defining three typed lists — `STARTUP_CHECKS`, `INTEGRATION_CHECKS`, and `CONNECTIVITY_CHECKS` — that the `HealthEngine` iterates to run diagnostics at the right time and in the right order. All individual check functions are re-exported so that existing imports remain stable across refactors.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [Health Engine Package: Singleton Factory for Self-Healing Diagnostics](./health-engine-package-singleton-factory-for-self-healing-diagnostics.md) — The health package `__init__.py` provides a lazy singleton factory `get_health_engine()` that creates and caches a `HealthEngine` instance on first access. The deferred import pattern means the `HealthEngine` class and all its check dependencies are only loaded when health checking is actually needed, keeping startup time minimal.
- [Health Repair Playbooks — Diagnostic Cause-and-Fix Mapping for Check IDs](./health-repair-playbooks-diagnostic-cause-and-fix-mapping-for-check-ids.md) — `playbooks.py` maps each health check ID to a structured diagnostic record describing the symptom, likely causes, and step-by-step fix instructions, providing the data layer for the dashboard's repair guidance UI and the `config_doctor` tool. It also includes a `build_health_summary()` helper that formats check results and playbook context into a human-readable or prompt-injectable text block.
- [HealthCheckResult — Shared Data Contract for All Health Checks](./healthcheckresult-shared-data-contract-for-all-health-checks.md) — `HealthCheckResult` is the dataclass that all PocketPaw health check functions return, providing a consistent schema with check identity, status severity, human-readable message, and an actionable fix hint. It auto-stamps a UTC ISO-8601 timestamp on creation and includes a `to_dict()` serializer for API and storage use.
- [HealthEngine — Health Check Orchestrator and Error Store Integration](./healthengine-health-check-orchestrator-and-error-store-integration.md) — `HealthEngine` is PocketPaw's central health orchestration class, running startup config/storage checks synchronously and connectivity checks asynchronously, storing results in memory and routing check failures to the persistent `ErrorStore`. It is deliberately LLM-independent so the health system remains operational even when the AI backend is down.
- [Integration Health Checks — Version Updates and Google Workspace CLI Detection](./integration-health-checks-version-updates-and-google-workspace-cli-detection.md) — This module provides two synchronous health check functions: one that queries PyPI to detect whether a newer PocketPaw version is available, and one that verifies whether the `gws` Google Workspace CLI binary is installed in PATH. Both return structured `HealthCheckResult` objects consumed by the health engine.
- [LLM Connectivity Health Checks — Provider-Aware Reachability](./llm-connectivity-health-checks-provider-aware-reachability.md) — This module implements async health checks that verify whether the configured LLM API endpoint is reachable, routing to the correct check function based on the active backend and provider. It covers Anthropic, OpenAI, Google AI, Ollama, OpenRouter, LiteLLM, and generic OpenAI-compatible endpoints, each with a 5-second timeout and structured pass/warn/critical results.
- [Self-Audit Daemon Tests — Stale Sessions, Config Conflicts, Disk Usage, and OAuth Tokens](./self-audit-daemon-tests-stale-sessions-config-conflicts-disk-usage-and-oauth-tok.md) — This test file covers `pocketpaw.daemon.self_audit`, a background health-check daemon that monitors for stale sessions, dangerous config combinations, excessive disk usage, oversized audit logs, and orphaned OAuth tokens. Tests validate each individual check and the full `run_self_audit` orchestrator.
- [Storage Health Checks — Disk Space, Audit Log Writability, and Memory Directory](./storage-health-checks-disk-space-audit-log-writability-and-memory-directory.md) — This module provides three synchronous health checks for PocketPaw's local storage: verifying the config directory does not exceed 500 MB, confirming the audit log file is writable, and checking that the memory directory is accessible. All checks return `HealthCheckResult` objects with actionable fix hints.

### `health-checks` — 1 article(s)

- [PocketPaw CLI Diagnostics — Doctor, Ollama, and OpenAI-Compatible Health Checks](./pocketpaw-cli-diagnostics-doctor-ollama-and-openai-compatible-health-checks.md) — diagnostics.py contains the three CLI health check implementations extracted from `__main__.py`: `run_doctor` for a full system health report, `check_ollama` for Ollama connectivity and model tool-calling support, and `check_openai_compatible` for OpenAI-compatible endpoint validation. All three produce ANSI-colored terminal output with structured pass/warn/fail indicators.

### `health-monitoring` — 1 article(s)

- [Health and Security Audit Schemas](./health-and-security-audit-schemas.md) — Defines Pydantic models for PocketPaw's health-monitoring and security-audit APIs, covering runtime health summaries, error log entries, per-check security results, and self-audit report summaries. These schemas give operators a structured view into the agent's operational integrity.

### `http` — 2 article(s)

- [Critical Gap Coverage: Real HTTP in DirectRESTAdapter and Agent Tools for Fabric and Instinct](./critical-gap-coverage-real-http-in-directrestadapter-and-agent-tools-for-fabric-.md) — This test file was written specifically to close gaps in test coverage identified during a gap analysis — areas where code existed but no test exercised it end-to-end. It covers real HTTP dispatch through `DirectRESTAdapter`, the Fabric agent tools (`FabricQueryTool`, `FabricCreateTool`), and the Instinct agent tools (`InstinctProposeTool`, `InstinctPendingTool`, `InstinctAuditTool`).
- [Generic Inbound Webhook Adapter with Sync and Async Modes](./generic-inbound-webhook-adapter-with-sync-and-async-modes.md) — WebhookAdapter allows any external service — GitHub, Zapier, n8n, Home Assistant, cron scripts — to push events into PocketPaw via HTTP POST. It supports both fire-and-forget (async) mode and a synchronous request-response mode where the HTTP call blocks until the agent produces a reply.

### `human-in-the-loop` — 4 article(s)

- [EE Instinct: Human-in-the-Loop Action Approval System Tests](./ee-instinct-human-in-the-loop-action-approval-system-tests.md) — This test suite validates the full lifecycle of PocketPaw's Instinct subsystem, which gates AI-proposed actions behind human approval before execution. It covers store-layer CRUD, audit trail integrity, REST endpoint contracts, lifecycle state transitions, and edge cases such as approving non-existent actions.
- [Instinct Tools: Action Proposal, Pending Queue, and Decision Audit](./instinct-tools-action-proposal-pending-queue-and-decision-audit.md) — The `instinct_tools.py` module provides three `BaseTool` subclasses — `InstinctProposeTool`, `InstinctPendingTool`, and `InstinctAuditTool` — that form the agent-facing surface of PocketPaw's human-in-the-loop decision pipeline. The agent proposes actions rather than executing them autonomously; humans approve or reject from a queue; all decisions are recorded in an audit log the agent can query.
- [InstinctCorrectionsTool: Agent-Facing Surface for Human Edit History](./instinctcorrectionstool-agent-facing-surface-for-human-edit-history.md) — The `InstinctCorrectionsTool` (tool name: `instinct_corrections`) lets an agent fetch the history of human corrections applied to previously proposed actions within a pocket before generating its next proposal. By loading past edits into context, the agent can match the user's preferred tone, thresholds, and style without requiring repeated instruction — effectively turning human corrections into implicit few-shot examples for future drafts.
- [Plan Mode Approval Schemas](./plan-mode-approval-schemas.md) — Defines the minimal Pydantic models for PocketPaw's plan-mode API — the human-in-the-loop gate where a user approves or rejects an agent's proposed plan before execution begins. The session key binds the approval action to a specific pending plan.

### `human-tasks` — 1 article(s)

- [Deep Work Human Task Router: Notification Broadcast Tests](./deep-work-human-task-router-notification-broadcast-tests.md) — This test suite validates the HumanTaskRouter, which bridges the automated Deep Work engine and human operators by publishing structured notifications over the MessageBus. It covers task notifications, review routing, plan-ready announcements, and project completion summaries, including edge cases like missing project IDs and empty task lists.

### `idempotency` — 1 article(s)

- [NotificationService Unit Tests: CRUD, Fan-Out, and Idempotency Contract](./notificationservice-unit-tests-crud-fan-out-and-idempotency-contract.md) — These unit tests exercise NotificationService in isolation by patching the Notification Beanie document and the emit function, verifying that create persists then emits, list_for_user filters at the database layer, mark_read is idempotent for already-read notifications, and clear_all uses a bulk update and emits a single event.

### `identifier generation` — 1 article(s)

- [Fabric Pydantic Models — Ontology Layer Data Structures](./fabric-pydantic-models-ontology-layer-data-structures.md) — This module defines the Pydantic models that represent the Fabric ontology layer's core entities: property definitions, object types, object instances, directional links between objects, query parameters, and query results. The `_gen_id` function produces time-ordered, prefix-tagged identifiers that balance human readability with sortability.

### `identity` — 2 article(s)

- [Identity API Tests: Reading and Partially Updating Agent Identity Files](./identity-api-tests-reading-and-partially-updating-agent-identity-files.md) — This test file covers PocketPaw's `/api/v1/identity` router, which exposes the agent's persistent identity configuration — the markdown files that define personality, soul, style, instructions, and user profile — as a readable and partially updatable API resource.
- [Soul Protocol REST API Endpoints](./soul-protocol-rest-api-endpoints.md) — Exposes the full Soul Protocol interface over REST, covering identity, OCEAN personality state, core memory editing, memory storage and recall, soul import/export, and rubric-based self-evaluation. All endpoints require the `settings:read` scope and safely handle the case where no soul is loaded.

### `identity management` — 1 article(s)

- [Bootstrap System Tests: System Prompt Assembly, Identity File Caching, and Channel Hints](./bootstrap-system-tests-system-prompt-assembly-identity-file-caching-and-channel-.md) — This test suite validates PocketPaw's bootstrap pipeline, which reads identity configuration files (IDENTITY.md, SOUL.md, STYLE.md, USER.md) and assembles them into a structured system prompt for the AI agent. Tests cover the XML-tag layout contract of `BootstrapContext`, the mtime-based file cache in `DefaultBootstrapProvider`, and the channel-specific formatting hints added by `AgentContextBuilder`.

### `identity system` — 1 article(s)

- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.

### `image generation` — 1 article(s)

- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.

### `indian-languages` — 1 article(s)

- [TranslateTool: English–Indian Language Translation via Sarvam AI Mayura](./translatetool-englishindian-language-translation-via-sarvam-ai-mayura.md) — `TranslateTool` integrates Sarvam AI's Mayura translation model to translate text between English and 22 Indian languages, with four register modes from formal to code-mixed. It enforces a 2,000-character input cap and requires a Sarvam API key from settings.

### `information retrieval` — 1 article(s)

- [Knowledge Search: BM25 Ranking with Substring Fallback](./knowledge-search-bm25-ranking-with-substring-fallback.md) — PocketPaw's knowledge search uses the `bm25s` library to rank wiki articles by relevance to a query, with a simple substring-match fallback when `bm25s` is not installed. The design deliberately avoids vector embeddings — LLM-compiled articles are semantically rich enough for BM25 to outperform naive chunked RAG.

### `information-retrieval` — 1 article(s)

- [WebSearchTool: Provider-Agnostic Web Search with Tavily, Brave, and Parallel AI](./websearchtool-provider-agnostic-web-search-with-tavily-brave-and-parallel-ai.md) — `WebSearchTool` routes web search queries to Tavily, Brave Search, or Parallel AI based on a settings-configured provider, with all three sharing a common `_format_results()` output shape. The provider selection is declarative — callers never specify the backend — keeping tool descriptions stable across deployment configurations.

### `infrastructure` — 7 article(s)

- [Cloudflare Tunnel Manager: Exposing Local PocketPaw Servers to the Public Internet](./cloudflare-tunnel-manager-exposing-local-pocketpaw-servers-to-the-public-interne.md) — The `TunnelManager` class manages a `cloudflared` subprocess to create a Cloudflare Tunnel that exposes a locally running PocketPaw server on a public HTTPS URL, enabling webhook delivery, mobile access, and external agent connectivity without firewall configuration. It supports auto-installation via Homebrew, URL extraction from stderr with a regex, graceful shutdown, and singleton lifecycle registration.
- [GCPAdapter: Google Cloud Platform Connector via gcloud CLI with Multi-Path Binary Discovery](./gcpadapter-google-cloud-platform-connector-via-gcloud-cli-with-multi-path-binary.md) — `GCPAdapter` implements `ConnectorProtocol` by wrapping the `gcloud` CLI, using `--format=json` for structured output and supporting project and region overrides via config. It scans several common install paths to locate the binary, making it resilient to non-standard SDK installations.
- [Health Checks Registry: Modular Check Catalogue with Tiered Execution Lists](./health-checks-registry-modular-check-catalogue-with-tiered-execution-lists.md) — The health checks `__init__.py` is both a re-export facade and a check registry, defining three typed lists — `STARTUP_CHECKS`, `INTEGRATION_CHECKS`, and `CONNECTIVITY_CHECKS` — that the `HealthEngine` iterates to run diagnostics at the right time and in the right order. All individual check functions are re-exported so that existing imports remain stable across refactors.
- [Health Engine Package: Singleton Factory for Self-Healing Diagnostics](./health-engine-package-singleton-factory-for-self-healing-diagnostics.md) — The health package `__init__.py` provides a lazy singleton factory `get_health_engine()` that creates and caches a `HealthEngine` instance on first access. The deferred import pattern means the `HealthEngine` class and all its check dependencies are only loaded when health checking is actually needed, keeping startup time minimal.
- [Remote Tunnel API Tests: Cloudflare Tunnel Status, Start, and Stop](./remote-tunnel-api-tests-cloudflare-tunnel-status-start-and-stop.md) — This test file covers PocketPaw's `/api/v1/remote` router, which manages a Cloudflare tunnel that exposes the local PocketPaw instance to the internet. It verifies status reporting, tunnel start with error handling for missing binaries, and graceful stop.
- [SystemInfoTool: Host Resource Monitoring with Optional psutil Integration](./systeminfotool-host-resource-monitoring-with-optional-psutil-integration.md) — `SystemInfoTool` reports CPU, RAM, disk, network I/O, and optionally top processes by delegating to `get_system_status()` for the base snapshot, then augmenting with `psutil` data when available. The psutil dependency is treated as optional — the tool degrades gracefully to the base snapshot if psutil is not installed.
- [Tools CLI Dispatcher: Bash-Accessible Tool Invocation for Agent Loops](./tools-cli-dispatcher-bash-accessible-tool-invocation-for-agent-loops.md) — The `pocketpaw.tools.cli` module is a CLI dispatcher that lets any builtin tool be invoked from a Bash shell via `python -m pocketpaw.tools.cli <tool_name> '<json_args>'`. It serves as the bridge between shell-based agent loops (like Claude Code) and PocketPaw's async Python tool ecosystem.

### `ingestion` — 1 article(s)

- [Knowledge Ingestion: Multi-Source Text Extraction to RawDocs](./knowledge-ingestion-multi-source-text-extraction-to-rawdocs.md) — The ingestion layer extracts clean text from plain text, URLs, PDFs, images (OCR), and DOCX files, producing `RawDoc` objects identified by a content hash. URL ingestion uses `trafilatura` as the primary extractor for its best-in-class boilerplate removal (F1=0.958), with a regex-based HTML strip as a fallback.

### `initialization` — 1 article(s)

- [MongoDB Connection and Beanie ODM Initialization](./mongodb-connection-and-beanie-odm-initialization.md) — This module manages the lifecycle of the MongoDB connection and initializes the Beanie ODM with all document models required by the cloud EE layer. It also bootstraps the memory backend after Beanie is ready to prevent race conditions on first database access.

### `installation` — 4 article(s)

- [Fleet Installer — Template Loading, Path Traversal Guard, and Journal Emission](./fleet-installer-template-loading-path-traversal-guard-and-journal-emission.md) — The Fleet installer loads YAML or JSON fleet templates, installs the bundled soul/pocket/connector/scope configuration, and emits correlated journal events for observability. A critical path-traversal security fix was applied in `fix/fleet-install-auth-guard`: string-path inputs from the REST router are now clamped to the bundled templates directory, preventing a workspace admin from coercing the server into reading arbitrary files.
- [Fleet Module Entry Point — One-Command Bundle Installation](./fleet-module-entry-point-one-command-bundle-installation.md) — The `ee/fleet/__init__.py` is the public entry point for PocketPaw's Fleet subsystem, re-exporting the models, installer functions, and report types so callers can manage installable bundles — FleetTemplates — with a single import. Fleet was introduced in Move 7 PR-B to let non-technical operators install a complete agent configuration (soul, pocket, connectors, scopes) in one command.
- [Fleet Pydantic Models — Manifest and Install Report Types](./fleet-pydantic-models-manifest-and-install-report-types.md) — This module defines the Pydantic models for the Fleet subsystem: the `FleetTemplate` manifest (what to install), `FleetConnector` (a single connector registration), `FleetInstallStep` (one step's outcome), and `FleetInstallReport` (the full install result with convenience methods). These models are thin data containers — no new runtime concepts, just structured representations of the install lifecycle.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.

### `installer` — 16 article(s)

- [Cross-Platform Auto-Start Manager for the Desktop Launcher](./cross-platform-auto-start-manager-for-the-desktop-launcher.md) — Implements start-on-login for the PocketPaw desktop launcher across macOS (launchd plist), Windows (registry Run key), and Linux (XDG .desktop file), handling both frozen PyInstaller executables and source-run scenarios through a unified `AutoStartManager` interface.
- [Desktop Launcher Bootstrap: Python Detection, venv Creation, and PocketPaw Installation](./desktop-launcher-bootstrap-python-detection-venv-creation-and-pocketpaw-installa.md) — The Bootstrap module is the entry point for the PocketPaw desktop launcher's first-run experience. It detects a suitable Python interpreter, creates an isolated virtual environment, and installs the PocketPaw package — handling Windows edge cases by downloading an embeddable Python build when none is found.
- [Desktop Launcher Build Pipeline: PyInstaller, Code Signing, DMG, and Inno Setup](./desktop-launcher-build-pipeline-pyinstaller-code-signing-dmg-and-inno-setup.md) — The build script packages the PocketPaw desktop launcher into a distributable native application for macOS and Windows. It chains icon generation, PyInstaller bundling, platform-specific code signing, DMG creation for macOS, and Inno Setup invocation for Windows into a single `build()` function.
- [Desktop Launcher Server Manager: Process Lifecycle, Health Checks, and PID File Management](./desktop-launcher-server-manager-process-lifecycle-health-checks-and-pid-file-man.md) — The `ServerManager` class controls the lifecycle of the PocketPaw FastAPI server as a subprocess launched from the desktop launcher. It handles start, stop, restart, health polling, stale PID file cleanup, and dynamic port allocation — all designed to survive launcher restarts and OS-level process deaths.
- [Desktop Launcher Splash Window: First-Run Progress UI with Threaded Installation](./desktop-launcher-splash-window-first-run-progress-ui-with-threaded-installation.md) — The `SplashWindow` class provides a lightweight tkinter progress window shown during first-run installation. It runs the bootstrap install function in a background thread so the UI remains responsive, and uses `after()` polling to safely update the progress bar from the main thread.
- [Desktop Launcher System Tray Icon: Controls, Auto-Start, Updates, and Notifications](./desktop-launcher-system-tray-icon-controls-auto-start-updates-and-notifications.md) — The `TrayIcon` class provides the persistent system tray presence for the PocketPaw desktop launcher using `pystray`. It exposes a context menu for all runtime operations — starting, stopping, restarting the server, opening the dashboard, managing auto-start, checking for updates, viewing logs, uninstalling, and quitting.
- [Desktop Launcher Uninstaller: Selective Component Removal with Interactive Confirmation](./desktop-launcher-uninstaller-selective-component-removal-with-interactive-confir.md) — The `Uninstaller` class manages the removal of PocketPaw's desktop installation components — venv, uv binary, embedded Python, logs, config, memory, and audit data — with per-component granularity. Each component can be individually included or excluded, and the interactive mode prompts the user before removing sensitive data like memory and audit logs.
- [Desktop Launcher Updater: PyPI Version Checks and In-Place venv Upgrades](./desktop-launcher-updater-pypi-version-checks-and-in-place-venv-upgrades.md) — The `Updater` class polls PyPI for newer PocketPaw releases and applies updates by running `uv pip install --upgrade` (or falling back to pip) inside the existing venv. Dev mode is detected via a marker file and switches the update path from PyPI to a git branch install.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.
- [Launcher Shared Constants and Helpers: Paths, Package Metadata, and Python Discovery](./launcher-shared-constants-and-helpers-paths-package-metadata-and-python-discover.md) — The `common.py` module is the single source of truth for paths, package names, and utility functions shared across all launcher components. Centralizing these definitions prevents the bootstrap, server manager, updater, and uninstaller from each hard-coding `~/.pocketpaw` and drifting out of sync.
- [Launcher Uninstaller: Component-Level Removal with Data Preservation](./launcher-uninstaller-component-level-removal-with-data-preservation.md) — The launcher uninstall test suite validates PocketPaw's structured uninstall logic, covering component discovery, selective removal of runtime artifacts, and preservation of user data by default. Tests verify that the `Uninstaller` class correctly reflects filesystem state and handles missing or partially-installed environments without raising exceptions.
- [Launcher Updater: PyPI Version Checking and In-Place Upgrade Flow](./launcher-updater-pypi-version-checking-and-in-place-upgrade-flow.md) — The updater test suite covers PocketPaw's self-update mechanism, validating version comparison logic, PyPI metadata fetching, installed version detection from the venv, and the full apply-upgrade path. It ensures the updater degrades gracefully when the network is unavailable or the package is not yet installed.
- [Platform Icon Generator: .ico and .icns from a Single PNG Source](./platform-icon-generator-ico-and-icns-from-a-single-png-source.md) — The `make_icons.py` script generates platform-specific application icon files for Windows (`.ico`) and macOS (`.icns`) from a single source `icon.png`. It uses the native macOS `iconutil` tool when available and falls back to a Pillow-based implementation for cross-platform compatibility.
- [PocketPaw Desktop Launcher Entry Point](./pocketpaw-desktop-launcher-entry-point.md) — The main entry point for the PocketPaw desktop launcher, handling first-run bootstrap (Python venv setup, pip install), subsequent-run startup (server launch, browser open, tray icon), and headless mode for server environments. Includes a PyInstaller package fixup that repairs relative imports when the frozen executable runs `__main__.py` as a top-level script.
- [PocketPaw Desktop Launcher Package Version Bootstrap](./pocketpaw-desktop-launcher-package-version-bootstrap.md) — Defines the `pocketpaw-launcher` package version using `importlib.metadata` with a fallback to the `POCKETPAW_VERSION` environment variable, supporting both installed-package and frozen-executable (PyInstaller) deployment scenarios. This thin init file establishes the package namespace for the launcher subsystem.
- [PocketPaw Interactive Installer](./pocketpaw-interactive-installer.md) — A self-contained, single-file installer that guides users through PocketPaw setup with optional rich prompts, falling back to plain text input when the terminal environment cannot support InquirerPy. Supports both interactive and headless (non-interactive) installation profiles, with a robust dependency bootstrap cascade that verifies each step before proceeding.

### `instinct` — 7 article(s)

- [Brew & Co. Business Scenario: End-to-End Fabric and Instinct Pipeline Walkthrough](./brew-co-business-scenario-end-to-end-fabric-and-instinct-pipeline-walkthrough.md) — A narrative-driven end-to-end test that simulates a real small business — a coffee shop on Monday morning — using PocketPaw's `FabricStore` for business objects and `InstinctStore` for the AI decision pipeline. It demonstrates the full operational flow: data modeling, connector sync simulation, threshold-triggered automation, human approval, execution, and audit export.
- [Connector-to-Fabric Pipeline: Stripe Integration, Threshold Rules, and Instinct Proposal Chain Tests](./connector-to-fabric-pipeline-stripe-integration-threshold-rules-and-instinct-pro.md) — End-to-end tests for the data ingestion pipeline that runs from a YAML-defined connector (Stripe) through HTTP execution, Fabric object creation, threshold evaluation, and Instinct action proposal — all without real network calls via patched `httpx`. Covers the full chain including error paths and source deduplication behavior.
- [Correction Soul Bridge: Observe Callbacks and Procedural Memory Promotion Tests](./correction-soul-bridge-observe-callbacks-and-procedural-memory-promotion-tests.md) — This test suite covers the `CorrectionSoulBridge` — the enterprise component that writes user corrections into the Soul Protocol's memory system and promotes recurring correction patterns into procedural memory. It also validates the `InstinctCorrectionsTool`, the agent-facing interface that exposes captured corrections as readable context.
- [Decision Trace Collector: Lifecycle, Event Aggregation, Deduplication, and Snapshot Persistence Tests](./decision-trace-collector-lifecycle-event-aggregation-deduplication-and-snapshot-.md) — This suite tests the `TraceCollector` context manager that records which Fabric objects, soul memories, KB articles, and tool calls an agent consulted while generating a decision. It also verifies the `FabricObjectSnapshot` persistence layer that captures point-in-time object state alongside each audit entry.
- [Decision Trace Wiring: Store-Level and Router-Level Integration Tests for Trace Persistence and Hydration](./decision-trace-wiring-store-level-and-router-level-integration-tests-for-trace-p.md) — Integration tests that verify `ReasoningTrace` objects are persisted into audit context when `propose()` is called, that `FabricObjectSnapshot` records are keyed to the correct audit row, and that the `/instinct/audit/{id}` hydration endpoint expands trace IDs into full records at the correct depth level.
- [Full Decision Loop: Fabric Threshold Detection to Instinct Audit Export Tests](./full-decision-loop-fabric-threshold-detection-to-instinct-audit-export-tests.md) — End-to-end tests for the complete decision loop from Fabric object creation through threshold rule evaluation to Instinct action proposal, approval, and JSON audit export — all using real store implementations with `tmp_path` SQLite databases. Three scenarios cover single trigger, multiple triggers, and audit trail identity.
- [Instinct Correction Loop: Patch Computation, Summarization, Store Persistence, and Approve Endpoint Tests](./instinct-correction-loop-patch-computation-summarization-store-persistence-and-a.md) — Covers the "Correction Loop" — the subsystem that detects when a user edits an AI-proposed action during approval, records what changed as structured patches, and surfaces those corrections for soul memory promotion. Tests span the pure diff functions, the `InstinctStore` correction persistence layer, and the `/approve` HTTP endpoint behavior.

### `instinct engine` — 8 article(s)

- [Instinct Correction Loop — Diff Capture and Soul Learning Signal](./instinct-correction-loop-diff-capture-and-soul-learning-signal.md) — Defines the data types and diff logic that record what a human changed when editing an agent's proposed action before approving it. These structured diffs are consumed by soul-protocol to bias future proposals toward the human's preferences.
- [Instinct Correction Soul Bridge — Wiring Human Edits to Soul Memory](./instinct-correction-soul-bridge-wiring-human-edits-to-soul-memory.md) — Acts as the connector between the Instinct correction system and soul-protocol's memory tiers, turning captured human edits into episodic observations and promoting repeated field-level corrections into procedural rules. It degrades silently when the soul is unavailable so the approval flow is never blocked.
- [Instinct Decision Pipeline — Core Data Models](./instinct-decision-pipeline-core-data-models.md) — Defines the Pydantic data models and enumerations that underpin the Instinct decision pipeline: the proposed action, its lifecycle states, audit entries, and the contextual metadata that connects an action to its triggering source. These types are the stable contract shared by the store, router, correction system, and any agent that proposes actions.
- [Instinct Decision Pipeline — HTTP Router](./instinct-decision-pipeline-http-router.md) — Provides the FastAPI HTTP surface for the Instinct decision pipeline, handling action proposals, approvals with optional human edits, rejections, and corrections listing. It enforces that soul bridge failures never block an approval and that edit diffs are captured and stored before the status transition.
- [Instinct Decision Pipeline — Package Entry Point](./instinct-decision-pipeline-package-entry-point.md) — The `ee.instinct` package is PocketPaw's human-in-the-loop decision engine: agents propose actions, humans approve or edit them, approved actions execute, and any edits are captured as corrections that feed back into the soul for future improvement. This module re-exports every public symbol from the five sub-modules so consumers import from a single namespace.
- [Instinct Reasoning Trace Types — Decision Audit Context](./instinct-reasoning-trace-types-decision-audit-context.md) — Defines the immutable data types that record the full reasoning context behind each proposed action: the queries made, soul memories consulted, KB articles injected, and tool calls executed. These types power the "Why?" audit drawer and enable humans to verify exactly what evidence the agent used.
- [Instinct Store — Async SQLite Persistence for the Decision Pipeline](./instinct-store-async-sqlite-persistence-for-the-decision-pipeline.md) — Provides all async SQLite read and write operations for the Instinct decision pipeline: action lifecycle management, audit logging, correction recording, reasoning traces, and Fabric object snapshots. It is the single source of truth for the decision state within a PocketPaw enterprise installation.
- [Instinct Trace Collector — Bus-Subscriber Context Manager for Reasoning Capture](./instinct-trace-collector-bus-subscriber-context-manager-for-reasoning-capture.md) — An async context manager that subscribes to the message bus for the duration of a single agent proposal, collecting fabric queries, soul recalls, KB injections, and tool calls into a structured `ReasoningTrace`. Each proposal gets its own isolated collector instance, preventing cross-contamination between concurrent proposals.

### `instinct system` — 2 article(s)

- [Instinct Tools: Action Proposal, Pending Queue, and Decision Audit](./instinct-tools-action-proposal-pending-queue-and-decision-audit.md) — The `instinct_tools.py` module provides three `BaseTool` subclasses — `InstinctProposeTool`, `InstinctPendingTool`, and `InstinctAuditTool` — that form the agent-facing surface of PocketPaw's human-in-the-loop decision pipeline. The agent proposes actions rather than executing them autonomously; humans approve or reject from a queue; all decisions are recorded in an audit log the agent can query.
- [InstinctCorrectionsTool: Agent-Facing Surface for Human Edit History](./instinctcorrectionstool-agent-facing-surface-for-human-edit-history.md) — The `InstinctCorrectionsTool` (tool name: `instinct_corrections`) lets an agent fetch the history of human corrections applied to previously proposed actions within a pocket before generating its next proposal. By loading past edits into context, the agent can match the user's preferred tone, thresholds, and style without requiring repeated instruction — effectively turning human corrections into implicit few-shot examples for future drafts.

### `integration` — 4 article(s)

- [Cloud Mount Integration Smoke Tests: Route and Error Handler Registration](./cloud-mount-integration-smoke-tests-route-and-error-handler-registration.md) — This smoke test suite verifies that `mount_cloud()` successfully registers all expected domain route groups (auth, workspaces, agents, chat, pockets, sessions, WebSocket, license) and the `CloudError` exception handler on a fresh FastAPI app. It acts as a fast regression guard ensuring no route group is accidentally omitted from the mount function.
- [Files Router Bootstrap Integration Test](./files-router-bootstrap-integration-test.md) — This module contains a single integration test that verifies the files router can be mounted into a FastAPI application via build_files_router and that the /files/tree endpoint responds correctly with a 200 status and a JSON body containing the expected tree structure.
- [Test Suite for A2A Protocol Live Smoke Tests](./test-suite-for-a2a-protocol-live-smoke-tests.md) — This script is a live, end-to-end smoke test suite for PocketPaw's A2A (Agent-to-Agent) protocol endpoints. It requires a running PocketPaw server with `POCKETPAW_A2A_ENABLED=true` and exercises every major A2A feature — agent card discovery, message sending, task management, streaming, cancellation, and error handling — reporting PASS/FAIL for each check.
- [Test Suite for Knowledge Ingestion: CFO Agent Creation and Document Upload Smoke Test](./test-suite-for-knowledge-ingestion-cfo-agent-creation-and-document-upload-smoke-.md) — A manual smoke test script that creates a CFO-persona agent via the REST API and ingests three fixture documents into its knowledge base. It lives in `scripts/` rather than `tests/` to keep it out of pytest collection and CI, and requires a running backend with a valid bearer token to execute.

### `integration tests` — 1 article(s)

- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.

### `integration-testing` — 1 article(s)

- [Deep Work Session: End-to-End Orchestration and Lifecycle Tests](./deep-work-session-end-to-end-orchestration-and-lifecycle-tests.md) — This integration test suite covers the DeepWorkSession, the top-level orchestrator that coordinates planning, task materialization, agent assignment, approval gating, pause/resume, and cancellation for multi-task AI projects. It verifies the full session lifecycle from DRAFT through PLANNING to AWAITING_APPROVAL and beyond, including edge cases like cyclic dependencies, empty plans, and planner exceptions.

### `integrations` — 24 article(s)

- [Agent Knowledge Service — kb-go Binary Wrapper](./agent-knowledge-service-kb-go-binary-wrapper.md) — `knowledge.py` is a Python service that gives each agent a private knowledge base by delegating to the `kb` Go binary for compilation, indexing, and search, while handling heavyweight extraction (PDF, DOCX, URL, images) in Python before piping the plain text to `kb` via stdin.
- [Channel Adapter Schemas — Status, Configuration, and Toggle](./channel-adapter-schemas-status-configuration-and-toggle.md) — The channel schemas define the state and control contracts for PocketPaw's messaging platform adapters (Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, Google Chat). They separate read (status queries) from write (save config, start/stop) operations.
- [Connector Protocol: YAML Parsing, Registry, and DirectREST Adapter Lifecycle Tests](./connector-protocol-yaml-parsing-registry-and-directrest-adapter-lifecycle-tests.md) — This module tests the PocketPaw connector infrastructure — YAML definition parsing, the `ConnectorRegistry` for connector discovery and lifecycle management, and the `DirectRESTAdapter` for connecting, executing actions, and disconnecting from external REST APIs like Stripe.
- [Generic Inbound Webhook Adapter with Sync and Async Modes](./generic-inbound-webhook-adapter-with-sync-and-async-modes.md) — WebhookAdapter allows any external service — GitHub, Zapier, n8n, Home Assistant, cron scripts — to push events into PocketPaw via HTTP POST. It supports both fire-and-forget (async) mode and a synchronous request-response mode where the HTTP call blocks until the agent produces a reply.
- [Gmail Client — OAuth-Backed HTTP Client for Gmail API](./gmail-client-oauth-backed-http-client-for-gmail-api.md) — `GmailClient` provides async methods for searching messages, reading full message content, sending emails, and listing labels via the Gmail API, using OAuth bearer tokens from `OAuthManager`. It handles base64url-encoded message bodies and MIME multipart structures for plain text extraction.
- [Gmail Tools: Search, Read, Send, Label, and Batch Modify Emails](./gmail-tools-search-read-send-label-and-batch-modify-emails.md) — The `gmail.py` module provides eight `BaseTool` subclasses covering the full Gmail management surface: `GmailSearchTool`, `GmailReadTool`, `GmailSendTool`, `GmailListLabelsTool`, `GmailCreateLabelTool`, `GmailModifyTool`, `GmailTrashTool`, and `GmailBatchModifyTool`. All tools carry `trust_level = "high"` because email access is sensitive and requires explicit OAuth authorization; the batch modify tool exists specifically to reduce API round-trips when applying label changes to many messages.
- [Google Calendar Client — OAuth-Backed HTTP Client for Calendar API v3](./google-calendar-client-oauth-backed-http-client-for-calendar-api-v3.md) — `CalendarClient` provides async methods for listing upcoming events and creating new calendar events via the Google Calendar API v3, using OAuth bearer tokens managed by `OAuthManager` with automatic token refresh. It is part of PocketPaw's Phase 2 Integration Ecosystem.
- [Google Docs Client — OAuth-Backed HTTP Client for Docs API v1](./google-docs-client-oauth-backed-http-client-for-docs-api-v1.md) — `DocsClient` provides async methods for reading Google Docs as plain text and creating new documents with initial content, using OAuth bearer tokens from `OAuthManager`. It includes a recursive text extraction helper that walks the Docs API's nested paragraph structure.
- [Google Docs Tools: Read, Create, and Search Documents](./google-docs-tools-read-create-and-search-documents.md) — The `gdocs.py` module provides three `BaseTool` subclasses — `DocsReadTool`, `DocsCreateTool`, and `DocsSearchTool` — that allow the PocketPaw agent to read, create, and search Google Docs documents via the Google Docs API. A helper function `_parse_doc_id` normalizes both raw document IDs and full Google Docs URLs so the agent does not need to extract the ID manually.
- [Google Drive Client — OAuth-Backed HTTP Client for Drive API v3](./google-drive-client-oauth-backed-http-client-for-drive-api-v3.md) — `DriveClient` provides async methods for listing, searching, downloading, uploading, and trashing files in Google Drive via the Drive API v3, using OAuth bearer tokens from `OAuthManager`. Downloaded files are saved to `~/.pocketpaw/downloads/` with automatic directory creation.
- [Google Drive Tools: List, Download, Upload, and Share Files](./google-drive-tools-list-download-upload-and-share-files.md) — The `gdrive.py` module exposes four `BaseTool` subclasses — `DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, and `DriveShareTool` — giving the PocketPaw agent full CRUD and sharing access to Google Drive. A `_GDRIVE_ROLES` frozenset enforces valid sharing permissions at the tool layer before the API call is made, preventing invalid-role errors from propagating to the user.
- [Integration Health Checks — Version Updates and Google Workspace CLI Detection](./integration-health-checks-version-updates-and-google-workspace-cli-detection.md) — This module provides two synchronous health check functions: one that queries PyPI to detect whether a newer PocketPaw version is available, and one that verifies whether the `gws` Google Workspace CLI binary is installed in PATH. Both return structured `HealthCheckResult` objects consumed by the health engine.
- [Integrations Package Initializer — OAuth and Google Workspace Integration Namespace](./integrations-package-initializer-oauth-and-google-workspace-integration-namespac.md) — The `integrations` package `__init__.py` is a minimal namespace marker created in February 2026 to anchor PocketPaw's OAuth and Google Workspace integration modules (Gmail, Calendar, Drive, Docs). It contains no exports or logic, serving purely as a package boundary.
- [OAuth Integration Router — Google and Spotify Service Authorization](./oauth-integration-router-google-and-spotify-service-authorization.md) — The OAuth integrations router handles the user-facing authorize and callback flow for connecting PocketPaw to third-party services like Gmail, Google Calendar, Google Drive, Google Docs, and Spotify. It was extracted from the dashboard router so the same flows work in both dashboard and headless serve modes.
- [OAuth Integration: Token Storage, Provider Configuration, and Token Validity](./oauth-integration-token-storage-provider-configuration-and-token-validity.md) — Tests for PocketPaw's OAuth subsystem, covering secure token persistence in TokenStore, provider URL generation via OAuthManager, and token validity checks based on expiry timestamps. Verifies file permissions, non-existent token handling, and graceful degradation for unknown providers.
- [OAuth Manager — Google OAuth 2.0 Authorization Code Flow and Token Lifecycle](./oauth-manager-google-oauth-20-authorization-code-flow-and-token-lifecycle.md) — `OAuthManager` implements the full OAuth 2.0 authorization code flow for Google (and optionally Spotify), handling authorization URL generation, code-for-token exchange, access token refresh, and token revocation, backed by `TokenStore` for persistent credential storage. It provides a `get_valid_token()` convenience method that transparently refreshes expired tokens.
- [OAuth Token Store: Secure File-Based Credential Persistence](./oauth-token-store-secure-file-based-credential-persistence.md) — The `TokenStore` persists OAuth 2.0 tokens to `~/.pocketpaw/oauth/{service}.json` with strict `0600` file permissions to prevent other OS users from reading credentials. It provides load, save, delete, and list operations over the `OAuthTokens` dataclass that covers the full OAuth 2.0 token surface.
- [OCRTool: Multi-Engine Text Extraction from Images and PDFs](./ocrtool-multi-engine-text-extraction-from-images-and-pdfs.md) — The `OCRTool` extracts text from images and PDFs using three interchangeable OCR backends — OpenAI Vision (GPT-4o), Sarvam Vision (specialized for 23 Indian languages), and Tesseract (offline fallback). All path operations go through `is_safe_path` to enforce the file jail, and outputs are saved to a dedicated OCR output directory so extracted text is recoverable after the tool call completes.
- [Reddit Integration: Read-Only Client via Public JSON API](./reddit-integration-read-only-client-via-public-json-api.md) — PocketPaw's Reddit client provides read-only access to Reddit posts and subreddits using Reddit's public JSON API, requiring no API key or authentication. It enforces a one-request-per-second rate limit to comply with Reddit's unauthenticated access policy.
- [Reddit Tool Tests: Search, Read, Trending, and Post Formatting](./reddit-tool-tests-search-read-trending-and-post-formatting.md) — PocketPaw's Reddit integration provides agents with three tools for interacting with Reddit: `reddit_search`, `reddit_read`, and `reddit_trending`. These tests validate tool schemas and trust levels, the `_format_post` helper for normalizing Reddit API responses, and async tool execution covering success, empty results, and error conditions.
- [Sarvam AI Integration Tests — Translation, TTS, STT, and OCR](./sarvam-ai-integration-tests-translation-tts-stt-and-ocr.md) — This comprehensive test file covers PocketPaw's integration with Sarvam AI's multilingual APIs: text translation, text-to-speech (TTS), speech-to-text (STT), and vision OCR. Tests validate API key gating, SDK error propagation, audio encoding, file-based I/O, provider routing, policy group membership, config field defaults, and tool registration.
- [Signal Channel Adapter Tests — Init, Lifecycle, Message Handling, and Bus Integration](./signal-channel-adapter-tests-init-lifecycle-message-handling-and-bus-integration.md) — This Sprint 20 test file validates `SignalAdapter`, PocketPaw's channel adapter for Signal messenger. Tests cover adapter configuration defaults, start/stop lifecycle, inbound message parsing with phone number authorization, outbound message sending with streaming support, error recovery, and MessageBus integration.
- [Spotify Integration: OAuth-Authenticated Web API Client](./spotify-integration-oauth-authenticated-web-api-client.md) — The Spotify client provides PocketPaw agents with authenticated access to the Spotify Web API for playback control, track search, and playlist management. It delegates token lifecycle management to the shared `OAuthManager` and `TokenStore` infrastructure, raising a clear error if the user has not completed the OAuth flow.
- [Tools Registry REST Endpoint](./tools-registry-rest-endpoint.md) — Provides a single GET endpoint that aggregates builtin tools, MCP tools, and tool groups into a unified discovery response. Lazy imports and broad exception handling ensure the endpoint returns partial results gracefully when the MCP manager or OAuth token store is not yet initialized.

### `intentions` — 1 article(s)

- [Intention Scheduling Schemas](./intention-scheduling-schemas.md) — Defines the Pydantic models for PocketPaw's intention system — scheduled, trigger-driven agent tasks that run prompts automatically. The schema covers the full CRUD lifecycle plus manual trigger acknowledgement, with field-level validation to prevent empty or oversized intentions.

### `interfaces` — 1 article(s)

- [VectorStoreProtocol: Runtime-Checkable Vector DB Interface](./vectorstoreprotocol-runtime-checkable-vector-db-interface.md) — VectorStoreProtocol defines the minimal async interface every vector store adapter must satisfy—`add`, `search`, `delete`, and `get_by_id`—using Python's `@runtime_checkable Protocol` so that implementations can be verified via `isinstance` without requiring inheritance. This creates a clean dependency boundary between the agent loop and any specific vector database backend.

### `interoperability` — 2 article(s)

- [A2A Protocol Server — Agent-to-Agent HTTP Endpoint for PocketPaw](./a2a-protocol-server-agent-to-agent-http-endpoint-for-pocketpaw.md) — Implements the Agent-to-Agent (A2A) protocol server that exposes PocketPaw as an interoperable AI agent over HTTP. It provides a JSON-RPC 2.0 dispatcher, an Agent Card capability manifest, task submission endpoints, SSE streaming, and an in-memory task store with TTL-based expiry.
- [Agent-to-Agent (A2A) Protocol Module: Public API and Roadmap](./agent-to-agent-a2a-protocol-module-public-api-and-roadmap.md) — The `pocketpaw.a2a` package implements Google's Agent-to-Agent (A2A) protocol, allowing PocketPaw to act as both an A2A server (accepting tasks from external agents) and an A2A client (delegating tasks to external agents). This `__init__.py` re-exports the full public surface and documents the three-phase implementation roadmap.

### `invite flow` — 2 article(s)

- [Workspace FastAPI Router — CRUD, Members, and Invites](./workspace-fastapi-router-crud-members-and-invites.md) — This module defines all HTTP endpoints for the workspace domain — workspace lifecycle, member management, and invite flows — using FastAPI's dependency injection to enforce authorization at the route layer while keeping service methods auth-agnostic. Authorization gates are declared inline as `Depends(require_action(...))` so the permission model is readable directly from the route signature.
- [Workspace Service — Business Logic for Workspaces, Members, and Invites](./workspace-service-business-logic-for-workspaces-members-and-invites.md) — The `WorkspaceService` class is a stateless async service that encapsulates all business logic for the workspace domain, including workspace lifecycle, member management, invite flow, and realtime event emission. It enforces several data-level invariants (slug uniqueness, seat limits, owner demotion guard) that cannot be expressed as route-level permission checks.

### `journal` — 6 article(s)

- [Fabric Event Payload Types and Action Constants](./fabric-event-payload-types-and-action-constants.md) — This module pins the three canonical event shapes that drive Fabric's journal projection — object created, updated, and archived — as stable action name constants and payload builder functions. It was created specifically to replace PR #938's scope-filtering approach in the legacy SQLite store, which had two unfixable structural bugs: a schema migration gap and a pagination leak.
- [Fabric Journal Store Tests: Scope Isolation, Pagination, Projection Rebuild, and Policy Correctness](./fabric-journal-store-tests-scope-isolation-pagination-projection-rebuild-and-pol.md) — This suite pins four invariants for PocketPaw's journal-backed Fabric object store: correct lifecycle (create/update/archive), scope-based access control that returns empty results rather than leaking existence, post-filter pagination totals, and deterministic projection rebuild from the event journal. It supersedes a previous implementation that leaked object counts across scope boundaries.
- [FabricJournalStore — Event-Sourced Object Lifecycle for Fabric](./fabricjournalstore-event-sourced-object-lifecycle-for-fabric.md) — `FabricJournalStore` is the Wave 3 replacement for the scope-filtering slice of PR #938, providing event-sourced create, update, archive, and query operations over Fabric objects backed by the org journal and served through an in-memory projection. Every write appends an `EventEntry` to the journal and immediately folds it into the live projection, so reads after a write are always consistent without a rebuild.
- [Fleet Installer Journal Emission Tests: Correlated Events, Backward Compatibility, and Partial Install](./fleet-installer-journal-emission-tests-correlated-events-backward-compatibility-.md) — This suite verifies that the fleet installer emits a correlated sequence of journal events (`fleet.install.started`, `agent.spawned` per soul, `fleet.installed` summary) when given a journal, remains silent when the journal is omitted, suppresses the terminal event on soul-creation failure, and falls back to a fleet-scoped tag when the fleet declares no scopes.
- [Fleet Installer — Template Loading, Path Traversal Guard, and Journal Emission](./fleet-installer-template-loading-path-traversal-guard-and-journal-emission.md) — The Fleet installer loads YAML or JSON fleet templates, installs the bundled soul/pocket/connector/scope configuration, and emits correlated journal events for observability. A critical path-traversal security fix was applied in `fix/fleet-install-auth-guard`: string-path inputs from the REST router are now clamped to the bundled templates directory, preventing a workspace admin from coercing the server into reading arbitrary files.
- [get_journal FastAPI Dependency: Caching, SOUL_DATA_DIR Override, and Cache Reset Tests](./getjournal-fastapi-dependency-caching-souldatadir-override-and-cache-reset-tests.md) — Tests the `get_journal` FastAPI dependency from `ee.journal_dep`, pinning three contracts that the rest of the `ee/` subsystem depends on: the dep returns a live Journal instance, repeated calls within one process return the same cached instance, and the `SOUL_DATA_DIR` environment variable is honoured at call time — not frozen at import. Also verifies `reset_journal_cache()` as the test-isolation escape hatch.

### `journal-pattern` — 2 article(s)

- [Journal-Backed Write Store for Retrieval and Graduation Events](./journal-backed-write-store-for-retrieval-and-graduation-events.md) — Provides the write path for the retrieval domain by appending `retrieval.query` and `graduation.applied` events to the org journal and immediately folding them into the shared in-memory projection. Replaces the JSONL-plus-asyncio-lock pattern from PR #936 with SQLite WAL semantics that are multi-process safe by design.
- [Journal-Backed Write Store for Widget Events](./journal-backed-write-store-for-widget-events.md) — Provides the write facade for the widget domain, emitting all five widget action types onto the org journal and folding each emission into the shared in-memory projection for immediate read consistency. Adds a seq-returning variant of the interaction writer to support the POST /widgets/track endpoint's cursor ack requirement.

### `key generation` — 1 article(s)

- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.

### `key management` — 2 article(s)

- [API Key Manager: Secure Key Lifecycle with Hash-Only Storage](./api-key-manager-secure-key-lifecycle-with-hash-only-storage.md) — `APIKeyManager` provides create, verify, revoke, rotate, and list operations for API keys using a GitHub PAT-style design: keys are shown once at creation and only their SHA-256 hash is persisted. Keys use the `pp_` prefix for log identification, support fine-grained scopes, and are stored in `~/.pocketpaw/api_keys.json`.
- [Storage Key Generation: Namespaced, Date-Bucketed, UUID-Based Keys for Upload Blobs](./storage-key-generation-namespaced-date-bucketed-uuid-based-keys-for-upload-blobs.md) — The `keys.py` module generates opaque storage keys with a predictable three-part structure -- kind, year-month bucket, and a UUID4 hex suffix -- that supports human-readable organization, time-based lifecycle policies, and guaranteed uniqueness. The companion `sanitize_ext()` function normalizes file extensions to prevent path injection through malformed extension strings.

### `kits` — 5 article(s)

- [PawKit Catalog: Registry of Installable Dashboard Presets](./pawkit-catalog-registry-of-installable-dashboard-presets.md) — The PawKit Catalog is a curated in-memory registry of pre-configured PawKit types that users can browse and install from the Pocket Store. Each entry describes a dashboard preset with ID, display name, category, Lucide icon, and preview text — without bundling the full YAML configuration.
- [PawKit Pydantic Models: Dashboard Configuration Schema](./pawkit-pydantic-models-dashboard-configuration-schema.md) — The `kits.models` module defines the full Pydantic schema hierarchy for PawKit configurations — from individual metric cards up to the root `PawKitConfig` that a YAML file is parsed into. It also defines `InstalledKit`, which tracks activation status and user-provided configuration values at runtime.
- [PawKit Store: File-Based Kit Persistence with Path Traversal Protection](./pawkit-store-file-based-kit-persistence-with-path-traversal-protection.md) — The `FileKitStore` manages installed PawKits on disk under `~/.pocketpaw/kits/`, using atomic writes (write-then-rename) and an in-memory index loaded lazily on first access. A March 2026 security hardening pass added input sanitization to prevent path traversal attacks via malicious kit IDs or data source names.
- [PawKits Builtins Package: Placeholder for Shipped Kit Definitions](./pawkits-builtins-package-placeholder-for-shipped-kit-definitions.md) — The `pocketpaw.kits.builtins` package is a reserved namespace for built-in PawKit definitions that ship with PocketPaw. Currently it contains only a module docstring, indicating the built-in kit definitions are loaded from YAML files via the catalog rather than from Python modules.
- [PawKits Package: Dashboard System Public API](./pawkits-package-dashboard-system-public-api.md) — The `pocketpaw.kits` package exposes the complete public surface for PawKits — PocketPaw's configurable command-center dashboard system. It re-exports models, catalog entries, and store operations as a single flat namespace so consumers never need to import from internal submodules.

### `knowledge` — 8 article(s)

- [Knowledge Compiler: LLM-Powered Raw Text to Structured Wiki Articles](./knowledge-compiler-llm-powered-raw-text-to-structured-wiki-articles.md) — The knowledge compiler is the intelligence layer of PocketPaw's knowledge engine — it sends raw documents to an LLM with a strict prompt that elicits a structured JSON article with title, summary, content, concepts, and categories. Robust JSON parsing handles markdown fences and partial LLM output that would otherwise cause silent data loss.
- [Knowledge Engine Data Models: RawDoc, WikiArticle, Concept, LintIssue, KnowledgeIndex](./knowledge-engine-data-models-rawdoc-wikiarticle-concept-lintissue-knowledgeindex.md) — The `knowledge.models` module defines the five core dataclasses of PocketPaw's knowledge engine — `RawDoc` (ingested source), `WikiArticle` (compiled artifact), `Concept` (cross-article entity), `LintIssue` (audit finding), and `KnowledgeIndex` (master index). Each includes a `to_dict()` method for JSON serialization to disk.
- [Knowledge Engine: Unified Ingest-Compile-Index-Search Pipeline](./knowledge-engine-unified-ingest-compile-index-search-pipeline.md) — The `KnowledgeEngine` class is the single entry point for all knowledge management operations — ingesting text, URLs, and files, compiling them into structured wiki articles via LLM, indexing concepts and backlinks, and searching with BM25. It is scope-aware, allowing separate knowledge bases per agent, workspace, or pocket.
- [Knowledge Indexer: Concept Graph and Backlink Builder](./knowledge-indexer-concept-graph-and-backlink-builder.md) — The knowledge indexer maintains a `KnowledgeIndex` that maps concepts to the articles mentioning them and builds bidirectional backlinks between articles that reference each other's titles. It supports both full rebuilds from scratch and incremental updates when a single article is added or removed.
- [Knowledge Ingestion: Multi-Source Text Extraction to RawDocs](./knowledge-ingestion-multi-source-text-extraction-to-rawdocs.md) — The ingestion layer extracts clean text from plain text, URLs, PDFs, images (OCR), and DOCX files, producing `RawDoc` objects identified by a content hash. URL ingestion uses `trafilatura` as the primary extractor for its best-in-class boilerplate removal (F1=0.958), with a regex-based HTML strip as a fallback.
- [Knowledge Linter: LLM-Powered Wiki Health Auditor](./knowledge-linter-llm-powered-wiki-health-auditor.md) — The knowledge linter uses an LLM to audit the wiki for inconsistencies, coverage gaps, missing cross-links, stale content, and uncompiled raw documents. It produces structured `LintIssue` objects with severity levels and actionable suggestions, functioning as an automated knowledge quality reviewer.
- [Knowledge Search: BM25 Ranking with Substring Fallback](./knowledge-search-bm25-ranking-with-substring-fallback.md) — PocketPaw's knowledge search uses the `bm25s` library to rank wiki articles by relevance to a query, with a simple substring-match fallback when `bm25s` is not installed. The design deliberately avoids vector embeddings — LLM-compiled articles are semantically rich enough for BM25 to outperform naive chunked RAG.
- [Test Suite for Knowledge Ingestion: CFO Agent Creation and Document Upload Smoke Test](./test-suite-for-knowledge-ingestion-cfo-agent-creation-and-document-upload-smoke-.md) — A manual smoke test script that creates a CFO-persona agent via the REST API and ingests three fixture documents into its knowledge base. It lives in `scripts/` rather than `tests/` to keep it out of pytest collection and CI, and requires a running backend with a valid bearer token to execute.

### `knowledge base` — 2 article(s)

- [Context Window Budget Tests: Priority-Based Block Assembly and KB Integration](./context-window-budget-tests-priority-based-block-assembly-and-kb-integration.md) — This test module, added in April 2026, validates `AgentContextBuilder._assemble_with_budget`, which assembles the agent's system prompt from priority-ranked blocks while respecting both a global character budget and per-block caps. It also tests the knowledge-base context injection path that calls the `kb-go` CLI subprocess to fetch relevant articles.
- [KbProvider Tests: Workspace Scoping, RBAC, and Mount Template](./kbprovider-tests-workspace-scoping-rbac-and-mount-template.md) — This module tests the KbProvider file provider, which surfaces the knowledge base as a virtual file tree. Tests verify that entries are scoped to the correct workspace, that baseline RBAC rules grant read access to workspace members and manage access to admins, and that the mount path template correctly resolves the workspace ID.

### `knowledge graph` — 1 article(s)

- [Memory System: FileMemoryStore, MemoryManager, and Knowledge Graph Extraction](./memory-system-filememorystore-memorymanager-and-knowledge-graph-extraction.md) — The core memory test suite validates PocketPaw's `FileMemoryStore` and `MemoryManager` — covering long-term and session memory CRUD, the agent context assembly flow, and an extensive suite for the knowledge graph extraction system that builds entity-relationship graphs from conversation text using conservative regex patterns.

### `knowledge management` — 4 article(s)

- [Agent Knowledge Service — kb-go Binary Wrapper](./agent-knowledge-service-kb-go-binary-wrapper.md) — `knowledge.py` is a Python service that gives each agent a private knowledge base by delegating to the `kb` Go binary for compilation, indexing, and search, while handling heavyweight extraction (PDF, DOCX, URL, images) in Python before piping the plain text to `kb` via stdin.
- [Knowledge Router: Multi-Scope Listing, Filtering, and Cross-Workspace Isolation Tests](./knowledge-router-multi-scope-listing-filtering-and-cross-workspace-isolation-tes.md) — This integration test suite exercises the `/api/v1/knowledge/articles` FastAPI endpoint end-to-end, verifying that it merges workspace and agent scopes, supports `agent_id` filtering, rejects cross-workspace queries with HTTP 403, and returns empty results (not a data leak) for unknown agent IDs.
- [WikiStore: File-Based CRUD for Knowledge Articles](./wikistore-file-based-crud-for-knowledge-articles.md) — WikiStore is the persistence layer for PocketPaw's knowledge subsystem, managing file-based CRUD for raw documents and compiled wiki articles organized by scope. It maintains a predictable three-directory layout per scope and handles index serialization to ensure fast listing without scanning the filesystem on every read.
- [Workspace KB Aggregator: Merge, Filter, Dedup, Sort, and Resilience Tests](./workspace-kb-aggregator-merge-filter-dedup-sort-and-resilience-tests.md) — This test suite validates the `ee.cloud.kb.workspace_aggregator` module, which merges knowledge base articles from workspace and agent scopes into a single sorted, deduplicated list. It tests merge logic, agent filtering, deduplication by (scope, id), newest-first ordering, resilience when a scope returns malformed data, and the `AggregatedArticle.to_dict()` shape.

### `knowledge-base` — 7 article(s)

- [KbProvider: Knowledge Base File Provider](./kbprovider-knowledge-base-file-provider.md) — Exposes workspace Knowledge Base documents as a browseable file mount in the unified files tree. `KbProvider` delegates reads to a `_KbService` Protocol adapter rather than directly calling the KB FastAPI routes, decoupling the files subsystem from the KB module's current route-and-Go-binary architecture.
- [Knowledge Base Domain Package Initialiser](./knowledge-base-domain-package-initialiser.md) — The package initialiser for the `ee.cloud.kb` Knowledge Base domain, which exposes workspace-scoped KB endpoints including search, ingest, browse, lint, and stats. The comment header documents the package's scope and public surface, serving as the entry point for the KB subsystem.
- [Knowledge Base Request Schemas](./knowledge-base-request-schemas.md) — Pydantic request models for the knowledge base REST API, covering search, text ingestion, URL ingestion, and lint operations. These schemas enforce input validation at the FastAPI boundary before any request reaches the kb Go binary.
- [Knowledge Base REST API Router (Per-Workspace Scope)](./knowledge-base-rest-api-router-per-workspace-scope.md) — The primary FastAPI router for workspace-scoped knowledge base operations — search, ingest, lint, and article browsing — all delegating to the kb Go binary. This router replaced a pure-Python knowledge_base package and defines the REST surface consumed by the wiki pocket template.
- [PocketPaw Compiler Backend Adapter for Knowledge Base](./pocketpaw-compiler-backend-adapter-for-knowledge-base.md) — Implements `PocketPawCompilerBackend`, a `CompilerBackend` Protocol adapter that makes PocketPaw's active agent LLM backend available to the `knowledge_base` package's compiler pipeline. This enables KB article compilation to use whatever AI backend is configured in the agent registry -- Claude, OpenAI, or any other -- without hardcoding a specific model.
- [Workspace Knowledge Aggregator — Cross-Agent KB Merge](./workspace-knowledge-aggregator-cross-agent-kb-merge.md) — A pure-Python aggregation layer that merges the workspace-scoped KB with every per-agent KB inside a workspace into a single sorted, deduplicated article stream. Designed to be thin, injectable, and unit-testable without a live database or kb binary.
- [Workspace-Level Knowledge Browser Router](./workspace-level-knowledge-browser-router.md) — A FastAPI router mounted at `/api/v1/knowledge/*` that fans out KB article listings across every agent in a workspace. It exists as a separate router from `/api/v1/kb/*` because it has different aggregation semantics — workspace-wide rollup rather than single-scope access.

### `langchain` — 1 article(s)

- [Deep Agents Backend — LangChain/LangGraph Agent Framework Integration](./deep-agents-backend-langchainlanggraph-agent-framework-integration.md) — Implements `DeepAgentsBackend`, which runs agents via the `deepagents` SDK built on LangChain and LangGraph. It provides durable execution, built-in planning and filesystem tools, multi-provider LLM routing, MCP tool integration, and streaming via LangGraph's event stream.

### `langgraph` — 1 article(s)

- [Deep Agents Backend — LangChain/LangGraph Agent Framework Integration](./deep-agents-backend-langchainlanggraph-agent-framework-integration.md) — Implements `DeepAgentsBackend`, which runs agents via the `deepagents` SDK built on LangChain and LangGraph. It provides durable execution, built-in planning and filesystem tools, multi-provider LLM routing, MCP tool integration, and streaming via LangGraph's event stream.

### `launcher` — 13 article(s)

- [Cross-Platform Auto-Start Manager for the Desktop Launcher](./cross-platform-auto-start-manager-for-the-desktop-launcher.md) — Implements start-on-login for the PocketPaw desktop launcher across macOS (launchd plist), Windows (registry Run key), and Linux (XDG .desktop file), handling both frozen PyInstaller executables and source-run scenarios through a unified `AutoStartManager` interface.
- [Desktop Launcher Bootstrap: Python Detection, venv Creation, and PocketPaw Installation](./desktop-launcher-bootstrap-python-detection-venv-creation-and-pocketpaw-installa.md) — The Bootstrap module is the entry point for the PocketPaw desktop launcher's first-run experience. It detects a suitable Python interpreter, creates an isolated virtual environment, and installs the PocketPaw package — handling Windows edge cases by downloading an embeddable Python build when none is found.
- [Desktop Launcher Server Manager: Process Lifecycle, Health Checks, and PID File Management](./desktop-launcher-server-manager-process-lifecycle-health-checks-and-pid-file-man.md) — The `ServerManager` class controls the lifecycle of the PocketPaw FastAPI server as a subprocess launched from the desktop launcher. It handles start, stop, restart, health polling, stale PID file cleanup, and dynamic port allocation — all designed to survive launcher restarts and OS-level process deaths.
- [Desktop Launcher Splash Window: First-Run Progress UI with Threaded Installation](./desktop-launcher-splash-window-first-run-progress-ui-with-threaded-installation.md) — The `SplashWindow` class provides a lightweight tkinter progress window shown during first-run installation. It runs the bootstrap install function in a background thread so the UI remains responsive, and uses `after()` polling to safely update the progress bar from the main thread.
- [Desktop Launcher System Tray Icon: Controls, Auto-Start, Updates, and Notifications](./desktop-launcher-system-tray-icon-controls-auto-start-updates-and-notifications.md) — The `TrayIcon` class provides the persistent system tray presence for the PocketPaw desktop launcher using `pystray`. It exposes a context menu for all runtime operations — starting, stopping, restarting the server, opening the dashboard, managing auto-start, checking for updates, viewing logs, uninstalling, and quitting.
- [Desktop Launcher Uninstaller: Selective Component Removal with Interactive Confirmation](./desktop-launcher-uninstaller-selective-component-removal-with-interactive-confir.md) — The `Uninstaller` class manages the removal of PocketPaw's desktop installation components — venv, uv binary, embedded Python, logs, config, memory, and audit data — with per-component granularity. Each component can be individually included or excluded, and the interactive mode prompts the user before removing sensitive data like memory and audit logs.
- [Desktop Launcher Updater: PyPI Version Checks and In-Place venv Upgrades](./desktop-launcher-updater-pypi-version-checks-and-in-place-venv-upgrades.md) — The `Updater` class polls PyPI for newer PocketPaw releases and applies updates by running `uv pip install --upgrade` (or falling back to pip) inside the existing venv. Dev mode is detected via a marker file and switches the update path from PyPI to a git branch install.
- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.
- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.
- [Launcher Shared Constants and Helpers: Paths, Package Metadata, and Python Discovery](./launcher-shared-constants-and-helpers-paths-package-metadata-and-python-discover.md) — The `common.py` module is the single source of truth for paths, package names, and utility functions shared across all launcher components. Centralizing these definitions prevents the bootstrap, server manager, updater, and uninstaller from each hard-coding `~/.pocketpaw` and drifting out of sync.
- [PocketPaw Desktop Launcher Entry Point](./pocketpaw-desktop-launcher-entry-point.md) — The main entry point for the PocketPaw desktop launcher, handling first-run bootstrap (Python venv setup, pip install), subsequent-run startup (server launch, browser open, tray icon), and headless mode for server environments. Includes a PyInstaller package fixup that repairs relative imports when the frozen executable runs `__main__.py` as a top-level script.
- [PocketPaw Desktop Launcher Package Version Bootstrap](./pocketpaw-desktop-launcher-package-version-bootstrap.md) — Defines the `pocketpaw-launcher` package version using `importlib.metadata` with a fallback to the `POCKETPAW_VERSION` environment variable, supporting both installed-package and frozen-executable (PyInstaller) deployment scenarios. This thin init file establishes the package namespace for the launcher subsystem.

### `layout` — 1 article(s)

- [Pocket Layout Export and User-Defined Template Registry](./pocket-layout-export-and-user-defined-template-registry.md) — This module provides two distinct but related capabilities: a pure YAML serialisation of a pocket's ripple spec for export or sharing, and an in-process workspace-scoped store for user-defined pocket templates. Both capabilities back new REST endpoints added in Cluster B Sub-PR #3 to close a gap in the UI testing guide around widget layout save and share.

### `licensing` — 3 article(s)

- [Enterprise Edition Package — Gated Feature Namespace](./enterprise-edition-package-gated-feature-namespace.md) — The `pocketpaw.ee` package is the root namespace for PocketPaw's Enterprise Edition, housing features gated behind team, business, and enterprise plans under the Functional Source License (FSL 1.1). It organizes five EE subsystems: guards (RBAC/ABAC), automations (trigger-action workflows), fabric (workspace mesh), instinct (proactive behaviors), and audit (compliance trail).
- [Enterprise Extensions (ee/) Package Overview](./enterprise-extensions-ee-package-overview.md) — The `ee/` package is the boundary layer separating PocketPaw's open-source core from its commercially-licensed Enterprise features. It groups capabilities like the instinct decision pipeline, audit/compliance logging, automations engine, and singleton API accessors under a single FSL 1.1 license gate.
- [Enterprise License Validation and FastAPI Dependency](./enterprise-license-validation-and-fastapi-dependency.md) — A self-contained license system that validates Ed25519-signed license keys for enterprise cloud features. License state is cached process-wide and gates API endpoints via FastAPI `Depends` integration, with a fallback HMAC-SHA256 path for self-hosted deployments that cannot configure asymmetric keys.

### `lifecycle` — 2 article(s)

- [Agent Pool — On-Demand Agent Instance Management with Idle Eviction](./agent-pool-on-demand-agent-instance-management-with-idle-eviction.md) — Implements `AgentPool`, a lifecycle manager for cloud-deployed agent instances. Each agent gets its own `AgentBackend`, `SoulManager`, and isolated memory namespace. Instances are cached for reuse and evicted after configurable idle timeout (default 5 minutes) to bound memory usage.
- [Desktop Launcher Uninstaller: Selective Component Removal with Interactive Confirmation](./desktop-launcher-uninstaller-selective-component-removal-with-interactive-confir.md) — The `Uninstaller` class manages the removal of PocketPaw's desktop installation components — venv, uv binary, embedded Python, logs, config, memory, and audit data — with per-component granularity. Each component can be individually included or excluded, and the interactive mode prompts the user before removing sensitive data like memory and audit logs.

### `lifecycle management` — 2 article(s)

- [Launcher Uninstaller: Component-Level Removal with Data Preservation](./launcher-uninstaller-component-level-removal-with-data-preservation.md) — The launcher uninstall test suite validates PocketPaw's structured uninstall logic, covering component discovery, selective removal of runtime artifacts, and preservation of user data by default. Tests verify that the `Uninstaller` class correctly reflects filesystem state and handles missing or partially-installed environments without raising exceptions.
- [Launcher Updater: PyPI Version Checking and In-Place Upgrade Flow](./launcher-updater-pypi-version-checking-and-in-place-upgrade-flow.md) — The updater test suite covers PocketPaw's self-update mechanism, validating version comparison logic, PyPI metadata fetching, installed version detection from the venv, and the full apply-upgrade path. It ensures the updater degrades gracefully when the network is unavailable or the package is not yet installed.

### `lifecycle-management` — 1 article(s)

- [Deep Work Session — Full Project Lifecycle Orchestrator](./deep-work-session-full-project-lifecycle-orchestrator.md) — DeepWorkSession is the top-level coordinator for a Deep Work project, tying together GoalParser, PlannerAgent, DependencyScheduler, MCTaskExecutor, and HumanTaskRouter into a single class that manages a project from user input through goal analysis, LLM planning, approval, execution, pause, resume, and cancellation. It also handles crash recovery for interrupted projects on startup.

### `llm-integration` — 5 article(s)

- [Deep Work Goal Parser — Structured Goal Analysis via LLM](./deep-work-goal-parser-structured-goal-analysis-via-llm.md) — GoalParser is the first primitive in the Deep Work pipeline, taking raw user input and producing a structured GoalAnalysis that captures domain, complexity, AI/human role split, constraints, and clarification questions. It routes the LLM prompt through AgentRouter, strips code fences from the response, and defensively validates every field before constructing the dataclass.
- [Deep Work Planner Agent — Four-Phase LLM Project Planning](./deep-work-planner-agent-four-phase-llm-project-planning.md) — PlannerAgent orchestrates the four-phase Deep Work planning pipeline — research, PRD generation, task breakdown, and team assembly — through sequential LLM calls via AgentRouter. It produces a PlannerResult containing the PRD markdown, a list of TaskSpec objects with dependency keys, and recommended AgentSpec team members.
- [Deep Work Prompt Templates — LLM Instruction Set for Planning Pipeline](./deep-work-prompt-templates-llm-instruction-set-for-planning-pipeline.md) — This module centralizes all LLM prompt templates used by the Deep Work planning pipeline as module-level string constants. Separating prompts from logic enables iteration on prompt text without touching orchestration code, and makes the planning pipeline's LLM behavior fully visible in one file.
- [PocketPawCognitiveEngine: Bridging Soul Protocol Cognition to Agent Backends](./pocketpawcognitiveengine-bridging-soul-protocol-cognition-to-agent-backends.md) — `PocketPawCognitiveEngine` implements the Soul Protocol's `CognitiveEngine` interface, routing fact-extraction and reflection calls to PocketPaw's active agent backend or to a cheaper dedicated Anthropic model when configured. It enables the soul's cognitive layer to reuse the existing LLM infrastructure rather than requiring a separate AI provider setup.
- [ResearchTool: Multi-Step Web Research Pipeline with LLM Summarization](./researchtool-multi-step-web-research-pipeline-with-llm-summarization.md) — `ResearchTool` orchestrates a four-stage pipeline — web search, URL discovery, content extraction, and LLM summarization — into a single `research` tool call. Depth levels let callers trade speed against thoroughness, and an optional memory-save step persists findings across conversations.

### `loader` — 1 article(s)

- [SkillLoader and SKILL.md Parsing Tests](./skillloader-and-skillmd-parsing-tests.md) — This test file validates `SkillLoader` and `parse_skill_md` from `pocketpaw.skills.loader`, covering SKILL.md frontmatter parsing, optional field handling, no-frontmatter rejection, directory-name fallback for skill naming, prompt argument substitution, loader lifecycle, skill retrieval, reload, multi-path override resolution, and integration with real skill paths.

### `local filesystem` — 1 article(s)

- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.

### `local inference` — 1 article(s)

- [OllamaAdapter: Local LLM Integration via OpenAI-Compatible API](./ollamaadapter-local-llm-integration-via-openai-compatible-api.md) — `OllamaAdapter` connects PocketPaw to locally running Ollama instances, using `"ollama"` as a placeholder API key to satisfy client SDK validation while pointing both OpenAI and Anthropic client builders at Ollama's OpenAI-compatible HTTP endpoint. Error messages distinguish between model-not-found and service-not-running failures to give users actionable guidance.

### `local-models` — 1 article(s)

- [OpenAI Agents SDK Backend — GPT and Ollama Agent Support with SQLite Session Persistence](./openai-agents-sdk-backend-gpt-and-ollama-agent-support-with-sqlite-session-persi.md) — Implements `OpenAIAgentsBackend` using OpenAI's `openai-agents` SDK, supporting GPT models and any OpenAI-compatible endpoint (Ollama, OpenRouter, LiteLLM). Sessions are persisted to a SQLite database so multi-turn conversations survive process restarts, unlike most other backends.

### `logging` — 2 article(s)

- [Logging Setup: Rich Console Output with Secret and PII Scrubbing](./logging-setup-rich-console-output-with-secret-and-pii-scrubbing.md) — `logging_setup.py` configures PocketPaw's logging infrastructure with Rich-formatted console output and two security filters: `SecretFilter` (scrubs API key patterns from all log messages) and `PIILogFilter` (opt-in scrubbing of personally identifiable information). These filters prevent credential and user data leakage through log output regardless of log level.
- [Security Scrub: PII and Secret Masking in Logs and Tool Parameters](./security-scrub-pii-and-secret-masking-in-logs-and-tool-parameters.md) — The logging scrub test suite validates PocketPaw's defense-in-depth approach to preventing secrets and personally identifiable information from appearing in logs, audit trails, and tool execution records. Tests cover pattern-based field masking, nested dict traversal, audit log fallback behavior, and dangerous-command detection logging introduced in security sprint cluster C.

### `manifest` — 1 article(s)

- [Fleet Pydantic Models — Manifest and Install Report Types](./fleet-pydantic-models-manifest-and-install-report-types.md) — This module defines the Pydantic models for the Fleet subsystem: the `FleetTemplate` manifest (what to install), `FleetConnector` (a single connector registration), `FleetInstallStep` (one step's outcome), and `FleetInstallReport` (the full install result with convenience methods). These models are thin data containers — no new runtime concepts, just structured representations of the install lifecycle.

### `markdown` — 1 article(s)

- [Channel-Aware Markdown Formatting and LLM Prompt Hints](./channel-aware-markdown-formatting-and-llm-prompt-hints.md) — format.py provides two capabilities: a `convert_markdown()` function that translates standard Markdown output from the LLM into each channel's native formatting syntax, and a `CHANNEL_FORMAT_HINTS` dictionary that injects per-channel formatting instructions into the LLM system prompt so the model generates natively formatted text in the first place.

### `marketplace` — 1 article(s)

- [Skills Management REST Router](./skills-management-rest-router.md) — Provides REST endpoints for listing installed skills, searching the skills.sh marketplace, installing skills from GitHub sources, removing skills, and force-reloading the skill index. Skills are user-invocable slash commands that extend PocketPaw's capabilities without core code changes.

### `matrix` — 1 article(s)

- [Matrix Channel Adapter — Long-Polling with Streaming via Message Edits](./matrix-channel-adapter-long-polling-with-streaming-via-message-edits.md) — MatrixAdapter connects PocketPaw to the Matrix protocol using matrix-nio's AsyncClient, supporting both access token and password authentication. Streaming agent responses are delivered by editing the initial message in place using Matrix's m.replace relation, rate-limited to one edit per 1.5 seconds to avoid homeserver rate limiting.

### `mcp` — 3 article(s)

- [Google ADK Backend — Gemini Agent Integration via Google Agent Development Kit](./google-adk-backend-gemini-agent-integration-via-google-agent-development-kit.md) — Implements `GoogleADKBackend`, which runs Gemini-powered agents using the official `google-adk` Python SDK. It supports built-in tools (Google Search, code execution), MCP toolset integration via both stdio and SSE transports, custom PocketPaw function tools, and streaming via the ADK's `InMemoryRunner`.
- [MCP Server Management Schemas](./mcp-server-management-schemas.md) — Defines the Pydantic request models for managing Model Context Protocol (MCP) servers in PocketPaw — adding servers by configuration, referencing them by name, test-connecting before saving, and installing from a curated preset library. These schemas expose the full MCP lifecycle through the REST API.
- [Tools Registry REST Endpoint](./tools-registry-rest-endpoint.md) — Provides a single GET endpoint that aggregates builtin tools, MCP tools, and tool groups into a unified discovery response. Lazy imports and broad exception handling ensure the endpoint returns partial results gracefully when the MCP manager or OAuth token store is not yet initialized.

### `media` — 3 article(s)

- [Media Download Utility for Channel Adapters](./media-download-utility-for-channel-adapters.md) — media.py provides a shared `MediaDownloader` singleton and related utilities that all channel adapters use to save incoming media attachments to local disk. It handles collision-free filename generation, file size enforcement, authenticated downloads, and MIME type guessing.
- [Reddit Integration: Read-Only Client via Public JSON API](./reddit-integration-read-only-client-via-public-json-api.md) — PocketPaw's Reddit client provides read-only access to Reddit posts and subreddits using Reddit's public JSON API, requiring no API key or authentication. It enforces a one-request-per-second rate limit to comply with Reddit's unauthenticated access policy.
- [Spotify Integration: OAuth-Authenticated Web API Client](./spotify-integration-oauth-authenticated-web-api-client.md) — The Spotify client provides PocketPaw agents with authenticated access to the Spotify Web API for playback control, track search, and playlist management. It delegates token lifecycle management to the shared `OAuthManager` and `TokenStore` infrastructure, raising a clear error if the user has not completed the OAuth flow.

### `media generation` — 1 article(s)

- [ImageGenerateTool: Text-to-Image Generation via Google Gemini](./imagegeneratetool-text-to-image-generation-via-google-gemini.md) — The `ImageGenerateTool` gives the PocketPaw agent the ability to generate images from text prompts using the Google Gemini image generation API (internally called "Nano Banana"). Generated images are saved to a deterministic directory under the PocketPaw config folder with UUID-based filenames, and the tool returns the local file path so the agent can chain the result with `OpenExplorerTool` or other file-handling tools.

### `media handling` — 4 article(s)

- [Agent Loop Media Helpers: _format_bytes Unit Tests](./agent-loop-media-helpers-formatbytes-unit-tests.md) — Focused unit tests for the `_format_bytes` helper in `pocketpaw.agents.loop`, verifying that file sizes are formatted correctly across the B, KB, MB, and GB ranges. This utility generates human-readable size strings for media attachment prompts injected into the agent's context.
- [Media Attachments: Cross-Channel File Delivery, Path Security, and Tool Result Tagging](./media-attachments-cross-channel-file-delivery-path-security-and-tool-result-tagg.md) — The media attachments test suite validates PocketPaw's end-to-end media pipeline — from tool result tagging via HTML-comment markers, through agent loop extraction, to per-channel delivery (Telegram, Discord, Slack, WhatsApp, WebSocket) and the secure `/api/media` serving endpoint. Path traversal protection for the media endpoint receives dedicated security coverage.
- [MediaDownloader: Filename Sanitization, Unique Naming, HTTP Download, and Size Limiting](./mediadownloader-filename-sanitization-unique-naming-http-download-and-size-limit.md) — The `MediaDownloader` test suite validates PocketPaw's utility for safely downloading and storing media files from URLs, covering filename sanitization (path traversal prevention), collision-free unique filename generation, configurable size limits, HTTP error handling, and authenticated downloads. The singleton pattern ensures a single downloader instance manages all media across the application.
- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.

### `media processing` — 1 article(s)

- [OCRTool: Multi-Engine Text Extraction from Images and PDFs](./ocrtool-multi-engine-text-extraction-from-images-and-pdfs.md) — The `OCRTool` extracts text from images and PDFs using three interchangeable OCR backends — OpenAI Vision (GPT-4o), Sarvam Vision (specialized for 23 Indian languages), and Tesseract (offline fallback). All path operations go through `is_safe_path` to enforce the file jail, and outputs are saved to a dedicated OCR output directory so extracted text is recoverable after the tool call completes.

### `media resolution` — 2 article(s)

- [EE Upload Resolver Tests: Workspace-Scoped Path Resolution, Isolation, and Soft-Delete Visibility](./ee-upload-resolver-tests-workspace-scoped-path-resolution-isolation-and-soft-del.md) — Tests for `EEUploadResolver` and `resolve_media_paths_scoped`, which translate `/api/v1/uploads/{id}` URLs in message content into local filesystem paths for downstream processing. Key invariants: workspace isolation (cross-workspace lookup returns `None`), non-upload URL pass-through, soft-delete invisibility, and missing-blob safety.
- [OSS-to-EE Resolver Fallback Tests: Bridging JSONL and Mongo Upload Stores](./oss-to-ee-resolver-fallback-tests-bridging-jsonl-and-mongo-upload-stores.md) — Tests for the `resolve_media_paths_any` OSS→EE fallback, which handles the hybrid scenario where uploads are stored in the EE Mongo store but chat rendering goes through the OSS JSONL lookup path. Without the fallback, EE-uploaded files would appear as broken links in OSS-rendered chat messages.

### `media-integrations` — 5 article(s)

- [Reddit Tools: Search, Read Posts, and Surface Trending Content](./reddit-tools-search-read-posts-and-surface-trending-content.md) — PocketPaw ships three Reddit tools — `reddit_search`, `reddit_read`, and `reddit_trending` — that give agents access to Reddit content without requiring an API key. They delegate to a shared `RedditClient` integration and return richly formatted markdown text suitable for direct LLM consumption.
- [SpeechToTextTool: Multi-Provider Audio Transcription with Path Jail Enforcement](./speechtotexttool-multi-provider-audio-transcription-with-path-jail-enforcement.md) — `SpeechToTextTool` transcribes audio files to text via OpenAI Whisper, ElevenLabs, or Sarvam AI Saaras, with automatic provider selection from settings. A path-safety check (`is_safe_path`) prevents directory traversal attacks before any file is read, and transcripts are written to a versioned output directory.
- [Spotify Tools: Search, Now Playing, Playback Control, and Playlist Management](./spotify-tools-search-now-playing-playback-control-and-playlist-management.md) — Four `BaseTool` subclasses — `spotify_search`, `spotify_now_playing`, `spotify_playback`, and `spotify_playlist` — wrap `SpotifyClient` to give agents full Spotify control: catalog search, current track status, transport controls, and playlist operations. All raise `RuntimeError` for auth failures, which surfaces as a clean error rather than a stack trace.
- [TextToSpeechTool: Multi-Provider TTS with Auto-Loop Integration and Media Tag Protocol](./texttospeechtool-multi-provider-tts-with-auto-loop-integration-and-media-tag-pro.md) — `TextToSpeechTool` converts text to audio via OpenAI TTS, ElevenLabs, or Sarvam AI Bulbul, saving output to `~/.pocketpaw/generated/audio/` with UUID filenames. A standalone `synthesize_speech()` helper and a `<!-- media:path -->` HTML comment protocol enable the agent loop to auto-play TTS responses without parsing tool output.
- [TranslateTool: English–Indian Language Translation via Sarvam AI Mayura](./translatetool-englishindian-language-translation-via-sarvam-ai-mayura.md) — `TranslateTool` integrates Sarvam AI's Mayura translation model to translate text between English and 22 Indian languages, with four register modes from formal to code-mixed. It enforces a 2,000-character input cap and requires a Sarvam API key from settings.

### `memory` — 28 article(s)

- [Agent Context Builder — System Prompt Assembly with Budget Tracking](./agent-context-builder-system-prompt-assembly-with-budget-tracking.md) — AgentContextBuilder is the central orchestrator that assembles the full system prompt for each agent turn, weaving together identity, instructions, memory recalls, knowledge-base results, channel hints, health state, and injected files while staying within a configurable character budget. Its priority-based injection model ensures critical blocks (identity, instructions) are never dropped, while optional blocks are trimmed or skipped as budget tightens.
- [AgentLoop Concurrency Controls: Session Locks, Global Semaphore, and File Memory Safety](./agentloop-concurrency-controls-session-locks-global-semaphore-and-file-memory-sa.md) — This test module validates the three-layer concurrency model in PocketPaw's AgentLoop: per-session serialization via asyncio locks, cross-session parallelism as the happy path, and a global semaphore that caps total simultaneous conversations. It also covers the FileMemoryStore's write lock and transient PermissionError retry logic that prevent JSON corruption under concurrent writes.
- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.
- [Cloud Memory Backend Bootstrap — MongoDB Default Registration](./cloud-memory-backend-bootstrap-mongodb-default-registration.md) — Bootstraps the MongoDB memory backend for cloud deployments by directly priming the `pocketpaw.memory.manager` singleton before Beanie is initialized, bypassing the file-based config that would otherwise retain a stale `memory_backend: "file"` setting. Respects the `POCKETPAW_MEMORY_BACKEND` environment variable as an explicit user override.
- [Cloud Memory Package — MongoMemoryStore Public API](./cloud-memory-package-mongomemorystore-public-api.md) — The `ee.cloud.memory` package's public interface, which re-exports `MongoMemoryStore` as the single entry point for MongoDB-backed agent memory in cloud deployments. The module is intentionally minimal, acting as a stable import boundary between the cloud layer and the rest of the application.
- [CreateSkillTool: Runtime Skill Generation with SKILL.md and Memory Persistence](./createskilltool-runtime-skill-generation-with-skillmd-and-memory-persistence.md) — `CreateSkillTool` lets agents create new reusable skills at runtime by writing SKILL.md files with YAML frontmatter to `~/.claude/skills/`. After creation, it reloads the skill loader and persists the skill's description to long-term memory so the agent remembers it across sessions.
- [Daily Notes Isolation Tests: Per-User Scoping and Backward-Compatible Legacy Note Visibility](./daily-notes-isolation-tests-per-user-scoping-and-backward-compatible-legacy-note.md) — This test file, added during security sprint cluster D (issue #887), validates that daily notes in the `MemoryManager` are scoped to the `sender_id` that created them, preventing one user's private notes from leaking into another user's agent context. It also tests the backward-compatibility guarantee that legacy notes written before the fix remain visible as system-wide entries.
- [Long-Term Memory API — CRUD, Settings, and Stats](./long-term-memory-api-crud-settings-and-stats.md) — The memory router exposes endpoints for managing PocketPaw's long-term memory store, configuring which backend is active (file vs. mem0), and retrieving usage statistics. It bridges the REST surface to the pluggable MemoryManager, ensuring settings changes take effect immediately by flushing cached singletons.
- [Memory API Tests: Settings, Long-Term Memory CRUD, and Backend Stats](./memory-api-tests-settings-long-term-memory-crud-and-backend-stats.md) — This test file covers PocketPaw's `/api/v1/memory` router, which exposes configuration for the memory backend (file store vs. mem0), CRUD operations over long-term memory entries, and per-backend statistics.
- [Memory Entry and Settings Schemas](./memory-entry-and-settings-schemas.md) — Defines the Pydantic models for PocketPaw's memory system — a single memory entry with tagging support, and a configuration response that exposes the pluggable memory backend settings including optional mem0 AI-inference configuration. The settings model reflects the system's multi-backend memory architecture.
- [Memory Tools: Remember, Recall, and Forget Long-Term Agent Memory](./memory-tools-remember-recall-and-forget-long-term-agent-memory.md) — The `memory.py` module provides three `BaseTool` subclasses — `RememberTool`, `RecallTool`, and `ForgetTool` — that expose PocketPaw's long-term memory manager to the agent, enabling it to save facts about the user, retrieve them in future sessions, and delete outdated or incorrect memories. These tools are the agent-facing surface of the memory manager, which persists memories across session boundaries independently of the LLM's context window.
- [MemoryFactDoc — Beanie Document for Agent Long-Term and Daily Memory](./memoryfactdoc-beanie-document-for-agent-long-term-and-daily-memory.md) — The `MemoryFactDoc` Beanie ODM document stores agent long-term and daily memory facts in the `memory_facts` MongoDB collection. It is distinct from the `messages` collection and carries workspace and user ownership fields to support multi-tenant scoping.
- [MongoMemoryStore — Full MemoryStoreProtocol Implementation on MongoDB](./mongomemorystore-full-memorystoreprotocol-implementation-on-mongodb.md) — The production MongoDB implementation of PocketPaw's `MemoryStoreProtocol`, bridging the OSS memory interface to the cloud's Beanie ODM schema. SESSION entries map to the `messages` collection; LONG_TERM and DAILY facts map to `memory_facts`. The store also manages session metadata lifecycle and normalizes session key formats between the message bus and the database.
- [Role Roundtrip Diagnostic: Verifying User and Assistant Message Persistence](./role-roundtrip-diagnostic-verifying-user-and-assistant-message-persistence.md) — A targeted diagnostic script that writes four messages with alternating `user` and `assistant` roles to MongoDB via the memory manager, then reads them back to verify that roles are persisted and retrieved correctly. It was created to debug a specific class of bug where the agent loop was saving messages but role information was being lost or corrupted during the roundtrip.
- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.
- [Session Index, REST Endpoints, WebSocket Switching, and Search Tests](./session-index-rest-endpoints-websocket-switching-and-search-tests.md) — This test file comprehensively covers `FileMemoryStore`'s session index subsystem: atomic index writes, index rebuild from disk, CRUD operations, WebSocket session switching, path traversal blocking, full-text session search, and the REST API endpoints that expose these operations to the dashboard.
- [Session Management API Tests — List, Delete, Title Update, and Search](./session-management-api-tests-list-delete-title-update-and-search.md) — This test module covers all four operations of the `/api/v1/sessions` router: listing sessions with pagination, deleting by ID, updating titles, and full-text searching session history. It uses mock-based isolation of the underlying memory store to prevent filesystem side effects while exercising the router's error-handling and edge-case paths.
- [Session Management REST Router](./session-management-rest-router.md) — Provides the REST endpoints for creating, listing, deleting, renaming, searching, and exporting conversation sessions. Session IDs are UUID-based slugs prefixed with `websocket_` to maintain compatibility with the WebSocket session key format used by the memory subsystem.
- [Session Management Tool Tests — New, List, Switch, Clear, Rename, and Delete](./session-management-tool-tests-new-list-switch-clear-rename-and-delete.md) — This test file validates the six session management tools in `pocketpaw.tools.builtin.sessions`: `NewSessionTool`, `ListSessionsTool`, `SwitchSessionTool`, `ClearSessionTool`, `RenameSessionTool`, and `DeleteSessionTool`. Tests cover tool naming, happy-path behavior, error handling, and policy group membership.
- [Session Management Tools: LLM-Driven Conversation Branching](./session-management-tools-llm-driven-conversation-branching.md) — Six `BaseTool` subclasses expose session lifecycle operations — create, list, switch, clear, rename, delete — as agent-callable tools, replacing an earlier regex-based NL detection approach. The LLM decides when to invoke them based on conversational context, making session control robust to any phrasing.
- [Smoke Test: MemoryManager Singleton Stays Live After Backend Swap](./smoke-test-memorymanager-singleton-stays-live-after-backend-swap.md) — This test reproduces a real bug where `AgentLoop` captured `get_memory_manager()` at construction time — before cloud database initialization — and then held a stale reference to the file-based store even after the MongoDB backend was registered. It verifies that the singleton's internal store is swapped in-place so pre-captured references automatically see the new backend.
- [Smoke Test: MongoDB Memory Backend End-to-End](./smoke-test-mongodb-memory-backend-end-to-end.md) — This script exercises the full MongoDB memory path from `init_cloud_db` through `MemoryManager.add_to_session`, `get_session_history`, raw Mongo inspection, and long-term memory storage in `memory_facts`. It serves as the canonical integration check for the enterprise memory backend.
- [Smoke Test: Session Creation Then Chat Persistence Round-Trip](./smoke-test-session-creation-then-chat-persistence-round-trip.md) — This test validates the core "desired flow": create a session via the API, send a user message, confirm it is stored in MongoDB keyed by the session ID, and retrieve it through the history endpoint. It was written to capture the exact behavior contract for session-scoped message storage.
- [Smoke Test: Session Key Normalization Between Agent Bus and UI Chat History](./smoke-test-session-key-normalization-between-agent-bus-and-ui-chat-history.md) — This test verifies that the colon-delimited session key written by the agent loop (`websocket:X`) is normalized to the underscore form used by the UI (`websocket_X`), so that chat history returned by `GET /api/v1/sessions/{sessionId}/history` actually contains the messages the agent wrote. Without this normalization, the session history API returns empty results despite messages being stored.
- [Soul-Protocol Integration Tests: End-to-End PocketPaw Wiring](./soul-protocol-integration-tests-end-to-end-pocketpaw-wiring.md) — Integration tests that validate the full lifecycle of the soul-protocol package within PocketPaw, including bootstrap prompt generation, memory observe-and-recall, tool bridge discovery of soul tools, and corrupt file recovery. All tests are conditionally skipped when `soul-protocol` is not installed.
- [Soul Protocol REST API Endpoints](./soul-protocol-rest-api-endpoints.md) — Exposes the full Soul Protocol interface over REST, covering identity, OCEAN personality state, core memory editing, memory storage and recall, soul import/export, and rubric-based self-evaluation. All endpoints require the `settings:read` scope and safely handle the case where no soul is loaded.
- [Unified Agent Loop — Core Orchestrator for Message Processing and Agent Execution](./unified-agent-loop-core-orchestrator-for-message-processing-and-agent-execution.md) — Implements `AgentLoop`, the central orchestrator that consumes messages from the bus, routes them through `AgentRouter` to the configured backend, applies security scanning and PII redaction, stores memories, and streams responses back to channels. It also manages the soul's cognitive pipeline and per-session concurrency locks.
- [WebSocket Persistence Diagnostic: Simulating the Chat Message Save Path with Full Debug Logging](./websocket-persistence-diagnostic-simulating-the-chat-message-save-path-with-full.md) — This script simulates the exact code path the WebSocket adapter executes when saving a chat message, but with DEBUG-level logging and no exception swallowing. It was created to surface silent persistence failures that were invisible in normal operation because the WebSocket handler caught and discarded exceptions.

### `memory architecture` — 1 article(s)

- [Soul Memories API Tests: Tier Filtering, Limit Clamping, and Entry Serialization](./soul-memories-api-tests-tier-filtering-limit-clamping-and-entry-serialization.md) — Unit tests for the `GET /api/v1/soul/memories` endpoint, focusing on the `_collect_tier_entries` helper that queries individual memory tiers, limit clamping to safe bounds, tier filter validation, and graceful handling of absent souls or missing tier stores. Tests operate at the function level to avoid spinning up a full SSE client.

### `memory graduation` — 1 article(s)

- [Retrieval Journal Projection — Package Entry Point and Design History](./retrieval-journal-projection-package-entry-point-and-design-history.md) — The `ee.retrieval` package implements an observable retrieval trail and access-count graduation policy as a projection over the org journal, superseding two earlier JSONL-based designs. This module re-exports every public symbol and documents the architectural decision to consolidate onto the journal rather than maintain a parallel JSONL sink.

### `memory management` — 1 article(s)

- [Retrieval Journal Projection Tests: Write Path, Scope Filtering, Graduation Policy, and REST Router](./retrieval-journal-projection-tests-write-path-scope-filtering-graduation-policy-.md) — Comprehensive tests for the `ee.retrieval` subsystem that journals every memory-retrieval query and drives a graduation policy promoting frequently-retrieved memories to higher tiers. Covers event write-path fidelity, scope containment, correlation grouping, threshold-based graduation decisions, projection rebuild equivalence, and the REST router's GET endpoints.

### `memory models` — 1 article(s)

- [Session and Message context_type Discriminator Validator Tests](./session-and-message-contexttype-discriminator-validator-tests.md) — These tests verify that Pydantic model validators on Message and Session enforce strict discriminator rules, ensuring group and pocket context types carry exactly the required fields and nothing more. They also confirm backward-compatible inference when context_type is omitted.

### `memory store` — 5 article(s)

- [MongoMemoryStore Facts Path: LONG_TERM and DAILY Memory Roundtrip Tests](./mongomemorystore-facts-path-longterm-and-daily-memory-roundtrip-tests.md) — These tests exercise the complete facts path of MongoMemoryStore, covering CRUD, type isolation between LONG_TERM and DAILY entries, cross-type search, regex safety, and protocol conformance. They also verify that user_id is stored as a dedicated column and never duplicated in the metadata dictionary.
- [MongoMemoryStore Multi-Tenant Workspace Isolation Tests](./mongomemorystore-multi-tenant-workspace-isolation-tests.md) — These tests verify that MongoMemoryStore stamps workspace_id onto both session messages and fact documents at write time, and that read operations are scoped to the requested workspace to prevent cross-tenant data leakage. Untagged rows with workspace_id=None must never appear in workspace-scoped queries.
- [MongoMemoryStore Session Index Shape and Filtering Contract Tests](./mongomemorystore-session-index-shape-and-filtering-contract-tests.md) — These tests verify `_load_session_index_async` in MongoMemoryStore, which powers the `GET /sessions/runtime` endpoint. They pin the exact dict shape, filtering of group and soft-deleted sessions, channel inference from session key prefix, and empty title coercion.
- [MongoMemoryStore SESSION Path: Protocol Conformance and Adapter-Specific Read Tests](./mongomemorystore-session-path-protocol-conformance-and-adapter-specific-read-tes.md) — These tests cover the full SESSION memory path in MongoMemoryStore — CRUD, chronological message retrieval, session key isolation, combined session-with-messages reads with limit behavior, and cross-collection isolation between pocket and group messages.
- [MongoMemoryStore Session Upkeep: messageCount and lastActivity Auto-Maintenance Tests](./mongomemorystore-session-upkeep-messagecount-and-lastactivity-auto-maintenance-t.md) — These tests verify that MongoMemoryStore automatically updates Session metadata (messageCount, lastActivity) on every save and auto-creates missing Session rows when a message arrives before the session is explicitly created. They pin behaviors introduced after a historical refactor that removed a broken parallel bus subscriber.

### `memory system` — 6 article(s)

- [FileMemoryStore Fixes: UUID Collision, Fuzzy Search, Deduplication, Persistent Delete, and Graph Tests](./filememorystore-fixes-uuid-collision-fuzzy-search-deduplication-persistent-delet.md) — This comprehensive test file covers a series of incremental fixes to `FileMemoryStore`, PocketPaw's markdown-based memory backend. It addresses UUID collisions, broken substring search, single-day file loading, lost deletions across restarts, and missing context limits — plus later phases covering vector semantic search, graph indexing, SVG escaping, and SQLite variable limit handling.
- [Mem0 Memory Store: Backend Selection, Config Building, and Pluggable Provider Integration](./mem0-memory-store-backend-selection-config-building-and-pluggable-provider-integ.md) — The Mem0 store test suite validates PocketPaw's integration with the `mem0ai` package as an optional semantic memory backend, covering the factory function's backend selection and fallback logic, `_build_mem0_config` helper output for all supported LLM and embedder providers, and the `Mem0MemoryStore` CRUD operations including tag filtering and semantic search.
- [Memory Edge Cases: Concurrency, Compaction, Unicode, Auto-Learn, and Session Lifecycle](./memory-edge-cases-concurrency-compaction-unicode-auto-learn-and-session-lifecycl.md) — The memory edge case test suite (addressing issue #36) stress-tests PocketPaw's memory subsystem under conditions that routine tests miss — concurrent writes to the same session, session compaction when history exceeds the threshold, Unicode and emoji content, empty session handling, auto-learn trigger conditions, and session listing and deletion operations.
- [Memory Isolation: Sender-Scoped Storage and Identity Injection](./memory-isolation-sender-scoped-storage-and-identity-injection.md) — Tests verifying that PocketPaw's MemoryManager correctly isolates memories by sender identity, so conversations from different users do not bleed into each other. Covers user ID resolution, per-user file routing in FileMemoryStore, and identity injection into system prompts.
- [Memory System: FileMemoryStore, MemoryManager, and Knowledge Graph Extraction](./memory-system-filememorystore-memorymanager-and-knowledge-graph-extraction.md) — The core memory test suite validates PocketPaw's `FileMemoryStore` and `MemoryManager` — covering long-term and session memory CRUD, the agent context assembly flow, and an extensive suite for the knowledge graph extraction system that builds entity-relationship graphs from conversation text using conservative regex patterns.
- [Memory Tools: RememberTool, RecallTool, and Session List API](./memory-tools-remembertool-recalltool-and-session-list-api.md) — Tests for the agent-facing memory tools (`remember` and `recall`) and the session list REST endpoint. Covers tool schema validation, content persistence, tag filtering, truncation behavior, and the API's ability to enumerate stored sessions.

### `memory-graduation` — 5 article(s)

- [FastAPI Router for Retrieval Logs and Graduation Endpoints](./fastapi-router-for-retrieval-logs-and-graduation-endpoints.md) — Exposes the retrieval projection and graduation policy through a FastAPI router, providing read endpoints for recent retrievals and graduation state, plus an action endpoint that triggers a graduation scan and applies decisions. The store is cached per journal instance so the projection is not rebuilt on every request.
- [In-Memory Projection for Retrieval and Graduation Events](./in-memory-projection-for-retrieval-and-graduation-events.md) — Provides the read-side of the retrieval domain by replaying the org journal into two in-memory views: a recent-retrievals list (filterable by scope, actor, and pocket) and a per-memory graduation state table. A single replay pass builds both views because they share the same event stream.
- [Memory Graduation Policy for the Retrieval Projection](./memory-graduation-policy-for-the-retrieval-projection.md) — Implements the access-count-based graduation logic that promotes memories across tiers (episodic to semantic, semantic to core, procedural promotion) by scanning the journal-backed retrieval projection rather than a flat JSONL file. All tuning thresholds are carried verbatim from PR #937 to preserve runtime behaviour after the storage layer refactor.
- [Retrieval and Graduation Event Payload Definitions](./retrieval-and-graduation-event-payload-definitions.md) — Defines the canonical action names and payload builders for the two journal event types that power PocketPaw's retrieval tracing and memory graduation system. These constants anchor the projection, policy, and store layers to a shared, stable vocabulary sourced from soul-protocol's v0.3.1 action catalog.
- [Widget Graduation and Co-occurrence Policy Engine](./widget-graduation-and-co-occurrence-policy-engine.md) — Implements the pin/fade/archive graduation logic for individual widgets and the threshold-based co-occurrence candidate scanner, both ported verbatim from PRs #941 and #942 onto the journal-backed projection. Decision objects and tuning knobs are preserved under their original names so existing tests and configuration carry over without renaming.

### `memory-system` — 1 article(s)

- [ChromaAdapter: Vector Database Integration for Semantic Memory Storage](./chromaadapter-vector-database-integration-for-semantic-memory-storage.md) — The `ChromaAdapter` wraps ChromaDB to provide async add, search, get-by-ID, delete, and upsert operations for PocketPaw's semantic memory tier. Tests are conditionally skipped when `chromadb` is not installed, and cover the core CRUD operations, graceful handling of small collections during search, upsert (duplicate ID update), and optional metadata storage.

### `message bus` — 5 article(s)

- [Instinct Trace Collector — Bus-Subscriber Context Manager for Reasoning Capture](./instinct-trace-collector-bus-subscriber-context-manager-for-reasoning-capture.md) — An async context manager that subscribes to the message bus for the duration of a single agent proposal, collecting fabric queries, soul recalls, KB injections, and tool calls into a structured `ReasoningTrace`. Each proposal gets its own isolated collector instance, preventing cross-contamination between concurrent proposals.
- [Message Bus Tests: Inbound/Outbound Pub-Sub, Multi-Subscriber Fan-out, and Channel Routing](./message-bus-tests-inboundoutbound-pub-sub-multi-subscriber-fan-out-and-channel-r.md) — This test suite validates PocketPaw's internal message bus, which decouples channel adapters (Telegram, Discord, WhatsApp, CLI) from the core agent logic using an async publish-subscribe queue. Tests cover inbound message queueing, outbound pub-sub subscription and delivery, multi-subscriber fan-out, unsubscribe, adapter integration, broadcast to all channels, and per-channel filtering.
- [Neonize WhatsApp Channel Adapter: Mocking, Streaming, and Bus Integration](./neonize-whatsapp-channel-adapter-mocking-streaming-and-bus-integration.md) — Tests for the NeonizeAdapter, which connects PocketPaw to WhatsApp Personal via the neonize library. Covers auto-installation of the optional dependency, message send/receive lifecycle, stream buffering, QR code exposure, and bus subscription.
- [Webhook Adapter Test Suite: Inbound Payload Handling, Sync Mode, and Send Routing](./webhook-adapter-test-suite-inbound-payload-handling-sync-mode-and-send-routing.md) — This module tests the `WebhookAdapter`, PocketPaw's generic inbound HTTP webhook channel. It covers slot configuration, payload normalization, sync vs. async dispatch modes, future-based response resolution, and streaming accumulation.
- [WhatsApp Adapter Test Suite: Webhook Verification, Lifecycle, and Message Sending](./whatsapp-adapter-test-suite-webhook-verification-lifecycle-and-message-sending.md) — This module tests PocketPaw's `WhatsAppAdapter`, covering the Meta webhook verification handshake, adapter lifecycle (`start`/`stop`), outbound message delivery, allowed-number filtering, inbound message routing, and media handling. It ensures the adapter correctly implements the WhatsApp Cloud API protocol.

### `message formatting` — 1 article(s)

- [Channel-Aware Markdown Formatting Tests](./channel-aware-markdown-formatting-tests.md) — This test module validates `convert_markdown`, PocketPaw's channel-aware formatter that translates LLM-generated markdown into the native formatting of each messaging platform. Each channel has its own rendering rules — WhatsApp uses `*bold*`, Slack uses `<url|label>` hyperlinks, Signal strips all formatting — and the tests prevent these channel-specific transformations from regressing.

### `message-bus` — 6 article(s)

- [Channel Adapter Base Classes and Auto-Install Utility](./channel-adapter-base-classes-and-auto-install-utility.md) — This module defines ChannelAdapter (a structural Protocol) and BaseChannelAdapter (an abstract base class with lifecycle management), providing the contracts that all channel adapters must satisfy. It also contains the auto_install() utility that lets adapters install their optional Python dependencies at runtime using uv or pip, with a restart-required path for packages that cannot be reloaded in-process.
- [Deep Work Human Task Router: Notification Broadcast Tests](./deep-work-human-task-router-notification-broadcast-tests.md) — This test suite validates the HumanTaskRouter, which bridges the automated Deep Work engine and human operators by publishing structured notifications over the MessageBus. It covers task notifications, review routing, plan-ready announcements, and project completion summaries, including edge cases like missing project IDs and empty task lists.
- [Discord Channel Adapter — discli Subprocess Bridge](./discord-channel-adapter-discli-subprocess-bridge.md) — DiscliAdapter connects PocketPaw to Discord by spawning a discli serve subprocess and communicating with it over stdin/stdout JSONL, rather than using discord.py directly. This architecture isolates the Discord event loop in a separate process, preventing it from interfering with PocketPaw's asyncio loop, and lets discli handle rate limiting, reconnection, and slash command registration independently.
- [Google Chat Channel Adapter — Webhook and Pub/Sub Modes](./google-chat-channel-adapter-webhook-and-pubsub-modes.md) — GoogleChatAdapter connects PocketPaw to Google Workspace Chat via two modes: webhook (for setups where the PocketPaw dashboard receives HTTP POST events from Google) and pubsub (for server-side polling via Google Cloud Pub/Sub). Both modes normalise incoming events to InboundMessage on the bus and use the Google Chat REST API to send replies, with markdown converted to Google Chat's Card format.
- [Matrix Channel Adapter — Long-Polling with Streaming via Message Edits](./matrix-channel-adapter-long-polling-with-streaming-via-message-edits.md) — MatrixAdapter connects PocketPaw to the Matrix protocol using matrix-nio's AsyncClient, supporting both access token and password authentication. Streaming agent responses are delivered by editing the initial message in place using Matrix's m.replace relation, rate-limited to one edit per 1.5 seconds to avoid homeserver rate limiting.
- [Message Bus Package Public API](./message-bus-package-public-api.md) — The pocketpaw.bus package re-exports the full public surface of the messaging subsystem: event types (InboundMessage, OutboundMessage, SystemEvent, Channel), the MessageBus and its singleton accessor, and the channel adapter contracts (ChannelAdapter protocol and BaseChannelAdapter base class). All runtime code that sends or receives messages imports from here rather than from sub-modules.

### `message-routing` — 1 article(s)

- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.

### `messaging` — 20 article(s)

- [Channel Notifier for Autonomous Outbound Alerts](./channel-notifier-for-autonomous-outbound-alerts.md) — notifier.py provides a `notify()` function that lets any part of PocketPaw push a message to configured notification channels without knowing which adapters are active. It parses `channel:chat_id` target strings, resolves them to `Channel` enum values, and publishes `OutboundMessage` events to the bus.
- [Chat Session Document for Pocket and Group Contexts](./chat-session-document-for-pocket-and-group-contexts.md) — The `Session` document stores chat session metadata shared between one-on-one pocket conversations and multi-user group chats, using a `context_type` discriminator validated on construction. It tracks message counts, last activity, and soft deletion, with four compound indexes covering the dominant query patterns.
- [Deep Work Human Task Router — Multi-Channel Notification Fanout](./deep-work-human-task-router-multi-channel-notification-fanout.md) — HumanTaskRouter bridges the Deep Work execution layer and the user by pushing human-required tasks and project lifecycle events to all configured messaging channels via MessageBus broadcast. It covers four notification types: human tasks needing action, agent-completed tasks awaiting review, plans ready for approval, and project completion summaries.
- [Discord Gateway — Bot Initialization and MessageBus Integration](./discord-gateway-bot-initialization-and-messagebus-integration.md) — discord_gateway.py provides the `run_discord_bot` entry point that wires the Discord adapter to the MessageBus and starts the AgentLoop for the Discord channel. It translates all Discord-specific configuration from Settings into the adapter, then hands off to the agent infrastructure.
- [Matrix Channel Adapter: Message Routing, Streaming, Error Recovery, and Bus Integration](./matrix-channel-adapter-message-routing-streaming-error-recovery-and-bus-integrat.md) — The Matrix adapter test suite (Sprint 21) validates PocketPaw's Matrix channel integration, covering initial-sync message filtering, room authorization, streaming with edit-in-place, MessageBus subscription lifecycle, and error isolation. The `matrix-nio` library is fully mocked as an optional dependency, allowing tests to run without a live Matrix homeserver.
- [Message Bus: Central Routing Hub for All Channel Communication](./message-bus-central-routing-hub-for-all-channel-communication.md) — MessageBus is the central publish-subscribe hub in PocketPaw that decouples channel adapters from the agent loop. Adapters publish inbound messages to a bounded asyncio queue; the agent loop consumes them. Outbound messages are fanned out to registered adapter callbacks. System events flow separately to dashboard and monitoring subscribers.
- [Message Bus Event Types: Channel, InboundMessage, OutboundMessage, SystemEvent](./message-bus-event-types-channel-inboundmessage-outboundmessage-systemevent.md) — events.py defines the four core data types that flow through PocketPaw's message bus: the Channel enum identifying communication platforms, InboundMessage for received messages, OutboundMessage for agent replies, and SystemEvent for internal runtime signals.
- [MessageService Realtime Emit Tests](./messageservice-realtime-emit-tests.md) — This test module enforces that every public mutation in MessageService fires the correct typed realtime event through emit(). It also includes regression guards ensuring the WebSocket router itself does not perform direct broadcasts that would bypass the event bus.
- [Microsoft Teams Channel Adapter via Bot Framework SDK](./microsoft-teams-channel-adapter-via-bot-framework-sdk.md) — TeamsAdapter integrates PocketPaw with Microsoft Teams using the Bot Framework SDK, receiving messages via an internal aiohttp webhook server and sending replies through the Bot Framework's `send_activity` API. Streaming is emulated by editing a placeholder message with `update_activity()`.
- [ReadState: Per-User Unread Message Tracking](./readstate-per-user-unread-message-tracking.md) — The `ReadState` document records the last message a user has read in each group, enabling O(1) unread count computation by comparing a single pointer against the group's `message_count` counter. It is updated on `read.ack` WebSocket events and tracks unread mention counts separately.
- [Signal Channel Adapter Tests — Init, Lifecycle, Message Handling, and Bus Integration](./signal-channel-adapter-tests-init-lifecycle-message-handling-and-bus-integration.md) — This Sprint 20 test file validates `SignalAdapter`, PocketPaw's channel adapter for Signal messenger. Tests cover adapter configuration defaults, start/stop lifecycle, inbound message parsing with phone number authorization, outbound message sending with streaming support, error recovery, and MessageBus integration.
- [Signal Channel Adapter via signal-cli REST API](./signal-channel-adapter-via-signal-cli-rest-api.md) — SignalAdapter integrates PocketPaw with Signal by polling a self-hosted signal-cli REST API instance every 2 seconds for inbound messages and sending replies via its HTTP endpoint. No streaming is supported — responses are buffered and sent as a single message when the agent finishes.
- [Slack Channel Adapter: Integration Tests for Messaging, Streaming, and Auth](./slack-channel-adapter-integration-tests-for-messaging-streaming-and-auth.md) — Tests for PocketPaw's Slack channel adapter, covering message send/receive, threaded replies, stream buffering, channel filtering, and token validation. Uses lightweight mock classes to avoid a real Slack dependency while exercising the full adapter surface.
- [Slack Channel Adapter with Socket Mode and Live Streaming](./slack-channel-adapter-with-socket-mode-and-live-streaming.md) — SlackAdapter connects PocketPaw to Slack using Socket Mode, which requires no public webhook URL — the bot maintains a persistent WebSocket connection to Slack's servers. It supports slash commands, DM messages, app mentions, threaded replies, file uploads, and live-edited streaming responses.
- [Smoke Test: Agent Dispatch Respond-Mode Matrix in Group Chat](./smoke-test-agent-dispatch-respond-mode-matrix-in-group-chat.md) — This script verifies that `_should_agent_respond` correctly routes messages in group chat based on each agent's respond mode (`auto`, `mention_only`, `silent`) and the set of @mentions present. It was written to lock down a real bug where two `auto` agents both fired when only one was mentioned.
- [Smoke Test: DM and Group Chat Persistence for Human Users](./smoke-test-dm-and-group-chat-persistence-for-human-users.md) — This end-to-end test verifies that DM creation between two workspace members, message exchange, and group chat all persist correctly in MongoDB — without any agent involvement. It validates the full social messaging layer: workspace invites, DM groups, populated member objects, message context types, and cross-member read access.
- [Telegram Channel Adapter with Streaming, Typing Indicators, and Forum Topic Support](./telegram-channel-adapter-with-streaming-typing-indicators-and-forum-topic-suppor.md) — TelegramAdapter connects PocketPaw to the Telegram Bot API using python-telegram-bot, supporting streaming responses via live message edits, animated typing indicators, media file sending, voice note detection, and Telegram forum topic (Supergroup) thread routing.
- [Unified Message Document for Group Chat and Agent Memory](./unified-message-document-for-group-chat-and-agent-memory.md) — The `Message` model stores two structurally different record types — multi-user group chat rows and single-agent LLM memory rows — in one MongoDB collection, discriminated by a `context_type` field. A model validator enforces strict field invariants per context type, preventing cross-contamination of group chat metadata into agent history and vice versa.
- [WhatsApp Business Cloud API Adapter](./whatsapp-business-cloud-api-adapter.md) — WhatsAppAdapter connects PocketPaw to the official Meta WhatsApp Business Cloud API, receiving messages via webhook callbacks verified with HMAC and sending replies via the Graph API. It handles the two-step media download flow and sends automatic read receipts.
- [WhatsApp Personal Mode Adapter via Neonize](./whatsapp-personal-mode-adapter-via-neonize.md) — NeonizeAdapter connects PocketPaw to WhatsApp using the neonize Python library, which wraps the open-source whatsmeow Go client. It requires only a QR-code scan to pair — no Meta Developer account or public webhook URL is needed.

### `metadata` — 2 article(s)

- [JSONL File Store: Append-Only Metadata Persistence for Uploaded Files](./jsonl-file-store-append-only-metadata-persistence-for-uploaded-files.md) — The `JSONLFileStore` provides a lightweight, append-only metadata store for file upload records using newline-delimited JSON, with an in-memory cache for fast lookups and thread-safe writes via a `Lock`. Soft deletes are implemented by appending a delete operation record rather than removing lines, preserving the full audit history of every file lifecycle event.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.

### `microsoft-teams` — 1 article(s)

- [Microsoft Teams Channel Adapter via Bot Framework SDK](./microsoft-teams-channel-adapter-via-bot-framework-sdk.md) — TeamsAdapter integrates PocketPaw with Microsoft Teams using the Bot Framework SDK, receiving messages via an internal aiohttp webhook server and sending replies through the Bot Framework's `send_activity` API. Streaming is emulated by editing a placeholder message with `update_activity()`.

### `mission-control` — 5 article(s)

- [Mission Control Data Models](./mission-control-data-models.md) — This module defines all core dataclasses and enums for Mission Control covering agent profiles, tasks, messages, activities, documents, and notifications. The models use Python dataclasses with ISO 8601 timestamps and StrEnum statuses, and have grown through two major revisions to support Deep Work v2 autonomous execution fields.
- [Mission Control File Store: JSON-Backed Persistence with In-Memory Indexes](./mission-control-file-store-json-backed-persistence-with-in-memory-indexes.md) — FileMissionControlStore implements MissionControlStoreProtocol using one JSON file per entity type, combined with in-memory Python dicts for O(1) lookups. Atomic writes via temp-file rename prevent data corruption, and a singleton factory ensures all subsystems share one loaded dataset.
- [Mission Control Heartbeat Daemon](./mission-control-heartbeat-daemon.md) — The HeartbeatDaemon is a background APScheduler-driven daemon that periodically wakes every registered agent to check for pending work, update status, and fire optional event callbacks. It uses staggered agent wakeups and a singleton pattern to ensure only one daemon runs per process.
- [Mission Control Manager: High-Level Orchestration Layer](./mission-control-manager-high-level-orchestration-layer.md) — MissionControlManager sits above the raw file store and combines storage operations with business logic: creating tasks with automatic activity logging, resolving @mention notifications, managing agent heartbeats, and orchestrating project lifecycle including on-disk directory creation.
- [Mission Control Storage Protocol Interface](./mission-control-storage-protocol-interface.md) — MissionControlStoreProtocol defines the abstract interface all Mission Control storage backends must implement using Python's structural Protocol pattern with runtime_checkable, enabling dependency injection and future backend swappability across all six entity types.

### `model selection` — 1 article(s)

- [Model Router: Task Complexity Classification and Tiered Model Selection](./model-router-task-complexity-classification-and-tiered-model-selection.md) — Tests for ModelRouter, which classifies incoming messages by complexity and routes them to the appropriate LLM tier (Haiku for simple, Sonnet for moderate, Opus for complex). Validates classification boundaries, edge cases like empty messages, and that the model names match the configured tiers.

### `model-selection` — 1 article(s)

- [Smart Model Router — Heuristic Classifier for Automatic Model Tier Selection](./smart-model-router-heuristic-classifier-for-automatic-model-tier-selection.md) — Implements `ModelRouter`, a zero-API-call heuristic classifier that maps each incoming message to a `TaskComplexity` tier (SIMPLE, MODERATE, COMPLEX) and selects the appropriate model (Haiku, Sonnet, Opus). Pattern matching prevents false-positive simple classifications for messages that require tools or deep reasoning.

### `models` — 3 article(s)

- [Audit Entry Model for Enterprise Compliance Logging](./audit-entry-model-for-enterprise-compliance-logging.md) — Defines the `AuditEntry` Pydantic model that captures the complete decision context for every auditable action in PocketPaw: who acted, on what data, what the AI recommended, and what actually happened. Field validators enforce closed enum sets for category and status, and serialization helpers bridge between the Pydantic model and SQLite storage.
- [Knowledge Engine Data Models: RawDoc, WikiArticle, Concept, LintIssue, KnowledgeIndex](./knowledge-engine-data-models-rawdoc-wikiarticle-concept-lintissue-knowledgeindex.md) — The `knowledge.models` module defines the five core dataclasses of PocketPaw's knowledge engine — `RawDoc` (ingested source), `WikiArticle` (compiled artifact), `Concept` (cross-article entity), `LintIssue` (audit finding), and `KnowledgeIndex` (master index). Each includes a `to_dict()` method for JSON serialization to disk.
- [PawKit Pydantic Models: Dashboard Configuration Schema](./pawkit-pydantic-models-dashboard-configuration-schema.md) — The `kits.models` module defines the full Pydantic schema hierarchy for PawKit configurations — from individual metric cards up to the root `PawKitConfig` that a YAML file is parsed into. It also defines `InstalledKit`, which tracks activation status and user-provided configuration values at runtime.

### `module organisation` — 3 article(s)

- [Fabric Module Entry Point — Ontology Layer Public API](./fabric-module-entry-point-ontology-layer-public-api.md) — The `ee/fabric/__init__.py` consolidates the public API of the Fabric ontology layer into a single import surface, exposing both the legacy SQLite store and the newer journal-backed path introduced in Wave 3. The `__all__` list is the canonical documentation of what is stable and intended for external use.
- [Fleet Module Entry Point — One-Command Bundle Installation](./fleet-module-entry-point-one-command-bundle-installation.md) — The `ee/fleet/__init__.py` is the public entry point for PocketPaw's Fleet subsystem, re-exporting the models, installer functions, and report types so callers can manage installable bundles — FleetTemplates — with a single import. Fleet was introduced in Move 7 PR-B to let non-technical operators install a complete agent configuration (soul, pocket, connectors, scopes) in one command.
- [Workspace Module Entry Point](./workspace-module-entry-point.md) — The `ee/cloud/workspace/__init__.py` file serves as the public entry point for the workspace domain, re-exporting the FastAPI router so that the top-level application assembler can mount it without knowing internal package structure. This single-line pattern deliberately decouples how the router is imported from where it physically lives.

### `module structure` — 2 article(s)

- [Constants Package Init: Empty Namespace Module for pocketpaw.constants](./constants-package-init-empty-namespace-module-for-pocketpawconstants.md) — The `pocketpaw/constants/__init__.py` is an intentionally empty module that exists solely to make `pocketpaw.constants` a Python package. Sub-modules like `tool_categories.py` are imported directly by consumers; no symbols are re-exported at the package level.
- [Google Drive Connector Module: Public Surface and Package Exports](./google-drive-connector-module-public-surface-and-package-exports.md) — The `pocketpaw.connectors.drive` package is the Google Drive adapter introduced in Workstream C2 of the Org Architecture RFC. Its `__init__.py` defines a minimal public surface, re-exporting the auth helper, HTTP client, error hierarchy, and the `DriveSourceAdapter` that powers zero-copy live federation.

### `monitoring` — 5 article(s)

- [Agent Status API Tests: Auth, Snapshot Shape, Version Tracking, and CLI Formatting](./agent-status-api-tests-auth-snapshot-shape-version-tracking-and-cli-formatting.md) — Tests for PocketPaw's agent status API endpoint, covering authentication via header and query parameter, snapshot structure validation against the API contract, version-based change detection for efficient polling, and CLI duration formatting helpers.
- [Desktop Tools: Screenshot Capture and System Status for Local Agent Sessions](./desktop-tools-screenshot-capture-and-system-status-for-local-agent-sessions.md) — `ScreenshotTool` and `StatusTool` are lightweight desktop interaction tools that give PocketPaw's agent visibility into the local machine — capturing the screen for visual context and querying system health metrics. Both tools are designed for local deployments where the agent runs on the same machine as the user.
- [StatusTracker: Real-Time Agent State Monitoring via Message Bus Events](./statustracker-real-time-agent-state-monitoring-via-message-bus-events.md) — `StatusTracker` subscribes to the PocketPaw message bus and maintains a live snapshot of every active agent session's current state — thinking, running a tool, streaming a response, or in an error condition. It powers the status indicator in the dashboard and supports long-poll queries so the UI can update without polling.
- [StatusTracker Tests: Event-Driven Agent State Machine](./statustracker-tests-event-driven-agent-state-machine.md) — Tests for `StatusTracker`, which maintains real-time agent state by consuming `SystemEvent` objects from the internal event bus. Covers state transitions from idle through thinking, tool execution, and error states, plus multi-session concurrency and token usage accumulation.
- [System and Usage Metrics API](./system-and-usage-metrics-api.md) — The metrics router provides two endpoints: one that reports real-time system resource usage (CPU, RAM, disk, uptime, battery), and one that clears accumulated usage records. Both are protected by an admin scope to prevent unprivileged callers from inspecting host hardware details.

### `mounts` — 1 article(s)

- [Mount Configuration YAML Loader](./mount-configuration-yaml-loader.md) — Provides `load_mounts`, which reads a `mounts.yaml` file, validates each row into a typed `MountConfig` object, and returns them sorted by `order`. Also exposes `resolve_template` for expanding path templates containing workspace or user variables at request time.

### `multi-agent` — 7 article(s)

- [A2ADelegateTool: Secure Cross-Agent Task Delegation via the A2A Protocol](./a2adelegatetool-secure-cross-agent-task-delegation-via-the-a2a-protocol.md) — `A2ADelegateTool` allows PocketPaw's agent to delegate tasks to any external agent that speaks the Agent-to-Agent (A2A) protocol, with built-in SSRF protection that blocks requests to private IP ranges. It bridges PocketPaw's internal tool system to the open A2A agent network.
- [Agent Pool — On-Demand Agent Instance Management with Idle Eviction](./agent-pool-on-demand-agent-instance-management-with-idle-eviction.md) — Implements `AgentPool`, a lifecycle manager for cloud-deployed agent instances. Each agent gets its own `AgentBackend`, `SoulManager`, and isolated memory namespace. Instances are cached for reuse and evicted after configurable idle timeout (default 5 minutes) to bound memory usage.
- [DelegateToClaudeCodeTool: Critical-Trust Agent-to-Agent Delegation via CLI](./delegatetoclaudecodetool-critical-trust-agent-to-agent-delegation-via-cli.md) — `DelegateToClaudeCodeTool` gives PocketPaw's agent the ability to hand off complex coding tasks to Claude Code CLI, the Anthropic autonomous coding agent. It is marked `critical` trust — the highest level in PocketPaw's trust hierarchy — because Claude Code has unrestricted filesystem, shell, and web access.
- [Mission Control: Data Models, File Store, and Manager Operations](./mission-control-data-models-file-store-and-manager-operations.md) — Tests for PocketPaw's multi-agent orchestration layer, covering AgentProfile and Task data models, the file-based persistence store, and the high-level MissionControlManager that coordinates agents, tasks, messages, documents, and heartbeats.
- [Mission Control Heartbeat Daemon: Agent Health and Work Polling](./mission-control-heartbeat-daemon-agent-health-and-work-polling.md) — Tests for HeartbeatDaemon, the background process that periodically wakes registered agents, checks for pending tasks and notifications, and records liveness signals. Covers daemon lifecycle, configurable intervals, work discovery, error resilience, and manual triggers.
- [Mission Control REST API: FastAPI Endpoint Tests](./mission-control-rest-api-fastapi-endpoint-tests.md) — Tests for the FastAPI router that exposes Mission Control functionality over HTTP, covering agent, task, message, document, activity, stats, and notification endpoints. Uses FastAPI's TestClient with monkeypatched singletons for full request/response validation without a live server.
- [Mission Control Task Executor: Security, Concurrency, and Streaming](./mission-control-task-executor-security-concurrency-and-streaming.md) — Tests for MCTaskExecutor, the component that runs agent tasks in the background, streams results, and broadcasts events. Covers singleton lifecycle, UUID validation, error sanitization, duplicate-execution prevention, and background task completion.

### `multi-backend` — 1 article(s)

- [Tool Bridge: Adapting PocketPaw Tools for Multiple Agent SDK Formats](./tool-bridge-adapting-pocketpaw-tools-for-multiple-agent-sdk-formats.md) — The tool bridge discovers all registered PocketPaw tools and wraps them in the native function-tool format expected by each agent backend (OpenAI Agents SDK `FunctionTool`, Google ADK `FunctionTool`, LangChain `StructuredTool`). It enforces backend-aware exclusion rules — the Claude Agent SDK gets shell/FS/edit tools excluded because the CLI provides them natively — and scans tool outputs for injection attacks before returning results to the agent.

### `multi-channel` — 1 article(s)

- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.

### `multi-tenancy` — 5 article(s)

- [Agent Loop Pocket Threading Tests: Cloud User and Workspace Context Resolution](./agent-loop-pocket-threading-tests-cloud-user-and-workspace-context-resolution.md) — Tests for the `_create_pocket_and_session` and `_publish_pocket_event` functions in `pocketpaw.agents.loop`, covering all resolution paths for threading cloud user identity and workspace context through pocket creation: explicit ids, active-workspace fallback, first-user fallback for self-hosted deployments, graceful handling of invalid ids, and metadata propagation in pocket events.
- [MemoryFactDoc — Beanie Document for Agent Long-Term and Daily Memory](./memoryfactdoc-beanie-document-for-agent-long-term-and-daily-memory.md) — The `MemoryFactDoc` Beanie ODM document stores agent long-term and daily memory facts in the `memory_facts` MongoDB collection. It is distinct from the `messages` collection and carries workspace and user ownership fields to support multi-tenant scoping.
- [MongoMemoryStore — Full MemoryStoreProtocol Implementation on MongoDB](./mongomemorystore-full-memorystoreprotocol-implementation-on-mongodb.md) — The production MongoDB implementation of PocketPaw's `MemoryStoreProtocol`, bridging the OSS memory interface to the cloud's Beanie ODM schema. SESSION entries map to the `messages` collection; LONG_TERM and DAILY facts map to `memory_facts`. The store also manages session metadata lifecycle and normalizes session key formats between the message bus and the database.
- [MongoMemoryStore Multi-Tenant Workspace Isolation Tests](./mongomemorystore-multi-tenant-workspace-isolation-tests.md) — These tests verify that MongoMemoryStore stamps workspace_id onto both session messages and fact documents at write time, and that read operations are scoped to the requested workspace to prevent cross-tenant data leakage. Untagged rows with workspace_id=None must never appear in workspace-scoped queries.
- [Workspace-Level Knowledge Browser Router](./workspace-level-knowledge-browser-router.md) — A FastAPI router mounted at `/api/v1/knowledge/*` that fans out KB article listings across every agent in a workspace. It exists as a separate router from `/api/v1/kb/*` because it has different aggregation semantics — workspace-wide rollup rather than single-scope access.

### `multi-tenant` — 1 article(s)

- [Cloud Context Threading in Chat API: User ID and Workspace Propagation Tests](./cloud-context-threading-in-chat-api-user-id-and-workspace-propagation-tests.md) — Tests that `resolve_cloud_context` correctly injects `cloud_user_id` and `cloud_workspace_id` into `InboundMessage.metadata` when an authenticated cloud user is present, and leaves those keys absent for anonymous or unauthenticated requests. The test file operates at the unit level — directly calling `_build_inbound_message` and `_send_message` — to avoid the overhead of standing up a full SSE streaming client.

### `multi-turn` — 1 article(s)

- [OpenAI Agents SDK Backend: Tool Wiring, Session Management, and Graceful Degradation](./openai-agents-sdk-backend-tool-wiring-session-management-and-graceful-degradatio.md) — Tests for the OpenAI Agents SDK backend adapter, covering custom tool registration, per-backend provider selection, SDK-absent graceful degradation, native SQLite session management, and lazy database path resolution. All tests run without a real OpenAI Agents SDK installation via comprehensive mocking.

### `multi-user` — 1 article(s)

- [Memory Isolation: Sender-Scoped Storage and Identity Injection](./memory-isolation-sender-scoped-storage-and-identity-injection.md) — Tests verifying that PocketPaw's MemoryManager correctly isolates memories by sender identity, so conversations from different users do not bleed into each other. Covers user ID resolution, per-user file routing in FileMemoryStore, and identity injection into system prompts.

### `multi-user isolation` — 1 article(s)

- [Daily Notes Isolation Tests: Per-User Scoping and Backward-Compatible Legacy Note Visibility](./daily-notes-isolation-tests-per-user-scoping-and-backward-compatible-legacy-note.md) — This test file, added during security sprint cluster D (issue #887), validates that daily notes in the `MemoryManager` are scoped to the `sender_id` that created them, preventing one user's private notes from leaking into another user's agent context. It also tests the backward-compatibility guarantee that legacy notes written before the fix remain visible as system-wide entries.

### `multilingual` — 1 article(s)

- [Sarvam AI Integration Tests — Translation, TTS, STT, and OCR](./sarvam-ai-integration-tests-translation-tts-stt-and-ocr.md) — This comprehensive test file covers PocketPaw's integration with Sarvam AI's multilingual APIs: text translation, text-to-speech (TTS), speech-to-text (STT), and vision OCR. Tests validate API key gating, SDK error propagation, audio encoding, file-based I/O, provider routing, policy group membership, config field defaults, and tool registration.

### `natural language` — 1 article(s)

- [Reminders API Tests: Natural Language Scheduling, Listing, and Deletion](./reminders-api-tests-natural-language-scheduling-listing-and-deletion.md) — This test file covers PocketPaw's `/api/v1/reminders` router, which accepts natural-language reminder messages, parses them into scheduled events via the scheduler, and exposes list and delete operations with human-readable time-remaining formatting.

### `natural language processing` — 1 article(s)

- [Natural Time Parsing Tests: parse_natural_time Scheduler Function](./natural-time-parsing-tests-parsenaturaltime-scheduler-function.md) — This test suite validates the `parse_natural_time` function in PocketPaw's scheduler module, covering every user-facing time expression format — with and without the 'in' prefix, abbreviations, singular/plural forms, and embedded sentences. The suite acts as both a regression guard and a specification for all accepted input shapes.

### `natural-language` — 1 article(s)

- [Reminder Management Schemas](./reminder-management-schemas.md) — Defines Pydantic models for PocketPaw's reminder system, which accepts natural-language reminder requests, parses them into scheduled entries, and delivers notifications when the trigger time arrives. The schema design keeps the creation API intentionally simple to support conversational input.

### `networking` — 3 article(s)

- [Cloudflare Tunnel Manager: Exposing Local PocketPaw Servers to the Public Internet](./cloudflare-tunnel-manager-exposing-local-pocketpaw-servers-to-the-public-interne.md) — The `TunnelManager` class manages a `cloudflared` subprocess to create a Cloudflare Tunnel that exposes a locally running PocketPaw server on a public HTTPS URL, enabling webhook delivery, mobile access, and external agent connectivity without firewall configuration. It supports auto-installation via Homebrew, URL extraction from stderr with a regex, graceful shutdown, and singleton lifecycle registration.
- [Remote Tunnel API — Cloudflare Tunnel Management](./remote-tunnel-api-cloudflare-tunnel-management.md) — The remote router exposes three endpoints for controlling a Cloudflare tunnel that makes the locally running PocketPaw instance accessible over the public internet. This lets users interact with their companion from mobile devices or share access without configuring port forwarding.
- [Remote Tunnel Status and Control Schemas](./remote-tunnel-status-and-control-schemas.md) — Defines the minimal Pydantic models for PocketPaw's remote tunnel API, which exposes the agent's local HTTP server to the internet via a secure tunnel. The two schemas cover tunnel status querying and tunnel start results, including error reporting for failed tunnel launches.

### `normalisation` — 1 article(s)

- [Ripple Spec Normalizer: Canonicalising AI-Generated Pocket Layouts Before Persistence](./ripple-spec-normalizer-canonicalising-ai-generated-pocket-layouts-before-persist.md) — The ripple normalizer is a single-function module that accepts the free-form dict output of agent-generated pocket specs and canonicalises it into a consistent envelope shape before it is written to MongoDB. It handles three distinct spec variants (multi-pane, UISpec v1.0, and flat widget lists) with minimal transformation, and generates stable widget IDs when they are missing to ensure idempotent downstream rendering.

### `notification` — 1 article(s)

- [Channel Notifier Tests: Multi-Target Message Dispatch, Validation, and Settings-Driven Defaults](./channel-notifier-tests-multi-target-message-dispatch-validation-and-settings-dri.md) — This test suite validates `notify()`, PocketPaw's high-level function for pushing outbound messages to one or more channels. Tests cover multi-target fan-out, graceful handling of malformed target strings, unknown channel names, empty target lists, settings-driven default targets, chat IDs containing colons, and delivery across all supported channels.

### `notifications` — 10 article(s)

- [Channel Notifier for Autonomous Outbound Alerts](./channel-notifier-for-autonomous-outbound-alerts.md) — notifier.py provides a `notify()` function that lets any part of PocketPaw push a message to configured notification channels without knowing which adapters are active. It parses `channel:chat_id` target strings, resolves them to `Channel` enum values, and publishes `OutboundMessage` events to the bus.
- [Deep Work Human Task Router — Multi-Channel Notification Fanout](./deep-work-human-task-router-multi-channel-notification-fanout.md) — HumanTaskRouter bridges the Deep Work execution layer and the user by pushing human-required tasks and project lifecycle events to all configured messaging channels via MessageBus broadcast. It covers four notification types: human tasks needing action, agent-completed tasks awaiting review, plans ready for approval, and project completion summaries.
- [In-App Notification Document](./in-app-notification-document.md) — The `Notification` model is the persistence layer for PocketPaw's in-app notification system, storing per-user alerts with typed sources, read state, and optional expiry. It pairs with the `NotificationService` for fan-out and the realtime layer for push delivery.
- [Notification Derivation Tests: Mention, Reaction, and Invite Trigger Verification](./notification-derivation-tests-mention-reaction-and-invite-trigger-verification.md) — These tests verify the three primary notification derivation paths — @mention in a group message, emoji reaction, and workspace invite — correctly call NotificationService.create with the right arguments. They also verify that self-targeted events and reaction removals never produce notifications.
- [Notification Response Schema](./notification-response-schema.md) — `NotificationResponse` is the Pydantic response model for the notifications API, defining the wire format returned to frontend clients. It normalizes MongoDB document fields into a consistent camelCase-compatible shape with string-typed timestamps for JSON serialization safety.
- [Notification Service: CRUD and Realtime Fan-Out](./notification-service-crud-and-realtime-fan-out.md) — The `NotificationService` is a stateless class providing four async operations — create, list, mark-read, and clear-all — each of which writes to MongoDB via Beanie and immediately emits a corresponding realtime event for connected WebSocket clients. The service is the single point of truth for notification lifecycle, keeping routers and other domain services free of MongoDB and emit logic.
- [Notifications Domain Package](./notifications-domain-package.md) — The notifications `__init__.py` serves as the package boundary marker for PocketPaw's in-app notifications domain, grouping the service, router, and response schemas into a cohesive module. No symbols are re-exported; the package is imported by the application factory to register the router.
- [Notifications REST API Router](./notifications-rest-api-router.md) — The notifications router exposes three endpoints for listing, marking read, and bulk-clearing a user's in-app notifications, with authentication enforced via `current_active_user` dependency injection. All mutation operations are scoped strictly to the authenticated user to prevent cross-user data access.
- [NotificationService Unit Tests: CRUD, Fan-Out, and Idempotency Contract](./notificationservice-unit-tests-crud-fan-out-and-idempotency-contract.md) — These unit tests exercise NotificationService in isolation by patching the Notification Beanie document and the emit function, verifying that create persists then emits, list_for_user filters at the database layer, mark_read is idempotent for already-read notifications, and clear_all uses a bulk update and emits a single event.
- [Unread Service - Per-User Unread Counts and Mention Badges](./unread-service-per-user-unread-counts-and-mention-badges.md) — `UnreadService` tracks per-user, per-group unread message counts and mention badges by combining a `ReadState` watermark document with a live message count query. It provides the data for inbox badges and the visual unread indicator in the sidebar.

### `oauth2` — 1 article(s)

- [OAuth2 Token Exchange and Revocation Schemas](./oauth2-token-exchange-and-revocation-schemas.md) — Defines the Pydantic models for PocketPaw's OAuth2 token lifecycle — code exchange, token refresh, standard token responses, and revocation. Field-level validation enforces RFC 6749 grant type restrictions at the schema boundary, catching invalid requests before they reach the OAuth2 implementation.

### `observability` — 9 article(s)

- [Agent Runtime Status Schemas](./agent-runtime-status-schemas.md) — Defines the Pydantic models for PocketPaw's agent status API, covering per-session execution states and global runtime health. The session model captures granular execution phase (thinking, tool running, streaming) alongside token usage and duration, giving operators deep visibility into what the agent is doing at any moment.
- [Audit Logging System: Append-Only Agent Action Trail](./audit-logging-system-append-only-agent-action-trail.md) — This module implements a secure, append-only audit log that records every critical agent action to a JSONL file at `~/.pocketpaw/audit.jsonl`. It provides structured severity tiers, optional PII filtering, a callback fan-out mechanism, and a singleton accessor — forming the forensic backbone of PocketPaw's security stack.
- [Credential Scrubber: Keeping Secrets Out of Audit Logs and Error Records](./credential-scrubber-keeping-secrets-out-of-audit-logs-and-error-records.md) — This module provides `scrub_params()` and `scrub_command()` — two functions that mask credential-like data before it enters audit logs, system logger fallbacks, and dangerous-command records. It combines an explicit secret field list from `credentials.py` with heuristic field-name patterns to catch secrets regardless of how they are named.
- [Health Diagnostic Tools: System Checks, Error Logs, and Config Doctor](./health-diagnostic-tools-system-checks-error-logs-and-config-doctor.md) — The `health.py` module provides three diagnostic `BaseTool` subclasses — `HealthCheckTool`, `ErrorLogTool`, and `ConfigDoctorTool` — that give the PocketPaw agent access to the runtime health engine, persistent error log, and configuration validator. These tools exist so the agent can self-diagnose problems when a user reports something is broken, rather than returning a generic error and asking the user to check logs manually.
- [Instinct Trace Collector — Bus-Subscriber Context Manager for Reasoning Capture](./instinct-trace-collector-bus-subscriber-context-manager-for-reasoning-capture.md) — An async context manager that subscribes to the message bus for the duration of a single agent proposal, collecting fabric queries, soul recalls, KB injections, and tool calls into a structured `ReasoningTrace`. Each proposal gets its own isolated collector instance, preventing cross-contamination between concurrent proposals.
- [Logging Setup: Rich Console Output with Secret and PII Scrubbing](./logging-setup-rich-console-output-with-secret-and-pii-scrubbing.md) — `logging_setup.py` configures PocketPaw's logging infrastructure with Rich-formatted console output and two security filters: `SecretFilter` (scrubs API key patterns from all log messages) and `PIILogFilter` (opt-in scrubbing of personally identifiable information). These filters prevent credential and user data leakage through log output regardless of log level.
- [PocketPaw CLI Diagnostics — Doctor, Ollama, and OpenAI-Compatible Health Checks](./pocketpaw-cli-diagnostics-doctor-ollama-and-openai-compatible-health-checks.md) — diagnostics.py contains the three CLI health check implementations extracted from `__main__.py`: `run_doctor` for a full system health report, `check_ollama` for Ollama connectivity and model tool-calling support, and `check_openai_compatible` for OpenAI-compatible endpoint validation. All three produce ANSI-colored terminal output with structured pass/warn/fail indicators.
- [SystemInfoTool: Host Resource Monitoring with Optional psutil Integration](./systeminfotool-host-resource-monitoring-with-optional-psutil-integration.md) — `SystemInfoTool` reports CPU, RAM, disk, network I/O, and optionally top processes by delegating to `get_system_status()` for the base snapshot, then augmenting with `psutil` data when available. The psutil dependency is treated as optional — the tool degrades gracefully to the base snapshot if psutil is not installed.
- [UsageTracker: Append-Only Token and Cost Tracking Across Sessions](./usagetracker-append-only-token-and-cost-tracking-across-sessions.md) — UsageTracker records per-request LLM usage (input tokens, output tokens, cached tokens, cost) as append-only JSONL entries in `~/.pocketpaw/usage.jsonl`, providing aggregation helpers for the `/api/v1/metrics/usage` endpoint. Thread safety is achieved with a `threading.Lock` so background telemetry threads and async request handlers can safely share the tracker.

### `onboarding` — 1 article(s)

- [Web Server: QR Code Pairing Flow for Telegram Bot Onboarding](./web-server-qr-code-pairing-flow-for-telegram-bot-onboarding.md) — The web server module drives PocketPaw's QR code–based Telegram pairing flow: it generates a signed deep link, serves a minimal HTML page with a scannable QR code, and simultaneously listens for the bot's `/start <secret>` confirmation over the Telegram API. Dynamic port allocation ensures the local server starts cleanly even when the default port is busy.

### `ontology` — 5 article(s)

- [Fabric FastAPI Router — Ontology CRUD and Query Endpoints](./fabric-fastapi-router-ontology-crud-and-query-endpoints.md) — The Fabric router exposes HTTP endpoints for managing the ontology layer: defining object types, creating and querying objects, creating and listing links, and retrieving stats. It uses the legacy SQLite `FabricStore` for all persistence and was updated in Cluster C / PR3 to add `GET /fabric/objects` and `GET /fabric/links` so the PocketDataPanel's Objects and Links sub-tabs render real data.
- [Fabric Module Entry Point — Ontology Layer Public API](./fabric-module-entry-point-ontology-layer-public-api.md) — The `ee/fabric/__init__.py` consolidates the public API of the Fabric ontology layer into a single import surface, exposing both the legacy SQLite store and the newer journal-backed path introduced in Wave 3. The `__all__` list is the canonical documentation of what is stable and intended for external use.
- [Fabric Pydantic Models — Ontology Layer Data Structures](./fabric-pydantic-models-ontology-layer-data-structures.md) — This module defines the Pydantic models that represent the Fabric ontology layer's core entities: property definitions, object types, object instances, directional links between objects, query parameters, and query results. The `_gen_id` function produces time-ordered, prefix-tagged identifiers that balance human readability with sortability.
- [Fabric Tools: Ontology Query, Creation, and Statistics](./fabric-tools-ontology-query-creation-and-statistics.md) — The Fabric tools module exposes three agent-facing tools — `FabricQueryTool`, `FabricCreateTool`, and `FabricStatsTool` — that allow the agent to interact with PocketPaw's Fabric ontology: a graph-based knowledge store for objects and their relationships. All three tools use lazy imports and silent telemetry to avoid circular dependencies and ensure tool calls never fail due to missing enterprise modules or sick tracing infrastructure.
- [FabricStore — Async SQLite Backend for the Ontology Layer](./fabricstore-async-sqlite-backend-for-the-ontology-layer.md) — `FabricStore` is the legacy async SQLite persistence layer for Fabric's ontology data, managing object types, object instances, and directional links. It uses lazy schema initialization, parameterized queries throughout, and a manual cascade-delete pattern because SQLite foreign key enforcement is not enabled by default.

### `openai` — 2 article(s)

- [Codex CLI Backend — Subprocess Wrapper for OpenAI's Terminal Agent](./codex-cli-backend-subprocess-wrapper-for-openais-terminal-agent.md) — Implements `CodexCLIBackend`, which spawns OpenAI's `codex` CLI as an async subprocess and parses its NDJSON event stream. Prompts are passed via stdin rather than as command-line arguments to avoid OS argument-length limits, and a 10 MiB subprocess buffer accommodates large MCP tool results.
- [OpenAI Agents SDK Backend — GPT and Ollama Agent Support with SQLite Session Persistence](./openai-agents-sdk-backend-gpt-and-ollama-agent-support-with-sqlite-session-persi.md) — Implements `OpenAIAgentsBackend` using OpenAI's `openai-agents` SDK, supporting GPT models and any OpenAI-compatible endpoint (Ollama, OpenRouter, LiteLLM). Sessions are persisted to a SQLite database so multi-turn conversations survive process restarts, unlike most other backends.

### `opencode` — 1 article(s)

- [OpenCode Backend — REST API Client for a Running OpenCode Server](./opencode-backend-rest-api-client-for-a-running-opencode-server.md) — Implements `OpenCodeBackend`, which communicates with a locally running OpenCode server (`opencode --server`) via its REST API using `httpx`. Sessions are mapped from PocketPaw session keys to OpenCode session IDs, and streaming responses are parsed from NDJSON parts.

### `optional features` — 1 article(s)

- [Screenshot Tool: Optional Desktop Capture with Graceful Headless Fallback](./screenshot-tool-optional-desktop-capture-with-graceful-headless-fallback.md) — The screenshot module provides a single function for capturing the current display as PNG bytes, using pyautogui when available and returning an actionable error string when it is not. The optional-import pattern ensures PocketPaw's core runtime does not require a GUI dependency, keeping server and headless deployments working without modification.

### `orchestration` — 11 article(s)

- [Deep Work Data Models — Project, TaskSpec, AgentSpec, and PlannerResult](./deep-work-data-models-project-taskspec-agentspec-and-plannerresult.md) — This module defines the core data structures for the Deep Work orchestration layer: Project (top-level orchestration unit), TaskSpec and AgentSpec (planning-phase blueprints), and PlannerResult (complete planner output). These models separate planning artifacts from execution artifacts, allowing the planner to produce a full plan before any Mission Control objects are created.
- [Deep Work Dependency Scheduler — DAG-Based Task Dispatch with Cycle Detection](./deep-work-dependency-scheduler-dag-based-task-dispatch-with-cycle-detection.md) — DependencyScheduler manages task dispatch order in a Deep Work project by treating tasks as a directed acyclic graph (DAG). It finds ready tasks (all blockers resolved), dispatches them concurrently, handles human task routing, validates for cycles via Kahn's algorithm, and optionally runs in a tick-synchronized mode driven by a SimulationClock.
- [Deep Work Planner Agent — Four-Phase LLM Project Planning](./deep-work-planner-agent-four-phase-llm-project-planning.md) — PlannerAgent orchestrates the four-phase Deep Work planning pipeline — research, PRD generation, task breakdown, and team assembly — through sequential LLM calls via AgentRouter. It produces a PlannerResult containing the PRD markdown, a list of TaskSpec objects with dependency keys, and recommended AgentSpec team members.
- [Deep Work Session — Full Project Lifecycle Orchestrator](./deep-work-session-full-project-lifecycle-orchestrator.md) — DeepWorkSession is the top-level coordinator for a Deep Work project, tying together GoalParser, PlannerAgent, DependencyScheduler, MCTaskExecutor, and HumanTaskRouter into a single class that manages a project from user input through goal analysis, LLM planning, approval, execution, pause, resume, and cancellation. It also handles crash recovery for interrupted projects on startup.
- [Fleet Installer: Manifest Loading, Path Traversal Guard, and Install Orchestration Tests](./fleet-installer-manifest-loading-path-traversal-guard-and-install-orchestration-.md) — This test suite covers PocketPaw's fleet installer, which provisions a full AI agent environment (soul, pocket, connectors) from a declarative manifest. It validates YAML and JSON manifest loading, a P0 path-traversal security fix that clamps string inputs to the bundled fleet directory, install orchestration with partial failure handling, and the InstallReport shape.
- [Fleet Installer — Template Loading, Path Traversal Guard, and Journal Emission](./fleet-installer-template-loading-path-traversal-guard-and-journal-emission.md) — The Fleet installer loads YAML or JSON fleet templates, installs the bundled soul/pocket/connector/scope configuration, and emits correlated journal events for observability. A critical path-traversal security fix was applied in `fix/fleet-install-auth-guard`: string-path inputs from the REST router are now clamped to the bundled templates directory, preventing a workspace admin from coercing the server into reading arbitrary files.
- [Fleet Module Entry Point — One-Command Bundle Installation](./fleet-module-entry-point-one-command-bundle-installation.md) — The `ee/fleet/__init__.py` is the public entry point for PocketPaw's Fleet subsystem, re-exporting the models, installer functions, and report types so callers can manage installable bundles — FleetTemplates — with a single import. Fleet was introduced in Move 7 PR-B to let non-technical operators install a complete agent configuration (soul, pocket, connectors, scopes) in one command.
- [Mission Control: Data Models, File Store, and Manager Operations](./mission-control-data-models-file-store-and-manager-operations.md) — Tests for PocketPaw's multi-agent orchestration layer, covering AgentProfile and Task data models, the file-based persistence store, and the high-level MissionControlManager that coordinates agents, tasks, messages, documents, and heartbeats.
- [Mission Control Heartbeat Daemon: Agent Health and Work Polling](./mission-control-heartbeat-daemon-agent-health-and-work-polling.md) — Tests for HeartbeatDaemon, the background process that periodically wakes registered agents, checks for pending tasks and notifications, and records liveness signals. Covers daemon lifecycle, configurable intervals, work discovery, error resilience, and manual triggers.
- [Mission Control REST API: FastAPI Endpoint Tests](./mission-control-rest-api-fastapi-endpoint-tests.md) — Tests for the FastAPI router that exposes Mission Control functionality over HTTP, covering agent, task, message, document, activity, stats, and notification endpoints. Uses FastAPI's TestClient with monkeypatched singletons for full request/response validation without a live server.
- [Unified Agent Loop — Core Orchestrator for Message Processing and Agent Execution](./unified-agent-loop-core-orchestrator-for-message-processing-and-agent-execution.md) — Implements `AgentLoop`, the central orchestrator that consumes messages from the bus, routes them through `AgentRouter` to the configured backend, applies security scanning and PII redaction, stores memories, and streams responses back to channels. It also manages the soul's cognitive pipeline and per-session concurrency locks.

### `outbound messaging` — 1 article(s)

- [Channel Notifier Tests: Multi-Target Message Dispatch, Validation, and Settings-Driven Defaults](./channel-notifier-tests-multi-target-message-dispatch-validation-and-settings-dri.md) — This test suite validates `notify()`, PocketPaw's high-level function for pushing outbound messages to one or more channels. Tests cover multi-target fan-out, graceful handling of malformed target strings, unknown channel names, empty target lists, settings-driven default targets, chat IDs containing colons, and delivery across all supported channels.

### `package management` — 3 article(s)

- [InstallPackageTool: Guardian-Reviewed pip Package Installation](./installpackagetool-guardian-reviewed-pip-package-installation.md) — The `InstallPackageTool` allows the PocketPaw agent to install Python packages via pip at runtime, subject to two security layers: a strict regex that validates the package spec format before any subprocess is spawned, and a Guardian AI review that checks the package name for typosquatting and malicious intent before installation proceeds. It runs with `trust_level = "elevated"` because it can permanently modify the Python environment.
- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.
- [Skill Installer: Secure GitHub-to-Disk Skill Installation with Audit Trail](./skill-installer-secure-github-to-disk-skill-installation-with-audit-trail.md) — This module handles the full lifecycle of installing skills from GitHub repositories: validating the source string, cloning to a temp directory, stripping symlinks, copying SKILL.md directories to the install location, and emitting audit events for every installation attempt. Symlink stripping is the key defensive measure against path traversal attacks via malicious repositories.

### `package organization` — 1 article(s)

- [Uploads Package Namespace Marker](./uploads-package-namespace-marker.md) — This is an empty `__init__.py` that marks `ee/cloud/uploads/` as a Python package. It contains no code or exports; all uploads functionality is provided by the submodules within this package.

### `package structure` — 17 article(s)

- [Agent-to-Agent (A2A) Protocol Module: Public API and Roadmap](./agent-to-agent-a2a-protocol-module-public-api-and-roadmap.md) — The `pocketpaw.a2a` package implements Google's Agent-to-Agent (A2A) protocol, allowing PocketPaw to act as both an A2A server (accepting tasks from external agents) and an A2A client (delegating tasks to external agents). This `__init__.py` re-exports the full public surface and documents the three-phase implementation roadmap.
- [Cloud Chat Test Sub-Package Initialiser](./cloud-chat-test-sub-package-initialiser.md) — The empty `tests/cloud/chat/__init__.py` marks the `tests/cloud/chat/` directory as a Python package so pytest can discover chat-specific cloud tests as `tests.cloud.chat.*` and so that the `conftest.py` in the same directory can provide fixtures to the full chat test sub-package.
- [Cloud Test Sub-Package Initialiser](./cloud-test-sub-package-initialiser.md) — The empty `tests/cloud/__init__.py` marks the `tests/cloud/` directory as a Python package, enabling pytest to discover cloud-related tests as part of the `tests.cloud` package namespace and allowing conftest fixtures defined at higher levels to propagate correctly into cloud test files.
- [Guards Package: Unified Re-Export for RBAC and ABAC Authorization](./guards-package-unified-re-export-for-rbac-and-abac-authorization.md) — The `guards` package `__init__.py` is a single-point re-export that collects all authorization primitives — roles, access levels, policy evaluation, action rules, FastAPI dependency factories, and audit helpers — under one importable namespace. This prevents callers from needing to know which sub-module owns each symbol, and makes future refactoring transparent to the rest of the codebase.
- [Integrations Package Initializer — OAuth and Google Workspace Integration Namespace](./integrations-package-initializer-oauth-and-google-workspace-integration-namespac.md) — The `integrations` package `__init__.py` is a minimal namespace marker created in February 2026 to anchor PocketPaw's OAuth and Google Workspace integration modules (Gmail, Calendar, Drive, Docs). It contains no exports or logic, serving purely as a package boundary.
- [LLM Package Public API: LLMClient, LLMRouter, and resolve_llm_client](./llm-package-public-api-llmclient-llmrouter-and-resolvellmclient.md) — The `pocketpaw.llm` package exposes three public symbols — `LLMClient`, `LLMRouter`, and `resolve_llm_client` — forming the canonical entry point for all LLM interactions in PocketPaw. This init file enforces a stable import surface so internal module restructuring does not break callsites.
- [LLM Provider Adapters Package: Public Interface for get_adapter](./llm-provider-adapters-package-public-interface-for-getadapter.md) — The `pocketpaw.llm.providers` package re-exports `ProviderAdapter`, `ProviderConfig`, `get_adapter`, and `resolve_model` as its public API, giving callers a single import point for the provider adapter system. The init file documents the canonical usage pattern so it doubles as inline API documentation.
- [Optional Dependency Helper: `require_extra`](./optional-dependency-helper-requireextra.md) — `_compat.py` provides a single utility function that raises a clean `ImportError` with pip install instructions when an optional PocketPaw feature's dependency is missing. This replaces scattered `try/except ImportError` blocks across channel adapters and integrations with a single, consistent error message pattern.
- [PawKits Builtins Package: Placeholder for Shipped Kit Definitions](./pawkits-builtins-package-placeholder-for-shipped-kit-definitions.md) — The `pocketpaw.kits.builtins` package is a reserved namespace for built-in PawKit definitions that ship with PocketPaw. Currently it contains only a module docstring, indicating the built-in kit definitions are loaded from YAML files via the catalog rather than from Python modules.
- [PawKits Package: Dashboard System Public API](./pawkits-package-dashboard-system-public-api.md) — The `pocketpaw.kits` package exposes the complete public surface for PawKits — PocketPaw's configurable command-center dashboard system. It re-exports models, catalog entries, and store operations as a single flat namespace so consumers never need to import from internal submodules.
- [PocketPaw Package Root and Version Declaration](./pocketpaw-package-root-and-version-declaration.md) — The top-level `__init__.py` for the `pocketpaw` package declares the current version string and the package tagline. It is intentionally minimal — no imports, no symbols exported — following the principle that the package root should not trigger side effects at import time.
- [Realtime WebSocket Event Package Entry Point](./realtime-websocket-event-package-entry-point.md) — This is the package initialiser for the `ee.cloud.realtime` module, which houses the WebSocket-based realtime event system for the EE cloud tier. The file itself carries only a module docstring, serving as the namespace anchor that makes `ee.cloud.realtime` importable as a package while the substantive components live in sub-modules.
- [Sessions Package Entry Point: Router Re-Export](./sessions-package-entry-point-router-re-export.md) — This is the package initialiser for `ee.cloud.sessions`, which re-exports the FastAPI `router` object from `ee.cloud.sessions.router` to make the router accessible via a short import path. The single re-export is the standard pattern used across EE cloud domain packages to allow the top-level application factory to mount domain routers without importing from sub-modules directly.
- [Shared Cross-Cutting Concerns Package Entry Point](./shared-cross-cutting-concerns-package-entry-point.md) — This is the package initialiser for `ee.cloud.shared`, the module that hosts cross-cutting utilities shared across all EE cloud domains. The file contains only a module docstring and no code, making it a namespace anchor that allows other modules to import from `ee.cloud.shared.*` sub-modules like `deps`, `errors`, `events`, and `time`.
- [Test Suite Root: PocketPaw Test Package Initialiser](./test-suite-root-pocketpaw-test-package-initialiser.md) — The top-level `tests/__init__.py` is a minimal package marker that makes the `tests/` directory a Python package, enabling pytest and other test runners to discover and import test modules using package-relative imports. Its content is intentionally minimal—a single comment—confirming its structural rather than executable role.
- [Uploads Package Init: Namespace Marker for the Upload Subsystem](./uploads-package-init-namespace-marker-for-the-upload-subsystem.md) — The `uploads/__init__.py` is an intentionally empty package initializer that establishes `pocketpaw.uploads` as a Python namespace without importing any submodules at package load time. This lazy-loading design means the upload subsystem's dependencies are only loaded when explicitly imported, keeping base startup time low.
- [vectordb Package: VectorStoreProtocol Public Interface](./vectordb-package-vectorstoreprotocol-public-interface.md) — The `vectordb` package's `__init__.py` re-exports `VectorStoreProtocol` as the single public symbol, giving callers a stable import path that is decoupled from the internal module layout. This thin re-export layer prevents callers from depending on the internal `protocol` module path directly and preserves refactoring freedom.

### `package-structure` — 12 article(s)

- [Agents Package Initializer — AgentRouter Public Surface](./agents-package-initializer-agentrouter-public-surface.md) — The `pocketpaw.agents` package init re-exports `AgentRouter` as the sole public symbol, establishing a clean import boundary for the agents subsystem. Consumers import the router without knowing its internal module location.
- [Audit Module Package Initialization](./audit-module-package-initialization.md) — The `pocketpaw.audit` package init re-exports the three public symbols consumers need: `AuditEntry` (the Pydantic model), `AuditStore` (the SQLite backend), and `get_audit_store` (the singleton accessor). This keeps the import path flat for callers while the implementations stay in separate sub-modules.
- [Automations Subpackage — Rule-Based Automation Engine Namespace](./automations-subpackage-rule-based-automation-engine-namespace.md) — The `pocketpaw.ee.automations` package is the namespace for PocketPaw's rule-based automation engine, grouping the models, bridge, evaluator, and store modules that together implement trigger-action workflows for enterprise pockets.
- [Bootstrap Package Public API](./bootstrap-package-public-api.md) — The bootstrap package exposes the four core primitives needed to assemble an agent's initial context: the protocol contracts, the default file-based provider, the cloud-agent provider, and the context builder. Centralising these re-exports here keeps consumer imports clean and makes it easy to see what the subsystem offers at a glance.
- [Browser Automation Module Public API](./browser-automation-module-public-api.md) — The browser package re-exports the public surface of PocketPaw's Playwright-based browser automation subsystem: snapshot types (RefMap, AccessibilityNode, SnapshotGenerator), the low-level driver (BrowserDriver, NavigationResult), and the session management layer (BrowserSession, BrowserSessionManager, get_browser_session_manager). These exports give agent tools a single import point for all browser interaction primitives.
- [Builtin Tools Lazy Loader: Optional Dependency Safety via __getattr__](./builtin-tools-lazy-loader-optional-dependency-safety-via-getattr.md) — The `pocketpaw/tools/builtin/__init__.py` implements a lazy import scheme using Python's module-level `__getattr__` hook, ensuring that importing one built-in tool never fails due to a missing optional dependency of a different tool. This lets PocketPaw operate with a minimal install footprint while still exposing a rich tool catalogue.
- [CLI Package Namespace for PocketPaw Subcommands](./cli-package-namespace-for-pocketpaw-subcommands.md) — The `pocketpaw.cli` package namespace serves as the entry point for all PocketPaw CLI subcommands. The `__init__.py` file contains only a module docstring, acting as a namespace marker that groups CLI subcommand modules under a shared package.
- [Enterprise Edition Package — Gated Feature Namespace](./enterprise-edition-package-gated-feature-namespace.md) — The `pocketpaw.ee` package is the root namespace for PocketPaw's Enterprise Edition, housing features gated behind team, business, and enterprise plans under the Functional Source License (FSL 1.1). It organizes five EE subsystems: guards (RBAC/ABAC), automations (trigger-action workflows), fabric (workspace mesh), instinct (proactive behaviors), and audit (compliance trail).
- [Message Bus Package Public API](./message-bus-package-public-api.md) — The pocketpaw.bus package re-exports the full public surface of the messaging subsystem: event types (InboundMessage, OutboundMessage, SystemEvent, Channel), the MessageBus and its singleton accessor, and the channel adapter contracts (ChannelAdapter protocol and BaseChannelAdapter base class). All runtime code that sends or receives messages imports from here rather than from sub-modules.
- [Soul Protocol Package Root: Integration Anchor for PocketPaw](./soul-protocol-package-root-integration-anchor-for-pocketpaw.md) — The `soul/__init__.py` file is the namespace anchor for PocketPaw's Soul Protocol integration sub-package. It signals the package boundary and establishes the import root from which `cognitive.py` and `manager.py` are accessible.
- [Tools Package Public API: Protocol, Registry, and Policy Exports](./tools-package-public-api-protocol-registry-and-policy-exports.md) — The `pocketpaw/tools/__init__.py` re-exports the three foundational primitives of PocketPaw's tool system: the `ToolProtocol`/`BaseTool` interface, the `ToolRegistry`, and the `ToolPolicy` with its grouping constants. This single import surface means tool consumers never need to know the internal module layout.
- [Widget Journal Projection Package — Public API](./widget-journal-projection-package-public-api.md) — Re-exports the complete public surface of the widget journal projection domain, including the store, projection classes, policy functions, and all canonical event names and payload builders. This module also documents why the package supersedes held PRs #941 and #942 and ships the fix for a token-sort bug that broke co-occurrence deduplication.

### `packaging` — 3 article(s)

- [Desktop Launcher Build Pipeline: PyInstaller, Code Signing, DMG, and Inno Setup](./desktop-launcher-build-pipeline-pyinstaller-code-signing-dmg-and-inno-setup.md) — The build script packages the PocketPaw desktop launcher into a distributable native application for macOS and Windows. It chains icon generation, PyInstaller bundling, platform-specific code signing, DMG creation for macOS, and Inno Setup invocation for Windows into a single `build()` function.
- [Packaging Sanity Tests: Dependency Conflicts, Version Bounds, and Extra Consistency](./packaging-sanity-tests-dependency-conflicts-version-bounds-and-extra-consistency.md) — Tests that parse `pyproject.toml` directly to catch dependency version conflicts and packaging invariants before they cause runtime failures. Specifically targets the FastAPI/Starlette version conflict introduced by MCP's transitive Starlette dependency, and verifies that dashboard-mode dependencies are included in the core package.
- [PocketPaw Desktop Launcher Package Version Bootstrap](./pocketpaw-desktop-launcher-package-version-bootstrap.md) — Defines the `pocketpaw-launcher` package version using `importlib.metadata` with a fallback to the `POCKETPAW_VERSION` environment variable, supporting both installed-package and frozen-executable (PyInstaller) deployment scenarios. This thin init file establishes the package namespace for the launcher subsystem.

### `parallelism` — 1 article(s)

- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.

### `path utilities` — 1 article(s)

- [Upload Path Utility Tests: Normalization, Traversal Defense, Subpath Checks, and Join Safety](./upload-path-utility-tests-normalization-traversal-defense-subpath-checks-and-joi.md) — Exhaustive unit tests for the `ee.cloud.uploads.paths` module, which provides the safe path utilities used throughout the upload folder system. Tests cover normalization (canonicalization, dot-stripping, repeated-slash collapse), rejection of path traversal sequences and dangerous characters, subpath relationship checks, and safe join operations.

### `paw` — 7 article(s)

- [Paw Agent Factory: Wiring Soul, Bootstrap, and Tools Together](./paw-agent-factory-wiring-soul-bootstrap-and-tools-together.md) — get_paw_agent() is the single composition root for a fully configured paw agent: it loads PawConfig, awakens or births a Soul, wires SoulBridge and SoulBootstrapProvider, registers the four core soul tools, and returns a PawAgent dataclass ready for CLI commands. A startup guard raises a clear error if soul-protocol is not installed.
- [Paw CLI: Click-Based Command Interface for Project-Level AI](./paw-cli-click-based-command-interface-for-project-level-ai.md) — paw/cli.py implements the full paw CLI using Click with eight commands — init, ask, chat, serve, status, doctor, os, channels — and an async bridge that handles both normal invocation and pytest-asyncio environments. The serve command is a documented placeholder; all other commands are functional.
- [Paw Module Entry Point](./paw-module-entry-point.md) — The paw package's __init__.py exposes only PawConfig from its config submodule, deliberately keeping the import surface minimal to avoid pulling in soul-protocol as a hard dependency for users who install the base pocketpaw package without the soul extra.
- [Paw Project Scanner: Heuristic Project Knowledge Ingestion](./paw-project-scanner-heuristic-project-knowledge-ingestion.md) — scan.py bootstraps a soul's project knowledge by reading common configuration files (README, pyproject.toml, package.json, Cargo.toml, go.mod, .env.example) and storing facts via soul.remember(). A full agent-powered scan is defined but currently delegates to the heuristic fallback pending AgentRouter wiring.
- [Paw Soul Tools: BaseTool Wrappers for Soul-Protocol Operations](./paw-soul-tools-basetool-wrappers-for-soul-protocol-operations.md) — tools.py defines nine BaseTool subclasses that expose soul-protocol's memory and identity operations as agent-callable tools, including remember, recall, edit core memory, status introspection, rubric-based self-evaluation, hot reload from disk, selective forgetting, and topic-aware context retrieval.
- [PawConfig: Project-Level Configuration with YAML Fallback Parser](./pawconfig-project-level-configuration-with-yaml-fallback-parser.md) — PawConfig is a dataclass that loads paw.yaml from the project root, applies PAW_PROVIDER and PAW_SOUL_PATH environment variable overrides, and provides computed properties for the .paw directory and default soul file path. A minimal built-in YAML parser handles environments where PyYAML is not installed.
- [Soul Bridge: Connecting soul-protocol to PocketPaw's Agent Loop](./soul-bridge-connecting-soul-protocol-to-pocketpaws-agent-loop.md) — soul_bridge.py provides two classes: SoulBootstrapProvider maps soul state and memories into the BootstrapContext consumed by AgentContextBuilder while preserving tool documentation from the default provider, and SoulBridge exposes a simple observe/recall interface with transparent fallback to older soul-protocol API versions.

### `paw print` — 4 article(s)

- [Paw Print Models — Secure Widget Render Vocabulary and Event Types](./paw-print-models-secure-widget-render-vocabulary-and-event-types.md) — Defines the Pydantic models for the Paw Print widget layer with a security-first design: no raw HTML, capped collection sizes, a per-widget rotatable access token, and a 4KB payload cap on inbound events. The models form the contract between the backend and the widget.js bundle.
- [Paw Print Router — HTTP Surface for Widget Management and Event Ingest](./paw-print-router-http-surface-for-widget-management-and-event-ingest.md) — Provides the FastAPI routes for the Paw Print widget layer: public CORS-gated spec serving, owner-authenticated widget CRUD, and a multi-layered event ingest endpoint that enforces domain validation, rate limiting, Guardian screening, and Fabric object mapping before persisting customer interactions.
- [Paw Print Store — Async SQLite Persistence for Widgets and Events](./paw-print-store-async-sqlite-persistence-for-widgets-and-events.md) — Provides all async SQLite read and write operations for the Paw Print widget layer, including widget CRUD with token rotation, append-only event recording, and a dual-level rate-limit enforcement method that checks both overall widget throughput and per-customer throughput within a rolling time window.
- [Paw Print Widget Layer — Package Entry Point](./paw-print-widget-layer-package-entry-point.md) — The `ee.paw_print` package provides the backend for customer-facing embedded widgets that close the decision loop: customer interactions on a rendered widget flow into a Pocket in real time, Instinct nudges the owner, and approved actions feed back to the widget. This module re-exports every public symbol from the two sub-modules.

### `pawkit` — 1 article(s)

- [PawKit Configuration Schema — Command Center Template Format](./pawkit-configuration-schema-command-center-template-format.md) — PawKit is a publishable Command Center template defined as a Pydantic v2 YAML configuration. It specifies dashboard layout (panels, sections, grid columns), automated workflows (scheduled or trigger-based), user-configurable install fields, bundled skills, and external integration requirements — everything needed to ship a reusable agent workspace.

### `performance` — 1 article(s)

- [ReadState: Per-User Unread Message Tracking](./readstate-per-user-unread-message-tracking.md) — The `ReadState` document records the last message a user has read in each group, enabling O(1) unread count computation by comparing a single pointer against the group's `message_count` counter. It is updated on `read.ack` WebSocket events and tracks unread mention counts separately.

### `performance optimization` — 1 article(s)

- [Tool Pre-Filtering: Relevance Scoring to Reduce Prompt Bloat](./tool-pre-filtering-relevance-scoring-to-reduce-prompt-bloat.md) — The tool filter module scores each registered tool against the current user message using token overlap, returning only the top-N most relevant tools to avoid overloading the LLM context window. A curated set of essential tools -- memory, search, and pocket management -- are always included regardless of relevance score.

### `permissions` — 2 article(s)

- [Files RBAC and ABAC Permission Evaluator](./files-rbac-and-abac-permission-evaluator.md) — Implements the two-layer permission model for the files module: RBAC permissions come from providers on a per-entry basis, while ABAC rules apply as a post-filter that can only further restrict visibility. The `PermissionsEvaluator` combines both layers to produce the final UI-facing capability list for each file entry.
- [Tool Category Mapping: Frontend Category IDs to Backend Tool Groups for Permission and Selection](./tool-category-mapping-frontend-category-ids-to-backend-tool-groups-for-permissio.md) — `tool_categories.py` provides two lookup tables that translate the frontend's UI category IDs (e.g., `"google_workspace"`) to the backend's tool group names (e.g., `["group:gmail", "group:calendar"]`), enabling the dashboard's category-level permission checkboxes to correctly gate individual tools without hard-coding the mapping in the UI layer.

### `persistence` — 12 article(s)

- [AutomationStore: JSON File-Backed Persistence for Automation Rules](./automationstore-json-file-backed-persistence-for-automation-rules.md) — The `AutomationStore` class manages CRUD operations and fire-count tracking for automation rules, persisting state to a JSON file at `~/.pocketpaw/automations/rules.json`. It is accessed as a module-level singleton to ensure all router and evaluator calls operate on the same in-memory rule set.
- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.
- [FabricStore — Async SQLite Backend for the Ontology Layer](./fabricstore-async-sqlite-backend-for-the-ontology-layer.md) — `FabricStore` is the legacy async SQLite persistence layer for Fabric's ontology data, managing object types, object instances, and directional links. It uses lazy schema initialization, parameterized queries throughout, and a manual cascade-delete pattern because SQLite foreign key enforcement is not enabled by default.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.
- [Memory Tools: Remember, Recall, and Forget Long-Term Agent Memory](./memory-tools-remember-recall-and-forget-long-term-agent-memory.md) — The `memory.py` module provides three `BaseTool` subclasses — `RememberTool`, `RecallTool`, and `ForgetTool` — that expose PocketPaw's long-term memory manager to the agent, enabling it to save facts about the user, retrieve them in future sessions, and delete outdated or incorrect memories. These tools are the agent-facing surface of the memory manager, which persists memories across session boundaries independently of the LLM's context window.
- [Mission Control: Data Models, File Store, and Manager Operations](./mission-control-data-models-file-store-and-manager-operations.md) — Tests for PocketPaw's multi-agent orchestration layer, covering AgentProfile and Task data models, the file-based persistence store, and the high-level MissionControlManager that coordinates agents, tasks, messages, documents, and heartbeats.
- [Mission Control File Store: JSON-Backed Persistence with In-Memory Indexes](./mission-control-file-store-json-backed-persistence-with-in-memory-indexes.md) — FileMissionControlStore implements MissionControlStoreProtocol using one JSON file per entity type, combined with in-memory Python dicts for O(1) lookups. Atomic writes via temp-file rename prevent data corruption, and a singleton factory ensures all subsystems share one loaded dataset.
- [OAuth2 Storage: In-Memory Auth Codes and File-Persisted Tokens](./oauth2-storage-in-memory-auth-codes-and-file-persisted-tokens.md) — `OAuthStorage` uses a two-tier storage strategy: authorization codes live in memory only (ephemeral, 10-minute TTL), while access and refresh tokens are persisted to `~/.pocketpaw/oauth_tokens.json` so authenticated desktop sessions survive server restarts. A pre-registered `pocketpaw-desktop` client with Tauri-compatible redirect URIs is always available.
- [ReminderScheduler Cron Expression and File Corruption Tests](./reminderscheduler-cron-expression-and-file-corruption-tests.md) — This test file validates `ReminderScheduler`'s support for recurring cron-based reminders and its resilience to corrupted or malformed reminder files. Tests cover cron expression validation, preset schedules, persistent-vs-oneshot behavior, file quarantine on corruption, and partial corruption recovery.
- [SoulManager: Lifecycle Management for the PocketPaw Soul Instance](./soulmanager-lifecycle-management-for-the-pocketpaw-soul-instance.md) — `SoulManager` is PocketPaw's singleton that owns the entire lifecycle of a `.soul` file — initialization, concurrency-safe observation, periodic auto-save, external change detection, and graceful shutdown. It is the integration seam between PocketPaw's real-time agent sessions and the persistent soul-protocol memory system.
- [SoulManager Lifecycle Tests: Birth, Persistence, Import, and Concurrency](./soulmanager-lifecycle-tests-birth-persistence-import-and-concurrency.md) — Comprehensive tests for `SoulManager`, the singleton that manages a PocketPaw soul's full lifecycle — from birth through autosave, import, reload, and shutdown. Covers tool caching, dirty tracking, concurrent observe serialization, corrupt file fallback, and biorhythm configuration.
- [WikiStore: File-Based CRUD for Knowledge Articles](./wikistore-file-based-crud-for-knowledge-articles.md) — WikiStore is the persistence layer for PocketPaw's knowledge subsystem, managing file-based CRUD for raw documents and compiled wiki articles organized by scope. It maintains a predictable three-directory layout per scope and handles index serialization to ensure fast listing without scanning the filesystem on every read.

### `personality` — 1 article(s)

- [Agent Soul Configuration and OCEAN Personality Schema Tests](./agent-soul-configuration-and-ocean-personality-schema-tests.md) — This module tests the `AgentConfig` model's soul-related fields — default values, persona assignment, custom OCEAN personality vectors, and absence of a `soul_path` attribute — and verifies that `CreateAgentRequest` correctly propagates `persona` and soul OCEAN customization fields.

### `personalization` — 1 article(s)

- [User Profile Bootstrap: USER.md Integration with BootstrapContext and System Prompt Injection](./user-profile-bootstrap-usermd-integration-with-bootstrapcontext-and-system-promp.md) — The bootstrap system supports a `USER.md` file that stores human-readable user profile information (name, timezone, preferences) which is loaded into `BootstrapContext` and injected into the agent's system prompt under a `# User Profile` section. Tests verify the default empty state, template creation, idempotent non-overwrite, loading, system prompt injection, and graceful handling of a missing file.

### `pii` — 1 article(s)

- [Security Package Entry Point: Audit, Guardian, and PII Surface](./security-package-entry-point-audit-guardian-and-pii-surface.md) — The pocketpaw.security package re-exports three security subsystems through its __init__.py: AuditLogger for structured event logging with severity levels, GuardianAgent for content policy enforcement, and PIIScanner for detecting and acting on personally identifiable information in agent inputs and outputs.

### `pipeline` — 1 article(s)

- [ResearchTool: Multi-Step Web Research Pipeline with LLM Summarization](./researchtool-multi-step-web-research-pipeline-with-llm-summarization.md) — `ResearchTool` orchestrates a four-stage pipeline — web search, URL discovery, content extraction, and LLM summarization — into a single `research` tool call. Depth levels let callers trade speed against thoroughness, and an optional memory-save step persists findings across conversations.

### `plan mode` — 2 article(s)

- [Plan Mode API Tests: Approve and Reject Active Agent Plans](./plan-mode-api-tests-approve-and-reject-active-agent-plans.md) — This test file covers PocketPaw's `/api/v1/plan` router, which lets the dashboard approve or reject an agent's proposed action plan before execution. It verifies the session-keyed lookup, the not-found response when no active plan exists, and input validation.
- [Plan Mode Tests: PlanStep, ExecutionPlan, PlanManager, and Approval Flow](./plan-mode-tests-planstep-executionplan-planmanager-and-approval-flow.md) — PocketPaw's plan mode allows agents to propose multi-step execution plans and wait for human approval before running potentially destructive operations. These tests cover `PlanStep` previews, `ExecutionPlan` assembly, `PlanManager` lifecycle (create, approve, reject, expire), and the async approval-wait mechanism.

### `plan-mode` — 1 article(s)

- [Plan Mode Approval Schemas](./plan-mode-approval-schemas.md) — Defines the minimal Pydantic models for PocketPaw's plan-mode API — the human-in-the-loop gate where a user approves or rejects an agent's proposed plan before execution begins. The session key binds the approval action to a specific pending plan.

### `planning` — 5 article(s)

- [Deep Work Data Models — Project, TaskSpec, AgentSpec, and PlannerResult](./deep-work-data-models-project-taskspec-agentspec-and-plannerresult.md) — This module defines the core data structures for the Deep Work orchestration layer: Project (top-level orchestration unit), TaskSpec and AgentSpec (planning-phase blueprints), and PlannerResult (complete planner output). These models separate planning artifacts from execution artifacts, allowing the planner to produce a full plan before any Mission Control objects are created.
- [Deep Work Goal Parser — Structured Goal Analysis via LLM](./deep-work-goal-parser-structured-goal-analysis-via-llm.md) — GoalParser is the first primitive in the Deep Work pipeline, taking raw user input and producing a structured GoalAnalysis that captures domain, complexity, AI/human role split, constraints, and clarification questions. It routes the LLM prompt through AgentRouter, strips code fences from the response, and defensively validates every field before constructing the dataclass.
- [Deep Work Planner Agent — Four-Phase LLM Project Planning](./deep-work-planner-agent-four-phase-llm-project-planning.md) — PlannerAgent orchestrates the four-phase Deep Work planning pipeline — research, PRD generation, task breakdown, and team assembly — through sequential LLM calls via AgentRouter. It produces a PlannerResult containing the PRD markdown, a list of TaskSpec objects with dependency keys, and recommended AgentSpec team members.
- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.
- [Deep Work Prompt Templates — LLM Instruction Set for Planning Pipeline](./deep-work-prompt-templates-llm-instruction-set-for-planning-pipeline.md) — This module centralizes all LLM prompt templates used by the Deep Work planning pipeline as module-level string constants. Separating prompts from logic enables iteration on prompt text without touching orchestration code, and makes the planning pipeline's LLM behavior fully visible in one file.

### `playback-control` — 1 article(s)

- [Spotify Tools: Search, Now Playing, Playback Control, and Playlist Management](./spotify-tools-search-now-playing-playback-control-and-playlist-management.md) — Four `BaseTool` subclasses — `spotify_search`, `spotify_now_playing`, `spotify_playback`, and `spotify_playlist` — wrap `SpotifyClient` to give agents full Spotify control: catalog search, current track status, transport controls, and playlist operations. All raise `RuntimeError` for auth failures, which surfaces as a clean error rather than a stack trace.

### `playwright` — 1 article(s)

- [Playwright Browser Driver — LLM-Oriented Browser Control](./playwright-browser-driver-llm-oriented-browser-control.md) — BrowserDriver wraps Playwright's async API into a simplified interface designed for LLM-driven browser automation, where the agent reads accessibility tree snapshots instead of raw HTML and interacts via stable integer reference numbers. It handles Playwright import validation at construction time, auto-installs Chromium if no browser binary is found, and prefers the system's installed Chrome to avoid downloading a separate Chromium build.

### `plugin architecture` — 1 article(s)

- [Backend Registry: Lazy Discovery and Plugin Support for Agent Backends](./backend-registry-lazy-discovery-and-plugin-support-for-agent-backends.md) — The backend registry maps short backend names to `(module_path, class_name)` pairs and imports them on demand, so PocketPaw starts cleanly even when optional SDKs like `openai-agents` or `google-adk` are not installed. It also handles legacy backend name migration and exposes a plugin registration API.

### `plugin system` — 1 article(s)

- [Agent Backend Registry: Lazy Import, Legacy Name Resolution, and Plugin Registration Tests](./agent-backend-registry-lazy-import-legacy-name-resolution-and-plugin-registratio.md) — This test suite covers PocketPaw's backend registry, which provides lazy-loaded access to agent backends (Claude SDK, OpenAI Agents, Google ADK, OpenCode) while maintaining backward compatibility through a legacy name mapping. Tests verify that deprecated names like `gemini_cli` transparently resolve to `google_adk`, that unknown backends return `None` rather than raising, and that third-party plugins can register themselves at runtime.

### `pocket layouts` — 2 article(s)

- [Pocket Layout Helpers: Export Determinism, Parse Validation, Round-Trip, and Store Scoping Tests](./pocket-layout-helpers-export-determinism-parse-validation-round-trip-and-store-s.md) — This unit test suite covers the pure Python helpers in `ee.cloud.pockets.layouts` — specifically `export_layout_yaml` (deterministic YAML serialization), `parse_layout_yaml` (safe YAML parsing with input validation), and `UserTemplateStore` (in-process template store with workspace scoping). No FastAPI, Beanie, or MongoDB is involved.
- [Pocket Layout Routes: Export, Template Create/List, and Workspace Scoping Tests](./pocket-layout-routes-export-template-createlist-and-workspace-scoping-tests.md) — This integration test suite covers three new routes on the pockets router that implement the layout save/share surface: `POST /pockets/{id}/export-layout` (YAML serialization), `POST /pockets/templates` (template creation), and `GET /pockets/templates` (workspace-scoped listing). It verifies the round-trip fidelity, metadata override support, cross-workspace isolation, and malformed YAML handling.

### `pocket management` — 1 article(s)

- [Pocket Tools: Create, Add Widget, and Remove Widget for Ripple UI Specs](./pocket-tools-create-add-widget-and-remove-widget-for-ripple-ui-specs.md) — The `pocket.py` module provides three `BaseTool` subclasses — `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — that allow the agent to generate and modify Ripple UniversalSpec JSON structures for pocket workspaces. The module handles three generations of the spec format (flat widget arrays, single-pane UISpec trees, and multi-pane pane-level UISpec trees) and maintains a display-type-to-Ripple-widget mapping for backward compatibility with legacy spec fields.

### `pocket spec` — 1 article(s)

- [Pocket Tool Test Suite: Widget Lifecycle, Legacy Conversion, and UISpec Contracts](./pocket-tool-test-suite-widget-lifecycle-legacy-conversion-and-uispec-contracts.md) — Comprehensive tests for `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — the three built-in tools that let AI agents build and mutate dashboard "pockets" in PocketPaw. The suite enforces spec versioning (v1.0 UISpec vs v2.0 widget array), backward compatibility of the `name→title` alias, and structural contracts on every tool's JSON output.

### `pockets` — 13 article(s)

- [Agent-Facing Pocket Context Fetcher](./agent-facing-pocket-context-fetcher.md) — `fetch_pocket_for_agent` retrieves a pocket document and strips sensitive fields before returning it as a compact JSON-safe dict for injection into AI agent tool responses. The function follows a defensive result-envelope pattern, always returning `{"ok": bool, ...}` rather than raising exceptions, so agent tool handlers can branch on success without try/except.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Pocket and Widget Documents](./pocket-and-widget-documents.md) — The `Pocket` document is the primary workspace unit in PocketPaw — a named canvas that holds an ordered grid of `Widget` sub-documents, a team roster, assigned agents, and a ripple spec for graph-based automation. Widgets are embedded directly in the Pocket document and carry their own ObjectId so the frontend can address them by stable ID rather than array index.
- [Pocket Chat Router — Context-Aware Workspace Creation and Interaction](./pocket-chat-router-context-aware-workspace-creation-and-interaction.md) — The pockets router is PocketPaw's dedicated endpoint for creating and interacting with pocket workspaces — themed dashboards of widgets, charts, and data panels. It streams responses via SSE, injects dynamic Ripple widget documentation for new pockets, and normalises three distinct AI output formats into render-ready Ripple specs.
- [Pocket Journal SSE Stream for RippleGraphWidget](./pocket-journal-sse-stream-for-ripplegraphwidget.md) — The journal stream router serves a live Server-Sent Events feed of soul-protocol journal entries filtered to a single pocket, enabling the frontend RippleGraphWidget to render agent decisions, retrievals, and tool traces as a live causation graph. It supports resumable cursors, cold-start backlog replay, and keepalive heartbeats to survive proxy timeouts.
- [Pocket Layout Export and User-Defined Template Registry](./pocket-layout-export-and-user-defined-template-registry.md) — This module provides two distinct but related capabilities: a pure YAML serialisation of a pocket's ripple spec for export or sharing, and an in-process workspace-scoped store for user-defined pocket templates. Both capabilities back new REST endpoints added in Cluster B Sub-PR #3 to close a gap in the UI testing guide around widget layout save and share.
- [Pockets Domain Package](./pockets-domain-package.md) — The pockets `__init__.py` re-exports the `router` symbol from `ee.cloud.pockets.router`, making the router available as `ee.cloud.pockets.router` for registration by the application factory. This single-line re-export pattern keeps the application factory import simple while preserving module boundaries within the domain.
- [Pockets Domain Pydantic Schemas: Request and Response Models](./pockets-domain-pydantic-schemas-request-and-response-models.md) — This file defines all Pydantic request and response models for the pockets domain, covering creation, update, widget management, reordering, share links, collaborators, and the full pocket response shape. A notable recent addition is support for passing agents, rippleSpec, and an initial widgets list on creation so the frontend can fully specify a pocket in a single request.
- [Pockets Domain Schemas: Request Validation and Response Model Tests](./pockets-domain-schemas-request-validation-and-response-model-tests.md) — This test file validates all Pydantic request/response schemas in the pockets domain — covering field defaults, enum constraints, length limits, optional-field behavior, and the full `PocketResponse` shape. It ensures the API surface contracts are locked in and that invalid inputs are rejected at the schema layer before reaching business logic.
- [Pockets FastAPI Router: CRUD, Widgets, Sharing, and Layout Export](./pockets-fastapi-router-crud-widgets-sharing-and-layout-export.md) — This is the FastAPI router for the pockets domain, covering the full lifecycle of a pocket from creation through sharing, widget management, agent and team membership, and the newer layout export and user template endpoints. All routes are gated behind a license check dependency, and owner-only operations use explicit access guard dependencies to prevent privilege escalation.
- [PocketService: Business Logic for Pocket Lifecycle, Widgets, Sharing, and Agents](./pocketservice-business-logic-for-pocket-lifecycle-widgets-sharing-and-agents.md) — PocketService is the stateless service class that encapsulates all pocket business logic — from CRUD and widget management through share link generation, collaborator access, and agent-generated pocket creation from ripple specs. Access control is enforced by two helper functions that distinguish owner-only actions from edit-access actions, and every mutation emits events to the realtime bus and shared event bus.
- [Ripple Spec Normalizer: Canonicalising AI-Generated Pocket Layouts Before Persistence](./ripple-spec-normalizer-canonicalising-ai-generated-pocket-layouts-before-persist.md) — The ripple normalizer is a single-function module that accepts the free-form dict output of agent-generated pocket specs and canonicalises it into a consistent envelope shape before it is written to MongoDB. It handles three distinct spec variants (multi-pane, UISpec v1.0, and flat widget lists) with minimal transformation, and generates stable widget IDs when they are missing to ensure idempotent downstream rendering.
- [Smoke Test: Pocket Sidebar Session Creation and Chat Persistence](./smoke-test-pocket-sidebar-session-creation-and-chat-persistence.md) — This test mirrors the full flow of PocketPaw's enterprise sidebar: a user creates a Pocket, opens a new session under that Pocket, sends a message, and verifies the message appears in the session history API. It validates that `Session` documents are correctly linked to their parent Pocket in MongoDB.

### `policy-engine` — 2 article(s)

- [Memory Graduation Policy for the Retrieval Projection](./memory-graduation-policy-for-the-retrieval-projection.md) — Implements the access-count-based graduation logic that promotes memories across tiers (episodic to semantic, semantic to core, procedural promotion) by scanning the journal-backed retrieval projection rather than a flat JSONL file. All tuning thresholds are carried verbatim from PR #937 to preserve runtime behaviour after the storage layer refactor.
- [Widget Graduation and Co-occurrence Policy Engine](./widget-graduation-and-co-occurrence-policy-engine.md) — Implements the pin/fade/archive graduation logic for individual widgets and the threshold-based co-occurrence candidate scanner, both ported verbatim from PRs #941 and #942 onto the journal-backed projection. Decision objects and tuning knobs are preserved under their original names so existing tests and configuration carry over without renaming.

### `polling` — 1 article(s)

- [Signal Channel Adapter via signal-cli REST API](./signal-channel-adapter-via-signal-cli-rest-api.md) — SignalAdapter integrates PocketPaw with Signal by polling a self-hosted signal-cli REST API instance every 2 seconds for inbound messages and sending replies via its HTTP endpoint. No streaming is supported — responses are buffered and sent as a single message when the agent finishes.

### `port management` — 1 article(s)

- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.

### `presence` — 1 article(s)

- [Presence Broadcast and Grace Window Tests](./presence-broadcast-and-grace-window-tests.md) — This test module verifies the WebSocket endpoint's presence online/offline lifecycle, specifically the grace window that delays broadcasting a user offline until reconnection is no longer possible. Tests drive _schedule_presence_offline directly and use time-acceleration to keep the suite fast.

### `privacy` — 1 article(s)

- [PII Detection and Masking: Regex-Based Personal Data Protection Layer](./pii-detection-and-masking-regex-based-personal-data-protection-layer.md) — This module provides a configurable PII detection and masking system using pre-compiled regex patterns for common personal data types (emails, phone numbers, SSNs, etc.). It supports per-type action overrides — mask, redact, hash, or passthrough — and integrates as both a standalone scanner and a subsystem of the audit logger's PII filter.

### `proactive agent` — 1 article(s)

- [Intentions API Tests: CRUD, Toggle, and On-Demand Execution](./intentions-api-tests-crud-toggle-and-on-demand-execution.md) — This test file covers PocketPaw's `/api/v1/intentions` router, which manages proactive agent intentions — scheduled or triggered actions the agent initiates autonomously. It verifies the full CRUD lifecycle, enable/disable toggling, input validation, and on-demand execution.

### `proactive features` — 1 article(s)

- [Proactive Daemon Tests: Cron Scheduling, Intention CRUD, Context Hub, and Stale Session Triggers](./proactive-daemon-tests-cron-scheduling-intention-crud-context-hub-and-stale-sess.md) — This test module covers PocketPaw's proactive daemon — the subsystem that fires agent prompts on a schedule rather than waiting for user messages. It validates cron expression parsing (including named presets), the `IntentionStore` CRUD lifecycle, `ContextHub` template variable injection, the `TriggerEngine` scheduling layer, and the stale-session trigger that nudges idle users.

### `process management` — 1 article(s)

- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.

### `process-management` — 1 article(s)

- [Desktop Launcher Server Manager: Process Lifecycle, Health Checks, and PID File Management](./desktop-launcher-server-manager-process-lifecycle-health-checks-and-pid-file-man.md) — The `ServerManager` class controls the lifecycle of the PocketPaw FastAPI server as a subprocess launched from the desktop launcher. It handles start, stop, restart, health polling, stale PID file cleanup, and dynamic port allocation — all designed to survive launcher restarts and OS-level process deaths.

### `productivity` — 1 article(s)

- [Google Calendar Tools: Event Listing, Creation, and Meeting Preparation](./google-calendar-tools-event-listing-creation-and-meeting-preparation.md) — The `calendar.py` module provides three `BaseTool` implementations — `CalendarListTool`, `CalendarCreateTool`, and `CalendarPrepTool` — that give PocketPaw's agent read/write access to Google Calendar and the ability to generate pre-meeting briefings. These tools were part of the Phase 2 Integration Ecosystem and require an authenticated Google OAuth session.

### `project configuration` — 1 article(s)

- [AGENTS.md Support Tests: Loader, Caching, Directory Walk, Context Injection, and SystemEvent Emission](./agentsmd-support-tests-loader-caching-directory-walk-context-injection-and-syste.md) — Tests for the AGENTS.md feature (issue #456) that allows project-specific constraints to be injected into the agent's system prompt. Covers the `AgentsMdLoader` find-and-cache logic (including `.git` boundary stopping, nearest-file preference, and mtime-based cache invalidation), `AgentsMd` formatting, `AgentContextBuilder` injection, and `AgentLoop` `SystemEvent` emission when the file is found.

### `project initialization` — 1 article(s)

- [Paw Project Scanner Tests: heuristic_scan and scan_prompt Coverage](./paw-project-scanner-tests-heuristicscan-and-scanprompt-coverage.md) — The `paw scan` heuristic reads a project's on-disk artifacts — README, `pyproject.toml`, `package.json`, `.env.example`, and top-level directory structure — and stores extracted facts into the soul's memory. These tests verify that each artifact type is correctly parsed, that the soul's `remember()` method is called with appropriate metadata, and that failures in memory storage do not abort the scan.

### `project management` — 1 article(s)

- [Mission Control (Deep Work) API Tests: Project Lifecycle, State Transitions, and REST Endpoints](./mission-control-deep-work-api-tests-project-lifecycle-state-transitions-and-rest.md) — This test module, created in February 2026, covers PocketPaw's Mission Control subsystem — the project and task management layer that tracks long-horizon AI work. It validates the full project lifecycle through both the `MissionControlManager` directly and the FastAPI REST endpoints, including state transitions (draft, approved, paused, resumed) and proper 404 handling for missing resources.

### `project structure` — 11 article(s)

- [Connectors Test Package Initializer](./connectors-test-package-initializer.md) — This `__init__.py` marks `tests/connectors/` as a Python package and documents its scope as the test home for `pocketpaw.connectors.*` adapter implementations. Its single comment line serves as a breadcrumb for developers adding new connector tests.
- [Enterprise Edition Test Package Initializer](./enterprise-edition-test-package-initializer.md) — This `__init__.py` marks `tests/ee/` as a Python package for pytest import resolution and documents in its header comment that it was introduced with the `feat/fleet-journal-emission` feature branch to enable fixture sharing across enterprise edition tests.
- [Files Providers Test Sub-Package Marker](./files-providers-test-sub-package-marker.md) — An empty __init__.py that makes tests/cloud/files/providers/ a Python package, enabling dotted imports from parent test modules such as tests.cloud.files.test_provider_contract. Identical in purpose and content to the sibling files test package marker.
- [Files Test Package Marker](./files-test-package-marker.md) — This is an empty __init__.py that marks tests/cloud/files/ as a Python package so pytest can discover and import tests within it. The SHA-256 hash e3b0c44298fc... is the well-known hash of an empty file.
- [Notifications Test Package Marker](./notifications-test-package-marker.md) — An empty `__init__.py` that marks `tests/cloud/notifications/` as a Python package for stable pytest discovery. No logic or fixtures live here; all test content is in the sibling test modules test_service.py and test_derived.py.
- [Realtime Test Package Marker](./realtime-test-package-marker.md) — An empty `__init__.py` that designates `tests/cloud/realtime/` as a Python package for stable pytest discovery and import resolution. No logic or fixtures live here; all test content is in the sibling test modules.
- [Sessions Test Package Initializer](./sessions-test-package-initializer.md) — This is the empty `__init__.py` marker file for the `tests/cloud/sessions` test package. Its sole purpose is to make Python treat the directory as a package so pytest can discover and import test modules within it.
- [Shared Cloud Tests Package Initializer](./shared-cloud-tests-package-initializer.md) — This is the empty `__init__.py` marker for the `tests/cloud/shared` test package. It enables Python's import system to treat the directory as a package, ensuring pytest can correctly discover and namespace test modules in this subdirectory.
- [Upload Tests Package Marker: Making the Uploads Test Directory a Python Package](./upload-tests-package-marker-making-the-uploads-test-directory-a-python-package.md) — An empty `__init__.py` that marks `tests/cloud/uploads/` as a Python package so pytest can discover and import test modules within it using absolute import paths. No logic is present; its presence is structural.
- [Uploads Test Package Initializer](./uploads-test-package-initializer.md) — This is an empty `__init__.py` that marks the `tests/uploads/` directory as a Python package, enabling pytest to discover and import tests within it using standard package-relative imports. No logic lives here; its presence is structural.
- [Workspace Cloud Test Package Initializer](./workspace-cloud-test-package-initializer.md) — This empty `__init__.py` marks the `tests/cloud/workspace/` directory as a Python package, enabling pytest to discover and import test modules within it using standard package import resolution. No code lives here; the file's presence is its entire purpose.

### `project-management` — 1 article(s)

- [Mission Control Manager: High-Level Orchestration Layer](./mission-control-manager-high-level-orchestration-layer.md) — MissionControlManager sits above the raw file store and combines storage operations with business logic: creating tasks with automatic activity logging, resolving @mention notifications, managing agent heartbeats, and orchestrating project lifecycle including on-disk directory creation.

### `project-scanning` — 1 article(s)

- [Paw Project Scanner: Heuristic Project Knowledge Ingestion](./paw-project-scanner-heuristic-project-knowledge-ingestion.md) — scan.py bootstraps a soul's project knowledge by reading common configuration files (README, pyproject.toml, package.json, Cargo.toml, go.mod, .env.example) and storing facts via soul.remember(). A full agent-powered scan is defined but currently delegates to the heuristic fallback pending AgentRouter wiring.

### `projection` — 3 article(s)

- [FabricProjection — In-Memory Journal Projection for Fabric Objects](./fabricprojection-in-memory-journal-projection-for-fabric-objects.md) — `FabricProjection` replays `fabric.object.*` journal events into an in-memory dictionary to maintain a current-state view of all Fabric objects, applying scope filters before computing query totals to permanently fix the pagination leak that blocked PR #938. It is a deliberately minimal CQRS read model: rebuild from the journal on startup, apply events incrementally, serve scoped queries.
- [Retrieval Journal Projection Tests: Write Path, Scope Filtering, Graduation Policy, and REST Router](./retrieval-journal-projection-tests-write-path-scope-filtering-graduation-policy-.md) — Comprehensive tests for the `ee.retrieval` subsystem that journals every memory-retrieval query and drives a graduation policy promoting frequently-retrieved memories to higher tiers. Covers event write-path fidelity, scope containment, correlation grouping, threshold-based graduation decisions, projection rebuild equivalence, and the REST router's GET endpoints.
- [Widget Journal Projection Tests: Interactions, Graduation, Co-occurrence Signature Fix, and Router](./widget-journal-projection-tests-interactions-graduation-co-occurrence-signature-.md) — Comprehensive tests for the widget journal projection subsystem covering the write path for interactions, graduations, and co-occurrence events; scope containment in usage roll-ups; the pin-threshold graduation policy; a regression guard for the `sorted(tokens[:6])` co-occurrence signature bug from PR #942; archive rules for inactive widgets; and the three GET router endpoints.

### `projection-pattern` — 2 article(s)

- [In-Memory Projection for Retrieval and Graduation Events](./in-memory-projection-for-retrieval-and-graduation-events.md) — Provides the read-side of the retrieval domain by replaying the org journal into two in-memory views: a recent-retrievals list (filterable by scope, actor, and pocket) and a per-memory graduation state table. A single replay pass builds both views because they share the same event stream.
- [In-Memory Projection for Widget Interactions, Usage, and Co-occurrence](./in-memory-projection-for-widget-interactions-usage-and-co-occurrence.md) — Provides three unified read views over the widget event stream — per-interaction history, per-widget usage roll-up for graduation decisions, and per-pair co-occurrence counts for suggestion candidates — all rebuilt from a single org journal replay pass. Supersedes the JSONL-based scans from PRs #941 and #942.

### `prompt engineering` — 2 article(s)

- [GWS CLI System Prompt Injection Tests](./gws-cli-system-prompt-injection-tests.md) — This test suite verifies that the AgentContextBuilder conditionally injects Google Workspace CLI guidance into the system prompt only when the google-workspace MCP server is active and enabled. It also confirms that the gws.md guidance file exists on disk with the required content.
- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.

### `prompt injection` — 2 article(s)

- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.
- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.

### `prompts` — 1 article(s)

- [Deep Work Prompt Templates — LLM Instruction Set for Planning Pipeline](./deep-work-prompt-templates-llm-instruction-set-for-planning-pipeline.md) — This module centralizes all LLM prompt templates used by the Deep Work planning pipeline as module-level string constants. Separating prompts from logic enables iteration on prompt text without touching orchestration code, and makes the planning pipeline's LLM behavior fully visible in one file.

### `protocol design` — 1 article(s)

- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.

### `protocol-contracts` — 1 article(s)

- [Bootstrap Protocol — BootstrapContext and BootstrapProviderProtocol Contracts](./bootstrap-protocol-bootstrapcontext-and-bootstrapproviderprotocol-contracts.md) — This module defines the two core contracts of the bootstrap subsystem: BootstrapContext, a dataclass holding all fields that compose an agent's system prompt, and BootstrapProviderProtocol, a structural Protocol that any identity source must implement. The system prompt layout — instructions first, identity block last — is a deliberate positioning strategy to keep the model anchored on persona during long conversations.

### `protocols` — 3 article(s)

- [Agent Protocol: Core Event Types and Legacy Interface](./agent-protocol-core-event-types-and-legacy-interface.md) — Defines the standardized `AgentEvent` dataclass that all agent backends emit, and the `AgentProtocol` structural typing interface that existed before the more capable `AgentBackend` ABC replaced it. Together they form the lingua franca for streaming agent output across PocketPaw's multi-backend architecture.
- [AgentBackend Protocol — Unified Interface for All Agent SDK Adapters](./agentbackend-protocol-unified-interface-for-all-agent-sdk-adapters.md) — Defines the structural `Protocol` and supporting types that every agent backend (Claude SDK, OpenAI Agents, Codex CLI, Google ADK, etc.) must satisfy. The contract enforces a consistent streaming `run()` generator and a static `info()` method so the router and loop can treat all backends polymorphically.
- [StorageAdapter Protocol: The Backend-Agnostic Interface for File Persistence](./storageadapter-protocol-the-backend-agnostic-interface-for-file-persistence.md) — The `StorageAdapter` protocol defines the minimal async interface that all storage backends (local disk, S3, GCS) must satisfy, keeping the upload pipeline decoupled from any specific storage technology. The companion `StoredObject` dataclass represents the canonical result of a successful write operation.

### `provider abstraction` — 1 article(s)

- [Files Test Fixtures: FakeProvider, ctx, make_entry, make_mount](./files-test-fixtures-fakeprovider-ctx-makeentry-makemount.md) — This conftest.py provides the shared test infrastructure for the files subsystem: a FakeProvider that implements the full provider protocol in memory, plus factory fixtures for RequestContext, FileEntry, and ResolvedMount. All file provider tests build on these primitives.

### `provider adapter` — 7 article(s)

- [AnthropicAdapter: Native Anthropic API Configuration and Client Construction](./anthropicadapter-native-anthropic-api-configuration-and-client-construction.md) — `AnthropicAdapter` implements the `ProviderAdapter` protocol for Anthropic's native API, resolving model and API key from settings and constructing `AsyncAnthropic` clients. It explicitly raises `NotImplementedError` on `build_openai_client()` to enforce that Anthropic-native callers never accidentally get an OpenAI-compatible client.
- [GeminiAdapter: Google AI via OpenAI-Compatible Endpoint](./geminiadapter-google-ai-via-openai-compatible-endpoint.md) — `GeminiAdapter` routes Gemini API calls through Google's OpenAI-compatible endpoint, allowing PocketPaw to use the same `AsyncOpenAI` and `AsyncAnthropic` client patterns it uses for other providers. Both client builders point to `GEMINI_BASE_URL` and use `"not-needed"` as a placeholder API key fallback to satisfy SDK validation without passing a real credential.
- [LiteLLMAdapter: Proxy and Direct SDK Modes for Universal LLM Routing](./litellmadapter-proxy-and-direct-sdk-modes-for-universal-llm-routing.md) — `LiteLLMAdapter` supports two integration modes — a proxy mode that routes through a running LiteLLM server via OpenAI-compatible HTTP, and a direct SDK mode that uses LiteLLM's native Python wrappers when available. This dual-mode design lets PocketPaw work with any model LiteLLM supports without requiring each deployment to run a proxy server.
- [OllamaAdapter: Local LLM Integration via OpenAI-Compatible API](./ollamaadapter-local-llm-integration-via-openai-compatible-api.md) — `OllamaAdapter` connects PocketPaw to locally running Ollama instances, using `"ollama"` as a placeholder API key to satisfy client SDK validation while pointing both OpenAI and Anthropic client builders at Ollama's OpenAI-compatible HTTP endpoint. Error messages distinguish between model-not-found and service-not-running failures to give users actionable guidance.
- [OpenAICompatibleAdapter: Generic OpenAI-Compatible Endpoint Support](./openaicompatibleadapter-generic-openai-compatible-endpoint-support.md) — `OpenAICompatibleAdapter` provides a catch-all provider adapter for any service that exposes an OpenAI-compatible REST API — vLLM, LocalAI, Together AI, Anyscale, and similar endpoints. It reads `openai_compatible_base_url` and `openai_compatible_api_key` from settings and constructs both `AsyncOpenAI` and `AsyncAnthropic` clients pointing at that endpoint.
- [OpenRouterAdapter: Multi-Model Routing with Anthropic-Compat Authentication Quirks](./openrouteradapter-multi-model-routing-with-anthropic-compat-authentication-quirk.md) — `OpenRouterAdapter` integrates OpenRouter's model routing service, which provides access to 100+ models via a single API key. Its most distinctive behavior is an intentional `ANTHROPIC_API_KEY=""` (empty string) workaround: OpenRouter authenticates via `ANTHROPIC_AUTH_TOKEN`, and if `ANTHROPIC_API_KEY` is set to a real value, the Anthropic SDK sends it as `Bearer` and OpenRouter rejects the request.
- [Provider Adapter Base Types: ProviderConfig, ProviderAdapter Protocol, and Model Resolution](./provider-adapter-base-types-providerconfig-provideradapter-protocol-and-model-re.md) — `base.py` defines the shared vocabulary for the entire provider adapter system: `ProviderConfig` (the resolved connection config), `ProviderAdapter` (the structural Protocol every adapter implements), and `resolve_model()` (the multi-tier model name fallback chain). These types are the contract between the LLM client layer and individual provider implementations.

### `provider management` — 3 article(s)

- [LLM Provider Adapters Package: Public Interface for get_adapter](./llm-provider-adapters-package-public-interface-for-getadapter.md) — The `pocketpaw.llm.providers` package re-exports `ProviderAdapter`, `ProviderConfig`, `get_adapter`, and `resolve_model` as its public API, giving callers a single import point for the provider adapter system. The init file documents the canonical usage pattern so it doubles as inline API documentation.
- [LLMClient: Centralized Provider Detection, Client Creation, and Environment Setup](./llmclient-centralized-provider-detection-client-creation-and-environment-setup.md) — `LLMClient` is an immutable descriptor that captures a fully resolved LLM provider configuration and exposes methods to create provider-specific async clients. The companion `resolve_backend_env()` function pushes the correct environment variables for the active backend, eliminating manual env-var wrangling when switching providers.
- [Provider Adapter Registry: Centralized Adapter Lookup with Descriptive Errors](./provider-adapter-registry-centralized-adapter-lookup-with-descriptive-errors.md) — The provider registry is a module-level dict mapping provider name strings to singleton adapter instances, with a `get_adapter()` function that raises `KeyError` with the list of available providers on unknown names. Pre-instantiating adapters as singletons avoids repeated construction overhead and provides a single point of truth for which providers PocketPaw supports.

### `provider-abstraction` — 1 article(s)

- [WebSearchTool: Provider-Agnostic Web Search with Tavily, Brave, and Parallel AI](./websearchtool-provider-agnostic-web-search-with-tavily-brave-and-parallel-ai.md) — `WebSearchTool` routes web search queries to Tavily, Brave Search, or Parallel AI based on a settings-configured provider, with all three sharing a common `_format_results()` output shape. The provider selection is declarative — callers never specify the backend — keeping tool descriptions stable across deployment configurations.

### `providers` — 4 article(s)

- [BaseFolderProvider: Default Provider Stub Implementation](./basefolderprovider-default-provider-stub-implementation.md) — Defines `BaseFolderProvider`, the default base class for all file providers, where every operation raises `ProviderUnsupported` unless overridden. This design enforces capability-based provider contracts: concrete providers only implement the operations they actually support, and the error type communicates unsupported operations cleanly to the API layer.
- [Built-in File Providers Package Initialiser](./built-in-file-providers-package-initialiser.md) — The package initialiser for the built-in `FolderProvider` implementations under `ee.cloud.files.providers`. This file acts as a namespace anchor, grouping the concrete provider modules (`base`, `kb`, `uploads`) under a single importable package without adding its own logic.
- [KbProvider: Knowledge Base File Provider](./kbprovider-knowledge-base-file-provider.md) — Exposes workspace Knowledge Base documents as a browseable file mount in the unified files tree. `KbProvider` delegates reads to a `_KbService` Protocol adapter rather than directly calling the KB FastAPI routes, decoupling the files subsystem from the KB module's current route-and-Go-binary architecture.
- [UploadsProvider: Personal Upload File Provider with Folder Support](./uploadsprovider-personal-upload-file-provider-with-folder-support.md) — Wraps `MongoFileStore` to expose user-uploaded files and folders as a personal 'My Files' mount in the unified files tree. As the only provider with folder support, it uses a dedicated `folder_store` and renders directory entries with `mime = application/x-directory`.

### `proxy pattern` — 1 article(s)

- [LiteLLMAdapter: Proxy and Direct SDK Modes for Universal LLM Routing](./litellmadapter-proxy-and-direct-sdk-modes-for-universal-llm-routing.md) — `LiteLLMAdapter` supports two integration modes — a proxy mode that routes through a running LiteLLM server via OpenAI-compatible HTTP, and a direct SDK mode that uses LiteLLM's native Python wrappers when available. This dual-mode design lets PocketPaw work with any model LiteLLM supports without requiring each deployment to run a proxy server.

### `pub-sub` — 2 article(s)

- [Message Bus: Central Routing Hub for All Channel Communication](./message-bus-central-routing-hub-for-all-channel-communication.md) — MessageBus is the central publish-subscribe hub in PocketPaw that decouples channel adapters from the agent loop. Adapters publish inbound messages to a bounded asyncio queue; the agent loop consumes them. Outbound messages are fanned out to registered adapter callbacks. System events flow separately to dashboard and monitoring subscribers.
- [Message Bus Tests: Inbound/Outbound Pub-Sub, Multi-Subscriber Fan-out, and Channel Routing](./message-bus-tests-inboundoutbound-pub-sub-multi-subscriber-fan-out-and-channel-r.md) — This test suite validates PocketPaw's internal message bus, which decouples channel adapters (Telegram, Discord, WhatsApp, CLI) from the core agent logic using an async publish-subscribe queue. Tests cover inbound message queueing, outbound pub-sub subscription and delivery, multi-subscriber fan-out, unsubscribe, adapter integration, broadcast to all channels, and per-channel filtering.

### `pytest` — 1 article(s)

- [Uploads Test Conftest: Isolated Temporary Upload Root Fixture](./uploads-test-conftest-isolated-temporary-upload-root-fixture.md) — This conftest defines `tmp_upload_root`, a pytest fixture that provides an isolated temporary directory for each test in the uploads test suite. It prevents test pollution between file system tests by ensuring every test starts with a clean, empty upload root.

### `pytest configuration` — 1 article(s)

- [Root Pytest Configuration: SSRF Relaxation, Scope Bypass, and Audit Log Isolation](./root-pytest-configuration-ssrf-relaxation-scope-bypass-and-audit-log-isolation.md) — The root `tests/conftest.py` installs three session-wide fixtures that configure PocketPaw's test environment: it relaxes SSRF URL validation so internal URLs work during tests, bypasses scope enforcement for non-security tests, and redirects the audit log to a temp file so tests never write to the developer's home directory.

### `python-patterns` — 1 article(s)

- [Builtin Tools Lazy Loader: Optional Dependency Safety via __getattr__](./builtin-tools-lazy-loader-optional-dependency-safety-via-getattr.md) — The `pocketpaw/tools/builtin/__init__.py` implements a lazy import scheme using Python's module-level `__getattr__` hook, ensuring that importing one built-in tool never fails due to a missing optional dependency of a different tool. This lets PocketPaw operate with a minimal install footprint while still exposing a rich tool catalogue.

### `quality assurance` — 1 article(s)

- [Knowledge Linter: LLM-Powered Wiki Health Auditor](./knowledge-linter-llm-powered-wiki-health-auditor.md) — The knowledge linter uses an LLM to audit the wiki for inconsistencies, coverage gaps, missing cross-links, stale content, and uncompiled raw documents. It produces structured `LintIssue` objects with severity levels and actionable suggestions, functioning as an automated knowledge quality reviewer.

### `rate limiting` — 2 article(s)

- [Paw Print Widget Backend: Model Validation, CRUD, and Rate Limiting Tests](./paw-print-widget-backend-model-validation-crud-and-rate-limiting-tests.md) — This test suite covers the backend layer of PocketPaw's Paw Print embeddable widget system — validating Pydantic model constraints (block caps, domain normalization, rate limits, token format), SQLite-backed CRUD operations, event persistence, and the dual rate-limit primitives (overall and per-customer) that protect the public ingest endpoint.
- [Rate Limiter, Audit API Events, and OpenAPI Config Tests](./rate-limiter-audit-api-events-and-openapi-config-tests.md) — Tests PocketPaw's token-bucket rate limiter, the `RateLimitInfo` response model and its header formatting, audit logging for API events (OAuth and key revocation), and the OpenAPI endpoint configuration. Together these tests ensure the API protects against request floods, produces correct rate limit headers, and maintains a searchable audit trail.

### `rate-limiting` — 1 article(s)

- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.

### `real-time` — 7 article(s)

- [AudienceResolver Behavioral Tests: Fan-Out Logic, Caching, and Edge Cases](./audienceresolver-behavioral-tests-fan-out-logic-caching-and-edge-cases.md) — These tests verify the complete behavioral contract of AudienceResolver — determining which user IDs receive each event type, including removed-member inclusion, invite routing to admins plus invitee, TTL caching with manual invalidation, session deduplication, and empty-list fallback for unknown event types.
- [AudienceResolver Exhaustive Coverage: All Event Subclasses Must Resolve and Have Unique Wire Types](./audienceresolver-exhaustive-coverage-all-event-subclasses-must-resolve-and-have-.md) — These tests use Python's inspect module to enumerate every Event subclass at runtime and verify AudienceResolver handles each without raising. A companion test enforces that no two event classes share the same EVENT_TYPE wire string, preventing silent routing collisions when new event types are added.
- [emit() Facade Tests: Bus Delegation, Error Swallowing, and Initialization Guard](./emit-facade-tests-bus-delegation-error-swallowing-and-initialization-guard.md) — These tests verify the `emit()` top-level facade that service code calls to publish real-time events. The suite ensures emit delegates unchanged event objects to the active bus, swallows bus errors to protect callers from infrastructure failures, and raises AssertionError when called before the bus singleton is initialized.
- [Event Model Structural Tests: Base Event Fields and Typed Subclass Wire Type Assignment](./event-model-structural-tests-base-event-fields-and-typed-subclass-wire-type-assi.md) — Two minimal tests verify the structural contract of the Event base model: that every Event has type, data, and ts fields; and that typed subclasses like GroupCreated automatically set the correct wire type string from EVENT_TYPE without the caller needing to pass it.
- [InProcessBus Tests: Fan-Out Correctness, Error Isolation, and Module Singleton](./inprocessbus-tests-fan-out-correctness-error-isolation-and-module-singleton.md) — These tests verify InProcessBus — the in-process real-time event bus — covering correct fan-out to all resolved recipients, the WsOutbound payload shape, per-recipient exception isolation so one dead socket does not stop others, graceful audience resolution error swallowing, and the module-level singleton get/set contract.
- [NotificationService Unit Tests: CRUD, Fan-Out, and Idempotency Contract](./notificationservice-unit-tests-crud-fan-out-and-idempotency-contract.md) — These unit tests exercise NotificationService in isolation by patching the Notification Beanie document and the emit function, verifying that create persists then emits, list_for_user filters at the database layer, mark_read is idempotent for already-read notifications, and clear_all uses a bulk update and emits a single event.
- [Pocket Journal SSE Stream Router Tests: Filtering, Cursors, and Frame Contract](./pocket-journal-sse-stream-router-tests-filtering-cursors-and-frame-contract.md) — Tests the Server-Sent Events (SSE) endpoint that streams journal events for a specific pocket to the RippleGraphWidget frontend. Covers the event-matching predicate, backlog replay with `since_seq` cursor support for reconnects, the `connected` handshake frame, idle-poll timeout, and strict isolation so events from other pockets never appear in a pocket's stream.

### `real-time chat` — 1 article(s)

- [WebSocket Connection Manager Tests: Multi-Device, Typing Indicators, and Offline Grace Periods](./websocket-connection-manager-tests-multi-device-typing-indicators-and-offline-gr.md) — Tests for `ee.cloud.chat.ws.ConnectionManager`, the in-process registry that tracks active WebSocket connections per user, routes outbound messages, and manages typing indicators with auto-expiry timers. Key behavioral contracts include dead-connection cleanup, multi-device fan-out, idempotent stop-typing, and cancellation of offline grace period tasks on reconnect.

### `real-time events` — 1 article(s)

- [EE Upload Service: Workspace-Scoped Pipeline over the OSS Upload Engine](./ee-upload-service-workspace-scoped-pipeline-over-the-oss-upload-engine.md) — EEUploadService wraps the OSS UploadService to add workspace scoping, per-file access control, Mongo metadata persistence, and real-time WebSocket events for file lifecycle. It substitutes a null metadata store at the OSS layer so that all persistence flows through MongoDB rather than the JSONL flat file.

### `real-time messaging` — 1 article(s)

- [Agent Bridge: Routing Chat Messages to the Agent Pool](./agent-bridge-routing-chat-messages-to-the-agent-pool.md) — The agent bridge connects incoming group-chat `message.sent` events to the PocketPaw agent pool, evaluating each agent's respond mode and streaming responses back over WebSocket. It decouples the chat send path from agent processing by dispatching all work to background tasks, preventing smart-mode LLM calls from blocking message delivery.

### `real-time streaming` — 2 article(s)

- [Chat API Test Suite: SSE Streaming, Session Bridge, and Request Aliases](./chat-api-test-suite-sse-streaming-session-bridge-and-request-aliases.md) — Covers the full test surface of PocketPaw's `/api/v1/chat` router, including the `_APISessionBridge` async queue, SSE stream/stop endpoints, non-streaming send, media URL rewriting, and camelCase field alias handling. These tests exist to catch silent regressions in the real-time chat path that would break frontend consumers without an obvious error.
- [Events SSE Router Tests: Endpoint Registration and OpenAPI Spec Inclusion](./events-sse-router-tests-endpoint-registration-and-openapi-spec-inclusion.md) — Tests that PocketPaw's server-sent events (SSE) endpoint `/api/v1/events/stream` is correctly registered in the events router, present in the v1 router registry, accessible on the dashboard app, and included in the OpenAPI specification. These structural checks prevent the events channel from being silently dropped during refactors.

### `realtime` — 23 article(s)

- [Agent Bridge Event Routing and Wire Type Tests](./agent-bridge-event-routing-and-wire-type-tests.md) — This module verifies that `agent_bridge` routes all group broadcasts through `emit()` rather than directly calling `ws_manager.broadcast_to_group`, and that the four agent stream event types (`AgentStreamStart`, `AgentStreamChunk`, `AgentToolUse`, `AgentStreamEnd`) are correctly defined and importable from the bridge.
- [AudienceResolver: Event-to-Recipient Mapping with Short-TTL Caching](./audienceresolver-event-to-recipient-mapping-with-short-ttl-caching.md) — AudienceResolver translates a typed event into the list of user IDs that should receive it over WebSocket, applying domain-specific fan-out rules for groups, messages, workspace membership, sessions, agent streams, notifications, and presence. Member lookups are cached with a configurable short TTL to avoid hammering the database on high-frequency events, and the cache can be invalidated selectively when membership changes.
- [Chat Router - REST Endpoints and WebSocket Handler](./chat-router-rest-endpoints-and-websocket-handler.md) — The chat router exposes the full `/chat` REST surface and the `/ws/cloud` WebSocket endpoint, both gated behind an enterprise license check. It manages presence lifecycle with a 30-second grace period and dispatches inbound WebSocket messages to the appropriate service methods.
- [Cloud Tests Shared Fixture: No-Op Realtime Bus](./cloud-tests-shared-fixture-no-op-realtime-bus.md) — This conftest.py installs an autouse fixture that replaces the realtime event bus with a no-op AsyncMock for the duration of every cloud test. This prevents AssertionError crashes when services call emit() in tests that do not start the full application lifecycle.
- [Enterprise Cloud Application Bootstrap and Router Mounting](./enterprise-cloud-application-bootstrap-and-router-mounting.md) — `ee/cloud/__init__.py` is the application assembly point for the PocketPaw Enterprise REST API. It provides two entry points: `init_realtime()` which wires up the in-process EventBus, and `mount_cloud()` which registers every domain router, the error handler, static file mounts, and lifecycle hooks onto a FastAPI app instance.
- [EventBus Protocol and In-Process Fan-Out Implementation](./eventbus-protocol-and-in-process-fan-out-implementation.md) — This module defines the EventBus protocol and the InProcessBus implementation that fans out realtime events to WebSocket connections on the same process, routing through AudienceResolver to determine recipients and ConnectionManager to deliver payloads. Module-level singletons with explicit initialisation guards make it safe to wire up during app startup while crashing loudly if accessed before initialisation.
- [File Mutation Domain Events](./file-mutation-domain-events.md) — Declares the Pydantic event shapes published to the `ee.cloud.realtime` bus whenever a file is added, updated, removed, or moved. These schemas form the Phase 1-2 contract between the files subsystem and future realtime subscribers, ensuring the event bus wire format is stable before the subscriber (Phase 4) is built.
- [Group Service - Channel and DM Business Logic](./group-service-channel-and-dm-business-logic.md) — The `GroupService` class encapsulates all group and channel CRUD, membership management, agent attachment, and DM creation logic for the chat domain. It coordinates between the Group ODM model, the realtime event bus, and the RBAC/audit guard layer.
- [GroupService Realtime Emit Tests](./groupservice-realtime-emit-tests.md) — This test module verifies that every public mutation in GroupService fires the correct realtime event via the bus emit() call. It also confirms that membership-changing operations correctly invalidate the AudienceResolver group cache so downstream subscribers see fresh data.
- [Message Service - Chat Message CRUD, Reactions, Threads, and Search](./message-service-chat-message-crud-reactions-threads-and-search.md) — The `MessageService` class handles all message lifecycle operations in the chat domain, including sending, editing, deletion, reactions, thread replies, pin management, mention fan-out, and full-text search. It was split from the original `service.py` monolith and extended with agent message creation and workspace-scoped search.
- [MessageService Realtime Emit Tests](./messageservice-realtime-emit-tests.md) — This test module enforces that every public mutation in MessageService fires the correct typed realtime event through emit(). It also includes regression guards ensuring the WebSocket router itself does not perform direct broadcasts that would bypass the event bus.
- [Notification Service: CRUD and Realtime Fan-Out](./notification-service-crud-and-realtime-fan-out.md) — The `NotificationService` is a stateless class providing four async operations — create, list, mark-read, and clear-all — each of which writes to MongoDB via Beanie and immediately emits a corresponding realtime event for connected WebSocket clients. The service is the single point of truth for notification lifecycle, keeping routers and other domain services free of MongoDB and emit logic.
- [Pocket Journal SSE Stream for RippleGraphWidget](./pocket-journal-sse-stream-for-ripplegraphwidget.md) — The journal stream router serves a live Server-Sent Events feed of soul-protocol journal entries filtered to a single pocket, enabling the frontend RippleGraphWidget to render agent decisions, retrievals, and tool traces as a live causation graph. It supports resumable cursors, cold-start backlog replay, and keepalive heartbeats to survive proxy timeouts.
- [Presence Broadcast and Grace Window Tests](./presence-broadcast-and-grace-window-tests.md) — This test module verifies the WebSocket endpoint's presence online/offline lifecycle, specifically the grace window that delays broadcasting a user offline until reconnection is no longer possible. Tests drive _schedule_presence_offline directly and use time-acceleration to keep the suite fast.
- [Realtime Bus Initialization and Group Member Resolution Tests](./realtime-bus-initialization-and-group-member-resolution-tests.md) — This test module verifies the wiring of `init_realtime`, ensuring the correct bus backend is selected by default and that the `AudienceResolver` is properly exposed. It also covers `GroupService.list_member_ids` behavior for found and missing groups.
- [Realtime emit() Facade: Fire-and-Forget Event Dispatch After Mutations](./realtime-emit-facade-fire-and-forget-event-dispatch-after-mutations.md) — The `emit` function is a thin, fire-and-forget facade that service methods call after successful database writes to push realtime events to connected clients. It never raises exceptions back to the caller — a delivery failure must not roll back or abort the preceding database mutation — while a missing bus initialisation surfaces immediately as an AssertionError.
- [Realtime Event Type Catalogue: Typed Dataclasses for All Domain Events](./realtime-event-type-catalogue-typed-dataclasses-for-all-domain-events.md) — This module defines the complete catalogue of realtime events as Python dataclasses, covering workspace lifecycle, group and message operations, presence, typing, file uploads, sessions, agent streaming, and notifications. Each concrete event subclass declares a `ClassVar EVENT_TYPE` string that is automatically applied in `__post_init__`, ensuring the `type` field is always set correctly without manual assignment at every call site.
- [Realtime WebSocket Event Package Entry Point](./realtime-websocket-event-package-entry-point.md) — This is the package initialiser for the `ee.cloud.realtime` module, which houses the WebSocket-based realtime event system for the EE cloud tier. The file itself carries only a module docstring, serving as the namespace anchor that makes `ee.cloud.realtime` importable as a package while the substantive components live in sub-modules.
- [SessionService Realtime Event Emission Tests](./sessionservice-realtime-event-emission-tests.md) — This module tests that every mutating `SessionService` method — create, create_for_pocket, update, delete, and touch — fires the correct realtime event through `emit()` after the database commit. Tests are unit-scoped, patching DB and bus primitives at seam boundaries to isolate emit behavior from persistence.
- [SessionService: Session Lifecycle, History, and Realtime Event Integration](./sessionservice-session-lifecycle-history-and-realtime-event-integration.md) — SessionService is the stateless service class handling the full lifecycle of chat sessions — creating with upsert support for runtime sessions, listing by agent or pocket, retrieving message history from MongoDB, soft-deleting, and emitting realtime events after each mutation. A touch method provides a lightweight activity heartbeat that increments message count and updates lastActivity.
- [SSE System Event Schema](./sse-system-event-schema.md) — Defines the Pydantic model for system events delivered to clients over Server-Sent Events (SSE). This thin schema ensures every event has a typed identity and an optional payload, keeping the SSE stream contract explicit and validated.
- [WebSocket Connection Manager - Presence, Rooms, and Typing Indicators](./websocket-connection-manager-presence-rooms-and-typing-indicators.md) — The `ConnectionManager` class maintains the in-memory registry of active WebSocket connections, manages presence state with a 30-second grace window, routes messages to group rooms, and expires typing indicators automatically after 5 seconds. It is the real-time backbone for the chat domain.
- [Workspace Service — Business Logic for Workspaces, Members, and Invites](./workspace-service-business-logic-for-workspaces-members-and-invites.md) — The `WorkspaceService` class is a stateless async service that encapsulates all business logic for the workspace domain, including workspace lifecycle, member management, invite flow, and realtime event emission. It enforces several data-level invariants (slug uniqueness, seat limits, owner demotion guard) that cannot be expressed as route-level permission checks.

### `realtime events` — 2 article(s)

- [EEUploadService Realtime Event Emission: FileReady and FileDeleted Tests](./eeuploadservice-realtime-event-emission-fileready-and-filedeleted-tests.md) — This suite verifies that `EEUploadService` fires `FileReady` events over the realtime bus when files are uploaded to a chat-scoped context, and `FileDeleted` events when those files are removed. It also verifies that neither event fires when the upload or file lacks a `chat_id`, preventing noise from workspace-level background operations.
- [WorkspaceService Realtime Event Emission and Cache Invalidation Tests](./workspaceservice-realtime-event-emission-and-cache-invalidation-tests.md) — This suite verifies that every mutating `WorkspaceService` method fires the correct typed event through `emit()`, and that membership or admin list changes also invalidate the `AudienceResolver` workspace cache. All database and permission primitives are patched at their seams, isolating the emit behavior from persistence concerns.

### `refactoring` — 1 article(s)

- [Chat Service - Backward-Compatibility Re-Export Facade](./chat-service-backward-compatibility-re-export-facade.md) — `service.py` is a thin facade that re-exports `GroupService`, `MessageService`, and their helper functions from their new dedicated modules, preserving import compatibility for all code written before the refactor. It carries no logic of its own.

### `registry` — 4 article(s)

- [Agent Backend Registry: Lazy Import, Legacy Name Resolution, and Plugin Registration Tests](./agent-backend-registry-lazy-import-legacy-name-resolution-and-plugin-registratio.md) — This test suite covers PocketPaw's backend registry, which provides lazy-loaded access to agent backends (Claude SDK, OpenAI Agents, Google ADK, OpenCode) while maintaining backward compatibility through a legacy name mapping. Tests verify that deprecated names like `gemini_cli` transparently resolve to `google_adk`, that unknown backends return `None` rather than raising, and that third-party plugins can register themselves at runtime.
- [Backend Registry: Lazy Discovery and Plugin Support for Agent Backends](./backend-registry-lazy-discovery-and-plugin-support-for-agent-backends.md) — The backend registry maps short backend names to `(module_path, class_name)` pairs and imports them on demand, so PocketPaw starts cleanly even when optional SDKs like `openai-agents` or `google-adk` are not installed. It also handles legacy backend name migration and exposes a plugin registration API.
- [ConnectorRegistry: Dynamic Connector Discovery for YAML, SQL, MongoDB, Firebase, and GCP Adapters](./connectorregistry-dynamic-connector-discovery-for-yaml-sql-mongodb-firebase-and-.md) — `ConnectorRegistry` scans a directory for YAML connector definitions and also hard-registers native Python adapters (SQL databases, MongoDB, Firebase, GCP) that cannot be expressed as simple REST YAML. It manages per-pocket adapter instances and provides a `reload()` method for hot configuration changes.
- [Provider Registry and Mount Path Router](./provider-registry-and-mount-path-router.md) — Implements `ProviderRegistry`, which maps mount path prefixes to registered `FolderProvider` instances and resolves incoming file paths to the correct provider via longest-prefix matching. The `FolderProvider` Protocol definition here is the canonical duck-typed interface contract all providers must satisfy.

### `registry pattern` — 1 article(s)

- [Provider Adapter Registry: Centralized Adapter Lookup with Descriptive Errors](./provider-adapter-registry-centralized-adapter-lookup-with-descriptive-errors.md) — The provider registry is a module-level dict mapping provider name strings to singleton adapter instances, with a `get_adapter()` function that raises `KeyError` with the list of available providers on unknown names. Pre-instantiating adapters as singletons avoids repeated construction overhead and provides a single point of truth for which providers PocketPaw supports.

### `regression` — 3 article(s)

- [Agent Bridge Attachment Forwarding Regression Test](./agent-bridge-attachment-forwarding-regression-test.md) — This regression test guards against the silent attachment-drop bug in `_run_agent_response`, where file attachments sent by users in channel messages were never forwarded into the agent's prompt. The test confirms that the channel code path formats attachment metadata — filename, MIME type, and size — into the prompt in the same shape used by the DM path.
- [Dashboard Starlette Compatibility Regression Test: Index Route on Starlette 1.0.0](./dashboard-starlette-compatibility-regression-test-index-route-on-starlette-100.md) — This short regression test ensures the dashboard's root route returns HTTP 200 with `text/html` content, specifically guarding against a breaking change introduced in Starlette 1.0.0 that changed the `TemplateResponse` constructor signature. The Starlette version is logged in assertion failure messages to make version-related CI failures immediately diagnosable.
- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.

### `release management` — 1 article(s)

- [Update Check System: Non-Blocking PyPI Version Checks with Release Notes and Styled CLI Notices](./update-check-system-non-blocking-pypi-version-checks-with-release-notes-and-styl.md) — The update check module queries PyPI once per 24 hours to determine whether a newer version of PocketPaw is available, caches the result to a local file, and prints a styled terminal box on TTY sessions when an update is found. It additionally fetches per-version release notes from GitHub with a separate 1-hour cache and tracks which version the user has last acknowledged.

### `reminders` — 3 article(s)

- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.
- [PocketPaw Scheduler: Reminders and Self-Audit Scheduling](./pocketpaw-scheduler-reminders-and-self-audit-scheduling.md) — ReminderScheduler wraps APScheduler to provide one-shot and recurring reminders with natural language time parsing, per-entry schema validation, and a corrupt-file quarantine mechanism. It also manages the daily self-audit job as a built-in named APScheduler task.
- [Reminder Management Schemas](./reminder-management-schemas.md) — Defines Pydantic models for PocketPaw's reminder system, which accepts natural-language reminder requests, parses them into scheduled entries, and delivers notifications when the trigger time arrives. The schema design keeps the creation API intentionally simple to support conversational input.

### `remote access` — 2 article(s)

- [Remote Tunnel API — Cloudflare Tunnel Management](./remote-tunnel-api-cloudflare-tunnel-management.md) — The remote router exposes three endpoints for controlling a Cloudflare tunnel that makes the locally running PocketPaw instance accessible over the public internet. This lets users interact with their companion from mobile devices or share access without configuring port forwarding.
- [Remote Tunnel API Tests: Cloudflare Tunnel Status, Start, and Stop](./remote-tunnel-api-tests-cloudflare-tunnel-status-start-and-stop.md) — This test file covers PocketPaw's `/api/v1/remote` router, which manages a Cloudflare tunnel that exposes the local PocketPaw instance to the internet. It verifies status reporting, tunnel start with error handling for missing binaries, and graceful stop.

### `remote-access` — 1 article(s)

- [Remote Tunnel Status and Control Schemas](./remote-tunnel-status-and-control-schemas.md) — Defines the minimal Pydantic models for PocketPaw's remote tunnel API, which exposes the agent's local HTTP server to the internet via a secure tunnel. The two schemas cover tunnel status querying and tunnel start results, including error reporting for failed tunnel launches.

### `research` — 1 article(s)

- [ResearchTool: Multi-Step Web Research Pipeline with LLM Summarization](./researchtool-multi-step-web-research-pipeline-with-llm-summarization.md) — `ResearchTool` orchestrates a four-stage pipeline — web search, URL discovery, content extraction, and LLM summarization — into a single `research` tool call. Depth levels let callers trade speed against thoroughness, and an optional memory-save step persists findings across conversations.

### `resilience` — 1 article(s)

- [ReminderScheduler Cron Expression and File Corruption Tests](./reminderscheduler-cron-expression-and-file-corruption-tests.md) — This test file validates `ReminderScheduler`'s support for recurring cron-based reminders and its resilience to corrupted or malformed reminder files. Tests cover cron expression validation, preset schedules, persistent-vs-oneshot behavior, file quarantine on corruption, and partial corruption recovery.

### `resolver` — 1 article(s)

- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.

### `resource management` — 1 article(s)

- [Singleton Lifecycle Management: Coordinated Shutdown and Test Reset](./singleton-lifecycle-management-coordinated-shutdown-and-test-reset.md) — The lifecycle module provides a central registry for singletons that need graceful shutdown on app teardown and deterministic reset between test runs. It decouples teardown orchestration from individual module implementations by letting each module register its own callbacks.

### `resource-management` — 1 article(s)

- [Agent Pool — On-Demand Agent Instance Management with Idle Eviction](./agent-pool-on-demand-agent-instance-management-with-idle-eviction.md) — Implements `AgentPool`, a lifecycle manager for cloud-deployed agent instances. Each agent gets its own `AgentBackend`, `SoulManager`, and isolated memory namespace. Instances are cached for reuse and evicted after configurable idle timeout (default 5 minutes) to bound memory usage.

### `rest-client` — 1 article(s)

- [OpenCode Backend — REST API Client for a Running OpenCode Server](./opencode-backend-rest-api-client-for-a-running-opencode-server.md) — Implements `OpenCodeBackend`, which communicates with a locally running OpenCode server (`opencode --server`) via its REST API using `httpx`. Sessions are mapped from PocketPaw session keys to OpenCode session IDs, and streaming responses are parsed from NDJSON parts.

### `rest-endpoints` — 2 article(s)

- [Session Management REST Router](./session-management-rest-router.md) — Provides the REST endpoints for creating, listing, deleting, renaming, searching, and exporting conversation sessions. Session IDs are UUID-based slugs prefixed with `websocket_` to maintain compatibility with the WebSocket session key format used by the memory subsystem.
- [Settings REST Router with Security Field Filtering](./settings-rest-router-with-security-field-filtering.md) — Exposes GET and PUT REST endpoints for reading and updating PocketPaw's runtime configuration, with two hard-coded protection layers: secrets are never returned over the wire, and a set of security-critical fields cannot be modified via the API at all. An asyncio lock prevents concurrent write races.

### `retrieval` — 8 article(s)

- [DriveSourceAdapter: Zero-Copy Live Federation over the Drive v3 API with Point-in-Time Query Support](./drivesourceadapter-zero-copy-live-federation-over-the-drive-v3-api-with-point-in.md) — `DriveSourceAdapter` is the first concrete `SourceAdapter` in PocketPaw, implementing zero-copy live federation against Google Drive: it returns `DataRef` pointers to live files rather than copying bytes into PocketPaw storage. It also pioneered a lightweight `@at=<iso>|query` convention for point-in-time retrieval before soul-protocol adds a native field.
- [FastAPI Router for Retrieval Logs and Graduation Endpoints](./fastapi-router-for-retrieval-logs-and-graduation-endpoints.md) — Exposes the retrieval projection and graduation policy through a FastAPI router, providing read endpoints for recent retrievals and graduation state, plus an action endpoint that triggers a graduation scan and applies decisions. The store is cached per journal instance so the projection is not rebuilt on every request.
- [Google Drive Connector: DriveClient, SourceAdapter, and RetrievalRouter Integration Tests](./google-drive-connector-driveclient-sourceadapter-and-retrievalrouter-integration.md) — This test suite covers PocketPaw's Google Drive connector end-to-end: HTTP request plumbing with rate-limit backoff, point-in-time revision lookup, `DriveSourceAdapter.query` result shaping, token resolution precedence, and `RetrievalRouter` integration with journal emission. All Drive API traffic is eliminated by a scripted fake `httpx.Client`.
- [In-Memory Projection for Retrieval and Graduation Events](./in-memory-projection-for-retrieval-and-graduation-events.md) — Provides the read-side of the retrieval domain by replaying the org journal into two in-memory views: a recent-retrievals list (filterable by scope, actor, and pocket) and a per-memory graduation state table. A single replay pass builds both views because they share the same event stream.
- [Journal-Backed Write Store for Retrieval and Graduation Events](./journal-backed-write-store-for-retrieval-and-graduation-events.md) — Provides the write path for the retrieval domain by appending `retrieval.query` and `graduation.applied` events to the org journal and immediately folding them into the shared in-memory projection. Replaces the JSONL-plus-asyncio-lock pattern from PR #936 with SQLite WAL semantics that are multi-process safe by design.
- [Memory Graduation Policy for the Retrieval Projection](./memory-graduation-policy-for-the-retrieval-projection.md) — Implements the access-count-based graduation logic that promotes memories across tiers (episodic to semantic, semantic to core, procedural promotion) by scanning the journal-backed retrieval projection rather than a flat JSONL file. All tuning thresholds are carried verbatim from PR #937 to preserve runtime behaviour after the storage layer refactor.
- [Retrieval and Graduation Event Payload Definitions](./retrieval-and-graduation-event-payload-definitions.md) — Defines the canonical action names and payload builders for the two journal event types that power PocketPaw's retrieval tracing and memory graduation system. These constants anchor the projection, policy, and store layers to a shared, stable vocabulary sourced from soul-protocol's v0.3.1 action catalog.
- [Retrieval Journal Projection — Package Entry Point and Design History](./retrieval-journal-projection-package-entry-point-and-design-history.md) — The `ee.retrieval` package implements an observable retrieval trail and access-count graduation policy as a projection over the org journal, superseding two earlier JSONL-based designs. This module re-exports every public symbol and documents the architectural decision to consolidate onto the journal rather than maintain a parallel JSONL sink.

### `ripple` — 1 article(s)

- [Ripple Spec Normalizer: Canonicalising AI-Generated Pocket Layouts Before Persistence](./ripple-spec-normalizer-canonicalising-ai-generated-pocket-layouts-before-persist.md) — The ripple normalizer is a single-function module that accepts the free-form dict output of agent-generated pocket specs and canonicalises it into a consistent envelope shape before it is written to MongoDB. It handles three distinct spec variants (multi-pane, UISpec v1.0, and flat widget lists) with minimal transformation, and generates stable widget IDs when they are missing to ensure idempotent downstream rendering.

### `routing` — 7 article(s)

- [A2A JSON-RPC Dispatcher: Method Registration and Request Routing](./a2a-json-rpc-dispatcher-method-registration-and-request-routing.md) — `A2ADispatcher` parses incoming JSON-RPC 2.0 requests, validates the envelope, and routes them to registered async handlers. It supports both blocking dispatch and SSE streaming dispatch, batch requests, and consistent error handling — with a deliberate separation between stream-only and non-stream methods.
- [Agent Router: Registry-Based Backend Selection with Fallback](./agent-router-registry-based-backend-selection-with-fallback.md) — `AgentRouter` is the runtime dispatcher that resolves a configured backend name through the registry, instantiates it, and routes all agent calls through it. It supports a user-configurable ordered list of fallback backends so that a misconfigured or unavailable primary backend degrades gracefully rather than crashing.
- [Files API Router: Unified Listing and v2 Tree/Browse Endpoints](./files-api-router-unified-listing-and-v2-treebrowse-endpoints.md) — Defines the FastAPI router for the EE `/files` endpoints, including the legacy `GET /files` unified listing endpoint and the v2 tree/browse API built on `ProviderRegistry`. The router factory `build_router` composes the registry, ABAC rules, and request-context factory into a single mountable router.
- [Pockets FastAPI Router: CRUD, Widgets, Sharing, and Layout Export](./pockets-fastapi-router-crud-widgets-sharing-and-layout-export.md) — This is the FastAPI router for the pockets domain, covering the full lifecycle of a pocket from creation through sharing, widget management, agent and team membership, and the newer layout export and user template endpoints. All routes are gated behind a license check dependency, and owner-only operations use explicit access guard dependencies to prevent privilege escalation.
- [Provider Registry and Mount Path Router](./provider-registry-and-mount-path-router.md) — Implements `ProviderRegistry`, which maps mount path prefixes to registered `FolderProvider` instances and resolves incoming file paths to the correct provider via longest-prefix matching. The `FolderProvider` Protocol definition here is the canonical duck-typed interface contract all providers must satisfy.
- [Sessions FastAPI Router: CRUD, History, Runtime Sessions, and Activity Tracking](./sessions-fastapi-router-crud-history-runtime-sessions-and-activity-tracking.md) — This is the FastAPI router for the sessions domain, providing endpoints to create, list, fetch, update, and delete user chat sessions, retrieve message history, and manage lightweight runtime sessions that are not persisted to MongoDB. A notable design is the runtime session endpoints that bridge the in-memory memory manager with the persistent session store.
- [Smart Model Router — Heuristic Classifier for Automatic Model Tier Selection](./smart-model-router-heuristic-classifier-for-automatic-model-tier-selection.md) — Implements `ModelRouter`, a zero-API-call heuristic classifier that maps each incoming message to a `TaskComplexity` tier (SIMPLE, MODERATE, COMPLEX) and selects the appropriate model (Haiku, Sonnet, Opus). Pattern matching prevents false-positive simple classifications for messages that require tools or deep reasoning.

### `runtime` — 1 article(s)

- [Headless Channel Runners: Multi-Adapter CLI Mode for PocketPaw](./headless-channel-runners-multi-adapter-cli-mode-for-pocketpaw.md) — `headless.py` provides the CLI runtime functions that launch PocketPaw in headless server mode — either as a single Telegram bot or as multiple channel adapters (Discord, Slack, WhatsApp, Signal, Matrix, Teams, Google Chat) sharing one `AgentLoop` and message bus. It was extracted from `__main__.py` to keep the entry point thin while keeping channel-setup complexity isolated.

### `runtime sessions` — 1 article(s)

- [Sessions FastAPI Router: CRUD, History, Runtime Sessions, and Activity Tracking](./sessions-fastapi-router-crud-history-runtime-sessions-and-activity-tracking.md) — This is the FastAPI router for the sessions domain, providing endpoints to create, list, fetch, update, and delete user chat sessions, retrieve message history, and manage lightweight runtime sessions that are not persisted to MongoDB. A notable design is the runtime session endpoints that bridge the in-memory memory manager with the persistent session store.

### `safety` — 3 article(s)

- [Plan Mode API — Human Approval Gate for Agent Actions](./plan-mode-api-human-approval-gate-for-agent-actions.md) — The plan mode router provides two endpoints that let users approve or reject an agent's proposed execution plan before any actions are taken. This human-in-the-loop gate prevents agents from running irreversible operations without explicit confirmation.
- [Plan Mode API Tests: Approve and Reject Active Agent Plans](./plan-mode-api-tests-approve-and-reject-active-agent-plans.md) — This test file covers PocketPaw's `/api/v1/plan` router, which lets the dashboard approve or reject an agent's proposed action plan before execution. It verifies the session-keyed lookup, the not-found response when no active plan exists, and input validation.
- [Plan Mode Approval Schemas](./plan-mode-approval-schemas.md) — Defines the minimal Pydantic models for PocketPaw's plan-mode API — the human-in-the-loop gate where a user approves or rejects an agent's proposed plan before execution begins. The session key binds the approval action to a specific pending plan.

### `scheduler` — 3 article(s)

- [Natural Time Parsing Tests: parse_natural_time Scheduler Function](./natural-time-parsing-tests-parsenaturaltime-scheduler-function.md) — This test suite validates the `parse_natural_time` function in PocketPaw's scheduler module, covering every user-facing time expression format — with and without the 'in' prefix, abbreviations, singular/plural forms, and embedded sentences. The suite acts as both a regression guard and a specification for all accepted input shapes.
- [ReminderScheduler Cron Expression and File Corruption Tests](./reminderscheduler-cron-expression-and-file-corruption-tests.md) — This test file validates `ReminderScheduler`'s support for recurring cron-based reminders and its resilience to corrupted or malformed reminder files. Tests cover cron expression validation, preset schedules, persistent-vs-oneshot behavior, file quarantine on corruption, and partial corruption recovery.
- [SimulationClock and Tick-Synchronized Scheduler Tests](./simulationclock-and-tick-synchronized-scheduler-tests.md) — This test file covers `SimulationClock` (a deterministic discrete-time clock for agent simulation), `TickSnapshot` (tick-state serialization), `DependencyScheduler.run_tick_synchronized` (tick-driven task dispatch), and simulation tick metadata injection into task prompts. Tests validate clock advancement, snapshot immutability, scheduler dispatch order, and prompt metadata inclusion.

### `scheduling` — 11 article(s)

- [Automations Bridge — Syncing EE Rules to Core Daemon Intentions](./automations-bridge-syncing-ee-rules-to-core-daemon-intentions.md) — bridge.py translates enterprise automation rules into core daemon Intention specs, and removes them when rules are deleted. It maps schedule presets and rule types (schedule, threshold, data_change) to cron, interval, and event trigger types respectively, ensuring automation rules run through the same scheduling infrastructure as built-in daemon behaviors.
- [Deep Work Dependency Scheduler — DAG-Based Task Dispatch with Cycle Detection](./deep-work-dependency-scheduler-dag-based-task-dispatch-with-cycle-detection.md) — DependencyScheduler manages task dispatch order in a Deep Work project by treating tasks as a directed acyclic graph (DAG). It finds ready tasks (all blockers resolved), dispatches them concurrently, handles human task routing, validates for cycles via Kahn's algorithm, and optionally runs in a tick-synchronized mode driven by a SimulationClock.
- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.
- [Intention Scheduling Schemas](./intention-scheduling-schemas.md) — Defines the Pydantic models for PocketPaw's intention system — scheduled, trigger-driven agent tasks that run prompts automatically. The schema covers the full CRUD lifecycle plus manual trigger acknowledgement, with field-level validation to prevent empty or oversized intentions.
- [Intentions API Tests: CRUD, Toggle, and On-Demand Execution](./intentions-api-tests-crud-toggle-and-on-demand-execution.md) — This test file covers PocketPaw's `/api/v1/intentions` router, which manages proactive agent intentions — scheduled or triggered actions the agent initiates autonomously. It verifies the full CRUD lifecycle, enable/disable toggling, input validation, and on-demand execution.
- [Mission Control Heartbeat Daemon](./mission-control-heartbeat-daemon.md) — The HeartbeatDaemon is a background APScheduler-driven daemon that periodically wakes every registered agent to check for pending work, update status, and fire optional event callbacks. It uses staggered agent wakeups and a singleton pattern to ensure only one daemon runs per process.
- [PocketPaw Scheduler: Reminders and Self-Audit Scheduling](./pocketpaw-scheduler-reminders-and-self-audit-scheduling.md) — ReminderScheduler wraps APScheduler to provide one-shot and recurring reminders with natural language time parsing, per-entry schema validation, and a corrupt-file quarantine mechanism. It also manages the daily self-audit job as a built-in named APScheduler task.
- [Proactive Daemon Tests: Cron Scheduling, Intention CRUD, Context Hub, and Stale Session Triggers](./proactive-daemon-tests-cron-scheduling-intention-crud-context-hub-and-stale-sess.md) — This test module covers PocketPaw's proactive daemon — the subsystem that fires agent prompts on a schedule rather than waiting for user messages. It validates cron expression parsing (including named presets), the `IntentionStore` CRUD lifecycle, `ContextHub` template variable injection, the `TriggerEngine` scheduling layer, and the stale-session trigger that nudges idle users.
- [Reminder Management Schemas](./reminder-management-schemas.md) — Defines Pydantic models for PocketPaw's reminder system, which accepts natural-language reminder requests, parses them into scheduled entries, and delivers notifications when the trigger time arrives. The schema design keeps the creation API intentionally simple to support conversational input.
- [Reminders API — Natural Language Scheduling](./reminders-api-natural-language-scheduling.md) — The reminders router provides list, add, and delete operations for time-based reminders. Reminders are created from natural language strings and processed by the scheduler, which handles parsing relative time expressions like "in 5 minutes" or "tomorrow at 9am".
- [Reminders API Tests: Natural Language Scheduling, Listing, and Deletion](./reminders-api-tests-natural-language-scheduling-listing-and-deletion.md) — This test file covers PocketPaw's `/api/v1/reminders` router, which accepts natural-language reminder messages, parses them into scheduled events via the scheduler, and exposes list and delete operations with human-readable time-remaining formatting.

### `schema` — 1 article(s)

- [PawKit Pydantic Models: Dashboard Configuration Schema](./pawkit-pydantic-models-dashboard-configuration-schema.md) — The `kits.models` module defines the full Pydantic schema hierarchy for PawKit configurations — from individual metric cards up to the root `PawKitConfig` that a YAML file is parsed into. It also defines `InstalledKit`, which tracks activation status and user-provided configuration values at runtime.

### `schema validation` — 2 article(s)

- [Cloud Pydantic Model Tests: Field Defaults and Schema Validation Without a Database](./cloud-pydantic-model-tests-field-defaults-and-schema-validation-without-a-databa.md) — This test file verifies field-level Pydantic model behavior for core cloud entities (Group, Message, Pocket, Invite, Workspace, Session, Notification) using `model_construct()` to bypass Beanie's MongoDB-dependent `__init__`. It pins default values, optional field nullability, and enum acceptance for fields added in recent schema migrations.
- [Pockets Domain Schemas: Request Validation and Response Model Tests](./pockets-domain-schemas-request-validation-and-response-model-tests.md) — This test file validates all Pydantic request/response schemas in the pockets domain — covering field defaults, enum constraints, length limits, optional-field behavior, and the full `PocketResponse` shape. It ensures the API surface contracts are locked in and that invalid inputs are rejected at the schema layer before reaching business logic.

### `schemas` — 17 article(s)

- [Agent Domain Schema Validation Tests](./agent-domain-schema-validation-tests.md) — This module comprehensively tests the Pydantic schemas for the agents domain — `CreateAgentRequest`, `UpdateAgentRequest`, `DiscoverRequest`, `AgentResponse`, `ScopeAssignmentRequest`, and `ScopeAssignmentResponse` — covering required fields, defaults, length constraints, visibility validation, and scope normalization. Scope-related tests were added in the `feat/cluster-d-agent-scope-picker` feature.
- [API Key Schemas — Create, Info, and One-Time Creation Response](./api-key-schemas-create-info-and-one-time-creation-response.md) — The API key schemas define the request and response shapes for managing PocketPaw API keys. The design enforces a security invariant: the full plaintext key is returned only once at creation time, after which only a short prefix is exposed for identification.
- [API v1 Schemas Package — Pydantic Schema Namespace](./api-v1-schemas-package-pydantic-schema-namespace.md) — The `schemas/__init__.py` file marks the `pocketpaw.api.v1.schemas` directory as a Python package. It contains no exports, serving purely as a namespace boundary that groups all Pydantic request and response models for the v1 API.
- [Auth Domain Schema Tests](./auth-domain-schema-tests.md) — This module tests the Pydantic schemas for the auth domain — `ProfileUpdateRequest`, `SetWorkspaceRequest`, and `UserResponse` — verifying optional field defaults, value round-trips, and the structure of the user identity response.
- [Authentication Schemas — Login, Session Token, and Token Regeneration](./authentication-schemas-login-session-token-and-token-regeneration.md) — The auth schemas define the request and response shapes for PocketPaw's cookie-based authentication flow, including master token login, session token exchange, and token regeneration. These three models represent the full lifecycle of an authenticated session.
- [Channel Adapter Schemas — Status, Configuration, and Toggle](./channel-adapter-schemas-status-configuration-and-toggle.md) — The channel schemas define the state and control contracts for PocketPaw's messaging platform adapters (Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, Google Chat). They separate read (status queries) from write (save config, start/stop) operations.
- [Chat Domain Schema Validation Tests](./chat-domain-schema-validation-tests.md) — This module comprehensively tests all Pydantic schemas in the chat domain — group management, message sending, editing, reactions, WebSocket inbound/outbound messages, cursor pagination, and attachment handling — covering field defaults, length constraints, enum validation, and discriminated union types for WebSocket message dispatch.
- [Chat Schemas — Request, Response, and Context Models](./chat-schemas-request-response-and-context-models.md) — The chat schemas define the core request and response contracts for PocketPaw's chat API, including file and pocket context injection, camelCase/snake_case aliasing to prevent session ID loss, and the SSE chunk model for streaming responses.
- [Chat Schemas - Request, Response, and WebSocket Message Models](./chat-schemas-request-response-and-websocket-message-models.md) — This module defines all Pydantic models used by the chat domain: REST request bodies, response shapes, cursor pagination, and WebSocket message envelopes. Centralising schemas here decouples validation logic from both the router and service layers.
- [Common API Response Schemas — Shared Base Types](./common-api-response-schemas-shared-base-types.md) — The common schemas define PocketPaw's shared response envelope types: a base class with ORM compatibility, a standard error shape, a generic paginated list, and two convenience success models. These types appear across every domain in the v1 API.
- [Files Module Public Pydantic Schemas](./files-module-public-pydantic-schemas.md) — Defines the canonical Pydantic data models shared across the entire files subsystem: `Permission`, `RequestContext`, `FileEntry`, `FolderNode`, `MountConfig`, `ResolvedMount`, `Page`, and `SearchQuery`. Validators on these models enforce invariants (absolute paths, namespaced IDs) that providers cannot violate, ensuring the API response shape is consistent regardless of which provider produced it.
- [LLM Backend Schemas — Capability and Installation Contracts](./llm-backend-schemas-capability-and-installation-contracts.md) — The backend schemas define how PocketPaw describes available LLM backends to clients. `BackendInfo` captures a backend's capabilities and installation requirements, while `BackendInstallRequest` provides the payload for triggering a backend installation.
- [Pockets Domain Pydantic Schemas: Request and Response Models](./pockets-domain-pydantic-schemas-request-and-response-models.md) — This file defines all Pydantic request and response models for the pockets domain, covering creation, update, widget management, reordering, share links, collaborators, and the full pocket response shape. A notable recent addition is support for passing agents, rippleSpec, and an initial widgets list on creation so the frontend can fully specify a pocket in a single request.
- [Session Schema Tests: Create, Update, and Response Contract Validation](./session-schema-tests-create-update-and-response-contract-validation.md) — Unit tests for the three Pydantic schemas governing session lifecycle in PocketPaw's EE cloud layer — `CreateSessionRequest`, `UpdateSessionRequest`, and `SessionResponse`. The tests pin default values, optional-field behavior, and the nullable `deleted_at` field used for soft-deletion semantics.
- [Sessions Domain Pydantic Schemas: Request and Response Models](./sessions-domain-pydantic-schemas-request-and-response-models.md) — This file defines the three Pydantic models for the sessions domain: CreateSessionRequest, UpdateSessionRequest, and SessionResponse. The schemas are intentionally lean, supporting optional linkage to pockets, groups, agents, and existing runtime sessions at creation time.
- [Workspace Pydantic Schemas — Requests and Responses](./workspace-pydantic-schemas-requests-and-responses.md) — This module defines the Pydantic request and response schemas for the workspace domain, enforcing input validation at the API boundary before any business logic runs. The slug validator is the key defensive piece: it rejects slugs that would break URL routing or clash with reserved path segments.
- [Workspace Schema Tests: Slug Validation, Invite Rules, and Member Role Contracts](./workspace-schema-tests-slug-validation-invite-rules-and-member-role-contracts.md) — Tests for the workspace domain Pydantic schemas in PocketPaw's EE layer, covering slug format enforcement, invite role validation, and member role update rules. The slug tests are particularly important because workspace slugs appear in URLs and must be lowercase, hyphen-separated, and free of leading/trailing hyphens.

### `scope filtering` — 4 article(s)

- [Fabric Event Payload Types and Action Constants](./fabric-event-payload-types-and-action-constants.md) — This module pins the three canonical event shapes that drive Fabric's journal projection — object created, updated, and archived — as stable action name constants and payload builder functions. It was created specifically to replace PR #938's scope-filtering approach in the legacy SQLite store, which had two unfixable structural bugs: a schema migration gap and a pagination leak.
- [Fabric Module Entry Point — Ontology Layer Public API](./fabric-module-entry-point-ontology-layer-public-api.md) — The `ee/fabric/__init__.py` consolidates the public API of the Fabric ontology layer into a single import surface, exposing both the legacy SQLite store and the newer journal-backed path introduced in Wave 3. The `__all__` list is the canonical documentation of what is stable and intended for external use.
- [FabricJournalStore — Event-Sourced Object Lifecycle for Fabric](./fabricjournalstore-event-sourced-object-lifecycle-for-fabric.md) — `FabricJournalStore` is the Wave 3 replacement for the scope-filtering slice of PR #938, providing event-sourced create, update, archive, and query operations over Fabric objects backed by the org journal and served through an in-memory projection. Every write appends an `EventEntry` to the journal and immediately folds it into the live projection, so reads after a write are always consistent without a rebuild.
- [FabricProjection — In-Memory Journal Projection for Fabric Objects](./fabricprojection-in-memory-journal-projection-for-fabric-objects.md) — `FabricProjection` replays `fabric.object.*` journal events into an in-memory dictionary to maintain a current-state view of all Fabric objects, applying scope filters before computing query totals to permanently fix the pagination leak that blocked PR #938. It is a deliberately minimal CQRS read model: rebuild from the journal on startup, apply events incrementally, serve scoped queries.

### `scope management` — 1 article(s)

- [Agent Scope Rules: Normalization and Assignment Authorization Tests](./agent-scope-rules-normalization-and-assignment-authorization-tests.md) — This module tests `ee.cloud.agents.scope_rules`, covering the `normalise_and_validate_scopes` function (grammar enforcement, lowercasing, deduplication, forbidden-wildcard rejection) and the `admin_can_assign_scopes` function (containment check that prevents scope-narrowed admins from assigning scopes outside their own grant).

### `scope policy` — 1 article(s)

- [Fabric Scope Policy Engine — Visibility Decisions for Ontology Objects](./fabric-scope-policy-engine-visibility-decisions-for-ontology-objects.md) — This module provides the scope-based visibility engine for Fabric objects, implementing bidirectional containment matching so that a caller with scope `org:sales:*` can see objects tagged `org:sales:leads` and vice versa. It is a local copy of the logic from soul-protocol's `scopes_overlap` function, kept independent to avoid pulling the full engine into minimal runtime slices and to preserve the audit trail of which scope granted access.

### `scripts` — 6 article(s)

- [MongoDB Cloud Database Diagnostic: Collection Counts and Sample Document Inspection](./mongodb-cloud-database-diagnostic-collection-counts-and-sample-document-inspecti.md) — A targeted diagnostic script for the PocketPaw enterprise cloud MongoDB database that lists collection document counts and prints sample documents from key collections. Unlike the broader `diag_mongo_all.py`, this script focuses on the `paw-cloud` database and respects the `POCKETPAW_CLOUD_MONGO_URI` environment variable for flexible targeting.
- [MongoDB Diagnostic: List All Databases and Collection Document Counts](./mongodb-diagnostic-list-all-databases-and-collection-document-counts.md) — A diagnostic script that connects to a local MongoDB instance and prints every non-system database alongside its collections and document counts. It exists to quickly answer the question "where did my data actually land?" when debugging unexpected data routing between the local dev database, the enterprise cloud database, and any other MongoDB instance on the machine.
- [Role Roundtrip Diagnostic: Verifying User and Assistant Message Persistence](./role-roundtrip-diagnostic-verifying-user-and-assistant-message-persistence.md) — A targeted diagnostic script that writes four messages with alternating `user` and `assistant` roles to MongoDB via the memory manager, then reads them back to verify that roles are persisted and retrieved correctly. It was created to debug a specific class of bug where the agent loop was saving messages but role information was being lost or corrupted during the roundtrip.
- [Test Suite for A2A Protocol Live Smoke Tests](./test-suite-for-a2a-protocol-live-smoke-tests.md) — This script is a live, end-to-end smoke test suite for PocketPaw's A2A (Agent-to-Agent) protocol endpoints. It requires a running PocketPaw server with `POCKETPAW_A2A_ENABLED=true` and exercises every major A2A feature — agent card discovery, message sending, task management, streaming, cancellation, and error handling — reporting PASS/FAIL for each check.
- [Test Suite for Knowledge Ingestion: CFO Agent Creation and Document Upload Smoke Test](./test-suite-for-knowledge-ingestion-cfo-agent-creation-and-document-upload-smoke-.md) — A manual smoke test script that creates a CFO-persona agent via the REST API and ingests three fixture documents into its knowledge base. It lives in `scripts/` rather than `tests/` to keep it out of pytest collection and CI, and requires a running backend with a valid bearer token to execute.
- [WebSocket Persistence Diagnostic: Simulating the Chat Message Save Path with Full Debug Logging](./websocket-persistence-diagnostic-simulating-the-chat-message-save-path-with-full.md) — This script simulates the exact code path the WebSocket adapter executes when saving a chat message, but with DEBUG-level logging and no exception swallowing. It was created to surface silent persistence failures that were invisible in normal operation because the WebSocket handler caught and discarded exceptions.

### `search` — 7 article(s)

- [Conversation Session Schemas](./conversation-session-schemas.md) — Defines the Pydantic models for PocketPaw's conversation session API — listing, creating, renaming, and searching sessions. The search model captures both the matched content and the message role, enabling contextually rich search result previews in the dashboard.
- [Knowledge Base REST API Router (Per-Workspace Scope)](./knowledge-base-rest-api-router-per-workspace-scope.md) — The primary FastAPI router for workspace-scoped knowledge base operations — search, ingest, lint, and article browsing — all delegating to the kb Go binary. This router replaced a pure-Python knowledge_base package and defines the REST surface consumed by the wiki pocket template.
- [Knowledge Engine: Unified Ingest-Compile-Index-Search Pipeline](./knowledge-engine-unified-ingest-compile-index-search-pipeline.md) — The `KnowledgeEngine` class is the single entry point for all knowledge management operations — ingesting text, URLs, and files, compiling them into structured wiki articles via LLM, indexing concepts and backlinks, and searching with BM25. It is scope-aware, allowing separate knowledge bases per agent, workspace, or pocket.
- [Knowledge Indexer: Concept Graph and Backlink Builder](./knowledge-indexer-concept-graph-and-backlink-builder.md) — The knowledge indexer maintains a `KnowledgeIndex` that maps concepts to the articles mentioning them and builds bidirectional backlinks between articles that reference each other's titles. It supports both full rebuilds from scratch and incremental updates when a single article is added or removed.
- [Knowledge Search: BM25 Ranking with Substring Fallback](./knowledge-search-bm25-ranking-with-substring-fallback.md) — PocketPaw's knowledge search uses the `bm25s` library to rank wiki articles by relevance to a query, with a simple substring-match fallback when `bm25s` is not installed. The design deliberately avoids vector embeddings — LLM-compiled articles are semantically rich enough for BM25 to outperform naive chunked RAG.
- [Message Service - Chat Message CRUD, Reactions, Threads, and Search](./message-service-chat-message-crud-reactions-threads-and-search.md) — The `MessageService` class handles all message lifecycle operations in the chat domain, including sending, editing, deletion, reactions, thread replies, pin management, mention fan-out, and full-text search. It was split from the original `service.py` monolith and extended with agent message creation and workspace-scoped search.
- [Workspace Message Search Tests](./workspace-message-search-tests.md) — This test module covers MessageService's workspace-wide message search, verifying that results respect group visibility (channels vs. private rooms), workspace scope isolation, regex-safety of user queries, and server-side result limits. Tests run against an in-memory MongoDB via the shared beanie_memory_db fixture to exercise real Mongo query behavior.

### `secret management` — 1 article(s)

- [Output Redaction Tests: API Keys, Tokens, and Secret Pattern Detection](./output-redaction-tests-api-keys-tokens-and-secret-pattern-detection.md) — PocketPaw's output redaction module (`pocketpaw.security.redact`) scans agent output for API keys, tokens, credentials, and other secrets before they are logged or sent to external systems. These tests validate detection of over a dozen secret patterns across major providers, verify that safe text is not falsely flagged, and confirm the specific scenario of an agent reading a `.env` file.

### `security` — 159 article(s)

- [A2A Client Tests: HTTP Operations, Context Manager Reuse, Streaming, Delegate Tool, and SSRF Protection](./a2a-client-tests-http-operations-context-manager-reuse-streaming-delegate-tool-a.md) — Tests for the PocketPaw Agent-to-Agent (A2A) client library, covering HTTP task operations (send, get, cancel, stream), response error handling, agent card fetching with caching, the `A2ADelegateTool` multi-turn wrapper, and SSRF protection that blocks requests to private IP ranges and invalid schemes.
- [A2ADelegateTool: Secure Cross-Agent Task Delegation via the A2A Protocol](./a2adelegatetool-secure-cross-agent-task-delegation-via-the-a2a-protocol.md) — `A2ADelegateTool` allows PocketPaw's agent to delegate tasks to any external agent that speaks the Agent-to-Agent (A2A) protocol, with built-in SSRF protection that blocks requests to private IP ranges. It bridges PocketPaw's internal tool system to the open A2A agent network.
- [ABAC Config Tests: Rule Loading and Allow/Deny Logic](./abac-config-tests-rule-loading-and-allowdeny-logic.md) — This module tests the Attribute-Based Access Control (ABAC) configuration layer for the files subsystem, covering YAML rule loading, the allow logic for tagged vs. untagged entries, attribute matching, and the deny-override behavior when a file carries multiple tags.
- [ABAC Policy Engine: Plan Gates, Role Checks, and Agent Ceilings](./abac-policy-engine-plan-gates-role-checks-and-agent-ceilings.md) — The `abac.py` module implements PocketPaw's attribute-based access control (ABAC) by combining three declarative tables — plan feature gates, action-to-role mappings, and per-role tool limits — into a single `evaluate_policy` function. It is designed to run after RBAC role resolution, adding plan-tier and agent-ceiling checks that role membership alone cannot express.
- [ABAC Rule Loader and Evaluator for File Access Control](./abac-rule-loader-and-evaluator-for-file-access-control.md) — The `abac_config.py` module implements an Attribute-Based Access Control (ABAC) engine for the files domain, loading restriction rules from a YAML configuration file and evaluating them against per-request user attributes and file entry tags. Rules can only restrict access - they cannot grant permissions beyond the RBAC baseline.
- [ACTIONS Registry: Canonical RBAC Action-to-Role Matrix](./actions-registry-canonical-rbac-action-to-role-matrix.md) — The `actions.py` module is the single source of truth for every guarded operation in PocketPaw, mapping action strings (in `"resource.action"` dotted form) to a minimum required role and a stable machine-readable denial code. Unknown actions fail loudly by design, preventing silent authorization bypasses when new routes are added without a corresponding ACTIONS entry.
- [Agent Backends and Router Tests: ClaudeSDKBackend, Registry Routing, Dangerous Hook, and CLI Auth](./agent-backends-and-router-tests-claudesdkbackend-registry-routing-dangerous-hook.md) — Tests for the multi-SDK agent architecture covering `AgentEvent` protocol, `ClaudeSDKBackend` (formerly ClaudeAgentSDKWrapper), registry-based `AgentRouter` backend selection with legacy fallbacks, the fail-closed dangerous command hook, and a regression test for PocketPaw incorrectly requiring an API key when Claude CLI authentication is active.
- [Agent-Facing Pocket Context Fetcher](./agent-facing-pocket-context-fetcher.md) — `fetch_pocket_for_agent` retrieves a pocket document and strips sensitive fields before returning it as a compact JSON-safe dict for injection into AI agent tool responses. The function follows a defensive result-envelope pattern, always returning `{"ok": bool, ...}` rather than raising exceptions, so agent tool handlers can branch on success without try/except.
- [Agent Scope Validation and Assignment Rules](./agent-scope-validation-and-assignment-rules.md) — `scope_rules.py` implements the server-side grammar and authorisation rules for hierarchical scope tags — normalising, validating, and deduplicating scope strings, and checking whether an admin's own scope grant covers the scopes they attempt to assign to an agent.
- [API Authorization Dependency: Fail-Closed Scope Enforcement](./api-authorization-dependency-fail-closed-scope-enforcement.md) — `require_scope` is a FastAPI dependency that enforces fail-closed authorization across all API endpoints. It accepts requests only when the caller has been explicitly marked as fully trusted, carries an API key with the required scope, or carries an OAuth2 token with the required scope — rejecting all other requests with HTTP 403.
- [API Key Health Checks: Presence, Format Validation, and Encryption Verification](./api-key-health-checks-presence-format-validation-and-encryption-verification.md) — The `api_keys.py` module implements four health checks covering the full lifecycle of API credentials: whether a key exists for the selected backend, whether it matches the expected format prefix, whether required backend packages are installed, and whether the secrets file is Fernet-encrypted rather than plaintext. Together these checks prevent the most common misconfiguration failures that cause silent agent startup errors.
- [API Key Management Tests: Creation, Verification, Rotation, and REST Endpoints](./api-key-management-tests-creation-verification-rotation-and-rest-endpoints.md) — Comprehensive tests for PocketPaw's `APIKeyManager` class and its REST endpoints covering the full key lifecycle — creation with scope validation, verification, revocation, rotation, and expiry rejection. The test suite isolates all disk I/O to a temp directory to prevent cross-test contamination and ensure file permission checks work correctly.
- [API Key Manager: Secure Key Lifecycle with Hash-Only Storage](./api-key-manager-secure-key-lifecycle-with-hash-only-storage.md) — `APIKeyManager` provides create, verify, revoke, rotate, and list operations for API keys using a GitHub PAT-style design: keys are shown once at creation and only their SHA-256 hash is persisted. Keys use the `pp_` prefix for log identification, support fine-grained scopes, and are stored in `~/.pocketpaw/api_keys.json`.
- [API Key Schemas — Create, Info, and One-Time Creation Response](./api-key-schemas-create-info-and-one-time-creation-response.md) — The API key schemas define the request and response shapes for managing PocketPaw API keys. The design enforces a security invariant: the full plaintext key is returned only once at creation time, after which only a short prefix is exposed for identification.
- [API-Only Serve Mode Tests: Structure, Auth Middleware, CLI Parsing, and Socket Safety](./api-only-serve-mode-tests-structure-auth-middleware-cli-parsing-and-socket-safet.md) — Tests for PocketPaw's `serve` command, which launches a lightweight API-only server without the dashboard UI. Covers endpoint availability, auth middleware behavior, CLI argument parsing, and a regression suite proving that sockets used for local IP detection are always closed — even when errors occur during detection.
- [Audit FTS Security Tests: SQL Injection Prevention and LIKE Wildcard Escaping](./audit-fts-security-tests-sql-injection-prevention-and-like-wildcard-escaping.md) — Security regression tests proving that the `q` parameter on PocketPaw's `/runtime/audit` search endpoint is fully parameter-bound and cannot corrupt or drop the `audit_log` table, and that LIKE special characters (`%`, `_`) in the query are correctly escaped to prevent wildcard-widened result sets. Tests correspond to Cluster C PR4.
- [Audit Logging Completeness Tests: Append-Only Guarantee, Rotation, Failure Resilience, and PII Masking](./audit-logging-completeness-tests-append-only-guarantee-rotation-failure-resilien.md) — Layer 7 security tests verifying that PocketPaw's audit logger captures all security-relevant events, maintains an append-only JSONL log that cannot be deleted through the logger interface, archives rather than deletes on rotation, survives write failures without crashing, and masks PII (e.g., SSNs) before writing to disk.
- [Audit Logging System: Append-Only Agent Action Trail](./audit-logging-system-append-only-agent-action-trail.md) — This module implements a secure, append-only audit log that records every critical agent action to a JSONL file at `~/.pocketpaw/audit.jsonl`. It provides structured severity tiers, optional PII filtering, a callback fan-out mechanism, and a singleton accessor — forming the forensic backbone of PocketPaw's security stack.
- [Auth API Tests: Session Exchange, Cookie Login, Rate Limiting, and QR Code](./auth-api-tests-session-exchange-cookie-login-rate-limiting-and-qr-code.md) — This test module comprehensively covers PocketPaw's `/api/v1/auth` router, verifying the master-token-to-session exchange, cookie-based login with correct Secure flag handling under reverse proxies, logout, token regeneration, QR code generation, and IP-based rate limiting on all three auth endpoints.
- [Auth Domain FastAPI Router — Profile, Avatar, and Workspace Management](./auth-domain-fastapi-router-profile-avatar-and-workspace-management.md) — The auth router assembles PocketPaw Enterprise's full authentication HTTP surface by composing fastapi-users' built-in auth/register routes with custom profile management, avatar upload, and workspace-switching endpoints.
- [Auth Exception Logging Tests: Silent Swallow Prevention in Auth Dispatch and OAuth2](./auth-exception-logging-tests-silent-swallow-prevention-in-auth-dispatch-and-oaut.md) — Regression tests for issue #627 — verifies that bare `except Exception: pass` blocks in authentication dispatch paths have been replaced with `logger.warning(..., exc_info=True)` calls. Tests cover API key validation, OAuth2 token validation, cookie login branches, and OAuth2 server audit log failures, ensuring auth failures are always logged and always return 401.
- [Authentication Schemas — Login, Session Token, and Token Regeneration](./authentication-schemas-login-session-token-and-token-regeneration.md) — The auth schemas define the request and response shapes for PocketPaw's cookie-based authentication flow, including master token login, session token exchange, and token regeneration. These three models represent the full lifecycle of an authenticated session.
- [Backends API Tests: Listing, Availability Checks, and Safe Install Subprocess Handling](./backends-api-tests-listing-availability-checks-and-safe-install-subprocess-handl.md) — This test file covers PocketPaw's `/api/v1/backends` router, verifying that the backend registry is exposed correctly, that the `_check_available` helper correctly detects missing imports, and that the async install endpoint handles subprocess timeouts and stderr secret redaction without leaking sensitive data.
- [Canonical Runtime Audit Router with Full-Text Search](./canonical-runtime-audit-router-with-full-text-search.md) — The authoritative audit query endpoint at `/api/v1/runtime/audit`, consolidating two legacy paths behind a single surface that adds workspace-level rollup filtering and bound-parameter full-text search. SQL injection is explicitly prevented by using parameterized LIKE queries, verified by a dedicated regression test.
- [Claude Agent SDK Backend — Recommended Default Agent Runtime](./claude-agent-sdk-backend-recommended-default-agent-runtime.md) — Implements `ClaudeSDKBackend`, PocketPaw's primary agent backend that wraps Anthropic's `claude-agent-sdk`. It provides built-in tools (Bash, Read, Write, Edit, Glob, Grep, WebSearch, WebFetch), streaming token delivery, a `PreToolUse` security hook, MCP server support, and headless permission bypass for messaging channels.
- [Codex CLI Backend — Subprocess Wrapper for OpenAI's Terminal Agent](./codex-cli-backend-subprocess-wrapper-for-openais-terminal-agent.md) — Implements `CodexCLIBackend`, which spawns OpenAI's `codex` CLI as an async subprocess and parses its NDJSON event stream. Prompts are passed via stdin rather than as command-line arguments to avoid OS argument-length limits, and a 10 MiB subprocess buffer accommodates large MCP tool results.
- [Comprehensive GuardianAgent Security Filter Tests](./comprehensive-guardianagent-security-filter-tests.md) — This module provides exhaustive coverage of `GuardianAgent` — PocketPaw's Layer 6 AI safety filter — including LLM-based classification, fail-closed behavior on all error conditions, local regex fallback when no API key is present, audit logging for every code path, concurrent safety, singleton correctness, and prompt-injection hardening (issue #873).
- [Config File Health Checks: Existence, JSON Validity, and Permission Auditing](./config-file-health-checks-existence-json-validity-and-permission-auditing.md) — The `config.py` health check module verifies three properties of PocketPaw's configuration file: that it exists, that it contains valid JSON, and that its filesystem permissions are restricted to 600. These checks run at startup to catch configuration problems before they produce cryptic runtime failures.
- [Config Validation Tests: API Key Formats, Numeric Constraints, and Enum Fields](./config-validation-tests-api-key-formats-numeric-constraints-and-enum-fields.md) — This test module covers three tiers of Settings validation: the `validate_api_key()` function that checks provider-specific key prefixes and warns on mismatches, Pydantic `gt`/`ge` constraints on numeric fields that prevent nonsensical configurations, and `Literal`-typed enum fields for provider selection. Together they ensure that misconfigured credentials and out-of-range settings fail loudly at startup rather than silently at runtime.
- [Connector Status Route Tests: State Transitions, Pocket Isolation, and Secret Non-Leakage](./connector-status-route-tests-state-transitions-pocket-isolation-and-secret-non-l.md) — This test file locks down PocketPaw's connector status route contract — the full state machine from disconnected through connected to expired, strict per-pocket credential isolation so one pocket cannot see another's connection state, and the guarantee that OAuth secrets passed at connect time never appear in status responses.
- [Core PocketPaw Tools: Status, Fetch, Screenshot, Config, and Router Tests](./core-pocketpaw-tools-status-fetch-screenshot-config-and-router-tests.md) — This test module provides broad coverage of PocketPaw's foundational tool and configuration layer, including the system status tool, the file fetch tool's path jail enforcement, the screenshot tool's optional dependency handling, the settings save/load lifecycle, and both the LLM and agent routers' initialization and error paths.
- [Core RBAC Primitives: Workspace Roles, Pocket Access Levels, and Forbidden Exception](./core-rbac-primitives-workspace-roles-pocket-access-levels-and-forbidden-exceptio.md) — `rbac.py` establishes the foundational enums and exception for PocketPaw's role-based access control: a 3-tier `WorkspaceRole` hierarchy, a 4-tier `PocketAccess` hierarchy, and the `Forbidden` exception that carries machine-readable denial codes. All other guard modules depend on these primitives without circular imports.
- [Credential Scrubber: Keeping Secrets Out of Audit Logs and Error Records](./credential-scrubber-keeping-secrets-out-of-audit-logs-and-error-records.md) — This module provides `scrub_params()` and `scrub_command()` — two functions that mask credential-like data before it enters audit logs, system logger fallbacks, and dangerous-command records. It combines an explicit secret field list from `credentials.py` with heuristic field-name patterns to catch secrets regardless of how they are named.
- [Credential Store Tests: Fernet Encryption, Config Separation, Migration, and Log Scrubbing](./credential-store-tests-fernet-encryption-config-separation-migration-and-log-scr.md) — This test module validates PocketPaw's `CredentialStore` — the Fernet-encrypted store that keeps API keys off disk in plaintext — along with the separation of secrets from non-secret config, a one-time migration from legacy plaintext `config.json`, strict Unix file permissions, and a `SecretFilter` that scrubs API keys from log output.
- [Credential Store v2 Tests: AES-256-GCM, Argon2id, AEAD, Migration, and Platform Fallbacks](./credential-store-v2-tests-aes-256-gcm-argon2id-aead-migration-and-platform-fallb.md) — This test module validates the v2 upgrade of PocketPaw's `CredentialStore`, which replaces Fernet (PBKDF2-based) encryption with AES-256-GCM authenticated encryption using Argon2id key derivation. It covers the full v2 round-trip, authenticated data enforcement, automatic v1-to-v2 migration with backup, corruption recovery, and cross-platform hardware UUID fallbacks used to derive the encryption key.
- [CredentialStore: Machine-Locked Encrypted Secret Storage with Argon2id + AES-256-GCM](./credentialstore-machine-locked-encrypted-secret-storage-with-argon2id-aes-256-gc.md) — `CredentialStore` encrypts API keys and OAuth tokens to `~/.pocketpaw/secrets.enc` using a key derived from machine identity via Argon2id, ensuring the encrypted file is only usable on the same machine and user account. Version 2 upgraded from Fernet + PBKDF2 to AES-256-GCM + Argon2id with automatic migration and AEAD authentication.
- [Daily Notes Isolation Tests: Per-User Scoping and Backward-Compatible Legacy Note Visibility](./daily-notes-isolation-tests-per-user-scoping-and-backward-compatible-legacy-note.md) — This test file, added during security sprint cluster D (issue #887), validates that daily notes in the `MemoryManager` are scoped to the `sender_id` that created them, preventing one user's private notes from leaking into another user's agent context. It also tests the backward-compatibility guarantee that legacy notes written before the fix remain visible as system-wide entries.
- [Dashboard Auth Cookie Tests: Secure Flag Behavior Under HTTP and HTTPS Proxies](./dashboard-auth-cookie-tests-secure-flag-behavior-under-http-and-https-proxies.md) — This test file validates that the dashboard's session cookie is set without the `Secure` flag in plain HTTP environments (to avoid breaking self-hosted HTTP deployments) and with the `Secure` flag when the request arrives via an HTTPS reverse proxy detected by the `X-Forwarded-Proto: https` header.
- [Dashboard Security Tests: Rate Limiting, Session Tokens, Tunnel Auth Bypass, Security Headers, and WebSocket Auth](./dashboard-security-tests-rate-limiting-session-tokens-tunnel-auth-bypass-securit.md) — This comprehensive test module validates PocketPaw's dashboard security hardening across six dimensions: token-bucket rate limiting, HMAC-signed session tokens with TTL, the localhost tunnel auth bypass fix, security headers, CORS rejection, and WebSocket authentication before the upgrade handshake completes.
- [DelegateToClaudeCodeTool: Critical-Trust Agent-to-Agent Delegation via CLI](./delegatetoclaudecodetool-critical-trust-agent-to-agent-delegation-via-cli.md) — `DelegateToClaudeCodeTool` gives PocketPaw's agent the ability to hand off complex coding tasks to Claude Code CLI, the Anthropic autonomous coding agent. It is marked `critical` trust — the highest level in PocketPaw's trust hierarchy — because Claude Code has unrestricted filesystem, shell, and web access.
- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.
- [DeliverArtifactTool: Secure File Delivery from Agent Workspace to User Channel](./deliverartifacttool-secure-file-delivery-from-agent-workspace-to-user-channel.md) — `DeliverArtifactTool` lets PocketPaw's agent send generated or downloaded files (images, PDFs, audio) to the user through their current channel — Slack, Discord, or the web dashboard — after verifying the file is within the agent's sandboxed working directory.
- [DirectoryTreeTool: File System Tree Visualization with Jail, Depth, and Security Controls](./directorytreetool-file-system-tree-visualization-with-jail-depth-and-security-co.md) — The `DirectoryTreeTool` renders an ASCII directory tree within a file jail, with configurable depth, hidden file visibility, file size display, and entry count truncation. Tests cover basic tree rendering, depth limiting, hidden file exclusion/inclusion, size annotation, edge cases (empty dir, nonexistent path, file-not-directory), security (jail break, prefix bypass, symlink skipping), truncation, and the tool's schema definition.
- [DirectoryTreeTool: Jail-Bounded Recursive Directory Listing with Entry Cap](./directorytreetool-jail-bounded-recursive-directory-listing-with-entry-cap.md) — `DirectoryTreeTool` generates a `tree`-style directory listing with configurable depth, hidden-file filtering, and optional file sizes — all bounded by the `file_jail_path` and a hard `MAX_ENTRIES = 500` cap. Path resolution uses `is_relative_to()` to prevent traversal outside the allowed root.
- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.
- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.
- [Download URL Endpoint Tests: TTL Responses, Cross-Workspace Isolation, and Path Traversal Defense](./download-url-endpoint-tests-ttl-responses-cross-workspace-isolation-and-path-tra.md) — Integration tests for the `/uploads/{file_id}/download-url` endpoint added in the EE upload router, verifying that the endpoint returns a time-bounded URL with the original filename, blocks cross-workspace access with 404, rejects path-traversal IDs, and hides soft-deleted files. Uses the same FastAPI test-client pattern as `test_router.py` with header-driven identity injection.
- [E2E Security Tests: WebSocket Authentication Protocol and HTTP Security Headers](./e2e-security-tests-websocket-authentication-protocol-and-http-security-headers.md) — This Playwright-based suite verifies PocketPaw's WebSocket authentication model — that connections succeed from localhost without explicit auth, that the frontend sends an authenticate action as the first WebSocket message, that tokens never appear in WebSocket URLs, and that API endpoints correctly reject unauthenticated requests while static assets remain accessible.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.
- [Enterprise Authentication Core — JWT, User Management, and Startup Seeding](./enterprise-authentication-core-jwt-user-management-and-startup-seeding.md) — `auth/core.py` implements PocketPaw Enterprise's authentication system using `fastapi-users` with dual JWT transports (cookie for browser, bearer for API/Tauri), and provides idempotent startup seeding functions that ensure a default admin, workspace, chat group, and default agent exist before the first user logs in.
- [Enterprise Guard Layer: RBAC, ABAC, Plan Feature Gates, and FastAPI Dependency Tests](./enterprise-guard-layer-rbac-abac-plan-feature-gates-and-fastapi-dependency-tests.md) — Comprehensive tests for PocketPaw's enterprise authorization layer, covering role-based access control (RBAC) enums and guard functions, attribute-based access control (ABAC) policy evaluation, plan feature tables, action-to-role mapping, tool whitelists, and three FastAPI dependency guards (`require_role`, `require_plan_feature`, `require_policy`).
- [Enterprise License Validation and FastAPI Dependency](./enterprise-license-validation-and-fastapi-dependency.md) — A self-contained license system that validates Ed25519-signed license keys for enterprise cloud features. License state is cached process-wide and gates API endpoints via FastAPI `Depends` integration, with a fallback HMAC-SHA256 path for self-hosted deployments that cannot configure asymmetric keys.
- [External Agent Delegation — Subprocess Execution of CLI Agents](./external-agent-delegation-subprocess-execution-of-cli-agents.md) — Provides `ExternalAgentDelegate`, a utility for dispatching tasks to external CLI agents (currently only Claude Code CLI) via `asyncio` subprocesses. It captures structured JSON output and surfaces it as a `DelegationResult`, enabling PocketPaw to use external agents as heavyweight tools.
- [Extras Install and Channel Dependency Endpoint Tests](./extras-install-and-channel-dependency-endpoint-tests.md) — This test module covers the `GET /api/extras/check` and `POST /api/extras/install` dashboard endpoints that manage optional channel adapter dependencies. It validates the allowlist-based security model that prevents arbitrary package installation and tests the `missing_dep` fallback path when a channel adapter fails to start due to an ImportError.
- [Fabric List Endpoints: Object and Link Filtering, Envelope Shape, and SQL Injection Safety Tests](./fabric-list-endpoints-object-and-link-filtering-envelope-shape-and-sql-injection.md) — Integration tests for the `/fabric/objects` and `/fabric/links` list endpoints added as part of the PocketDataPanel's Objects/Links sub-tab wiring. Covers store-level link filtering by type and source, HTTP envelope shape, type-based filtering, pagination parameter validation, and an explicit SQL injection safety test for the `link_type` parameter.
- [Fetch Tool Path Security and Jail Boundary Tests](./fetch-tool-path-security-and-jail-boundary-tests.md) — This test module validates the security model of PocketPaw's file fetch tool, specifically the `is_safe_path` predicate and `handle_path`/`list_directory` handlers that enforce a configurable jail directory. The tests were created as regression guards for issue #619, where an empty path string could bypass the jail check by resolving to the current working directory.
- [fetch.py: Jail-Safe File Browser with Pydantic Validation and Telegram Keyboard Support](./fetchpy-jail-safe-file-browser-with-pydantic-validation-and-telegram-keyboard-su.md) — `fetch.py` provides path-traversal-safe file browsing via `is_safe_path()`, a `FetchRequest` Pydantic model, and `handle_path()` / `list_directory()` helpers. The Telegram `InlineKeyboardMarkup` import is optional, enabling interactive directory navigation buttons in Telegram without breaking non-Telegram deployments.
- [File Uploads Router with Signed URL Access](./file-uploads-router-with-signed-url-access.md) — Implements the OSS single-user file upload system with POST (single and bulk), streaming GET with signed URL authorization, and DELETE endpoints. Uploaded files are stored under `~/.pocketpaw/uploads/` with a JSONL metadata index, and downloads are gated by short-lived HMAC-signed tokens to prevent unauthorized access.
- [FileMemoryStore Fixes: UUID Collision, Fuzzy Search, Deduplication, Persistent Delete, and Graph Tests](./filememorystore-fixes-uuid-collision-fuzzy-search-deduplication-persistent-delet.md) — This comprehensive test file covers a series of incremental fixes to `FileMemoryStore`, PocketPaw's markdown-based memory backend. It addresses UUID collisions, broken substring search, single-day file loading, lost deletions across restarts, and missing context limits — plus later phases covering vector semantic search, graph indexing, SVG escaping, and SQLite variable limit handling.
- [Files API Tests: Browse, Download, Zip, Write, and Path Traversal Defence](./files-api-tests-browse-download-zip-write-and-path-traversal-defence.md) — This test file covers PocketPaw's `/api/v1/files` router, which exposes local filesystem operations (browse, single-file download, directory zip download, and file write) to the dashboard. It places particular emphasis on path traversal prevention, file jail enforcement, and zip bomb mitigations.
- [Files RBAC and ABAC Permission Evaluator](./files-rbac-and-abac-permission-evaluator.md) — Implements the two-layer permission model for the files module: RBAC permissions come from providers on a per-entry basis, while ABAC rules apply as a post-filter that can only further restrict visibility. The `PermissionsEvaluator` combines both layers to produce the final UI-facing capability list for each file entry.
- [Files Router Security Tests: Scope Enforcement and Symlink Jail Traversal Prevention](./files-router-security-tests-scope-enforcement-and-symlink-jail-traversal-prevent.md) — Security regression tests for issues #884 and #886 in PocketPaw's files router: #884 proved any API key (even one with no scopes) could read arbitrary files; #886 proved `/files/download-zip` followed symlinks outside the configured file jail. These tests lock in the fixes and prevent both vulnerabilities from regressing.
- [Filesystem Tools: Read, Write, List, and Edit Files](./filesystem-tools-read-write-list-and-edit-files.md) — The `filesystem.py` module provides four foundational `BaseTool` subclasses — `ReadFileTool`, `WriteFileTool`, `ListDirTool`, and `EditFileTool` — that give the PocketPaw agent controlled access to the local filesystem. All path operations go through an `is_safe_path` guard that enforces the configured `file_jail_path` boundary, preventing agents from reading or writing outside the permitted sandbox.
- [Fleet Installer: Manifest Loading, Path Traversal Guard, and Install Orchestration Tests](./fleet-installer-manifest-loading-path-traversal-guard-and-install-orchestration-.md) — This test suite covers PocketPaw's fleet installer, which provisions a full AI agent environment (soul, pocket, connectors) from a declarative manifest. It validates YAML and JSON manifest loading, a P0 path-traversal security fix that clamps string inputs to the bundled fleet directory, install orchestration with partial failure handling, and the InstallReport shape.
- [Fleet Installer — Template Loading, Path Traversal Guard, and Journal Emission](./fleet-installer-template-loading-path-traversal-guard-and-journal-emission.md) — The Fleet installer loads YAML or JSON fleet templates, installs the bundled soul/pocket/connector/scope configuration, and emits correlated journal events for observability. A critical path-traversal security fix was applied in `fix/fleet-install-auth-guard`: string-path inputs from the REST router are now clamped to the bundled templates directory, preventing a workspace admin from coercing the server into reading arbitrary files.
- [Guardian Agent: Secondary LLM-Based Security Filter for Dangerous Commands](./guardian-agent-secondary-llm-based-security-filter-for-dangerous-commands.md) — The Guardian Agent provides a second-opinion AI security layer that evaluates commands and actions against a SAFE/DANGEROUS classification before execution. It sits above the deterministic regex rails, handling ambiguous cases where pattern matching alone cannot determine intent.
- [Guardian Agent Verification Script — Manual Safety Layer Smoke Test for ShellTool](./guardian-agent-verification-script-manual-safety-layer-smoke-test-for-shelltool.md) — This standalone script manually validates that the Guardian safety layer correctly permits safe shell commands while blocking dangerous ones, providing a human-readable pass/fail report for the tool permission model. It distinguishes between two blocking mechanisms — regex-based pre-screening in ShellTool and the Guardian agent's semantic analysis — to help developers diagnose which layer is active.
- [GuardianAgent Empty Response Regression Tests (Issue #636)](./guardianagent-empty-response-regression-tests-issue-636.md) — This test module documents the fix for issue #636, where an empty `response.content` list from the Anthropic API caused an `IndexError` inside `GuardianAgent.check_command`. The fix implements fail-closed behavior: any empty, malformed, or missing API response defaults to `DANGEROUS` rather than crashing or defaulting to safe.
- [Guards Dependency Factories: FastAPI-Injectable RBAC and ABAC Guards](./guards-dependency-factories-fastapi-injectable-rbac-and-abac-guards.md) — The `deps.py` module generates FastAPI dependency callables that enforce authorization on route handlers, pulling workspace membership and role from `request.state` (populated by upstream middleware) and raising `HTTPException(403)` on any denial. It also provides non-FastAPI helper functions for cloud routes that authenticate via fastapi-users and carry full user models rather than request state.
- [Guards Package: Unified Re-Export for RBAC and ABAC Authorization](./guards-package-unified-re-export-for-rbac-and-abac-authorization.md) — The `guards` package `__init__.py` is a single-point re-export that collects all authorization primitives — roles, access levels, policy evaluation, action rules, FastAPI dependency factories, and audit helpers — under one importable namespace. This prevents callers from needing to know which sub-module owns each symbol, and makes future refactoring transparent to the rest of the codebase.
- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.
- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.
- [Health and Security Audit Schemas](./health-and-security-audit-schemas.md) — Defines Pydantic models for PocketPaw's health-monitoring and security-audit APIs, covering runtime health summaries, error log entries, per-check security results, and self-audit report summaries. These schemas give operators a structured view into the agent's operational integrity.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [HMAC Session Tokens: Stateless Dashboard Authentication with Instant Invalidation](./hmac-session-tokens-stateless-dashboard-authentication-with-instant-invalidation.md) — This module implements stateless session tokens using HMAC-SHA256 with a `{expires_unix}:{hex_hmac}` format, keyed against the master access token. Regenerating the master token instantly invalidates all outstanding sessions without requiring a server-side token store or database.
- [HTTP Utilities — Secure Request Detection via Forwarded Headers](./http-utilities-secure-request-detection-via-forwarded-headers.md) — `http_utils.py` provides `is_request_secure()`, a FastAPI helper that determines whether an incoming request arrived over HTTPS by inspecting both the direct URL scheme and reverse proxy forwarding headers (`X-Forwarded-Proto` and RFC 7239 `Forwarded`). This is essential for correctly setting secure cookies and enforcing HTTPS-only features when PocketPaw runs behind a tunnel or proxy.
- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.
- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.
- [InstallPackageTool: Guardian-Reviewed pip Package Installation](./installpackagetool-guardian-reviewed-pip-package-installation.md) — The `InstallPackageTool` allows the PocketPaw agent to install Python packages via pip at runtime, subject to two security layers: a strict regex that validates the package spec format before any subprocess is spawned, and a Guardian AI review that checks the package name for typosquatting and malicious intent before installation proceeds. It runs with `trust_level = "elevated"` because it can permanently modify the Python environment.
- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.
- [Knowledge Router: Multi-Scope Listing, Filtering, and Cross-Workspace Isolation Tests](./knowledge-router-multi-scope-listing-filtering-and-cross-workspace-isolation-tes.md) — This integration test suite exercises the `/api/v1/knowledge/articles` FastAPI endpoint end-to-end, verifying that it merges workspace and agent scopes, supports `agent_id` filtering, rejects cross-workspace queries with HTTP 403, and returns empty results (not a data leak) for unknown agent IDs.
- [Logging Setup: Rich Console Output with Secret and PII Scrubbing](./logging-setup-rich-console-output-with-secret-and-pii-scrubbing.md) — `logging_setup.py` configures PocketPaw's logging infrastructure with Rich-formatted console output and two security filters: `SecretFilter` (scrubs API key patterns from all log messages) and `PIILogFilter` (opt-in scrubbing of personally identifiable information). These filters prevent credential and user data leakage through log output regardless of log level.
- [Media Attachments: Cross-Channel File Delivery, Path Security, and Tool Result Tagging](./media-attachments-cross-channel-file-delivery-path-security-and-tool-result-tagg.md) — The media attachments test suite validates PocketPaw's end-to-end media pipeline — from tool result tagging via HTML-comment markers, through agent loop extraction, to per-channel delivery (Telegram, Discord, Slack, WhatsApp, WebSocket) and the secure `/api/media` serving endpoint. Path traversal protection for the media endpoint receives dedicated security coverage.
- [Memory Isolation: Sender-Scoped Storage and Identity Injection](./memory-isolation-sender-scoped-storage-and-identity-injection.md) — Tests verifying that PocketPaw's MemoryManager correctly isolates memories by sender identity, so conversations from different users do not bleed into each other. Covers user ID resolution, per-user file routing in FileMemoryStore, and identity injection into system prompts.
- [Mission Control Task Executor: Security, Concurrency, and Streaming](./mission-control-task-executor-security-concurrency-and-streaming.md) — Tests for MCTaskExecutor, the component that runs agent tasks in the background, streams results, and broadcasts events. Covers singleton lifecycle, UUID validation, error sanitization, duplicate-execution prevention, and background task completion.
- [MongoMemoryStore Multi-Tenant Workspace Isolation Tests](./mongomemorystore-multi-tenant-workspace-isolation-tests.md) — These tests verify that MongoMemoryStore stamps workspace_id onto both session messages and fact documents at write time, and that read operations are scoped to the requested workspace to prevent cross-tenant data leakage. Untagged rows with workspace_id=None must never appear in workspace-scoped queries.
- [OAuth Integration: Token Storage, Provider Configuration, and Token Validity](./oauth-integration-token-storage-provider-configuration-and-token-validity.md) — Tests for PocketPaw's OAuth subsystem, covering secure token persistence in TokenStore, provider URL generation via OAuthManager, and token validity checks based on expiry timestamps. Verifies file permissions, non-existent token handling, and graceful degradation for unknown providers.
- [OAuth Token Store: Secure File-Based Credential Persistence](./oauth-token-store-secure-file-based-credential-persistence.md) — The `TokenStore` persists OAuth 2.0 tokens to `~/.pocketpaw/oauth/{service}.json` with strict `0600` file permissions to prevent other OS users from reading credentials. It provides load, save, delete, and list operations over the `OAuthTokens` dataclass that covers the full OAuth 2.0 token surface.
- [OAuth2 Authorization Server: PKCE Flow Implementation](./oauth2-authorization-server-pkce-flow-implementation.md) — `AuthorizationServer` implements the full OAuth2 authorization code + PKCE flow: issuing authorization codes, exchanging them for tokens via S256 challenge verification, refreshing tokens, and revoking tokens. Token lifetimes are 1-hour access / 30-day refresh / 10-minute authorization code.
- [OAuth2 Data Models: Client, Authorization Code, and Token](./oauth2-data-models-client-authorization-code-and-token.md) — Defines the three core data structures for PocketPaw's OAuth2 implementation: `OAuthClient` (registered application with redirect URI validation), `AuthorizationCode` (short-lived PKCE exchange token), and `OAuthToken` (access + refresh token pair). The redirect URI matching logic implements RFC 8252 Section 7.3 for native app loopback flexibility.
- [OAuth2 PKCE Authorization Server Tests: Full Flow, Token Exchange, and Consent](./oauth2-pkce-authorization-server-tests-full-flow-token-exchange-and-consent.md) — Tests PocketPaw's OAuth2 authorization server with PKCE (Proof Key for Code Exchange), covering authorization code issuance, token exchange, token refresh, revocation, and the consent UI endpoints. The tests validate both the server logic class directly and the REST endpoints, ensuring the security guarantees of PKCE are enforced end-to-end.
- [OAuth2 Token Exchange and Revocation Schemas](./oauth2-token-exchange-and-revocation-schemas.md) — Defines the Pydantic models for PocketPaw's OAuth2 token lifecycle — code exchange, token refresh, standard token responses, and revocation. Field-level validation enforces RFC 6749 grant type restrictions at the schema boundary, catching invalid requests before they reach the OAuth2 implementation.
- [OAuth2/PKCE Authorization Server Package](./oauth2pkce-authorization-server-package.md) — The `oauth2` package implements a self-contained OAuth2 authorization server with PKCE support, designed primarily for the PocketPaw Tauri desktop application to authenticate against a locally running PocketPaw instance without exposing a client secret. The package is organized into models, server logic, and storage layers.
- [OCR Tool: Schema Validation, File Jail, and Provider Fallback Tests](./ocr-tool-schema-validation-file-jail-and-provider-fallback-tests.md) — Tests for OCRTool, a built-in agent tool that extracts text from images using OpenAI Vision or Tesseract as fallback. Validates the tool schema, enforces a file-system jail to prevent path traversal, and covers error paths for unsupported formats, oversized files, missing providers, and API failures.
- [Output Redaction: Preventing Secret Leakage in Agent Responses](./output-redaction-preventing-secret-leakage-in-agent-responses.md) — This module provides output-level redaction that strips API keys, tokens, and other credentials from agent response text before it reaches clients. Operating at the message bus level makes it backend-agnostic — the same redaction applies regardless of whether the agent backend is Claude Code, Open Interpreter, or any future addition.
- [Output Redaction Tests: API Keys, Tokens, and Secret Pattern Detection](./output-redaction-tests-api-keys-tokens-and-secret-pattern-detection.md) — PocketPaw's output redaction module (`pocketpaw.security.redact`) scans agent output for API keys, tokens, credentials, and other secrets before they are logged or sent to external systems. These tests validate detection of over a dozen secret patterns across major providers, verify that safe text is not falsely flagged, and confirm the specific scenario of an agent reading a `.env` file.
- [Path Normalization and Validation for the My Files Folder Tree](./path-normalization-and-validation-for-the-my-files-folder-tree.md) — This module provides a small set of path utilities that enforce a strict, well-defined path format for the uploads folder tree. All paths must be absolute, forward-slash separated, with no trailing slash, no traversal sequences, and no control characters — preventing path injection and ensuring consistent storage key formatting across the uploads subsystem.
- [Paw Print Ingest Router: HTTP Surface, CORS, Rate Limiting, and Fabric Mapping Tests](./paw-print-ingest-router-http-surface-cors-rate-limiting-and-fabric-mapping-tests.md) — This test suite covers the HTTP layer of the Paw Print widget system — including owner-authenticated CRUD endpoints, CORS enforcement for the public spec and event ingest routes, payload size limits, per-customer rate limiting, and event-to-Fabric-object mapping via Jinja-style template interpolation.
- [Paw Print Models — Secure Widget Render Vocabulary and Event Types](./paw-print-models-secure-widget-render-vocabulary-and-event-types.md) — Defines the Pydantic models for the Paw Print widget layer with a security-first design: no raw HTML, capped collection sizes, a per-widget rotatable access token, and a 4KB payload cap on inbound events. The models form the contract between the backend and the widget.js bundle.
- [Paw Print Router — HTTP Surface for Widget Management and Event Ingest](./paw-print-router-http-surface-for-widget-management-and-event-ingest.md) — Provides the FastAPI routes for the Paw Print widget layer: public CORS-gated spec serving, owner-authenticated widget CRUD, and a multi-layered event ingest endpoint that enforces domain validation, rate limiting, Guardian screening, and Fabric object mapping before persisting customer interactions.
- [PawKit Store: File-Based Kit Persistence with Path Traversal Protection](./pawkit-store-file-based-kit-persistence-with-path-traversal-protection.md) — The `FileKitStore` manages installed PawKits on disk under `~/.pocketpaw/kits/`, using atomic writes (write-then-rename) and an in-memory index loaded lazily on first access. A March 2026 security hardening pass added input sanitization to prevent path traversal attacks via malicious kit IDs or data source names.
- [PII Detection and Masking: Regex-Based Personal Data Protection Layer](./pii-detection-and-masking-regex-based-personal-data-protection-layer.md) — This module provides a configurable PII detection and masking system using pre-compiled regex patterns for common personal data types (emails, phone numbers, SSNs, etc.). It supports per-type action overrides — mask, redact, hash, or passthrough — and integrates as both a standalone scanner and a subsystem of the audit logger's PII filter.
- [PII Scanner Tests: Detection, Actions, False Positives, and Singleton Pattern](./pii-scanner-tests-detection-actions-false-positives-and-singleton-pattern.md) — PocketPaw's PII scanner (`pocketpaw.security.pii`) detects sensitive personal information in text before it reaches logs, LLM context, or external storage. These tests validate detection of SSNs, emails, phones, credit cards, IP addresses, and dates of birth, verify that safe text generates no false positives, and confirm the singleton pattern for the default scanner instance.
- [Plan Mode — Human-in-the-Loop Approval Flow for Agent Tool Execution](./plan-mode-human-in-the-loop-approval-flow-for-agent-tool-execution.md) — Implements `PlanManager` and `ExecutionPlan`, which intercept agent tool calls and hold them pending user approval before execution. Plans have a per-session lifecycle with timeout-based auto-rejection, providing an auditable gate between agent intent and system-modifying actions.
- [PolicyContext and PolicyResult: Authorization Decision Data Types](./policycontext-and-policyresult-authorization-decision-data-types.md) — `policy.py` defines the two frozen dataclasses that form the data contract for the ABAC evaluation pipeline: `PolicyContext` bundles everything a guard needs to make a decision, and `PolicyResult` carries the outcome with a machine-readable code. Their immutability and slot optimization make them safe to pass across async boundaries without defensive copying.
- [Prompt Injection Scanner: Two-Tier Heuristic and LLM Detection](./prompt-injection-scanner-two-tier-heuristic-and-llm-detection.md) — This module implements a two-tier prompt injection detection system: fast regex heuristics (~20 patterns) as Tier 1 for known attack signatures, and an optional LLM-based deep scan as Tier 2 for novel or obfuscated injections. It normalizes Unicode before scanning to defeat homoglyph substitution attacks.
- [Rate Limiter, Audit API Events, and OpenAPI Config Tests](./rate-limiter-audit-api-events-and-openapi-config-tests.md) — Tests PocketPaw's token-bucket rate limiter, the `RateLimitInfo` response model and its header formatting, audit logging for API events (OAuth and key revocation), and the OpenAPI endpoint configuration. Together these tests ensure the API protects against request floods, produces correct rate limit headers, and maintains a searchable audit trail.
- [RBAC Action Matrix Tests: Parametrized Role Enforcement and Coverage Guard](./rbac-action-matrix-tests-parametrized-role-enforcement-and-coverage-guard.md) — A parametrized test matrix that exercises every entry in the canonical `ACTIONS` table against every role in the matching role family, verifying that `check_action` allows or raises `Forbidden` with the correct deny code. A meta-test enforces that no new action can be added without both an allow-path and a deny-path assertion, preventing silent coverage gaps.
- [RBAC Audit Helpers: Structured Denial and Privileged Action Logging](./rbac-audit-helpers-structured-denial-and-privileged-action-logging.md) — The `audit.py` module provides two thin wrapper functions — `log_denial` and `log_privileged_action` — that emit structured `AuditEvent` records whenever authorization decisions block or allow a sensitive operation. These wrappers ensure that RBAC events are logged at the correct severity level with machine-readable codes that can be queried, alerted on, or exported to a SIEM.
- [Remote Access Auth Tests: Token Generation, Middleware, and QR Endpoint Security](./remote-access-auth-tests-token-generation-middleware-and-qr-endpoint-security.md) — PocketPaw's dashboard uses a file-backed token for remote access authentication. These tests validate token generation and persistence, the auth middleware's enforcement of Bearer token and query parameter authentication, and a security fix (issue #854) that closed an unauthenticated `/api/qr` endpoint.
- [Room-Scoped WebSocket Routing Tests](./room-scoped-websocket-routing-tests.md) — This module tests the room-scoped routing layer in ConnectionManager, which limits typing indicators and read receipts to only the sockets currently joined to a specific group. It also verifies that non-members cannot inject typing or read-ack events into rooms they do not belong to.
- [Root Pytest Configuration: SSRF Relaxation, Scope Bypass, and Audit Log Isolation](./root-pytest-configuration-ssrf-relaxation-scope-bypass-and-audit-log-isolation.md) — The root `tests/conftest.py` installs three session-wide fixtures that configure PocketPaw's test environment: it relaxes SSRF URL validation so internal URLs work during tests, bypasses scope enforcement for non-security tests, and redirects the audit log to a temp file so tests never write to the developer's home directory.
- [RunPythonTool: Guardian-Reviewed Sandboxed Python Execution](./runpythontool-guardian-reviewed-sandboxed-python-execution.md) — The `RunPythonTool` lets the PocketPaw agent execute arbitrary Python code in a sandboxed subprocess, subject to a Guardian AI safety review before execution. The sandbox uses the configured `file_jail_path` as its working directory, and UUID-based script filenames prevent filename collisions when multiple executions overlap. Trust level is `elevated` because arbitrary code execution is the most powerful and dangerous capability an agent can have.
- [RunPythonTool Test Suite — Sandboxed Code Execution with Guardian and File Jail](./runpythontool-test-suite-sandboxed-code-execution-with-guardian-and-file-jail.md) — This test file covers `RunPythonTool`, which executes arbitrary Python code in a sandboxed subprocess controlled by a file jail and a guardian policy engine. Tests validate stdout capture, stderr surfacing, exit code reporting, timeout enforcement, guardian blocking, filesystem isolation, temp file cleanup, and tool metadata.
- [Runtime Audit API Integration Tests — Workspace Rollup, Search, CSV Export, and Security](./runtime-audit-api-integration-tests-workspace-rollup-search-csv-export-and-secur.md) — This test module validates the canonical `/runtime/audit` endpoint introduced in Cluster C/PR4, covering workspace-scoped filtering, full-text search with length caps, CSV export, SQL injection resistance, and the deprecation forwarding behavior of the legacy `/audit` alias. It exercises both the new runtime router and the legacy router mounted together to prove they share the same underlying AuditStore and scope guard.
- [Scope Enforcement and Tool Policy Fail-Closed Tests](./scope-enforcement-and-tool-policy-fail-closed-tests.md) — This test module validates that PocketPaw's `require_scope` FastAPI dependency rejects requests that lack explicit authorization markers, preventing silent scope bypass via master, session, or cookie auth. It also verifies that `ToolPolicy` raises on unknown profile names rather than defaulting to an open or undefined state.
- [Security Audit CLI: Runtime Health Checks and Permission Repair](./security-audit-cli-runtime-health-checks-and-permission-repair.md) — This module provides an async CLI-callable security auditor that checks file permissions, validates the audit log setup, and optionally scans memory files for PII. It was built as a "Phase 1 Quick Wins" hardening measure to catch misconfigurations before they become vulnerabilities.
- [Security Audit CLI Tests — Config Permissions, API Key Exposure, and Audit Runner](./security-audit-cli-tests-config-permissions-api-key-exposure-and-audit-runner.md) — This test file validates PocketPaw's security audit command-line tool, which checks for world-readable config files, plaintext API keys, missing audit logs, unreachable guardian agents, invalid file jails, risky tool profiles, and dangerous permission bypasses. Tests cover both detection and auto-fix capabilities.
- [Security Package Entry Point: Audit, Guardian, and PII Surface](./security-package-entry-point-audit-guardian-and-pii-surface.md) — The pocketpaw.security package re-exports three security subsystems through its __init__.py: AuditLogger for structured event logging with severity levels, GuardianAgent for content policy enforcement, and PIIScanner for detecting and acting on personally identifiable information in agent inputs and outputs.
- [Security Rails: Single Source of Truth for Dangerous Command Patterns](./security-rails-single-source-of-truth-for-dangerous-command-patterns.md) — This module defines the canonical set of dangerous command patterns used across every security layer in PocketPaw — Guardian, ShellTool, native agent, and Claude SDK hooks all import from here. Centralizing patterns in one place eliminates the drift and inconsistency that would arise from each subsystem maintaining its own ad-hoc block list.
- [Security Rails Tests: Dangerous Command Blocking, Bypass Detection, and Safe Allowlist](./security-rails-tests-dangerous-command-blocking-bypass-detection-and-safe-allowl.md) — Layer 5 of PocketPaw's security stack (`pocketpaw.security.rails`) blocks dangerous shell commands before they reach the execution layer. These tests validate the blocking of destructive file operations, remote code execution patterns, obfuscation bypasses, privilege escalation, data exfiltration, and system damage commands, while confirming that safe commands are not falsely blocked.
- [Security Scrub: PII and Secret Masking in Logs and Tool Parameters](./security-scrub-pii-and-secret-masking-in-logs-and-tool-parameters.md) — The logging scrub test suite validates PocketPaw's defense-in-depth approach to preventing secrets and personally identifiable information from appearing in logs, audit trails, and tool execution records. Tests cover pattern-based field masking, nested dict traversal, audit log fallback behavior, and dangerous-command detection logging introduced in security sprint cluster C.
- [Self-Audit Daemon Tests — Stale Sessions, Config Conflicts, Disk Usage, and OAuth Tokens](./self-audit-daemon-tests-stale-sessions-config-conflicts-disk-usage-and-oauth-tok.md) — This test file covers `pocketpaw.daemon.self_audit`, a background health-check daemon that monitors for stale sessions, dangerous config combinations, excessive disk usage, oversized audit logs, and orphaned OAuth tokens. Tests validate each individual check and the full `run_self_audit` orchestrator.
- [Session Index, REST Endpoints, WebSocket Switching, and Search Tests](./session-index-rest-endpoints-websocket-switching-and-search-tests.md) — This test file comprehensively covers `FileMemoryStore`'s session index subsystem: atomic index writes, index rebuild from disk, CRUD operations, WebSocket session switching, path traversal blocking, full-text session search, and the REST API endpoints that expose these operations to the dashboard.
- [Settings API Security Tests — Secret Field Filtering and Immutable Field Guards](./settings-api-security-tests-secret-field-filtering-and-immutable-field-guards.md) — This test module validates two critical security properties of the `/api/v1/settings` router: that GET never returns API keys or tokens (regression for a v0.4.16 leak), and that PUT rejects writes to security-critical immutable fields like `file_jail_path` and `bypass_permissions`. It also verifies normal settings read and write paths work correctly for non-secret, non-immutable fields.
- [Settings REST Router with Security Field Filtering](./settings-rest-router-with-security-field-filtering.md) — Exposes GET and PUT REST endpoints for reading and updating PocketPaw's runtime configuration, with two hard-coded protection layers: secrets are never returned over the wire, and a set of security-critical fields cannot be modified via the API at all. An asyncio lock prevents concurrent write races.
- [ShellTool: Sandboxed Shell Command Execution with Guardian Approval](./shelltool-sandboxed-shell-command-execution-with-guardian-approval.md) — `ShellTool` wraps `asyncio.subprocess` with two security layers — a precompiled dangerous-pattern regex rail and a Guardian Agent check — before executing any shell command. It operates at `critical` trust level, the highest in PocketPaw's trust hierarchy, and defaults to a jailed working directory.
- [Skill Installer: Secure GitHub-to-Disk Skill Installation with Audit Trail](./skill-installer-secure-github-to-disk-skill-installation-with-audit-trail.md) — This module handles the full lifecycle of installing skills from GitHub repositories: validating the source string, cloning to a temp directory, stripping symlinks, copying SKILL.md directories to the install location, and emitting audit events for every installation attempt. Symlink stripping is the key defensive measure against path traversal attacks via malicious repositories.
- [Skills API Security and Installation Tests — Input Validation, Injection Prevention, and Audit Logging](./skills-api-security-and-installation-tests-input-validation-injection-prevention.md) — This test module covers the full lifecycle of the skills management API: listing, searching, installing from GitHub sources, removing, and reloading. The installation tests are especially dense, exercising a whitelist-based input validator to block path traversal, shell injection, null bytes, and over-segmented source strings before any subprocess is spawned.
- [Speech-to-Text Tool Tests: Schema, File Jail, Multi-Provider, and Error Handling](./speech-to-text-tool-tests-schema-file-jail-multi-provider-and-error-handling.md) — Tests for PocketPaw's `SpeechToTextTool`, covering schema validation, file path jailing for security, size limits, successful transcription with both OpenAI Whisper and ElevenLabs providers, language parameter forwarding, and graceful handling of missing API keys and provider errors.
- [SpeechToTextTool: Multi-Provider Audio Transcription with Path Jail Enforcement](./speechtotexttool-multi-provider-audio-transcription-with-path-jail-enforcement.md) — `SpeechToTextTool` transcribes audio files to text via OpenAI Whisper, ElevenLabs, or Sarvam AI Saaras, with automatic provider selection from settings. A path-safety check (`is_safe_path`) prevents directory traversal attacks before any file is read, and transcripts are written to a versioned output directory.
- [SQLite-Backed Audit Log Store](./sqlite-backed-audit-log-store.md) — Implements the `AuditStore` class that persists audit entries to a SQLite database using the stdlib `sqlite3` module with async-friendly execution via `run_in_executor`. Provides filtered querying, full-text search with injection-safe LIKE escaping, CSV/JSON export, and convenience methods for common audit event types.
- [SSRF URL Validation Tests: Blocking Internal and Malicious URLs in Settings](./ssrf-url-validation-tests-blocking-internal-and-malicious-urls-in-settings.md) — This test file, added during security sprint cluster E (issue #703), validates the `validate_external_url` function that guards all URL-typed settings fields against Server-Side Request Forgery (SSRF) attacks. It covers rejection of cloud metadata endpoints, RFC 1918 ranges, non-HTTP schemes, and malformed inputs, while confirming a developer-friendly escape hatch for local development.
- [Storage Key Generation: Namespaced, Date-Bucketed, UUID-Based Keys for Upload Blobs](./storage-key-generation-namespaced-date-bucketed-uuid-based-keys-for-upload-blobs.md) — The `keys.py` module generates opaque storage keys with a predictable three-part structure -- kind, year-month bucket, and a UUID4 hex suffix -- that supports human-readable organization, time-based lifecycle policies, and guaranteed uniqueness. The companion `sanitize_ext()` function normalizes file extensions to prevent path injection through malformed extension strings.
- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.
- [Token-Bucket Rate Limiter: Stdlib-Only DoS Protection for the Web Dashboard](./token-bucket-rate-limiter-stdlib-only-dos-protection-for-the-web-dashboard.md) — This module implements an in-memory token-bucket rate limiter using only Python's standard library, providing pre-configured tiers for API endpoints, authentication, WebSocket connections, and per-API-key limiting. The zero-dependency design makes it suitable for embedding directly in the dashboard without adding external Redis or other state store requirements.
- [Tool Bridge: Adapting PocketPaw Tools for Multiple Agent SDK Formats](./tool-bridge-adapting-pocketpaw-tools-for-multiple-agent-sdk-formats.md) — The tool bridge discovers all registered PocketPaw tools and wraps them in the native function-tool format expected by each agent backend (OpenAI Agents SDK `FunctionTool`, Google ADK `FunctionTool`, LangChain `StructuredTool`). It enforces backend-aware exclusion rules — the Claude Agent SDK gets shell/FS/edit tools excluded because the CLI provides them natively — and scans tool outputs for injection attacks before returning results to the agent.
- [Tool Policy System: Profile-Based Access Control and MCP Server Authorization](./tool-policy-system-profile-based-access-control-and-mcp-server-authorization.md) — The tool policy system (`tools/policy.py`) enforces which tools an agent instance is allowed to use, based on named profiles (`minimal`, `coding`, `full`), explicit allow/deny lists, and group references. Tests cover profile resolution, group expansion, allow/deny precedence, ToolRegistry integration, and MCP server authorization — including a critical fix for fail-closed behavior on unknown profile names.
- [Tool Policy System: Profile-Based Access Control for Agent Tools](./tool-policy-system-profile-based-access-control-for-agent-tools.md) — The tool policy module implements a layered access control system that determines which tools an agent backend is allowed to use, using named profiles, logical tool groups, and explicit allow/deny lists with a fixed precedence order. It prevents misconfiguration from silently granting full access and supports fine-grained control over MCP server tools using wildcard patterns.
- [Tool Protocol and Registry: Core Registration, Execution, and Filesystem Jail Tests](./tool-protocol-and-registry-core-registration-execution-and-filesystem-jail-tests.md) — These tests exercise the foundational `ToolRegistry` and `BaseTool` protocol, the `ShellTool`'s command-blocking and timeout behavior, and the filesystem tools' write/read/list operations within a file jail. The jail fixture pattern and dangerous-command blocking together form PocketPaw's first line of defense against unsafe tool invocations.
- [Tool Registry: Centralized Execution Hub with Policy Enforcement and Audit Logging](./tool-registry-centralized-execution-hub-with-policy-enforcement-and-audit-loggin.md) — The `ToolRegistry` is PocketPaw's central runtime for tool management -- it stores registered tools, enforces `ToolPolicy` access control, validates parameters, executes tools under a configurable timeout, scrubs sensitive data from logs, and writes to the audit trail. Multiple iterative hardening passes reflect its role as the primary security enforcement point at execution time.
- [Tool Registry Validation: Required Parameter Guards and Audit Logging (Issue #793)](./tool-registry-validation-required-parameter-guards-and-audit-logging-issue-793.md) — These tests cover the fix for issue #793, where `ToolRegistry.execute` was accepting empty strings and whitespace-only values for required tool parameters, allowing agent-generated calls to silently bypass input validation. The fix adds strict pre-execution validation that rejects `None`, missing, empty, and whitespace-only values for string parameters marked as required, and emits an audit log entry on failure.
- [Unified Agent Loop — Core Orchestrator for Message Processing and Agent Execution](./unified-agent-loop-core-orchestrator-for-message-processing-and-agent-execution.md) — Implements `AgentLoop`, the central orchestrator that consumes messages from the bus, routes them through `AgentRouter` to the configured backend, applies security scanning and PII redaction, stores memories, and streams responses back to channels. It also manages the soul's cognitive pipeline and per-session concurrency locks.
- [Upload Configuration: Size Limits, MIME Allowlist, and Inline Rendering Rules](./upload-configuration-size-limits-mime-allowlist-and-inline-rendering-rules.md) — The `uploads/config.py` module centralizes all static configuration for the upload pipeline -- maximum file size, per-batch file count, allowed MIME types, and local storage root -- while also defining which MIME types are safe for inline browser rendering versus forcing an attachment download. The `extension_for()` helper maps canonical MIME types to file extensions for consistent key generation.
- [Upload Grant Signing Tests: HMAC Token Integrity and Expiry](./upload-grant-signing-tests-hmac-token-integrity-and-expiry.md) — This small but security-critical test module verifies the `sign_grant` / `verify_grant` pair in PocketPaw's upload subsystem, ensuring that download grants are cryptographically bound to a specific file ID, expire correctly, and cannot be forged or transplanted across files.
- [Upload Grant Tokens: HMAC-Signed Short-Lived URL Access](./upload-grant-tokens-hmac-signed-short-lived-url-access.md) — The signing module issues and verifies HMAC-SHA256 grant tokens that allow unauthenticated HTTP access to a specific upload for a limited time window. Grants are designed for use in raw `<img src>` and `<a href download>` attributes where an HTTP `Authorization` header cannot be sent.
- [Upload Path Utility Tests: Normalization, Traversal Defense, Subpath Checks, and Join Safety](./upload-path-utility-tests-normalization-traversal-defense-subpath-checks-and-joi.md) — Exhaustive unit tests for the `ee.cloud.uploads.paths` module, which provides the safe path utilities used throughout the upload folder system. Tests cover normalization (canonicalization, dot-stripping, repeated-slash collapse), rejection of path traversal sequences and dangerous characters, subpath relationship checks, and safe join operations.
- [Upload Read Authorization Tests: Owner, Chat Member, Workspace Admin, and Existence-Hiding](./upload-read-authorization-tests-owner-chat-member-workspace-admin-and-existence-.md) — Tests for `EEUploadService._assert_can_read`, the read gate used by `stream`, `presigned_get`, and the `/download-url` and `/grant` endpoints. The parametrized suite verifies that the owner, chat members, and workspace admins can read while strangers receive `NotFound` (not `Forbidden`) — an existence-hiding design that prevents file-ID probing.
- [Upload Service Tests: Validation, Bulk Handling, and Streaming](./upload-service-tests-validation-bulk-handling-and-streaming.md) — This test suite exercises the `UploadService` layer in PocketPaw's file upload subsystem, covering single and bulk file ingestion, magic-byte MIME sniffing, path sanitisation, and ownership-gated streaming and deletion. It uses an in-memory `_FakeAdapter` to isolate tests from real storage backends while still exercising the full service contract.
- [URL Validators: SSRF Protection for Settings Fields](./url-validators-ssrf-protection-for-settings-fields.md) — This module provides a Pydantic validator (`validate_external_url`) that guards Settings URL fields against Server-Side Request Forgery by blocking internal, loopback, and private-range hostnames. An environment variable escape hatch allows operators to opt in to internal URLs for legitimate local deployments.
- [UrlExtractTool: SSRF Defense, DNS Pinning, Private IP Blocking, and Multi-Provider Extraction](./urlextracttool-ssrf-defense-dns-pinning-private-ip-blocking-and-multi-provider-e.md) — The `UrlExtractTool` fetches and extracts text from URLs, supporting Parallel AI (cloud) and local httpx (self-hosted) providers with auto-selection. The test suite places particular emphasis on SSRF (Server-Side Request Forgery) defenses: private IP blocking, DNS TOCTOU race condition prevention via IP pinning, and IPv4-mapped IPv6 address rejection.
- [UrlExtractTool: SSRF-Safe URL Content Extraction with IP Pinning and Parallel AI Fallback](./urlextracttool-ssrf-safe-url-content-extraction-with-ip-pinning-and-parallel-ai-.md) — `UrlExtractTool` extracts clean text from URLs using Parallel AI's extract API as the primary path and a secure local HTTP fetch as fallback. The local path resolves DNS before connecting and validates the resolved IP against a public-address allowlist, preventing SSRF attacks against private infrastructure.
- [V1 Router Registration and CORS Configuration Tests](./v1-router-registration-and-cors-configuration-tests.md) — Validates that all expected domain routers are registered in PocketPaw's v1 API surface and that CORS origins include the Tauri desktop app origin. These tests act as a registry contract — any new router added to the system must be explicitly listed, preventing silent omissions that would cause 404s from newly added features.
- [Web Server Security Tests: Bot Token Redaction in /setup Errors](./web-server-security-tests-bot-token-redaction-in-setup-errors.md) — This module tests that the `/setup` endpoint in PocketPaw's web server never leaks the Telegram bot token in HTTP error responses (F-07, issue #445). It verifies that when the upstream Telegram API returns an error whose message embeds the token URL, the response is redacted before reaching the client.
- [Webhook Slot Management Router](./webhook-slot-management-router.md) — Manages the lifecycle of PocketPaw's inbound webhook slots — creating named endpoints, listing them with dynamically constructed URLs, removing them, and rotating their HMAC secrets. All operations require the `admin` scope, reflecting that webhook configuration controls which external systems can trigger agent workflows.
- [Webhook Slot Schema Contracts](./webhook-slot-schema-contracts.md) — Defines the Pydantic models that represent a webhook slot's configuration and the request payloads for creating or identifying webhooks. The name pattern constraint prevents injection-prone characters from reaching URL-based routing.
- [Webhooks API Tests — CRUD, Secret Redaction, Duplicate Prevention, and Secret Rotation](./webhooks-api-tests-crud-secret-redaction-duplicate-prevention-and-secret-rotatio.md) — This test module covers the full lifecycle of PocketPaw's webhook configuration API: listing (with secret redaction), adding (with name validation and duplicate prevention), removing, and regenerating shared secrets. Webhooks enable external services to trigger agent actions, and the tests enforce that secrets are never exposed at rest through the API listing endpoint.
- [Workspace Message Search Tests](./workspace-message-search-tests.md) — This test module covers MessageService's workspace-wide message search, verifying that results respect group visibility (channels vs. private rooms), workspace scope isolation, regex-safety of user queries, and server-side result limits. Tests run against an in-memory MongoDB via the shared beanie_memory_db fixture to exercise real Mongo query behavior.
- [Workspace-Scoped Upload URL Resolver with Cross-Tenant Isolation](./workspace-scoped-upload-url-resolver-with-cross-tenant-isolation.md) — EEUploadResolver translates opaque upload URLs into local filesystem paths while enforcing workspace isolation — a lookup for a file that exists in a different workspace returns `None`, preventing existence leaks across tenants. It is used by agents and media processors that need to access uploaded file content.

### `self-hosted inference` — 1 article(s)

- [OpenAICompatibleAdapter: Generic OpenAI-Compatible Endpoint Support](./openaicompatibleadapter-generic-openai-compatible-endpoint-support.md) — `OpenAICompatibleAdapter` provides a catch-all provider adapter for any service that exposes an OpenAI-compatible REST API — vLLM, LocalAI, Together AI, Anyscale, and similar endpoints. It reads `openai_compatible_base_url` and `openai_compatible_api_key` from settings and constructs both `AsyncOpenAI` and `AsyncAnthropic` clients pointing at that endpoint.

### `semantic search` — 1 article(s)

- [ChromaAdapter: Pluggable Chroma Vector Store with Multi-Provider Embeddings](./chromaadapter-pluggable-chroma-vector-store-with-multi-provider-embeddings.md) — ChromaAdapter wraps ChromaDB's persistent local vector store behind the `VectorStoreProtocol` interface and supports five embedding providers—Chroma's default SentenceTransformer, HuggingFace, OpenAI, Google Gemini, and Voyage AI. Blocking Chroma calls are pushed off the asyncio event loop via `asyncio.to_thread`, keeping the agent loop non-blocking.

### `serialization` — 1 article(s)

- [Deep Work Task Model Extensions: New Field Tests](./deep-work-task-model-extensions-new-field-tests.md) — This suite tests the Deep Work extensions added to the core Task model in PocketPaw's mission control system, specifically the five new fields—project_id, task_type, blocks, active_description, and estimated_minutes—that enable multi-task project management. It verifies defaults, settability, serialization, backward compatibility, and round-trip fidelity.

### `server` — 1 article(s)

- [Desktop Launcher Server Manager: Process Lifecycle, Health Checks, and PID File Management](./desktop-launcher-server-manager-process-lifecycle-health-checks-and-pid-file-man.md) — The `ServerManager` class controls the lifecycle of the PocketPaw FastAPI server as a subprocess launched from the desktop launcher. It handles start, stop, restart, health polling, stale PID file cleanup, and dynamic port allocation — all designed to survive launcher restarts and OS-level process deaths.

### `server lifecycle` — 1 article(s)

- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.

### `server management` — 1 article(s)

- [MCP Manager: Server Lifecycle, Tool Discovery, Config Persistence, and HTTP Transport Auto-Detection](./mcp-manager-server-lifecycle-tool-discovery-config-persistence-and-http-transpor.md) — The MCP Manager test suite (Sprint 16) covers the full lifecycle of MCP server connections — config serialization, parallel server startup, tool discovery, tool invocation, graceful shutdown, and the Streamable-HTTP-to-SSE fallback protocol negotiation. All MCP SDK imports are mocked as the `mcp` package is optional.

### `server startup` — 1 article(s)

- [PocketPaw CLI Entry Point and Server Launcher](./pocketpaw-cli-entry-point-and-server-launcher.md) — The `__main__.py` module is PocketPaw's CLI entry point, handling argument parsing, subcommand dispatch, server startup with automatic port fallback, and channel mode selection. It has evolved significantly from a simple server launcher into a full-featured CLI with subcommands for diagnostics, memory inspection, session management, configuration, and log tailing.

### `service` — 1 article(s)

- [Unified Files Service: Merging Upload and Drive Sources](./unified-files-service-merging-upload-and-drive-sources.md) — Implements `UnifiedFilesService`, the Cluster E sub-PR 4 facade that merges chat S3 uploads and (stub) Google Drive files into a single flat list for the legacy `GET /files` endpoint. A `_dedupe` function drops duplicates by content identity to prevent the same file appearing twice when sources overlap.

### `service layer` — 5 article(s)

- [EE Upload Service: Workspace-Scoped Pipeline over the OSS Upload Engine](./ee-upload-service-workspace-scoped-pipeline-over-the-oss-upload-engine.md) — EEUploadService wraps the OSS UploadService to add workspace scoping, per-file access control, Mongo metadata persistence, and real-time WebSocket events for file lifecycle. It substitutes a null metadata store at the OSS layer so that all persistence flows through MongoDB rather than the JSONL flat file.
- [PocketService: Business Logic for Pocket Lifecycle, Widgets, Sharing, and Agents](./pocketservice-business-logic-for-pocket-lifecycle-widgets-sharing-and-agents.md) — PocketService is the stateless service class that encapsulates all pocket business logic — from CRUD and widget management through share link generation, collaborator access, and agent-generated pocket creation from ripple specs. Access control is enforced by two helper functions that distinguish owner-only actions from edit-access actions, and every mutation emits events to the realtime bus and shared event bus.
- [SessionService: Session Lifecycle, History, and Realtime Event Integration](./sessionservice-session-lifecycle-history-and-realtime-event-integration.md) — SessionService is the stateless service class handling the full lifecycle of chat sessions — creating with upsert support for runtime sessions, listing by agent or pocket, retrieving message history from MongoDB, soft-deleting, and emitting realtime events after each mutation. A touch method provides a lightweight activity heartbeat that increments message count and updates lastActivity.
- [UploadService: Validation, Storage, and Metadata Persistence for File Uploads](./uploadservice-validation-storage-and-metadata-persistence-for-file-uploads.md) — UploadService is the application-layer coordinator for file uploads—it validates MIME types and sizes, delegates byte storage to the active StorageAdapter, and writes FileRecord metadata to the persistent store. It exposes both single-file and bulk-upload paths, collecting partial failures without aborting the whole batch.
- [Workspace Service — Business Logic for Workspaces, Members, and Invites](./workspace-service-business-logic-for-workspaces-members-and-invites.md) — The `WorkspaceService` class is a stateless async service that encapsulates all business logic for the workspace domain, including workspace lifecycle, member management, invite flow, and realtime event emission. It enforces several data-level invariants (slug uniqueness, seat limits, owner demotion guard) that cannot be expressed as route-level permission checks.

### `service pattern` — 1 article(s)

- [Realtime emit() Facade: Fire-and-Forget Event Dispatch After Mutations](./realtime-emit-facade-fire-and-forget-event-dispatch-after-mutations.md) — The `emit` function is a thin, fire-and-forget facade that service methods call after successful database writes to push realtime events to connected clients. It never raises exceptions back to the caller — a delivery failure must not roll back or abort the preceding database mutation — while a missing bus initialisation surfaces immediately as an AssertionError.

### `services` — 1 article(s)

- [Notification Service: CRUD and Realtime Fan-Out](./notification-service-crud-and-realtime-fan-out.md) — The `NotificationService` is a stateless class providing four async operations — create, list, mark-read, and clear-all — each of which writes to MongoDB via Beanie and immediately emits a corresponding realtime event for connected WebSocket clients. The service is the single point of truth for notification lifecycle, keeping routers and other domain services free of MongoDB and emit logic.

### `session history` — 1 article(s)

- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.

### `session management` — 11 article(s)

- [Browser Session Management Tests: Lifecycle, Idle Cleanup, Singleton, and Concurrency](./browser-session-management-tests-lifecycle-idle-cleanup-singleton-and-concurrenc.md) — This test suite validates `BrowserSessionManager`, which maintains a pool of named `BrowserSession` objects backed by individual `BrowserDriver` instances. Tests cover session creation, reuse, idle timeout cleanup, graceful close, the global singleton accessor, and thread-safety under concurrent `get_or_create` calls.
- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.
- [MongoMemoryStore Session Index Shape and Filtering Contract Tests](./mongomemorystore-session-index-shape-and-filtering-contract-tests.md) — These tests verify `_load_session_index_async` in MongoMemoryStore, which powers the `GET /sessions/runtime` endpoint. They pin the exact dict shape, filtering of group and soft-deleted sessions, channel inference from session key prefix, and empty title coercion.
- [MongoMemoryStore SESSION Path: Protocol Conformance and Adapter-Specific Read Tests](./mongomemorystore-session-path-protocol-conformance-and-adapter-specific-read-tes.md) — These tests cover the full SESSION memory path in MongoMemoryStore — CRUD, chronological message retrieval, session key isolation, combined session-with-messages reads with limit behavior, and cross-collection isolation between pocket and group messages.
- [MongoMemoryStore Session Upkeep: messageCount and lastActivity Auto-Maintenance Tests](./mongomemorystore-session-upkeep-messagecount-and-lastactivity-auto-maintenance-t.md) — These tests verify that MongoMemoryStore automatically updates Session metadata (messageCount, lastActivity) on every save and auto-creates missing Session rows when a message arrives before the session is explicitly created. They pin behaviors introduced after a historical refactor that removed a broken parallel bus subscriber.
- [Session Index, REST Endpoints, WebSocket Switching, and Search Tests](./session-index-rest-endpoints-websocket-switching-and-search-tests.md) — This test file comprehensively covers `FileMemoryStore`'s session index subsystem: atomic index writes, index rebuild from disk, CRUD operations, WebSocket session switching, path traversal blocking, full-text session search, and the REST API endpoints that expose these operations to the dashboard.
- [Session Management API Tests — List, Delete, Title Update, and Search](./session-management-api-tests-list-delete-title-update-and-search.md) — This test module covers all four operations of the `/api/v1/sessions` router: listing sessions with pagination, deleting by ID, updating titles, and full-text searching session history. It uses mock-based isolation of the underlying memory store to prevent filesystem side effects while exercising the router's error-handling and edge-case paths.
- [Session Management Tool Tests — New, List, Switch, Clear, Rename, and Delete](./session-management-tool-tests-new-list-switch-clear-rename-and-delete.md) — This test file validates the six session management tools in `pocketpaw.tools.builtin.sessions`: `NewSessionTool`, `ListSessionsTool`, `SwitchSessionTool`, `ClearSessionTool`, `RenameSessionTool`, and `DeleteSessionTool`. Tests cover tool naming, happy-path behavior, error handling, and policy group membership.
- [Smoke Test: Pocket Sidebar Session Creation and Chat Persistence](./smoke-test-pocket-sidebar-session-creation-and-chat-persistence.md) — This test mirrors the full flow of PocketPaw's enterprise sidebar: a user creates a Pocket, opens a new session under that Pocket, sends a message, and verifies the message appears in the session history API. It validates that `Session` documents are correctly linked to their parent Pocket in MongoDB.
- [Smoke Test: Session Creation Then Chat Persistence Round-Trip](./smoke-test-session-creation-then-chat-persistence-round-trip.md) — This test validates the core "desired flow": create a session via the API, send a user message, confirm it is stored in MongoDB keyed by the session ID, and retrieve it through the history endpoint. It was written to capture the exact behavior contract for session-scoped message storage.
- [Smoke Test: Session Key Normalization Between Agent Bus and UI Chat History](./smoke-test-session-key-normalization-between-agent-bus-and-ui-chat-history.md) — This test verifies that the colon-delimited session key written by the agent loop (`websocket:X`) is normalized to the underscore form used by the UI (`websocket_X`), so that chat history returned by `GET /api/v1/sessions/{sessionId}/history` actually contains the messages the agent wrote. Without this normalization, the session history API returns empty results despite messages being stored.

### `session-management` — 5 article(s)

- [Browser Session Manager — Named Session Pool with Idle Cleanup](./browser-session-manager-named-session-pool-with-idle-cleanup.md) — BrowserSession and BrowserSessionManager provide a named pool of persistent browser sessions so that agent tools can reuse an open browser across multiple tool calls without launching and closing a new browser for every interaction. Per-session asyncio locks prevent race conditions when concurrent tool calls reference the same session, and idle cleanup reclaims resources from sessions that have not been used within a configurable timeout.
- [Cross-Channel Slash Command Handler](./cross-channel-slash-command-handler.md) — CommandHandler intercepts slash commands from any channel (Telegram, Slack, WhatsApp, CLI, etc.) and processes them directly without invoking the AI agent backend. It manages session lifecycle operations — creating, listing, resuming, renaming, and deleting conversation sessions — as well as runtime configuration of backend, model, and tool settings.
- [Deep Work Session — Full Project Lifecycle Orchestrator](./deep-work-session-full-project-lifecycle-orchestrator.md) — DeepWorkSession is the top-level coordinator for a Deep Work project, tying together GoalParser, PlannerAgent, DependencyScheduler, MCTaskExecutor, and HumanTaskRouter into a single class that manages a project from user input through goal analysis, LLM planning, approval, execution, pause, resume, and cancellation. It also handles crash recovery for interrupted projects on startup.
- [OpenAI Agents SDK Backend — GPT and Ollama Agent Support with SQLite Session Persistence](./openai-agents-sdk-backend-gpt-and-ollama-agent-support-with-sqlite-session-persi.md) — Implements `OpenAIAgentsBackend` using OpenAI's `openai-agents` SDK, supporting GPT models and any OpenAI-compatible endpoint (Ollama, OpenRouter, LiteLLM). Sessions are persisted to a SQLite database so multi-turn conversations survive process restarts, unlike most other backends.
- [Session Management Tools: LLM-Driven Conversation Branching](./session-management-tools-llm-driven-conversation-branching.md) — Six `BaseTool` subclasses expose session lifecycle operations — create, list, switch, clear, rename, delete — as agent-callable tools, replacing an earlier regex-based NL detection approach. The LLM decides when to invoke them based on conversational context, making session control robust to any phrasing.

### `session-orchestration` — 1 article(s)

- [Deep Work Session: End-to-End Orchestration and Lifecycle Tests](./deep-work-session-end-to-end-orchestration-and-lifecycle-tests.md) — This integration test suite covers the DeepWorkSession, the top-level orchestrator that coordinates planning, task materialization, agent assignment, approval gating, pause/resume, and cancellation for multi-task AI projects. It verifies the full session lifecycle from DRAFT through PLANNING to AWAITING_APPROVAL and beyond, including edge cases like cyclic dependencies, empty plans, and planner exceptions.

### `sessions` — 10 article(s)

- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.
- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.
- [Conversation Session Schemas](./conversation-session-schemas.md) — Defines the Pydantic models for PocketPaw's conversation session API — listing, creating, renaming, and searching sessions. The search model captures both the matched content and the message role, enabling contextually rich search result previews in the dashboard.
- [Session Management REST Router](./session-management-rest-router.md) — Provides the REST endpoints for creating, listing, deleting, renaming, searching, and exporting conversation sessions. Session IDs are UUID-based slugs prefixed with `websocket_` to maintain compatibility with the WebSocket session key format used by the memory subsystem.
- [Session Schema Tests: Create, Update, and Response Contract Validation](./session-schema-tests-create-update-and-response-contract-validation.md) — Unit tests for the three Pydantic schemas governing session lifecycle in PocketPaw's EE cloud layer — `CreateSessionRequest`, `UpdateSessionRequest`, and `SessionResponse`. The tests pin default values, optional-field behavior, and the nullable `deleted_at` field used for soft-deletion semantics.
- [Sessions Domain Pydantic Schemas: Request and Response Models](./sessions-domain-pydantic-schemas-request-and-response-models.md) — This file defines the three Pydantic models for the sessions domain: CreateSessionRequest, UpdateSessionRequest, and SessionResponse. The schemas are intentionally lean, supporting optional linkage to pockets, groups, agents, and existing runtime sessions at creation time.
- [Sessions FastAPI Router: CRUD, History, Runtime Sessions, and Activity Tracking](./sessions-fastapi-router-crud-history-runtime-sessions-and-activity-tracking.md) — This is the FastAPI router for the sessions domain, providing endpoints to create, list, fetch, update, and delete user chat sessions, retrieve message history, and manage lightweight runtime sessions that are not persisted to MongoDB. A notable design is the runtime session endpoints that bridge the in-memory memory manager with the persistent session store.
- [Sessions Package Entry Point: Router Re-Export](./sessions-package-entry-point-router-re-export.md) — This is the package initialiser for `ee.cloud.sessions`, which re-exports the FastAPI `router` object from `ee.cloud.sessions.router` to make the router accessible via a short import path. The single re-export is the standard pattern used across EE cloud domain packages to allow the top-level application factory to mount domain routers without importing from sub-modules directly.
- [SessionService Realtime Event Emission Tests](./sessionservice-realtime-event-emission-tests.md) — This module tests that every mutating `SessionService` method — create, create_for_pocket, update, delete, and touch — fires the correct realtime event through `emit()` after the database commit. Tests are unit-scoped, patching DB and bus primitives at seam boundaries to isolate emit behavior from persistence.
- [SessionService: Session Lifecycle, History, and Realtime Event Integration](./sessionservice-session-lifecycle-history-and-realtime-event-integration.md) — SessionService is the stateless service class handling the full lifecycle of chat sessions — creating with upsert support for runtime sessions, listing by agent or pocket, retrieving message history from MongoDB, soft-deleting, and emitting realtime events after each mutation. A touch method provides a lightweight activity heartbeat that increments message count and updates lastActivity.

### `settings` — 1 article(s)

- [Generic Settings Read and Update Schemas](./generic-settings-read-and-update-schemas.md) — Defines the Pydantic models for PocketPaw's settings API, using a deliberately generic dict-based approach to handle the wide variety of configurable settings without requiring a separate schema for each. The design prioritises flexibility over strict typing, with secrets excluded from the read response.

### `shared utilities` — 1 article(s)

- [Shared Cross-Cutting Concerns Package Entry Point](./shared-cross-cutting-concerns-package-entry-point.md) — This is the package initialiser for `ee.cloud.shared`, the module that hosts cross-cutting utilities shared across all EE cloud domains. The file contains only a module docstring and no code, making it a namespace anchor that allows other modules to import from `ee.cloud.shared.*` sub-modules like `deps`, `errors`, `events`, and `time`.

### `shared-utilities` — 1 article(s)

- [Launcher Shared Constants and Helpers: Paths, Package Metadata, and Python Discovery](./launcher-shared-constants-and-helpers-paths-package-metadata-and-python-discover.md) — The `common.py` module is the single source of truth for paths, package names, and utility functions shared across all launcher components. Centralizing these definitions prevents the bootstrap, server manager, updater, and uninstaller from each hard-coding `~/.pocketpaw` and drifting out of sync.

### `shell-execution` — 1 article(s)

- [ShellTool: Sandboxed Shell Command Execution with Guardian Approval](./shelltool-sandboxed-shell-command-execution-with-guardian-approval.md) — `ShellTool` wraps `asyncio.subprocess` with two security layers — a precompiled dangerous-pattern regex rail and a Guardian Agent check — before executing any shell command. It operates at `critical` trust level, the highest in PocketPaw's trust hierarchy, and defaults to a jailed working directory.

### `signal` — 1 article(s)

- [Signal Channel Adapter via signal-cli REST API](./signal-channel-adapter-via-signal-cli-rest-api.md) — SignalAdapter integrates PocketPaw with Signal by polling a self-hosted signal-cli REST API instance every 2 seconds for inbound messages and sending replies via its HTTP endpoint. No streaming is supported — responses are buffered and sent as a single message when the agent finishes.

### `simulation` — 1 article(s)

- [SimulationClock and Tick-Synchronized Scheduler Tests](./simulationclock-and-tick-synchronized-scheduler-tests.md) — This test file covers `SimulationClock` (a deterministic discrete-time clock for agent simulation), `TickSnapshot` (tick-state serialization), `DependencyScheduler.run_tick_synchronized` (tick-driven task dispatch), and simulation tick metadata injection into task prompts. Tests validate clock advancement, snapshot immutability, scheduler dispatch order, and prompt metadata inclusion.

### `skill-generation` — 1 article(s)

- [CreateSkillTool: Runtime Skill Generation with SKILL.md and Memory Persistence](./createskilltool-runtime-skill-generation-with-skillmd-and-memory-persistence.md) — `CreateSkillTool` lets agents create new reusable skills at runtime by writing SKILL.md files with YAML frontmatter to `~/.claude/skills/`. After creation, it reloads the skill loader and persists the skill's description to long-term memory so the agent remembers it across sessions.

### `skills` — 5 article(s)

- [Agent Skill Management Schemas](./agent-skill-management-schemas.md) — Defines the Pydantic models for PocketPaw's skill management API — listing installed skills, installing new ones from a GitHub source, and removing existing ones. The install schema enforces a minimum source format to prevent obviously malformed installation requests.
- [CreateSkillTool Tests — Skill Generation, Naming Validation, and YAML Frontmatter](./createskilltool-tests-skill-generation-naming-validation-and-yaml-frontmatter.md) — This test file validates `CreateSkillTool`, which allows agents to generate new PocketPaw skill files (SKILL.md) at runtime. Tests cover tool metadata, skill name regex validation, YAML frontmatter format, overwrite protection, skill loader reload triggering, and the allowed-tools and user-invocable flags.
- [SkillLoader and SKILL.md Parsing Tests](./skillloader-and-skillmd-parsing-tests.md) — This test file validates `SkillLoader` and `parse_skill_md` from `pocketpaw.skills.loader`, covering SKILL.md frontmatter parsing, optional field handling, no-frontmatter rejection, directory-name fallback for skill naming, prompt argument substitution, loader lifecycle, skill retrieval, reload, multi-path override resolution, and integration with real skill paths.
- [Skills API Tests — SkillLoader Search, REST Endpoints, Install, Remove, and MCP Presets](./skills-api-tests-skillloader-search-rest-endpoints-install-remove-and-mcp-preset.md) — This test file covers `SkillLoader.search()` and the full skills REST API: listing installed skills, searching the skills.sh library, installing skills via git clone, removing skills, force-reloading, and MCP preset configuration with `needs_args` metadata. Tests validate filtering, case-insensitive matching, subprocess handling, and preset response shapes.
- [Skills Management REST Router](./skills-management-rest-router.md) — Provides REST endpoints for listing installed skills, searching the skills.sh marketplace, installing skills from GitHub sources, removing skills, and force-reloading the skill index. Skills are user-invocable slash commands that extend PocketPaw's capabilities without core code changes.

### `skills management` — 1 article(s)

- [Skills API Security and Installation Tests — Input Validation, Injection Prevention, and Audit Logging](./skills-api-security-and-installation-tests-input-validation-injection-prevention.md) — This test module covers the full lifecycle of the skills management API: listing, searching, installing from GitHub sources, removing, and reloading. The installation tests are especially dense, exercising a whitelist-based input validator to block path traversal, shell injection, null bytes, and over-segmented source strings before any subprocess is spawned.

### `skills system` — 5 article(s)

- [GWS Skills Auto-Install and GitHub Skill Installer Tests](./gws-skills-auto-install-and-github-skill-installer-tests.md) — This test suite covers `install_skills_from_github`, a shared async helper that clones a GitHub repository and copies SKILL.md-bearing subdirectories into the local skills install directory, with support for prefix filtering and single-skill targeting. It also tests that the GWS preset installation triggers a non-blocking background task that auto-installs Google Workspace skills.
- [PocketPaw Skills Module: Entry Point for the AgentSkills Ecosystem Integration](./pocketpaw-skills-module-entry-point-for-the-agentskills-ecosystem-integration.md) — The `skills` package `__init__.py` assembles and re-exports the three subsystems that make up PocketPaw's skill integration layer: loading, installation, and execution. By surfacing a clean public API here, consumers throughout the codebase can import skill primitives from a single location without knowing the internal module structure.
- [Skill Installer: Secure GitHub-to-Disk Skill Installation with Audit Trail](./skill-installer-secure-github-to-disk-skill-installation-with-audit-trail.md) — This module handles the full lifecycle of installing skills from GitHub repositories: validating the source string, cloning to a temp directory, stripping symlinks, copying SKILL.md directories to the install location, and emitting audit events for every installation attempt. Symlink stripping is the key defensive measure against path traversal attacks via malicious repositories.
- [SkillExecutor: Streaming Skill Execution Through the Agent Backend](./skillexecutor-streaming-skill-execution-through-the-agent-backend.md) — The `SkillExecutor` bridges skill definitions (Markdown files) and the agent backend (Open Interpreter or Claude Code) by building a prompt from skill content and user arguments, then streaming the agent's response back as an async iterator of result dicts. It is the runtime engine that transforms a loaded skill into an executed task.
- [SkillLoader: Discovery, Parsing, and Hot-Reload of AgentSkills from Disk](./skillloader-discovery-parsing-and-hot-reload-of-agentskills-from-disk.md) — The `SkillLoader` discovers and parses skills from two filesystem paths — the central AgentSkills directory and a PocketPaw-specific directory — by reading YAML frontmatter and Markdown content from `SKILL.md` files. It supports hot-reloading, invocability filtering, search, and a singleton accessor for shared use across the application.

### `slack` — 1 article(s)

- [Slack Channel Adapter with Socket Mode and Live Streaming](./slack-channel-adapter-with-socket-mode-and-live-streaming.md) — SlackAdapter connects PocketPaw to Slack using Socket Mode, which requires no public webhook URL — the bot maintains a persistent WebSocket connection to Slack's servers. It supports slash commands, DM messages, app mentions, threaded replies, file uploads, and live-edited streaming responses.

### `slack-integration` — 1 article(s)

- [Slack Bot Gateway: Bootstrapping the PocketPaw Slack Integration](./slack-bot-gateway-bootstrapping-the-pocketpaw-slack-integration.md) — The `slack_gateway.py` module is the top-level entry point for running PocketPaw as a Slack bot, wiring together the message bus, Slack adapter, and agent loop into a single coroutine. It handles clean startup and shutdown so the bot can run reliably as a long-lived async process.

### `snapshot` — 1 article(s)

- [Accessibility Tree Snapshot Generator — Semantic Browser State for LLMs](./accessibility-tree-snapshot-generator-semantic-browser-state-for-llms.md) — snapshot.py converts Playwright's raw accessibility tree into a compact, indented text representation with [ref=N] markers that an LLM can read to understand page structure and reference specific elements for interaction. RefMap maintains the mapping from integer references back to element selectors, and SnapshotGenerator handles recursive tree traversal, role filtering, name truncation, and selector generation.

### `snapshot generation` — 1 article(s)

- [Browser Snapshot Generator Tests: Accessibility Tree to Agent-Readable Text Conversion](./browser-snapshot-generator-tests-accessibility-tree-to-agent-readable-text-conve.md) — This test suite validates `SnapshotGenerator`, which converts Playwright's accessibility tree JSON into a compact, ref-tagged text representation that AI agents can read and reason about. Tests cover the full rendering pipeline: interactive element ref assignment, non-interactive element handling, heading/list/image rendering, hidden element filtering, password field masking, and the `RefMap` reference number system.

### `social-media` — 1 article(s)

- [Reddit Tools: Search, Read Posts, and Surface Trending Content](./reddit-tools-search-read-posts-and-surface-trending-content.md) — PocketPaw ships three Reddit tools — `reddit_search`, `reddit_read`, and `reddit_trending` — that give agents access to Reddit content without requiring an API key. They delegate to a shared `RedditClient` integration and return richly formatted markdown text suitable for direct LLM consumption.

### `soul integration` — 4 article(s)

- [Paw Project Scanner Tests: heuristic_scan and scan_prompt Coverage](./paw-project-scanner-tests-heuristicscan-and-scanprompt-coverage.md) — The `paw scan` heuristic reads a project's on-disk artifacts — README, `pyproject.toml`, `package.json`, `.env.example`, and top-level directory structure — and stores extracted facts into the soul's memory. These tests verify that each artifact type is correctly parsed, that the soul's `remember()` method is called with appropriate metadata, and that failures in memory storage do not abort the scan.
- [PocketPaw Cognitive Engine: Wiring Tests for Soul-Aware Thinking](./pocketpaw-cognitive-engine-wiring-tests-for-soul-aware-thinking.md) — Tests for the `PocketPawCognitiveEngine`, which bridges the soul-protocol's identity layer with PocketPaw's LLM backend to produce soul-contextualised responses. Covers correct parameter forwarding, graceful error handling, stream termination logic, and end-to-end wiring through `SoulManager` and `AgentLoop`.
- [Soul Bridge and Bootstrap Provider Tests: SoulBridge and SoulBootstrapProvider](./soul-bridge-and-bootstrap-provider-tests-soulbridge-and-soulbootstrapprovider.md) — These tests cover the `SoulBootstrapProvider` — which translates a Soul Protocol soul object into a `BootstrapContext` for PocketPaw's agent runtime — and `SoulBridge`, the async shim that wraps soul `observe()` and `recall()` calls with error-swallowing guarantees. Together they ensure the paw module can safely integrate with soul-protocol without crashing when the soul is unavailable or misbehaves.
- [Soul-Protocol Integration Tests: End-to-End PocketPaw Wiring](./soul-protocol-integration-tests-end-to-end-pocketpaw-wiring.md) — Integration tests that validate the full lifecycle of the soul-protocol package within PocketPaw, including bootstrap prompt generation, memory observe-and-recall, tool bridge discovery of soul tools, and corrupt file recovery. All tests are conditionally skipped when `soul-protocol` is not installed.

### `soul management` — 2 article(s)

- [Soul Protocol v0.2.4 Smoke Tests: Reload, Evaluate, Biorhythm, and Fatigue](./soul-protocol-v024-smoke-tests-reload-evaluate-biorhythm-and-fatigue.md) — Smoke tests for soul-protocol v0.2.4 features including hot-reload, the evaluate endpoint, biorhythm config round-trips, dirty tracking for autosave optimization, external change detection, and the fatigue hint in bootstrap context. Designed to catch regressions when upgrading the soul-protocol dependency.
- [SoulManager Lifecycle Tests: Birth, Persistence, Import, and Concurrency](./soulmanager-lifecycle-tests-birth-persistence-import-and-concurrency.md) — Comprehensive tests for `SoulManager`, the singleton that manages a PocketPaw soul's full lifecycle — from birth through autosave, import, reload, and shutdown. Covers tool caching, dirty tracking, concurrent observe serialization, corrupt file fallback, and biorhythm configuration.

### `soul protocol` — 2 article(s)

- [Agent Soul Configuration and OCEAN Personality Schema Tests](./agent-soul-configuration-and-ocean-personality-schema-tests.md) — This module tests the `AgentConfig` model's soul-related fields — default values, persona assignment, custom OCEAN personality vectors, and absence of a `soul_path` attribute — and verifies that `CreateAgentRequest` correctly propagates `persona` and soul OCEAN customization fields.
- [Soul Memories API Tests: Tier Filtering, Limit Clamping, and Entry Serialization](./soul-memories-api-tests-tier-filtering-limit-clamping-and-entry-serialization.md) — Unit tests for the `GET /api/v1/soul/memories` endpoint, focusing on the `_collect_tier_entries` helper that queries individual memory tiers, limit clamping to safe bounds, tier filter validation, and graceful handling of absent souls or missing tier stores. Tests operate at the function level to avoid spinning up a full SSE client.

### `soul protocol integration` — 3 article(s)

- [Instinct Correction Loop — Diff Capture and Soul Learning Signal](./instinct-correction-loop-diff-capture-and-soul-learning-signal.md) — Defines the data types and diff logic that record what a human changed when editing an agent's proposed action before approving it. These structured diffs are consumed by soul-protocol to bias future proposals toward the human's preferences.
- [Instinct Correction Soul Bridge — Wiring Human Edits to Soul Memory](./instinct-correction-soul-bridge-wiring-human-edits-to-soul-memory.md) — Acts as the connector between the Instinct correction system and soul-protocol's memory tiers, turning captured human edits into episodic observations and promoting repeated field-level corrections into procedural rules. It degrades silently when the soul is unavailable so the approval flow is never blocked.
- [Retrieval Journal Projection — Package Entry Point and Design History](./retrieval-journal-projection-package-entry-point-and-design-history.md) — The `ee.retrieval` package implements an observable retrieval trail and access-count graduation policy as a projection over the org journal, superseding two earlier JSONL-based designs. This module re-exports every public symbol and documents the architectural decision to consolidate onto the journal rather than maintain a parallel JSONL sink.

### `soul tools` — 1 article(s)

- [Paw Soul Tools Tests: SoulRemember, SoulRecall, SoulEditCore, and SoulStatus](./paw-soul-tools-tests-soulremember-soulrecall-souleditcore-and-soulstatus.md) — These tests cover the four built-in soul tools that PocketPaw exposes to LLMs: `soul_remember`, `soul_recall`, `soul_edit_core`, and `soul_status`. Each tool is validated for its parameter schema, execution behavior, error handling, and JSON Schema export in both Anthropic and OpenAI formats.

### `soul-protocol` — 11 article(s)

- [Agent Document — Configuration, Soul Integration, and Scope Assignment](./agent-document-configuration-soul-integration-and-scope-assignment.md) — The Beanie ODM document representing an agent's persistent configuration in MongoDB — covering backend selection, system prompt, tool allowlist, trust level, soul personality settings, and hierarchical scope assignments. This document captures agent identity and configuration, not execution state.
- [Correction Soul Bridge: Observe Callbacks and Procedural Memory Promotion Tests](./correction-soul-bridge-observe-callbacks-and-procedural-memory-promotion-tests.md) — This test suite covers the `CorrectionSoulBridge` — the enterprise component that writes user corrections into the Soul Protocol's memory system and promotes recurring correction patterns into procedural memory. It also validates the `InstinctCorrectionsTool`, the agent-facing interface that exposes captured corrections as readable context.
- [Paw Agent Factory: Wiring Soul, Bootstrap, and Tools Together](./paw-agent-factory-wiring-soul-bootstrap-and-tools-together.md) — get_paw_agent() is the single composition root for a fully configured paw agent: it loads PawConfig, awakens or births a Soul, wires SoulBridge and SoulBootstrapProvider, registers the four core soul tools, and returns a PawAgent dataclass ready for CLI commands. A startup guard raises a clear error if soul-protocol is not installed.
- [Paw CLI: Click-Based Command Interface for Project-Level AI](./paw-cli-click-based-command-interface-for-project-level-ai.md) — paw/cli.py implements the full paw CLI using Click with eight commands — init, ask, chat, serve, status, doctor, os, channels — and an async bridge that handles both normal invocation and pytest-asyncio environments. The serve command is a documented placeholder; all other commands are functional.
- [Paw Project Scanner: Heuristic Project Knowledge Ingestion](./paw-project-scanner-heuristic-project-knowledge-ingestion.md) — scan.py bootstraps a soul's project knowledge by reading common configuration files (README, pyproject.toml, package.json, Cargo.toml, go.mod, .env.example) and storing facts via soul.remember(). A full agent-powered scan is defined but currently delegates to the heuristic fallback pending AgentRouter wiring.
- [Paw Soul Tools: BaseTool Wrappers for Soul-Protocol Operations](./paw-soul-tools-basetool-wrappers-for-soul-protocol-operations.md) — tools.py defines nine BaseTool subclasses that expose soul-protocol's memory and identity operations as agent-callable tools, including remember, recall, edit core memory, status introspection, rubric-based self-evaluation, hot reload from disk, selective forgetting, and topic-aware context retrieval.
- [PocketPawCognitiveEngine: Bridging Soul Protocol Cognition to Agent Backends](./pocketpawcognitiveengine-bridging-soul-protocol-cognition-to-agent-backends.md) — `PocketPawCognitiveEngine` implements the Soul Protocol's `CognitiveEngine` interface, routing fact-extraction and reflection calls to PocketPaw's active agent backend or to a cheaper dedicated Anthropic model when configured. It enables the soul's cognitive layer to reuse the existing LLM infrastructure rather than requiring a separate AI provider setup.
- [Soul Bridge: Connecting soul-protocol to PocketPaw's Agent Loop](./soul-bridge-connecting-soul-protocol-to-pocketpaws-agent-loop.md) — soul_bridge.py provides two classes: SoulBootstrapProvider maps soul state and memories into the BootstrapContext consumed by AgentContextBuilder while preserving tool documentation from the default provider, and SoulBridge exposes a simple observe/recall interface with transparent fallback to older soul-protocol API versions.
- [Soul Protocol Package Root: Integration Anchor for PocketPaw](./soul-protocol-package-root-integration-anchor-for-pocketpaw.md) — The `soul/__init__.py` file is the namespace anchor for PocketPaw's Soul Protocol integration sub-package. It signals the package boundary and establishes the import root from which `cognitive.py` and `manager.py` are accessible.
- [Soul Protocol REST API Endpoints](./soul-protocol-rest-api-endpoints.md) — Exposes the full Soul Protocol interface over REST, covering identity, OCEAN personality state, core memory editing, memory storage and recall, soul import/export, and rubric-based self-evaluation. All endpoints require the `settings:read` scope and safely handle the case where no soul is loaded.
- [SoulManager: Lifecycle Management for the PocketPaw Soul Instance](./soulmanager-lifecycle-management-for-the-pocketpaw-soul-instance.md) — `SoulManager` is PocketPaw's singleton that owns the entire lifecycle of a `.soul` file — initialization, concurrency-safe observation, periodic auto-save, external change detection, and graceful shutdown. It is the integration seam between PocketPaw's real-time agent sessions and the persistent soul-protocol memory system.

### `soul-protocol-integration` — 2 article(s)

- [Memory Graduation Policy for the Retrieval Projection](./memory-graduation-policy-for-the-retrieval-projection.md) — Implements the access-count-based graduation logic that promotes memories across tiers (episodic to semantic, semantic to core, procedural promotion) by scanning the journal-backed retrieval projection rather than a flat JSONL file. All tuning thresholds are carried verbatim from PR #937 to preserve runtime behaviour after the storage layer refactor.
- [Retrieval and Graduation Event Payload Definitions](./retrieval-and-graduation-event-payload-definitions.md) — Defines the canonical action names and payload builders for the two journal event types that power PocketPaw's retrieval tracing and memory graduation system. These constants anchor the projection, policy, and store layers to a shared, stable vocabulary sourced from soul-protocol's v0.3.1 action catalog.

### `speech-to-text` — 1 article(s)

- [SpeechToTextTool: Multi-Provider Audio Transcription with Path Jail Enforcement](./speechtotexttool-multi-provider-audio-transcription-with-path-jail-enforcement.md) — `SpeechToTextTool` transcribes audio files to text via OpenAI Whisper, ElevenLabs, or Sarvam AI Saaras, with automatic provider selection from settings. A path-safety check (`is_safe_path`) prevents directory traversal attacks before any file is read, and transcripts are written to a versioned output directory.

### `spotify` — 1 article(s)

- [Spotify Tools: Search, Now Playing, Playback Control, and Playlist Management](./spotify-tools-search-now-playing-playback-control-and-playlist-management.md) — Four `BaseTool` subclasses — `spotify_search`, `spotify_now_playing`, `spotify_playback`, and `spotify_playlist` — wrap `SpotifyClient` to give agents full Spotify control: catalog search, current track status, transport controls, and playlist operations. All raise `RuntimeError` for auth failures, which surfaces as a clean error rather than a stack trace.

### `standards` — 1 article(s)

- [AGENTS.md Support Package: Standardized Agent Capability Declaration](./agentsmd-support-package-standardized-agent-capability-declaration.md) — The `agents_md` package implements support for the Anthropic AGENTS.md specification — a convention where repositories publish a markdown file describing constraints, capabilities, and instructions for AI agents operating on that codebase. PocketPaw both reads AGENTS.md from target repositories (to inject constraints into the agent system prompt) and publishes its own AGENTS.md for other agents to consume.

### `startup` — 3 article(s)

- [Cloud Memory Backend Bootstrap — MongoDB Default Registration](./cloud-memory-backend-bootstrap-mongodb-default-registration.md) — Bootstraps the MongoDB memory backend for cloud deployments by directly priming the `pocketpaw.memory.manager` singleton before Beanie is initialized, bypassing the file-based config that would otherwise retain a stale `memory_backend: "file"` setting. Respects the `POCKETPAW_MEMORY_BACKEND` environment variable as an explicit user override.
- [Health Checks Registry: Modular Check Catalogue with Tiered Execution Lists](./health-checks-registry-modular-check-catalogue-with-tiered-execution-lists.md) — The health checks `__init__.py` is both a re-export facade and a check registry, defining three typed lists — `STARTUP_CHECKS`, `INTEGRATION_CHECKS`, and `CONNECTIVITY_CHECKS` — that the `HealthEngine` iterates to run diagnostics at the right time and in the right order. All individual check functions are re-exported so that existing imports remain stable across refactors.
- [Health Engine Package: Singleton Factory for Self-Healing Diagnostics](./health-engine-package-singleton-factory-for-self-healing-diagnostics.md) — The health package `__init__.py` provides a lazy singleton factory `get_health_engine()` that creates and caches a `HealthEngine` instance on first access. The deferred import pattern means the `HealthEngine` class and all its check dependencies are only loaded when health checking is actually needed, keeping startup time minimal.

### `status` — 1 article(s)

- [Agent Runtime Status Schemas](./agent-runtime-status-schemas.md) — Defines the Pydantic models for PocketPaw's agent status API, covering per-session execution states and global runtime health. The session model captures granular execution phase (thinking, tool running, streaming) alongside token usage and duration, giving operators deep visibility into what the agent is doing at any moment.

### `storage` — 24 article(s)

- [ErrorStore — Persistent Append-Only JSONL Error Log for the Health Engine](./errorstore-persistent-append-only-jsonl-error-log-for-the-health-engine.md) — `ErrorStore` is PocketPaw's persistent error logging backend, storing health engine errors as newline-delimited JSON in `~/.pocketpaw/health/errors.jsonl` with log rotation, tail-read retrieval, and optional search filtering. It survives server restarts and page refreshes, ensuring operators can inspect errors from previous sessions.
- [File Uploads Router with Signed URL Access](./file-uploads-router-with-signed-url-access.md) — Implements the OSS single-user file upload system with POST (single and bulk), streaming GET with signed URL authorization, and DELETE endpoints. Uploaded files are stored under `~/.pocketpaw/uploads/` with a JSONL metadata index, and downloads are gated by short-lived HMAC-signed tokens to prevent unauthorized access.
- [FileMemoryStore Fixes: UUID Collision, Fuzzy Search, Deduplication, Persistent Delete, and Graph Tests](./filememorystore-fixes-uuid-collision-fuzzy-search-deduplication-persistent-delet.md) — This comprehensive test file covers a series of incremental fixes to `FileMemoryStore`, PocketPaw's markdown-based memory backend. It addresses UUID collisions, broken substring search, single-day file loading, lost deletions across restarts, and missing context limits — plus later phases covering vector semantic search, graph indexing, SVG escaping, and SQLite variable limit handling.
- [Journal-Backed Write Store for Retrieval and Graduation Events](./journal-backed-write-store-for-retrieval-and-graduation-events.md) — Provides the write path for the retrieval domain by appending `retrieval.query` and `graduation.applied` events to the org journal and immediately folding them into the shared in-memory projection. Replaces the JSONL-plus-asyncio-lock pattern from PR #936 with SQLite WAL semantics that are multi-process safe by design.
- [Journal-Backed Write Store for Widget Events](./journal-backed-write-store-for-widget-events.md) — Provides the write facade for the widget domain, emitting all five widget action types onto the org journal and folding each emission into the shared in-memory projection for immediate read consistency. Adds a seq-returning variant of the interaction writer to support the POST /widgets/track endpoint's cursor ack requirement.
- [JSONL File Store: Append-Only Metadata Persistence for Uploaded Files](./jsonl-file-store-append-only-metadata-persistence-for-uploaded-files.md) — The `JSONLFileStore` provides a lightweight, append-only metadata store for file upload records using newline-delimited JSON, with an in-memory cache for fast lookups and thread-safe writes via a `Lock`. Soft deletes are implemented by appending a delete operation record rather than removing lines, preserving the full audit history of every file lifecycle event.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.
- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.
- [LocalStorageAdapter: Atomic Local-Disk Blob Storage](./localstorageadapter-atomic-local-disk-blob-storage.md) — LocalStorageAdapter provides a local-filesystem implementation of the StorageAdapter protocol, using aiofiles for non-blocking I/O and an atomic write pattern to prevent partial-file corruption. It enforces strict path isolation so that no key—however crafted—can read or write outside the designated root directory.
- [Mission Control File Store: JSON-Backed Persistence with In-Memory Indexes](./mission-control-file-store-json-backed-persistence-with-in-memory-indexes.md) — FileMissionControlStore implements MissionControlStoreProtocol using one JSON file per entity type, combined with in-memory Python dicts for O(1) lookups. Atomic writes via temp-file rename prevent data corruption, and a singleton factory ensures all subsystems share one loaded dataset.
- [Mission Control Storage Protocol Interface](./mission-control-storage-protocol-interface.md) — MissionControlStoreProtocol defines the abstract interface all Mission Control storage backends must implement using Python's structural Protocol pattern with runtime_checkable, enabling dependency injection and future backend swappability across all six entity types.
- [OAuth2 Storage: In-Memory Auth Codes and File-Persisted Tokens](./oauth2-storage-in-memory-auth-codes-and-file-persisted-tokens.md) — `OAuthStorage` uses a two-tier storage strategy: authorization codes live in memory only (ephemeral, 10-minute TTL), while access and refresh tokens are persisted to `~/.pocketpaw/oauth_tokens.json` so authenticated desktop sessions survive server restarts. A pre-registered `pocketpaw-desktop` client with Tauri-compatible redirect URIs is always available.
- [PawKit Store: File-Based Kit Persistence with Path Traversal Protection](./pawkit-store-file-based-kit-persistence-with-path-traversal-protection.md) — The `FileKitStore` manages installed PawKits on disk under `~/.pocketpaw/kits/`, using atomic writes (write-then-rename) and an in-memory index loaded lazily on first access. A March 2026 security hardening pass added input sanitization to prevent path traversal attacks via malicious kit IDs or data source names.
- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.
- [S3StorageAdapter: S3-Compatible Cloud Blob Storage](./s3storageadapter-s3-compatible-cloud-blob-storage.md) — S3StorageAdapter implements the StorageAdapter interface against any S3-compatible object store—AWS S3, MinIO, Cloudflare R2, or Wasabi—using the synchronous boto3 library pushed off the event loop via `asyncio.to_thread`. Credentials use the same environment variable names as the companion interacly-backend service, enabling shared-bucket deployments.
- [SQLite-Backed Audit Log Store](./sqlite-backed-audit-log-store.md) — Implements the `AuditStore` class that persists audit entries to a SQLite database using the stdlib `sqlite3` module with async-friendly execution via `run_in_executor`. Provides filtered querying, full-text search with injection-safe LIKE escaping, CSV/JSON export, and convenience methods for common audit event types.
- [Storage Adapter Factory: Environment-Driven Backend Selection for the Upload Pipeline](./storage-adapter-factory-environment-driven-backend-selection-for-the-upload-pipe.md) — The `factory.py` module provides a single `build_adapter()` function that reads the `POCKETPAW_UPLOAD_ADAPTER` environment variable and returns either a local disk adapter or an S3-compatible adapter, enabling backend swaps without config file changes. A defensive `load_dotenv()` call at build time ensures environment variables are visible even when the factory runs before the application lifecycle loads `.env`.
- [Storage Health Checks — Disk Space, Audit Log Writability, and Memory Directory](./storage-health-checks-disk-space-audit-log-writability-and-memory-directory.md) — This module provides three synchronous health checks for PocketPaw's local storage: verifying the config directory does not exceed 500 MB, confirming the audit log file is writable, and checking that the memory directory is accessible. All checks return `HealthCheckResult` objects with actionable fix hints.
- [Storage Key Generation: Namespaced, Date-Bucketed, UUID-Based Keys for Upload Blobs](./storage-key-generation-namespaced-date-bucketed-uuid-based-keys-for-upload-blobs.md) — The `keys.py` module generates opaque storage keys with a predictable three-part structure -- kind, year-month bucket, and a UUID4 hex suffix -- that supports human-readable organization, time-based lifecycle policies, and guaranteed uniqueness. The companion `sanitize_ext()` function normalizes file extensions to prevent path injection through malformed extension strings.
- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.
- [StorageAdapter Protocol: The Backend-Agnostic Interface for File Persistence](./storageadapter-protocol-the-backend-agnostic-interface-for-file-persistence.md) — The `StorageAdapter` protocol defines the minimal async interface that all storage backends (local disk, S3, GCS) must satisfy, keeping the upload pipeline decoupled from any specific storage technology. The companion `StoredObject` dataclass represents the canonical result of a successful write operation.
- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.
- [Upload Service Tests: Validation, Bulk Handling, and Streaming](./upload-service-tests-validation-bulk-handling-and-streaming.md) — This test suite exercises the `UploadService` layer in PocketPaw's file upload subsystem, covering single and bulk file ingestion, magic-byte MIME sniffing, path sanitisation, and ownership-gated streaming and deletion. It uses an in-memory `_FakeAdapter` to isolate tests from real storage backends while still exercising the full service contract.
- [WikiStore: File-Based CRUD for Knowledge Articles](./wikistore-file-based-crud-for-knowledge-articles.md) — WikiStore is the persistence layer for PocketPaw's knowledge subsystem, managing file-based CRUD for raw documents and compiled wiki articles organized by scope. It maintains a predictable three-directory layout per scope and handles index serialization to ensure fast listing without scanning the filesystem on every read.

### `store` — 1 article(s)

- [Fabric Store: Object Type CRUD, Object Lifecycle, Link Traversal, and Query Tests](./fabric-store-object-type-crud-object-lifecycle-link-traversal-and-query-tests.md) — Unit tests for the enterprise `FabricStore` — PocketPaw's ontology-based business object store backed by SQLite. Covers defining and querying object types, creating and updating objects with source tracking, linking objects and traversing relationships, filtering queries, pagination, and cascade deletion.

### `streaming` — 20 article(s)

- [A2A Client: HTTP Client for Delegating Tasks to External A2A Agents](./a2a-client-http-client-for-delegating-tasks-to-external-a2a-agents.md) — `A2AClient` is an async HTTP client that allows PocketPaw to communicate with external A2A-compatible agents. It supports blocking task submission, SSE streaming, task polling, cancellation, and Agent Card discovery — with connection sharing, TTL-cached Agent Cards, and transparent wrapping of direct-Message responses into the Task model.
- [A2A JSON-RPC Dispatcher: Method Registration and Request Routing](./a2a-json-rpc-dispatcher-method-registration-and-request-routing.md) — `A2ADispatcher` parses incoming JSON-RPC 2.0 requests, validates the envelope, and routes them to registered async handlers. It supports both blocking dispatch and SSE streaming dispatch, batch requests, and consistent error handling — with a deliberate separation between stream-only and non-stream methods.
- [A2A Protocol Pydantic Models: Tasks, Messages, Parts, and Agent Cards](./a2a-protocol-pydantic-models-tasks-messages-parts-and-agent-cards.md) — This module defines all Pydantic models for the A2A protocol (v0.2.5+), including the task lifecycle state machine, typed content parts (text/file/data), message and artifact models, request parameter schemas, the Agent Card capabilities manifest, and JSON-RPC 2.0 envelope models with streaming event types.
- [A2A Protocol Server — Agent-to-Agent HTTP Endpoint for PocketPaw](./a2a-protocol-server-agent-to-agent-http-endpoint-for-pocketpaw.md) — Implements the Agent-to-Agent (A2A) protocol server that exposes PocketPaw as an interoperable AI agent over HTTP. It provides a JSON-RPC 2.0 dispatcher, an Agent Card capability manifest, task submission endpoints, SSE streaming, and an in-memory task store with TTL-based expiry.
- [Agent Protocol: Core Event Types and Legacy Interface](./agent-protocol-core-event-types-and-legacy-interface.md) — Defines the standardized `AgentEvent` dataclass that all agent backends emit, and the `AgentProtocol` structural typing interface that existed before the more capable `AgentBackend` ABC replaced it. Together they form the lingua franca for streaming agent output across PocketPaw's multi-backend architecture.
- [AgentRouter Fallback and Streaming Tests](./agentrouter-fallback-and-streaming-tests.md) — This test file validates `AgentRouter`'s fault-tolerant backend selection logic, covering hard exception failures, error-event failures, full backend chain exhaustion, streaming event pass-through, and no-fallback configurations. The tests use in-process stub backends injected via `monkeypatch` to exercise the router without real LLM backends.
- [Claude Agent SDK Backend — Recommended Default Agent Runtime](./claude-agent-sdk-backend-recommended-default-agent-runtime.md) — Implements `ClaudeSDKBackend`, PocketPaw's primary agent backend that wraps Anthropic's `claude-agent-sdk`. It provides built-in tools (Bash, Read, Write, Edit, Glob, Grep, WebSearch, WebFetch), streaming token delivery, a `PreToolUse` security hook, MCP server support, and headless permission bypass for messaging channels.
- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.
- [Deep Agents Backend — LangChain/LangGraph Agent Framework Integration](./deep-agents-backend-langchainlanggraph-agent-framework-integration.md) — Implements `DeepAgentsBackend`, which runs agents via the `deepagents` SDK built on LangChain and LangGraph. It provides durable execution, built-in planning and filesystem tools, multi-provider LLM routing, MCP tool integration, and streaming via LangGraph's event stream.
- [Discord Channel Adapter — discli Subprocess Bridge](./discord-channel-adapter-discli-subprocess-bridge.md) — DiscliAdapter connects PocketPaw to Discord by spawning a discli serve subprocess and communicating with it over stdin/stdout JSONL, rather than using discord.py directly. This architecture isolates the Discord event loop in a separate process, preventing it from interfering with PocketPaw's asyncio loop, and lets discli handle rate limiting, reconnection, and slash command registration independently.
- [GitHub Copilot SDK Backend — Copilot CLI Agent Integration](./github-copilot-sdk-backend-copilot-cli-agent-integration.md) — Implements `CopilotSDKBackend`, which wraps the `github-copilot-sdk` Python package to run GitHub Copilot as a PocketPaw agent. The SDK communicates with the `copilot` CLI via JSON-RPC internally, providing streaming, tool use, and multi-provider support (OpenAI, Azure, Anthropic, LiteLLM).
- [Matrix Channel Adapter — Long-Polling with Streaming via Message Edits](./matrix-channel-adapter-long-polling-with-streaming-via-message-edits.md) — MatrixAdapter connects PocketPaw to the Matrix protocol using matrix-nio's AsyncClient, supporting both access token and password authentication. Streaming agent responses are delivered by editing the initial message in place using Matrix's m.replace relation, rate-limited to one edit per 1.5 seconds to avoid homeserver rate limiting.
- [OpenCode Backend — REST API Client for a Running OpenCode Server](./opencode-backend-rest-api-client-for-a-running-opencode-server.md) — Implements `OpenCodeBackend`, which communicates with a locally running OpenCode server (`opencode --server`) via its REST API using `httpx`. Sessions are mapped from PocketPaw session keys to OpenCode session IDs, and streaming responses are parsed from NDJSON parts.
- [Pocket Chat Router — Context-Aware Workspace Creation and Interaction](./pocket-chat-router-context-aware-workspace-creation-and-interaction.md) — The pockets router is PocketPaw's dedicated endpoint for creating and interacting with pocket workspaces — themed dashboards of widgets, charts, and data panels. It streams responses via SSE, injects dynamic Ripple widget documentation for new pockets, and normalises three distinct AI output formats into render-ready Ripple specs.
- [Pocket Journal SSE Stream for RippleGraphWidget](./pocket-journal-sse-stream-for-ripplegraphwidget.md) — The journal stream router serves a live Server-Sent Events feed of soul-protocol journal entries filtered to a single pocket, enabling the frontend RippleGraphWidget to render agent decisions, retrievals, and tool traces as a live causation graph. It supports resumable cursors, cold-start backlog replay, and keepalive heartbeats to survive proxy timeouts.
- [Pocket Journal SSE Stream Router Tests: Filtering, Cursors, and Frame Contract](./pocket-journal-sse-stream-router-tests-filtering-cursors-and-frame-contract.md) — Tests the Server-Sent Events (SSE) endpoint that streams journal events for a specific pocket to the RippleGraphWidget frontend. Covers the event-matching predicate, backlog replay with `since_seq` cursor support for reconnects, the `connected` handshake frame, idle-poll timeout, and strict isolation so events from other pockets never appear in a pocket's stream.
- [Slack Channel Adapter with Socket Mode and Live Streaming](./slack-channel-adapter-with-socket-mode-and-live-streaming.md) — SlackAdapter connects PocketPaw to Slack using Socket Mode, which requires no public webhook URL — the bot maintains a persistent WebSocket connection to Slack's servers. It supports slash commands, DM messages, app mentions, threaded replies, file uploads, and live-edited streaming responses.
- [StreamEvent Processing Tests: Token-by-Token Streaming and Thinking Integration](./streamevent-processing-tests-token-by-token-streaming-and-thinking-integration.md) — Tests for PocketPaw's handling of `StreamEvent` objects from the Claude Agent SDK, covering text delta emission, thinking block lifecycle, tool-use start events, duplicate suppression, multi-turn state reset, and integration of thinking events into the AgentLoop's system event bus.
- [Telegram Channel Adapter with Streaming, Typing Indicators, and Forum Topic Support](./telegram-channel-adapter-with-streaming-typing-indicators-and-forum-topic-suppor.md) — TelegramAdapter connects PocketPaw to the Telegram Bot API using python-telegram-bot, supporting streaming responses via live message edits, animated typing indicators, media file sending, voice note detection, and Telegram forum topic (Supergroup) thread routing.
- [WebSocket Channel Adapter for the PocketPaw Dashboard](./websocket-channel-adapter-for-the-pocketpaw-dashboard.md) — WebSocketAdapter manages real-time bidirectional communication between PocketPaw's FastAPI backend and its web dashboard frontend, routing messages, streaming chunks, and system events (tool execution, thinking indicators) to the correct connected browser session.

### `subprocess` — 3 article(s)

- [Codex CLI Backend — Subprocess Wrapper for OpenAI's Terminal Agent](./codex-cli-backend-subprocess-wrapper-for-openais-terminal-agent.md) — Implements `CodexCLIBackend`, which spawns OpenAI's `codex` CLI as an async subprocess and parses its NDJSON event stream. Prompts are passed via stdin rather than as command-line arguments to avoid OS argument-length limits, and a 10 MiB subprocess buffer accommodates large MCP tool results.
- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.
- [External Agent Delegation — Subprocess Execution of CLI Agents](./external-agent-delegation-subprocess-execution-of-cli-agents.md) — Provides `ExternalAgentDelegate`, a utility for dispatching tasks to external CLI agents (currently only Claude Code CLI) via `asyncio` subprocesses. It captures structured JSON output and surfaces it as a `DelegationResult`, enabling PocketPaw to use external agents as heavyweight tools.

### `system` — 1 article(s)

- [System and Usage Metrics API](./system-and-usage-metrics-api.md) — The metrics router provides two endpoints: one that reports real-time system resource usage (CPU, RAM, disk, uptime, battery), and one that clears accumulated usage records. Both are protected by an admin scope to prevent unprivileged callers from inspecting host hardware details.

### `system hardening` — 1 article(s)

- [Security Audit CLI Tests — Config Permissions, API Key Exposure, and Audit Runner](./security-audit-cli-tests-config-permissions-api-key-exposure-and-audit-runner.md) — This test file validates PocketPaw's security audit command-line tool, which checks for world-readable config files, plaintext API keys, missing audit logs, unreachable guardian agents, invalid file jails, risky tool profiles, and dangerous permission bypasses. Tests cover both detection and auto-fix capabilities.

### `system health` — 1 article(s)

- [Health Diagnostic Tools: System Checks, Error Logs, and Config Doctor](./health-diagnostic-tools-system-checks-error-logs-and-config-doctor.md) — The `health.py` module provides three diagnostic `BaseTool` subclasses — `HealthCheckTool`, `ErrorLogTool`, and `ConfigDoctorTool` — that give the PocketPaw agent access to the runtime health engine, persistent error log, and configuration validator. These tools exist so the agent can self-diagnose problems when a user reports something is broken, rather than returning a generic error and asking the user to check logs manually.

### `system integration` — 1 article(s)

- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.

### `system monitoring` — 1 article(s)

- [System Status Tool: Real-Time Hardware Metrics with psutil Fallback](./system-status-tool-real-time-hardware-metrics-with-psutil-fallback.md) — The system status tool collects CPU, memory, disk, battery, and uptime metrics using psutil and formats them as a human-readable markdown string for display in the agent UI. When psutil is not installed, it falls back to a limited report using the standard library `platform` module, preserving basic utility without requiring the desktop extras.

### `system prompt` — 2 article(s)

- [AGENTS.md Support Package: Standardized Agent Capability Declaration](./agentsmd-support-package-standardized-agent-capability-declaration.md) — The `agents_md` package implements support for the Anthropic AGENTS.md specification — a convention where repositories publish a markdown file describing constraints, capabilities, and instructions for AI agents operating on that codebase. PocketPaw both reads AGENTS.md from target repositories (to inject constraints into the agent system prompt) and publishes its own AGENTS.md for other agents to consume.
- [Bootstrap System Tests: System Prompt Assembly, Identity File Caching, and Channel Hints](./bootstrap-system-tests-system-prompt-assembly-identity-file-caching-and-channel-.md) — This test suite validates PocketPaw's bootstrap pipeline, which reads identity configuration files (IDENTITY.md, SOUL.md, STYLE.md, USER.md) and assembles them into a structured system prompt for the AI agent. Tests cover the XML-tag layout contract of `BootstrapContext`, the mtime-based file cache in `DefaultBootstrapProvider`, and the channel-specific formatting hints added by `AgentContextBuilder`.

### `system-monitoring` — 2 article(s)

- [SystemInfoTool: Host Resource Monitoring with Optional psutil Integration](./systeminfotool-host-resource-monitoring-with-optional-psutil-integration.md) — `SystemInfoTool` reports CPU, RAM, disk, network I/O, and optionally top processes by delegating to `get_system_status()` for the base snapshot, then augmenting with `psutil` data when available. The psutil dependency is treated as optional — the tool degrades gracefully to the base snapshot if psutil is not installed.
- [SystemInfoTool: System Status with Optional psutil Dependency](./systeminfotool-system-status-with-optional-psutil-dependency.md) — The `SystemInfoTool` reports CPU, RAM, disk, network, and process information to agents, with graceful degradation to platform-only info when `psutil` is not installed. Tests cover the basic output contract, psutil-dependent sections (network, processes), fallback behavior without psutil, error handling when `get_system_status` raises, and the tool's name and schema definition.

### `system-prompt` — 1 article(s)

- [Agent Context Builder — System Prompt Assembly with Budget Tracking](./agent-context-builder-system-prompt-assembly-with-budget-tracking.md) — AgentContextBuilder is the central orchestrator that assembles the full system prompt for each agent turn, weaving together identity, instructions, memory recalls, knowledge-base results, channel hints, health state, and injected files while staying within a configurable character budget. Its priority-based injection model ensures critical blocks (identity, instructions) are never dropped, while optional blocks are trimmed or skipped as budget tightens.

### `task execution` — 1 article(s)

- [SimulationClock and Tick-Synchronized Scheduler Tests](./simulationclock-and-tick-synchronized-scheduler-tests.md) — This test file covers `SimulationClock` (a deterministic discrete-time clock for agent simulation), `TickSnapshot` (tick-state serialization), `DependencyScheduler.run_tick_synchronized` (tick-driven task dispatch), and simulation tick metadata injection into task prompts. Tests validate clock advancement, snapshot immutability, scheduler dispatch order, and prompt metadata inclusion.

### `task lifecycle` — 1 article(s)

- [A2A Protocol Pydantic Models: Tasks, Messages, Parts, and Agent Cards](./a2a-protocol-pydantic-models-tasks-messages-parts-and-agent-cards.md) — This module defines all Pydantic models for the A2A protocol (v0.2.5+), including the task lifecycle state machine, typed content parts (text/file/data), message and artifact models, request parameter schemas, the Agent Card capabilities manifest, and JSON-RPC 2.0 envelope models with streaming event types.

### `task orchestration` — 1 article(s)

- [Deep Work E2E: Project Lifecycle, Output Chaining, Auto-Retry, Timeout, and Cancellation](./deep-work-e2e-project-lifecycle-output-chaining-auto-retry-timeout-and-cancellat.md) — This test suite exercises PocketPaw's Deep Work (multi-agent task orchestration) system end-to-end through the FastAPI test client, covering project approval, task dependency cascades, output persistence, auto-retry on failure, timeout triggers, project cancellation in all lifecycle states, and the manual retry and skip APIs.

### `task scheduling` — 1 article(s)

- [Task Skip Semantics: Dependency Unblocking and Project Completion](./task-skip-semantics-dependency-unblocking-and-project-completion.md) — Covers the test suite that validates how SKIPPED tasks interact with the dependency scheduler and project completion logic in PocketPaw's deep-work system. Ensures skipped tasks satisfy blocked_by conditions, contribute to project completion, and appear correctly in progress reporting.

### `task-dispatch` — 1 article(s)

- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.

### `telegram` — 5 article(s)

- [Telegram Bot Gateway — Agent Runtime Entry Point](./telegram-bot-gateway-agent-runtime-entry-point.md) — bot_gateway.py wires the three core runtime components — MessageBus, TelegramAdapter, and AgentLoop — into a running process for Telegram-based deployments. It handles clean shutdown on cancellation and is the primary entry point when running PocketPaw as a Telegram bot.
- [Telegram Channel Adapter with Streaming, Typing Indicators, and Forum Topic Support](./telegram-channel-adapter-with-streaming-typing-indicators-and-forum-topic-suppor.md) — TelegramAdapter connects PocketPaw to the Telegram Bot API using python-telegram-bot, supporting streaming responses via live message edits, animated typing indicators, media file sending, voice note detection, and Telegram forum topic (Supergroup) thread routing.
- [Telegram Channel Router: Setup and Pairing Flow](./telegram-channel-router-setup-and-pairing-flow.md) — Implements the three REST endpoints that drive Telegram bot pairing: checking current configuration status, initiating the pairing flow with a bot token, and polling for pairing completion. The router wraps the shared pairing state dict from the dashboard module, adding token validation and graceful handling of missing optional dependencies.
- [Telegram Pairing Schema Contracts](./telegram-pairing-schema-contracts.md) — Defines the Pydantic request and response models used by the Telegram channel pairing flow. These schemas enforce a clean contract between the REST API layer and the bot token validation and QR code generation logic.
- [Web Server: QR Code Pairing Flow for Telegram Bot Onboarding](./web-server-qr-code-pairing-flow-for-telegram-bot-onboarding.md) — The web server module drives PocketPaw's QR code–based Telegram pairing flow: it generates a signed deep link, serves a minimal HTML page with a scannable QR code, and simultaneously listens for the bot's `/start <secret>` confirmation over the Telegram API. Dynamic port allocation ensures the local server starts cleanly even when the default port is busy.

### `telegram-integration` — 1 article(s)

- [fetch.py: Jail-Safe File Browser with Pydantic Validation and Telegram Keyboard Support](./fetchpy-jail-safe-file-browser-with-pydantic-validation-and-telegram-keyboard-su.md) — `fetch.py` provides path-traversal-safe file browsing via `is_safe_path()`, a `FetchRequest` Pydantic model, and `handle_path()` / `list_directory()` helpers. The Telegram `InlineKeyboardMarkup` import is optional, enabling interactive directory navigation buttons in Telegram without breaking non-Telegram deployments.

### `telemetry` — 1 article(s)

- [UsageTracker: Append-Only Token and Cost Tracking Across Sessions](./usagetracker-append-only-token-and-cost-tracking-across-sessions.md) — UsageTracker records per-request LLM usage (input tokens, output tokens, cached tokens, cost) as append-only JSONL entries in `~/.pocketpaw/usage.jsonl`, providing aggregation helpers for the `/api/v1/metrics/usage` endpoint. Thread safety is achieved with a `threading.Lock` so background telemetry threads and async request handlers can safely share the tracker.

### `templates` — 1 article(s)

- [Pocket Layout Export and User-Defined Template Registry](./pocket-layout-export-and-user-defined-template-registry.md) — This module provides two distinct but related capabilities: a pure YAML serialisation of a pocket's ripple spec for export or sharing, and an in-process workspace-scoped store for user-defined pocket templates. Both capabilities back new REST endpoints added in Cluster B Sub-PR #3 to close a gap in the UI testing guide around widget layout save and share.

### `tenancy` — 1 article(s)

- [Workspace Document: Organization Tenancy Model](./workspace-document-organization-tenancy-model.md) — The `Workspace` document is the top-level tenancy boundary in PocketPaw EE, representing one organization or deployment. It records billing plan, seat count, configurable settings, and supports soft deletion, with a unique `slug` index for human-readable URL routing.

### `test` — 355 article(s)

- [A2A Client Tests: HTTP Operations, Context Manager Reuse, Streaming, Delegate Tool, and SSRF Protection](./a2a-client-tests-http-operations-context-manager-reuse-streaming-delegate-tool-a.md) — Tests for the PocketPaw Agent-to-Agent (A2A) client library, covering HTTP task operations (send, get, cancel, stream), response error handling, agent card fetching with caching, the `A2ADelegateTool` multi-turn wrapper, and SSRF protection that blocks requests to private IP ranges and invalid schemes.
- [A2A Server Tests: Models, State Machines, JSON-RPC Dispatch, and Task Lifecycle](./a2a-server-tests-models-state-machines-json-rpc-dispatch-and-task-lifecycle.md) — Extensive tests for the PocketPaw A2A server implementation covering Pydantic model serialisation with type discriminators, TaskState transition validation, JSON-RPC 2.0 dispatcher behaviour (parse errors, batch requests, method routing), and the full task lifecycle (submit, poll, stream, cancel) against a minimal in-memory FastAPI app.
- [ABAC Config Tests: Rule Loading and Allow/Deny Logic](./abac-config-tests-rule-loading-and-allowdeny-logic.md) — This module tests the Attribute-Based Access Control (ABAC) configuration layer for the files subsystem, covering YAML rule loading, the allow logic for tagged vs. untagged entries, attribute matching, and the deny-override behavior when a file carries multiple tags.
- [Agent Backend Protocol: Capability Flags, BackendInfo, and Identity Contract Tests](./agent-backend-protocol-capability-flags-backendinfo-and-identity-contract-tests.md) — This test suite validates the core contracts of PocketPaw's agent backend abstraction layer, covering the `_DEFAULT_IDENTITY` fallback string, the `Capability` flag enum, the immutable `BackendInfo` descriptor, and the `AgentBackend.run()` method signature. Together these tests enforce the interface that every backend adapter must satisfy to be a valid plugin in PocketPaw's multi-backend architecture.
- [Agent Backend Registry: Lazy Import, Legacy Name Resolution, and Plugin Registration Tests](./agent-backend-registry-lazy-import-legacy-name-resolution-and-plugin-registratio.md) — This test suite covers PocketPaw's backend registry, which provides lazy-loaded access to agent backends (Claude SDK, OpenAI Agents, Google ADK, OpenCode) while maintaining backward compatibility through a legacy name mapping. Tests verify that deprecated names like `gemini_cli` transparently resolve to `google_adk`, that unknown backends return `None` rather than raising, and that third-party plugins can register themselves at runtime.
- [Agent Backends and Router Tests: ClaudeSDKBackend, Registry Routing, Dangerous Hook, and CLI Auth](./agent-backends-and-router-tests-claudesdkbackend-registry-routing-dangerous-hook.md) — Tests for the multi-SDK agent architecture covering `AgentEvent` protocol, `ClaudeSDKBackend` (formerly ClaudeAgentSDKWrapper), registry-based `AgentRouter` backend selection with legacy fallbacks, the fail-closed dangerous command hook, and a regression test for PocketPaw incorrectly requiring an API key when Claude CLI authentication is active.
- [Agent Bridge Attachment Forwarding Regression Test](./agent-bridge-attachment-forwarding-regression-test.md) — This regression test guards against the silent attachment-drop bug in `_run_agent_response`, where file attachments sent by users in channel messages were never forwarded into the agent's prompt. The test confirms that the channel code path formats attachment metadata — filename, MIME type, and size — into the prompt in the same shape used by the DM path.
- [Agent Bridge Event Routing and Wire Type Tests](./agent-bridge-event-routing-and-wire-type-tests.md) — This module verifies that `agent_bridge` routes all group broadcasts through `emit()` rather than directly calling `ws_manager.broadcast_to_group`, and that the four agent stream event types (`AgentStreamStart`, `AgentStreamChunk`, `AgentToolUse`, `AgentStreamEnd`) are correctly defined and importable from the bridge.
- [Agent Delegation: ExternalAgentDelegate and DelegateTool Tests](./agent-delegation-externalagentdelegate-and-delegatetool-tests.md) — This suite tests PocketPaw's agent delegation system, which allows a running agent to hand off subtasks to external CLI-based agents (initially Claude Code). It covers the DelegationResult dataclass, availability detection via PATH lookup, unknown agent rejection, and the DelegateTool's trust level, parameter schema, and execution paths including error handling for missing installations.
- [Agent Domain Schema Validation Tests](./agent-domain-schema-validation-tests.md) — This module comprehensively tests the Pydantic schemas for the agents domain — `CreateAgentRequest`, `UpdateAgentRequest`, `DiscoverRequest`, `AgentResponse`, `ScopeAssignmentRequest`, and `ScopeAssignmentResponse` — covering required fields, defaults, length constraints, visibility validation, and scope normalization. Scope-related tests were added in the `feat/cluster-d-agent-scope-picker` feature.
- [Agent Loop Media Helpers: _format_bytes Unit Tests](./agent-loop-media-helpers-formatbytes-unit-tests.md) — Focused unit tests for the `_format_bytes` helper in `pocketpaw.agents.loop`, verifying that file sizes are formatted correctly across the B, KB, MB, and GB ranges. This utility generates human-readable size strings for media attachment prompts injected into the agent's context.
- [Agent Loop Pocket Threading Tests: Cloud User and Workspace Context Resolution](./agent-loop-pocket-threading-tests-cloud-user-and-workspace-context-resolution.md) — Tests for the `_create_pocket_and_session` and `_publish_pocket_event` functions in `pocketpaw.agents.loop`, covering all resolution paths for threading cloud user identity and workspace context through pocket creation: explicit ids, active-workspace fallback, first-user fallback for self-hosted deployments, graceful handling of invalid ids, and metadata propagation in pocket events.
- [Agent Loop Tests: Message Processing, AgentEvent Architecture, Error Handling, and GC Lock Management](./agent-loop-tests-message-processing-agentevent-architecture-error-handling-and-g.md) — Tests for the unified `AgentLoop` that orchestrates message consumption, agent router invocation, memory management, and outbound publishing. Updated for the `AgentEvent`-based streaming architecture (replacing raw dict chunks), these tests verify end-to-end message processing, graceful error handling, stale-lock GC, and loop teardown.
- [Agent Scope Rules: Normalization and Assignment Authorization Tests](./agent-scope-rules-normalization-and-assignment-authorization-tests.md) — This module tests `ee.cloud.agents.scope_rules`, covering the `normalise_and_validate_scopes` function (grammar enforcement, lowercasing, deduplication, forbidden-wildcard rejection) and the `admin_can_assign_scopes` function (containment check that prevents scope-narrowed admins from assigning scopes outside their own grant).
- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.
- [Agent Soul Configuration and OCEAN Personality Schema Tests](./agent-soul-configuration-and-ocean-personality-schema-tests.md) — This module tests the `AgentConfig` model's soul-related fields — default values, persona assignment, custom OCEAN personality vectors, and absence of a `soul_path` attribute — and verifies that `CreateAgentRequest` correctly propagates `persona` and soul OCEAN customization fields.
- [Agent Status API Tests: Auth, Snapshot Shape, Version Tracking, and CLI Formatting](./agent-status-api-tests-auth-snapshot-shape-version-tracking-and-cli-formatting.md) — Tests for PocketPaw's agent status API endpoint, covering authentication via header and query parameter, snapshot structure validation against the API contract, version-based change detection for efficient polling, and CLI duration formatting helpers.
- [AgentLoop Concurrency Controls: Session Locks, Global Semaphore, and File Memory Safety](./agentloop-concurrency-controls-session-locks-global-semaphore-and-file-memory-sa.md) — This test module validates the three-layer concurrency model in PocketPaw's AgentLoop: per-session serialization via asyncio locks, cross-session parallelism as the happy path, and a global semaphore that caps total simultaneous conversations. It also covers the FileMemoryStore's write lock and transient PermissionError retry logic that prevent JSON corruption under concurrent writes.
- [AgentRouter Fallback and Streaming Tests](./agentrouter-fallback-and-streaming-tests.md) — This test file validates `AgentRouter`'s fault-tolerant backend selection logic, covering hard exception failures, error-event failures, full backend chain exhaustion, streaming event pass-through, and no-fallback configurations. The tests use in-process stub backends injected via `monkeypatch` to exercise the router without real LLM backends.
- [AGENTS.md Support Tests: Loader, Caching, Directory Walk, Context Injection, and SystemEvent Emission](./agentsmd-support-tests-loader-caching-directory-walk-context-injection-and-syste.md) — Tests for the AGENTS.md feature (issue #456) that allows project-specific constraints to be injected into the agent's system prompt. Covers the `AgentsMdLoader` find-and-cache logic (including `.git` boundary stopping, nearest-file preference, and mtime-based cache invalidation), `AgentsMd` formatting, `AgentContextBuilder` injection, and `AgentLoop` `SystemEvent` emission when the file is found.
- [API Backward Compatibility and Versioned Endpoint Tests](./api-backward-compatibility-and-versioned-endpoint-tests.md) — This module verifies that both the legacy `/api/` endpoints and the versioned `/api/v1/` endpoints in the PocketPaw dashboard respond with HTTP 200, guarding against breaking changes during the v1 API migration. It also checks that OpenAPI documentation endpoints remain accessible.
- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.
- [API Key Management Tests: Creation, Verification, Rotation, and REST Endpoints](./api-key-management-tests-creation-verification-rotation-and-rest-endpoints.md) — Comprehensive tests for PocketPaw's `APIKeyManager` class and its REST endpoints covering the full key lifecycle — creation with scope validation, verification, revocation, rotation, and expiry rejection. The test suite isolates all disk I/O to a temp directory to prevent cross-test contamination and ensure file permission checks work correctly.
- [API Keys Panel Acceptance Tests: UI Lifecycle Flows for Cluster F](./api-keys-panel-acceptance-tests-ui-lifecycle-flows-for-cluster-f.md) — End-to-end acceptance tests that walk the exact flows the `ApiKeysPanel.svelte` UI executes — create, list, rotate (with old key invalidation), revoke, and sort-by-creation. These tests complement unit-level coverage by pinning the API shape the frontend depends on, catching regressions that break the dashboard silently.
- [API-Only Serve Mode Tests: Structure, Auth Middleware, CLI Parsing, and Socket Safety](./api-only-serve-mode-tests-structure-auth-middleware-cli-parsing-and-socket-safet.md) — Tests for PocketPaw's `serve` command, which launches a lightweight API-only server without the dashboard UI. Covers endpoint availability, auth middleware behavior, CLI argument parsing, and a regression suite proving that sockets used for local IP detection are always closed — even when errors occur during detection.
- [AudienceResolver Behavioral Tests: Fan-Out Logic, Caching, and Edge Cases](./audienceresolver-behavioral-tests-fan-out-logic-caching-and-edge-cases.md) — These tests verify the complete behavioral contract of AudienceResolver — determining which user IDs receive each event type, including removed-member inclusion, invite routing to admins plus invitee, TTL caching with manual invalidation, session deduplication, and empty-list fallback for unknown event types.
- [AudienceResolver Exhaustive Coverage: All Event Subclasses Must Resolve and Have Unique Wire Types](./audienceresolver-exhaustive-coverage-all-event-subclasses-must-resolve-and-have-.md) — These tests use Python's inspect module to enumerate every Event subclass at runtime and verify AudienceResolver handles each without raising. A companion test enforces that no two event classes share the same EVENT_TYPE wire string, preventing silent routing collisions when new event types are added.
- [Audit FTS Security Tests: SQL Injection Prevention and LIKE Wildcard Escaping](./audit-fts-security-tests-sql-injection-prevention-and-like-wildcard-escaping.md) — Security regression tests proving that the `q` parameter on PocketPaw's `/runtime/audit` search endpoint is fully parameter-bound and cannot corrupt or drop the `audit_log` table, and that LIKE special characters (`%`, `_`) in the query are correctly escaped to prevent wildcard-widened result sets. Tests correspond to Cluster C PR4.
- [Audit Logging Completeness Tests: Append-Only Guarantee, Rotation, Failure Resilience, and PII Masking](./audit-logging-completeness-tests-append-only-guarantee-rotation-failure-resilien.md) — Layer 7 security tests verifying that PocketPaw's audit logger captures all security-relevant events, maintains an append-only JSONL log that cannot be deleted through the logger interface, archives rather than deletes on rotation, survives write failures without crashing, and masks PII (e.g., SSNs) before writing to disk.
- [Audit Store Tests: Model Validation, Filtering, Export, and API Endpoints](./audit-store-tests-model-validation-filtering-export-and-api-endpoints.md) — Comprehensive tests for PocketPaw's `AuditStore` — the SQLite-backed audit trail that records all security-relevant operations. Tests cover the `AuditEntry` model, log persistence, multi-field query filtering, date range filtering, CSV and JSON export, REST API endpoints, and integration helpers for tool execution and connector sync logging.
- [Auth API Tests: Session Exchange, Cookie Login, Rate Limiting, and QR Code](./auth-api-tests-session-exchange-cookie-login-rate-limiting-and-qr-code.md) — This test module comprehensively covers PocketPaw's `/api/v1/auth` router, verifying the master-token-to-session exchange, cookie-based login with correct Secure flag handling under reverse proxies, logout, token regeneration, QR code generation, and IP-based rate limiting on all three auth endpoints.
- [Auth Domain Schema Tests](./auth-domain-schema-tests.md) — This module tests the Pydantic schemas for the auth domain — `ProfileUpdateRequest`, `SetWorkspaceRequest`, and `UserResponse` — verifying optional field defaults, value round-trips, and the structure of the user identity response.
- [Auth Exception Logging Tests: Silent Swallow Prevention in Auth Dispatch and OAuth2](./auth-exception-logging-tests-silent-swallow-prevention-in-auth-dispatch-and-oaut.md) — Regression tests for issue #627 — verifies that bare `except Exception: pass` blocks in authentication dispatch paths have been replaced with `logger.warning(..., exc_info=True)` calls. Tests cover API key validation, OAuth2 token validation, cookie login branches, and OAuth2 server audit log failures, ensuring auth failures are always logged and always return 401.
- [Automation Evaluator: Rule-to-Daemon Sync, Lifecycle, Cooldown, Fire Modes, and Router Integration Tests](./automation-evaluator-rule-to-daemon-sync-lifecycle-cooldown-fire-modes-and-route.md) — Tests for the `AutomationEvaluator` — the runtime that translates `AutomationStore` rules into daemon intentions, evaluates which rules should fire, and dispatches actions. Covers the full evaluator lifecycle, cooldown enforcement, three execution modes (require approval, auto-execute, notify-only), daemon sync/unsync, and the router bridge integration.
- [Backends API Tests: Listing, Availability Checks, and Safe Install Subprocess Handling](./backends-api-tests-listing-availability-checks-and-safe-install-subprocess-handl.md) — This test file covers PocketPaw's `/api/v1/backends` router, verifying that the backend registry is exposed correctly, that the `_check_available` helper correctly detects missing imports, and that the async install endpoint handles subprocess timeouts and stderr secret redaction without leaking sensitive data.
- [Bootstrap System Tests: System Prompt Assembly, Identity File Caching, and Channel Hints](./bootstrap-system-tests-system-prompt-assembly-identity-file-caching-and-channel-.md) — This test suite validates PocketPaw's bootstrap pipeline, which reads identity configuration files (IDENTITY.md, SOUL.md, STYLE.md, USER.md) and assembles them into a structured system prompt for the AI agent. Tests cover the XML-tag layout contract of `BootstrapContext`, the mtime-based file cache in `DefaultBootstrapProvider`, and the channel-specific formatting hints added by `AgentContextBuilder`.
- [Brew & Co. Business Scenario: End-to-End Fabric and Instinct Pipeline Walkthrough](./brew-co-business-scenario-end-to-end-fabric-and-instinct-pipeline-walkthrough.md) — A narrative-driven end-to-end test that simulates a real small business — a coffee shop on Monday morning — using PocketPaw's `FabricStore` for business objects and `InstinctStore` for the AI decision pipeline. It demonstrates the full operational flow: data modeling, connector sync simulation, threshold-triggered automation, human approval, execution, and audit export.
- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.
- [Browser Automation Test Package Initializer](./browser-automation-test-package-initializer.md) — This is the `__init__.py` for the `tests/test_browser/` package, marking the browser automation tests as a proper Python package so pytest can discover and import them as a cohesive module group. The file contains no code or fixtures — its presence enables relative imports between the four browser test modules.
- [Browser Session Management Tests: Lifecycle, Idle Cleanup, Singleton, and Concurrency](./browser-session-management-tests-lifecycle-idle-cleanup-singleton-and-concurrenc.md) — This test suite validates `BrowserSessionManager`, which maintains a pool of named `BrowserSession` objects backed by individual `BrowserDriver` instances. Tests cover session creation, reuse, idle timeout cleanup, graceful close, the global singleton accessor, and thread-safety under concurrent `get_or_create` calls.
- [Browser Snapshot Generator Tests: Accessibility Tree to Agent-Readable Text Conversion](./browser-snapshot-generator-tests-accessibility-tree-to-agent-readable-text-conve.md) — This test suite validates `SnapshotGenerator`, which converts Playwright's accessibility tree JSON into a compact, ref-tagged text representation that AI agents can read and reason about. Tests cover the full rendering pipeline: interactive element ref assignment, non-interactive element handling, heading/list/image rendering, hidden element filtering, password field masking, and the `RefMap` reference number system.
- [BrowserDriver Tests: Playwright Wrapper for Navigation, Interaction, and Lifecycle Management](./browserdriver-tests-playwright-wrapper-for-navigation-interaction-and-lifecycle-.md) — This test suite validates `BrowserDriver`, PocketPaw's thin wrapper around the Playwright async API. Tests cover initialization modes (headless/headful), async context manager lifecycle, browser launch and viewport configuration, all navigation and interaction operations (navigate, click, type, scroll), snapshot generation, screenshot capture, and defensive checks for using the driver before it's launched.
- [BrowserTool Integration Tests: Agent-Facing Browser Interface, Actions, Error Handling, and Session Management](./browsertool-integration-tests-agent-facing-browser-interface-actions-error-handl.md) — This test suite validates `BrowserTool`, the agent-facing wrapper that exposes browser automation as a single tool with an action-dispatch interface. Tests cover the tool's metadata contract (name, trust level, JSON schema), all seven supported actions (navigate, click, type, scroll, snapshot, screenshot, close), input validation, error propagation, and multi-session isolation.
- [Channel Autostart Tests: Per-Channel Boot Control, Dashboard Startup, API Integration, and Config Persistence](./channel-autostart-tests-per-channel-boot-control-dashboard-startup-api-integrati.md) — This test suite validates PocketPaw's per-channel autostart feature, which lets operators control which channel adapters automatically start when the dashboard boots. Tests cover the `_channel_autostart_enabled()` helper, startup behavior with mixed enabled/disabled channels, the REST API endpoints for reading and saving autostart config, and round-trip persistence to disk.
- [Channel-Aware Markdown Formatting Tests](./channel-aware-markdown-formatting-tests.md) — This test module validates `convert_markdown`, PocketPaw's channel-aware formatter that translates LLM-generated markdown into the native formatting of each messaging platform. Each channel has its own rendering rules — WhatsApp uses `*bold*`, Slack uses `<url|label>` hyperlinks, Signal strips all formatting — and the tests prevent these channel-specific transformations from regressing.
- [Channel Notifier Tests: Multi-Target Message Dispatch, Validation, and Settings-Driven Defaults](./channel-notifier-tests-multi-target-message-dispatch-validation-and-settings-dri.md) — This test suite validates `notify()`, PocketPaw's high-level function for pushing outbound messages to one or more channels. Tests cover multi-target fan-out, graceful handling of malformed target strings, unknown channel names, empty target lists, settings-driven default targets, chat IDs containing colons, and delivery across all supported channels.
- [Channels API Tests: Status, Config Save, Toggle, and Extras Check](./channels-api-tests-status-config-save-toggle-and-extras-check.md) — This test file covers PocketPaw's `/api/v1/channels` router, which exposes status, configuration, and lifecycle management for messaging channel adapters such as Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It also covers `/extras/check`, the endpoint that detects whether optional channel dependencies are installed.
- [Chat API Test Suite: SSE Streaming, Session Bridge, and Request Aliases](./chat-api-test-suite-sse-streaming-session-bridge-and-request-aliases.md) — Covers the full test surface of PocketPaw's `/api/v1/chat` router, including the `_APISessionBridge` async queue, SSE stream/stop endpoints, non-streaming send, media URL rewriting, and camelCase field alias handling. These tests exist to catch silent regressions in the real-time chat path that would break frontend consumers without an obvious error.
- [Chat Domain Schema Validation Tests](./chat-domain-schema-validation-tests.md) — This module comprehensively tests all Pydantic schemas in the chat domain — group management, message sending, editing, reactions, WebSocket inbound/outbound messages, cursor pagination, and attachment handling — covering field defaults, length constraints, enum validation, and discriminated union types for WebSocket message dispatch.
- [ChromaAdapter: Vector Database Integration for Semantic Memory Storage](./chromaadapter-vector-database-integration-for-semantic-memory-storage.md) — The `ChromaAdapter` wraps ChromaDB to provide async add, search, get-by-ID, delete, and upsert operations for PocketPaw's semantic memory tier. Tests are conditionally skipped when `chromadb` is not installed, and cover the core CRUD operations, graceful handling of small collections during search, upsert (duplicate ID update), and optional metadata storage.
- [ClaudeAgentSDK Dispatch and Persistent Client Tests](./claudeagentsdk-dispatch-and-persistent-client-tests.md) — This module tests the dispatch logic for `ClaudeAgentSDK`, documenting the removal of the `_fast_chat` direct-API bypass in version 0.4.16 and the introduction of a persistent `ClaudeSDKClient` that reuses connections across calls. It verifies that all task complexities (SIMPLE, MODERATE, routing disabled) flow through the CLI path and validates the client lifecycle: creation, reuse, reconnection on model change, fallback on failure, interrupt, and cleanup.
- [Cloud Chat Test Fixtures: In-Memory MongoDB via mongomock-motor](./cloud-chat-test-fixtures-in-memory-mongodb-via-mongomock-motor.md) — The `beanie_memory_db` fixture initialises Beanie against an isolated in-memory MongoDB database for each test using `mongomock-motor`, enabling the full cloud chat test suite to run in CI without a real MongoDB service. It includes a compatibility shim that silences unsupported keyword arguments in mongomock-motor's `list_collection_names` stub.
- [Cloud Chat Test Sub-Package Initialiser](./cloud-chat-test-sub-package-initialiser.md) — The empty `tests/cloud/chat/__init__.py` marks the `tests/cloud/chat/` directory as a Python package so pytest can discover chat-specific cloud tests as `tests.cloud.chat.*` and so that the `conftest.py` in the same directory can provide fixtures to the full chat test sub-package.
- [Cloud Context Threading in Chat API: User ID and Workspace Propagation Tests](./cloud-context-threading-in-chat-api-user-id-and-workspace-propagation-tests.md) — Tests that `resolve_cloud_context` correctly injects `cloud_user_id` and `cloud_workspace_id` into `InboundMessage.metadata` when an authenticated cloud user is present, and leaves those keys absent for anonymous or unauthenticated requests. The test file operates at the unit level — directly calling `_build_inbound_message` and `_send_message` — to avoid the overhead of standing up a full SSE streaming client.
- [Cloud Error Hierarchy: Structured HTTP Error Contract Tests](./cloud-error-hierarchy-structured-http-error-contract-tests.md) — This test file validates the `ee.cloud.shared.errors` module, which provides a typed exception hierarchy that maps Python exceptions to HTTP status codes and machine-readable error codes. It ensures that every error class produces the correct status, code, message, and serialization shape expected by API consumers.
- [Cloud Files Browse Mount Tests: ABAC Filtering and Provider Resolution](./cloud-files-browse-mount-tests-abac-filtering-and-provider-resolution.md) — This test module validates the `browse_mount` function in PocketPaw's cloud files subsystem, covering successful entry listing, ABAC tag-based filtering, and graceful errors when a mount references an unregistered provider. Together these tests protect the file-browsing surface against misconfiguration and unauthorized data exposure.
- [Cloud Files Error Codes and Event Importability Contract Tests](./cloud-files-error-codes-and-event-importability-contract-tests.md) — This test module enforces the public contract of PocketPaw's cloud files error hierarchy and event definitions, verifying that each error carries the correct machine-readable code and HTTP status, and that all file lifecycle events are importable. These tests act as a regression guard for the API surface consumed by clients and downstream event handlers.
- [Cloud Files FastAPI Router Integration Tests: Tree and Browse Endpoints](./cloud-files-fastapi-router-integration-tests-tree-and-browse-endpoints.md) — This module runs end-to-end HTTP integration tests against the FastAPI router built by `build_router`, covering tree structure rendering, entry browsing, 404 for unknown mounts, and 403 for cross-workspace access attempts. It uses HTTPX's `ASGITransport` to drive the full request-response cycle in-process without a running server.
- [Cloud Files Permissions: ABAC Filtering and Capability Derivation Tests](./cloud-files-permissions-abac-filtering-and-capability-derivation-tests.md) — This module validates the layered permissions model in PocketPaw's cloud files system, covering ABAC tag filtering, RBAC-and-mount-writable capability intersection, and the `PermissionsEvaluator` composite. These tests ensure that no combination of user attributes, mount configuration, or entry tags can produce a capability set broader than what all three permission layers jointly allow.
- [Cloud Files Schema Validation Tests: FileEntry, MountConfig, Permission, and Page](./cloud-files-schema-validation-tests-fileentry-mountconfig-permission-and-page.md) — This module validates the Pydantic schema constraints for PocketPaw's core cloud files data models, confirming invariants like namespaced entry IDs, enumerated scopes, valid capability sets, absolute mount templates, and permission intersection semantics. These tests prevent malformed data from entering the system by verifying that validation errors are raised at construction time.
- [Cloud Files Tree Builder Tests: Ordering, Nesting, and Resilient Provider Failures](./cloud-files-tree-builder-tests-ordering-nesting-and-resilient-provider-failures.md) — This module tests `build_tree`, which assembles a hierarchical folder tree from all registered provider mounts. Key behaviors tested include mount ordering by the `order` field, recursive path-segment nesting, and graceful warning collection when individual providers fail rather than aborting the entire tree build.
- [Cloud Files Tree Cache Tests: TTL, Invalidation, and Multi-Tenant Key Isolation](./cloud-files-tree-cache-tests-ttl-invalidation-and-multi-tenant-key-isolation.md) — This module tests `CachedTreeBuilder`, the in-process 30-second cache layer over `build_tree` that prevents repeated provider fan-outs on every tree request. Tests validate that cache hits suppress redundant `list_mounts` calls, that explicit invalidation forces a rebuild, that TTL expiry triggers a new fan-out, and that clearing the cache invalidates all per-user keys.
- [Cloud Memory Test Fixtures: mongomock-motor Beanie Isolation and MongoMemoryStore Setup](./cloud-memory-test-fixtures-mongomock-motor-beanie-isolation-and-mongomemorystore.md) — This conftest provides two pytest fixtures for the cloud memory test suite: `beanie_memory_db` initializes an isolated in-memory MongoDB via `mongomock-motor` with a compatibility shim for Beanie's `list_collection_names` signature mismatch, and `store` returns a fresh `MongoMemoryStore` bound to that database. Each test gets its own uniquely-named in-memory database to prevent state leakage.
- [Cloud Memory Test Package Marker](./cloud-memory-test-package-marker.md) — This is an empty `__init__.py` that marks `tests/cloud/memory/` as a Python package, enabling pytest to discover tests within the directory and allowing relative imports from sibling test modules. No functional code is present; its role is purely structural.
- [Cloud Mount Integration Smoke Tests: Route and Error Handler Registration](./cloud-mount-integration-smoke-tests-route-and-error-handler-registration.md) — This smoke test suite verifies that `mount_cloud()` successfully registers all expected domain route groups (auth, workspaces, agents, chat, pockets, sessions, WebSocket, license) and the `CloudError` exception handler on a fresh FastAPI app. It acts as a fast regression guard ensuring no route group is accidentally omitted from the mount function.
- [Cloud Pydantic Model Tests: Field Defaults and Schema Validation Without a Database](./cloud-pydantic-model-tests-field-defaults-and-schema-validation-without-a-databa.md) — This test file verifies field-level Pydantic model behavior for core cloud entities (Group, Message, Pocket, Invite, Workspace, Session, Notification) using `model_construct()` to bypass Beanie's MongoDB-dependent `__init__`. It pins default values, optional field nullability, and enum acceptance for fields added in recent schema migrations.
- [Cloud Test Sub-Package Initialiser](./cloud-test-sub-package-initialiser.md) — The empty `tests/cloud/__init__.py` marks the `tests/cloud/` directory as a Python package, enabling pytest to discover cloud-related tests as part of the `tests.cloud` package namespace and allowing conftest fixtures defined at higher levels to propagate correctly into cloud test files.
- [Cloud Tests Shared Fixture: No-Op Realtime Bus](./cloud-tests-shared-fixture-no-op-realtime-bus.md) — This conftest.py installs an autouse fixture that replaces the realtime event bus with a no-op AsyncMock for the duration of every cloud test. This prevents AssertionError crashes when services call emit() in tests that do not start the full application lifecycle.
- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.
- [Comprehensive GuardianAgent Security Filter Tests](./comprehensive-guardianagent-security-filter-tests.md) — This module provides exhaustive coverage of `GuardianAgent` — PocketPaw's Layer 6 AI safety filter — including LLM-based classification, fail-closed behavior on all error conditions, local regex fallback when no API key is present, audit logging for every code path, concurrent safety, singleton correctness, and prompt-injection hardening (issue #873).
- [Config Validation Tests: API Key Formats, Numeric Constraints, and Enum Fields](./config-validation-tests-api-key-formats-numeric-constraints-and-enum-fields.md) — This test module covers three tiers of Settings validation: the `validate_api_key()` function that checks provider-specific key prefixes and warns on mismatches, Pydantic `gt`/`ge` constraints on numeric fields that prevent nonsensical configurations, and `Literal`-typed enum fields for provider selection. Together they ensure that misconfigured credentials and out-of-range settings fail loudly at startup rather than silently at runtime.
- [Connector Protocol: YAML Parsing, Registry, and DirectREST Adapter Lifecycle Tests](./connector-protocol-yaml-parsing-registry-and-directrest-adapter-lifecycle-tests.md) — This module tests the PocketPaw connector infrastructure — YAML definition parsing, the `ConnectorRegistry` for connector discovery and lifecycle management, and the `DirectRESTAdapter` for connecting, executing actions, and disconnecting from external REST APIs like Stripe.
- [Connector Status Route Tests: State Transitions, Pocket Isolation, and Secret Non-Leakage](./connector-status-route-tests-state-transitions-pocket-isolation-and-secret-non-l.md) — This test file locks down PocketPaw's connector status route contract — the full state machine from disconnected through connected to expired, strict per-pocket credential isolation so one pocket cannot see another's connection state, and the guarantee that OAuth secrets passed at connect time never appear in status responses.
- [Connector-to-Fabric Pipeline: Stripe Integration, Threshold Rules, and Instinct Proposal Chain Tests](./connector-to-fabric-pipeline-stripe-integration-threshold-rules-and-instinct-pro.md) — End-to-end tests for the data ingestion pipeline that runs from a YAML-defined connector (Stripe) through HTTP execution, Fabric object creation, threshold evaluation, and Instinct action proposal — all without real network calls via patched `httpx`. Covers the full chain including error paths and source deduplication behavior.
- [Connectors Test Package Initializer](./connectors-test-package-initializer.md) — This `__init__.py` marks `tests/connectors/` as a Python package and documents its scope as the test home for `pocketpaw.connectors.*` adapter implementations. Its single comment line serves as a breadcrumb for developers adding new connector tests.
- [Context Window Budget Tests: Priority-Based Block Assembly and KB Integration](./context-window-budget-tests-priority-based-block-assembly-and-kb-integration.md) — This test module, added in April 2026, validates `AgentContextBuilder._assemble_with_budget`, which assembles the agent's system prompt from priority-ranked blocks while respecting both a global character budget and per-block caps. It also tests the knowledge-base context injection path that calls the `kb-go` CLI subprocess to fetch relevant articles.
- [Copilot SDK Backend Tests: Capabilities, Provider Config, Session Management, and Tool Injection](./copilot-sdk-backend-tests-capabilities-provider-config-session-management-and-to.md) — This test module validates the `CopilotSDKBackend` — PocketPaw's adapter for GitHub Copilot's agent SDK — entirely through mocks, requiring no real CLI binary or SDK installation. It covers static capability metadata, provider-specific configuration dispatch, multi-turn session reuse, event stream mapping, and the injection of PocketPaw tool instructions into the Copilot prompt.
- [Core PocketPaw Tools: Status, Fetch, Screenshot, Config, and Router Tests](./core-pocketpaw-tools-status-fetch-screenshot-config-and-router-tests.md) — This test module provides broad coverage of PocketPaw's foundational tool and configuration layer, including the system status tool, the file fetch tool's path jail enforcement, the screenshot tool's optional dependency handling, the settings save/load lifecycle, and both the LLM and agent routers' initialization and error paths.
- [Correction Soul Bridge: Observe Callbacks and Procedural Memory Promotion Tests](./correction-soul-bridge-observe-callbacks-and-procedural-memory-promotion-tests.md) — This test suite covers the `CorrectionSoulBridge` — the enterprise component that writes user corrections into the Soul Protocol's memory system and promotes recurring correction patterns into procedural memory. It also validates the `InstinctCorrectionsTool`, the agent-facing interface that exposes captured corrections as readable context.
- [CreateSkillTool Tests — Skill Generation, Naming Validation, and YAML Frontmatter](./createskilltool-tests-skill-generation-naming-validation-and-yaml-frontmatter.md) — This test file validates `CreateSkillTool`, which allows agents to generate new PocketPaw skill files (SKILL.md) at runtime. Tests cover tool metadata, skill name regex validation, YAML frontmatter format, overwrite protection, skill loader reload triggering, and the allowed-tools and user-invocable flags.
- [Credential Store Tests: Fernet Encryption, Config Separation, Migration, and Log Scrubbing](./credential-store-tests-fernet-encryption-config-separation-migration-and-log-scr.md) — This test module validates PocketPaw's `CredentialStore` — the Fernet-encrypted store that keeps API keys off disk in plaintext — along with the separation of secrets from non-secret config, a one-time migration from legacy plaintext `config.json`, strict Unix file permissions, and a `SecretFilter` that scrubs API keys from log output.
- [Credential Store v2 Tests: AES-256-GCM, Argon2id, AEAD, Migration, and Platform Fallbacks](./credential-store-v2-tests-aes-256-gcm-argon2id-aead-migration-and-platform-fallb.md) — This test module validates the v2 upgrade of PocketPaw's `CredentialStore`, which replaces Fernet (PBKDF2-based) encryption with AES-256-GCM authenticated encryption using Argon2id key derivation. It covers the full v2 round-trip, authenticated data enforcement, automatic v1-to-v2 migration with backup, corruption recovery, and cross-platform hardware UUID fallbacks used to derive the encryption key.
- [Critical Gap Coverage: Real HTTP in DirectRESTAdapter and Agent Tools for Fabric and Instinct](./critical-gap-coverage-real-http-in-directrestadapter-and-agent-tools-for-fabric-.md) — This test file was written specifically to close gaps in test coverage identified during a gap analysis — areas where code existed but no test exercised it end-to-end. It covers real HTTP dispatch through `DirectRESTAdapter`, the Fabric agent tools (`FabricQueryTool`, `FabricCreateTool`), and the Instinct agent tools (`InstinctProposeTool`, `InstinctPendingTool`, `InstinctAuditTool`).
- [Critical Gaps Tests: Real HTTP in YAML Connectors, Fabric Queries, and Instinct Tool Operations](./critical-gaps-tests-real-http-in-yaml-connectors-fabric-queries-and-instinct-too.md) — This test module, created in March 2026, addresses three previously untested integration paths: the `DirectRESTAdapter` making authenticated HTTP calls to YAML-defined connectors like Stripe, the Fabric knowledge-graph query tools, and the Instinct action-proposal tools. It uses real YAML connector definitions from the connectors directory and mocks only the network layer.
- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.
- [Daily Notes Isolation Tests: Per-User Scoping and Backward-Compatible Legacy Note Visibility](./daily-notes-isolation-tests-per-user-scoping-and-backward-compatible-legacy-note.md) — This test file, added during security sprint cluster D (issue #887), validates that daily notes in the `MemoryManager` are scoped to the `sender_id` that created them, preventing one user's private notes from leaking into another user's agent context. It also tests the backward-compatibility guarantee that legacy notes written before the fix remain visible as system-wide entries.
- [Dashboard Auth Cookie Tests: Secure Flag Behavior Under HTTP and HTTPS Proxies](./dashboard-auth-cookie-tests-secure-flag-behavior-under-http-and-https-proxies.md) — This test file validates that the dashboard's session cookie is set without the `Secure` flag in plain HTTP environments (to avoid breaking self-hosted HTTP deployments) and with the `Secure` flag when the request arrives via an HTTPS reverse proxy detected by the `X-Forwarded-Proto: https` header.
- [Dashboard E2E Tests: UI Load, Deep Work View, Agent/Task CRUD, and Navigation](./dashboard-e2e-tests-ui-load-deep-work-view-agenttask-crud-and-navigation.md) — This Playwright-based end-to-end test suite verifies that the PocketPaw dashboard loads correctly in a real browser, that key UI elements (chat view, Deep Work view, agent/task creation modals, sidebar, remote access) are present and functional, and that the dashboard title matches the expected beta branding.
- [Dashboard Security Tests: Rate Limiting, Session Tokens, Tunnel Auth Bypass, Security Headers, and WebSocket Auth](./dashboard-security-tests-rate-limiting-session-tokens-tunnel-auth-bypass-securit.md) — This comprehensive test module validates PocketPaw's dashboard security hardening across six dimensions: token-bucket rate limiting, HMAC-signed session tokens with TTL, the localhost tunnel auth bypass fix, security headers, CORS rejection, and WebSocket authentication before the upgrade handshake completes.
- [Dashboard Starlette Compatibility Regression Test: Index Route on Starlette 1.0.0](./dashboard-starlette-compatibility-regression-test-index-route-on-starlette-100.md) — This short regression test ensures the dashboard's root route returns HTTP 200 with `text/html` content, specifically guarding against a breaking change introduced in Starlette 1.0.0 that changed the `TemplateResponse` constructor signature. The Starlette version is logged in assertion failure messages to make version-related CI failures immediately diagnosable.
- [Decision Trace Collector: Lifecycle, Event Aggregation, Deduplication, and Snapshot Persistence Tests](./decision-trace-collector-lifecycle-event-aggregation-deduplication-and-snapshot-.md) — This suite tests the `TraceCollector` context manager that records which Fabric objects, soul memories, KB articles, and tool calls an agent consulted while generating a decision. It also verifies the `FabricObjectSnapshot` persistence layer that captures point-in-time object state alongside each audit entry.
- [Decision Trace Wiring: Store-Level and Router-Level Integration Tests for Trace Persistence and Hydration](./decision-trace-wiring-store-level-and-router-level-integration-tests-for-trace-p.md) — Integration tests that verify `ReasoningTrace` objects are persisted into audit context when `propose()` is called, that `FabricObjectSnapshot` records are keyed to the correct audit row, and that the `/instinct/audit/{id}` hydration endpoint expands trace IDs into full records at the correct depth level.
- [Deep Agents Backend Tests: LangChain Integration, Provider Parsing, Content Extraction, and Registry](./deep-agents-backend-tests-langchain-integration-provider-parsing-content-extract.md) — This test module validates the `DeepAgentsBackend` — PocketPaw's LangChain-based agent backend — through mocks, requiring no real LangChain or LangGraph installation. It covers static metadata, the `provider:model` string parsing convention, LangGraph `Overwrite` object unwrapping, content extraction from mixed message formats, graceful degradation when the SDK is absent, and backend registry integration.
- [Deep Agents Tool Bridge: LangChain Integration and Graceful Degradation](./deep-agents-tool-bridge-langchain-integration-and-graceful-degradation.md) — These tests cover the `build_deep_agents_tools` bridge function which adapts PocketPaw tools for LangChain-based Deep Agents backends. The key behavior tested is that the function returns an empty list gracefully when `langchain-core` is not installed, and that the Deep Agents backend is not subject to the same tool exclusions as the Claude Agent SDK.
- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.
- [Deep Work E2E: Project Lifecycle, Output Chaining, Auto-Retry, Timeout, and Cancellation](./deep-work-e2e-project-lifecycle-output-chaining-auto-retry-timeout-and-cancellat.md) — This test suite exercises PocketPaw's Deep Work (multi-agent task orchestration) system end-to-end through the FastAPI test client, covering project approval, task dependency cascades, output persistence, auto-retry on failure, timeout triggers, project cancellation in all lifecycle states, and the manual retry and skip APIs.
- [Deep Work Goal Parser: GoalAnalysis Validation and Parsing Tests](./deep-work-goal-parser-goalanalysis-validation-and-parsing-tests.md) — This test suite covers the GoalParser and GoalAnalysis components in PocketPaw's Deep Work system, validating JSON parsing, domain/complexity/research-depth validation, field clamping, and serialization round-trips. It ensures robust defensive behavior when LLM-generated goal payloads contain invalid or out-of-range values.
- [Deep Work Human Task Router: Notification Broadcast Tests](./deep-work-human-task-router-notification-broadcast-tests.md) — This test suite validates the HumanTaskRouter, which bridges the automated Deep Work engine and human operators by publishing structured notifications over the MessageBus. It covers task notifications, review routing, plan-ready announcements, and project completion summaries, including edge cases like missing project IDs and empty task lists.
- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.
- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.
- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.
- [Deep Work Session: End-to-End Orchestration and Lifecycle Tests](./deep-work-session-end-to-end-orchestration-and-lifecycle-tests.md) — This integration test suite covers the DeepWorkSession, the top-level orchestrator that coordinates planning, task materialization, agent assignment, approval gating, pause/resume, and cancellation for multi-task AI projects. It verifies the full session lifecycle from DRAFT through PLANNING to AWAITING_APPROVAL and beyond, including edge cases like cyclic dependencies, empty plans, and planner exceptions.
- [Deep Work Task Model Extensions: New Field Tests](./deep-work-task-model-extensions-new-field-tests.md) — This suite tests the Deep Work extensions added to the core Task model in PocketPaw's mission control system, specifically the five new fields—project_id, task_type, blocks, active_description, and estimated_minutes—that enable multi-task project management. It verifies defaults, settability, serialization, backward compatibility, and round-trip fidelity.
- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.
- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.
- [DirectoryTreeTool: File System Tree Visualization with Jail, Depth, and Security Controls](./directorytreetool-file-system-tree-visualization-with-jail-depth-and-security-co.md) — The `DirectoryTreeTool` renders an ASCII directory tree within a file jail, with configurable depth, hidden file visibility, file size display, and entry count truncation. Tests cover basic tree rendering, depth limiting, hidden file exclusion/inclusion, size annotation, edge cases (empty dir, nonexistent path, file-not-directory), security (jail break, prefix bypass, symlink skipping), truncation, and the tool's schema definition.
- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.
- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.
- [Download URL Endpoint Tests: TTL Responses, Cross-Workspace Isolation, and Path Traversal Defense](./download-url-endpoint-tests-ttl-responses-cross-workspace-isolation-and-path-tra.md) — Integration tests for the `/uploads/{file_id}/download-url` endpoint added in the EE upload router, verifying that the endpoint returns a time-bounded URL with the original filename, blocks cross-workspace access with 404, rejects path-traversal IDs, and hides soft-deleted files. Uses the same FastAPI test-client pattern as `test_router.py` with header-driven identity injection.
- [E2E Security Tests: WebSocket Authentication Protocol and HTTP Security Headers](./e2e-security-tests-websocket-authentication-protocol-and-http-security-headers.md) — This Playwright-based suite verifies PocketPaw's WebSocket authentication model — that connections succeed from localhost without explicit auth, that the frontend sends an authenticate action as the first WebSocket message, that tokens never appear in WebSocket URLs, and that API endpoints correctly reject unauthenticated requests while static assets remain accessible.
- [E2E Test Configuration: Playwright Browser Setup and Dashboard Server Lifecycle](./e2e-test-configuration-playwright-browser-setup-and-dashboard-server-lifecycle.md) — This conftest manages the full lifecycle of PocketPaw's end-to-end test environment: it checks for Playwright browser installations and skips the entire e2e suite if they are missing, finds a free OS port, starts the dashboard server in a subprocess, waits for it to become ready, and exposes the base URL to all e2e tests through a session-scoped fixture.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.
- [EE Instinct: Human-in-the-Loop Action Approval System Tests](./ee-instinct-human-in-the-loop-action-approval-system-tests.md) — This test suite validates the full lifecycle of PocketPaw's Instinct subsystem, which gates AI-proposed actions behind human approval before execution. It covers store-layer CRUD, audit trail integrity, REST endpoint contracts, lifecycle state transitions, and edge cases such as approving non-existent actions.
- [EE Upload Resolver Tests: Workspace-Scoped Path Resolution, Isolation, and Soft-Delete Visibility](./ee-upload-resolver-tests-workspace-scoped-path-resolution-isolation-and-soft-del.md) — Tests for `EEUploadResolver` and `resolve_media_paths_scoped`, which translate `/api/v1/uploads/{id}` URLs in message content into local filesystem paths for downstream processing. Key invariants: workspace isolation (cross-workspace lookup returns `None`), non-upload URL pass-through, soft-delete invisibility, and missing-blob safety.
- [EE Upload Router Integration Tests: Roundtrip, Workspace Isolation, Owner-Only Access, and Bulk Partial Success](./ee-upload-router-integration-tests-roundtrip-workspace-isolation-owner-only-acce.md) — Integration tests for the EE uploads FastAPI router covering the full upload-download-delete cycle, cross-workspace 404 enforcement, owner-only read access within a workspace, and the partial-success bulk upload response that distinguishes accepted files from rejected MIME types.
- [EEUploadService Core Behavior: Workspace-Scoped Storage and Streaming Tests](./eeuploadservice-core-behavior-workspace-scoped-storage-and-streaming-tests.md) — This test suite verifies that EEUploadService correctly stores uploaded files under a workspace scope, enforces cross-workspace access boundaries, streams content back to authorized requesters, and handles bulk uploads atomically. The in-memory `_MemAdapter` stands in for real cloud storage so tests run without external dependencies.
- [EEUploadService Realtime Event Emission: FileReady and FileDeleted Tests](./eeuploadservice-realtime-event-emission-fileready-and-filedeleted-tests.md) — This suite verifies that `EEUploadService` fires `FileReady` events over the realtime bus when files are uploaded to a chat-scoped context, and `FileDeleted` events when those files are removed. It also verifies that neither event fires when the upload or file lacks a `chat_id`, preventing noise from workspace-level background operations.
- [emit() Facade Tests: Bus Delegation, Error Swallowing, and Initialization Guard](./emit-facade-tests-bus-delegation-error-swallowing-and-initialization-guard.md) — These tests verify the `emit()` top-level facade that service code calls to publish real-time events. The suite ensures emit delegates unchanged event objects to the active bus, swallows bus errors to protect callers from infrastructure failures, and raises AssertionError when called before the bus singleton is initialized.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Enterprise Automations Rule CRUD: Store and FastAPI Router Tests](./enterprise-automations-rule-crud-store-and-fastapi-router-tests.md) — Full test coverage for the enterprise `AutomationStore` and its FastAPI router, covering three rule types (threshold, schedule, data change), all CRUD operations, toggle/enable behavior, fire tracking, file persistence, and a complete lifecycle end-to-end test. Every test uses `tmp_path` isolation to avoid touching the user's real `~/.pocketpaw` configuration.
- [Enterprise Edition Test Fixtures: License, MongoDB, FastAPI App, S3 Mock, and User/Workspace Factories](./enterprise-edition-test-fixtures-license-mongodb-fastapi-app-s3-mock-and-userwor.md) — This conftest provides the full test infrastructure for PocketPaw's enterprise edition integration tests: HMAC-signed license key injection, isolated in-memory MongoDB via mongomock-motor, a mounted FastAPI app with mocked agent pool, an async HTTP client, callable factories for users and workspaces, and a session-scoped S3 mock via moto.
- [Enterprise Edition Test Package Initializer](./enterprise-edition-test-package-initializer.md) — This `__init__.py` marks `tests/ee/` as a Python package for pytest import resolution and documents in its header comment that it was introduced with the `feat/fleet-journal-emission` feature branch to enable fixture sharing across enterprise edition tests.
- [Enterprise Guard Layer: RBAC, ABAC, Plan Feature Gates, and FastAPI Dependency Tests](./enterprise-guard-layer-rbac-abac-plan-feature-gates-and-fastapi-dependency-tests.md) — Comprehensive tests for PocketPaw's enterprise authorization layer, covering role-based access control (RBAC) enums and guard functions, attribute-based access control (ABAC) policy evaluation, plan feature tables, action-to-role mapping, tool whitelists, and three FastAPI dependency guards (`require_role`, `require_plan_feature`, `require_policy`).
- [Event Model Structural Tests: Base Event Fields and Typed Subclass Wire Type Assignment](./event-model-structural-tests-base-event-fields-and-typed-subclass-wire-type-assi.md) — Two minimal tests verify the structural contract of the Event base model: that every Event has type, data, and ts fields; and that typed subclasses like GroupCreated automatically set the correct wire type string from EVENT_TYPE without the caller needing to pass it.
- [Events SSE Router Tests: Endpoint Registration and OpenAPI Spec Inclusion](./events-sse-router-tests-endpoint-registration-and-openapi-spec-inclusion.md) — Tests that PocketPaw's server-sent events (SSE) endpoint `/api/v1/events/stream` is correctly registered in the events router, present in the v1 router registry, accessible on the dashboard app, and included in the OpenAPI specification. These structural checks prevent the events channel from being silently dropped during refactors.
- [Extras Install and Channel Dependency Endpoint Tests](./extras-install-and-channel-dependency-endpoint-tests.md) — This test module covers the `GET /api/extras/check` and `POST /api/extras/install` dashboard endpoints that manage optional channel adapter dependencies. It validates the allowlist-based security model that prevents arbitrary package installation and tests the `missing_dep` fallback path when a channel adapter fails to start due to an ImportError.
- [Fabric Journal Store Tests: Scope Isolation, Pagination, Projection Rebuild, and Policy Correctness](./fabric-journal-store-tests-scope-isolation-pagination-projection-rebuild-and-pol.md) — This suite pins four invariants for PocketPaw's journal-backed Fabric object store: correct lifecycle (create/update/archive), scope-based access control that returns empty results rather than leaking existence, post-filter pagination totals, and deterministic projection rebuild from the event journal. It supersedes a previous implementation that leaked object counts across scope boundaries.
- [Fabric List Endpoints: Object and Link Filtering, Envelope Shape, and SQL Injection Safety Tests](./fabric-list-endpoints-object-and-link-filtering-envelope-shape-and-sql-injection.md) — Integration tests for the `/fabric/objects` and `/fabric/links` list endpoints added as part of the PocketDataPanel's Objects/Links sub-tab wiring. Covers store-level link filtering by type and source, HTTP envelope shape, type-based filtering, pagination parameter validation, and an explicit SQL injection safety test for the `link_type` parameter.
- [Fabric Store: Object Type CRUD, Object Lifecycle, Link Traversal, and Query Tests](./fabric-store-object-type-crud-object-lifecycle-link-traversal-and-query-tests.md) — Unit tests for the enterprise `FabricStore` — PocketPaw's ontology-based business object store backed by SQLite. Covers defining and querying object types, creating and updating objects with source tracking, linking objects and traversing relationships, filtering queries, pagination, and cascade deletion.
- [Fetch Tool Path Security and Jail Boundary Tests](./fetch-tool-path-security-and-jail-boundary-tests.md) — This test module validates the security model of PocketPaw's file fetch tool, specifically the `is_safe_path` predicate and `handle_path`/`list_directory` handlers that enforce a configurable jail directory. The tests were created as regression guards for issue #619, where an empty path string could bypass the jail check by resolving to the current working directory.
- [File Browser Hidden File Ordering Bug Regression Tests](./file-browser-hidden-file-ordering-bug-regression-tests.md) — This test file reproduces and documents a bug in `handle_file_browse` where applying the `items[:50]` slice before the hidden-file filter caused all visible files to disappear from results when a directory contained 50 or more dot-prefixed items. The tests were created on 2026-02-12 to lock in the correct behavior — filter first, then slice.
- [FileMemoryStore Fixes: UUID Collision, Fuzzy Search, Deduplication, Persistent Delete, and Graph Tests](./filememorystore-fixes-uuid-collision-fuzzy-search-deduplication-persistent-delet.md) — This comprehensive test file covers a series of incremental fixes to `FileMemoryStore`, PocketPaw's markdown-based memory backend. It addresses UUID collisions, broken substring search, single-day file loading, lost deletions across restarts, and missing context limits — plus later phases covering vector semantic search, graph indexing, SVG escaping, and SQLite variable limit handling.
- [Files API Tests: Browse, Download, Zip, Write, and Path Traversal Defence](./files-api-tests-browse-download-zip-write-and-path-traversal-defence.md) — This test file covers PocketPaw's `/api/v1/files` router, which exposes local filesystem operations (browse, single-file download, directory zip download, and file write) to the dashboard. It places particular emphasis on path traversal prevention, file jail enforcement, and zip bomb mitigations.
- [Files Providers Test Sub-Package Marker](./files-providers-test-sub-package-marker.md) — An empty __init__.py that makes tests/cloud/files/providers/ a Python package, enabling dotted imports from parent test modules such as tests.cloud.files.test_provider_contract. Identical in purpose and content to the sibling files test package marker.
- [Files Router Bootstrap Integration Test](./files-router-bootstrap-integration-test.md) — This module contains a single integration test that verifies the files router can be mounted into a FastAPI application via build_files_router and that the /files/tree endpoint responds correctly with a 200 status and a JSON body containing the expected tree structure.
- [Files Router Security Tests: Scope Enforcement and Symlink Jail Traversal Prevention](./files-router-security-tests-scope-enforcement-and-symlink-jail-traversal-prevent.md) — Security regression tests for issues #884 and #886 in PocketPaw's files router: #884 proved any API key (even one with no scopes) could read arbitrary files; #886 proved `/files/download-zip` followed symlinks outside the configured file jail. These tests lock in the fixes and prevent both vulnerabilities from regressing.
- [Files Test Fixtures: FakeProvider, ctx, make_entry, make_mount](./files-test-fixtures-fakeprovider-ctx-makeentry-makemount.md) — This conftest.py provides the shared test infrastructure for the files subsystem: a FakeProvider that implements the full provider protocol in memory, plus factory fixtures for RequestContext, FileEntry, and ResolvedMount. All file provider tests build on these primitives.
- [Files Test Package Marker](./files-test-package-marker.md) — This is an empty __init__.py that marks tests/cloud/files/ as a Python package so pytest can discover and import tests within it. The SHA-256 hash e3b0c44298fc... is the well-known hash of an empty file.
- [Firebase Connector: YAML Definition, CLI Adapter, and Trust Level Tests](./firebase-connector-yaml-definition-cli-adapter-and-trust-level-tests.md) — This test suite validates PocketPaw's Firebase connector from three angles: the declarative YAML definition (action count, trust levels, auth method), the `FirebaseAdapter` class that wraps the Firebase CLI via async subprocess, and the connector registry integration. It uses mocked subprocess calls to test connect, execute, timeout, and error paths without touching a real Firebase project.
- [Fleet Installer Journal Emission Tests: Correlated Events, Backward Compatibility, and Partial Install](./fleet-installer-journal-emission-tests-correlated-events-backward-compatibility-.md) — This suite verifies that the fleet installer emits a correlated sequence of journal events (`fleet.install.started`, `agent.spawned` per soul, `fleet.installed` summary) when given a journal, remains silent when the journal is omitted, suppresses the terminal event on soul-creation failure, and falls back to a fleet-scoped tag when the fleet declares no scopes.
- [Fleet Installer: Manifest Loading, Path Traversal Guard, and Install Orchestration Tests](./fleet-installer-manifest-loading-path-traversal-guard-and-install-orchestration-.md) — This test suite covers PocketPaw's fleet installer, which provisions a full AI agent environment (soul, pocket, connectors) from a declarative manifest. It validates YAML and JSON manifest loading, a P0 path-traversal security fix that clamps string inputs to the bundled fleet directory, install orchestration with partial failure handling, and the InstallReport shape.
- [Fleet Router Test Suite: Template Listing, Install Auth, and Journal Integration](./fleet-router-test-suite-template-listing-install-auth-and-journal-integration.md) — Covers the FastAPI fleet REST router that powers the paw-enterprise InstallFleetPanel, verifying template enumeration, end-to-end install flows, journal event emission, and a layered auth guard (401/403/200) added when the P0 auth requirement landed. Uses FastAPI dependency_overrides throughout so tests remain hermetic — no real Mongo, no real soul-protocol runtime, no writes to ~/.soul/.
- [Folder CRUD Integration Tests: Auto-Chain Creation, Rename Propagation, and RBAC Enforcement](./folder-crud-integration-tests-auto-chain-creation-rename-propagation-and-rbac-en.md) — Integration tests for the EE uploads folder system covering folder creation with parent requirement, duplicate detection, automatic folder chain creation on upload, cascade rename of descendant paths, safe and cascading deletion, and permission enforcement distinguishing non-admin strangers from workspace admins.
- [FolderProvider Contract Test Base Class: Reusable Compliance Suite](./folderprovider-contract-test-base-class-reusable-compliance-suite.md) — This module defines `ProviderContract`, an abstract base class that encodes the behavioral contract every `FolderProvider` implementation must satisfy, including mount listing, entry listing with pagination, namespaced IDs, and graceful `ProviderUnsupported` errors for write operations. Concrete provider tests subclass it to inherit full compliance coverage automatically.
- [Frontend JavaScript Syntax Validation Tests](./frontend-javascript-syntax-validation-tests.md) — This module validates that all JavaScript files in PocketPaw's frontend directory have syntactically correct code by running each file through `node --check`. It also verifies structural contracts — that `app.js` defines the Alpine.js `app()` function and that `websocket.js` references `WebSocket` — catching integration breakage before it reaches users.
- [Full Decision Loop: Fabric Threshold Detection to Instinct Audit Export Tests](./full-decision-loop-fabric-threshold-detection-to-instinct-audit-export-tests.md) — End-to-end tests for the complete decision loop from Fabric object creation through threshold rule evaluation to Instinct action proposal, approval, and JSON audit export — all using real store implementations with `tmp_path` SQLite databases. Three scenarios cover single trigger, multiple triggers, and audit trail identity.
- [GCP Connector: YAML Definition, Adapter Authentication, and Execute Path Tests](./gcp-connector-yaml-definition-adapter-authentication-and-execute-path-tests.md) — This test suite validates PocketPaw's Google Cloud Platform connector, covering the `gcp.yaml` declarative definition (20 actions across compute, storage, pub/sub, secrets, Cloud Run, and logging), the `GCPAdapter` lifecycle (connect, execute, disconnect), and registry wiring. All subprocess calls are mocked to avoid requiring live GCP credentials.
- [get_journal FastAPI Dependency: Caching, SOUL_DATA_DIR Override, and Cache Reset Tests](./getjournal-fastapi-dependency-caching-souldatadir-override-and-cache-reset-tests.md) — Tests the `get_journal` FastAPI dependency from `ee.journal_dep`, pinning three contracts that the rest of the `ee/` subsystem depends on: the dep returns a live Journal instance, repeated calls within one process return the same cached instance, and the `SOUL_DATA_DIR` environment variable is honoured at call time — not frozen at import. Also verifies `reset_journal_cache()` as the test-isolation escape hatch.
- [Gmail Integration Body Extraction and Tool Tests](./gmail-integration-body-extraction-and-tool-tests.md) — This test module validates `GmailClient._extract_body`, which parses Gmail's MIME structure to extract plain text from messages, and the three Gmail tools (`GmailSearchTool`, `GmailReadTool`, `GmailSendTool`) — checking their schemas and verifying that each returns a user-readable error when OAuth authentication is absent.
- [Google ADK Backend Tests](./google-adk-backend-tests.md) — This module provides fully mocked tests for `GoogleADKBackend`, PocketPaw's Google Agent Development Kit integration. It covers metadata/capability discovery, initialization with and without the SDK, event stream processing (text, function calls, function responses, errors), session management, MCP toolset policy enforcement, status reporting, and the backward-compatibility alias `GeminiCLIBackend`.
- [Google Calendar Tool Schema and Auth Error Tests](./google-calendar-tool-schema-and-auth-error-tests.md) — This test module validates the three Google Calendar tools (`CalendarListTool`, `CalendarCreateTool`, `CalendarPrepTool`) — checking their names, trust levels, and required parameter schemas — and verifies that each tool returns a user-readable error message rather than propagating an exception when OAuth authentication is unavailable.
- [Google Chat Channel Adapter Tests](./google-chat-channel-adapter-tests.md) — This test module provides full coverage for `GoogleChatAdapter`, which connects PocketPaw to Google Chat via webhook or Pub/Sub mode. It covers initialization, webhook message handling (including slash commands and space filtering), streaming message buffering, error recovery, `MessageBus` integration, and adapter lifecycle management.
- [Google Docs Integration Tool Schema and Execution Tests](./google-docs-integration-tool-schema-and-execution-tests.md) — This test module validates the three Google Docs tools (`DocsReadTool`, `DocsCreateTool`, `DocsSearchTool`), the `_parse_doc_id` URL-parsing utility, and the `DocsClient._extract_text` method that converts the Docs API's nested JSON structure into plain text. It also confirms that each tool returns actionable error messages when OAuth authentication is absent.
- [Google Drive Connector: DriveClient, SourceAdapter, and RetrievalRouter Integration Tests](./google-drive-connector-driveclient-sourceadapter-and-retrievalrouter-integration.md) — This test suite covers PocketPaw's Google Drive connector end-to-end: HTTP request plumbing with rate-limit backoff, point-in-time revision lookup, `DriveSourceAdapter.query` result shaping, token resolution precedence, and `RetrievalRouter` integration with journal emission. All Drive API traffic is eliminated by a scripted fake `httpx.Client`.
- [Google Drive Integration Tool Schema and Execution Tests](./google-drive-integration-tool-schema-and-execution-tests.md) — This test module validates the four Google Drive tools (`DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, `DriveShareTool`), their parameter schemas, and their behavior when authentication is absent or inputs are invalid. It also verifies `DriveListTool`'s happy path with a mocked HTTP response and confirms that `DriveShareTool` rejects invalid sharing roles at the tool layer.
- [GroupService Realtime Emit Tests](./groupservice-realtime-emit-tests.md) — This test module verifies that every public mutation in GroupService fires the correct realtime event via the bus emit() call. It also confirms that membership-changing operations correctly invalidate the AudienceResolver group cache so downstream subscribers see fresh data.
- [GuardianAgent Empty Response Regression Tests (Issue #636)](./guardianagent-empty-response-regression-tests-issue-636.md) — This test module documents the fix for issue #636, where an empty `response.content` list from the Anthropic API caused an `IndexError` inside `GuardianAgent.check_command`. The fix implements fail-closed behavior: any empty, malformed, or missing API response defaults to `DANGEROUS` rather than crashing or defaulting to safe.
- [GWS CLI System Prompt Injection Tests](./gws-cli-system-prompt-injection-tests.md) — This test suite verifies that the AgentContextBuilder conditionally injects Google Workspace CLI guidance into the system prompt only when the google-workspace MCP server is active and enabled. It also confirms that the gws.md guidance file exists on disk with the required content.
- [GWS Skills Auto-Install and GitHub Skill Installer Tests](./gws-skills-auto-install-and-github-skill-installer-tests.md) — This test suite covers `install_skills_from_github`, a shared async helper that clones a GitHub repository and copies SKILL.md-bearing subdirectories into the local skills install directory, with support for prefix filtering and single-skill targeting. It also tests that the GWS preset installation triggers a non-blocking background task that auto-installs Google Workspace skills.
- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.
- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.
- [Identity API Tests: Reading and Partially Updating Agent Identity Files](./identity-api-tests-reading-and-partially-updating-agent-identity-files.md) — This test file covers PocketPaw's `/api/v1/identity` router, which exposes the agent's persistent identity configuration — the markdown files that define personality, soul, style, instructions, and user profile — as a readable and partially updatable API resource.
- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.
- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.
- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.
- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.
- [InProcessBus Tests: Fan-Out Correctness, Error Isolation, and Module Singleton](./inprocessbus-tests-fan-out-correctness-error-isolation-and-module-singleton.md) — These tests verify InProcessBus — the in-process real-time event bus — covering correct fan-out to all resolved recipients, the WsOutbound payload shape, per-recipient exception isolation so one dead socket does not stop others, graceful audience resolution error swallowing, and the module-level singleton get/set contract.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.
- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.
- [Instinct Correction Loop: Patch Computation, Summarization, Store Persistence, and Approve Endpoint Tests](./instinct-correction-loop-patch-computation-summarization-store-persistence-and-a.md) — Covers the "Correction Loop" — the subsystem that detects when a user edits an AI-proposed action during approval, records what changed as structured patches, and surfaces those corrections for soul memory promotion. Tests span the pure diff functions, the `InstinctStore` correction persistence layer, and the `/approve` HTTP endpoint behavior.
- [Intentions API Tests: CRUD, Toggle, and On-Demand Execution](./intentions-api-tests-crud-toggle-and-on-demand-execution.md) — This test file covers PocketPaw's `/api/v1/intentions` router, which manages proactive agent intentions — scheduled or triggered actions the agent initiates autonomously. It verifies the full CRUD lifecycle, enable/disable toggling, input validation, and on-demand execution.
- [Internal Async Event Bus: Subscribe, Emit, and Fault Isolation Tests](./internal-async-event-bus-subscribe-emit-and-fault-isolation-tests.md) — This test file validates the `ee.cloud.shared.events` module's async event bus, which enables decoupled communication between cloud subsystems without direct imports. The tests confirm subscription, emission, unsubscription, multi-handler ordering, handler fault isolation, and the existence of a module-level singleton.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.
- [KbProvider Tests: Workspace Scoping, RBAC, and Mount Template](./kbprovider-tests-workspace-scoping-rbac-and-mount-template.md) — This module tests the KbProvider file provider, which surfaces the knowledge base as a virtual file tree. Tests verify that entries are scoped to the correct workspace, that baseline RBAC rules grant read access to workspace members and manage access to admins, and that the mount path template correctly resolves the workspace ID.
- [Knowledge Router: Multi-Scope Listing, Filtering, and Cross-Workspace Isolation Tests](./knowledge-router-multi-scope-listing-filtering-and-cross-workspace-isolation-tes.md) — This integration test suite exercises the `/api/v1/knowledge/articles` FastAPI endpoint end-to-end, verifying that it merges workspace and agent scopes, supports `agent_id` filtering, rejects cross-workspace queries with HTTP 403, and returns empty results (not a data leak) for unknown agent IDs.
- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.
- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.
- [Launcher Uninstaller: Component-Level Removal with Data Preservation](./launcher-uninstaller-component-level-removal-with-data-preservation.md) — The launcher uninstall test suite validates PocketPaw's structured uninstall logic, covering component discovery, selective removal of runtime artifacts, and preservation of user data by default. Tests verify that the `Uninstaller` class correctly reflects filesystem state and handles missing or partially-installed environments without raising exceptions.
- [Launcher Updater: PyPI Version Checking and In-Place Upgrade Flow](./launcher-updater-pypi-version-checking-and-in-place-upgrade-flow.md) — The updater test suite covers PocketPaw's self-update mechanism, validating version comparison logic, PyPI metadata fetching, installed version detection from the venv, and the full apply-upgrade path. It ensures the updater degrades gracefully when the network is unavailable or the package is not yet installed.
- [Live Cloud API Smoke Tests with Backend Reachability Guard](./live-cloud-api-smoke-tests-with-backend-reachability-guard.md) — Smoke tests that run real HTTP calls against a locally running `paw-cloud` backend. The entire module skips automatically when the backend is unreachable, making these safe to include in CI without a live server requirement. Tests cover login, profile retrieval, agent listing, room lifecycle, DMs, and unauthenticated rejection.
- [LLM Provider Adapter Tests: Registry, Model Resolution, and Per-Provider Configuration](./llm-provider-adapter-tests-registry-model-resolution-and-per-provider-configurat.md) — PocketPaw supports multiple LLM providers (Anthropic, Ollama, OpenRouter, Gemini, LiteLLM) through a unified adapter pattern. These tests validate the provider registry, model string resolution priority, per-adapter environment variable construction, and the `LLMClient` delegation layer that dispatches to the correct adapter.
- [LLMClient Abstraction: Provider Resolution, Client Construction, and SDK Environment Setup](./llmclient-abstraction-provider-resolution-client-construction-and-sdk-environmen.md) — The `LLMClient` tests validate the centralized abstraction over multiple LLM backends — Anthropic, OpenAI, Ollama, and OpenRouter — ensuring that provider auto-detection, client instantiation, and SDK environment variable injection all behave correctly for each provider and credential combination. The frozen dataclass design prevents accidental mutation after resolution.
- [LLMRouter: Empty-Response Fallback and Multi-Provider Routing (Issue #664)](./llmrouter-empty-response-fallback-and-multi-provider-routing-issue-664.md) — The LLMRouter test suite focuses on a specific regression (issue #664) where an empty `choices` or `content` list from the upstream LLM API caused an unhandled `IndexError`, crashing the agent loop. Tests also validate backend auto-detection logic for all supported providers — OpenAI-compatible, OpenRouter, Gemini, LiteLLM — and ensure `chat()` routes each provider through the correct internal method.
- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.
- [Matrix Channel Adapter: Message Routing, Streaming, Error Recovery, and Bus Integration](./matrix-channel-adapter-message-routing-streaming-error-recovery-and-bus-integrat.md) — The Matrix adapter test suite (Sprint 21) validates PocketPaw's Matrix channel integration, covering initial-sync message filtering, room authorization, streaming with edit-in-place, MessageBus subscription lifecycle, and error isolation. The `matrix-nio` library is fully mocked as an optional dependency, allowing tests to run without a live Matrix homeserver.
- [MCP + Claude Agent SDK Integration: Server Filtering, Transport Mapping, and Policy Enforcement](./mcp-claude-agent-sdk-integration-server-filtering-transport-mapping-and-policy-e.md) — The MCP-Claude SDK integration tests (Sprint 17) validate that `ClaudeAgentSDK._get_mcp_servers()` correctly translates PocketPaw's internal `MCPServerConfig` objects into the format expected by the Claude Agent SDK, applying enable/disable filtering, transport type mapping, and security policy checks before handing the server list to the SDK.
- [MCP Manager: Server Lifecycle, Tool Discovery, Config Persistence, and HTTP Transport Auto-Detection](./mcp-manager-server-lifecycle-tool-discovery-config-persistence-and-http-transpor.md) — The MCP Manager test suite (Sprint 16) covers the full lifecycle of MCP server connections — config serialization, parallel server startup, tool discovery, tool invocation, graceful shutdown, and the Streamable-HTTP-to-SSE fallback protocol negotiation. All MCP SDK imports are mocked as the `mcp` package is optional.
- [MCP OAuth Flow: Token Storage, Callback Coordination, Preset Flags, and Dashboard Integration](./mcp-oauth-flow-token-storage-callback-coordination-preset-flags-and-dashboard-in.md) — The MCP OAuth test suite validates PocketPaw's support for OAuth-authenticated MCP servers, covering secure token and client-info persistence, WebSocket-based callback coordination, preset flag propagation, and the unauthenticated OAuth callback dashboard endpoint. Tests also verify that token files are created with restricted permissions and that corrupted token files degrade gracefully.
- [MCP Router Tests: Server Management and OAuth Callback Handling](./mcp-router-tests-server-management-and-oauth-callback-handling.md) — This test file covers PocketPaw's `/api/v1/mcp` router, which manages Model Context Protocol (MCP) server connections — add, remove, toggle, and status — and handles the OAuth callback that completes browser-based MCP server authentication flows.
- [MCP Server Presets: Catalog Registry, Config Conversion, and Dashboard Installation](./mcp-server-presets-catalog-registry-config-conversion-and-dashboard-installation.md) — The MCP presets test suite validates PocketPaw's built-in catalog of popular MCP servers — covering unique ID enforcement, required field validation, category filtering, config generation from presets, and the dashboard API endpoints for browsing and installing presets. The Notion preset's hosted-OAuth path and transform functions receive dedicated coverage.
- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.
- [Media Attachments: Cross-Channel File Delivery, Path Security, and Tool Result Tagging](./media-attachments-cross-channel-file-delivery-path-security-and-tool-result-tagg.md) — The media attachments test suite validates PocketPaw's end-to-end media pipeline — from tool result tagging via HTML-comment markers, through agent loop extraction, to per-channel delivery (Telegram, Discord, Slack, WhatsApp, WebSocket) and the secure `/api/media` serving endpoint. Path traversal protection for the media endpoint receives dedicated security coverage.
- [MediaDownloader: Filename Sanitization, Unique Naming, HTTP Download, and Size Limiting](./mediadownloader-filename-sanitization-unique-naming-http-download-and-size-limit.md) — The `MediaDownloader` test suite validates PocketPaw's utility for safely downloading and storing media files from URLs, covering filename sanitization (path traversal prevention), collision-free unique filename generation, configurable size limits, HTTP error handling, and authenticated downloads. The singleton pattern ensures a single downloader instance manages all media across the application.
- [Mem0 Memory Store: Backend Selection, Config Building, and Pluggable Provider Integration](./mem0-memory-store-backend-selection-config-building-and-pluggable-provider-integ.md) — The Mem0 store test suite validates PocketPaw's integration with the `mem0ai` package as an optional semantic memory backend, covering the factory function's backend selection and fallback logic, `_build_mem0_config` helper output for all supported LLM and embedder providers, and the `Mem0MemoryStore` CRUD operations including tag filtering and semantic search.
- [Memory API Tests: Settings, Long-Term Memory CRUD, and Backend Stats](./memory-api-tests-settings-long-term-memory-crud-and-backend-stats.md) — This test file covers PocketPaw's `/api/v1/memory` router, which exposes configuration for the memory backend (file store vs. mem0), CRUD operations over long-term memory entries, and per-backend statistics.
- [Memory Backend Selection Tests: EE MongoDB Default Flip and OSS Import Isolation](./memory-backend-selection-tests-ee-mongodb-default-flip-and-oss-import-isolation.md) — This module tests that the EE edition flips the default memory backend from `file` to `mongodb` at startup without overriding explicit user choices, verifies that `get_memory_manager()` returns a `MongoMemoryStore`-backed manager after the flip, and enforces that OSS package modules (`src/pocketpaw/memory/`) contain no top-level imports from `ee.*` to preserve clean open-source/enterprise boundary separation.
- [Memory Edge Cases: Concurrency, Compaction, Unicode, Auto-Learn, and Session Lifecycle](./memory-edge-cases-concurrency-compaction-unicode-auto-learn-and-session-lifecycl.md) — The memory edge case test suite (addressing issue #36) stress-tests PocketPaw's memory subsystem under conditions that routine tests miss — concurrent writes to the same session, session compaction when history exceeds the threshold, Unicode and emoji content, empty session handling, auto-learn trigger conditions, and session listing and deletion operations.
- [Memory Isolation: Sender-Scoped Storage and Identity Injection](./memory-isolation-sender-scoped-storage-and-identity-injection.md) — Tests verifying that PocketPaw's MemoryManager correctly isolates memories by sender identity, so conversations from different users do not bleed into each other. Covers user ID resolution, per-user file routing in FileMemoryStore, and identity injection into system prompts.
- [Memory System: FileMemoryStore, MemoryManager, and Knowledge Graph Extraction](./memory-system-filememorystore-memorymanager-and-knowledge-graph-extraction.md) — The core memory test suite validates PocketPaw's `FileMemoryStore` and `MemoryManager` — covering long-term and session memory CRUD, the agent context assembly flow, and an extensive suite for the knowledge graph extraction system that builds entity-relationship graphs from conversation text using conservative regex patterns.
- [Memory Tools: RememberTool, RecallTool, and Session List API](./memory-tools-remembertool-recalltool-and-session-list-api.md) — Tests for the agent-facing memory tools (`remember` and `recall`) and the session list REST endpoint. Covers tool schema validation, content persistence, tag filtering, truncation behavior, and the API's ability to enumerate stored sessions.
- [Message Bus Tests: Inbound/Outbound Pub-Sub, Multi-Subscriber Fan-out, and Channel Routing](./message-bus-tests-inboundoutbound-pub-sub-multi-subscriber-fan-out-and-channel-r.md) — This test suite validates PocketPaw's internal message bus, which decouples channel adapters (Telegram, Discord, WhatsApp, CLI) from the core agent logic using an async publish-subscribe queue. Tests cover inbound message queueing, outbound pub-sub subscription and delivery, multi-subscriber fan-out, unsubscribe, adapter integration, broadcast to all channels, and per-channel filtering.
- [MessageService Realtime Emit Tests](./messageservice-realtime-emit-tests.md) — This test module enforces that every public mutation in MessageService fires the correct typed realtime event through emit(). It also includes regression guards ensuring the WebSocket router itself does not perform direct broadcasts that would bypass the event bus.
- [Microsoft Teams Channel Adapter Tests: Messaging, Tenant Filtering, and Error Recovery](./microsoft-teams-channel-adapter-tests-messaging-tenant-filtering-and-error-recov.md) — Tests for PocketPaw's Microsoft Teams channel adapter, covering initialisation, message processing, stream buffering, tenant-based filtering, bus integration, lifecycle management, and error recovery. The `botbuilder-core` dependency is mocked since it is optional.
- [Mission Control: Data Models, File Store, and Manager Operations](./mission-control-data-models-file-store-and-manager-operations.md) — Tests for PocketPaw's multi-agent orchestration layer, covering AgentProfile and Task data models, the file-based persistence store, and the high-level MissionControlManager that coordinates agents, tasks, messages, documents, and heartbeats.
- [Mission Control (Deep Work) API Tests: Project Lifecycle, State Transitions, and REST Endpoints](./mission-control-deep-work-api-tests-project-lifecycle-state-transitions-and-rest.md) — This test module, created in February 2026, covers PocketPaw's Mission Control subsystem — the project and task management layer that tracks long-horizon AI work. It validates the full project lifecycle through both the `MissionControlManager` directly and the FastAPI REST endpoints, including state transitions (draft, approved, paused, resumed) and proper 404 handling for missing resources.
- [Mission Control Heartbeat Daemon: Agent Health and Work Polling](./mission-control-heartbeat-daemon-agent-health-and-work-polling.md) — Tests for HeartbeatDaemon, the background process that periodically wakes registered agents, checks for pending tasks and notifications, and records liveness signals. Covers daemon lifecycle, configurable intervals, work discovery, error resilience, and manual triggers.
- [Mission Control REST API: FastAPI Endpoint Tests](./mission-control-rest-api-fastapi-endpoint-tests.md) — Tests for the FastAPI router that exposes Mission Control functionality over HTTP, covering agent, task, message, document, activity, stats, and notification endpoints. Uses FastAPI's TestClient with monkeypatched singletons for full request/response validation without a live server.
- [Mission Control Task Executor: Security, Concurrency, and Streaming](./mission-control-task-executor-security-concurrency-and-streaming.md) — Tests for MCTaskExecutor, the component that runs agent tasks in the background, streams results, and broadcasts events. Covers singleton lifecycle, UUID validation, error sanitization, duplicate-execution prevention, and background task completion.
- [Model Router: Task Complexity Classification and Tiered Model Selection](./model-router-task-complexity-classification-and-tiered-model-selection.md) — Tests for ModelRouter, which classifies incoming messages by complexity and routes them to the appropriate LLM tier (Haiku for simple, Sonnet for moderate, Opus for complex). Validates classification boundaries, edge cases like empty messages, and that the model names match the configured tiers.
- [MongoFileStore Unit Tests: Scoped Save/Get, Cross-Workspace Isolation, and Soft Delete](./mongofilestore-unit-tests-scoped-saveget-cross-workspace-isolation-and-soft-dele.md) — Unit tests for `MongoFileStore`'s workspace-scoped CRUD operations, validating that files are stored and retrieved correctly within a workspace, are invisible across workspace boundaries, disappear after soft deletion, and return `None` (not raise) for missing IDs.
- [MongoMemoryStore Deduplication Tests: Idempotent Saves Within the 5-Second Window](./mongomemorystore-deduplication-tests-idempotent-saves-within-the-5-second-window.md) — This module tests the deduplication logic in `MongoMemoryStore`, which prevents identical back-to-back writes from creating duplicate memory entries when an agent loop retries the same turn. The dedup key is `(session_key, role, content)` with a 5-second window, and the tests verify that only different content, session, or role values produce distinct database rows.
- [MongoMemoryStore Facts Path: LONG_TERM and DAILY Memory Roundtrip Tests](./mongomemorystore-facts-path-longterm-and-daily-memory-roundtrip-tests.md) — These tests exercise the complete facts path of MongoMemoryStore, covering CRUD, type isolation between LONG_TERM and DAILY entries, cross-type search, regex safety, and protocol conformance. They also verify that user_id is stored as a dedicated column and never duplicated in the metadata dictionary.
- [MongoMemoryStore Multi-Tenant Workspace Isolation Tests](./mongomemorystore-multi-tenant-workspace-isolation-tests.md) — These tests verify that MongoMemoryStore stamps workspace_id onto both session messages and fact documents at write time, and that read operations are scoped to the requested workspace to prevent cross-tenant data leakage. Untagged rows with workspace_id=None must never appear in workspace-scoped queries.
- [MongoMemoryStore Session Index Shape and Filtering Contract Tests](./mongomemorystore-session-index-shape-and-filtering-contract-tests.md) — These tests verify `_load_session_index_async` in MongoMemoryStore, which powers the `GET /sessions/runtime` endpoint. They pin the exact dict shape, filtering of group and soft-deleted sessions, channel inference from session key prefix, and empty title coercion.
- [MongoMemoryStore SESSION Path: Protocol Conformance and Adapter-Specific Read Tests](./mongomemorystore-session-path-protocol-conformance-and-adapter-specific-read-tes.md) — These tests cover the full SESSION memory path in MongoMemoryStore — CRUD, chronological message retrieval, session key isolation, combined session-with-messages reads with limit behavior, and cross-collection isolation between pocket and group messages.
- [MongoMemoryStore Session Upkeep: messageCount and lastActivity Auto-Maintenance Tests](./mongomemorystore-session-upkeep-messagecount-and-lastactivity-auto-maintenance-t.md) — These tests verify that MongoMemoryStore automatically updates Session metadata (messageCount, lastActivity) on every save and auto-creates missing Session rows when a message arrives before the session is explicitly created. They pin behaviors introduced after a historical refactor that removed a broken parallel bus subscriber.
- [Mount Configuration Loading and Template Resolution Tests](./mount-configuration-loading-and-template-resolution-tests.md) — This module tests the `load_mounts` and `resolve_template` functions that parse and validate `mounts.yaml` configuration files, covering sort order, variable substitution, unknown-variable rejection, and absolute-path enforcement. These tests prevent misconfigured mount definitions from silently producing incorrect or insecure virtual filesystem paths.
- [Natural Time Parsing Tests: parse_natural_time Scheduler Function](./natural-time-parsing-tests-parsenaturaltime-scheduler-function.md) — This test suite validates the `parse_natural_time` function in PocketPaw's scheduler module, covering every user-facing time expression format — with and without the 'in' prefix, abbreviations, singular/plural forms, and embedded sentences. The suite acts as both a regression guard and a specification for all accepted input shapes.
- [Neonize WhatsApp Channel Adapter: Mocking, Streaming, and Bus Integration](./neonize-whatsapp-channel-adapter-mocking-streaming-and-bus-integration.md) — Tests for the NeonizeAdapter, which connects PocketPaw to WhatsApp Personal via the neonize library. Covers auto-installation of the optional dependency, message send/receive lifecycle, stream buffering, QR code exposure, and bus subscription.
- [Notification Derivation Tests: Mention, Reaction, and Invite Trigger Verification](./notification-derivation-tests-mention-reaction-and-invite-trigger-verification.md) — These tests verify the three primary notification derivation paths — @mention in a group message, emoji reaction, and workspace invite — correctly call NotificationService.create with the right arguments. They also verify that self-targeted events and reaction removals never produce notifications.
- [Notifications Test Package Marker](./notifications-test-package-marker.md) — An empty `__init__.py` that marks `tests/cloud/notifications/` as a Python package for stable pytest discovery. No logic or fixtures live here; all test content is in the sibling test modules test_service.py and test_derived.py.
- [NotificationService Unit Tests: CRUD, Fan-Out, and Idempotency Contract](./notificationservice-unit-tests-crud-fan-out-and-idempotency-contract.md) — These unit tests exercise NotificationService in isolation by patching the Notification Beanie document and the emit function, verifying that create persists then emits, list_for_user filters at the database layer, mark_read is idempotent for already-read notifications, and clear_all uses a bulk update and emits a single event.
- [OAuth Integration: Token Storage, Provider Configuration, and Token Validity](./oauth-integration-token-storage-provider-configuration-and-token-validity.md) — Tests for PocketPaw's OAuth subsystem, covering secure token persistence in TokenStore, provider URL generation via OAuthManager, and token validity checks based on expiry timestamps. Verifies file permissions, non-existent token handling, and graceful degradation for unknown providers.
- [OAuth2 PKCE Authorization Server Tests: Full Flow, Token Exchange, and Consent](./oauth2-pkce-authorization-server-tests-full-flow-token-exchange-and-consent.md) — Tests PocketPaw's OAuth2 authorization server with PKCE (Proof Key for Code Exchange), covering authorization code issuance, token exchange, token refresh, revocation, and the consent UI endpoints. The tests validate both the server logic class directly and the REST endpoints, ensuring the security guarantees of PKCE are enforced end-to-end.
- [OCR Tool: Schema Validation, File Jail, and Provider Fallback Tests](./ocr-tool-schema-validation-file-jail-and-provider-fallback-tests.md) — Tests for OCRTool, a built-in agent tool that extracts text from images using OpenAI Vision or Tesseract as fallback. Validates the tool schema, enforces a file-system jail to prevent path traversal, and covers error paths for unsupported formats, oversized files, missing providers, and API failures.
- [Ollama Integration: Provider Detection, Env Var Construction, and CLI Health Check](./ollama-integration-provider-detection-env-var-construction-and-cli-health-check.md) — Tests for PocketPaw's Ollama provider support, verifying that LLMClient correctly detects Ollama from settings, constructs the right subprocess environment variables, and skips smart model routing for local models. Also covers the `--check-ollama` CLI command that validates server reachability and model availability.
- [OpenAI Agents SDK Backend: Tool Wiring, Session Management, and Graceful Degradation](./openai-agents-sdk-backend-tool-wiring-session-management-and-graceful-degradatio.md) — Tests for the OpenAI Agents SDK backend adapter, covering custom tool registration, per-backend provider selection, SDK-absent graceful degradation, native SQLite session management, and lazy database path resolution. All tests run without a real OpenAI Agents SDK installation via comprehensive mocking.
- [OpenAI-Compatible Provider: Detection, Env Vars, Client Creation, and Error Formatting](./openai-compatible-provider-detection-env-vars-client-creation-and-error-formatti.md) — Tests for PocketPaw's OpenAI-compatible endpoint support in LLMClient, enabling any OpenAI-compatible API (LiteLLM, Groq, local proxies) as the LLM backend. Covers provider detection, model resolution, environment variable construction for subprocess mode, Anthropic client creation with custom base URL, and structured error formatting for connection and authentication failures.
- [OpenCode Backend: REST API Integration, Session Caching, and Tool Injection](./opencode-backend-rest-api-integration-session-caching-and-tool-injection.md) — Tests for the OpenCodeBackend, which connects PocketPaw to OpenCode's REST API for agentic coding tasks. Covers health checking, session creation and caching, the message endpoint contract, system prompt tool instruction injection, streaming responses, and graceful error handling for unreachable servers.
- [OSS-to-EE Resolver Fallback Tests: Bridging JSONL and Mongo Upload Stores](./oss-to-ee-resolver-fallback-tests-bridging-jsonl-and-mongo-upload-stores.md) — Tests for the `resolve_media_paths_any` OSS→EE fallback, which handles the hybrid scenario where uploads are stored in the EE Mongo store but chat rendering goes through the OSS JSONL lookup path. Without the fallback, EE-uploaded files would appear as broken links in OSS-rendered chat messages.
- [Output Redaction Tests: API Keys, Tokens, and Secret Pattern Detection](./output-redaction-tests-api-keys-tokens-and-secret-pattern-detection.md) — PocketPaw's output redaction module (`pocketpaw.security.redact`) scans agent output for API keys, tokens, credentials, and other secrets before they are logged or sent to external systems. These tests validate detection of over a dozen secret patterns across major providers, verify that safe text is not falsely flagged, and confirm the specific scenario of an agent reading a `.env` file.
- [Packaging Sanity Tests: Dependency Conflicts, Version Bounds, and Extra Consistency](./packaging-sanity-tests-dependency-conflicts-version-bounds-and-extra-consistency.md) — Tests that parse `pyproject.toml` directly to catch dependency version conflicts and packaging invariants before they cause runtime failures. Specifically targets the FastAPI/Starlette version conflict introduced by MCP's transitive Starlette dependency, and verifies that dashboard-mode dependencies are included in the core package.
- [Paw CLI Tests: init, doctor, channels, and serve Command Coverage](./paw-cli-tests-init-doctor-channels-and-serve-command-coverage.md) — This test file validates PocketPaw's `paw` CLI entry point using Click's `CliRunner`, covering the main command group, `init`, `doctor`, `channels`, and `serve` subcommands. It focuses on exit codes, output presence, option parsing, and graceful degradation when optional dependencies like `soul-protocol` are absent.
- [Paw Print Ingest Router: HTTP Surface, CORS, Rate Limiting, and Fabric Mapping Tests](./paw-print-ingest-router-http-surface-cors-rate-limiting-and-fabric-mapping-tests.md) — This test suite covers the HTTP layer of the Paw Print widget system — including owner-authenticated CRUD endpoints, CORS enforcement for the public spec and event ingest routes, payload size limits, per-customer rate limiting, and event-to-Fabric-object mapping via Jinja-style template interpolation.
- [Paw Print Widget Backend: Model Validation, CRUD, and Rate Limiting Tests](./paw-print-widget-backend-model-validation-crud-and-rate-limiting-tests.md) — This test suite covers the backend layer of PocketPaw's Paw Print embeddable widget system — validating Pydantic model constraints (block caps, domain normalization, rate limits, token format), SQLite-backed CRUD operations, event persistence, and the dual rate-limit primitives (overall and per-customer) that protect the public ingest endpoint.
- [Paw Project Scanner Tests: heuristic_scan and scan_prompt Coverage](./paw-project-scanner-tests-heuristicscan-and-scanprompt-coverage.md) — The `paw scan` heuristic reads a project's on-disk artifacts — README, `pyproject.toml`, `package.json`, `.env.example`, and top-level directory structure — and stores extracted facts into the soul's memory. These tests verify that each artifact type is correctly parsed, that the soul's `remember()` method is called with appropriate metadata, and that failures in memory storage do not abort the scan.
- [Paw Soul Tools Tests: SoulRemember, SoulRecall, SoulEditCore, and SoulStatus](./paw-soul-tools-tests-soulremember-soulrecall-souleditcore-and-soulstatus.md) — These tests cover the four built-in soul tools that PocketPaw exposes to LLMs: `soul_remember`, `soul_recall`, `soul_edit_core`, and `soul_status`. Each tool is validated for its parameter schema, execution behavior, error handling, and JSON Schema export in both Anthropic and OpenAI formats.
- [PawConfig Tests: YAML Loading, Defaults, and Environment Variable Overrides](./pawconfig-tests-yaml-loading-defaults-and-environment-variable-overrides.md) — `PawConfig` is PocketPaw's project-level configuration object, loaded from `paw.yaml` in the project root. These tests cover the full priority chain: defaults when no file exists, YAML values when the file is present, and environment variable overrides that take precedence over both.
- [PII Scanner Tests: Detection, Actions, False Positives, and Singleton Pattern](./pii-scanner-tests-detection-actions-false-positives-and-singleton-pattern.md) — PocketPaw's PII scanner (`pocketpaw.security.pii`) detects sensitive personal information in text before it reaches logs, LLM context, or external storage. These tests validate detection of SSNs, emails, phones, credit cards, IP addresses, and dates of birth, verify that safe text generates no false positives, and confirm the singleton pattern for the default scanner instance.
- [Plan Mode API Tests: Approve and Reject Active Agent Plans](./plan-mode-api-tests-approve-and-reject-active-agent-plans.md) — This test file covers PocketPaw's `/api/v1/plan` router, which lets the dashboard approve or reject an agent's proposed action plan before execution. It verifies the session-keyed lookup, the not-found response when no active plan exists, and input validation.
- [Plan Mode Tests: PlanStep, ExecutionPlan, PlanManager, and Approval Flow](./plan-mode-tests-planstep-executionplan-planmanager-and-approval-flow.md) — PocketPaw's plan mode allows agents to propose multi-step execution plans and wait for human approval before running potentially destructive operations. These tests cover `PlanStep` previews, `ExecutionPlan` assembly, `PlanManager` lifecycle (create, approve, reject, expire), and the async approval-wait mechanism.
- [Pocket Journal SSE Stream Router Tests: Filtering, Cursors, and Frame Contract](./pocket-journal-sse-stream-router-tests-filtering-cursors-and-frame-contract.md) — Tests the Server-Sent Events (SSE) endpoint that streams journal events for a specific pocket to the RippleGraphWidget frontend. Covers the event-matching predicate, backlog replay with `since_seq` cursor support for reconnects, the `connected` handshake frame, idle-poll timeout, and strict isolation so events from other pockets never appear in a pocket's stream.
- [Pocket Layout Helpers: Export Determinism, Parse Validation, Round-Trip, and Store Scoping Tests](./pocket-layout-helpers-export-determinism-parse-validation-round-trip-and-store-s.md) — This unit test suite covers the pure Python helpers in `ee.cloud.pockets.layouts` — specifically `export_layout_yaml` (deterministic YAML serialization), `parse_layout_yaml` (safe YAML parsing with input validation), and `UserTemplateStore` (in-process template store with workspace scoping). No FastAPI, Beanie, or MongoDB is involved.
- [Pocket Layout Routes: Export, Template Create/List, and Workspace Scoping Tests](./pocket-layout-routes-export-template-createlist-and-workspace-scoping-tests.md) — This integration test suite covers three new routes on the pockets router that implement the layout save/share surface: `POST /pockets/{id}/export-layout` (YAML serialization), `POST /pockets/templates` (template creation), and `GET /pockets/templates` (workspace-scoped listing). It verifies the round-trip fidelity, metadata override support, cross-workspace isolation, and malformed YAML handling.
- [Pocket Tool Test Suite: Widget Lifecycle, Legacy Conversion, and UISpec Contracts](./pocket-tool-test-suite-widget-lifecycle-legacy-conversion-and-uispec-contracts.md) — Comprehensive tests for `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — the three built-in tools that let AI agents build and mutate dashboard "pockets" in PocketPaw. The suite enforces spec versioning (v1.0 UISpec vs v2.0 widget array), backward compatibility of the `name→title` alias, and structural contracts on every tool's JSON output.
- [Pocket Tool Tests: Dashboard Widget Creation, Legacy Conversion, and UI Spec Generation](./pocket-tool-tests-dashboard-widget-creation-legacy-conversion-and-ui-spec-genera.md) — The pocket tool system lets AI agents create and modify PocketPaw dashboard "pockets" — widget-based layouts containing metrics, charts, tables, and feeds. These tests validate the `create_pocket`, `add_widget`, and `remove_widget` tools, including backward-compatible legacy widget conversion, the `ui` parameter for pre-built specs, and the JSON Schema definitions used by LLM providers.
- [PocketPaw Cognitive Engine: Wiring Tests for Soul-Aware Thinking](./pocketpaw-cognitive-engine-wiring-tests-for-soul-aware-thinking.md) — Tests for the `PocketPawCognitiveEngine`, which bridges the soul-protocol's identity layer with PocketPaw's LLM backend to produce soul-contextualised responses. Covers correct parameter forwarding, graceful error handling, stream termination logic, and end-to-end wiring through `SoulManager` and `AgentLoop`.
- [Pockets Domain Schemas: Request Validation and Response Model Tests](./pockets-domain-schemas-request-validation-and-response-model-tests.md) — This test file validates all Pydantic request/response schemas in the pockets domain — covering field defaults, enum constraints, length limits, optional-field behavior, and the full `PocketResponse` shape. It ensures the API surface contracts are locked in and that invalid inputs are rejected at the schema layer before reaching business logic.
- [Presence Broadcast and Grace Window Tests](./presence-broadcast-and-grace-window-tests.md) — This test module verifies the WebSocket endpoint's presence online/offline lifecycle, specifically the grace window that delays broadcasting a user offline until reconnection is no longer possible. Tests drive _schedule_presence_offline directly and use time-acceleration to keep the suite fast.
- [Proactive Daemon Tests: Cron Scheduling, Intention CRUD, Context Hub, and Stale Session Triggers](./proactive-daemon-tests-cron-scheduling-intention-crud-context-hub-and-stale-sess.md) — This test module covers PocketPaw's proactive daemon — the subsystem that fires agent prompts on a schedule rather than waiting for user messages. It validates cron expression parsing (including named presets), the `IntentionStore` CRUD lifecycle, `ContextHub` template variable injection, the `TriggerEngine` scheduling layer, and the stale-session trigger that nudges idle users.
- [ProviderRegistry Tests: Registration, Duplicate Detection, and Mount Resolution](./providerregistry-tests-registration-duplicate-detection-and-mount-resolution.md) — This module tests `ProviderRegistry`, the central index that maps provider IDs to `FolderProvider` implementations and resolves virtual mount paths to their owning providers. Key behaviors tested include duplicate registration rejection, longest-prefix mount resolution, and template variable substitution during path matching.
- [Rate Limiter, Audit API Events, and OpenAPI Config Tests](./rate-limiter-audit-api-events-and-openapi-config-tests.md) — Tests PocketPaw's token-bucket rate limiter, the `RateLimitInfo` response model and its header formatting, audit logging for API events (OAuth and key revocation), and the OpenAPI endpoint configuration. Together these tests ensure the API protects against request floods, produces correct rate limit headers, and maintains a searchable audit trail.
- [RBAC Action Matrix Tests: Parametrized Role Enforcement and Coverage Guard](./rbac-action-matrix-tests-parametrized-role-enforcement-and-coverage-guard.md) — A parametrized test matrix that exercises every entry in the canonical `ACTIONS` table against every role in the matching role family, verifying that `check_action` allows or raises `Forbidden` with the correct deny code. A meta-test enforces that no new action can be added without both an allow-path and a deny-path assertion, preventing silent coverage gaps.
- [ReadState Model and UnreadService Upsert Tests](./readstate-model-and-unreadservice-upsert-tests.md) — This module tests the ReadState model's default values and the atomic upsert behavior of UnreadService.mark_read and bump_mention against a real in-memory MongoDB. The tests enforce the unique (user, group) index constraint and verify that concurrent operations remain idempotent.
- [Realtime Bus Initialization and Group Member Resolution Tests](./realtime-bus-initialization-and-group-member-resolution-tests.md) — This test module verifies the wiring of `init_realtime`, ensuring the correct bus backend is selected by default and that the `AudienceResolver` is properly exposed. It also covers `GroupService.list_member_ids` behavior for found and missing groups.
- [Realtime Test Package Marker](./realtime-test-package-marker.md) — An empty `__init__.py` that designates `tests/cloud/realtime/` as a Python package for stable pytest discovery and import resolution. No logic or fixtures live here; all test content is in the sibling test modules.
- [Recent Files Tracker Tests: Bash Path Heuristic and Tool Path Extraction](./recent-files-tracker-tests-bash-path-heuristic-and-tool-path-extraction.md) — `RecentFilesTracker` maintains a most-recently-used list of files accessed by the agent, enabling context-aware suggestions and memory seeding. These tests cover the Bash command path heuristic (which was missing and is now implemented), tool-specific path extraction for Read/Write/Edit/Bash tools, and tracker deduplication and ordering behavior.
- [Reddit Tool Tests: Search, Read, Trending, and Post Formatting](./reddit-tool-tests-search-read-trending-and-post-formatting.md) — PocketPaw's Reddit integration provides agents with three tools for interacting with Reddit: `reddit_search`, `reddit_read`, and `reddit_trending`. These tests validate tool schemas and trust levels, the `_format_post` helper for normalizing Reddit API responses, and async tool execution covering success, empty results, and error conditions.
- [Reminders API Tests: Natural Language Scheduling, Listing, and Deletion](./reminders-api-tests-natural-language-scheduling-listing-and-deletion.md) — This test file covers PocketPaw's `/api/v1/reminders` router, which accepts natural-language reminder messages, parses them into scheduled events via the scheduler, and exposes list and delete operations with human-readable time-remaining formatting.
- [ReminderScheduler Cron Expression and File Corruption Tests](./reminderscheduler-cron-expression-and-file-corruption-tests.md) — This test file validates `ReminderScheduler`'s support for recurring cron-based reminders and its resilience to corrupted or malformed reminder files. Tests cover cron expression validation, preset schedules, persistent-vs-oneshot behavior, file quarantine on corruption, and partial corruption recovery.
- [Remote Access Auth Tests: Token Generation, Middleware, and QR Endpoint Security](./remote-access-auth-tests-token-generation-middleware-and-qr-endpoint-security.md) — PocketPaw's dashboard uses a file-backed token for remote access authentication. These tests validate token generation and persistence, the auth middleware's enforcement of Bearer token and query parameter authentication, and a security fix (issue #854) that closed an unauthenticated `/api/qr` endpoint.
- [Remote Tunnel API Tests: Cloudflare Tunnel Status, Start, and Stop](./remote-tunnel-api-tests-cloudflare-tunnel-status-start-and-stop.md) — This test file covers PocketPaw's `/api/v1/remote` router, which manages a Cloudflare tunnel that exposes the local PocketPaw instance to the internet. It verifies status reporting, tunnel start with error handling for missing binaries, and graceful stop.
- [ResearchTool Test Suite — Web Search, URL Extraction, and Happy Path](./researchtool-test-suite-web-search-url-extraction-and-happy-path.md) — This test file validates `ResearchTool`, PocketPaw's built-in agentic research capability that chains web search, URL content extraction, and LLM summarization. Tests cover tool metadata, URL parsing edge cases, search failure handling, and the full happy-path pipeline using mocked external dependencies.
- [Retrieval Journal Projection Tests: Write Path, Scope Filtering, Graduation Policy, and REST Router](./retrieval-journal-projection-tests-write-path-scope-filtering-graduation-policy-.md) — Comprehensive tests for the `ee.retrieval` subsystem that journals every memory-retrieval query and drives a graduation policy promoting frequently-retrieved memories to higher tiers. Covers event write-path fidelity, scope containment, correlation grouping, threshold-based graduation decisions, projection rebuild equivalence, and the REST router's GET endpoints.
- [Room-Scoped WebSocket Routing Tests](./room-scoped-websocket-routing-tests.md) — This module tests the room-scoped routing layer in ConnectionManager, which limits typing indicators and read receipts to only the sockets currently joined to a specific group. It also verifies that non-members cannot inject typing or read-ack events into rooms they do not belong to.
- [RunPythonTool Test Suite — Sandboxed Code Execution with Guardian and File Jail](./runpythontool-test-suite-sandboxed-code-execution-with-guardian-and-file-jail.md) — This test file covers `RunPythonTool`, which executes arbitrary Python code in a sandboxed subprocess controlled by a file jail and a guardian policy engine. Tests validate stdout capture, stderr surfacing, exit code reporting, timeout enforcement, guardian blocking, filesystem isolation, temp file cleanup, and tool metadata.
- [Runtime Audit API Integration Tests — Workspace Rollup, Search, CSV Export, and Security](./runtime-audit-api-integration-tests-workspace-rollup-search-csv-export-and-secur.md) — This test module validates the canonical `/runtime/audit` endpoint introduced in Cluster C/PR4, covering workspace-scoped filtering, full-text search with length caps, CSV export, SQL injection resistance, and the deprecation forwarding behavior of the legacy `/audit` alias. It exercises both the new runtime router and the legacy router mounted together to prove they share the same underlying AuditStore and scope guard.
- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.
- [Sarvam AI Integration Tests — Translation, TTS, STT, and OCR](./sarvam-ai-integration-tests-translation-tts-stt-and-ocr.md) — This comprehensive test file covers PocketPaw's integration with Sarvam AI's multilingual APIs: text translation, text-to-speech (TTS), speech-to-text (STT), and vision OCR. Tests validate API key gating, SDK error propagation, audio encoding, file-based I/O, provider routing, policy group membership, config field defaults, and tool registration.
- [Scope Enforcement and Tool Policy Fail-Closed Tests](./scope-enforcement-and-tool-policy-fail-closed-tests.md) — This test module validates that PocketPaw's `require_scope` FastAPI dependency rejects requests that lack explicit authorization markers, preventing silent scope bypass via master, session, or cookie auth. It also verifies that `ToolPolicy` raises on unknown profile names rather than defaulting to an open or undefined state.
- [Security Audit CLI Tests — Config Permissions, API Key Exposure, and Audit Runner](./security-audit-cli-tests-config-permissions-api-key-exposure-and-audit-runner.md) — This test file validates PocketPaw's security audit command-line tool, which checks for world-readable config files, plaintext API keys, missing audit logs, unreachable guardian agents, invalid file jails, risky tool profiles, and dangerous permission bypasses. Tests cover both detection and auto-fix capabilities.
- [Security Rails Tests: Dangerous Command Blocking, Bypass Detection, and Safe Allowlist](./security-rails-tests-dangerous-command-blocking-bypass-detection-and-safe-allowl.md) — Layer 5 of PocketPaw's security stack (`pocketpaw.security.rails`) blocks dangerous shell commands before they reach the execution layer. These tests validate the blocking of destructive file operations, remote code execution patterns, obfuscation bypasses, privilege escalation, data exfiltration, and system damage commands, while confirming that safe commands are not falsely blocked.
- [Security Scrub: PII and Secret Masking in Logs and Tool Parameters](./security-scrub-pii-and-secret-masking-in-logs-and-tool-parameters.md) — The logging scrub test suite validates PocketPaw's defense-in-depth approach to preventing secrets and personally identifiable information from appearing in logs, audit trails, and tool execution records. Tests cover pattern-based field masking, nested dict traversal, audit log fallback behavior, and dangerous-command detection logging introduced in security sprint cluster C.
- [Self-Audit Daemon Tests — Stale Sessions, Config Conflicts, Disk Usage, and OAuth Tokens](./self-audit-daemon-tests-stale-sessions-config-conflicts-disk-usage-and-oauth-tok.md) — This test file covers `pocketpaw.daemon.self_audit`, a background health-check daemon that monitors for stale sessions, dangerous config combinations, excessive disk usage, oversized audit logs, and orphaned OAuth tokens. Tests validate each individual check and the full `run_self_audit` orchestrator.
- [Session and Message context_type Discriminator Validator Tests](./session-and-message-contexttype-discriminator-validator-tests.md) — These tests verify that Pydantic model validators on Message and Session enforce strict discriminator rules, ensuring group and pocket context types carry exactly the required fields and nothing more. They also confirm backward-compatible inference when context_type is omitted.
- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.
- [Session Index, REST Endpoints, WebSocket Switching, and Search Tests](./session-index-rest-endpoints-websocket-switching-and-search-tests.md) — This test file comprehensively covers `FileMemoryStore`'s session index subsystem: atomic index writes, index rebuild from disk, CRUD operations, WebSocket session switching, path traversal blocking, full-text session search, and the REST API endpoints that expose these operations to the dashboard.
- [Session Management API Tests — List, Delete, Title Update, and Search](./session-management-api-tests-list-delete-title-update-and-search.md) — This test module covers all four operations of the `/api/v1/sessions` router: listing sessions with pagination, deleting by ID, updating titles, and full-text searching session history. It uses mock-based isolation of the underlying memory store to prevent filesystem side effects while exercising the router's error-handling and edge-case paths.
- [Session Management Tool Tests — New, List, Switch, Clear, Rename, and Delete](./session-management-tool-tests-new-list-switch-clear-rename-and-delete.md) — This test file validates the six session management tools in `pocketpaw.tools.builtin.sessions`: `NewSessionTool`, `ListSessionsTool`, `SwitchSessionTool`, `ClearSessionTool`, `RenameSessionTool`, and `DeleteSessionTool`. Tests cover tool naming, happy-path behavior, error handling, and policy group membership.
- [Session Schema Tests: Create, Update, and Response Contract Validation](./session-schema-tests-create-update-and-response-contract-validation.md) — Unit tests for the three Pydantic schemas governing session lifecycle in PocketPaw's EE cloud layer — `CreateSessionRequest`, `UpdateSessionRequest`, and `SessionResponse`. The tests pin default values, optional-field behavior, and the nullable `deleted_at` field used for soft-deletion semantics.
- [Sessions Test Package Initializer](./sessions-test-package-initializer.md) — This is the empty `__init__.py` marker file for the `tests/cloud/sessions` test package. Its sole purpose is to make Python treat the directory as a package so pytest can discover and import test modules within it.
- [SessionService Realtime Event Emission Tests](./sessionservice-realtime-event-emission-tests.md) — This module tests that every mutating `SessionService` method — create, create_for_pocket, update, delete, and touch — fires the correct realtime event through `emit()` after the database commit. Tests are unit-scoped, patching DB and bus primitives at seam boundaries to isolate emit behavior from persistence.
- [Settings API Security Tests — Secret Field Filtering and Immutable Field Guards](./settings-api-security-tests-secret-field-filtering-and-immutable-field-guards.md) — This test module validates two critical security properties of the `/api/v1/settings` router: that GET never returns API keys or tokens (regression for a v0.4.16 leak), and that PUT rejects writes to security-critical immutable fields like `file_jail_path` and `bypass_permissions`. It also verifies normal settings read and write paths work correctly for non-secret, non-immutable fields.
- [Shared Cloud Tests Package Initializer](./shared-cloud-tests-package-initializer.md) — This is the empty `__init__.py` marker for the `tests/cloud/shared` test package. It enables Python's import system to treat the directory as a package, ensuring pytest can correctly discover and namespace test modules in this subdirectory.
- [Signal Channel Adapter Tests — Init, Lifecycle, Message Handling, and Bus Integration](./signal-channel-adapter-tests-init-lifecycle-message-handling-and-bus-integration.md) — This Sprint 20 test file validates `SignalAdapter`, PocketPaw's channel adapter for Signal messenger. Tests cover adapter configuration defaults, start/stop lifecycle, inbound message parsing with phone number authorization, outbound message sending with streaming support, error recovery, and MessageBus integration.
- [SimulationClock and Tick-Synchronized Scheduler Tests](./simulationclock-and-tick-synchronized-scheduler-tests.md) — This test file covers `SimulationClock` (a deterministic discrete-time clock for agent simulation), `TickSnapshot` (tick-state serialization), `DependencyScheduler.run_tick_synchronized` (tick-driven task dispatch), and simulation tick metadata injection into task prompts. Tests validate clock advancement, snapshot immutability, scheduler dispatch order, and prompt metadata inclusion.
- [SkillLoader and SKILL.md Parsing Tests](./skillloader-and-skillmd-parsing-tests.md) — This test file validates `SkillLoader` and `parse_skill_md` from `pocketpaw.skills.loader`, covering SKILL.md frontmatter parsing, optional field handling, no-frontmatter rejection, directory-name fallback for skill naming, prompt argument substitution, loader lifecycle, skill retrieval, reload, multi-path override resolution, and integration with real skill paths.
- [Skills API Security and Installation Tests — Input Validation, Injection Prevention, and Audit Logging](./skills-api-security-and-installation-tests-input-validation-injection-prevention.md) — This test module covers the full lifecycle of the skills management API: listing, searching, installing from GitHub sources, removing, and reloading. The installation tests are especially dense, exercising a whitelist-based input validator to block path traversal, shell injection, null bytes, and over-segmented source strings before any subprocess is spawned.
- [Skills API Tests — SkillLoader Search, REST Endpoints, Install, Remove, and MCP Presets](./skills-api-tests-skillloader-search-rest-endpoints-install-remove-and-mcp-preset.md) — This test file covers `SkillLoader.search()` and the full skills REST API: listing installed skills, searching the skills.sh library, installing skills via git clone, removing skills, force-reloading, and MCP preset configuration with `needs_args` metadata. Tests validate filtering, case-insensitive matching, subprocess handling, and preset response shapes.
- [Slack Channel Adapter: Integration Tests for Messaging, Streaming, and Auth](./slack-channel-adapter-integration-tests-for-messaging-streaming-and-auth.md) — Tests for PocketPaw's Slack channel adapter, covering message send/receive, threaded replies, stream buffering, channel filtering, and token validation. Uses lightweight mock classes to avoid a real Slack dependency while exercising the full adapter surface.
- [Soul Bridge and Bootstrap Provider Tests: SoulBridge and SoulBootstrapProvider](./soul-bridge-and-bootstrap-provider-tests-soulbridge-and-soulbootstrapprovider.md) — These tests cover the `SoulBootstrapProvider` — which translates a Soul Protocol soul object into a `BootstrapContext` for PocketPaw's agent runtime — and `SoulBridge`, the async shim that wraps soul `observe()` and `recall()` calls with error-swallowing guarantees. Together they ensure the paw module can safely integrate with soul-protocol without crashing when the soul is unavailable or misbehaves.
- [Soul Memories API Tests: Tier Filtering, Limit Clamping, and Entry Serialization](./soul-memories-api-tests-tier-filtering-limit-clamping-and-entry-serialization.md) — Unit tests for the `GET /api/v1/soul/memories` endpoint, focusing on the `_collect_tier_entries` helper that queries individual memory tiers, limit clamping to safe bounds, tier filter validation, and graceful handling of absent souls or missing tier stores. Tests operate at the function level to avoid spinning up a full SSE client.
- [Soul-Protocol Integration Tests: End-to-End PocketPaw Wiring](./soul-protocol-integration-tests-end-to-end-pocketpaw-wiring.md) — Integration tests that validate the full lifecycle of the soul-protocol package within PocketPaw, including bootstrap prompt generation, memory observe-and-recall, tool bridge discovery of soul tools, and corrupt file recovery. All tests are conditionally skipped when `soul-protocol` is not installed.
- [Soul Protocol v0.2.4 Smoke Tests: Reload, Evaluate, Biorhythm, and Fatigue](./soul-protocol-v024-smoke-tests-reload-evaluate-biorhythm-and-fatigue.md) — Smoke tests for soul-protocol v0.2.4 features including hot-reload, the evaluate endpoint, biorhythm config round-trips, dirty tracking for autosave optimization, external change detection, and the fatigue hint in bootstrap context. Designed to catch regressions when upgrading the soul-protocol dependency.
- [SoulManager Lifecycle Tests: Birth, Persistence, Import, and Concurrency](./soulmanager-lifecycle-tests-birth-persistence-import-and-concurrency.md) — Comprehensive tests for `SoulManager`, the singleton that manages a PocketPaw soul's full lifecycle — from birth through autosave, import, reload, and shutdown. Covers tool caching, dirty tracking, concurrent observe serialization, corrupt file fallback, and biorhythm configuration.
- [Speech-to-Text Tool Tests: Schema, File Jail, Multi-Provider, and Error Handling](./speech-to-text-tool-tests-schema-file-jail-multi-provider-and-error-handling.md) — Tests for PocketPaw's `SpeechToTextTool`, covering schema validation, file path jailing for security, size limits, successful transcription with both OpenAI Whisper and ElevenLabs providers, language parameter forwarding, and graceful handling of missing API keys and provider errors.
- [Spotify Tool Tests: Schema Validation, Auth Guards, and Playback Control](./spotify-tool-tests-schema-validation-auth-guards-and-playback-control.md) — Tests for PocketPaw's Spotify integration tools covering schema correctness, authentication failure handling, playback action validation, and successful API interactions. Ensures the tools surface clear errors when credentials are absent and route API calls correctly when authenticated.
- [SSRF URL Validation Tests: Blocking Internal and Malicious URLs in Settings](./ssrf-url-validation-tests-blocking-internal-and-malicious-urls-in-settings.md) — This test file, added during security sprint cluster E (issue #703), validates the `validate_external_url` function that guards all URL-typed settings fields against Server-Side Request Forgery (SSRF) attacks. It covers rejection of cloud metadata endpoints, RFC 1918 ranges, non-HTTP schemes, and malformed inputs, while confirming a developer-friendly escape hatch for local development.
- [StatusTracker Tests: Event-Driven Agent State Machine](./statustracker-tests-event-driven-agent-state-machine.md) — Tests for `StatusTracker`, which maintains real-time agent state by consuming `SystemEvent` objects from the internal event bus. Covers state transitions from idle through thinking, tool execution, and error states, plus multi-session concurrency and token usage accumulation.
- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.
- [StreamEvent Processing Tests: Token-by-Token Streaming and Thinking Integration](./streamevent-processing-tests-token-by-token-streaming-and-thinking-integration.md) — Tests for PocketPaw's handling of `StreamEvent` objects from the Claude Agent SDK, covering text delta emission, thinking block lifecycle, tool-use start events, duplicate suppression, multi-turn state reset, and integration of thinking events into the AgentLoop's system event bus.
- [SystemInfoTool: System Status with Optional psutil Dependency](./systeminfotool-system-status-with-optional-psutil-dependency.md) — The `SystemInfoTool` reports CPU, RAM, disk, network, and process information to agents, with graceful degradation to platform-only info when `psutil` is not installed. Tests cover the basic output contract, psutil-dependent sections (network, processes), fallback behavior without psutil, error handling when `get_system_status` raises, and the tool's name and schema definition.
- [Task Skip Semantics: Dependency Unblocking and Project Completion](./task-skip-semantics-dependency-unblocking-and-project-completion.md) — Covers the test suite that validates how SKIPPED tasks interact with the dependency scheduler and project completion logic in PocketPaw's deep-work system. Ensures skipped tasks satisfy blocked_by conditions, contribute to project completion, and appear correctly in progress reporting.
- [Task Status Persistence Tests: Reproducing and Fixing a FastAPI Body vs Query Param Bug](./task-status-persistence-tests-reproducing-and-fixing-a-fastapi-body-vs-query-par.md) — Tests that reproduce and validate the fix for a bug where task status updates via the API did not persist — caused by FastAPI treating a bare `str` parameter as a query param while the frontend sent it as a JSON body. Also validates that `projectTasks` lists are updated alongside the main tasks list.
- [Telegram Channel Adapter Tests: Typing Indicators, Smart Buffering, and Voice Routing](./telegram-channel-adapter-tests-typing-indicators-smart-buffering-and-voice-routi.md) — Tests for PocketPaw's Telegram adapter covering typing indicator lifecycle, smart stream buffering with rate limiting, markdown formatting on flush, voice media routing, and the complete streaming cycle from first chunk to final message. The `python-telegram-bot` library is mocked as an optional dependency.
- [Telegram Channel API Tests — Configuration Status and Pairing State](./telegram-channel-api-tests-configuration-status-and-pairing-state.md) — This test module covers the two read-only Telegram status endpoints: `/api/v1/telegram/status` (reports whether a bot token and allowed user ID are configured) and `/api/v1/telegram/pairing-status` (reports live pairing state from a dashboard-level in-memory dict). Both endpoints inform the dashboard whether the Telegram channel is ready to receive messages.
- [Telegram Forum Topics Support: Chat ID Parsing and Session Key Isolation](./telegram-forum-topics-support-chat-id-parsing-and-session-key-isolation.md) — Tests for Telegram Group Topics (forum supergroups) support in the PocketPaw Telegram adapter, covering the `_parse_chat_id` utility that splits composite chat IDs into chat and topic components, and session key generation that ensures different forum topics produce different session keys.
- [Test Suite for A2A Protocol Live Smoke Tests](./test-suite-for-a2a-protocol-live-smoke-tests.md) — This script is a live, end-to-end smoke test suite for PocketPaw's A2A (Agent-to-Agent) protocol endpoints. It requires a running PocketPaw server with `POCKETPAW_A2A_ENABLED=true` and exercises every major A2A feature — agent card discovery, message sending, task management, streaming, cancellation, and error handling — reporting PASS/FAIL for each check.
- [Test Suite for Knowledge Ingestion: CFO Agent Creation and Document Upload Smoke Test](./test-suite-for-knowledge-ingestion-cfo-agent-creation-and-document-upload-smoke-.md) — A manual smoke test script that creates a CFO-persona agent via the REST API and ingests three fixture documents into its knowledge base. It lives in `scripts/` rather than `tests/` to keep it out of pytest collection and CI, and requires a running backend with a valid bearer token to execute.
- [TextToSpeechTool: Multi-Provider TTS with Error Handling and Audio File Management](./texttospeechtool-multi-provider-tts-with-error-handling-and-audio-file-managemen.md) — The `TextToSpeechTool` converts text to speech using configurable providers (OpenAI TTS, ElevenLabs) and saves audio files to a managed directory. Tests cover the tool's definition (name, trust level, parameters), provider-specific error paths for missing API keys, unknown provider rejection, successful ElevenLabs synthesis, API error handling, the `synthesize_speech` helper, and the audio directory creation utility.
- [Tool Bridge: Multi-Backend Tool Adaptation and Policy Enforcement](./tool-bridge-multi-backend-tool-adaptation-and-policy-enforcement.md) — The tool bridge (`agents/tool_bridge.py`) adapts PocketPaw's internal tool registry for multiple AI backend SDKs — OpenAI Agents, Google ADK, Claude Agent SDK, and others — while enforcing tool policies and generating compact markdown instructions. Tests cover backend-specific tool exclusions, graceful import failure handling, OpenAI schema normalization, invoke callback error handling, and policy-filtered instruction generation.
- [Tool Filter: Relevance-Based Tool Pre-Selection for Context Window Management](./tool-filter-relevance-based-tool-pre-selection-for-context-window-management.md) — The tool filter (`tools/filter.py`) limits the number of tools injected into an LLM context window by scoring tools against the current user message and always pinning a core set. Tests cover passthrough below the cap, max-tools truncation, always-include pinning semantics, relevance scoring, empty message handling, pinned slot accounting, and the tokenizer helper.
- [Tool Policy System: Profile-Based Access Control and MCP Server Authorization](./tool-policy-system-profile-based-access-control-and-mcp-server-authorization.md) — The tool policy system (`tools/policy.py`) enforces which tools an agent instance is allowed to use, based on named profiles (`minimal`, `coding`, `full`), explicit allow/deny lists, and group references. Tests cover profile resolution, group expansion, allow/deny precedence, ToolRegistry integration, and MCP server authorization — including a critical fix for fail-closed behavior on unknown profile names.
- [Tool Protocol and Registry: Core Registration, Execution, and Filesystem Jail Tests](./tool-protocol-and-registry-core-registration-execution-and-filesystem-jail-tests.md) — These tests exercise the foundational `ToolRegistry` and `BaseTool` protocol, the `ShellTool`'s command-blocking and timeout behavior, and the filesystem tools' write/read/list operations within a file jail. The jail fixture pattern and dangerous-command blocking together form PocketPaw's first line of defense against unsafe tool invocations.
- [Tool Registry Validation: Required Parameter Guards and Audit Logging (Issue #793)](./tool-registry-validation-required-parameter-guards-and-audit-logging-issue-793.md) — These tests cover the fix for issue #793, where `ToolRegistry.execute` was accepting empty strings and whitespace-only values for required tool parameters, allowing agent-generated calls to silently bypass input validation. The fix adds strict pre-execution validation that rejects `None`, missing, empty, and whitespace-only values for string parameters marked as required, and emits an audit log entry on failure.
- [Unified Files Service Tests: Workspace Isolation, Dedup, Drive Warnings, and Limit Cap](./unified-files-service-tests-workspace-isolation-dedup-drive-warnings-and-limit-c.md) — This module tests `UnifiedFilesService`, which aggregates files from multiple sources (chat uploads, Drive, local) into a single normalized list, covering workspace scope isolation, soft-delete filtering, cross-source deduplication, source-specific warnings, and the per-request limit cap. It uses `mongomock-motor` to run Beanie document tests in CI without a live MongoDB instance.
- [UnreadService List Unreads Calculation Tests](./unreadservice-list-unreads-calculation-tests.md) — This module tests UnreadService.list_unreads, which computes per-group unread message and mention counts for a user. Tests cover the caught-up state, partial reads, the fresh-user fallback, and the edge case where a ReadState row exists but has no recorded read position.
- [Update Check System: PyPI Version Polling, Caching, Release Notes, and Styled Notices](./update-check-system-pypi-version-polling-caching-release-notes-and-styled-notice.md) — The update check module polls PyPI for new PocketPaw versions, caches results to avoid repeated network calls, fetches release notes from GitHub, tracks which versions the user has already seen, and displays styled terminal notices. Tests cover version parsing, cache freshness/staleness, network error handling, corrupted cache recovery, async wrapping, TTY detection for notice suppression, and the version-seen tracking mechanism.
- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.
- [Upload Error Hierarchy Tests: Typed Exception Codes and Inheritance](./upload-error-hierarchy-tests-typed-exception-codes-and-inheritance.md) — This module validates the upload error hierarchy in `pocketpaw.uploads.errors`, ensuring all domain-specific exceptions inherit from `UploadError`, carry typed `code` attributes, and preserve error messages. These contracts allow API routes and clients to handle upload failures programmatically without string matching.
- [Upload Grant Signing Tests: HMAC Token Integrity and Expiry](./upload-grant-signing-tests-hmac-token-integrity-and-expiry.md) — This small but security-critical test module verifies the `sign_grant` / `verify_grant` pair in PocketPaw's upload subsystem, ensuring that download grants are cryptographically bound to a specific file ID, expire correctly, and cannot be forged or transplanted across files.
- [Upload Path Utility Tests: Normalization, Traversal Defense, Subpath Checks, and Join Safety](./upload-path-utility-tests-normalization-traversal-defense-subpath-checks-and-joi.md) — Exhaustive unit tests for the `ee.cloud.uploads.paths` module, which provides the safe path utilities used throughout the upload folder system. Tests cover normalization (canonicalization, dot-stripping, repeated-slash collapse), rejection of path traversal sequences and dangerous characters, subpath relationship checks, and safe join operations.
- [Upload Read Authorization Tests: Owner, Chat Member, Workspace Admin, and Existence-Hiding](./upload-read-authorization-tests-owner-chat-member-workspace-admin-and-existence-.md) — Tests for `EEUploadService._assert_can_read`, the read gate used by `stream`, `presigned_get`, and the `/download-url` and `/grant` endpoints. The parametrized suite verifies that the owner, chat members, and workspace admins can read while strangers receive `NotFound` (not `Forbidden`) — an existence-hiding design that prevents file-ID probing.
- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.
- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.
- [Upload Service Tests: Validation, Bulk Handling, and Streaming](./upload-service-tests-validation-bulk-handling-and-streaming.md) — This test suite exercises the `UploadService` layer in PocketPaw's file upload subsystem, covering single and bulk file ingestion, magic-byte MIME sniffing, path sanitisation, and ownership-gated streaming and deletion. It uses an in-memory `_FakeAdapter` to isolate tests from real storage backends while still exercising the full service contract.
- [Upload Test Fixtures: Isolated Filesystem Root, In-Memory MongoDB, and MongoFileStore](./upload-test-fixtures-isolated-filesystem-root-in-memory-mongodb-and-mongofilesto.md) — Shared pytest fixtures for the uploads test suite providing a per-test isolated filesystem root, an in-memory MongoDB instance via `mongomock-motor`, and a ready-to-use `MongoFileStore`. The `beanie_upload_db` fixture includes a workaround for a `mongomock-motor` incompatibility with Beanie's `list_collection_names` call.
- [Upload Tests Package Marker: Making the Uploads Test Directory a Python Package](./upload-tests-package-marker-making-the-uploads-test-directory-a-python-package.md) — An empty `__init__.py` that marks `tests/cloud/uploads/` as a Python package so pytest can discover and import test modules within it using absolute import paths. No logic is present; its presence is structural.
- [Uploads Test Conftest: Isolated Temporary Upload Root Fixture](./uploads-test-conftest-isolated-temporary-upload-root-fixture.md) — This conftest defines `tmp_upload_root`, a pytest fixture that provides an isolated temporary directory for each test in the uploads test suite. It prevents test pollution between file system tests by ensuring every test starts with a clean, empty upload root.
- [Uploads Test Package Initializer](./uploads-test-package-initializer.md) — This is an empty `__init__.py` that marks the `tests/uploads/` directory as a Python package, enabling pytest to discover and import tests within it using standard package-relative imports. No logic lives here; its presence is structural.
- [UploadsProvider Tests: Field Mapping, Mounts, RBAC, and Folder Listing](./uploadsprovider-tests-field-mapping-mounts-rbac-and-folder-listing.md) — This module tests the UploadsProvider, which surfaces user file uploads as a virtual file tree with folder hierarchy support. Tests cover field mapping from the raw upload store format to FileEntry, mount resolution, baseline RBAC (owner vs. non-owner vs. admin), and the combined folder+file listing behavior.
- [UrlExtractTool: SSRF Defense, DNS Pinning, Private IP Blocking, and Multi-Provider Extraction](./urlextracttool-ssrf-defense-dns-pinning-private-ip-blocking-and-multi-provider-e.md) — The `UrlExtractTool` fetches and extracts text from URLs, supporting Parallel AI (cloud) and local httpx (self-hosted) providers with auto-selection. The test suite places particular emphasis on SSRF (Server-Side Request Forgery) defenses: private IP blocking, DNS TOCTOU race condition prevention via IP pinning, and IPv4-mapped IPv6 address rejection.
- [UsageTracker: Cached Token Counting Fix and Unbounded Summary Aggregation](./usagetracker-cached-token-counting-fix-and-unbounded-summary-aggregation.md) — These tests document and verify two bug fixes in `UsageTracker`: cached input tokens were silently excluded from `total_tokens`, and `get_summary()` was capped at 10,000 records, causing understated lifetime aggregation totals for high-volume installations. Both fixes ensure accurate cost accounting across all Anthropic and OpenAI backends.
- [User Profile Bootstrap: USER.md Integration with BootstrapContext and System Prompt Injection](./user-profile-bootstrap-usermd-integration-with-bootstrapcontext-and-system-promp.md) — The bootstrap system supports a `USER.md` file that stores human-readable user profile information (name, timezone, preferences) which is loaded into `BootstrapContext` and injected into the agent's system prompt under a `# User Profile` section. Tests verify the default empty state, template creation, idempotent non-overwrite, loading, system prompt injection, and graceful handling of a missing file.
- [V1 Router Registration and CORS Configuration Tests](./v1-router-registration-and-cors-configuration-tests.md) — Validates that all expected domain routers are registered in PocketPaw's v1 API surface and that CORS origins include the Tauri desktop app origin. These tests act as a registry contract — any new router added to the system must be explicitly listed, preventing silent omissions that would cause 404s from newly added features.
- [Wave 2 Shared Test Fixtures: Workspace Factory, User Token, Seeded Channel, and Mock S3 Smoke Tests](./wave-2-shared-test-fixtures-workspace-factory-user-token-seeded-channel-and-mock.md) — Smoke tests that validate the four shared pytest fixtures defined in `tests/ee/conftest.py` for the Wave 2 ee/cloud test infrastructure. Each test exercises exactly one factory (workspace creation, user authentication, channel seeding, and in-memory S3), proving the conftest wiring works end-to-end before the feature-level tests that depend on it are run.
- [Web Server Security Tests: Bot Token Redaction in /setup Errors](./web-server-security-tests-bot-token-redaction-in-setup-errors.md) — This module tests that the `/setup` endpoint in PocketPaw's web server never leaks the Telegram bot token in HTTP error responses (F-07, issue #445). It verifies that when the upstream Telegram API returns an error whose message embeds the token URL, the response is redacted before reaching the client.
- [Webhook Adapter Test Suite: Inbound Payload Handling, Sync Mode, and Send Routing](./webhook-adapter-test-suite-inbound-payload-handling-sync-mode-and-send-routing.md) — This module tests the `WebhookAdapter`, PocketPaw's generic inbound HTTP webhook channel. It covers slot configuration, payload normalization, sync vs. async dispatch modes, future-based response resolution, and streaming accumulation.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.
- [Webhooks API Tests — CRUD, Secret Redaction, Duplicate Prevention, and Secret Rotation](./webhooks-api-tests-crud-secret-redaction-duplicate-prevention-and-secret-rotatio.md) — This test module covers the full lifecycle of PocketPaw's webhook configuration API: listing (with secret redaction), adding (with name validation and duplicate prevention), removing, and regenerating shared secrets. Webhooks enable external services to trigger agent actions, and the tests enforce that secrets are never exposed at rest through the API listing endpoint.
- [WebSearchTool Test Suite: Multi-Provider Search Validation](./websearchtool-test-suite-multi-provider-search-validation.md) — This test module validates the `WebSearchTool` built-in tool in PocketPaw, covering provider dispatch, API key guards, HTTP error handling, result clamping, and the parallel-search variant. It ensures the tool behaves safely and predictably across Tavily, Brave, and Parallel AI backends.
- [WebSocket Connection Manager Tests: Multi-Device, Typing Indicators, and Offline Grace Periods](./websocket-connection-manager-tests-multi-device-typing-indicators-and-offline-gr.md) — Tests for `ee.cloud.chat.ws.ConnectionManager`, the in-process registry that tracks active WebSocket connections per user, routes outbound messages, and manages typing indicators with auto-expiry timers. Key behavioral contracts include dead-connection cleanup, multi-device fan-out, idempotent stop-typing, and cancellation of offline grace period tasks on reconnect.
- [WhatsApp Adapter Test Suite: Webhook Verification, Lifecycle, and Message Sending](./whatsapp-adapter-test-suite-webhook-verification-lifecycle-and-message-sending.md) — This module tests PocketPaw's `WhatsAppAdapter`, covering the Meta webhook verification handshake, adapter lifecycle (`start`/`stop`), outbound message delivery, allowed-number filtering, inbound message routing, and media handling. It ensures the adapter correctly implements the WhatsApp Cloud API protocol.
- [Widget Co-occurrence Accept/Dismiss Endpoint Tests: Route Separation, Validation, and Signature Echo](./widget-co-occurrence-acceptdismiss-endpoint-tests-route-separation-validation-an.md) — Tests the two writer endpoints (`POST /widgets/cooccurrence/accept` and `POST /widgets/cooccurrence/dismiss`) that record user decisions on co-occurring widget suggestions from the SuggestedWidgetsFeed. Pins happy-path response shape, Pydantic validation gates, scope fallback to `["org:*"]`, strict route separation between the two event types, and a signature-echo contract that prevents feed state corruption if the tokenisation rule changes.
- [Widget Journal Projection Tests: Interactions, Graduation, Co-occurrence Signature Fix, and Router](./widget-journal-projection-tests-interactions-graduation-co-occurrence-signature-.md) — Comprehensive tests for the widget journal projection subsystem covering the write path for interactions, graduations, and co-occurrence events; scope containment in usage roll-ups; the pin-threshold graduation policy; a regression guard for the `sorted(tokens[:6])` co-occurrence signature bug from PR #942; archive rules for inactive widgets; and the three GET router endpoints.
- [Widget Track Endpoint Tests: Interaction Recording, Validation, Scope Fallback, and Projection Integration](./widget-track-endpoint-tests-interaction-recording-validation-scope-fallback-and-.md) — Tests for `POST /widgets/track`, the writer endpoint that records widget interactions from the SuggestedWidgetsFeed UI. Covers the full happy-path event emission, Pydantic validation of required fields, metadata defaulting, correlation-id propagation, sequential seq increments, the `org:*` scope fallback for anonymous actors, and an end-to-end proof that three POSTs reflect correctly in the `GET /widgets/usage` projection.
- [Workspace Cloud Test Package Initializer](./workspace-cloud-test-package-initializer.md) — This empty `__init__.py` marks the `tests/cloud/workspace/` directory as a Python package, enabling pytest to discover and import test modules within it using standard package import resolution. No code lives here; the file's presence is its entire purpose.
- [Workspace KB Aggregator: Merge, Filter, Dedup, Sort, and Resilience Tests](./workspace-kb-aggregator-merge-filter-dedup-sort-and-resilience-tests.md) — This test suite validates the `ee.cloud.kb.workspace_aggregator` module, which merges knowledge base articles from workspace and agent scopes into a single sorted, deduplicated list. It tests merge logic, agent filtering, deduplication by (scope, id), newest-first ordering, resilience when a scope returns malformed data, and the `AggregatedArticle.to_dict()` shape.
- [Workspace Message Search Tests](./workspace-message-search-tests.md) — This test module covers MessageService's workspace-wide message search, verifying that results respect group visibility (channels vs. private rooms), workspace scope isolation, regex-safety of user queries, and server-side result limits. Tests run against an in-memory MongoDB via the shared beanie_memory_db fixture to exercise real Mongo query behavior.
- [Workspace Schema Tests: Slug Validation, Invite Rules, and Member Role Contracts](./workspace-schema-tests-slug-validation-invite-rules-and-member-role-contracts.md) — Tests for the workspace domain Pydantic schemas in PocketPaw's EE layer, covering slug format enforcement, invite role validation, and member role update rules. The slug tests are particularly important because workspace slugs appear in URLs and must be lowercase, hyphen-separated, and free of leading/trailing hyphens.
- [WorkspaceService Realtime Event Emission and Cache Invalidation Tests](./workspaceservice-realtime-event-emission-and-cache-invalidation-tests.md) — This suite verifies that every mutating `WorkspaceService` method fires the correct typed event through `emit()`, and that membership or admin list changes also invalidate the `AudienceResolver` workspace cache. All database and permission primitives are patched at their seams, isolating the emit behavior from persistence concerns.

### `test fixtures` — 1 article(s)

- [Upload Test Fixtures: Isolated Filesystem Root, In-Memory MongoDB, and MongoFileStore](./upload-test-fixtures-isolated-filesystem-root-in-memory-mongodb-and-mongofilesto.md) — Shared pytest fixtures for the uploads test suite providing a per-test isolated filesystem root, an in-memory MongoDB instance via `mongomock-motor`, and a ready-to-use `MongoFileStore`. The `beanie_upload_db` fixture includes a workaround for a `mongomock-motor` incompatibility with Beanie's `list_collection_names` call.

### `test infrastructure` — 2 article(s)

- [Browser Automation Test Package Initializer](./browser-automation-test-package-initializer.md) — This is the `__init__.py` for the `tests/test_browser/` package, marking the browser automation tests as a proper Python package so pytest can discover and import them as a cohesive module group. The file contains no code or fixtures — its presence enables relative imports between the four browser test modules.
- [Wave 2 Shared Test Fixtures: Workspace Factory, User Token, Seeded Channel, and Mock S3 Smoke Tests](./wave-2-shared-test-fixtures-workspace-factory-user-token-seeded-channel-and-mock.md) — Smoke tests that validate the four shared pytest fixtures defined in `tests/ee/conftest.py` for the Wave 2 ee/cloud test infrastructure. Each test exercises exactly one factory (workspace creation, user authentication, channel seeding, and in-memory S3), proving the conftest wiring works end-to-end before the feature-level tests that depend on it are run.

### `testing` — 336 article(s)

- [A2A Client Tests: HTTP Operations, Context Manager Reuse, Streaming, Delegate Tool, and SSRF Protection](./a2a-client-tests-http-operations-context-manager-reuse-streaming-delegate-tool-a.md) — Tests for the PocketPaw Agent-to-Agent (A2A) client library, covering HTTP task operations (send, get, cancel, stream), response error handling, agent card fetching with caching, the `A2ADelegateTool` multi-turn wrapper, and SSRF protection that blocks requests to private IP ranges and invalid schemes.
- [A2A Server Tests: Models, State Machines, JSON-RPC Dispatch, and Task Lifecycle](./a2a-server-tests-models-state-machines-json-rpc-dispatch-and-task-lifecycle.md) — Extensive tests for the PocketPaw A2A server implementation covering Pydantic model serialisation with type discriminators, TaskState transition validation, JSON-RPC 2.0 dispatcher behaviour (parse errors, batch requests, method routing), and the full task lifecycle (submit, poll, stream, cancel) against a minimal in-memory FastAPI app.
- [ABAC Config Tests: Rule Loading and Allow/Deny Logic](./abac-config-tests-rule-loading-and-allowdeny-logic.md) — This module tests the Attribute-Based Access Control (ABAC) configuration layer for the files subsystem, covering YAML rule loading, the allow logic for tagged vs. untagged entries, attribute matching, and the deny-override behavior when a file carries multiple tags.
- [Agent Backend Protocol: Capability Flags, BackendInfo, and Identity Contract Tests](./agent-backend-protocol-capability-flags-backendinfo-and-identity-contract-tests.md) — This test suite validates the core contracts of PocketPaw's agent backend abstraction layer, covering the `_DEFAULT_IDENTITY` fallback string, the `Capability` flag enum, the immutable `BackendInfo` descriptor, and the `AgentBackend.run()` method signature. Together these tests enforce the interface that every backend adapter must satisfy to be a valid plugin in PocketPaw's multi-backend architecture.
- [Agent Backend Registry: Lazy Import, Legacy Name Resolution, and Plugin Registration Tests](./agent-backend-registry-lazy-import-legacy-name-resolution-and-plugin-registratio.md) — This test suite covers PocketPaw's backend registry, which provides lazy-loaded access to agent backends (Claude SDK, OpenAI Agents, Google ADK, OpenCode) while maintaining backward compatibility through a legacy name mapping. Tests verify that deprecated names like `gemini_cli` transparently resolve to `google_adk`, that unknown backends return `None` rather than raising, and that third-party plugins can register themselves at runtime.
- [Agent Backends and Router Tests: ClaudeSDKBackend, Registry Routing, Dangerous Hook, and CLI Auth](./agent-backends-and-router-tests-claudesdkbackend-registry-routing-dangerous-hook.md) — Tests for the multi-SDK agent architecture covering `AgentEvent` protocol, `ClaudeSDKBackend` (formerly ClaudeAgentSDKWrapper), registry-based `AgentRouter` backend selection with legacy fallbacks, the fail-closed dangerous command hook, and a regression test for PocketPaw incorrectly requiring an API key when Claude CLI authentication is active.
- [Agent Bridge Attachment Forwarding Regression Test](./agent-bridge-attachment-forwarding-regression-test.md) — This regression test guards against the silent attachment-drop bug in `_run_agent_response`, where file attachments sent by users in channel messages were never forwarded into the agent's prompt. The test confirms that the channel code path formats attachment metadata — filename, MIME type, and size — into the prompt in the same shape used by the DM path.
- [Agent Bridge Event Routing and Wire Type Tests](./agent-bridge-event-routing-and-wire-type-tests.md) — This module verifies that `agent_bridge` routes all group broadcasts through `emit()` rather than directly calling `ws_manager.broadcast_to_group`, and that the four agent stream event types (`AgentStreamStart`, `AgentStreamChunk`, `AgentToolUse`, `AgentStreamEnd`) are correctly defined and importable from the bridge.
- [Agent Delegation: ExternalAgentDelegate and DelegateTool Tests](./agent-delegation-externalagentdelegate-and-delegatetool-tests.md) — This suite tests PocketPaw's agent delegation system, which allows a running agent to hand off subtasks to external CLI-based agents (initially Claude Code). It covers the DelegationResult dataclass, availability detection via PATH lookup, unknown agent rejection, and the DelegateTool's trust level, parameter schema, and execution paths including error handling for missing installations.
- [Agent Domain Schema Validation Tests](./agent-domain-schema-validation-tests.md) — This module comprehensively tests the Pydantic schemas for the agents domain — `CreateAgentRequest`, `UpdateAgentRequest`, `DiscoverRequest`, `AgentResponse`, `ScopeAssignmentRequest`, and `ScopeAssignmentResponse` — covering required fields, defaults, length constraints, visibility validation, and scope normalization. Scope-related tests were added in the `feat/cluster-d-agent-scope-picker` feature.
- [Agent Loop Media Helpers: _format_bytes Unit Tests](./agent-loop-media-helpers-formatbytes-unit-tests.md) — Focused unit tests for the `_format_bytes` helper in `pocketpaw.agents.loop`, verifying that file sizes are formatted correctly across the B, KB, MB, and GB ranges. This utility generates human-readable size strings for media attachment prompts injected into the agent's context.
- [Agent Loop Pocket Threading Tests: Cloud User and Workspace Context Resolution](./agent-loop-pocket-threading-tests-cloud-user-and-workspace-context-resolution.md) — Tests for the `_create_pocket_and_session` and `_publish_pocket_event` functions in `pocketpaw.agents.loop`, covering all resolution paths for threading cloud user identity and workspace context through pocket creation: explicit ids, active-workspace fallback, first-user fallback for self-hosted deployments, graceful handling of invalid ids, and metadata propagation in pocket events.
- [Agent Loop Tests: Message Processing, AgentEvent Architecture, Error Handling, and GC Lock Management](./agent-loop-tests-message-processing-agentevent-architecture-error-handling-and-g.md) — Tests for the unified `AgentLoop` that orchestrates message consumption, agent router invocation, memory management, and outbound publishing. Updated for the `AgentEvent`-based streaming architecture (replacing raw dict chunks), these tests verify end-to-end message processing, graceful error handling, stale-lock GC, and loop teardown.
- [Agent Scope Rules: Normalization and Assignment Authorization Tests](./agent-scope-rules-normalization-and-assignment-authorization-tests.md) — This module tests `ee.cloud.agents.scope_rules`, covering the `normalise_and_validate_scopes` function (grammar enforcement, lowercasing, deduplication, forbidden-wildcard rejection) and the `admin_can_assign_scopes` function (containment check that prevents scope-narrowed admins from assigning scopes outside their own grant).
- [Agent Seed Idempotency and DM Chat Persistence with Attachments Tests](./agent-seed-idempotency-and-dm-chat-persistence-with-attachments-tests.md) — This module tests the `seed_default_agent` function for idempotency and per-workspace isolation, validates that `MongoMemoryStore.save` persists attachments to the `Message` document and updates the session's `messageCount` and `lastActivity`, and verifies that `SessionService.get_history` returns attachments and `SessionService.list_by_agent` applies soft-delete filtering.
- [Agent Soul Configuration and OCEAN Personality Schema Tests](./agent-soul-configuration-and-ocean-personality-schema-tests.md) — This module tests the `AgentConfig` model's soul-related fields — default values, persona assignment, custom OCEAN personality vectors, and absence of a `soul_path` attribute — and verifies that `CreateAgentRequest` correctly propagates `persona` and soul OCEAN customization fields.
- [Agent Status API Tests: Auth, Snapshot Shape, Version Tracking, and CLI Formatting](./agent-status-api-tests-auth-snapshot-shape-version-tracking-and-cli-formatting.md) — Tests for PocketPaw's agent status API endpoint, covering authentication via header and query parameter, snapshot structure validation against the API contract, version-based change detection for efficient polling, and CLI duration formatting helpers.
- [AgentLoop Concurrency Controls: Session Locks, Global Semaphore, and File Memory Safety](./agentloop-concurrency-controls-session-locks-global-semaphore-and-file-memory-sa.md) — This test module validates the three-layer concurrency model in PocketPaw's AgentLoop: per-session serialization via asyncio locks, cross-session parallelism as the happy path, and a global semaphore that caps total simultaneous conversations. It also covers the FileMemoryStore's write lock and transient PermissionError retry logic that prevent JSON corruption under concurrent writes.
- [AgentRouter Fallback and Streaming Tests](./agentrouter-fallback-and-streaming-tests.md) — This test file validates `AgentRouter`'s fault-tolerant backend selection logic, covering hard exception failures, error-event failures, full backend chain exhaustion, streaming event pass-through, and no-fallback configurations. The tests use in-process stub backends injected via `monkeypatch` to exercise the router without real LLM backends.
- [AGENTS.md Support Tests: Loader, Caching, Directory Walk, Context Injection, and SystemEvent Emission](./agentsmd-support-tests-loader-caching-directory-walk-context-injection-and-syste.md) — Tests for the AGENTS.md feature (issue #456) that allows project-specific constraints to be injected into the agent's system prompt. Covers the `AgentsMdLoader` find-and-cache logic (including `.git` boundary stopping, nearest-file preference, and mtime-based cache invalidation), `AgentsMd` formatting, `AgentContextBuilder` injection, and `AgentLoop` `SystemEvent` emission when the file is found.
- [API Backward Compatibility and Versioned Endpoint Tests](./api-backward-compatibility-and-versioned-endpoint-tests.md) — This module verifies that both the legacy `/api/` endpoints and the versioned `/api/v1/` endpoints in the PocketPaw dashboard respond with HTTP 200, guarding against breaking changes during the v1 API migration. It also checks that OpenAPI documentation endpoints remain accessible.
- [API Contract Tests: Response Shape Pinning for Schema Rewrite](./api-contract-tests-response-shape-pinning-for-schema-rewrite.md) — This module pins the exact JSON field sets for PocketPaw's cloud messaging and session endpoints before a planned unified-schema rewrite, using frozen `frozenset` constants as shape contracts. Tests run against real MongoDB via `mongomock` and a full ASGI test client, ensuring any field rename or addition detected post-rewrite breaks the test by design.
- [API Key Management Tests: Creation, Verification, Rotation, and REST Endpoints](./api-key-management-tests-creation-verification-rotation-and-rest-endpoints.md) — Comprehensive tests for PocketPaw's `APIKeyManager` class and its REST endpoints covering the full key lifecycle — creation with scope validation, verification, revocation, rotation, and expiry rejection. The test suite isolates all disk I/O to a temp directory to prevent cross-test contamination and ensure file permission checks work correctly.
- [API Keys Panel Acceptance Tests: UI Lifecycle Flows for Cluster F](./api-keys-panel-acceptance-tests-ui-lifecycle-flows-for-cluster-f.md) — End-to-end acceptance tests that walk the exact flows the `ApiKeysPanel.svelte` UI executes — create, list, rotate (with old key invalidation), revoke, and sort-by-creation. These tests complement unit-level coverage by pinning the API shape the frontend depends on, catching regressions that break the dashboard silently.
- [API-Only Serve Mode Tests: Structure, Auth Middleware, CLI Parsing, and Socket Safety](./api-only-serve-mode-tests-structure-auth-middleware-cli-parsing-and-socket-safet.md) — Tests for PocketPaw's `serve` command, which launches a lightweight API-only server without the dashboard UI. Covers endpoint availability, auth middleware behavior, CLI argument parsing, and a regression suite proving that sockets used for local IP detection are always closed — even when errors occur during detection.
- [AudienceResolver Behavioral Tests: Fan-Out Logic, Caching, and Edge Cases](./audienceresolver-behavioral-tests-fan-out-logic-caching-and-edge-cases.md) — These tests verify the complete behavioral contract of AudienceResolver — determining which user IDs receive each event type, including removed-member inclusion, invite routing to admins plus invitee, TTL caching with manual invalidation, session deduplication, and empty-list fallback for unknown event types.
- [AudienceResolver Exhaustive Coverage: All Event Subclasses Must Resolve and Have Unique Wire Types](./audienceresolver-exhaustive-coverage-all-event-subclasses-must-resolve-and-have-.md) — These tests use Python's inspect module to enumerate every Event subclass at runtime and verify AudienceResolver handles each without raising. A companion test enforces that no two event classes share the same EVENT_TYPE wire string, preventing silent routing collisions when new event types are added.
- [Audit FTS Security Tests: SQL Injection Prevention and LIKE Wildcard Escaping](./audit-fts-security-tests-sql-injection-prevention-and-like-wildcard-escaping.md) — Security regression tests proving that the `q` parameter on PocketPaw's `/runtime/audit` search endpoint is fully parameter-bound and cannot corrupt or drop the `audit_log` table, and that LIKE special characters (`%`, `_`) in the query are correctly escaped to prevent wildcard-widened result sets. Tests correspond to Cluster C PR4.
- [Audit Logging Completeness Tests: Append-Only Guarantee, Rotation, Failure Resilience, and PII Masking](./audit-logging-completeness-tests-append-only-guarantee-rotation-failure-resilien.md) — Layer 7 security tests verifying that PocketPaw's audit logger captures all security-relevant events, maintains an append-only JSONL log that cannot be deleted through the logger interface, archives rather than deletes on rotation, survives write failures without crashing, and masks PII (e.g., SSNs) before writing to disk.
- [Audit Logging Verification Script — Manual Smoke Test for Tool Execution Audit Trail](./audit-logging-verification-script-manual-smoke-test-for-tool-execution-audit-tra.md) — This standalone script provides a manual, human-readable verification that the audit logging pipeline records both `attempt` and `success` entries when a tool is executed through the ToolRegistry. It runs outside the pytest harness and is designed to be invoked directly to confirm audit infrastructure is wired correctly in a live environment.
- [Audit Store Tests: Model Validation, Filtering, Export, and API Endpoints](./audit-store-tests-model-validation-filtering-export-and-api-endpoints.md) — Comprehensive tests for PocketPaw's `AuditStore` — the SQLite-backed audit trail that records all security-relevant operations. Tests cover the `AuditEntry` model, log persistence, multi-field query filtering, date range filtering, CSV and JSON export, REST API endpoints, and integration helpers for tool execution and connector sync logging.
- [Auth API Tests: Session Exchange, Cookie Login, Rate Limiting, and QR Code](./auth-api-tests-session-exchange-cookie-login-rate-limiting-and-qr-code.md) — This test module comprehensively covers PocketPaw's `/api/v1/auth` router, verifying the master-token-to-session exchange, cookie-based login with correct Secure flag handling under reverse proxies, logout, token regeneration, QR code generation, and IP-based rate limiting on all three auth endpoints.
- [Auth Domain Schema Tests](./auth-domain-schema-tests.md) — This module tests the Pydantic schemas for the auth domain — `ProfileUpdateRequest`, `SetWorkspaceRequest`, and `UserResponse` — verifying optional field defaults, value round-trips, and the structure of the user identity response.
- [Auth Exception Logging Tests: Silent Swallow Prevention in Auth Dispatch and OAuth2](./auth-exception-logging-tests-silent-swallow-prevention-in-auth-dispatch-and-oaut.md) — Regression tests for issue #627 — verifies that bare `except Exception: pass` blocks in authentication dispatch paths have been replaced with `logger.warning(..., exc_info=True)` calls. Tests cover API key validation, OAuth2 token validation, cookie login branches, and OAuth2 server audit log failures, ensuring auth failures are always logged and always return 401.
- [Automation Evaluator: Rule-to-Daemon Sync, Lifecycle, Cooldown, Fire Modes, and Router Integration Tests](./automation-evaluator-rule-to-daemon-sync-lifecycle-cooldown-fire-modes-and-route.md) — Tests for the `AutomationEvaluator` — the runtime that translates `AutomationStore` rules into daemon intentions, evaluates which rules should fire, and dispatches actions. Covers the full evaluator lifecycle, cooldown enforcement, three execution modes (require approval, auto-execute, notify-only), daemon sync/unsync, and the router bridge integration.
- [Backends API Tests: Listing, Availability Checks, and Safe Install Subprocess Handling](./backends-api-tests-listing-availability-checks-and-safe-install-subprocess-handl.md) — This test file covers PocketPaw's `/api/v1/backends` router, verifying that the backend registry is exposed correctly, that the `_check_available` helper correctly detects missing imports, and that the async install endpoint handles subprocess timeouts and stderr secret redaction without leaking sensitive data.
- [Bootstrap System Tests: System Prompt Assembly, Identity File Caching, and Channel Hints](./bootstrap-system-tests-system-prompt-assembly-identity-file-caching-and-channel-.md) — This test suite validates PocketPaw's bootstrap pipeline, which reads identity configuration files (IDENTITY.md, SOUL.md, STYLE.md, USER.md) and assembles them into a structured system prompt for the AI agent. Tests cover the XML-tag layout contract of `BootstrapContext`, the mtime-based file cache in `DefaultBootstrapProvider`, and the channel-specific formatting hints added by `AgentContextBuilder`.
- [Brew & Co. Business Scenario: End-to-End Fabric and Instinct Pipeline Walkthrough](./brew-co-business-scenario-end-to-end-fabric-and-instinct-pipeline-walkthrough.md) — A narrative-driven end-to-end test that simulates a real small business — a coffee shop on Monday morning — using PocketPaw's `FabricStore` for business objects and `InstinctStore` for the AI decision pipeline. It demonstrates the full operational flow: data modeling, connector sync simulation, threshold-triggered automation, human approval, execution, and audit export.
- [Broadcast Reminder Persistence Tests: Session History Durability Across WebSocket Connections](./broadcast-reminder-persistence-tests-session-history-durability-across-websocket.md) — This test suite verifies the fix for GitHub issue #364, where reminder messages fired by the scheduler were broadcast over WebSocket but never saved to session history, causing them to disappear after a tab switch or page reload. Tests confirm that `broadcast_reminder()` persists each reminder to every active WebSocket session's history and that a persistence failure does not block the broadcast.
- [Browser Automation Test Package Initializer](./browser-automation-test-package-initializer.md) — This is the `__init__.py` for the `tests/test_browser/` package, marking the browser automation tests as a proper Python package so pytest can discover and import them as a cohesive module group. The file contains no code or fixtures — its presence enables relative imports between the four browser test modules.
- [Browser Session Management Tests: Lifecycle, Idle Cleanup, Singleton, and Concurrency](./browser-session-management-tests-lifecycle-idle-cleanup-singleton-and-concurrenc.md) — This test suite validates `BrowserSessionManager`, which maintains a pool of named `BrowserSession` objects backed by individual `BrowserDriver` instances. Tests cover session creation, reuse, idle timeout cleanup, graceful close, the global singleton accessor, and thread-safety under concurrent `get_or_create` calls.
- [Browser Snapshot Generator Tests: Accessibility Tree to Agent-Readable Text Conversion](./browser-snapshot-generator-tests-accessibility-tree-to-agent-readable-text-conve.md) — This test suite validates `SnapshotGenerator`, which converts Playwright's accessibility tree JSON into a compact, ref-tagged text representation that AI agents can read and reason about. Tests cover the full rendering pipeline: interactive element ref assignment, non-interactive element handling, heading/list/image rendering, hidden element filtering, password field masking, and the `RefMap` reference number system.
- [BrowserDriver Tests: Playwright Wrapper for Navigation, Interaction, and Lifecycle Management](./browserdriver-tests-playwright-wrapper-for-navigation-interaction-and-lifecycle-.md) — This test suite validates `BrowserDriver`, PocketPaw's thin wrapper around the Playwright async API. Tests cover initialization modes (headless/headful), async context manager lifecycle, browser launch and viewport configuration, all navigation and interaction operations (navigate, click, type, scroll), snapshot generation, screenshot capture, and defensive checks for using the driver before it's launched.
- [BrowserTool Integration Tests: Agent-Facing Browser Interface, Actions, Error Handling, and Session Management](./browsertool-integration-tests-agent-facing-browser-interface-actions-error-handl.md) — This test suite validates `BrowserTool`, the agent-facing wrapper that exposes browser automation as a single tool with an action-dispatch interface. Tests cover the tool's metadata contract (name, trust level, JSON schema), all seven supported actions (navigate, click, type, scroll, snapshot, screenshot, close), input validation, error propagation, and multi-session isolation.
- [Channel Autostart Tests: Per-Channel Boot Control, Dashboard Startup, API Integration, and Config Persistence](./channel-autostart-tests-per-channel-boot-control-dashboard-startup-api-integrati.md) — This test suite validates PocketPaw's per-channel autostart feature, which lets operators control which channel adapters automatically start when the dashboard boots. Tests cover the `_channel_autostart_enabled()` helper, startup behavior with mixed enabled/disabled channels, the REST API endpoints for reading and saving autostart config, and round-trip persistence to disk.
- [Channel-Aware Markdown Formatting Tests](./channel-aware-markdown-formatting-tests.md) — This test module validates `convert_markdown`, PocketPaw's channel-aware formatter that translates LLM-generated markdown into the native formatting of each messaging platform. Each channel has its own rendering rules — WhatsApp uses `*bold*`, Slack uses `<url|label>` hyperlinks, Signal strips all formatting — and the tests prevent these channel-specific transformations from regressing.
- [Channel Notifier Tests: Multi-Target Message Dispatch, Validation, and Settings-Driven Defaults](./channel-notifier-tests-multi-target-message-dispatch-validation-and-settings-dri.md) — This test suite validates `notify()`, PocketPaw's high-level function for pushing outbound messages to one or more channels. Tests cover multi-target fan-out, graceful handling of malformed target strings, unknown channel names, empty target lists, settings-driven default targets, chat IDs containing colons, and delivery across all supported channels.
- [Channels API Tests: Status, Config Save, Toggle, and Extras Check](./channels-api-tests-status-config-save-toggle-and-extras-check.md) — This test file covers PocketPaw's `/api/v1/channels` router, which exposes status, configuration, and lifecycle management for messaging channel adapters such as Discord, Slack, WhatsApp, Telegram, Signal, Matrix, Teams, and Google Chat. It also covers `/extras/check`, the endpoint that detects whether optional channel dependencies are installed.
- [Chat API Test Suite: SSE Streaming, Session Bridge, and Request Aliases](./chat-api-test-suite-sse-streaming-session-bridge-and-request-aliases.md) — Covers the full test surface of PocketPaw's `/api/v1/chat` router, including the `_APISessionBridge` async queue, SSE stream/stop endpoints, non-streaming send, media URL rewriting, and camelCase field alias handling. These tests exist to catch silent regressions in the real-time chat path that would break frontend consumers without an obvious error.
- [Chat Domain Schema Validation Tests](./chat-domain-schema-validation-tests.md) — This module comprehensively tests all Pydantic schemas in the chat domain — group management, message sending, editing, reactions, WebSocket inbound/outbound messages, cursor pagination, and attachment handling — covering field defaults, length constraints, enum validation, and discriminated union types for WebSocket message dispatch.
- [ChromaAdapter: Vector Database Integration for Semantic Memory Storage](./chromaadapter-vector-database-integration-for-semantic-memory-storage.md) — The `ChromaAdapter` wraps ChromaDB to provide async add, search, get-by-ID, delete, and upsert operations for PocketPaw's semantic memory tier. Tests are conditionally skipped when `chromadb` is not installed, and cover the core CRUD operations, graceful handling of small collections during search, upsert (duplicate ID update), and optional metadata storage.
- [ClaudeAgentSDK Dispatch and Persistent Client Tests](./claudeagentsdk-dispatch-and-persistent-client-tests.md) — This module tests the dispatch logic for `ClaudeAgentSDK`, documenting the removal of the `_fast_chat` direct-API bypass in version 0.4.16 and the introduction of a persistent `ClaudeSDKClient` that reuses connections across calls. It verifies that all task complexities (SIMPLE, MODERATE, routing disabled) flow through the CLI path and validates the client lifecycle: creation, reuse, reconnection on model change, fallback on failure, interrupt, and cleanup.
- [Cloud Chat Test Fixtures: In-Memory MongoDB via mongomock-motor](./cloud-chat-test-fixtures-in-memory-mongodb-via-mongomock-motor.md) — The `beanie_memory_db` fixture initialises Beanie against an isolated in-memory MongoDB database for each test using `mongomock-motor`, enabling the full cloud chat test suite to run in CI without a real MongoDB service. It includes a compatibility shim that silences unsupported keyword arguments in mongomock-motor's `list_collection_names` stub.
- [Cloud Chat Test Sub-Package Initialiser](./cloud-chat-test-sub-package-initialiser.md) — The empty `tests/cloud/chat/__init__.py` marks the `tests/cloud/chat/` directory as a Python package so pytest can discover chat-specific cloud tests as `tests.cloud.chat.*` and so that the `conftest.py` in the same directory can provide fixtures to the full chat test sub-package.
- [Cloud Context Threading in Chat API: User ID and Workspace Propagation Tests](./cloud-context-threading-in-chat-api-user-id-and-workspace-propagation-tests.md) — Tests that `resolve_cloud_context` correctly injects `cloud_user_id` and `cloud_workspace_id` into `InboundMessage.metadata` when an authenticated cloud user is present, and leaves those keys absent for anonymous or unauthenticated requests. The test file operates at the unit level — directly calling `_build_inbound_message` and `_send_message` — to avoid the overhead of standing up a full SSE streaming client.
- [Cloud Error Hierarchy: Structured HTTP Error Contract Tests](./cloud-error-hierarchy-structured-http-error-contract-tests.md) — This test file validates the `ee.cloud.shared.errors` module, which provides a typed exception hierarchy that maps Python exceptions to HTTP status codes and machine-readable error codes. It ensures that every error class produces the correct status, code, message, and serialization shape expected by API consumers.
- [Cloud Mount Integration Smoke Tests: Route and Error Handler Registration](./cloud-mount-integration-smoke-tests-route-and-error-handler-registration.md) — This smoke test suite verifies that `mount_cloud()` successfully registers all expected domain route groups (auth, workspaces, agents, chat, pockets, sessions, WebSocket, license) and the `CloudError` exception handler on a fresh FastAPI app. It acts as a fast regression guard ensuring no route group is accidentally omitted from the mount function.
- [Cloud Pydantic Model Tests: Field Defaults and Schema Validation Without a Database](./cloud-pydantic-model-tests-field-defaults-and-schema-validation-without-a-databa.md) — This test file verifies field-level Pydantic model behavior for core cloud entities (Group, Message, Pocket, Invite, Workspace, Session, Notification) using `model_construct()` to bypass Beanie's MongoDB-dependent `__init__`. It pins default values, optional field nullability, and enum acceptance for fields added in recent schema migrations.
- [Cloud Test Sub-Package Initialiser](./cloud-test-sub-package-initialiser.md) — The empty `tests/cloud/__init__.py` marks the `tests/cloud/` directory as a Python package, enabling pytest to discover cloud-related tests as part of the `tests.cloud` package namespace and allowing conftest fixtures defined at higher levels to propagate correctly into cloud test files.
- [Cloud Tests Shared Fixture: No-Op Realtime Bus](./cloud-tests-shared-fixture-no-op-realtime-bus.md) — This conftest.py installs an autouse fixture that replaces the realtime event bus with a no-op AsyncMock for the duration of every cloud test. This prevents AssertionError crashes when services call emit() in tests that do not start the full application lifecycle.
- [Codex CLI Backend Tests: Subprocess-Based Agent with Streaming JSON Event Parsing](./codex-cli-backend-tests-subprocess-based-agent-with-streaming-json-event-parsing.md) — This test suite validates `CodexCLIBackend`, PocketPaw's adapter for the OpenAI Codex CLI tool. The backend runs the CLI as a subprocess and parses its streaming JSON event output to extract agent messages, tool calls, file changes, web searches, MCP calls, reasoning steps, and usage statistics, all without requiring the real CLI binary to be installed.
- [Comprehensive GuardianAgent Security Filter Tests](./comprehensive-guardianagent-security-filter-tests.md) — This module provides exhaustive coverage of `GuardianAgent` — PocketPaw's Layer 6 AI safety filter — including LLM-based classification, fail-closed behavior on all error conditions, local regex fallback when no API key is present, audit logging for every code path, concurrent safety, singleton correctness, and prompt-injection hardening (issue #873).
- [Config Validation Tests: API Key Formats, Numeric Constraints, and Enum Fields](./config-validation-tests-api-key-formats-numeric-constraints-and-enum-fields.md) — This test module covers three tiers of Settings validation: the `validate_api_key()` function that checks provider-specific key prefixes and warns on mismatches, Pydantic `gt`/`ge` constraints on numeric fields that prevent nonsensical configurations, and `Literal`-typed enum fields for provider selection. Together they ensure that misconfigured credentials and out-of-range settings fail loudly at startup rather than silently at runtime.
- [Connector Protocol: YAML Parsing, Registry, and DirectREST Adapter Lifecycle Tests](./connector-protocol-yaml-parsing-registry-and-directrest-adapter-lifecycle-tests.md) — This module tests the PocketPaw connector infrastructure — YAML definition parsing, the `ConnectorRegistry` for connector discovery and lifecycle management, and the `DirectRESTAdapter` for connecting, executing actions, and disconnecting from external REST APIs like Stripe.
- [Connector Status Route Tests: State Transitions, Pocket Isolation, and Secret Non-Leakage](./connector-status-route-tests-state-transitions-pocket-isolation-and-secret-non-l.md) — This test file locks down PocketPaw's connector status route contract — the full state machine from disconnected through connected to expired, strict per-pocket credential isolation so one pocket cannot see another's connection state, and the guarantee that OAuth secrets passed at connect time never appear in status responses.
- [Connector-to-Fabric Pipeline: Stripe Integration, Threshold Rules, and Instinct Proposal Chain Tests](./connector-to-fabric-pipeline-stripe-integration-threshold-rules-and-instinct-pro.md) — End-to-end tests for the data ingestion pipeline that runs from a YAML-defined connector (Stripe) through HTTP execution, Fabric object creation, threshold evaluation, and Instinct action proposal — all without real network calls via patched `httpx`. Covers the full chain including error paths and source deduplication behavior.
- [Connectors Test Package Initializer](./connectors-test-package-initializer.md) — This `__init__.py` marks `tests/connectors/` as a Python package and documents its scope as the test home for `pocketpaw.connectors.*` adapter implementations. Its single comment line serves as a breadcrumb for developers adding new connector tests.
- [Context Window Budget Tests: Priority-Based Block Assembly and KB Integration](./context-window-budget-tests-priority-based-block-assembly-and-kb-integration.md) — This test module, added in April 2026, validates `AgentContextBuilder._assemble_with_budget`, which assembles the agent's system prompt from priority-ranked blocks while respecting both a global character budget and per-block caps. It also tests the knowledge-base context injection path that calls the `kb-go` CLI subprocess to fetch relevant articles.
- [Copilot SDK Backend Tests: Capabilities, Provider Config, Session Management, and Tool Injection](./copilot-sdk-backend-tests-capabilities-provider-config-session-management-and-to.md) — This test module validates the `CopilotSDKBackend` — PocketPaw's adapter for GitHub Copilot's agent SDK — entirely through mocks, requiring no real CLI binary or SDK installation. It covers static capability metadata, provider-specific configuration dispatch, multi-turn session reuse, event stream mapping, and the injection of PocketPaw tool instructions into the Copilot prompt.
- [Core PocketPaw Tools: Status, Fetch, Screenshot, Config, and Router Tests](./core-pocketpaw-tools-status-fetch-screenshot-config-and-router-tests.md) — This test module provides broad coverage of PocketPaw's foundational tool and configuration layer, including the system status tool, the file fetch tool's path jail enforcement, the screenshot tool's optional dependency handling, the settings save/load lifecycle, and both the LLM and agent routers' initialization and error paths.
- [Correction Soul Bridge: Observe Callbacks and Procedural Memory Promotion Tests](./correction-soul-bridge-observe-callbacks-and-procedural-memory-promotion-tests.md) — This test suite covers the `CorrectionSoulBridge` — the enterprise component that writes user corrections into the Soul Protocol's memory system and promotes recurring correction patterns into procedural memory. It also validates the `InstinctCorrectionsTool`, the agent-facing interface that exposes captured corrections as readable context.
- [CreateSkillTool Tests — Skill Generation, Naming Validation, and YAML Frontmatter](./createskilltool-tests-skill-generation-naming-validation-and-yaml-frontmatter.md) — This test file validates `CreateSkillTool`, which allows agents to generate new PocketPaw skill files (SKILL.md) at runtime. Tests cover tool metadata, skill name regex validation, YAML frontmatter format, overwrite protection, skill loader reload triggering, and the allowed-tools and user-invocable flags.
- [Credential Store Tests: Fernet Encryption, Config Separation, Migration, and Log Scrubbing](./credential-store-tests-fernet-encryption-config-separation-migration-and-log-scr.md) — This test module validates PocketPaw's `CredentialStore` — the Fernet-encrypted store that keeps API keys off disk in plaintext — along with the separation of secrets from non-secret config, a one-time migration from legacy plaintext `config.json`, strict Unix file permissions, and a `SecretFilter` that scrubs API keys from log output.
- [Credential Store v2 Tests: AES-256-GCM, Argon2id, AEAD, Migration, and Platform Fallbacks](./credential-store-v2-tests-aes-256-gcm-argon2id-aead-migration-and-platform-fallb.md) — This test module validates the v2 upgrade of PocketPaw's `CredentialStore`, which replaces Fernet (PBKDF2-based) encryption with AES-256-GCM authenticated encryption using Argon2id key derivation. It covers the full v2 round-trip, authenticated data enforcement, automatic v1-to-v2 migration with backup, corruption recovery, and cross-platform hardware UUID fallbacks used to derive the encryption key.
- [Critical Gap Coverage: Real HTTP in DirectRESTAdapter and Agent Tools for Fabric and Instinct](./critical-gap-coverage-real-http-in-directrestadapter-and-agent-tools-for-fabric-.md) — This test file was written specifically to close gaps in test coverage identified during a gap analysis — areas where code existed but no test exercised it end-to-end. It covers real HTTP dispatch through `DirectRESTAdapter`, the Fabric agent tools (`FabricQueryTool`, `FabricCreateTool`), and the Instinct agent tools (`InstinctProposeTool`, `InstinctPendingTool`, `InstinctAuditTool`).
- [Critical Gaps Tests: Real HTTP in YAML Connectors, Fabric Queries, and Instinct Tool Operations](./critical-gaps-tests-real-http-in-yaml-connectors-fabric-queries-and-instinct-too.md) — This test module, created in March 2026, addresses three previously untested integration paths: the `DirectRESTAdapter` making authenticated HTTP calls to YAML-defined connectors like Stripe, the Fabric knowledge-graph query tools, and the Instinct action-proposal tools. It uses real YAML connector definitions from the connectors directory and mocks only the network layer.
- [Cross-Channel Command Handler Tests: Command Parsing, Session Aliases, and Concurrent Write Safety](./cross-channel-command-handler-tests-command-parsing-session-aliases-and-concurre.md) — This test suite validates PocketPaw's slash-command system and session alias manager, which allow users on any channel (Discord, Telegram, WhatsApp, CLI) to issue commands like `/new`, `/sessions`, `/resume`, and `/help`. Tests cover command recognition, alias CRUD operations, concurrent alias write safety, and the full command execution flow.
- [Daily Notes Isolation Tests: Per-User Scoping and Backward-Compatible Legacy Note Visibility](./daily-notes-isolation-tests-per-user-scoping-and-backward-compatible-legacy-note.md) — This test file, added during security sprint cluster D (issue #887), validates that daily notes in the `MemoryManager` are scoped to the `sender_id` that created them, preventing one user's private notes from leaking into another user's agent context. It also tests the backward-compatibility guarantee that legacy notes written before the fix remain visible as system-wide entries.
- [Dashboard Auth Cookie Tests: Secure Flag Behavior Under HTTP and HTTPS Proxies](./dashboard-auth-cookie-tests-secure-flag-behavior-under-http-and-https-proxies.md) — This test file validates that the dashboard's session cookie is set without the `Secure` flag in plain HTTP environments (to avoid breaking self-hosted HTTP deployments) and with the `Secure` flag when the request arrives via an HTTPS reverse proxy detected by the `X-Forwarded-Proto: https` header.
- [Dashboard E2E Tests: UI Load, Deep Work View, Agent/Task CRUD, and Navigation](./dashboard-e2e-tests-ui-load-deep-work-view-agenttask-crud-and-navigation.md) — This Playwright-based end-to-end test suite verifies that the PocketPaw dashboard loads correctly in a real browser, that key UI elements (chat view, Deep Work view, agent/task creation modals, sidebar, remote access) are present and functional, and that the dashboard title matches the expected beta branding.
- [Dashboard Security Tests: Rate Limiting, Session Tokens, Tunnel Auth Bypass, Security Headers, and WebSocket Auth](./dashboard-security-tests-rate-limiting-session-tokens-tunnel-auth-bypass-securit.md) — This comprehensive test module validates PocketPaw's dashboard security hardening across six dimensions: token-bucket rate limiting, HMAC-signed session tokens with TTL, the localhost tunnel auth bypass fix, security headers, CORS rejection, and WebSocket authentication before the upgrade handshake completes.
- [Dashboard Starlette Compatibility Regression Test: Index Route on Starlette 1.0.0](./dashboard-starlette-compatibility-regression-test-index-route-on-starlette-100.md) — This short regression test ensures the dashboard's root route returns HTTP 200 with `text/html` content, specifically guarding against a breaking change introduced in Starlette 1.0.0 that changed the `TemplateResponse` constructor signature. The Starlette version is logged in assertion failure messages to make version-related CI failures immediately diagnosable.
- [Decision Trace Collector: Lifecycle, Event Aggregation, Deduplication, and Snapshot Persistence Tests](./decision-trace-collector-lifecycle-event-aggregation-deduplication-and-snapshot-.md) — This suite tests the `TraceCollector` context manager that records which Fabric objects, soul memories, KB articles, and tool calls an agent consulted while generating a decision. It also verifies the `FabricObjectSnapshot` persistence layer that captures point-in-time object state alongside each audit entry.
- [Decision Trace Wiring: Store-Level and Router-Level Integration Tests for Trace Persistence and Hydration](./decision-trace-wiring-store-level-and-router-level-integration-tests-for-trace-p.md) — Integration tests that verify `ReasoningTrace` objects are persisted into audit context when `propose()` is called, that `FabricObjectSnapshot` records are keyed to the correct audit row, and that the `/instinct/audit/{id}` hydration endpoint expands trace IDs into full records at the correct depth level.
- [Deep Agents Backend Tests: LangChain Integration, Provider Parsing, Content Extraction, and Registry](./deep-agents-backend-tests-langchain-integration-provider-parsing-content-extract.md) — This test module validates the `DeepAgentsBackend` — PocketPaw's LangChain-based agent backend — through mocks, requiring no real LangChain or LangGraph installation. It covers static metadata, the `provider:model` string parsing convention, LangGraph `Overwrite` object unwrapping, content extraction from mixed message formats, graceful degradation when the SDK is absent, and backend registry integration.
- [Deep Agents Tool Bridge: LangChain Integration and Graceful Degradation](./deep-agents-tool-bridge-langchain-integration-and-graceful-degradation.md) — These tests cover the `build_deep_agents_tools` bridge function which adapts PocketPaw tools for LangChain-based Deep Agents backends. The key behavior tested is that the function returns an empty list gracefully when `langchain-core` is not installed, and that the Deep Agents backend is not subject to the same tool exclusions as the Claude Agent SDK.
- [Deep Work Dependency Scheduler: DAG Validation, Task Readiness, and Dispatch Tests](./deep-work-dependency-scheduler-dag-validation-task-readiness-and-dispatch-tests.md) — This suite validates the DependencyScheduler, the core orchestration engine that determines which tasks are ready to run based on dependency graphs, dispatches agent and human tasks to the correct handlers, and detects project completion. It also tests DAG validation logic that prevents cyclic dependencies and missing references from corrupting project execution.
- [Deep Work E2E: Project Lifecycle, Output Chaining, Auto-Retry, Timeout, and Cancellation](./deep-work-e2e-project-lifecycle-output-chaining-auto-retry-timeout-and-cancellat.md) — This test suite exercises PocketPaw's Deep Work (multi-agent task orchestration) system end-to-end through the FastAPI test client, covering project approval, task dependency cascades, output persistence, auto-retry on failure, timeout triggers, project cancellation in all lifecycle states, and the manual retry and skip APIs.
- [Deep Work Goal Parser: GoalAnalysis Validation and Parsing Tests](./deep-work-goal-parser-goalanalysis-validation-and-parsing-tests.md) — This test suite covers the GoalParser and GoalAnalysis components in PocketPaw's Deep Work system, validating JSON parsing, domain/complexity/research-depth validation, field clamping, and serialization round-trips. It ensures robust defensive behavior when LLM-generated goal payloads contain invalid or out-of-range values.
- [Deep Work Human Task Router: Notification Broadcast Tests](./deep-work-human-task-router-notification-broadcast-tests.md) — This test suite validates the HumanTaskRouter, which bridges the automated Deep Work engine and human operators by publishing structured notifications over the MessageBus. It covers task notifications, review routing, plan-ready announcements, and project completion summaries, including edge cases like missing project IDs and empty task lists.
- [Deep Work Models and Store: Project CRUD and Model Serialization Tests](./deep-work-models-and-store-project-crud-and-model-serialization-tests.md) — This suite tests the Deep Work data models (Project, TaskSpec, AgentSpec, PlannerResult) and the FileMissionControlStore's project CRUD operations, ensuring correct defaults, serialization round-trips, persistence across store reinitializations, status filtering, and safe handling of missing records.
- [Deep Work Plan API: Execution Levels and Task Level Map Tests](./deep-work-plan-api-execution-levels-and-task-level-map-tests.md) — This suite tests the GET /projects/{id}/plan endpoint, which returns a project's dependency graph as parallel execution levels — groups of tasks that can run concurrently because their upstream dependencies are satisfied. It covers linear chains, diamond-shaped dependency graphs, and fully independent task sets using a real FastAPI TestClient with an in-memory store.
- [Deep Work Planner: Prompt Templates, JSON Parsing, and LLM Error Handling Tests](./deep-work-planner-prompt-templates-json-parsing-and-llm-error-handling-tests.md) — This suite tests PocketPaw's PlannerAgent, covering prompt template validation, robust JSON parsing of LLM responses (plain and code-fenced), PlannerResult construction, and a regression test for silent error swallowing when the LLM returns only error events. It documents the four-phase planning flow: research, PRD generation, task breakdown, and team assembly.
- [Deep Work Session: End-to-End Orchestration and Lifecycle Tests](./deep-work-session-end-to-end-orchestration-and-lifecycle-tests.md) — This integration test suite covers the DeepWorkSession, the top-level orchestrator that coordinates planning, task materialization, agent assignment, approval gating, pause/resume, and cancellation for multi-task AI projects. It verifies the full session lifecycle from DRAFT through PLANNING to AWAITING_APPROVAL and beyond, including edge cases like cyclic dependencies, empty plans, and planner exceptions.
- [Deep Work Task Model Extensions: New Field Tests](./deep-work-task-model-extensions-new-field-tests.md) — This suite tests the Deep Work extensions added to the core Task model in PocketPaw's mission control system, specifically the five new fields—project_id, task_type, blocks, active_description, and estimated_minutes—that enable multi-task project management. It verifies defaults, settability, serialization, backward compatibility, and round-trip fidelity.
- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.
- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.
- [DirectoryTreeTool: File System Tree Visualization with Jail, Depth, and Security Controls](./directorytreetool-file-system-tree-visualization-with-jail-depth-and-security-co.md) — The `DirectoryTreeTool` renders an ASCII directory tree within a file jail, with configurable depth, hidden file visibility, file size display, and entry count truncation. Tests cover basic tree rendering, depth limiting, hidden file exclusion/inclusion, size annotation, edge cases (empty dir, nonexistent path, file-not-directory), security (jail break, prefix bypass, symlink skipping), truncation, and the tool's schema definition.
- [Discord Channel Adapter: Message Routing, Conversation Mode, and Security Filter Tests](./discord-channel-adapter-message-routing-conversation-mode-and-security-filter-te.md) — This suite tests PocketPaw's DiscliAdapter, which connects the agent runtime to Discord via the discli CLI tool. It covers channel property defaults, status type validation, start/stop lifecycle, inbound message handling with guild/user allow-lists, conversation mode with history context, outbound message delivery, and the no-response marker that suppresses bot replies in monitored channels.
- [DoS Hardening: Rate Limiter TOCTOU Concurrency and ReDoS Regression Tests](./dos-hardening-rate-limiter-toctou-concurrency-and-redos-regression-tests.md) — This security-focused suite reproduces two DoS vulnerability classes found during a 2026-04 security sprint: a TOCTOU race condition in the rate limiter that allows more tokens than the bucket capacity under thread contention, and a ReDoS (Regular Expression Denial of Service) vulnerability where a pathological input causes catastrophic regex backtracking. Both are bug-reproduction tests written before the fixes.
- [Download URL Endpoint Tests: TTL Responses, Cross-Workspace Isolation, and Path Traversal Defense](./download-url-endpoint-tests-ttl-responses-cross-workspace-isolation-and-path-tra.md) — Integration tests for the `/uploads/{file_id}/download-url` endpoint added in the EE upload router, verifying that the endpoint returns a time-bounded URL with the original filename, blocks cross-workspace access with 404, rejects path-traversal IDs, and hides soft-deleted files. Uses the same FastAPI test-client pattern as `test_router.py` with header-driven identity injection.
- [E2E Security Tests: WebSocket Authentication Protocol and HTTP Security Headers](./e2e-security-tests-websocket-authentication-protocol-and-http-security-headers.md) — This Playwright-based suite verifies PocketPaw's WebSocket authentication model — that connections succeed from localhost without explicit auth, that the frontend sends an authenticate action as the first WebSocket message, that tokens never appear in WebSocket URLs, and that API endpoints correctly reject unauthenticated requests while static assets remain accessible.
- [E2E Test Configuration: Playwright Browser Setup and Dashboard Server Lifecycle](./e2e-test-configuration-playwright-browser-setup-and-dashboard-server-lifecycle.md) — This conftest manages the full lifecycle of PocketPaw's end-to-end test environment: it checks for Playwright browser installations and skips the entire e2e suite if they are missing, finds a free OS port, starts the dashboard server in a subprocess, waits for it to become ready, and exposes the base URL to all e2e tests through a session-scoped fixture.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.
- [EE Instinct: Human-in-the-Loop Action Approval System Tests](./ee-instinct-human-in-the-loop-action-approval-system-tests.md) — This test suite validates the full lifecycle of PocketPaw's Instinct subsystem, which gates AI-proposed actions behind human approval before execution. It covers store-layer CRUD, audit trail integrity, REST endpoint contracts, lifecycle state transitions, and edge cases such as approving non-existent actions.
- [EE Upload Resolver Tests: Workspace-Scoped Path Resolution, Isolation, and Soft-Delete Visibility](./ee-upload-resolver-tests-workspace-scoped-path-resolution-isolation-and-soft-del.md) — Tests for `EEUploadResolver` and `resolve_media_paths_scoped`, which translate `/api/v1/uploads/{id}` URLs in message content into local filesystem paths for downstream processing. Key invariants: workspace isolation (cross-workspace lookup returns `None`), non-upload URL pass-through, soft-delete invisibility, and missing-blob safety.
- [EE Upload Router Integration Tests: Roundtrip, Workspace Isolation, Owner-Only Access, and Bulk Partial Success](./ee-upload-router-integration-tests-roundtrip-workspace-isolation-owner-only-acce.md) — Integration tests for the EE uploads FastAPI router covering the full upload-download-delete cycle, cross-workspace 404 enforcement, owner-only read access within a workspace, and the partial-success bulk upload response that distinguishes accepted files from rejected MIME types.
- [EEUploadService Core Behavior: Workspace-Scoped Storage and Streaming Tests](./eeuploadservice-core-behavior-workspace-scoped-storage-and-streaming-tests.md) — This test suite verifies that EEUploadService correctly stores uploaded files under a workspace scope, enforces cross-workspace access boundaries, streams content back to authorized requesters, and handles bulk uploads atomically. The in-memory `_MemAdapter` stands in for real cloud storage so tests run without external dependencies.
- [EEUploadService Realtime Event Emission: FileReady and FileDeleted Tests](./eeuploadservice-realtime-event-emission-fileready-and-filedeleted-tests.md) — This suite verifies that `EEUploadService` fires `FileReady` events over the realtime bus when files are uploaded to a chat-scoped context, and `FileDeleted` events when those files are removed. It also verifies that neither event fires when the upload or file lacks a `chat_id`, preventing noise from workspace-level background operations.
- [emit() Facade Tests: Bus Delegation, Error Swallowing, and Initialization Guard](./emit-facade-tests-bus-delegation-error-swallowing-and-initialization-guard.md) — These tests verify the `emit()` top-level facade that service code calls to publish real-time events. The suite ensures emit delegates unchanged event objects to the active bus, swallows bus errors to protect callers from infrastructure failures, and raises AssertionError when called before the bus singleton is initialized.
- [End-to-End API Test Suite: Auth, Workspaces, Chat, Pockets, Sessions, and Agents](./end-to-end-api-test-suite-auth-workspaces-chat-pockets-sessions-and-agents.md) — A comprehensive end-to-end test suite that exercises the full PocketPaw cloud API surface through HTTP calls against a live FastAPI application. It covers user registration and JWT auth, workspace CRUD and invite flows, group chat with reactions/pins/search, pocket management with widgets and share links, session lifecycle tracking, and agent configuration.
- [Enterprise Automations Rule CRUD: Store and FastAPI Router Tests](./enterprise-automations-rule-crud-store-and-fastapi-router-tests.md) — Full test coverage for the enterprise `AutomationStore` and its FastAPI router, covering three rule types (threshold, schedule, data change), all CRUD operations, toggle/enable behavior, fire tracking, file persistence, and a complete lifecycle end-to-end test. Every test uses `tmp_path` isolation to avoid touching the user's real `~/.pocketpaw` configuration.
- [Enterprise Edition Test Fixtures: License, MongoDB, FastAPI App, S3 Mock, and User/Workspace Factories](./enterprise-edition-test-fixtures-license-mongodb-fastapi-app-s3-mock-and-userwor.md) — This conftest provides the full test infrastructure for PocketPaw's enterprise edition integration tests: HMAC-signed license key injection, isolated in-memory MongoDB via mongomock-motor, a mounted FastAPI app with mocked agent pool, an async HTTP client, callable factories for users and workspaces, and a session-scoped S3 mock via moto.
- [Enterprise Edition Test Package Initializer](./enterprise-edition-test-package-initializer.md) — This `__init__.py` marks `tests/ee/` as a Python package for pytest import resolution and documents in its header comment that it was introduced with the `feat/fleet-journal-emission` feature branch to enable fixture sharing across enterprise edition tests.
- [Enterprise Guard Layer: RBAC, ABAC, Plan Feature Gates, and FastAPI Dependency Tests](./enterprise-guard-layer-rbac-abac-plan-feature-gates-and-fastapi-dependency-tests.md) — Comprehensive tests for PocketPaw's enterprise authorization layer, covering role-based access control (RBAC) enums and guard functions, attribute-based access control (ABAC) policy evaluation, plan feature tables, action-to-role mapping, tool whitelists, and three FastAPI dependency guards (`require_role`, `require_plan_feature`, `require_policy`).
- [Event Model Structural Tests: Base Event Fields and Typed Subclass Wire Type Assignment](./event-model-structural-tests-base-event-fields-and-typed-subclass-wire-type-assi.md) — Two minimal tests verify the structural contract of the Event base model: that every Event has type, data, and ts fields; and that typed subclasses like GroupCreated automatically set the correct wire type string from EVENT_TYPE without the caller needing to pass it.
- [Events SSE Router Tests: Endpoint Registration and OpenAPI Spec Inclusion](./events-sse-router-tests-endpoint-registration-and-openapi-spec-inclusion.md) — Tests that PocketPaw's server-sent events (SSE) endpoint `/api/v1/events/stream` is correctly registered in the events router, present in the v1 router registry, accessible on the dashboard app, and included in the OpenAPI specification. These structural checks prevent the events channel from being silently dropped during refactors.
- [Extras Install and Channel Dependency Endpoint Tests](./extras-install-and-channel-dependency-endpoint-tests.md) — This test module covers the `GET /api/extras/check` and `POST /api/extras/install` dashboard endpoints that manage optional channel adapter dependencies. It validates the allowlist-based security model that prevents arbitrary package installation and tests the `missing_dep` fallback path when a channel adapter fails to start due to an ImportError.
- [Fabric Journal Store Tests: Scope Isolation, Pagination, Projection Rebuild, and Policy Correctness](./fabric-journal-store-tests-scope-isolation-pagination-projection-rebuild-and-pol.md) — This suite pins four invariants for PocketPaw's journal-backed Fabric object store: correct lifecycle (create/update/archive), scope-based access control that returns empty results rather than leaking existence, post-filter pagination totals, and deterministic projection rebuild from the event journal. It supersedes a previous implementation that leaked object counts across scope boundaries.
- [Fabric List Endpoints: Object and Link Filtering, Envelope Shape, and SQL Injection Safety Tests](./fabric-list-endpoints-object-and-link-filtering-envelope-shape-and-sql-injection.md) — Integration tests for the `/fabric/objects` and `/fabric/links` list endpoints added as part of the PocketDataPanel's Objects/Links sub-tab wiring. Covers store-level link filtering by type and source, HTTP envelope shape, type-based filtering, pagination parameter validation, and an explicit SQL injection safety test for the `link_type` parameter.
- [Fabric Store: Object Type CRUD, Object Lifecycle, Link Traversal, and Query Tests](./fabric-store-object-type-crud-object-lifecycle-link-traversal-and-query-tests.md) — Unit tests for the enterprise `FabricStore` — PocketPaw's ontology-based business object store backed by SQLite. Covers defining and querying object types, creating and updating objects with source tracking, linking objects and traversing relationships, filtering queries, pagination, and cascade deletion.
- [Fetch Tool Path Security and Jail Boundary Tests](./fetch-tool-path-security-and-jail-boundary-tests.md) — This test module validates the security model of PocketPaw's file fetch tool, specifically the `is_safe_path` predicate and `handle_path`/`list_directory` handlers that enforce a configurable jail directory. The tests were created as regression guards for issue #619, where an empty path string could bypass the jail check by resolving to the current working directory.
- [File Browser Hidden File Ordering Bug Regression Tests](./file-browser-hidden-file-ordering-bug-regression-tests.md) — This test file reproduces and documents a bug in `handle_file_browse` where applying the `items[:50]` slice before the hidden-file filter caused all visible files to disappear from results when a directory contained 50 or more dot-prefixed items. The tests were created on 2026-02-12 to lock in the correct behavior — filter first, then slice.
- [FileMemoryStore Fixes: UUID Collision, Fuzzy Search, Deduplication, Persistent Delete, and Graph Tests](./filememorystore-fixes-uuid-collision-fuzzy-search-deduplication-persistent-delet.md) — This comprehensive test file covers a series of incremental fixes to `FileMemoryStore`, PocketPaw's markdown-based memory backend. It addresses UUID collisions, broken substring search, single-day file loading, lost deletions across restarts, and missing context limits — plus later phases covering vector semantic search, graph indexing, SVG escaping, and SQLite variable limit handling.
- [Files API Tests: Browse, Download, Zip, Write, and Path Traversal Defence](./files-api-tests-browse-download-zip-write-and-path-traversal-defence.md) — This test file covers PocketPaw's `/api/v1/files` router, which exposes local filesystem operations (browse, single-file download, directory zip download, and file write) to the dashboard. It places particular emphasis on path traversal prevention, file jail enforcement, and zip bomb mitigations.
- [Files Providers Test Sub-Package Marker](./files-providers-test-sub-package-marker.md) — An empty __init__.py that makes tests/cloud/files/providers/ a Python package, enabling dotted imports from parent test modules such as tests.cloud.files.test_provider_contract. Identical in purpose and content to the sibling files test package marker.
- [Files Router Bootstrap Integration Test](./files-router-bootstrap-integration-test.md) — This module contains a single integration test that verifies the files router can be mounted into a FastAPI application via build_files_router and that the /files/tree endpoint responds correctly with a 200 status and a JSON body containing the expected tree structure.
- [Files Router Security Tests: Scope Enforcement and Symlink Jail Traversal Prevention](./files-router-security-tests-scope-enforcement-and-symlink-jail-traversal-prevent.md) — Security regression tests for issues #884 and #886 in PocketPaw's files router: #884 proved any API key (even one with no scopes) could read arbitrary files; #886 proved `/files/download-zip` followed symlinks outside the configured file jail. These tests lock in the fixes and prevent both vulnerabilities from regressing.
- [Files Test Fixtures: FakeProvider, ctx, make_entry, make_mount](./files-test-fixtures-fakeprovider-ctx-makeentry-makemount.md) — This conftest.py provides the shared test infrastructure for the files subsystem: a FakeProvider that implements the full provider protocol in memory, plus factory fixtures for RequestContext, FileEntry, and ResolvedMount. All file provider tests build on these primitives.
- [Files Test Package Marker](./files-test-package-marker.md) — This is an empty __init__.py that marks tests/cloud/files/ as a Python package so pytest can discover and import tests within it. The SHA-256 hash e3b0c44298fc... is the well-known hash of an empty file.
- [Firebase Connector: YAML Definition, CLI Adapter, and Trust Level Tests](./firebase-connector-yaml-definition-cli-adapter-and-trust-level-tests.md) — This test suite validates PocketPaw's Firebase connector from three angles: the declarative YAML definition (action count, trust levels, auth method), the `FirebaseAdapter` class that wraps the Firebase CLI via async subprocess, and the connector registry integration. It uses mocked subprocess calls to test connect, execute, timeout, and error paths without touching a real Firebase project.
- [Fleet Installer Journal Emission Tests: Correlated Events, Backward Compatibility, and Partial Install](./fleet-installer-journal-emission-tests-correlated-events-backward-compatibility-.md) — This suite verifies that the fleet installer emits a correlated sequence of journal events (`fleet.install.started`, `agent.spawned` per soul, `fleet.installed` summary) when given a journal, remains silent when the journal is omitted, suppresses the terminal event on soul-creation failure, and falls back to a fleet-scoped tag when the fleet declares no scopes.
- [Fleet Installer: Manifest Loading, Path Traversal Guard, and Install Orchestration Tests](./fleet-installer-manifest-loading-path-traversal-guard-and-install-orchestration-.md) — This test suite covers PocketPaw's fleet installer, which provisions a full AI agent environment (soul, pocket, connectors) from a declarative manifest. It validates YAML and JSON manifest loading, a P0 path-traversal security fix that clamps string inputs to the bundled fleet directory, install orchestration with partial failure handling, and the InstallReport shape.
- [Fleet Router Test Suite: Template Listing, Install Auth, and Journal Integration](./fleet-router-test-suite-template-listing-install-auth-and-journal-integration.md) — Covers the FastAPI fleet REST router that powers the paw-enterprise InstallFleetPanel, verifying template enumeration, end-to-end install flows, journal event emission, and a layered auth guard (401/403/200) added when the P0 auth requirement landed. Uses FastAPI dependency_overrides throughout so tests remain hermetic — no real Mongo, no real soul-protocol runtime, no writes to ~/.soul/.
- [Folder CRUD Integration Tests: Auto-Chain Creation, Rename Propagation, and RBAC Enforcement](./folder-crud-integration-tests-auto-chain-creation-rename-propagation-and-rbac-en.md) — Integration tests for the EE uploads folder system covering folder creation with parent requirement, duplicate detection, automatic folder chain creation on upload, cascade rename of descendant paths, safe and cascading deletion, and permission enforcement distinguishing non-admin strangers from workspace admins.
- [Frontend JavaScript Syntax Validation Tests](./frontend-javascript-syntax-validation-tests.md) — This module validates that all JavaScript files in PocketPaw's frontend directory have syntactically correct code by running each file through `node --check`. It also verifies structural contracts — that `app.js` defines the Alpine.js `app()` function and that `websocket.js` references `WebSocket` — catching integration breakage before it reaches users.
- [Full Decision Loop: Fabric Threshold Detection to Instinct Audit Export Tests](./full-decision-loop-fabric-threshold-detection-to-instinct-audit-export-tests.md) — End-to-end tests for the complete decision loop from Fabric object creation through threshold rule evaluation to Instinct action proposal, approval, and JSON audit export — all using real store implementations with `tmp_path` SQLite databases. Three scenarios cover single trigger, multiple triggers, and audit trail identity.
- [GCP Connector: YAML Definition, Adapter Authentication, and Execute Path Tests](./gcp-connector-yaml-definition-adapter-authentication-and-execute-path-tests.md) — This test suite validates PocketPaw's Google Cloud Platform connector, covering the `gcp.yaml` declarative definition (20 actions across compute, storage, pub/sub, secrets, Cloud Run, and logging), the `GCPAdapter` lifecycle (connect, execute, disconnect), and registry wiring. All subprocess calls are mocked to avoid requiring live GCP credentials.
- [get_journal FastAPI Dependency: Caching, SOUL_DATA_DIR Override, and Cache Reset Tests](./getjournal-fastapi-dependency-caching-souldatadir-override-and-cache-reset-tests.md) — Tests the `get_journal` FastAPI dependency from `ee.journal_dep`, pinning three contracts that the rest of the `ee/` subsystem depends on: the dep returns a live Journal instance, repeated calls within one process return the same cached instance, and the `SOUL_DATA_DIR` environment variable is honoured at call time — not frozen at import. Also verifies `reset_journal_cache()` as the test-isolation escape hatch.
- [Gmail Integration Body Extraction and Tool Tests](./gmail-integration-body-extraction-and-tool-tests.md) — This test module validates `GmailClient._extract_body`, which parses Gmail's MIME structure to extract plain text from messages, and the three Gmail tools (`GmailSearchTool`, `GmailReadTool`, `GmailSendTool`) — checking their schemas and verifying that each returns a user-readable error when OAuth authentication is absent.
- [Google ADK Backend Tests](./google-adk-backend-tests.md) — This module provides fully mocked tests for `GoogleADKBackend`, PocketPaw's Google Agent Development Kit integration. It covers metadata/capability discovery, initialization with and without the SDK, event stream processing (text, function calls, function responses, errors), session management, MCP toolset policy enforcement, status reporting, and the backward-compatibility alias `GeminiCLIBackend`.
- [Google Calendar Tool Schema and Auth Error Tests](./google-calendar-tool-schema-and-auth-error-tests.md) — This test module validates the three Google Calendar tools (`CalendarListTool`, `CalendarCreateTool`, `CalendarPrepTool`) — checking their names, trust levels, and required parameter schemas — and verifies that each tool returns a user-readable error message rather than propagating an exception when OAuth authentication is unavailable.
- [Google Chat Channel Adapter Tests](./google-chat-channel-adapter-tests.md) — This test module provides full coverage for `GoogleChatAdapter`, which connects PocketPaw to Google Chat via webhook or Pub/Sub mode. It covers initialization, webhook message handling (including slash commands and space filtering), streaming message buffering, error recovery, `MessageBus` integration, and adapter lifecycle management.
- [Google Docs Integration Tool Schema and Execution Tests](./google-docs-integration-tool-schema-and-execution-tests.md) — This test module validates the three Google Docs tools (`DocsReadTool`, `DocsCreateTool`, `DocsSearchTool`), the `_parse_doc_id` URL-parsing utility, and the `DocsClient._extract_text` method that converts the Docs API's nested JSON structure into plain text. It also confirms that each tool returns actionable error messages when OAuth authentication is absent.
- [Google Drive Connector: DriveClient, SourceAdapter, and RetrievalRouter Integration Tests](./google-drive-connector-driveclient-sourceadapter-and-retrievalrouter-integration.md) — This test suite covers PocketPaw's Google Drive connector end-to-end: HTTP request plumbing with rate-limit backoff, point-in-time revision lookup, `DriveSourceAdapter.query` result shaping, token resolution precedence, and `RetrievalRouter` integration with journal emission. All Drive API traffic is eliminated by a scripted fake `httpx.Client`.
- [Google Drive Integration Tool Schema and Execution Tests](./google-drive-integration-tool-schema-and-execution-tests.md) — This test module validates the four Google Drive tools (`DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, `DriveShareTool`), their parameter schemas, and their behavior when authentication is absent or inputs are invalid. It also verifies `DriveListTool`'s happy path with a mocked HTTP response and confirms that `DriveShareTool` rejects invalid sharing roles at the tool layer.
- [GroupService Realtime Emit Tests](./groupservice-realtime-emit-tests.md) — This test module verifies that every public mutation in GroupService fires the correct realtime event via the bus emit() call. It also confirms that membership-changing operations correctly invalidate the AudienceResolver group cache so downstream subscribers see fresh data.
- [Guardian Agent Verification Script — Manual Safety Layer Smoke Test for ShellTool](./guardian-agent-verification-script-manual-safety-layer-smoke-test-for-shelltool.md) — This standalone script manually validates that the Guardian safety layer correctly permits safe shell commands while blocking dangerous ones, providing a human-readable pass/fail report for the tool permission model. It distinguishes between two blocking mechanisms — regex-based pre-screening in ShellTool and the Guardian agent's semantic analysis — to help developers diagnose which layer is active.
- [GuardianAgent Empty Response Regression Tests (Issue #636)](./guardianagent-empty-response-regression-tests-issue-636.md) — This test module documents the fix for issue #636, where an empty `response.content` list from the Anthropic API caused an `IndexError` inside `GuardianAgent.check_command`. The fix implements fail-closed behavior: any empty, malformed, or missing API response defaults to `DANGEROUS` rather than crashing or defaulting to safe.
- [GWS CLI System Prompt Injection Tests](./gws-cli-system-prompt-injection-tests.md) — This test suite verifies that the AgentContextBuilder conditionally injects Google Workspace CLI guidance into the system prompt only when the google-workspace MCP server is active and enabled. It also confirms that the gws.md guidance file exists on disk with the required content.
- [GWS Skills Auto-Install and GitHub Skill Installer Tests](./gws-skills-auto-install-and-github-skill-installer-tests.md) — This test suite covers `install_skills_from_github`, a shared async helper that clones a GitHub repository and copies SKILL.md-bearing subdirectories into the local skills install directory, with support for prefix filtering and single-skill targeting. It also tests that the GWS preset installation triggers a non-blocking background task that auto-installs Google Workspace skills.
- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.
- [Headless Permission Mode and Subprocess Tool Execution Tests](./headless-permission-mode-and-subprocess-tool-execution-tests.md) — This regression test suite guards against the permission hang bug introduced in commit 24f16e2, where gating `bypassPermissions` behind a user setting caused all Bash-based tool calls on messaging channels (Telegram, Discord, Slack) to hang indefinitely waiting for interactive approval. It also exercises the full subprocess path that the Claude SDK takes when executing memory tools.
- [Health API Tests: Status, Error Log, Audit Log, Security Audit, and Self-Audit Reports](./health-api-tests-status-error-log-audit-log-security-audit-and-self-audit-report.md) — This test file covers PocketPaw's `/api/v1/health` router and related audit endpoints, verifying health check aggregation, error log retrieval and clearance, manual health check triggering, structured audit log reading, security audit execution, and the self-audit report lifecycle.
- [Health Engine, Error Store, and Diagnostic Checks Test Suite](./health-engine-error-store-and-diagnostic-checks-test-suite.md) — This comprehensive test module covers the full health monitoring stack: `HealthCheckResult` data model, `ErrorStore` JSONL persistence with rotation, individual diagnostic checks (config, API keys, secrets, disk, LLM reachability), the `HealthEngine` orchestrator, playbook-driven diagnostics, agent tools (`HealthCheckTool`, `ErrorLogTool`, `ConfigDoctorTool`), and the ContextHub `health_status` integration.
- [Identity API GET and PUT Endpoint Tests](./identity-api-get-and-put-endpoint-tests.md) — This test module verifies the `/api/identity` REST endpoints that allow users to read and update the five identity files (identity, soul, style, instructions, user_profile) that shape the agent's persona. It also confirms that changes saved via the API are immediately reflected in the system prompt built by `AgentContextBuilder`.
- [Identity API Tests: Reading and Partially Updating Agent Identity Files](./identity-api-tests-reading-and-partially-updating-agent-identity-files.md) — This test file covers PocketPaw's `/api/v1/identity` router, which exposes the agent's persistent identity configuration — the markdown files that define personality, soul, style, instructions, and user profile — as a readable and partially updatable API resource.
- [ImageGenerateTool Unit Tests](./imagegeneratetool-unit-tests.md) — This test module verifies `ImageGenerateTool`, a built-in tool that generates images via the Google Gemini image generation API. It covers tool metadata (name, trust level, schema), graceful error handling for missing API keys and missing `google-genai` package, the generated directory creation logic, and the partial integration test structure for the image generation happy path.
- [IngestAdapter Protocol and IngestACL Contract Tests](./ingestadapter-protocol-and-ingestacl-contract-tests.md) — This test module pins the wire-shape contract for `IngestAdapter` — a superset of `ConnectorProtocol` that adds a `permissions()` method for per-record access control — and validates the `IngestACL` dataclass that carries scope, visibility, and principal metadata. It was created as part of Move 7 PR-A to establish the ACL-aware connector interface before the fleet template runtime (PR-B) consumed it.
- [Injection Scanner Bypass Resistance and Unicode Normalization Tests](./injection-scanner-bypass-resistance-and-unicode-normalization-tests.md) — This test module verifies that `InjectionScanner` is resistant to obfuscation techniques that attempt to evade heuristic pattern matching: fullwidth Unicode characters, zero-width characters, homoglyphs, and byte-order marks. It also tests that the deep scan LLM path is only triggered when the heuristic flags content, and that the `_normalize` method handles all Unicode edge cases correctly.
- [Injection Scanner Core Detection Tests](./injection-scanner-core-detection-tests.md) — This test suite validates `InjectionScanner`'s heuristic-based prompt injection detection across seven attack categories: instruction overrides, persona hijacks, delimiter attacks, data exfiltration commands, jailbreak attempts, tool abuse, and safe content. It also tests sanitization output wrapping and the singleton accessor pattern.
- [InProcessBus Tests: Fan-Out Correctness, Error Isolation, and Module Singleton](./inprocessbus-tests-fan-out-correctness-error-isolation-and-module-singleton.md) — These tests verify InProcessBus — the in-process real-time event bus — covering correct fan-out to all resolved recipients, the WsOutbound payload shape, per-recipient exception isolation so one dead socket does not stop others, graceful audience resolution error swallowing, and the module-level singleton get/set contract.
- [Installer Bootstrap Dependency Verification Tests](./installer-bootstrap-dependency-verification-tests.md) — This test module reproduces and guards against a specific bootstrap bug where `uv install` succeeded but installed packages to the wrong Python interpreter, causing `_HAS_RICH` to be set `True` even though `rich` was not importable in the running process. Tests verify the `_verify_imports` helper logic and the uv command's Python targeting flags.
- [InstallPackageTool: pip Install with Guardian Review Tests](./installpackagetool-pip-install-with-guardian-review-tests.md) — This test module covers `InstallPackageTool`, a built-in tool that runs `pip install` with Guardian policy review before execution. Tests verify correct pip invocation, shell injection prevention (semicolons, pipes, backticks), Guardian block behavior, subprocess timeout handling, and pip failure reporting.
- [Instinct Correction Loop: Patch Computation, Summarization, Store Persistence, and Approve Endpoint Tests](./instinct-correction-loop-patch-computation-summarization-store-persistence-and-a.md) — Covers the "Correction Loop" — the subsystem that detects when a user edits an AI-proposed action during approval, records what changed as structured patches, and surfaces those corrections for soul memory promotion. Tests span the pure diff functions, the `InstinctStore` correction persistence layer, and the `/approve` HTTP endpoint behavior.
- [Intentions API Tests: CRUD, Toggle, and On-Demand Execution](./intentions-api-tests-crud-toggle-and-on-demand-execution.md) — This test file covers PocketPaw's `/api/v1/intentions` router, which manages proactive agent intentions — scheduled or triggered actions the agent initiates autonomously. It verifies the full CRUD lifecycle, enable/disable toggling, input validation, and on-demand execution.
- [Internal Async Event Bus: Subscribe, Emit, and Fault Isolation Tests](./internal-async-event-bus-subscribe-emit-and-fault-isolation-tests.md) — This test file validates the `ee.cloud.shared.events` module's async event bus, which enables decoupled communication between cloud subsystems without direct imports. The tests confirm subscription, emission, unsubscription, multi-handler ordering, handler fault isolation, and the existence of a module-level singleton.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.
- [KbProvider Tests: Workspace Scoping, RBAC, and Mount Template](./kbprovider-tests-workspace-scoping-rbac-and-mount-template.md) — This module tests the KbProvider file provider, which surfaces the knowledge base as a virtual file tree. Tests verify that entries are scoped to the correct workspace, that baseline RBAC rules grant read access to workspace members and manage access to admins, and that the mount path template correctly resolves the workspace ID.
- [Knowledge Router: Multi-Scope Listing, Filtering, and Cross-Workspace Isolation Tests](./knowledge-router-multi-scope-listing-filtering-and-cross-workspace-isolation-tes.md) — This integration test suite exercises the `/api/v1/knowledge/articles` FastAPI endpoint end-to-end, verifying that it merges workspace and agent scopes, supports `agent_id` filtering, rejects cross-workspace queries with HTTP 403, and returns empty results (not a data leak) for unknown agent IDs.
- [Launcher Autostart Manager Tests: macOS, Windows, and Linux](./launcher-autostart-manager-tests-macos-windows-and-linux.md) — This test module verifies the `AutoStartManager` class, which enables the PocketPaw launcher to start automatically at system login across macOS (Launch Agents plist), Windows (registry key), and Linux (XDG autostart .desktop file). Tests cover enable, disable, and status detection for each platform, with appropriate mocking of platform-specific APIs.
- [Launcher Bootstrap: Python Detection, Venv Creation, and Installation Tests](./launcher-bootstrap-python-detection-venv-creation-and-installation-tests.md) — This test module covers the `Bootstrap` class responsible for the launcher's dependency setup: detecting a suitable Python 3.11+ interpreter, creating a virtual environment, installing PocketPaw with optional extras, and reporting progress. It tests both the happy path and critical failure scenarios including missing Python, uv fallback, and pip failures.
- [Launcher ServerManager: Port Management, PID Lifecycle, and Health Check Tests](./launcher-servermanager-port-management-pid-lifecycle-and-health-check-tests.md) — This test module covers `ServerManager`, the launcher component responsible for managing the PocketPaw FastAPI server process: detecting free ports, reading port configuration, managing PID files, performing health checks, and cleanly starting and stopping the server process.
- [Live Cloud API Smoke Tests with Backend Reachability Guard](./live-cloud-api-smoke-tests-with-backend-reachability-guard.md) — Smoke tests that run real HTTP calls against a locally running `paw-cloud` backend. The entire module skips automatically when the backend is unreachable, making these safe to include in CI without a live server requirement. Tests cover login, profile retrieval, agent listing, room lifecycle, DMs, and unauthenticated rejection.
- [LLM Provider Adapter Tests: Registry, Model Resolution, and Per-Provider Configuration](./llm-provider-adapter-tests-registry-model-resolution-and-per-provider-configurat.md) — PocketPaw supports multiple LLM providers (Anthropic, Ollama, OpenRouter, Gemini, LiteLLM) through a unified adapter pattern. These tests validate the provider registry, model string resolution priority, per-adapter environment variable construction, and the `LLMClient` delegation layer that dispatches to the correct adapter.
- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.
- [MCP Router Tests: Server Management and OAuth Callback Handling](./mcp-router-tests-server-management-and-oauth-callback-handling.md) — This test file covers PocketPaw's `/api/v1/mcp` router, which manages Model Context Protocol (MCP) server connections — add, remove, toggle, and status — and handles the OAuth callback that completes browser-based MCP server authentication flows.
- [MCTaskExecutor AgentEvent Regression: Dataclass vs Dict Protocol Fix](./mctaskexecutor-agentevent-regression-dataclass-vs-dict-protocol-fix.md) — This regression test reproduces a bug from PR #482 where MCTaskExecutor called .get() on AgentEvent dataclass objects, causing AttributeError crashes. The test covers the full executor pipeline — event streaming, task status transitions, output capture, and error handling — using a fake agent that emits AgentEvent dataclass objects rather than plain dicts.
- [Memory API Tests: Settings, Long-Term Memory CRUD, and Backend Stats](./memory-api-tests-settings-long-term-memory-crud-and-backend-stats.md) — This test file covers PocketPaw's `/api/v1/memory` router, which exposes configuration for the memory backend (file store vs. mem0), CRUD operations over long-term memory entries, and per-backend statistics.
- [Memory Isolation: Sender-Scoped Storage and Identity Injection](./memory-isolation-sender-scoped-storage-and-identity-injection.md) — Tests verifying that PocketPaw's MemoryManager correctly isolates memories by sender identity, so conversations from different users do not bleed into each other. Covers user ID resolution, per-user file routing in FileMemoryStore, and identity injection into system prompts.
- [Memory Tools: RememberTool, RecallTool, and Session List API](./memory-tools-remembertool-recalltool-and-session-list-api.md) — Tests for the agent-facing memory tools (`remember` and `recall`) and the session list REST endpoint. Covers tool schema validation, content persistence, tag filtering, truncation behavior, and the API's ability to enumerate stored sessions.
- [Message Bus Tests: Inbound/Outbound Pub-Sub, Multi-Subscriber Fan-out, and Channel Routing](./message-bus-tests-inboundoutbound-pub-sub-multi-subscriber-fan-out-and-channel-r.md) — This test suite validates PocketPaw's internal message bus, which decouples channel adapters (Telegram, Discord, WhatsApp, CLI) from the core agent logic using an async publish-subscribe queue. Tests cover inbound message queueing, outbound pub-sub subscription and delivery, multi-subscriber fan-out, unsubscribe, adapter integration, broadcast to all channels, and per-channel filtering.
- [MessageService Realtime Emit Tests](./messageservice-realtime-emit-tests.md) — This test module enforces that every public mutation in MessageService fires the correct typed realtime event through emit(). It also includes regression guards ensuring the WebSocket router itself does not perform direct broadcasts that would bypass the event bus.
- [Microsoft Teams Channel Adapter Tests: Messaging, Tenant Filtering, and Error Recovery](./microsoft-teams-channel-adapter-tests-messaging-tenant-filtering-and-error-recov.md) — Tests for PocketPaw's Microsoft Teams channel adapter, covering initialisation, message processing, stream buffering, tenant-based filtering, bus integration, lifecycle management, and error recovery. The `botbuilder-core` dependency is mocked since it is optional.
- [Mission Control: Data Models, File Store, and Manager Operations](./mission-control-data-models-file-store-and-manager-operations.md) — Tests for PocketPaw's multi-agent orchestration layer, covering AgentProfile and Task data models, the file-based persistence store, and the high-level MissionControlManager that coordinates agents, tasks, messages, documents, and heartbeats.
- [Mission Control (Deep Work) API Tests: Project Lifecycle, State Transitions, and REST Endpoints](./mission-control-deep-work-api-tests-project-lifecycle-state-transitions-and-rest.md) — This test module, created in February 2026, covers PocketPaw's Mission Control subsystem — the project and task management layer that tracks long-horizon AI work. It validates the full project lifecycle through both the `MissionControlManager` directly and the FastAPI REST endpoints, including state transitions (draft, approved, paused, resumed) and proper 404 handling for missing resources.
- [Mission Control Heartbeat Daemon: Agent Health and Work Polling](./mission-control-heartbeat-daemon-agent-health-and-work-polling.md) — Tests for HeartbeatDaemon, the background process that periodically wakes registered agents, checks for pending tasks and notifications, and records liveness signals. Covers daemon lifecycle, configurable intervals, work discovery, error resilience, and manual triggers.
- [Mission Control REST API: FastAPI Endpoint Tests](./mission-control-rest-api-fastapi-endpoint-tests.md) — Tests for the FastAPI router that exposes Mission Control functionality over HTTP, covering agent, task, message, document, activity, stats, and notification endpoints. Uses FastAPI's TestClient with monkeypatched singletons for full request/response validation without a live server.
- [Mission Control Task Executor: Security, Concurrency, and Streaming](./mission-control-task-executor-security-concurrency-and-streaming.md) — Tests for MCTaskExecutor, the component that runs agent tasks in the background, streams results, and broadcasts events. Covers singleton lifecycle, UUID validation, error sanitization, duplicate-execution prevention, and background task completion.
- [Model Router: Task Complexity Classification and Tiered Model Selection](./model-router-task-complexity-classification-and-tiered-model-selection.md) — Tests for ModelRouter, which classifies incoming messages by complexity and routes them to the appropriate LLM tier (Haiku for simple, Sonnet for moderate, Opus for complex). Validates classification boundaries, edge cases like empty messages, and that the model names match the configured tiers.
- [MongoFileStore Unit Tests: Scoped Save/Get, Cross-Workspace Isolation, and Soft Delete](./mongofilestore-unit-tests-scoped-saveget-cross-workspace-isolation-and-soft-dele.md) — Unit tests for `MongoFileStore`'s workspace-scoped CRUD operations, validating that files are stored and retrieved correctly within a workspace, are invisible across workspace boundaries, disappear after soft deletion, and return `None` (not raise) for missing IDs.
- [MongoMemoryStore Facts Path: LONG_TERM and DAILY Memory Roundtrip Tests](./mongomemorystore-facts-path-longterm-and-daily-memory-roundtrip-tests.md) — These tests exercise the complete facts path of MongoMemoryStore, covering CRUD, type isolation between LONG_TERM and DAILY entries, cross-type search, regex safety, and protocol conformance. They also verify that user_id is stored as a dedicated column and never duplicated in the metadata dictionary.
- [MongoMemoryStore Multi-Tenant Workspace Isolation Tests](./mongomemorystore-multi-tenant-workspace-isolation-tests.md) — These tests verify that MongoMemoryStore stamps workspace_id onto both session messages and fact documents at write time, and that read operations are scoped to the requested workspace to prevent cross-tenant data leakage. Untagged rows with workspace_id=None must never appear in workspace-scoped queries.
- [MongoMemoryStore Session Index Shape and Filtering Contract Tests](./mongomemorystore-session-index-shape-and-filtering-contract-tests.md) — These tests verify `_load_session_index_async` in MongoMemoryStore, which powers the `GET /sessions/runtime` endpoint. They pin the exact dict shape, filtering of group and soft-deleted sessions, channel inference from session key prefix, and empty title coercion.
- [MongoMemoryStore SESSION Path: Protocol Conformance and Adapter-Specific Read Tests](./mongomemorystore-session-path-protocol-conformance-and-adapter-specific-read-tes.md) — These tests cover the full SESSION memory path in MongoMemoryStore — CRUD, chronological message retrieval, session key isolation, combined session-with-messages reads with limit behavior, and cross-collection isolation between pocket and group messages.
- [MongoMemoryStore Session Upkeep: messageCount and lastActivity Auto-Maintenance Tests](./mongomemorystore-session-upkeep-messagecount-and-lastactivity-auto-maintenance-t.md) — These tests verify that MongoMemoryStore automatically updates Session metadata (messageCount, lastActivity) on every save and auto-creates missing Session rows when a message arrives before the session is explicitly created. They pin behaviors introduced after a historical refactor that removed a broken parallel bus subscriber.
- [Natural Time Parsing Tests: parse_natural_time Scheduler Function](./natural-time-parsing-tests-parsenaturaltime-scheduler-function.md) — This test suite validates the `parse_natural_time` function in PocketPaw's scheduler module, covering every user-facing time expression format — with and without the 'in' prefix, abbreviations, singular/plural forms, and embedded sentences. The suite acts as both a regression guard and a specification for all accepted input shapes.
- [Neonize WhatsApp Channel Adapter: Mocking, Streaming, and Bus Integration](./neonize-whatsapp-channel-adapter-mocking-streaming-and-bus-integration.md) — Tests for the NeonizeAdapter, which connects PocketPaw to WhatsApp Personal via the neonize library. Covers auto-installation of the optional dependency, message send/receive lifecycle, stream buffering, QR code exposure, and bus subscription.
- [Notification Derivation Tests: Mention, Reaction, and Invite Trigger Verification](./notification-derivation-tests-mention-reaction-and-invite-trigger-verification.md) — These tests verify the three primary notification derivation paths — @mention in a group message, emoji reaction, and workspace invite — correctly call NotificationService.create with the right arguments. They also verify that self-targeted events and reaction removals never produce notifications.
- [Notifications Test Package Marker](./notifications-test-package-marker.md) — An empty `__init__.py` that marks `tests/cloud/notifications/` as a Python package for stable pytest discovery. No logic or fixtures live here; all test content is in the sibling test modules test_service.py and test_derived.py.
- [NotificationService Unit Tests: CRUD, Fan-Out, and Idempotency Contract](./notificationservice-unit-tests-crud-fan-out-and-idempotency-contract.md) — These unit tests exercise NotificationService in isolation by patching the Notification Beanie document and the emit function, verifying that create persists then emits, list_for_user filters at the database layer, mark_read is idempotent for already-read notifications, and clear_all uses a bulk update and emits a single event.
- [OAuth Integration: Token Storage, Provider Configuration, and Token Validity](./oauth-integration-token-storage-provider-configuration-and-token-validity.md) — Tests for PocketPaw's OAuth subsystem, covering secure token persistence in TokenStore, provider URL generation via OAuthManager, and token validity checks based on expiry timestamps. Verifies file permissions, non-existent token handling, and graceful degradation for unknown providers.
- [OAuth2 PKCE Authorization Server Tests: Full Flow, Token Exchange, and Consent](./oauth2-pkce-authorization-server-tests-full-flow-token-exchange-and-consent.md) — Tests PocketPaw's OAuth2 authorization server with PKCE (Proof Key for Code Exchange), covering authorization code issuance, token exchange, token refresh, revocation, and the consent UI endpoints. The tests validate both the server logic class directly and the REST endpoints, ensuring the security guarantees of PKCE are enforced end-to-end.
- [OCR Tool: Schema Validation, File Jail, and Provider Fallback Tests](./ocr-tool-schema-validation-file-jail-and-provider-fallback-tests.md) — Tests for OCRTool, a built-in agent tool that extracts text from images using OpenAI Vision or Tesseract as fallback. Validates the tool schema, enforces a file-system jail to prevent path traversal, and covers error paths for unsupported formats, oversized files, missing providers, and API failures.
- [Ollama Integration: Provider Detection, Env Var Construction, and CLI Health Check](./ollama-integration-provider-detection-env-var-construction-and-cli-health-check.md) — Tests for PocketPaw's Ollama provider support, verifying that LLMClient correctly detects Ollama from settings, constructs the right subprocess environment variables, and skips smart model routing for local models. Also covers the `--check-ollama` CLI command that validates server reachability and model availability.
- [OpenAI Agents SDK Backend: Tool Wiring, Session Management, and Graceful Degradation](./openai-agents-sdk-backend-tool-wiring-session-management-and-graceful-degradatio.md) — Tests for the OpenAI Agents SDK backend adapter, covering custom tool registration, per-backend provider selection, SDK-absent graceful degradation, native SQLite session management, and lazy database path resolution. All tests run without a real OpenAI Agents SDK installation via comprehensive mocking.
- [OpenAI-Compatible Provider: Detection, Env Vars, Client Creation, and Error Formatting](./openai-compatible-provider-detection-env-vars-client-creation-and-error-formatti.md) — Tests for PocketPaw's OpenAI-compatible endpoint support in LLMClient, enabling any OpenAI-compatible API (LiteLLM, Groq, local proxies) as the LLM backend. Covers provider detection, model resolution, environment variable construction for subprocess mode, Anthropic client creation with custom base URL, and structured error formatting for connection and authentication failures.
- [OpenCode Backend: REST API Integration, Session Caching, and Tool Injection](./opencode-backend-rest-api-integration-session-caching-and-tool-injection.md) — Tests for the OpenCodeBackend, which connects PocketPaw to OpenCode's REST API for agentic coding tasks. Covers health checking, session creation and caching, the message endpoint contract, system prompt tool instruction injection, streaming responses, and graceful error handling for unreachable servers.
- [OSS-to-EE Resolver Fallback Tests: Bridging JSONL and Mongo Upload Stores](./oss-to-ee-resolver-fallback-tests-bridging-jsonl-and-mongo-upload-stores.md) — Tests for the `resolve_media_paths_any` OSS→EE fallback, which handles the hybrid scenario where uploads are stored in the EE Mongo store but chat rendering goes through the OSS JSONL lookup path. Without the fallback, EE-uploaded files would appear as broken links in OSS-rendered chat messages.
- [Output Redaction Tests: API Keys, Tokens, and Secret Pattern Detection](./output-redaction-tests-api-keys-tokens-and-secret-pattern-detection.md) — PocketPaw's output redaction module (`pocketpaw.security.redact`) scans agent output for API keys, tokens, credentials, and other secrets before they are logged or sent to external systems. These tests validate detection of over a dozen secret patterns across major providers, verify that safe text is not falsely flagged, and confirm the specific scenario of an agent reading a `.env` file.
- [Packaging Sanity Tests: Dependency Conflicts, Version Bounds, and Extra Consistency](./packaging-sanity-tests-dependency-conflicts-version-bounds-and-extra-consistency.md) — Tests that parse `pyproject.toml` directly to catch dependency version conflicts and packaging invariants before they cause runtime failures. Specifically targets the FastAPI/Starlette version conflict introduced by MCP's transitive Starlette dependency, and verifies that dashboard-mode dependencies are included in the core package.
- [Paw CLI Tests: init, doctor, channels, and serve Command Coverage](./paw-cli-tests-init-doctor-channels-and-serve-command-coverage.md) — This test file validates PocketPaw's `paw` CLI entry point using Click's `CliRunner`, covering the main command group, `init`, `doctor`, `channels`, and `serve` subcommands. It focuses on exit codes, output presence, option parsing, and graceful degradation when optional dependencies like `soul-protocol` are absent.
- [Paw Print Ingest Router: HTTP Surface, CORS, Rate Limiting, and Fabric Mapping Tests](./paw-print-ingest-router-http-surface-cors-rate-limiting-and-fabric-mapping-tests.md) — This test suite covers the HTTP layer of the Paw Print widget system — including owner-authenticated CRUD endpoints, CORS enforcement for the public spec and event ingest routes, payload size limits, per-customer rate limiting, and event-to-Fabric-object mapping via Jinja-style template interpolation.
- [Paw Print Widget Backend: Model Validation, CRUD, and Rate Limiting Tests](./paw-print-widget-backend-model-validation-crud-and-rate-limiting-tests.md) — This test suite covers the backend layer of PocketPaw's Paw Print embeddable widget system — validating Pydantic model constraints (block caps, domain normalization, rate limits, token format), SQLite-backed CRUD operations, event persistence, and the dual rate-limit primitives (overall and per-customer) that protect the public ingest endpoint.
- [Paw Project Scanner Tests: heuristic_scan and scan_prompt Coverage](./paw-project-scanner-tests-heuristicscan-and-scanprompt-coverage.md) — The `paw scan` heuristic reads a project's on-disk artifacts — README, `pyproject.toml`, `package.json`, `.env.example`, and top-level directory structure — and stores extracted facts into the soul's memory. These tests verify that each artifact type is correctly parsed, that the soul's `remember()` method is called with appropriate metadata, and that failures in memory storage do not abort the scan.
- [Paw Soul Tools Tests: SoulRemember, SoulRecall, SoulEditCore, and SoulStatus](./paw-soul-tools-tests-soulremember-soulrecall-souleditcore-and-soulstatus.md) — These tests cover the four built-in soul tools that PocketPaw exposes to LLMs: `soul_remember`, `soul_recall`, `soul_edit_core`, and `soul_status`. Each tool is validated for its parameter schema, execution behavior, error handling, and JSON Schema export in both Anthropic and OpenAI formats.
- [PawConfig Tests: YAML Loading, Defaults, and Environment Variable Overrides](./pawconfig-tests-yaml-loading-defaults-and-environment-variable-overrides.md) — `PawConfig` is PocketPaw's project-level configuration object, loaded from `paw.yaml` in the project root. These tests cover the full priority chain: defaults when no file exists, YAML values when the file is present, and environment variable overrides that take precedence over both.
- [PII Scanner Tests: Detection, Actions, False Positives, and Singleton Pattern](./pii-scanner-tests-detection-actions-false-positives-and-singleton-pattern.md) — PocketPaw's PII scanner (`pocketpaw.security.pii`) detects sensitive personal information in text before it reaches logs, LLM context, or external storage. These tests validate detection of SSNs, emails, phones, credit cards, IP addresses, and dates of birth, verify that safe text generates no false positives, and confirm the singleton pattern for the default scanner instance.
- [Plan Mode API Tests: Approve and Reject Active Agent Plans](./plan-mode-api-tests-approve-and-reject-active-agent-plans.md) — This test file covers PocketPaw's `/api/v1/plan` router, which lets the dashboard approve or reject an agent's proposed action plan before execution. It verifies the session-keyed lookup, the not-found response when no active plan exists, and input validation.
- [Plan Mode Tests: PlanStep, ExecutionPlan, PlanManager, and Approval Flow](./plan-mode-tests-planstep-executionplan-planmanager-and-approval-flow.md) — PocketPaw's plan mode allows agents to propose multi-step execution plans and wait for human approval before running potentially destructive operations. These tests cover `PlanStep` previews, `ExecutionPlan` assembly, `PlanManager` lifecycle (create, approve, reject, expire), and the async approval-wait mechanism.
- [Pocket Journal SSE Stream Router Tests: Filtering, Cursors, and Frame Contract](./pocket-journal-sse-stream-router-tests-filtering-cursors-and-frame-contract.md) — Tests the Server-Sent Events (SSE) endpoint that streams journal events for a specific pocket to the RippleGraphWidget frontend. Covers the event-matching predicate, backlog replay with `since_seq` cursor support for reconnects, the `connected` handshake frame, idle-poll timeout, and strict isolation so events from other pockets never appear in a pocket's stream.
- [Pocket Layout Helpers: Export Determinism, Parse Validation, Round-Trip, and Store Scoping Tests](./pocket-layout-helpers-export-determinism-parse-validation-round-trip-and-store-s.md) — This unit test suite covers the pure Python helpers in `ee.cloud.pockets.layouts` — specifically `export_layout_yaml` (deterministic YAML serialization), `parse_layout_yaml` (safe YAML parsing with input validation), and `UserTemplateStore` (in-process template store with workspace scoping). No FastAPI, Beanie, or MongoDB is involved.
- [Pocket Layout Routes: Export, Template Create/List, and Workspace Scoping Tests](./pocket-layout-routes-export-template-createlist-and-workspace-scoping-tests.md) — This integration test suite covers three new routes on the pockets router that implement the layout save/share surface: `POST /pockets/{id}/export-layout` (YAML serialization), `POST /pockets/templates` (template creation), and `GET /pockets/templates` (workspace-scoped listing). It verifies the round-trip fidelity, metadata override support, cross-workspace isolation, and malformed YAML handling.
- [Pocket Tool Test Suite: Widget Lifecycle, Legacy Conversion, and UISpec Contracts](./pocket-tool-test-suite-widget-lifecycle-legacy-conversion-and-uispec-contracts.md) — Comprehensive tests for `CreatePocketTool`, `AddWidgetTool`, and `RemoveWidgetTool` — the three built-in tools that let AI agents build and mutate dashboard "pockets" in PocketPaw. The suite enforces spec versioning (v1.0 UISpec vs v2.0 widget array), backward compatibility of the `name→title` alias, and structural contracts on every tool's JSON output.
- [Pocket Tool Tests: Dashboard Widget Creation, Legacy Conversion, and UI Spec Generation](./pocket-tool-tests-dashboard-widget-creation-legacy-conversion-and-ui-spec-genera.md) — The pocket tool system lets AI agents create and modify PocketPaw dashboard "pockets" — widget-based layouts containing metrics, charts, tables, and feeds. These tests validate the `create_pocket`, `add_widget`, and `remove_widget` tools, including backward-compatible legacy widget conversion, the `ui` parameter for pre-built specs, and the JSON Schema definitions used by LLM providers.
- [PocketPaw Cognitive Engine: Wiring Tests for Soul-Aware Thinking](./pocketpaw-cognitive-engine-wiring-tests-for-soul-aware-thinking.md) — Tests for the `PocketPawCognitiveEngine`, which bridges the soul-protocol's identity layer with PocketPaw's LLM backend to produce soul-contextualised responses. Covers correct parameter forwarding, graceful error handling, stream termination logic, and end-to-end wiring through `SoulManager` and `AgentLoop`.
- [Pockets Domain Schemas: Request Validation and Response Model Tests](./pockets-domain-schemas-request-validation-and-response-model-tests.md) — This test file validates all Pydantic request/response schemas in the pockets domain — covering field defaults, enum constraints, length limits, optional-field behavior, and the full `PocketResponse` shape. It ensures the API surface contracts are locked in and that invalid inputs are rejected at the schema layer before reaching business logic.
- [Presence Broadcast and Grace Window Tests](./presence-broadcast-and-grace-window-tests.md) — This test module verifies the WebSocket endpoint's presence online/offline lifecycle, specifically the grace window that delays broadcasting a user offline until reconnection is no longer possible. Tests drive _schedule_presence_offline directly and use time-acceleration to keep the suite fast.
- [Proactive Daemon Tests: Cron Scheduling, Intention CRUD, Context Hub, and Stale Session Triggers](./proactive-daemon-tests-cron-scheduling-intention-crud-context-hub-and-stale-sess.md) — This test module covers PocketPaw's proactive daemon — the subsystem that fires agent prompts on a schedule rather than waiting for user messages. It validates cron expression parsing (including named presets), the `IntentionStore` CRUD lifecycle, `ContextHub` template variable injection, the `TriggerEngine` scheduling layer, and the stale-session trigger that nudges idle users.
- [Rate Limiter, Audit API Events, and OpenAPI Config Tests](./rate-limiter-audit-api-events-and-openapi-config-tests.md) — Tests PocketPaw's token-bucket rate limiter, the `RateLimitInfo` response model and its header formatting, audit logging for API events (OAuth and key revocation), and the OpenAPI endpoint configuration. Together these tests ensure the API protects against request floods, produces correct rate limit headers, and maintains a searchable audit trail.
- [RBAC Action Matrix Tests: Parametrized Role Enforcement and Coverage Guard](./rbac-action-matrix-tests-parametrized-role-enforcement-and-coverage-guard.md) — A parametrized test matrix that exercises every entry in the canonical `ACTIONS` table against every role in the matching role family, verifying that `check_action` allows or raises `Forbidden` with the correct deny code. A meta-test enforces that no new action can be added without both an allow-path and a deny-path assertion, preventing silent coverage gaps.
- [ReadState Model and UnreadService Upsert Tests](./readstate-model-and-unreadservice-upsert-tests.md) — This module tests the ReadState model's default values and the atomic upsert behavior of UnreadService.mark_read and bump_mention against a real in-memory MongoDB. The tests enforce the unique (user, group) index constraint and verify that concurrent operations remain idempotent.
- [Realtime Bus Initialization and Group Member Resolution Tests](./realtime-bus-initialization-and-group-member-resolution-tests.md) — This test module verifies the wiring of `init_realtime`, ensuring the correct bus backend is selected by default and that the `AudienceResolver` is properly exposed. It also covers `GroupService.list_member_ids` behavior for found and missing groups.
- [Realtime Test Package Marker](./realtime-test-package-marker.md) — An empty `__init__.py` that designates `tests/cloud/realtime/` as a Python package for stable pytest discovery and import resolution. No logic or fixtures live here; all test content is in the sibling test modules.
- [Recent Files Tracker Tests: Bash Path Heuristic and Tool Path Extraction](./recent-files-tracker-tests-bash-path-heuristic-and-tool-path-extraction.md) — `RecentFilesTracker` maintains a most-recently-used list of files accessed by the agent, enabling context-aware suggestions and memory seeding. These tests cover the Bash command path heuristic (which was missing and is now implemented), tool-specific path extraction for Read/Write/Edit/Bash tools, and tracker deduplication and ordering behavior.
- [Reddit Tool Tests: Search, Read, Trending, and Post Formatting](./reddit-tool-tests-search-read-trending-and-post-formatting.md) — PocketPaw's Reddit integration provides agents with three tools for interacting with Reddit: `reddit_search`, `reddit_read`, and `reddit_trending`. These tests validate tool schemas and trust levels, the `_format_post` helper for normalizing Reddit API responses, and async tool execution covering success, empty results, and error conditions.
- [Reminders API Tests: Natural Language Scheduling, Listing, and Deletion](./reminders-api-tests-natural-language-scheduling-listing-and-deletion.md) — This test file covers PocketPaw's `/api/v1/reminders` router, which accepts natural-language reminder messages, parses them into scheduled events via the scheduler, and exposes list and delete operations with human-readable time-remaining formatting.
- [ReminderScheduler Cron Expression and File Corruption Tests](./reminderscheduler-cron-expression-and-file-corruption-tests.md) — This test file validates `ReminderScheduler`'s support for recurring cron-based reminders and its resilience to corrupted or malformed reminder files. Tests cover cron expression validation, preset schedules, persistent-vs-oneshot behavior, file quarantine on corruption, and partial corruption recovery.
- [Remote Access Auth Tests: Token Generation, Middleware, and QR Endpoint Security](./remote-access-auth-tests-token-generation-middleware-and-qr-endpoint-security.md) — PocketPaw's dashboard uses a file-backed token for remote access authentication. These tests validate token generation and persistence, the auth middleware's enforcement of Bearer token and query parameter authentication, and a security fix (issue #854) that closed an unauthenticated `/api/qr` endpoint.
- [Remote Tunnel API Tests: Cloudflare Tunnel Status, Start, and Stop](./remote-tunnel-api-tests-cloudflare-tunnel-status-start-and-stop.md) — This test file covers PocketPaw's `/api/v1/remote` router, which manages a Cloudflare tunnel that exposes the local PocketPaw instance to the internet. It verifies status reporting, tunnel start with error handling for missing binaries, and graceful stop.
- [ResearchTool Test Suite — Web Search, URL Extraction, and Happy Path](./researchtool-test-suite-web-search-url-extraction-and-happy-path.md) — This test file validates `ResearchTool`, PocketPaw's built-in agentic research capability that chains web search, URL content extraction, and LLM summarization. Tests cover tool metadata, URL parsing edge cases, search failure handling, and the full happy-path pipeline using mocked external dependencies.
- [Retrieval Journal Projection Tests: Write Path, Scope Filtering, Graduation Policy, and REST Router](./retrieval-journal-projection-tests-write-path-scope-filtering-graduation-policy-.md) — Comprehensive tests for the `ee.retrieval` subsystem that journals every memory-retrieval query and drives a graduation policy promoting frequently-retrieved memories to higher tiers. Covers event write-path fidelity, scope containment, correlation grouping, threshold-based graduation decisions, projection rebuild equivalence, and the REST router's GET endpoints.
- [Room-Scoped WebSocket Routing Tests](./room-scoped-websocket-routing-tests.md) — This module tests the room-scoped routing layer in ConnectionManager, which limits typing indicators and read receipts to only the sockets currently joined to a specific group. It also verifies that non-members cannot inject typing or read-ack events into rooms they do not belong to.
- [Root Pytest Configuration: SSRF Relaxation, Scope Bypass, and Audit Log Isolation](./root-pytest-configuration-ssrf-relaxation-scope-bypass-and-audit-log-isolation.md) — The root `tests/conftest.py` installs three session-wide fixtures that configure PocketPaw's test environment: it relaxes SSRF URL validation so internal URLs work during tests, bypasses scope enforcement for non-security tests, and redirects the audit log to a temp file so tests never write to the developer's home directory.
- [RunPythonTool Test Suite — Sandboxed Code Execution with Guardian and File Jail](./runpythontool-test-suite-sandboxed-code-execution-with-guardian-and-file-jail.md) — This test file covers `RunPythonTool`, which executes arbitrary Python code in a sandboxed subprocess controlled by a file jail and a guardian policy engine. Tests validate stdout capture, stderr surfacing, exit code reporting, timeout enforcement, guardian blocking, filesystem isolation, temp file cleanup, and tool metadata.
- [Runtime Audit API Integration Tests — Workspace Rollup, Search, CSV Export, and Security](./runtime-audit-api-integration-tests-workspace-rollup-search-csv-export-and-secur.md) — This test module validates the canonical `/runtime/audit` endpoint introduced in Cluster C/PR4, covering workspace-scoped filtering, full-text search with length caps, CSV export, SQL injection resistance, and the deprecation forwarding behavior of the legacy `/audit` alias. It exercises both the new runtime router and the legacy router mounted together to prove they share the same underlying AuditStore and scope guard.
- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.
- [Sarvam AI Integration Tests — Translation, TTS, STT, and OCR](./sarvam-ai-integration-tests-translation-tts-stt-and-ocr.md) — This comprehensive test file covers PocketPaw's integration with Sarvam AI's multilingual APIs: text translation, text-to-speech (TTS), speech-to-text (STT), and vision OCR. Tests validate API key gating, SDK error propagation, audio encoding, file-based I/O, provider routing, policy group membership, config field defaults, and tool registration.
- [Scope Enforcement and Tool Policy Fail-Closed Tests](./scope-enforcement-and-tool-policy-fail-closed-tests.md) — This test module validates that PocketPaw's `require_scope` FastAPI dependency rejects requests that lack explicit authorization markers, preventing silent scope bypass via master, session, or cookie auth. It also verifies that `ToolPolicy` raises on unknown profile names rather than defaulting to an open or undefined state.
- [Security Audit CLI Tests — Config Permissions, API Key Exposure, and Audit Runner](./security-audit-cli-tests-config-permissions-api-key-exposure-and-audit-runner.md) — This test file validates PocketPaw's security audit command-line tool, which checks for world-readable config files, plaintext API keys, missing audit logs, unreachable guardian agents, invalid file jails, risky tool profiles, and dangerous permission bypasses. Tests cover both detection and auto-fix capabilities.
- [Security Rails Tests: Dangerous Command Blocking, Bypass Detection, and Safe Allowlist](./security-rails-tests-dangerous-command-blocking-bypass-detection-and-safe-allowl.md) — Layer 5 of PocketPaw's security stack (`pocketpaw.security.rails`) blocks dangerous shell commands before they reach the execution layer. These tests validate the blocking of destructive file operations, remote code execution patterns, obfuscation bypasses, privilege escalation, data exfiltration, and system damage commands, while confirming that safe commands are not falsely blocked.
- [Self-Audit Daemon Tests — Stale Sessions, Config Conflicts, Disk Usage, and OAuth Tokens](./self-audit-daemon-tests-stale-sessions-config-conflicts-disk-usage-and-oauth-tok.md) — This test file covers `pocketpaw.daemon.self_audit`, a background health-check daemon that monitors for stale sessions, dangerous config combinations, excessive disk usage, oversized audit logs, and orphaned OAuth tokens. Tests validate each individual check and the full `run_self_audit` orchestrator.
- [Session and Message context_type Discriminator Validator Tests](./session-and-message-contexttype-discriminator-validator-tests.md) — These tests verify that Pydantic model validators on Message and Session enforce strict discriminator rules, ensuring group and pocket context types carry exactly the required fields and nothing more. They also confirm backward-compatible inference when context_type is omitted.
- [Session History Compaction Tests: Tier 1 Window Pruning, Budget Enforcement, LLM Summarization, and Cache](./session-history-compaction-tests-tier-1-window-pruning-budget-enforcement-llm-su.md) — This test suite validates PocketPaw's two-tier session history compaction system, which prevents unbounded conversation history from exhausting LLM context windows. Tier 1 collapses old messages into a synthetic summary, Tier 2 uses an LLM call to produce a semantic summary with disk caching, and the budget enforcer drops the oldest messages when compacted history still exceeds the token limit.
- [Session Index, REST Endpoints, WebSocket Switching, and Search Tests](./session-index-rest-endpoints-websocket-switching-and-search-tests.md) — This test file comprehensively covers `FileMemoryStore`'s session index subsystem: atomic index writes, index rebuild from disk, CRUD operations, WebSocket session switching, path traversal blocking, full-text session search, and the REST API endpoints that expose these operations to the dashboard.
- [Session Management API Tests — List, Delete, Title Update, and Search](./session-management-api-tests-list-delete-title-update-and-search.md) — This test module covers all four operations of the `/api/v1/sessions` router: listing sessions with pagination, deleting by ID, updating titles, and full-text searching session history. It uses mock-based isolation of the underlying memory store to prevent filesystem side effects while exercising the router's error-handling and edge-case paths.
- [Session Management Tool Tests — New, List, Switch, Clear, Rename, and Delete](./session-management-tool-tests-new-list-switch-clear-rename-and-delete.md) — This test file validates the six session management tools in `pocketpaw.tools.builtin.sessions`: `NewSessionTool`, `ListSessionsTool`, `SwitchSessionTool`, `ClearSessionTool`, `RenameSessionTool`, and `DeleteSessionTool`. Tests cover tool naming, happy-path behavior, error handling, and policy group membership.
- [Session Schema Tests: Create, Update, and Response Contract Validation](./session-schema-tests-create-update-and-response-contract-validation.md) — Unit tests for the three Pydantic schemas governing session lifecycle in PocketPaw's EE cloud layer — `CreateSessionRequest`, `UpdateSessionRequest`, and `SessionResponse`. The tests pin default values, optional-field behavior, and the nullable `deleted_at` field used for soft-deletion semantics.
- [Sessions Test Package Initializer](./sessions-test-package-initializer.md) — This is the empty `__init__.py` marker file for the `tests/cloud/sessions` test package. Its sole purpose is to make Python treat the directory as a package so pytest can discover and import test modules within it.
- [SessionService Realtime Event Emission Tests](./sessionservice-realtime-event-emission-tests.md) — This module tests that every mutating `SessionService` method — create, create_for_pocket, update, delete, and touch — fires the correct realtime event through `emit()` after the database commit. Tests are unit-scoped, patching DB and bus primitives at seam boundaries to isolate emit behavior from persistence.
- [Settings API Security Tests — Secret Field Filtering and Immutable Field Guards](./settings-api-security-tests-secret-field-filtering-and-immutable-field-guards.md) — This test module validates two critical security properties of the `/api/v1/settings` router: that GET never returns API keys or tokens (regression for a v0.4.16 leak), and that PUT rejects writes to security-critical immutable fields like `file_jail_path` and `bypass_permissions`. It also verifies normal settings read and write paths work correctly for non-secret, non-immutable fields.
- [Shared Cloud Tests Package Initializer](./shared-cloud-tests-package-initializer.md) — This is the empty `__init__.py` marker for the `tests/cloud/shared` test package. It enables Python's import system to treat the directory as a package, ensuring pytest can correctly discover and namespace test modules in this subdirectory.
- [Signal Channel Adapter Tests — Init, Lifecycle, Message Handling, and Bus Integration](./signal-channel-adapter-tests-init-lifecycle-message-handling-and-bus-integration.md) — This Sprint 20 test file validates `SignalAdapter`, PocketPaw's channel adapter for Signal messenger. Tests cover adapter configuration defaults, start/stop lifecycle, inbound message parsing with phone number authorization, outbound message sending with streaming support, error recovery, and MessageBus integration.
- [SimulationClock and Tick-Synchronized Scheduler Tests](./simulationclock-and-tick-synchronized-scheduler-tests.md) — This test file covers `SimulationClock` (a deterministic discrete-time clock for agent simulation), `TickSnapshot` (tick-state serialization), `DependencyScheduler.run_tick_synchronized` (tick-driven task dispatch), and simulation tick metadata injection into task prompts. Tests validate clock advancement, snapshot immutability, scheduler dispatch order, and prompt metadata inclusion.
- [SkillLoader and SKILL.md Parsing Tests](./skillloader-and-skillmd-parsing-tests.md) — This test file validates `SkillLoader` and `parse_skill_md` from `pocketpaw.skills.loader`, covering SKILL.md frontmatter parsing, optional field handling, no-frontmatter rejection, directory-name fallback for skill naming, prompt argument substitution, loader lifecycle, skill retrieval, reload, multi-path override resolution, and integration with real skill paths.
- [Skills API Security and Installation Tests — Input Validation, Injection Prevention, and Audit Logging](./skills-api-security-and-installation-tests-input-validation-injection-prevention.md) — This test module covers the full lifecycle of the skills management API: listing, searching, installing from GitHub sources, removing, and reloading. The installation tests are especially dense, exercising a whitelist-based input validator to block path traversal, shell injection, null bytes, and over-segmented source strings before any subprocess is spawned.
- [Skills API Tests — SkillLoader Search, REST Endpoints, Install, Remove, and MCP Presets](./skills-api-tests-skillloader-search-rest-endpoints-install-remove-and-mcp-preset.md) — This test file covers `SkillLoader.search()` and the full skills REST API: listing installed skills, searching the skills.sh library, installing skills via git clone, removing skills, force-reloading, and MCP preset configuration with `needs_args` metadata. Tests validate filtering, case-insensitive matching, subprocess handling, and preset response shapes.
- [Slack Channel Adapter: Integration Tests for Messaging, Streaming, and Auth](./slack-channel-adapter-integration-tests-for-messaging-streaming-and-auth.md) — Tests for PocketPaw's Slack channel adapter, covering message send/receive, threaded replies, stream buffering, channel filtering, and token validation. Uses lightweight mock classes to avoid a real Slack dependency while exercising the full adapter surface.
- [Smoke Test: Agent Dispatch Respond-Mode Matrix in Group Chat](./smoke-test-agent-dispatch-respond-mode-matrix-in-group-chat.md) — This script verifies that `_should_agent_respond` correctly routes messages in group chat based on each agent's respond mode (`auto`, `mention_only`, `silent`) and the set of @mentions present. It was written to lock down a real bug where two `auto` agents both fired when only one was mentioned.
- [Smoke Test: DM and Group Chat Persistence for Human Users](./smoke-test-dm-and-group-chat-persistence-for-human-users.md) — This end-to-end test verifies that DM creation between two workspace members, message exchange, and group chat all persist correctly in MongoDB — without any agent involvement. It validates the full social messaging layer: workspace invites, DM groups, populated member objects, message context types, and cross-member read access.
- [Smoke Test: MemoryManager Singleton Stays Live After Backend Swap](./smoke-test-memorymanager-singleton-stays-live-after-backend-swap.md) — This test reproduces a real bug where `AgentLoop` captured `get_memory_manager()` at construction time — before cloud database initialization — and then held a stale reference to the file-based store even after the MongoDB backend was registered. It verifies that the singleton's internal store is swapped in-place so pre-captured references automatically see the new backend.
- [Smoke Test: MongoDB Memory Backend End-to-End](./smoke-test-mongodb-memory-backend-end-to-end.md) — This script exercises the full MongoDB memory path from `init_cloud_db` through `MemoryManager.add_to_session`, `get_session_history`, raw Mongo inspection, and long-term memory storage in `memory_facts`. It serves as the canonical integration check for the enterprise memory backend.
- [Smoke Test: Pocket Sidebar Session Creation and Chat Persistence](./smoke-test-pocket-sidebar-session-creation-and-chat-persistence.md) — This test mirrors the full flow of PocketPaw's enterprise sidebar: a user creates a Pocket, opens a new session under that Pocket, sends a message, and verifies the message appears in the session history API. It validates that `Session` documents are correctly linked to their parent Pocket in MongoDB.
- [Smoke Test: Session Creation Then Chat Persistence Round-Trip](./smoke-test-session-creation-then-chat-persistence-round-trip.md) — This test validates the core "desired flow": create a session via the API, send a user message, confirm it is stored in MongoDB keyed by the session ID, and retrieve it through the history endpoint. It was written to capture the exact behavior contract for session-scoped message storage.
- [Smoke Test: Session Key Normalization Between Agent Bus and UI Chat History](./smoke-test-session-key-normalization-between-agent-bus-and-ui-chat-history.md) — This test verifies that the colon-delimited session key written by the agent loop (`websocket:X`) is normalized to the underscore form used by the UI (`websocket_X`), so that chat history returned by `GET /api/v1/sessions/{sessionId}/history` actually contains the messages the agent wrote. Without this normalization, the session history API returns empty results despite messages being stored.
- [Soul Bridge and Bootstrap Provider Tests: SoulBridge and SoulBootstrapProvider](./soul-bridge-and-bootstrap-provider-tests-soulbridge-and-soulbootstrapprovider.md) — These tests cover the `SoulBootstrapProvider` — which translates a Soul Protocol soul object into a `BootstrapContext` for PocketPaw's agent runtime — and `SoulBridge`, the async shim that wraps soul `observe()` and `recall()` calls with error-swallowing guarantees. Together they ensure the paw module can safely integrate with soul-protocol without crashing when the soul is unavailable or misbehaves.
- [Soul Memories API Tests: Tier Filtering, Limit Clamping, and Entry Serialization](./soul-memories-api-tests-tier-filtering-limit-clamping-and-entry-serialization.md) — Unit tests for the `GET /api/v1/soul/memories` endpoint, focusing on the `_collect_tier_entries` helper that queries individual memory tiers, limit clamping to safe bounds, tier filter validation, and graceful handling of absent souls or missing tier stores. Tests operate at the function level to avoid spinning up a full SSE client.
- [Soul-Protocol Integration Tests: End-to-End PocketPaw Wiring](./soul-protocol-integration-tests-end-to-end-pocketpaw-wiring.md) — Integration tests that validate the full lifecycle of the soul-protocol package within PocketPaw, including bootstrap prompt generation, memory observe-and-recall, tool bridge discovery of soul tools, and corrupt file recovery. All tests are conditionally skipped when `soul-protocol` is not installed.
- [Soul Protocol v0.2.4 Smoke Tests: Reload, Evaluate, Biorhythm, and Fatigue](./soul-protocol-v024-smoke-tests-reload-evaluate-biorhythm-and-fatigue.md) — Smoke tests for soul-protocol v0.2.4 features including hot-reload, the evaluate endpoint, biorhythm config round-trips, dirty tracking for autosave optimization, external change detection, and the fatigue hint in bootstrap context. Designed to catch regressions when upgrading the soul-protocol dependency.
- [SoulManager Lifecycle Tests: Birth, Persistence, Import, and Concurrency](./soulmanager-lifecycle-tests-birth-persistence-import-and-concurrency.md) — Comprehensive tests for `SoulManager`, the singleton that manages a PocketPaw soul's full lifecycle — from birth through autosave, import, reload, and shutdown. Covers tool caching, dirty tracking, concurrent observe serialization, corrupt file fallback, and biorhythm configuration.
- [Speech-to-Text Tool Tests: Schema, File Jail, Multi-Provider, and Error Handling](./speech-to-text-tool-tests-schema-file-jail-multi-provider-and-error-handling.md) — Tests for PocketPaw's `SpeechToTextTool`, covering schema validation, file path jailing for security, size limits, successful transcription with both OpenAI Whisper and ElevenLabs providers, language parameter forwarding, and graceful handling of missing API keys and provider errors.
- [Spotify Tool Tests: Schema Validation, Auth Guards, and Playback Control](./spotify-tool-tests-schema-validation-auth-guards-and-playback-control.md) — Tests for PocketPaw's Spotify integration tools covering schema correctness, authentication failure handling, playback action validation, and successful API interactions. Ensures the tools surface clear errors when credentials are absent and route API calls correctly when authenticated.
- [SSRF URL Validation Tests: Blocking Internal and Malicious URLs in Settings](./ssrf-url-validation-tests-blocking-internal-and-malicious-urls-in-settings.md) — This test file, added during security sprint cluster E (issue #703), validates the `validate_external_url` function that guards all URL-typed settings fields against Server-Side Request Forgery (SSRF) attacks. It covers rejection of cloud metadata endpoints, RFC 1918 ranges, non-HTTP schemes, and malformed inputs, while confirming a developer-friendly escape hatch for local development.
- [StatusTracker Tests: Event-Driven Agent State Machine](./statustracker-tests-event-driven-agent-state-machine.md) — Tests for `StatusTracker`, which maintains real-time agent state by consuming `SystemEvent` objects from the internal event bus. Covers state transitions from idle through thinking, tool execution, and error states, plus multi-session concurrency and token usage accumulation.
- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.
- [StreamEvent Processing Tests: Token-by-Token Streaming and Thinking Integration](./streamevent-processing-tests-token-by-token-streaming-and-thinking-integration.md) — Tests for PocketPaw's handling of `StreamEvent` objects from the Claude Agent SDK, covering text delta emission, thinking block lifecycle, tool-use start events, duplicate suppression, multi-turn state reset, and integration of thinking events into the AgentLoop's system event bus.
- [SystemInfoTool: System Status with Optional psutil Dependency](./systeminfotool-system-status-with-optional-psutil-dependency.md) — The `SystemInfoTool` reports CPU, RAM, disk, network, and process information to agents, with graceful degradation to platform-only info when `psutil` is not installed. Tests cover the basic output contract, psutil-dependent sections (network, processes), fallback behavior without psutil, error handling when `get_system_status` raises, and the tool's name and schema definition.
- [Task Skip Semantics: Dependency Unblocking and Project Completion](./task-skip-semantics-dependency-unblocking-and-project-completion.md) — Covers the test suite that validates how SKIPPED tasks interact with the dependency scheduler and project completion logic in PocketPaw's deep-work system. Ensures skipped tasks satisfy blocked_by conditions, contribute to project completion, and appear correctly in progress reporting.
- [Task Status Persistence Tests: Reproducing and Fixing a FastAPI Body vs Query Param Bug](./task-status-persistence-tests-reproducing-and-fixing-a-fastapi-body-vs-query-par.md) — Tests that reproduce and validate the fix for a bug where task status updates via the API did not persist — caused by FastAPI treating a bare `str` parameter as a query param while the frontend sent it as a JSON body. Also validates that `projectTasks` lists are updated alongside the main tasks list.
- [Telegram Channel Adapter Tests: Typing Indicators, Smart Buffering, and Voice Routing](./telegram-channel-adapter-tests-typing-indicators-smart-buffering-and-voice-routi.md) — Tests for PocketPaw's Telegram adapter covering typing indicator lifecycle, smart stream buffering with rate limiting, markdown formatting on flush, voice media routing, and the complete streaming cycle from first chunk to final message. The `python-telegram-bot` library is mocked as an optional dependency.
- [Telegram Channel API Tests — Configuration Status and Pairing State](./telegram-channel-api-tests-configuration-status-and-pairing-state.md) — This test module covers the two read-only Telegram status endpoints: `/api/v1/telegram/status` (reports whether a bot token and allowed user ID are configured) and `/api/v1/telegram/pairing-status` (reports live pairing state from a dashboard-level in-memory dict). Both endpoints inform the dashboard whether the Telegram channel is ready to receive messages.
- [Telegram Forum Topics Support: Chat ID Parsing and Session Key Isolation](./telegram-forum-topics-support-chat-id-parsing-and-session-key-isolation.md) — Tests for Telegram Group Topics (forum supergroups) support in the PocketPaw Telegram adapter, covering the `_parse_chat_id` utility that splits composite chat IDs into chat and topic components, and session key generation that ensures different forum topics produce different session keys.
- [Test Suite for A2A Protocol Live Smoke Tests](./test-suite-for-a2a-protocol-live-smoke-tests.md) — This script is a live, end-to-end smoke test suite for PocketPaw's A2A (Agent-to-Agent) protocol endpoints. It requires a running PocketPaw server with `POCKETPAW_A2A_ENABLED=true` and exercises every major A2A feature — agent card discovery, message sending, task management, streaming, cancellation, and error handling — reporting PASS/FAIL for each check.
- [Test Suite for Knowledge Ingestion: CFO Agent Creation and Document Upload Smoke Test](./test-suite-for-knowledge-ingestion-cfo-agent-creation-and-document-upload-smoke-.md) — A manual smoke test script that creates a CFO-persona agent via the REST API and ingests three fixture documents into its knowledge base. It lives in `scripts/` rather than `tests/` to keep it out of pytest collection and CI, and requires a running backend with a valid bearer token to execute.
- [Test Suite Root: PocketPaw Test Package Initialiser](./test-suite-root-pocketpaw-test-package-initialiser.md) — The top-level `tests/__init__.py` is a minimal package marker that makes the `tests/` directory a Python package, enabling pytest and other test runners to discover and import test modules using package-relative imports. Its content is intentionally minimal—a single comment—confirming its structural rather than executable role.
- [TextToSpeechTool: Multi-Provider TTS with Error Handling and Audio File Management](./texttospeechtool-multi-provider-tts-with-error-handling-and-audio-file-managemen.md) — The `TextToSpeechTool` converts text to speech using configurable providers (OpenAI TTS, ElevenLabs) and saves audio files to a managed directory. Tests cover the tool's definition (name, trust level, parameters), provider-specific error paths for missing API keys, unknown provider rejection, successful ElevenLabs synthesis, API error handling, the `synthesize_speech` helper, and the audio directory creation utility.
- [Tool Bridge: Multi-Backend Tool Adaptation and Policy Enforcement](./tool-bridge-multi-backend-tool-adaptation-and-policy-enforcement.md) — The tool bridge (`agents/tool_bridge.py`) adapts PocketPaw's internal tool registry for multiple AI backend SDKs — OpenAI Agents, Google ADK, Claude Agent SDK, and others — while enforcing tool policies and generating compact markdown instructions. Tests cover backend-specific tool exclusions, graceful import failure handling, OpenAI schema normalization, invoke callback error handling, and policy-filtered instruction generation.
- [Tool Filter: Relevance-Based Tool Pre-Selection for Context Window Management](./tool-filter-relevance-based-tool-pre-selection-for-context-window-management.md) — The tool filter (`tools/filter.py`) limits the number of tools injected into an LLM context window by scoring tools against the current user message and always pinning a core set. Tests cover passthrough below the cap, max-tools truncation, always-include pinning semantics, relevance scoring, empty message handling, pinned slot accounting, and the tokenizer helper.
- [Tool Policy System: Profile-Based Access Control and MCP Server Authorization](./tool-policy-system-profile-based-access-control-and-mcp-server-authorization.md) — The tool policy system (`tools/policy.py`) enforces which tools an agent instance is allowed to use, based on named profiles (`minimal`, `coding`, `full`), explicit allow/deny lists, and group references. Tests cover profile resolution, group expansion, allow/deny precedence, ToolRegistry integration, and MCP server authorization — including a critical fix for fail-closed behavior on unknown profile names.
- [Tool Protocol and Registry: Core Registration, Execution, and Filesystem Jail Tests](./tool-protocol-and-registry-core-registration-execution-and-filesystem-jail-tests.md) — These tests exercise the foundational `ToolRegistry` and `BaseTool` protocol, the `ShellTool`'s command-blocking and timeout behavior, and the filesystem tools' write/read/list operations within a file jail. The jail fixture pattern and dangerous-command blocking together form PocketPaw's first line of defense against unsafe tool invocations.
- [Tool Registry Validation: Required Parameter Guards and Audit Logging (Issue #793)](./tool-registry-validation-required-parameter-guards-and-audit-logging-issue-793.md) — These tests cover the fix for issue #793, where `ToolRegistry.execute` was accepting empty strings and whitespace-only values for required tool parameters, allowing agent-generated calls to silently bypass input validation. The fix adds strict pre-execution validation that rejects `None`, missing, empty, and whitespace-only values for string parameters marked as required, and emits an audit log entry on failure.
- [UnreadService List Unreads Calculation Tests](./unreadservice-list-unreads-calculation-tests.md) — This module tests UnreadService.list_unreads, which computes per-group unread message and mention counts for a user. Tests cover the caught-up state, partial reads, the fresh-user fallback, and the edge case where a ReadState row exists but has no recorded read position.
- [Update Check System: PyPI Version Polling, Caching, Release Notes, and Styled Notices](./update-check-system-pypi-version-polling-caching-release-notes-and-styled-notice.md) — The update check module polls PyPI for new PocketPaw versions, caches results to avoid repeated network calls, fetches release notes from GitHub, tracks which versions the user has already seen, and displays styled terminal notices. Tests cover version parsing, cache freshness/staleness, network error handling, corrupted cache recovery, async wrapping, TTY detection for notice suppression, and the version-seen tracking mechanism.
- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.
- [Upload Error Hierarchy Tests: Typed Exception Codes and Inheritance](./upload-error-hierarchy-tests-typed-exception-codes-and-inheritance.md) — This module validates the upload error hierarchy in `pocketpaw.uploads.errors`, ensuring all domain-specific exceptions inherit from `UploadError`, carry typed `code` attributes, and preserve error messages. These contracts allow API routes and clients to handle upload failures programmatically without string matching.
- [Upload Grant Signing Tests: HMAC Token Integrity and Expiry](./upload-grant-signing-tests-hmac-token-integrity-and-expiry.md) — This small but security-critical test module verifies the `sign_grant` / `verify_grant` pair in PocketPaw's upload subsystem, ensuring that download grants are cryptographically bound to a specific file ID, expire correctly, and cannot be forged or transplanted across files.
- [Upload Path Utility Tests: Normalization, Traversal Defense, Subpath Checks, and Join Safety](./upload-path-utility-tests-normalization-traversal-defense-subpath-checks-and-joi.md) — Exhaustive unit tests for the `ee.cloud.uploads.paths` module, which provides the safe path utilities used throughout the upload folder system. Tests cover normalization (canonicalization, dot-stripping, repeated-slash collapse), rejection of path traversal sequences and dangerous characters, subpath relationship checks, and safe join operations.
- [Upload Read Authorization Tests: Owner, Chat Member, Workspace Admin, and Existence-Hiding](./upload-read-authorization-tests-owner-chat-member-workspace-admin-and-existence-.md) — Tests for `EEUploadService._assert_can_read`, the read gate used by `stream`, `presigned_get`, and the `/download-url` and `/grant` endpoints. The parametrized suite verifies that the owner, chat members, and workspace admins can read while strangers receive `NotFound` (not `Forbidden`) — an existence-hiding design that prevents file-ID probing.
- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.
- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.
- [Upload Service Tests: Validation, Bulk Handling, and Streaming](./upload-service-tests-validation-bulk-handling-and-streaming.md) — This test suite exercises the `UploadService` layer in PocketPaw's file upload subsystem, covering single and bulk file ingestion, magic-byte MIME sniffing, path sanitisation, and ownership-gated streaming and deletion. It uses an in-memory `_FakeAdapter` to isolate tests from real storage backends while still exercising the full service contract.
- [Upload Test Fixtures: Isolated Filesystem Root, In-Memory MongoDB, and MongoFileStore](./upload-test-fixtures-isolated-filesystem-root-in-memory-mongodb-and-mongofilesto.md) — Shared pytest fixtures for the uploads test suite providing a per-test isolated filesystem root, an in-memory MongoDB instance via `mongomock-motor`, and a ready-to-use `MongoFileStore`. The `beanie_upload_db` fixture includes a workaround for a `mongomock-motor` incompatibility with Beanie's `list_collection_names` call.
- [Upload Tests Package Marker: Making the Uploads Test Directory a Python Package](./upload-tests-package-marker-making-the-uploads-test-directory-a-python-package.md) — An empty `__init__.py` that marks `tests/cloud/uploads/` as a Python package so pytest can discover and import test modules within it using absolute import paths. No logic is present; its presence is structural.
- [Uploads Test Conftest: Isolated Temporary Upload Root Fixture](./uploads-test-conftest-isolated-temporary-upload-root-fixture.md) — This conftest defines `tmp_upload_root`, a pytest fixture that provides an isolated temporary directory for each test in the uploads test suite. It prevents test pollution between file system tests by ensuring every test starts with a clean, empty upload root.
- [Uploads Test Package Initializer](./uploads-test-package-initializer.md) — This is an empty `__init__.py` that marks the `tests/uploads/` directory as a Python package, enabling pytest to discover and import tests within it using standard package-relative imports. No logic lives here; its presence is structural.
- [UploadsProvider Tests: Field Mapping, Mounts, RBAC, and Folder Listing](./uploadsprovider-tests-field-mapping-mounts-rbac-and-folder-listing.md) — This module tests the UploadsProvider, which surfaces user file uploads as a virtual file tree with folder hierarchy support. Tests cover field mapping from the raw upload store format to FileEntry, mount resolution, baseline RBAC (owner vs. non-owner vs. admin), and the combined folder+file listing behavior.
- [UrlExtractTool: SSRF Defense, DNS Pinning, Private IP Blocking, and Multi-Provider Extraction](./urlextracttool-ssrf-defense-dns-pinning-private-ip-blocking-and-multi-provider-e.md) — The `UrlExtractTool` fetches and extracts text from URLs, supporting Parallel AI (cloud) and local httpx (self-hosted) providers with auto-selection. The test suite places particular emphasis on SSRF (Server-Side Request Forgery) defenses: private IP blocking, DNS TOCTOU race condition prevention via IP pinning, and IPv4-mapped IPv6 address rejection.
- [UsageTracker: Cached Token Counting Fix and Unbounded Summary Aggregation](./usagetracker-cached-token-counting-fix-and-unbounded-summary-aggregation.md) — These tests document and verify two bug fixes in `UsageTracker`: cached input tokens were silently excluded from `total_tokens`, and `get_summary()` was capped at 10,000 records, causing understated lifetime aggregation totals for high-volume installations. Both fixes ensure accurate cost accounting across all Anthropic and OpenAI backends.
- [User Profile Bootstrap: USER.md Integration with BootstrapContext and System Prompt Injection](./user-profile-bootstrap-usermd-integration-with-bootstrapcontext-and-system-promp.md) — The bootstrap system supports a `USER.md` file that stores human-readable user profile information (name, timezone, preferences) which is loaded into `BootstrapContext` and injected into the agent's system prompt under a `# User Profile` section. Tests verify the default empty state, template creation, idempotent non-overwrite, loading, system prompt injection, and graceful handling of a missing file.
- [V1 Router Registration and CORS Configuration Tests](./v1-router-registration-and-cors-configuration-tests.md) — Validates that all expected domain routers are registered in PocketPaw's v1 API surface and that CORS origins include the Tauri desktop app origin. These tests act as a registry contract — any new router added to the system must be explicitly listed, preventing silent omissions that would cause 404s from newly added features.
- [Wave 2 Shared Test Fixtures: Workspace Factory, User Token, Seeded Channel, and Mock S3 Smoke Tests](./wave-2-shared-test-fixtures-workspace-factory-user-token-seeded-channel-and-mock.md) — Smoke tests that validate the four shared pytest fixtures defined in `tests/ee/conftest.py` for the Wave 2 ee/cloud test infrastructure. Each test exercises exactly one factory (workspace creation, user authentication, channel seeding, and in-memory S3), proving the conftest wiring works end-to-end before the feature-level tests that depend on it are run.
- [Web Server Security Tests: Bot Token Redaction in /setup Errors](./web-server-security-tests-bot-token-redaction-in-setup-errors.md) — This module tests that the `/setup` endpoint in PocketPaw's web server never leaks the Telegram bot token in HTTP error responses (F-07, issue #445). It verifies that when the upstream Telegram API returns an error whose message embeds the token URL, the response is redacted before reaching the client.
- [Webhook Adapter Test Suite: Inbound Payload Handling, Sync Mode, and Send Routing](./webhook-adapter-test-suite-inbound-payload-handling-sync-mode-and-send-routing.md) — This module tests the `WebhookAdapter`, PocketPaw's generic inbound HTTP webhook channel. It covers slot configuration, payload normalization, sync vs. async dispatch modes, future-based response resolution, and streaming accumulation.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.
- [Webhooks API Tests — CRUD, Secret Redaction, Duplicate Prevention, and Secret Rotation](./webhooks-api-tests-crud-secret-redaction-duplicate-prevention-and-secret-rotatio.md) — This test module covers the full lifecycle of PocketPaw's webhook configuration API: listing (with secret redaction), adding (with name validation and duplicate prevention), removing, and regenerating shared secrets. Webhooks enable external services to trigger agent actions, and the tests enforce that secrets are never exposed at rest through the API listing endpoint.
- [WebSearchTool Test Suite: Multi-Provider Search Validation](./websearchtool-test-suite-multi-provider-search-validation.md) — This test module validates the `WebSearchTool` built-in tool in PocketPaw, covering provider dispatch, API key guards, HTTP error handling, result clamping, and the parallel-search variant. It ensures the tool behaves safely and predictably across Tavily, Brave, and Parallel AI backends.
- [WebSocket Connection Manager Tests: Multi-Device, Typing Indicators, and Offline Grace Periods](./websocket-connection-manager-tests-multi-device-typing-indicators-and-offline-gr.md) — Tests for `ee.cloud.chat.ws.ConnectionManager`, the in-process registry that tracks active WebSocket connections per user, routes outbound messages, and manages typing indicators with auto-expiry timers. Key behavioral contracts include dead-connection cleanup, multi-device fan-out, idempotent stop-typing, and cancellation of offline grace period tasks on reconnect.
- [WhatsApp Adapter Test Suite: Webhook Verification, Lifecycle, and Message Sending](./whatsapp-adapter-test-suite-webhook-verification-lifecycle-and-message-sending.md) — This module tests PocketPaw's `WhatsAppAdapter`, covering the Meta webhook verification handshake, adapter lifecycle (`start`/`stop`), outbound message delivery, allowed-number filtering, inbound message routing, and media handling. It ensures the adapter correctly implements the WhatsApp Cloud API protocol.
- [Widget Co-occurrence Accept/Dismiss Endpoint Tests: Route Separation, Validation, and Signature Echo](./widget-co-occurrence-acceptdismiss-endpoint-tests-route-separation-validation-an.md) — Tests the two writer endpoints (`POST /widgets/cooccurrence/accept` and `POST /widgets/cooccurrence/dismiss`) that record user decisions on co-occurring widget suggestions from the SuggestedWidgetsFeed. Pins happy-path response shape, Pydantic validation gates, scope fallback to `["org:*"]`, strict route separation between the two event types, and a signature-echo contract that prevents feed state corruption if the tokenisation rule changes.
- [Widget Journal Projection Tests: Interactions, Graduation, Co-occurrence Signature Fix, and Router](./widget-journal-projection-tests-interactions-graduation-co-occurrence-signature-.md) — Comprehensive tests for the widget journal projection subsystem covering the write path for interactions, graduations, and co-occurrence events; scope containment in usage roll-ups; the pin-threshold graduation policy; a regression guard for the `sorted(tokens[:6])` co-occurrence signature bug from PR #942; archive rules for inactive widgets; and the three GET router endpoints.
- [Widget Track Endpoint Tests: Interaction Recording, Validation, Scope Fallback, and Projection Integration](./widget-track-endpoint-tests-interaction-recording-validation-scope-fallback-and-.md) — Tests for `POST /widgets/track`, the writer endpoint that records widget interactions from the SuggestedWidgetsFeed UI. Covers the full happy-path event emission, Pydantic validation of required fields, metadata defaulting, correlation-id propagation, sequential seq increments, the `org:*` scope fallback for anonymous actors, and an end-to-end proof that three POSTs reflect correctly in the `GET /widgets/usage` projection.
- [Workspace Cloud Test Package Initializer](./workspace-cloud-test-package-initializer.md) — This empty `__init__.py` marks the `tests/cloud/workspace/` directory as a Python package, enabling pytest to discover and import test modules within it using standard package import resolution. No code lives here; the file's presence is its entire purpose.
- [Workspace KB Aggregator: Merge, Filter, Dedup, Sort, and Resilience Tests](./workspace-kb-aggregator-merge-filter-dedup-sort-and-resilience-tests.md) — This test suite validates the `ee.cloud.kb.workspace_aggregator` module, which merges knowledge base articles from workspace and agent scopes into a single sorted, deduplicated list. It tests merge logic, agent filtering, deduplication by (scope, id), newest-first ordering, resilience when a scope returns malformed data, and the `AggregatedArticle.to_dict()` shape.
- [Workspace Message Search Tests](./workspace-message-search-tests.md) — This test module covers MessageService's workspace-wide message search, verifying that results respect group visibility (channels vs. private rooms), workspace scope isolation, regex-safety of user queries, and server-side result limits. Tests run against an in-memory MongoDB via the shared beanie_memory_db fixture to exercise real Mongo query behavior.
- [Workspace Schema Tests: Slug Validation, Invite Rules, and Member Role Contracts](./workspace-schema-tests-slug-validation-invite-rules-and-member-role-contracts.md) — Tests for the workspace domain Pydantic schemas in PocketPaw's EE layer, covering slug format enforcement, invite role validation, and member role update rules. The slug tests are particularly important because workspace slugs appear in URLs and must be lowercase, hyphen-separated, and free of leading/trailing hyphens.
- [WorkspaceService Realtime Event Emission and Cache Invalidation Tests](./workspaceservice-realtime-event-emission-and-cache-invalidation-tests.md) — This suite verifies that every mutating `WorkspaceService` method fires the correct typed event through `emit()`, and that membership or admin list changes also invalidate the `AudienceResolver` workspace cache. All database and permission primitives are patched at their seams, isolating the emit behavior from persistence concerns.

### `testing infrastructure` — 1 article(s)

- [Singleton Lifecycle Management: Coordinated Shutdown and Test Reset](./singleton-lifecycle-management-coordinated-shutdown-and-test-reset.md) — The lifecycle module provides a central registry for singletons that need graceful shutdown on app teardown and deterministic reset between test runs. It decouples teardown orchestration from individual module implementations by letting each module register its own callbacks.

### `text-to-speech` — 1 article(s)

- [TextToSpeechTool: Multi-Provider TTS with Auto-Loop Integration and Media Tag Protocol](./texttospeechtool-multi-provider-tts-with-auto-loop-integration-and-media-tag-pro.md) — `TextToSpeechTool` converts text to audio via OpenAI TTS, ElevenLabs, or Sarvam AI Bulbul, saving output to `~/.pocketpaw/generated/audio/` with UUID filenames. A standalone `synthesize_speech()` helper and a `<!-- media:path -->` HTML comment protocol enable the agent loop to auto-play TTS responses without parsing tool output.

### `third-party integrations` — 1 article(s)

- [Spotify Tool Tests: Schema Validation, Auth Guards, and Playback Control](./spotify-tool-tests-schema-validation-auth-guards-and-playback-control.md) — Tests for PocketPaw's Spotify integration tools covering schema correctness, authentication failure handling, playback action validation, and successful API interactions. Ensures the tools surface clear errors when credentials are absent and route API calls correctly when authenticated.

### `tool bridge` — 1 article(s)

- [Headless Integration Tests: Server Startup, Tool Bridge, and Execution Timeouts](./headless-integration-tests-server-startup-tool-bridge-and-execution-timeouts.md) — This integration test module verifies the full headless stack from FastAPI router mounting through tool bridge availability across all agent backends, enforcing that memory tools are never excluded, that `bypassPermissions` is set unconditionally, and that individual tool calls complete within a 5-second timeout that catches the historic permission hang bug.

### `tool integration` — 1 article(s)

- [Copilot SDK Backend Tests: Capabilities, Provider Config, Session Management, and Tool Injection](./copilot-sdk-backend-tests-capabilities-provider-config-session-management-and-to.md) — This test module validates the `CopilotSDKBackend` — PocketPaw's adapter for GitHub Copilot's agent SDK — entirely through mocks, requiring no real CLI binary or SDK installation. It covers static capability metadata, provider-specific configuration dispatch, multi-turn session reuse, event stream mapping, and the injection of PocketPaw tool instructions into the Copilot prompt.

### `tool policy` — 1 article(s)

- [Scope Enforcement and Tool Policy Fail-Closed Tests](./scope-enforcement-and-tool-policy-fail-closed-tests.md) — This test module validates that PocketPaw's `require_scope` FastAPI dependency rejects requests that lack explicit authorization markers, preventing silent scope bypass via master, session, or cookie auth. It also verifies that `ToolPolicy` raises on unknown profile names rather than defaulting to an open or undefined state.

### `tool system` — 2 article(s)

- [Pocket Tool Tests: Dashboard Widget Creation, Legacy Conversion, and UI Spec Generation](./pocket-tool-tests-dashboard-widget-creation-legacy-conversion-and-ui-spec-genera.md) — The pocket tool system lets AI agents create and modify PocketPaw dashboard "pockets" — widget-based layouts containing metrics, charts, tables, and feeds. These tests validate the `create_pocket`, `add_widget`, and `remove_widget` tools, including backward-compatible legacy widget conversion, the `ui` parameter for pre-built specs, and the JSON Schema definitions used by LLM providers.
- [Reddit Tool Tests: Search, Read, Trending, and Post Formatting](./reddit-tool-tests-search-read-trending-and-post-formatting.md) — PocketPaw's Reddit integration provides agents with three tools for interacting with Reddit: `reddit_search`, `reddit_read`, and `reddit_trending`. These tests validate tool schemas and trust levels, the `_format_post` helper for normalizing Reddit API responses, and async tool execution covering success, empty results, and error conditions.

### `tool-management` — 1 article(s)

- [MCP Server Management Schemas](./mcp-server-management-schemas.md) — Defines the Pydantic request models for managing Model Context Protocol (MCP) servers in PocketPaw — adding servers by configuration, referencing them by name, test-connecting before saving, and installing from a curated preset library. These schemas expose the full MCP lifecycle through the REST API.

### `tool-system` — 21 article(s)

- [A2ADelegateTool: Secure Cross-Agent Task Delegation via the A2A Protocol](./a2adelegatetool-secure-cross-agent-task-delegation-via-the-a2a-protocol.md) — `A2ADelegateTool` allows PocketPaw's agent to delegate tasks to any external agent that speaks the Agent-to-Agent (A2A) protocol, with built-in SSRF protection that blocks requests to private IP ranges. It bridges PocketPaw's internal tool system to the open A2A agent network.
- [BrowserTool: Playwright-Powered Web Automation with Accessibility Snapshots](./browsertool-playwright-powered-web-automation-with-accessibility-snapshots.md) — `BrowserTool` exposes browser automation to PocketPaw's agent via a Playwright-backed session manager, using accessibility tree snapshots (rather than raw HTML or pixel screenshots) for element identification — a technique that dramatically reduces token usage while maintaining LLM-parseable page structure.
- [Builtin Tools Lazy Loader: Optional Dependency Safety via __getattr__](./builtin-tools-lazy-loader-optional-dependency-safety-via-getattr.md) — The `pocketpaw/tools/builtin/__init__.py` implements a lazy import scheme using Python's module-level `__getattr__` hook, ensuring that importing one built-in tool never fails due to a missing optional dependency of a different tool. This lets PocketPaw operate with a minimal install footprint while still exposing a rich tool catalogue.
- [Connector Tools: Agent Interface for the Dynamic Connector Registry](./connector-tools-agent-interface-for-the-dynamic-connector-registry.md) — The connector tools module wires PocketPaw's `ConnectorRegistry` into the agent's tool interface, giving the LLM the ability to discover available data connectors, establish connections, execute connector actions, and inspect what actions a connector supports — all through natural language tool calls.
- [Core PocketPaw Tools: Status, Fetch, Screenshot, Config, and Router Tests](./core-pocketpaw-tools-status-fetch-screenshot-config-and-router-tests.md) — This test module provides broad coverage of PocketPaw's foundational tool and configuration layer, including the system status tool, the file fetch tool's path jail enforcement, the screenshot tool's optional dependency handling, the settings save/load lifecycle, and both the LLM and agent routers' initialization and error paths.
- [Deep Agents Tool Bridge: LangChain Integration and Graceful Degradation](./deep-agents-tool-bridge-langchain-integration-and-graceful-degradation.md) — These tests cover the `build_deep_agents_tools` bridge function which adapts PocketPaw tools for LangChain-based Deep Agents backends. The key behavior tested is that the function returns an empty list gracefully when `langchain-core` is not installed, and that the Deep Agents backend is not subject to the same tool exclusions as the Claude Agent SDK.
- [DelegateToClaudeCodeTool: Critical-Trust Agent-to-Agent Delegation via CLI](./delegatetoclaudecodetool-critical-trust-agent-to-agent-delegation-via-cli.md) — `DelegateToClaudeCodeTool` gives PocketPaw's agent the ability to hand off complex coding tasks to Claude Code CLI, the Anthropic autonomous coding agent. It is marked `critical` trust — the highest level in PocketPaw's trust hierarchy — because Claude Code has unrestricted filesystem, shell, and web access.
- [DeliverArtifactTool: Secure File Delivery from Agent Workspace to User Channel](./deliverartifacttool-secure-file-delivery-from-agent-workspace-to-user-channel.md) — `DeliverArtifactTool` lets PocketPaw's agent send generated or downloaded files (images, PDFs, audio) to the user through their current channel — Slack, Discord, or the web dashboard — after verifying the file is within the agent's sandboxed working directory.
- [Desktop Tools: Screenshot Capture and System Status for Local Agent Sessions](./desktop-tools-screenshot-capture-and-system-status-for-local-agent-sessions.md) — `ScreenshotTool` and `StatusTool` are lightweight desktop interaction tools that give PocketPaw's agent visibility into the local machine — capturing the screen for visual context and querying system health metrics. Both tools are designed for local deployments where the agent runs on the same machine as the user.
- [DirectoryTreeTool: File System Tree Visualization with Jail, Depth, and Security Controls](./directorytreetool-file-system-tree-visualization-with-jail-depth-and-security-co.md) — The `DirectoryTreeTool` renders an ASCII directory tree within a file jail, with configurable depth, hidden file visibility, file size display, and entry count truncation. Tests cover basic tree rendering, depth limiting, hidden file exclusion/inclusion, size annotation, edge cases (empty dir, nonexistent path, file-not-directory), security (jail break, prefix bypass, symlink skipping), truncation, and the tool's schema definition.
- [DiscordCLITool: Full Discord Management via the discli CLI Subprocess](./discordclitool-full-discord-management-via-the-discli-cli-subprocess.md) — `DiscordCLITool` wraps the `discli` command-line tool to give PocketPaw's agent comprehensive Discord server management capabilities — messaging, channel management, moderation, polls, webhooks, and more — all through a single tool that constructs safe subprocess invocations with shell-split argument parsing.
- [Google Calendar Tools: Event Listing, Creation, and Meeting Preparation](./google-calendar-tools-event-listing-creation-and-meeting-preparation.md) — The `calendar.py` module provides three `BaseTool` implementations — `CalendarListTool`, `CalendarCreateTool`, and `CalendarPrepTool` — that give PocketPaw's agent read/write access to Google Calendar and the ability to generate pre-meeting briefings. These tools were part of the Phase 2 Integration Ecosystem and require an authenticated Google OAuth session.
- [SystemInfoTool: System Status with Optional psutil Dependency](./systeminfotool-system-status-with-optional-psutil-dependency.md) — The `SystemInfoTool` reports CPU, RAM, disk, network, and process information to agents, with graceful degradation to platform-only info when `psutil` is not installed. Tests cover the basic output contract, psutil-dependent sections (network, processes), fallback behavior without psutil, error handling when `get_system_status` raises, and the tool's name and schema definition.
- [TextToSpeechTool: Multi-Provider TTS with Error Handling and Audio File Management](./texttospeechtool-multi-provider-tts-with-error-handling-and-audio-file-managemen.md) — The `TextToSpeechTool` converts text to speech using configurable providers (OpenAI TTS, ElevenLabs) and saves audio files to a managed directory. Tests cover the tool's definition (name, trust level, parameters), provider-specific error paths for missing API keys, unknown provider rejection, successful ElevenLabs synthesis, API error handling, the `synthesize_speech` helper, and the audio directory creation utility.
- [Tool Bridge: Multi-Backend Tool Adaptation and Policy Enforcement](./tool-bridge-multi-backend-tool-adaptation-and-policy-enforcement.md) — The tool bridge (`agents/tool_bridge.py`) adapts PocketPaw's internal tool registry for multiple AI backend SDKs — OpenAI Agents, Google ADK, Claude Agent SDK, and others — while enforcing tool policies and generating compact markdown instructions. Tests cover backend-specific tool exclusions, graceful import failure handling, OpenAI schema normalization, invoke callback error handling, and policy-filtered instruction generation.
- [Tool Filter: Relevance-Based Tool Pre-Selection for Context Window Management](./tool-filter-relevance-based-tool-pre-selection-for-context-window-management.md) — The tool filter (`tools/filter.py`) limits the number of tools injected into an LLM context window by scoring tools against the current user message and always pinning a core set. Tests cover passthrough below the cap, max-tools truncation, always-include pinning semantics, relevance scoring, empty message handling, pinned slot accounting, and the tokenizer helper.
- [Tool Policy System: Profile-Based Access Control and MCP Server Authorization](./tool-policy-system-profile-based-access-control-and-mcp-server-authorization.md) — The tool policy system (`tools/policy.py`) enforces which tools an agent instance is allowed to use, based on named profiles (`minimal`, `coding`, `full`), explicit allow/deny lists, and group references. Tests cover profile resolution, group expansion, allow/deny precedence, ToolRegistry integration, and MCP server authorization — including a critical fix for fail-closed behavior on unknown profile names.
- [Tool Protocol and Registry: Core Registration, Execution, and Filesystem Jail Tests](./tool-protocol-and-registry-core-registration-execution-and-filesystem-jail-tests.md) — These tests exercise the foundational `ToolRegistry` and `BaseTool` protocol, the `ShellTool`'s command-blocking and timeout behavior, and the filesystem tools' write/read/list operations within a file jail. The jail fixture pattern and dangerous-command blocking together form PocketPaw's first line of defense against unsafe tool invocations.
- [Tool Registry Validation: Required Parameter Guards and Audit Logging (Issue #793)](./tool-registry-validation-required-parameter-guards-and-audit-logging-issue-793.md) — These tests cover the fix for issue #793, where `ToolRegistry.execute` was accepting empty strings and whitespace-only values for required tool parameters, allowing agent-generated calls to silently bypass input validation. The fix adds strict pre-execution validation that rejects `None`, missing, empty, and whitespace-only values for string parameters marked as required, and emits an audit log entry on failure.
- [Tools Package Public API: Protocol, Registry, and Policy Exports](./tools-package-public-api-protocol-registry-and-policy-exports.md) — The `pocketpaw/tools/__init__.py` re-exports the three foundational primitives of PocketPaw's tool system: the `ToolProtocol`/`BaseTool` interface, the `ToolRegistry`, and the `ToolPolicy` with its grouping constants. This single import surface means tool consumers never need to know the internal module layout.
- [UrlExtractTool: SSRF Defense, DNS Pinning, Private IP Blocking, and Multi-Provider Extraction](./urlextracttool-ssrf-defense-dns-pinning-private-ip-blocking-and-multi-provider-e.md) — The `UrlExtractTool` fetches and extracts text from URLs, supporting Parallel AI (cloud) and local httpx (self-hosted) providers with auto-selection. The test suite places particular emphasis on SSRF (Server-Side Request Forgery) defenses: private IP blocking, DNS TOCTOU race condition prevention via IP pinning, and IPv4-mapped IPv6 address rejection.

### `tool-use` — 1 article(s)

- [Plan Mode — Human-in-the-Loop Approval Flow for Agent Tool Execution](./plan-mode-human-in-the-loop-approval-flow-for-agent-tool-execution.md) — Implements `PlanManager` and `ExecutionPlan`, which intercept agent tool calls and hold them pending user approval before execution. Plans have a per-session lifecycle with timeout-based auto-rejection, providing an auditable gate between agent intent and system-modifying actions.

### `tooling` — 3 article(s)

- [Audit Logging Verification Script — Manual Smoke Test for Tool Execution Audit Trail](./audit-logging-verification-script-manual-smoke-test-for-tool-execution-audit-tra.md) — This standalone script provides a manual, human-readable verification that the audit logging pipeline records both `attempt` and `success` entries when a tool is executed through the ToolRegistry. It runs outside the pytest harness and is designed to be invoked directly to confirm audit infrastructure is wired correctly in a live environment.
- [Guardian Agent Verification Script — Manual Safety Layer Smoke Test for ShellTool](./guardian-agent-verification-script-manual-safety-layer-smoke-test-for-shelltool.md) — This standalone script manually validates that the Guardian safety layer correctly permits safe shell commands while blocking dangerous ones, providing a human-readable pass/fail report for the tool permission model. It distinguishes between two blocking mechanisms — regex-based pre-screening in ShellTool and the Guardian agent's semantic analysis — to help developers diagnose which layer is active.
- [Recent Files Tracker: Agent Tool Usage History](./recent-files-tracker-agent-tool-usage-history.md) — RecentFilesTracker intercepts agent tool calls to extract file paths and maintains a capped LRU list of the 50 most recently accessed files with metadata. It uses lazy loading, a heuristic Bash path extractor via regex, and JSON persistence to power the dashboard's recent files widget.

### `tools` — 38 article(s)

- [Agent Delegation: ExternalAgentDelegate and DelegateTool Tests](./agent-delegation-externalagentdelegate-and-delegatetool-tests.md) — This suite tests PocketPaw's agent delegation system, which allows a running agent to hand off subtasks to external CLI-based agents (initially Claude Code). It covers the DelegationResult dataclass, availability detection via PATH lookup, unknown agent rejection, and the DelegateTool's trust level, parameter schema, and execution paths including error handling for missing installations.
- [BrowserTool Integration Tests: Agent-Facing Browser Interface, Actions, Error Handling, and Session Management](./browsertool-integration-tests-agent-facing-browser-interface-actions-error-handl.md) — This test suite validates `BrowserTool`, the agent-facing wrapper that exposes browser automation as a single tool with an action-dispatch interface. Tests cover the tool's metadata contract (name, trust level, JSON schema), all seven supported actions (navigate, click, type, scroll, snapshot, screenshot, close), input validation, error propagation, and multi-session isolation.
- [CreateSkillTool: Runtime Skill Generation with SKILL.md and Memory Persistence](./createskilltool-runtime-skill-generation-with-skillmd-and-memory-persistence.md) — `CreateSkillTool` lets agents create new reusable skills at runtime by writing SKILL.md files with YAML frontmatter to `~/.claude/skills/`. After creation, it reloads the skill loader and persists the skill's description to long-term memory so the agent remembers it across sessions.
- [CreateSkillTool Tests — Skill Generation, Naming Validation, and YAML Frontmatter](./createskilltool-tests-skill-generation-naming-validation-and-yaml-frontmatter.md) — This test file validates `CreateSkillTool`, which allows agents to generate new PocketPaw skill files (SKILL.md) at runtime. Tests cover tool metadata, skill name regex validation, YAML frontmatter format, overwrite protection, skill loader reload triggering, and the allowed-tools and user-invocable flags.
- [DeliverArtifactTool: File Delivery, MIME Detection, and File Jail Security Tests](./deliverartifacttool-file-delivery-mime-detection-and-file-jail-security-tests.md) — This suite tests DeliverArtifactTool, which allows PocketPaw agents to deliver files (documents, images, videos) to user-facing channels by embedding them as media tags in the response. It covers basic delivery, optional captions, MIME type detection for images and video, error cases for missing files and directories, and the file jail security boundary that prevents agents from delivering arbitrary files from outside the allowed directory.
- [DirectoryTreeTool: Jail-Bounded Recursive Directory Listing with Entry Cap](./directorytreetool-jail-bounded-recursive-directory-listing-with-entry-cap.md) — `DirectoryTreeTool` generates a `tree`-style directory listing with configurable depth, hidden-file filtering, and optional file sizes — all bounded by the `file_jail_path` and a hard `MAX_ENTRIES = 500` cap. Path resolution uses `is_relative_to()` to prevent traversal outside the allowed root.
- [EditFileTool: Find-and-Replace File Editing with Jail Security Tests](./editfiletool-find-and-replace-file-editing-with-jail-security-tests.md) — This suite tests EditFileTool, a built-in agent tool that performs find-and-replace edits on files within a sandboxed jail directory. It covers successful edits, missing file errors, old_string-not-found errors, multiple-occurrence ambiguity errors, and the file jail security boundary that prevents agents from editing files outside their allowed working area.
- [fetch.py: Jail-Safe File Browser with Pydantic Validation and Telegram Keyboard Support](./fetchpy-jail-safe-file-browser-with-pydantic-validation-and-telegram-keyboard-su.md) — `fetch.py` provides path-traversal-safe file browsing via `is_safe_path()`, a `FetchRequest` Pydantic model, and `handle_path()` / `list_directory()` helpers. The Telegram `InlineKeyboardMarkup` import is optional, enabling interactive directory navigation buttons in Telegram without breaking non-Telegram deployments.
- [Gmail Integration Body Extraction and Tool Tests](./gmail-integration-body-extraction-and-tool-tests.md) — This test module validates `GmailClient._extract_body`, which parses Gmail's MIME structure to extract plain text from messages, and the three Gmail tools (`GmailSearchTool`, `GmailReadTool`, `GmailSendTool`) — checking their schemas and verifying that each returns a user-readable error when OAuth authentication is absent.
- [Google Calendar Tool Schema and Auth Error Tests](./google-calendar-tool-schema-and-auth-error-tests.md) — This test module validates the three Google Calendar tools (`CalendarListTool`, `CalendarCreateTool`, `CalendarPrepTool`) — checking their names, trust levels, and required parameter schemas — and verifies that each tool returns a user-readable error message rather than propagating an exception when OAuth authentication is unavailable.
- [Google Docs Integration Tool Schema and Execution Tests](./google-docs-integration-tool-schema-and-execution-tests.md) — This test module validates the three Google Docs tools (`DocsReadTool`, `DocsCreateTool`, `DocsSearchTool`), the `_parse_doc_id` URL-parsing utility, and the `DocsClient._extract_text` method that converts the Docs API's nested JSON structure into plain text. It also confirms that each tool returns actionable error messages when OAuth authentication is absent.
- [Google Drive Integration Tool Schema and Execution Tests](./google-drive-integration-tool-schema-and-execution-tests.md) — This test module validates the four Google Drive tools (`DriveListTool`, `DriveDownloadTool`, `DriveUploadTool`, `DriveShareTool`), their parameter schemas, and their behavior when authentication is absent or inputs are invalid. It also verifies `DriveListTool`'s happy path with a mocked HTTP response and confirms that `DriveShareTool` rejects invalid sharing roles at the tool layer.
- [Paw Soul Tools: BaseTool Wrappers for Soul-Protocol Operations](./paw-soul-tools-basetool-wrappers-for-soul-protocol-operations.md) — tools.py defines nine BaseTool subclasses that expose soul-protocol's memory and identity operations as agent-callable tools, including remember, recall, edit core memory, status introspection, rubric-based self-evaluation, hot reload from disk, selective forgetting, and topic-aware context retrieval.
- [Reddit Tools: Search, Read Posts, and Surface Trending Content](./reddit-tools-search-read-posts-and-surface-trending-content.md) — PocketPaw ships three Reddit tools — `reddit_search`, `reddit_read`, and `reddit_trending` — that give agents access to Reddit content without requiring an API key. They delegate to a shared `RedditClient` integration and return richly formatted markdown text suitable for direct LLM consumption.
- [ResearchTool: Multi-Step Web Research Pipeline with LLM Summarization](./researchtool-multi-step-web-research-pipeline-with-llm-summarization.md) — `ResearchTool` orchestrates a four-stage pipeline — web search, URL discovery, content extraction, and LLM summarization — into a single `research` tool call. Depth levels let callers trade speed against thoroughness, and an optional memory-save step persists findings across conversations.
- [ResearchTool Test Suite — Web Search, URL Extraction, and Happy Path](./researchtool-test-suite-web-search-url-extraction-and-happy-path.md) — This test file validates `ResearchTool`, PocketPaw's built-in agentic research capability that chains web search, URL content extraction, and LLM summarization. Tests cover tool metadata, URL parsing edge cases, search failure handling, and the full happy-path pipeline using mocked external dependencies.
- [RunPythonTool Test Suite — Sandboxed Code Execution with Guardian and File Jail](./runpythontool-test-suite-sandboxed-code-execution-with-guardian-and-file-jail.md) — This test file covers `RunPythonTool`, which executes arbitrary Python code in a sandboxed subprocess controlled by a file jail and a guardian policy engine. Tests validate stdout capture, stderr surfacing, exit code reporting, timeout enforcement, guardian blocking, filesystem isolation, temp file cleanup, and tool metadata.
- [Screenshot Tool: Optional Desktop Capture with Graceful Headless Fallback](./screenshot-tool-optional-desktop-capture-with-graceful-headless-fallback.md) — The screenshot module provides a single function for capturing the current display as PNG bytes, using pyautogui when available and returning an actionable error string when it is not. The optional-import pattern ensures PocketPaw's core runtime does not require a GUI dependency, keeping server and headless deployments working without modification.
- [Session Management Tool Tests — New, List, Switch, Clear, Rename, and Delete](./session-management-tool-tests-new-list-switch-clear-rename-and-delete.md) — This test file validates the six session management tools in `pocketpaw.tools.builtin.sessions`: `NewSessionTool`, `ListSessionsTool`, `SwitchSessionTool`, `ClearSessionTool`, `RenameSessionTool`, and `DeleteSessionTool`. Tests cover tool naming, happy-path behavior, error handling, and policy group membership.
- [Session Management Tools: LLM-Driven Conversation Branching](./session-management-tools-llm-driven-conversation-branching.md) — Six `BaseTool` subclasses expose session lifecycle operations — create, list, switch, clear, rename, delete — as agent-callable tools, replacing an earlier regex-based NL detection approach. The LLM decides when to invoke them based on conversational context, making session control robust to any phrasing.
- [ShellTool: Sandboxed Shell Command Execution with Guardian Approval](./shelltool-sandboxed-shell-command-execution-with-guardian-approval.md) — `ShellTool` wraps `asyncio.subprocess` with two security layers — a precompiled dangerous-pattern regex rail and a Guardian Agent check — before executing any shell command. It operates at `critical` trust level, the highest in PocketPaw's trust hierarchy, and defaults to a jailed working directory.
- [SpeechToTextTool: Multi-Provider Audio Transcription with Path Jail Enforcement](./speechtotexttool-multi-provider-audio-transcription-with-path-jail-enforcement.md) — `SpeechToTextTool` transcribes audio files to text via OpenAI Whisper, ElevenLabs, or Sarvam AI Saaras, with automatic provider selection from settings. A path-safety check (`is_safe_path`) prevents directory traversal attacks before any file is read, and transcripts are written to a versioned output directory.
- [Spotify Tools: Search, Now Playing, Playback Control, and Playlist Management](./spotify-tools-search-now-playing-playback-control-and-playlist-management.md) — Four `BaseTool` subclasses — `spotify_search`, `spotify_now_playing`, `spotify_playback`, and `spotify_playlist` — wrap `SpotifyClient` to give agents full Spotify control: catalog search, current track status, transport controls, and playlist operations. All raise `RuntimeError` for auth failures, which surfaces as a clean error rather than a stack trace.
- [System Status Tool: Real-Time Hardware Metrics with psutil Fallback](./system-status-tool-real-time-hardware-metrics-with-psutil-fallback.md) — The system status tool collects CPU, memory, disk, battery, and uptime metrics using psutil and formats them as a human-readable markdown string for display in the agent UI. When psutil is not installed, it falls back to a limited report using the standard library `platform` module, preserving basic utility without requiring the desktop extras.
- [SystemInfoTool: Host Resource Monitoring with Optional psutil Integration](./systeminfotool-host-resource-monitoring-with-optional-psutil-integration.md) — `SystemInfoTool` reports CPU, RAM, disk, network I/O, and optionally top processes by delegating to `get_system_status()` for the base snapshot, then augmenting with `psutil` data when available. The psutil dependency is treated as optional — the tool degrades gracefully to the base snapshot if psutil is not installed.
- [TextToSpeechTool: Multi-Provider TTS with Auto-Loop Integration and Media Tag Protocol](./texttospeechtool-multi-provider-tts-with-auto-loop-integration-and-media-tag-pro.md) — `TextToSpeechTool` converts text to audio via OpenAI TTS, ElevenLabs, or Sarvam AI Bulbul, saving output to `~/.pocketpaw/generated/audio/` with UUID filenames. A standalone `synthesize_speech()` helper and a `<!-- media:path -->` HTML comment protocol enable the agent loop to auto-play TTS responses without parsing tool output.
- [Tool Bridge: Adapting PocketPaw Tools for Multiple Agent SDK Formats](./tool-bridge-adapting-pocketpaw-tools-for-multiple-agent-sdk-formats.md) — The tool bridge discovers all registered PocketPaw tools and wraps them in the native function-tool format expected by each agent backend (OpenAI Agents SDK `FunctionTool`, Google ADK `FunctionTool`, LangChain `StructuredTool`). It enforces backend-aware exclusion rules — the Claude Agent SDK gets shell/FS/edit tools excluded because the CLI provides them natively — and scans tool outputs for injection attacks before returning results to the agent.
- [Tool Category Mapping: Frontend Category IDs to Backend Tool Groups for Permission and Selection](./tool-category-mapping-frontend-category-ids-to-backend-tool-groups-for-permissio.md) — `tool_categories.py` provides two lookup tables that translate the frontend's UI category IDs (e.g., `"google_workspace"`) to the backend's tool group names (e.g., `["group:gmail", "group:calendar"]`), enabling the dashboard's category-level permission checkboxes to correctly gate individual tools without hard-coding the mapping in the UI layer.
- [Tool Policy System: Profile-Based Access Control for Agent Tools](./tool-policy-system-profile-based-access-control-for-agent-tools.md) — The tool policy module implements a layered access control system that determines which tools an agent backend is allowed to use, using named profiles, logical tool groups, and explicit allow/deny lists with a fixed precedence order. It prevents misconfiguration from silently granting full access and supports fine-grained control over MCP server tools using wildcard patterns.
- [Tool Pre-Filtering: Relevance Scoring to Reduce Prompt Bloat](./tool-pre-filtering-relevance-scoring-to-reduce-prompt-bloat.md) — The tool filter module scores each registered tool against the current user message using token overlap, returning only the top-N most relevant tools to avoid overloading the LLM context window. A curated set of essential tools -- memory, search, and pocket management -- are always included regardless of relevance score.
- [Tool Protocol and Base Class: The Contract Every PocketPaw Tool Implements](./tool-protocol-and-base-class-the-contract-every-pocketpaw-tool-implements.md) — The `protocol.py` module defines the structural contract that all PocketPaw tools must satisfy -- a `ToolDefinition` dataclass for LLM schema representation, a `ToolProtocol` for structural subtyping, and a `BaseTool` abstract class providing default implementations and helper methods. Cross-backend schema normalization is handled here to prevent silent failures with strict OpenAI-style validators.
- [Tool Registry: Centralized Execution Hub with Policy Enforcement and Audit Logging](./tool-registry-centralized-execution-hub-with-policy-enforcement-and-audit-loggin.md) — The `ToolRegistry` is PocketPaw's central runtime for tool management -- it stores registered tools, enforces `ToolPolicy` access control, validates parameters, executes tools under a configurable timeout, scrubs sensitive data from logs, and writes to the audit trail. Multiple iterative hardening passes reflect its role as the primary security enforcement point at execution time.
- [Tools CLI Dispatcher: Bash-Accessible Tool Invocation for Agent Loops](./tools-cli-dispatcher-bash-accessible-tool-invocation-for-agent-loops.md) — The `pocketpaw.tools.cli` module is a CLI dispatcher that lets any builtin tool be invoked from a Bash shell via `python -m pocketpaw.tools.cli <tool_name> '<json_args>'`. It serves as the bridge between shell-based agent loops (like Claude Code) and PocketPaw's async Python tool ecosystem.
- [Tools Registry REST Endpoint](./tools-registry-rest-endpoint.md) — Provides a single GET endpoint that aggregates builtin tools, MCP tools, and tool groups into a unified discovery response. Lazy imports and broad exception handling ensure the endpoint returns partial results gracefully when the MCP manager or OAuth token store is not yet initialized.
- [TranslateTool: English–Indian Language Translation via Sarvam AI Mayura](./translatetool-englishindian-language-translation-via-sarvam-ai-mayura.md) — `TranslateTool` integrates Sarvam AI's Mayura translation model to translate text between English and 22 Indian languages, with four register modes from formal to code-mixed. It enforces a 2,000-character input cap and requires a Sarvam API key from settings.
- [UrlExtractTool: SSRF-Safe URL Content Extraction with IP Pinning and Parallel AI Fallback](./urlextracttool-ssrf-safe-url-content-extraction-with-ip-pinning-and-parallel-ai-.md) — `UrlExtractTool` extracts clean text from URLs using Parallel AI's extract API as the primary path and a secure local HTTP fetch as fallback. The local path resolves DNS before connecting and validates the resolved IP against a public-address allowlist, preventing SSRF attacks against private infrastructure.
- [WebSearchTool: Provider-Agnostic Web Search with Tavily, Brave, and Parallel AI](./websearchtool-provider-agnostic-web-search-with-tavily-brave-and-parallel-ai.md) — `WebSearchTool` routes web search queries to Tavily, Brave Search, or Parallel AI based on a settings-configured provider, with all three sharing a common `_format_results()` output shape. The provider selection is declarative — callers never specify the backend — keeping tool descriptions stable across deployment configurations.
- [WebSearchTool Test Suite: Multi-Provider Search Validation](./websearchtool-test-suite-multi-provider-search-validation.md) — This test module validates the `WebSearchTool` built-in tool in PocketPaw, covering provider dispatch, API key guards, HTTP error handling, result clamping, and the parallel-search variant. It ensures the tool behaves safely and predictably across Tavily, Brave, and Parallel AI backends.

### `tracing` — 1 article(s)

- [Decision Trace Collector: Lifecycle, Event Aggregation, Deduplication, and Snapshot Persistence Tests](./decision-trace-collector-lifecycle-event-aggregation-deduplication-and-snapshot-.md) — This suite tests the `TraceCollector` context manager that records which Fabric objects, soul memories, KB articles, and tool calls an agent consulted while generating a decision. It also verifies the `FabricObjectSnapshot` persistence layer that captures point-in-time object state alongside each audit entry.

### `translation` — 1 article(s)

- [TranslateTool: English–Indian Language Translation via Sarvam AI Mayura](./translatetool-englishindian-language-translation-via-sarvam-ai-mayura.md) — `TranslateTool` integrates Sarvam AI's Mayura translation model to translate text between English and 22 Indian languages, with four register modes from formal to code-mixed. It enforces a 2,000-character input cap and requires a Sarvam API key from settings.

### `tree` — 1 article(s)

- [Parallel File Tree Builder with TTL Cache](./parallel-file-tree-builder-with-ttl-cache.md) — Implements the parallel fan-out logic that queries all registered providers' mount lists simultaneously, merges them into a hierarchical `FolderNode` tree, and applies ABAC mount-level filtering. `CachedTreeBuilder` wraps this with a per-user, per-workspace TTL cache to avoid hammering providers on repeated tree requests.

### `type system` — 2 article(s)

- [Agent Protocol: Core Event Types and Legacy Interface](./agent-protocol-core-event-types-and-legacy-interface.md) — Defines the standardized `AgentEvent` dataclass that all agent backends emit, and the `AgentProtocol` structural typing interface that existed before the more capable `AgentBackend` ABC replaced it. Together they form the lingua franca for streaming agent output across PocketPaw's multi-backend architecture.
- [Provider Adapter Base Types: ProviderConfig, ProviderAdapter Protocol, and Model Resolution](./provider-adapter-base-types-providerconfig-provideradapter-protocol-and-model-re.md) — `base.py` defines the shared vocabulary for the entire provider adapter system: `ProviderConfig` (the resolved connection config), `ProviderAdapter` (the structural Protocol every adapter implements), and `resolve_model()` (the multi-tier model name fallback chain). These types are the contract between the LLM client layer and individual provider implementations.

### `ui` — 2 article(s)

- [Desktop Launcher Splash Window: First-Run Progress UI with Threaded Installation](./desktop-launcher-splash-window-first-run-progress-ui-with-threaded-installation.md) — The `SplashWindow` class provides a lightweight tkinter progress window shown during first-run installation. It runs the bootstrap install function in a background thread so the UI remains responsive, and uses `after()` polling to safely update the progress bar from the main thread.
- [Desktop Launcher System Tray Icon: Controls, Auto-Start, Updates, and Notifications](./desktop-launcher-system-tray-icon-controls-auto-start-updates-and-notifications.md) — The `TrayIcon` class provides the persistent system tray presence for the PocketPaw desktop launcher using `pystray`. It exposes a context menu for all runtime operations — starting, stopping, restarting the server, opening the dashboard, managing auto-start, checking for updates, viewing logs, uninstalling, and quitting.

### `unread tracking` — 2 article(s)

- [ReadState Model and UnreadService Upsert Tests](./readstate-model-and-unreadservice-upsert-tests.md) — This module tests the ReadState model's default values and the atomic upsert behavior of UnreadService.mark_read and bump_mention against a real in-memory MongoDB. The tests enforce the unique (user, group) index constraint and verify that concurrent operations remain idempotent.
- [UnreadService List Unreads Calculation Tests](./unreadservice-list-unreads-calculation-tests.md) — This module tests UnreadService.list_unreads, which computes per-group unread message and mention counts for a user. Tests cover the caught-up state, partial reads, the fresh-user fallback, and the edge case where a ReadState row exists but has no recorded read position.

### `update-management` — 1 article(s)

- [Desktop Launcher Updater: PyPI Version Checks and In-Place venv Upgrades](./desktop-launcher-updater-pypi-version-checks-and-in-place-venv-upgrades.md) — The `Updater` class polls PyPI for newer PocketPaw releases and applies updates by running `uv pip install --upgrade` (or falling back to pip) inside the existing venv. Dev mode is detected via a marker file and switches the update path from PyPI to a git branch install.

### `update-system` — 1 article(s)

- [Update Check System: PyPI Version Polling, Caching, Release Notes, and Styled Notices](./update-check-system-pypi-version-polling-caching-release-notes-and-styled-notice.md) — The update check module polls PyPI for new PocketPaw versions, caches results to avoid repeated network calls, fetches release notes from GitHub, tracks which versions the user has already seen, and displays styled terminal notices. Tests cover version parsing, cache freshness/staleness, network error handling, corrupted cache recovery, async wrapping, TTY detection for notice suppression, and the version-seen tracking mechanism.

### `upload management` — 1 article(s)

- [UploadsProvider Tests: Field Mapping, Mounts, RBAC, and Folder Listing](./uploadsprovider-tests-field-mapping-mounts-rbac-and-folder-listing.md) — This module tests the UploadsProvider, which surfaces user file uploads as a virtual file tree with folder hierarchy support. Tests cover field mapping from the raw upload store format to FileEntry, mount resolution, baseline RBAC (owner vs. non-owner vs. admin), and the combined folder+file listing behavior.

### `uploads` — 42 article(s)

- [Download URL Endpoint Tests: TTL Responses, Cross-Workspace Isolation, and Path Traversal Defense](./download-url-endpoint-tests-ttl-responses-cross-workspace-isolation-and-path-tra.md) — Integration tests for the `/uploads/{file_id}/download-url` endpoint added in the EE upload router, verifying that the endpoint returns a time-bounded URL with the original filename, blocks cross-workspace access with 404, rejects path-traversal IDs, and hides soft-deleted files. Uses the same FastAPI test-client pattern as `test_router.py` with header-driven identity injection.
- [EE Upload Resolver Tests: Workspace-Scoped Path Resolution, Isolation, and Soft-Delete Visibility](./ee-upload-resolver-tests-workspace-scoped-path-resolution-isolation-and-soft-del.md) — Tests for `EEUploadResolver` and `resolve_media_paths_scoped`, which translate `/api/v1/uploads/{id}` URLs in message content into local filesystem paths for downstream processing. Key invariants: workspace isolation (cross-workspace lookup returns `None`), non-upload URL pass-through, soft-delete invisibility, and missing-blob safety.
- [EE Upload Router Integration Tests: Roundtrip, Workspace Isolation, Owner-Only Access, and Bulk Partial Success](./ee-upload-router-integration-tests-roundtrip-workspace-isolation-owner-only-acce.md) — Integration tests for the EE uploads FastAPI router covering the full upload-download-delete cycle, cross-workspace 404 enforcement, owner-only read access within a workspace, and the partial-success bulk upload response that distinguishes accepted files from rejected MIME types.
- [EE Upload Service: Workspace-Scoped Pipeline over the OSS Upload Engine](./ee-upload-service-workspace-scoped-pipeline-over-the-oss-upload-engine.md) — EEUploadService wraps the OSS UploadService to add workspace scoping, per-file access control, Mongo metadata persistence, and real-time WebSocket events for file lifecycle. It substitutes a null metadata store at the OSS layer so that all persistence flows through MongoDB rather than the JSONL flat file.
- [EE Uploads API Router: Workspace-Scoped Upload and Folder Endpoints](./ee-uploads-api-router-workspace-scoped-upload-and-folder-endpoints.md) — This FastAPI router exposes all upload and folder management endpoints for the EE cloud layer, enforcing workspace scope and per-file access control on every operation. It manages the full file lifecycle — upload, download URL generation, folder CRUD, file move, and soft delete — backed by EEUploadService and FolderStore.
- [File Uploads Router with Signed URL Access](./file-uploads-router-with-signed-url-access.md) — Implements the OSS single-user file upload system with POST (single and bulk), streaming GET with signed URL authorization, and DELETE endpoints. Uploaded files are stored under `~/.pocketpaw/uploads/` with a JSONL metadata index, and downloads are gated by short-lived HMAC-signed tokens to prevent unauthorized access.
- [Folder CRUD Integration Tests: Auto-Chain Creation, Rename Propagation, and RBAC Enforcement](./folder-crud-integration-tests-auto-chain-creation-rename-propagation-and-rbac-en.md) — Integration tests for the EE uploads folder system covering folder creation with parent requirement, duplicate detection, automatic folder chain creation on upload, cascade rename of descendant paths, safe and cascading deletion, and permission enforcement distinguishing non-admin strangers from workspace admins.
- [JSONL File Store: Append-Only Metadata Persistence for Uploaded Files](./jsonl-file-store-append-only-metadata-persistence-for-uploaded-files.md) — The `JSONLFileStore` provides a lightweight, append-only metadata store for file upload records using newline-delimited JSON, with an in-memory cache for fast lookups and thread-safe writes via a `Lock`. Soft deletes are implemented by appending a delete operation record rather than removing lines, preserving the full audit history of every file lifecycle event.
- [JSONL File Store Tests: Append-Only Metadata Persistence and Resilience](./jsonl-file-store-tests-append-only-metadata-persistence-and-resilience.md) — This module tests `JSONLFileStore`, PocketPaw's append-only JSONL-based file metadata store. It validates save/get round-trips, soft-delete semantics, cold-reload durability, and resilience to corrupt log lines—all critical properties for a store that must survive process restarts without data loss.
- [Local Storage Adapter Tests: Atomic Writes, Streaming, and Error Mapping](./local-storage-adapter-tests-atomic-writes-streaming-and-error-mapping.md) — This module tests `LocalStorageAdapter`, PocketPaw's filesystem-based upload backend. It validates atomic writes (no partial files on stream errors), parent directory creation, chunk concatenation, streaming reads, and correct exception mapping from filesystem errors to typed `UploadError` subclasses.
- [LocalStorageAdapter: Atomic Local-Disk Blob Storage](./localstorageadapter-atomic-local-disk-blob-storage.md) — LocalStorageAdapter provides a local-filesystem implementation of the StorageAdapter protocol, using aiofiles for non-blocking I/O and an atomic write pattern to prevent partial-file corruption. It enforces strict path isolation so that no key—however crafted—can read or write outside the designated root directory.
- [MongoDB Metadata Store for EE Workspace-Scoped Uploads](./mongodb-metadata-store-for-ee-workspace-scoped-uploads.md) — MongoFileStore is the persistence layer for EE upload metadata, extending the OSS file store interface with workspace isolation, folder path tracking, and bulk operations for path rewriting and soft deletion. It bridges the adapter-agnostic `FileRecord` abstraction with the EE `FileUpload` Beanie document.
- [MongoFileStore Unit Tests: Scoped Save/Get, Cross-Workspace Isolation, and Soft Delete](./mongofilestore-unit-tests-scoped-saveget-cross-workspace-isolation-and-soft-dele.md) — Unit tests for `MongoFileStore`'s workspace-scoped CRUD operations, validating that files are stored and retrieved correctly within a workspace, are invisible across workspace boundaries, disappear after soft deletion, and return `None` (not raise) for missing IDs.
- [OSS-to-EE Resolver Fallback Tests: Bridging JSONL and Mongo Upload Stores](./oss-to-ee-resolver-fallback-tests-bridging-jsonl-and-mongo-upload-stores.md) — Tests for the `resolve_media_paths_any` OSS→EE fallback, which handles the hybrid scenario where uploads are stored in the EE Mongo store but chat rendering goes through the OSS JSONL lookup path. Without the fallback, EE-uploaded files would appear as broken links in OSS-rendered chat messages.
- [Path Normalization and Validation for the My Files Folder Tree](./path-normalization-and-validation-for-the-my-files-folder-tree.md) — This module provides a small set of path utilities that enforce a strict, well-defined path format for the uploads folder tree. All paths must be absolute, forward-slash separated, with no trailing slash, no traversal sequences, and no control characters — preventing path injection and ensuring consistent storage key formatting across the uploads subsystem.
- [S3 Storage Adapter Tests: boto3 Call Validation and Exception Mapping](./s3-storage-adapter-tests-boto3-call-validation-and-exception-mapping.md) — This module unit-tests `S3StorageAdapter` by mocking the boto3 client directly, validating that the adapter translates `put`, `open`, and `delete` operations into the correct S3 API calls and maps boto3 exceptions to PocketPaw's typed `UploadError` hierarchy. It skips automatically when boto3 is not installed.
- [S3StorageAdapter: S3-Compatible Cloud Blob Storage](./s3storageadapter-s3-compatible-cloud-blob-storage.md) — S3StorageAdapter implements the StorageAdapter interface against any S3-compatible object store—AWS S3, MinIO, Cloudflare R2, or Wasabi—using the synchronous boto3 library pushed off the event loop via `asyncio.to_thread`. Credentials use the same environment variable names as the companion interacly-backend service, enabling shared-bucket deployments.
- [Storage Adapter Factory: Environment-Driven Backend Selection for the Upload Pipeline](./storage-adapter-factory-environment-driven-backend-selection-for-the-upload-pipe.md) — The `factory.py` module provides a single `build_adapter()` function that reads the `POCKETPAW_UPLOAD_ADAPTER` environment variable and returns either a local disk adapter or an S3-compatible adapter, enabling backend swaps without config file changes. A defensive `load_dotenv()` call at build time ensures environment variables are visible even when the factory runs before the application lifecycle loads `.env`.
- [Storage Key Generation: Namespaced, Date-Bucketed, UUID-Based Keys for Upload Blobs](./storage-key-generation-namespaced-date-bucketed-uuid-based-keys-for-upload-blobs.md) — The `keys.py` module generates opaque storage keys with a predictable three-part structure -- kind, year-month bucket, and a UUID4 hex suffix -- that supports human-readable organization, time-based lifecycle policies, and guaranteed uniqueness. The companion `sanitize_ext()` function normalizes file extensions to prevent path injection through malformed extension strings.
- [Storage Key Generation Tests: Uniqueness, Shape, and Extension Sanitization](./storage-key-generation-tests-uniqueness-shape-and-extension-sanitization.md) — This module tests `pocketpaw.uploads.keys`, which generates structured, collision-resistant storage keys and sanitizes file extensions for safe use in object storage paths. The tests enforce uniqueness, naming conventions, and extension normalization to prevent path traversal and metadata confusion.
- [StorageAdapter Protocol: The Backend-Agnostic Interface for File Persistence](./storageadapter-protocol-the-backend-agnostic-interface-for-file-persistence.md) — The `StorageAdapter` protocol defines the minimal async interface that all storage backends (local disk, S3, GCS) must satisfy, keeping the upload pipeline decoupled from any specific storage technology. The companion `StoredObject` dataclass represents the canonical result of a successful write operation.
- [Unified Files Service: Merging Upload and Drive Sources](./unified-files-service-merging-upload-and-drive-sources.md) — Implements `UnifiedFilesService`, the Cluster E sub-PR 4 facade that merges chat S3 uploads and (stub) Google Drive files into a single flat list for the legacy `GET /files` endpoint. A `_dedupe` function drops duplicates by content identity to prevent the same file appearing twice when sources overlap.
- [Upload Adapter Factory Tests: Environment-Driven Backend Selection](./upload-adapter-factory-tests-environment-driven-backend-selection.md) — This module tests `pocketpaw.uploads.factory.build_adapter`, which selects and constructs the correct storage adapter (local or S3) based on environment variables. It validates default behavior, explicit configuration, error handling for missing S3 credentials, and environment isolation to prevent test contamination from the developer's `.env` file.
- [Upload and Folder Document Models for the EE Storage Layer](./upload-and-folder-document-models-for-the-ee-storage-layer.md) — This module defines the two Beanie document models underpinning the EE uploads subsystem: `FileUpload` for adapter-backed file metadata and `FileFolder` for the virtual folder tree. Both documents are workspace-scoped with compound indexes designed for the access patterns of the uploads and files listing endpoints.
- [Upload Configuration: Size Limits, MIME Allowlist, and Inline Rendering Rules](./upload-configuration-size-limits-mime-allowlist-and-inline-rendering-rules.md) — The `uploads/config.py` module centralizes all static configuration for the upload pipeline -- maximum file size, per-batch file count, allowed MIME types, and local storage root -- while also defining which MIME types are safe for inline browser rendering versus forcing an attachment download. The `extension_for()` helper maps canonical MIME types to file extensions for consistent key generation.
- [Upload Error Hierarchy Tests: Typed Exception Codes and Inheritance](./upload-error-hierarchy-tests-typed-exception-codes-and-inheritance.md) — This module validates the upload error hierarchy in `pocketpaw.uploads.errors`, ensuring all domain-specific exceptions inherit from `UploadError`, carry typed `code` attributes, and preserve error messages. These contracts allow API routes and clients to handle upload failures programmatically without string matching.
- [Upload Error Hierarchy: Typed Exceptions for the Upload Pipeline](./upload-error-hierarchy-typed-exceptions-for-the-upload-pipeline.md) — The `uploads/errors.py` module defines a typed exception hierarchy rooted at `UploadError`, with specific subclasses for each failure mode (file too large, unsupported MIME, empty file, not found, access denied, storage failure). Machine-readable `code` class attributes on each exception type enable API layers to translate exceptions into structured error responses without string matching.
- [Upload Grant Tokens: HMAC-Signed Short-Lived URL Access](./upload-grant-tokens-hmac-signed-short-lived-url-access.md) — The signing module issues and verifies HMAC-SHA256 grant tokens that allow unauthenticated HTTP access to a specific upload for a limited time window. Grants are designed for use in raw `<img src>` and `<a href download>` attributes where an HTTP `Authorization` header cannot be sent.
- [Upload Path Utility Tests: Normalization, Traversal Defense, Subpath Checks, and Join Safety](./upload-path-utility-tests-normalization-traversal-defense-subpath-checks-and-joi.md) — Exhaustive unit tests for the `ee.cloud.uploads.paths` module, which provides the safe path utilities used throughout the upload folder system. Tests cover normalization (canonicalization, dot-stripping, repeated-slash collapse), rejection of path traversal sequences and dangerous characters, subpath relationship checks, and safe join operations.
- [Upload Read Authorization Tests: Owner, Chat Member, Workspace Admin, and Existence-Hiding](./upload-read-authorization-tests-owner-chat-member-workspace-admin-and-existence-.md) — Tests for `EEUploadService._assert_can_read`, the read gate used by `stream`, `presigned_get`, and the `/download-url` and `/grant` endpoints. The parametrized suite verifies that the owner, chat members, and workspace admins can read while strangers receive `NotFound` (not `Forbidden`) — an existence-hiding design that prevents file-ID probing.
- [Upload Resolver Tests: URL Parsing, Media Resolution, and Record Hydration](./upload-resolver-tests-url-parsing-media-resolution-and-record-hydration.md) — This module tests `pocketpaw.uploads.resolver`, which converts upload URLs (like `/api/v1/uploads/{id}`) into hydrated `ResolvedMedia` objects containing the file's storage path, MIME type, and metadata. It validates URL parsing precision, resolver lookup behavior, and the convenience helpers for bulk resolution.
- [Upload Router Tests: Multipart Upload, Streaming Download, Bulk Partial Success, and Delete](./upload-router-tests-multipart-upload-streaming-download-bulk-partial-success-and.md) — This module tests PocketPaw's `/api/v1/uploads` FastAPI router, covering single-file round-trips, bulk partial success (some files fail validation), download with correct Content-Type headers, and soft-delete followed by 404. It validates the full HTTP contract of the upload API.
- [Upload Test Fixtures: Isolated Filesystem Root, In-Memory MongoDB, and MongoFileStore](./upload-test-fixtures-isolated-filesystem-root-in-memory-mongodb-and-mongofilesto.md) — Shared pytest fixtures for the uploads test suite providing a per-test isolated filesystem root, an in-memory MongoDB instance via `mongomock-motor`, and a ready-to-use `MongoFileStore`. The `beanie_upload_db` fixture includes a workaround for a `mongomock-motor` incompatibility with Beanie's `list_collection_names` call.
- [UploadResolver: Bridging Upload URLs to Local Agent Paths](./uploadresolver-bridging-upload-urls-to-local-agent-paths.md) — UploadResolver translates frontend upload URLs (e.g., `/api/v1/uploads/{id}`) into local filesystem paths that the agent loop can inject into Read tool calls or image blocks. It is the OSS-only half of a two-track design, with EE supplying a workspace-scoped variant backed by MongoDB.
- [Uploads Package Init: Namespace Marker for the Upload Subsystem](./uploads-package-init-namespace-marker-for-the-upload-subsystem.md) — The `uploads/__init__.py` is an intentionally empty package initializer that establishes `pocketpaw.uploads` as a Python namespace without importing any submodules at package load time. This lazy-loading design means the upload subsystem's dependencies are only loaded when explicitly imported, keeping base startup time low.
- [Uploads Package Namespace Marker](./uploads-package-namespace-marker.md) — This is an empty `__init__.py` that marks `ee/cloud/uploads/` as a Python package. It contains no code or exports; all uploads functionality is provided by the submodules within this package.
- [Uploads Test Conftest: Isolated Temporary Upload Root Fixture](./uploads-test-conftest-isolated-temporary-upload-root-fixture.md) — This conftest defines `tmp_upload_root`, a pytest fixture that provides an isolated temporary directory for each test in the uploads test suite. It prevents test pollution between file system tests by ensuring every test starts with a clean, empty upload root.
- [Uploads Test Package Initializer](./uploads-test-package-initializer.md) — This is an empty `__init__.py` that marks the `tests/uploads/` directory as a Python package, enabling pytest to discover and import tests within it using standard package-relative imports. No logic lives here; its presence is structural.
- [UploadService: Validation, Storage, and Metadata Persistence for File Uploads](./uploadservice-validation-storage-and-metadata-persistence-for-file-uploads.md) — UploadService is the application-layer coordinator for file uploads—it validates MIME types and sizes, delegates byte storage to the active StorageAdapter, and writes FileRecord metadata to the persistent store. It exposes both single-file and bulk-upload paths, collecting partial failures without aborting the whole batch.
- [UploadsProvider: Personal Upload File Provider with Folder Support](./uploadsprovider-personal-upload-file-provider-with-folder-support.md) — Wraps `MongoFileStore` to expose user-uploaded files and folders as a personal 'My Files' mount in the unified files tree. As the only provider with folder support, it uses a dedicated `folder_store` and renders directory entries with `mime = application/x-directory`.
- [Workspace-Scoped Folder Store for the My Files Mount](./workspace-scoped-folder-store-for-the-my-files-mount.md) — FolderStore is a MongoDB-backed CRUD layer for the virtual folder tree in the uploads provider's My Files feature. It handles path normalization, recursive path creation, bulk path rewrites for renames, and soft delete — all constrained to a single workspace.
- [Workspace-Scoped Upload URL Resolver with Cross-Tenant Isolation](./workspace-scoped-upload-url-resolver-with-cross-tenant-isolation.md) — EEUploadResolver translates opaque upload URLs into local filesystem paths while enforcing workspace isolation — a lookup for a file that exists in a different workspace returns `None`, preventing existence leaks across tenants. It is used by agents and media processors that need to access uploaded file content.

### `usage-tracking` — 1 article(s)

- [UsageTracker: Cached Token Counting Fix and Unbounded Summary Aggregation](./usagetracker-cached-token-counting-fix-and-unbounded-summary-aggregation.md) — These tests document and verify two bug fixes in `UsageTracker`: cached input tokens were silently excluded from `total_tokens`, and `get_summary()` was capped at 10,000 records, causing understated lifetime aggregation totals for high-volume installations. Both fixes ensure accurate cost accounting across all Anthropic and OpenAI backends.

### `user feedback` — 1 article(s)

- [Widget Co-occurrence Accept/Dismiss Endpoint Tests: Route Separation, Validation, and Signature Echo](./widget-co-occurrence-acceptdismiss-endpoint-tests-route-separation-validation-an.md) — Tests the two writer endpoints (`POST /widgets/cooccurrence/accept` and `POST /widgets/cooccurrence/dismiss`) that record user decisions on co-occurring widget suggestions from the SuggestedWidgetsFeed. Pins happy-path response shape, Pydantic validation gates, scope fallback to `["org:*"]`, strict route separation between the two event types, and a signature-echo contract that prevents feed state corruption if the tokenisation rule changes.

### `user management` — 1 article(s)

- [Auth Service - User Profile and Workspace Management](./auth-service-user-profile-and-workspace-management.md) — A stateless FastAPI service class handling the business logic for user authentication, profile management, and active workspace switching. It keeps route handlers thin by acting as the bridge between HTTP requests and the User ODM model.

### `utilities` — 6 article(s)

- [Agent Loop Media Helpers: _format_bytes Unit Tests](./agent-loop-media-helpers-formatbytes-unit-tests.md) — Focused unit tests for the `_format_bytes` helper in `pocketpaw.agents.loop`, verifying that file sizes are formatted correctly across the B, KB, MB, and GB ranges. This utility generates human-readable size strings for media attachment prompts injected into the agent's context.
- [Channel Notifier for Autonomous Outbound Alerts](./channel-notifier-for-autonomous-outbound-alerts.md) — notifier.py provides a `notify()` function that lets any part of PocketPaw push a message to configured notification channels without knowing which adapters are active. It parses `channel:chat_id` target strings, resolves them to `Channel` enum values, and publishes `OutboundMessage` events to the bus.
- [LLMRouter: Lightweight Chat Completion Utility for Non-Agent Callers](./llmrouter-lightweight-chat-completion-utility-for-non-agent-callers.md) — `LLMRouter` is a simple, stateful chat utility for one-off completions — not used by agent backends. It auto-detects the best available backend (Ollama first, then cloud providers), maintains a conversation history list, and provides `clear_history()` for resetting between sessions. Its documented limitations (no streaming, no token counting, unbounded history) make its intended scope explicit.
- [Media Download Utility for Channel Adapters](./media-download-utility-for-channel-adapters.md) — media.py provides a shared `MediaDownloader` singleton and related utilities that all channel adapters use to save incoming media attachments to local disk. It handles collision-free filename generation, file size enforcement, authenticated downloads, and MIME type guessing.
- [Path Normalization and Validation for the My Files Folder Tree](./path-normalization-and-validation-for-the-my-files-folder-tree.md) — This module provides a small set of path utilities that enforce a strict, well-defined path format for the uploads folder tree. All paths must be absolute, forward-slash separated, with no trailing slash, no traversal sequences, and no control characters — preventing path injection and ensuring consistent storage key formatting across the uploads subsystem.
- [UTC Datetime Serialization Helper for MongoDB Timestamps](./utc-datetime-serialization-helper-for-mongodb-timestamps.md) — This module provides `iso_utc`, a one-function utility that fixes a silent timestamp corruption bug caused by MongoDB and Beanie returning naive datetime objects that JavaScript then misinterprets as local time. By re-anchoring naive datetimes to UTC before formatting, it ensures all serialized timestamps carry an unambiguous UTC offset.

### `v2-features` — 1 article(s)

- [Deep Work v2: Retry, Timeout, Cancellation, PawKit Config, and API Tests](./deep-work-v2-retry-timeout-cancellation-pawkit-config-and-api-tests.md) — This suite covers the v2 Deep Work feature additions: Task and TaskSpec retry/timeout fields, the ProjectStatus.CANCELLED state, the PawKit configuration schema (panels, sections, layouts, workflows), YAML round-trips, executor output storage and auto-retry logic, session cancellation, and the cancel/retry REST API endpoints.

### `validation` — 7 article(s)

- [Agent Domain Schema Validation Tests](./agent-domain-schema-validation-tests.md) — This module comprehensively tests the Pydantic schemas for the agents domain — `CreateAgentRequest`, `UpdateAgentRequest`, `DiscoverRequest`, `AgentResponse`, `ScopeAssignmentRequest`, and `ScopeAssignmentResponse` — covering required fields, defaults, length constraints, visibility validation, and scope normalization. Scope-related tests were added in the `feat/cluster-d-agent-scope-picker` feature.
- [Agent Scope Validation and Assignment Rules](./agent-scope-validation-and-assignment-rules.md) — `scope_rules.py` implements the server-side grammar and authorisation rules for hierarchical scope tags — normalising, validating, and deduplicating scope strings, and checking whether an admin's own scope grant covers the scopes they attempt to assign to an agent.
- [Agents Domain Pydantic Schemas](./agents-domain-pydantic-schemas.md) — The agents schemas module defines the Pydantic request and response models for creating, updating, and querying agents, including server-side scope normalisation validators that enforce the same grammar rules as the frontend ScopePicker regardless of how the API is called.
- [Auth Domain Request and Response Schemas](./auth-domain-request-and-response-schemas.md) — The auth schemas module defines the three Pydantic models used by the auth router: a profile update body, a workspace-switch body, and a user response envelope that shapes how user records are serialised for API consumers.
- [Config Validation Tests: API Key Formats, Numeric Constraints, and Enum Fields](./config-validation-tests-api-key-formats-numeric-constraints-and-enum-fields.md) — This test module covers three tiers of Settings validation: the `validate_api_key()` function that checks provider-specific key prefixes and warns on mismatches, Pydantic `gt`/`ge` constraints on numeric fields that prevent nonsensical configurations, and `Literal`-typed enum fields for provider selection. Together they ensure that misconfigured credentials and out-of-range settings fail loudly at startup rather than silently at runtime.
- [Knowledge Base Request Schemas](./knowledge-base-request-schemas.md) — Pydantic request models for the knowledge base REST API, covering search, text ingestion, URL ingestion, and lint operations. These schemas enforce input validation at the FastAPI boundary before any request reaches the kb Go binary.
- [Workspace Pydantic Schemas — Requests and Responses](./workspace-pydantic-schemas-requests-and-responses.md) — This module defines the Pydantic request and response schemas for the workspace domain, enforcing input validation at the API boundary before any business logic runs. The slug validator is the key defensive piece: it rejects slugs that would break URL routing or clash with reserved path segments.

### `vector-database` — 1 article(s)

- [ChromaAdapter: Vector Database Integration for Semantic Memory Storage](./chromaadapter-vector-database-integration-for-semantic-memory-storage.md) — The `ChromaAdapter` wraps ChromaDB to provide async add, search, get-by-ID, delete, and upsert operations for PocketPaw's semantic memory tier. Tests are conditionally skipped when `chromadb` is not installed, and cover the core CRUD operations, graceful handling of small collections during search, upsert (duplicate ID update), and optional metadata storage.

### `vectordb` — 3 article(s)

- [ChromaAdapter: Pluggable Chroma Vector Store with Multi-Provider Embeddings](./chromaadapter-pluggable-chroma-vector-store-with-multi-provider-embeddings.md) — ChromaAdapter wraps ChromaDB's persistent local vector store behind the `VectorStoreProtocol` interface and supports five embedding providers—Chroma's default SentenceTransformer, HuggingFace, OpenAI, Google Gemini, and Voyage AI. Blocking Chroma calls are pushed off the asyncio event loop via `asyncio.to_thread`, keeping the agent loop non-blocking.
- [vectordb Package: VectorStoreProtocol Public Interface](./vectordb-package-vectorstoreprotocol-public-interface.md) — The `vectordb` package's `__init__.py` re-exports `VectorStoreProtocol` as the single public symbol, giving callers a stable import path that is decoupled from the internal module layout. This thin re-export layer prevents callers from depending on the internal `protocol` module path directly and preserves refactoring freedom.
- [VectorStoreProtocol: Runtime-Checkable Vector DB Interface](./vectorstoreprotocol-runtime-checkable-vector-db-interface.md) — VectorStoreProtocol defines the minimal async interface every vector store adapter must satisfy—`add`, `search`, `delete`, and `get_by_id`—using Python's `@runtime_checkable Protocol` so that implementations can be verified via `isinstance` without requiring inheritance. This creates a clean dependency boundary between the agent loop and any specific vector database backend.

### `version compatibility` — 1 article(s)

- [Soul Protocol v0.2.4 Smoke Tests: Reload, Evaluate, Biorhythm, and Fatigue](./soul-protocol-v024-smoke-tests-reload-evaluate-biorhythm-and-fatigue.md) — Smoke tests for soul-protocol v0.2.4 features including hot-reload, the evaluate endpoint, biorhythm config round-trips, dirty tracking for autosave optimization, external change detection, and the fatigue hint in bootstrap context. Designed to catch regressions when upgrading the soul-protocol dependency.

### `versioning` — 2 article(s)

- [API Layer Package: Versioned REST Endpoints for External Clients](./api-layer-package-versioned-rest-endpoints-for-external-clients.md) — The `api` package provides a versioned REST API layer (`/api/v1/`) alongside backward-compatible aliases at `/api/`, designed for external clients such as the Tauri desktop application and automation scripts. It mounts alongside the dashboard routes rather than replacing them, enabling both UI and programmatic access to the same PocketPaw instance.
- [PocketPaw Package Root and Version Declaration](./pocketpaw-package-root-and-version-declaration.md) — The top-level `__init__.py` for the `pocketpaw` package declares the current version string and the package tagline. It is intentionally minimal — no imports, no symbols exported — following the principle that the package root should not trigger side effects at import time.

### `visibility filtering` — 1 article(s)

- [Fabric Scope Policy Engine — Visibility Decisions for Ontology Objects](./fabric-scope-policy-engine-visibility-decisions-for-ontology-objects.md) — This module provides the scope-based visibility engine for Fabric objects, implementing bidirectional containment matching so that a caller with scope `org:sales:*` can see objects tagged `org:sales:leads` and vice versa. It is a local copy of the logic from soul-protocol's `scopes_overlap` function, kept independent to avoid pulling the full engine into minimal runtime slices and to preserve the audit trail of which scope granted access.

### `voice and audio` — 1 article(s)

- [Sarvam AI Integration Tests — Translation, TTS, STT, and OCR](./sarvam-ai-integration-tests-translation-tts-stt-and-ocr.md) — This comprehensive test file covers PocketPaw's integration with Sarvam AI's multilingual APIs: text translation, text-to-speech (TTS), speech-to-text (STT), and vision OCR. Tests validate API key gating, SDK error propagation, audio encoding, file-based I/O, provider routing, policy group membership, config field defaults, and tool registration.

### `web dashboard` — 2 article(s)

- [HMAC Session Tokens: Stateless Dashboard Authentication with Instant Invalidation](./hmac-session-tokens-stateless-dashboard-authentication-with-instant-invalidation.md) — This module implements stateless session tokens using HMAC-SHA256 with a `{expires_unix}:{hex_hmac}` format, keyed against the master access token. Regenerating the master token instantly invalidates all outstanding sessions without requiring a server-side token store or database.
- [Token-Bucket Rate Limiter: Stdlib-Only DoS Protection for the Web Dashboard](./token-bucket-rate-limiter-stdlib-only-dos-protection-for-the-web-dashboard.md) — This module implements an in-memory token-bucket rate limiter using only Python's standard library, providing pre-configured tiers for API endpoints, authentication, WebSocket connections, and per-API-key limiting. The zero-dependency design makes it suitable for embedding directly in the dashboard without adding external Redis or other state store requirements.

### `web search` — 2 article(s)

- [ResearchTool Test Suite — Web Search, URL Extraction, and Happy Path](./researchtool-test-suite-web-search-url-extraction-and-happy-path.md) — This test file validates `ResearchTool`, PocketPaw's built-in agentic research capability that chains web search, URL content extraction, and LLM summarization. Tests cover tool metadata, URL parsing edge cases, search failure handling, and the full happy-path pipeline using mocked external dependencies.
- [WebSearchTool Test Suite: Multi-Provider Search Validation](./websearchtool-test-suite-multi-provider-search-validation.md) — This test module validates the `WebSearchTool` built-in tool in PocketPaw, covering provider dispatch, API key guards, HTTP error handling, result clamping, and the parallel-search variant. It ensures the tool behaves safely and predictably across Tavily, Brave, and Parallel AI backends.

### `web server` — 3 article(s)

- [Web Server: QR Code Pairing Flow for Telegram Bot Onboarding](./web-server-qr-code-pairing-flow-for-telegram-bot-onboarding.md) — The web server module drives PocketPaw's QR code–based Telegram pairing flow: it generates a signed deep link, serves a minimal HTML page with a scannable QR code, and simultaneously listens for the bot's `/start <secret>` confirmation over the Telegram API. Dynamic port allocation ensures the local server starts cleanly even when the default port is busy.
- [Web Server Security Tests: Bot Token Redaction in /setup Errors](./web-server-security-tests-bot-token-redaction-in-setup-errors.md) — This module tests that the `/setup` endpoint in PocketPaw's web server never leaks the Telegram bot token in HTTP error responses (F-07, issue #445). It verifies that when the upstream Telegram API returns an error whose message embeds the token URL, the response is redacted before reaching the client.
- [WhatsApp Gateway: Standalone Webhook Server for WhatsApp Bot](./whatsapp-gateway-standalone-webhook-server-for-whatsapp-bot.md) — The WhatsApp gateway module creates a minimal FastAPI application with Meta webhook verification and message-delivery endpoints, then runs it under uvicorn as a standalone service. It delegates all WhatsApp message handling to `WhatsAppAdapter`, keeping the gateway thin and the adapter reusable across deployment modes.

### `web-fetching` — 1 article(s)

- [UrlExtractTool: SSRF-Safe URL Content Extraction with IP Pinning and Parallel AI Fallback](./urlextracttool-ssrf-safe-url-content-extraction-with-ip-pinning-and-parallel-ai-.md) — `UrlExtractTool` extracts clean text from URLs using Parallel AI's extract API as the primary path and a secure local HTTP fetch as fallback. The local path resolves DNS before connecting and validates the resolved IP against a public-address allowlist, preventing SSRF attacks against private infrastructure.

### `web-search` — 1 article(s)

- [WebSearchTool: Provider-Agnostic Web Search with Tavily, Brave, and Parallel AI](./websearchtool-provider-agnostic-web-search-with-tavily-brave-and-parallel-ai.md) — `WebSearchTool` routes web search queries to Tavily, Brave Search, or Parallel AI based on a settings-configured provider, with all three sharing a common `_format_results()` output shape. The provider selection is declarative — callers never specify the backend — keeping tool descriptions stable across deployment configurations.

### `webhook` — 3 article(s)

- [Generic Inbound Webhook Adapter with Sync and Async Modes](./generic-inbound-webhook-adapter-with-sync-and-async-modes.md) — WebhookAdapter allows any external service — GitHub, Zapier, n8n, Home Assistant, cron scripts — to push events into PocketPaw via HTTP POST. It supports both fire-and-forget (async) mode and a synchronous request-response mode where the HTTP call blocks until the agent produces a reply.
- [Webhook Adapter Test Suite: Inbound Payload Handling, Sync Mode, and Send Routing](./webhook-adapter-test-suite-inbound-payload-handling-sync-mode-and-send-routing.md) — This module tests the `WebhookAdapter`, PocketPaw's generic inbound HTTP webhook channel. It covers slot configuration, payload normalization, sync vs. async dispatch modes, future-based response resolution, and streaming accumulation.
- [Webhook Routes Test Suite: Dashboard Auth, HMAC Validation, and Slot Management](./webhook-routes-test-suite-dashboard-auth-hmac-validation-and-slot-management.md) — This module tests the webhook dashboard routes in PocketPaw, covering Bearer token authentication, HMAC-based inbound secret validation, slot CRUD via the dashboard API, and async dispatch through the webhook adapter. It ensures that unauthenticated requests are rejected and that slot configuration persists correctly.

### `webhooks` — 3 article(s)

- [Webhook Slot Management Router](./webhook-slot-management-router.md) — Manages the lifecycle of PocketPaw's inbound webhook slots — creating named endpoints, listing them with dynamically constructed URLs, removing them, and rotating their HMAC secrets. All operations require the `admin` scope, reflecting that webhook configuration controls which external systems can trigger agent workflows.
- [Webhook Slot Schema Contracts](./webhook-slot-schema-contracts.md) — Defines the Pydantic models that represent a webhook slot's configuration and the request payloads for creating or identifying webhooks. The name pattern constraint prevents injection-prone characters from reaching URL-based routing.
- [Webhooks API Tests — CRUD, Secret Redaction, Duplicate Prevention, and Secret Rotation](./webhooks-api-tests-crud-secret-redaction-duplicate-prevention-and-secret-rotatio.md) — This test module covers the full lifecycle of PocketPaw's webhook configuration API: listing (with secret redaction), adding (with name validation and duplicate prevention), removing, and regenerating shared secrets. Webhooks enable external services to trigger agent actions, and the tests enforce that secrets are never exposed at rest through the API listing endpoint.

### `websocket` — 2 article(s)

- [WebSocket Channel Adapter for the PocketPaw Dashboard](./websocket-channel-adapter-for-the-pocketpaw-dashboard.md) — WebSocketAdapter manages real-time bidirectional communication between PocketPaw's FastAPI backend and its web dashboard frontend, routing messages, streaming chunks, and system events (tool execution, thinking indicators) to the correct connected browser session.
- [WebSocket Persistence Diagnostic: Simulating the Chat Message Save Path with Full Debug Logging](./websocket-persistence-diagnostic-simulating-the-chat-message-save-path-with-full.md) — This script simulates the exact code path the WebSocket adapter executes when saving a chat message, but with DEBUG-level logging and no exception swallowing. It was created to surface silent persistence failures that were invisible in normal operation because the WebSocket handler caught and discarded exceptions.

### `whatsapp` — 3 article(s)

- [WhatsApp Business Cloud API Adapter](./whatsapp-business-cloud-api-adapter.md) — WhatsAppAdapter connects PocketPaw to the official Meta WhatsApp Business Cloud API, receiving messages via webhook callbacks verified with HMAC and sending replies via the Graph API. It handles the two-step media download flow and sends automatic read receipts.
- [WhatsApp Gateway: Standalone Webhook Server for WhatsApp Bot](./whatsapp-gateway-standalone-webhook-server-for-whatsapp-bot.md) — The WhatsApp gateway module creates a minimal FastAPI application with Meta webhook verification and message-delivery endpoints, then runs it under uvicorn as a standalone service. It delegates all WhatsApp message handling to `WhatsAppAdapter`, keeping the gateway thin and the adapter reusable across deployment modes.
- [WhatsApp Personal Mode Adapter via Neonize](./whatsapp-personal-mode-adapter-via-neonize.md) — NeonizeAdapter connects PocketPaw to WhatsApp using the neonize Python library, which wraps the open-source whatsmeow Go client. It requires only a QR-code scan to pair — no Meta Developer account or public webhook URL is needed.

### `widget layer` — 1 article(s)

- [Paw Print Widget Layer — Package Entry Point](./paw-print-widget-layer-package-entry-point.md) — The `ee.paw_print` package provides the backend for customer-facing embedded widgets that close the decision loop: customer interactions on a rendered widget flow into a Pocket in real time, Instinct nudges the owner, and approved actions feed back to the widget. This module re-exports every public symbol from the two sub-modules.

### `widget system` — 3 article(s)

- [Widget Co-occurrence Accept/Dismiss Endpoint Tests: Route Separation, Validation, and Signature Echo](./widget-co-occurrence-acceptdismiss-endpoint-tests-route-separation-validation-an.md) — Tests the two writer endpoints (`POST /widgets/cooccurrence/accept` and `POST /widgets/cooccurrence/dismiss`) that record user decisions on co-occurring widget suggestions from the SuggestedWidgetsFeed. Pins happy-path response shape, Pydantic validation gates, scope fallback to `["org:*"]`, strict route separation between the two event types, and a signature-echo contract that prevents feed state corruption if the tokenisation rule changes.
- [Widget Journal Projection Tests: Interactions, Graduation, Co-occurrence Signature Fix, and Router](./widget-journal-projection-tests-interactions-graduation-co-occurrence-signature-.md) — Comprehensive tests for the widget journal projection subsystem covering the write path for interactions, graduations, and co-occurrence events; scope containment in usage roll-ups; the pin-threshold graduation policy; a regression guard for the `sorted(tokens[:6])` co-occurrence signature bug from PR #942; archive rules for inactive widgets; and the three GET router endpoints.
- [Widget Track Endpoint Tests: Interaction Recording, Validation, Scope Fallback, and Projection Integration](./widget-track-endpoint-tests-interaction-recording-validation-scope-fallback-and-.md) — Tests for `POST /widgets/track`, the writer endpoint that records widget interactions from the SuggestedWidgetsFeed UI. Covers the full happy-path event emission, Pydantic validation of required fields, metadata defaulting, correlation-id propagation, sequential seq increments, the `org:*` scope fallback for anonymous actors, and an end-to-end proof that three POSTs reflect correctly in the `GET /widgets/usage` projection.

### `widget-system` — 6 article(s)

- [FastAPI Router for Widget Interactions, Graduation, and Co-occurrence](./fastapi-router-for-widget-interactions-graduation-and-co-occurrence.md) — Exposes the widget journal projection through REST endpoints for reading widget usage stats, graduation state, and co-occurrence suggestions, plus write endpoints for tracking interactions and recording accept/dismiss decisions on suggested widget pairings. The POST /widgets/track endpoint closes a silent data-loss bug where every dashboard interaction 404'd before this change.
- [In-Memory Projection for Widget Interactions, Usage, and Co-occurrence](./in-memory-projection-for-widget-interactions-usage-and-co-occurrence.md) — Provides three unified read views over the widget event stream — per-interaction history, per-widget usage roll-up for graduation decisions, and per-pair co-occurrence counts for suggestion candidates — all rebuilt from a single org journal replay pass. Supersedes the JSONL-based scans from PRs #941 and #942.
- [Journal-Backed Write Store for Widget Events](./journal-backed-write-store-for-widget-events.md) — Provides the write facade for the widget domain, emitting all five widget action types onto the org journal and folding each emission into the shared in-memory projection for immediate read consistency. Adds a seq-returning variant of the interaction writer to support the POST /widgets/track endpoint's cursor ack requirement.
- [Widget Event Payload Definitions and Co-occurrence Signature Utilities](./widget-event-payload-definitions-and-co-occurrence-signature-utilities.md) — Defines the five canonical action names for the widget journal projection domain, their payload builders, and the stable co-occurrence signature algorithm that fixes a token-sort ordering bug shipped in PR #942. All constants are pinned at the module level so the projection, policy, store, and tests share a single import path.
- [Widget Graduation and Co-occurrence Policy Engine](./widget-graduation-and-co-occurrence-policy-engine.md) — Implements the pin/fade/archive graduation logic for individual widgets and the threshold-based co-occurrence candidate scanner, both ported verbatim from PRs #941 and #942 onto the journal-backed projection. Decision objects and tuning knobs are preserved under their original names so existing tests and configuration carry over without renaming.
- [Widget Journal Projection Package — Public API](./widget-journal-projection-package-public-api.md) — Re-exports the complete public surface of the widget journal projection domain, including the store, projection classes, policy functions, and all canonical event names and payload builders. This module also documents why the package supersedes held PRs #941 and #942 and ships the fix for a token-sort bug that broke co-occurrence deduplication.

### `workspace` — 6 article(s)

- [Smoke Test: DM and Group Chat Persistence for Human Users](./smoke-test-dm-and-group-chat-persistence-for-human-users.md) — This end-to-end test verifies that DM creation between two workspace members, message exchange, and group chat all persist correctly in MongoDB — without any agent involvement. It validates the full social messaging layer: workspace invites, DM groups, populated member objects, message context types, and cross-member read access.
- [Workspace FastAPI Router — CRUD, Members, and Invites](./workspace-fastapi-router-crud-members-and-invites.md) — This module defines all HTTP endpoints for the workspace domain — workspace lifecycle, member management, and invite flows — using FastAPI's dependency injection to enforce authorization at the route layer while keeping service methods auth-agnostic. Authorization gates are declared inline as `Depends(require_action(...))` so the permission model is readable directly from the route signature.
- [Workspace Module Entry Point](./workspace-module-entry-point.md) — The `ee/cloud/workspace/__init__.py` file serves as the public entry point for the workspace domain, re-exporting the FastAPI router so that the top-level application assembler can mount it without knowing internal package structure. This single-line pattern deliberately decouples how the router is imported from where it physically lives.
- [Workspace Pydantic Schemas — Requests and Responses](./workspace-pydantic-schemas-requests-and-responses.md) — This module defines the Pydantic request and response schemas for the workspace domain, enforcing input validation at the API boundary before any business logic runs. The slug validator is the key defensive piece: it rejects slugs that would break URL routing or clash with reserved path segments.
- [Workspace Schema Tests: Slug Validation, Invite Rules, and Member Role Contracts](./workspace-schema-tests-slug-validation-invite-rules-and-member-role-contracts.md) — Tests for the workspace domain Pydantic schemas in PocketPaw's EE layer, covering slug format enforcement, invite role validation, and member role update rules. The slug tests are particularly important because workspace slugs appear in URLs and must be lowercase, hyphen-separated, and free of leading/trailing hyphens.
- [Workspace Service — Business Logic for Workspaces, Members, and Invites](./workspace-service-business-logic-for-workspaces-members-and-invites.md) — The `WorkspaceService` class is a stateless async service that encapsulates all business logic for the workspace domain, including workspace lifecycle, member management, invite flow, and realtime event emission. It enforces several data-level invariants (slug uniqueness, seat limits, owner demotion guard) that cannot be expressed as route-level permission checks.

### `workspace isolation` — 2 article(s)

- [EE Upload Resolver Tests: Workspace-Scoped Path Resolution, Isolation, and Soft-Delete Visibility](./ee-upload-resolver-tests-workspace-scoped-path-resolution-isolation-and-soft-del.md) — Tests for `EEUploadResolver` and `resolve_media_paths_scoped`, which translate `/api/v1/uploads/{id}` URLs in message content into local filesystem paths for downstream processing. Key invariants: workspace isolation (cross-workspace lookup returns `None`), non-upload URL pass-through, soft-delete invisibility, and missing-blob safety.
- [Pocket Layout Routes: Export, Template Create/List, and Workspace Scoping Tests](./pocket-layout-routes-export-template-createlist-and-workspace-scoping-tests.md) — This integration test suite covers three new routes on the pockets router that implement the layout save/share surface: `POST /pockets/{id}/export-layout` (YAML serialization), `POST /pockets/templates` (template creation), and `GET /pockets/templates` (workspace-scoped listing). It verifies the round-trip fidelity, metadata override support, cross-workspace isolation, and malformed YAML handling.

### `workspace management` — 1 article(s)

- [WorkspaceService Realtime Event Emission and Cache Invalidation Tests](./workspaceservice-realtime-event-emission-and-cache-invalidation-tests.md) — This suite verifies that every mutating `WorkspaceService` method fires the correct typed event through `emit()`, and that membership or admin list changes also invalidate the `AudienceResolver` workspace cache. All database and permission primitives are patched at their seams, isolating the emit behavior from persistence concerns.

### `workspace security` — 1 article(s)

- [EEUploadService Core Behavior: Workspace-Scoped Storage and Streaming Tests](./eeuploadservice-core-behavior-workspace-scoped-storage-and-streaming-tests.md) — This test suite verifies that EEUploadService correctly stores uploaded files under a workspace scope, enforces cross-workspace access boundaries, streams content back to authorized requesters, and handles bulk uploads atomically. The in-memory `_MemAdapter` stands in for real cloud storage so tests run without external dependencies.

## Top Concepts (top 50 of 5826)

| Concept | Referenced by |
|---|---:|
| httpx | 31 |
| BaseTool | 30 |
| Pydantic | 24 |
| FastAPI router | 23 |
| soft delete | 21 |
| lazy import | 20 |
| singleton pattern | 20 |
| MessageBus | 19 |
| singleton | 19 |
| trust level | 19 |
| Beanie | 18 |
| FastAPI | 18 |
| __init__.py | 18 |
| rate limiting | 18 |
| OAuth | 17 |
| backward compatibility | 17 |
| Beanie ODM | 16 |
| MemoryManager | 16 |
| MongoMemoryStore | 16 |
| FileMemoryStore | 15 |
| path traversal | 15 |
| AgentLoop | 13 |
| Ollama | 13 |
| Settings | 13 |
| access control | 13 |
| smoke test | 13 |
| subprocess | 13 |
| Pydantic validation | 12 |
| WebSocket | 12 |
| audit log | 12 |
| session management | 12 |
| trust_level | 12 |
| ABAC | 11 |
| AgentEvent | 11 |
| BaseChannelAdapter | 11 |
| FileEntry | 11 |
| Protocol | 11 |
| TestClient | 11 |
| audit logging | 11 |
| emit | 11 |
| file permissions | 11 |
| idempotency | 11 |
| message bus | 11 |
| workspace scoping | 11 |
| AgentRouter | 10 |
| CRUD | 10 |
| Discord | 10 |
| InboundMessage | 10 |
| NotFound | 10 |
| Python package | 10 |

