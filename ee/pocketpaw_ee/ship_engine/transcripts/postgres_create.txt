# transcript: `dokku postgres:create demo-db` — dokku-postgres prints the full
# DSN including the generated password; the driver must never let it reach logs
# or DTOs.
EXIT: 0
--- stdout ---
       Waiting for container to be ready
=====> Postgres container created: demo-db
=====> demo-db postgres service information
       Dsn:                 postgres://postgres:s3cr3tpass8f2a@dokku-postgres-demo-db:5432/demo_db
       Status:              running
       Version:             postgres:16.4
--- stderr ---
