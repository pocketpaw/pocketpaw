"""Daytona integration — VM/container workspaces for cloud projects.

Provides a client wrapper around the Daytona SDK, config module for
reading env-based configuration, and pre-built image definitions that
include common development tools (Python, Node.js, GCC, Docker, UV) so
every sandbox is ready to run code without manual setup.

Moved from OSS to EE: 2026-06-24
"""
