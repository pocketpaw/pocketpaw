# Game worlds (the /game creation surface) — the cloud-side glue that
# composes a described "living world" into a Pocket type="game". This
# package is a sibling of `cloud/` (not nested under it), mirroring the
# `sites/` package layout, and is intentionally thin: the deterministic
# create service (service.py) with the v0 vibe→dials preset table and the
# shared plan gate. Created: 2026-07-02 (feat/game-surface, PW-2).
