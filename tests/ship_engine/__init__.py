# tests/ship_engine/__init__.py — package marker for the /ship deploy-engine
# contract tests (SHIP-1), so `tests.ship_engine.contract` is importable by
# the conftest wiring and future driver suites.
#
# Created 2026-07-21 (feat/ship-1-engine-contract): new package.
